import {
  __spreadProps,
  __spreadValues
} from "./chunk-4AJYGB4N.js";

// node_modules/@angular/core/fesm2022/_effect-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var activeConsumer = null;
var inNotificationPhase = false;
var epoch = 1;
var postProducerCreatedFn = null;
var SIGNAL = /* @__PURE__ */ Symbol("SIGNAL");
function setActiveConsumer(consumer) {
  const prev = activeConsumer;
  activeConsumer = consumer;
  return prev;
}
function getActiveConsumer() {
  return activeConsumer;
}
function isInNotificationPhase() {
  return inNotificationPhase;
}
var REACTIVE_NODE = {
  version: 0,
  lastCleanEpoch: 0,
  dirty: false,
  producers: void 0,
  producersTail: void 0,
  consumers: void 0,
  consumersTail: void 0,
  recomputing: false,
  consumerAllowSignalWrites: false,
  consumerIsAlwaysLive: false,
  kind: "unknown",
  producerMustRecompute: () => false,
  producerRecomputeValue: () => {
  },
  consumerMarkedDirty: () => {
  },
  consumerOnSignalRead: () => {
  }
};
function producerAccessed(node) {
  if (inNotificationPhase) {
    throw new Error(typeof ngDevMode !== "undefined" && ngDevMode ? `Assertion error: signal read during notification phase` : "");
  }
  if (activeConsumer === null) {
    return;
  }
  activeConsumer.consumerOnSignalRead(node);
  const prevProducerLink = activeConsumer.producersTail;
  if (prevProducerLink !== void 0 && prevProducerLink.producer === node) {
    return;
  }
  let nextProducerLink = void 0;
  const isRecomputing = activeConsumer.recomputing;
  if (isRecomputing) {
    nextProducerLink = prevProducerLink !== void 0 ? prevProducerLink.nextProducer : activeConsumer.producers;
    if (nextProducerLink !== void 0 && nextProducerLink.producer === node) {
      activeConsumer.producersTail = nextProducerLink;
      nextProducerLink.lastReadVersion = node.version;
      return;
    }
  }
  const prevConsumerLink = node.consumersTail;
  if (prevConsumerLink !== void 0 && prevConsumerLink.consumer === activeConsumer && (!isRecomputing || isValidLink(prevConsumerLink, activeConsumer))) {
    return;
  }
  const isLive = consumerIsLive(activeConsumer);
  const newLink = {
    producer: node,
    consumer: activeConsumer,
    nextProducer: nextProducerLink,
    prevConsumer: prevConsumerLink,
    lastReadVersion: node.version,
    nextConsumer: void 0
  };
  activeConsumer.producersTail = newLink;
  if (prevProducerLink !== void 0) {
    prevProducerLink.nextProducer = newLink;
  } else {
    activeConsumer.producers = newLink;
  }
  if (isLive) {
    producerAddLiveConsumer(node, newLink);
  }
}
function producerIncrementEpoch() {
  epoch++;
}
function producerUpdateValueVersion(node) {
  if (consumerIsLive(node) && !node.dirty) {
    return;
  }
  if (!node.dirty && node.lastCleanEpoch === epoch) {
    return;
  }
  if (!node.producerMustRecompute(node) && !consumerPollProducersForChange(node)) {
    producerMarkClean(node);
    return;
  }
  node.producerRecomputeValue(node);
  producerMarkClean(node);
}
function producerNotifyConsumers(node) {
  if (node.consumers === void 0) {
    return;
  }
  const prev = inNotificationPhase;
  inNotificationPhase = true;
  try {
    for (let link = node.consumers; link !== void 0; link = link.nextConsumer) {
      const consumer = link.consumer;
      if (!consumer.dirty) {
        consumerMarkDirty(consumer);
      }
    }
  } finally {
    inNotificationPhase = prev;
  }
}
function producerUpdatesAllowed() {
  return activeConsumer?.consumerAllowSignalWrites !== false;
}
function consumerMarkDirty(node) {
  node.dirty = true;
  producerNotifyConsumers(node);
  node.consumerMarkedDirty?.(node);
}
function producerMarkClean(node) {
  node.dirty = false;
  node.lastCleanEpoch = epoch;
}
function consumerBeforeComputation(node) {
  if (node) resetConsumerBeforeComputation(node);
  return setActiveConsumer(node);
}
function resetConsumerBeforeComputation(node) {
  node.producersTail = void 0;
  node.recomputing = true;
}
function consumerAfterComputation(node, prevConsumer) {
  setActiveConsumer(prevConsumer);
  if (node) finalizeConsumerAfterComputation(node);
}
function finalizeConsumerAfterComputation(node) {
  node.recomputing = false;
  const producersTail = node.producersTail;
  let toRemove = producersTail !== void 0 ? producersTail.nextProducer : node.producers;
  if (toRemove !== void 0) {
    if (consumerIsLive(node)) {
      do {
        toRemove = producerRemoveLiveConsumerLink(toRemove);
      } while (toRemove !== void 0);
    }
    if (producersTail !== void 0) {
      producersTail.nextProducer = void 0;
    } else {
      node.producers = void 0;
    }
  }
}
function consumerPollProducersForChange(node) {
  for (let link = node.producers; link !== void 0; link = link.nextProducer) {
    const producer = link.producer;
    const seenVersion = link.lastReadVersion;
    if (seenVersion !== producer.version) {
      return true;
    }
    producerUpdateValueVersion(producer);
    if (seenVersion !== producer.version) {
      return true;
    }
  }
  return false;
}
function consumerDestroy(node) {
  if (consumerIsLive(node)) {
    let link = node.producers;
    while (link !== void 0) {
      link = producerRemoveLiveConsumerLink(link);
    }
  }
  node.producers = void 0;
  node.producersTail = void 0;
  node.consumers = void 0;
  node.consumersTail = void 0;
}
function producerAddLiveConsumer(node, link) {
  const consumersTail = node.consumersTail;
  const wasLive = consumerIsLive(node);
  if (consumersTail !== void 0) {
    link.nextConsumer = consumersTail.nextConsumer;
    consumersTail.nextConsumer = link;
  } else {
    link.nextConsumer = void 0;
    node.consumers = link;
  }
  link.prevConsumer = consumersTail;
  node.consumersTail = link;
  if (!wasLive) {
    for (let link2 = node.producers; link2 !== void 0; link2 = link2.nextProducer) {
      producerAddLiveConsumer(link2.producer, link2);
    }
  }
}
function producerRemoveLiveConsumerLink(link) {
  const producer = link.producer;
  const nextProducer = link.nextProducer;
  const nextConsumer = link.nextConsumer;
  const prevConsumer = link.prevConsumer;
  link.nextConsumer = void 0;
  link.prevConsumer = void 0;
  if (nextConsumer !== void 0) {
    nextConsumer.prevConsumer = prevConsumer;
  } else {
    producer.consumersTail = prevConsumer;
  }
  if (prevConsumer !== void 0) {
    prevConsumer.nextConsumer = nextConsumer;
  } else {
    producer.consumers = nextConsumer;
    if (!consumerIsLive(producer)) {
      let producerLink = producer.producers;
      while (producerLink !== void 0) {
        producerLink = producerRemoveLiveConsumerLink(producerLink);
      }
    }
  }
  return nextProducer;
}
function consumerIsLive(node) {
  return node.consumerIsAlwaysLive || node.consumers !== void 0;
}
function runPostProducerCreatedFn(node) {
  postProducerCreatedFn?.(node);
}
function isValidLink(checkLink, consumer) {
  const producersTail = consumer.producersTail;
  if (producersTail !== void 0) {
    let link = consumer.producers;
    do {
      if (link === checkLink) {
        return true;
      }
      if (link === producersTail) {
        break;
      }
      link = link.nextProducer;
    } while (link !== void 0);
  }
  return false;
}
function defaultEquals(a, b) {
  return Object.is(a, b);
}
function createComputed(computation, equal) {
  const node = Object.create(COMPUTED_NODE);
  node.computation = computation;
  if (equal !== void 0) {
    node.equal = equal;
  }
  const computed2 = () => {
    producerUpdateValueVersion(node);
    producerAccessed(node);
    if (node.value === ERRORED) {
      throw node.error;
    }
    return node.value;
  };
  computed2[SIGNAL] = node;
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    computed2.toString = () => `[Computed${node.debugName ? " (" + node.debugName + ")" : ""}: ${String(node.value)}]`;
  }
  runPostProducerCreatedFn(node);
  return computed2;
}
var UNSET = /* @__PURE__ */ Symbol("UNSET");
var COMPUTING = /* @__PURE__ */ Symbol("COMPUTING");
var ERRORED = /* @__PURE__ */ Symbol("ERRORED");
var COMPUTED_NODE = /* @__PURE__ */ (() => {
  return __spreadProps(__spreadValues({}, REACTIVE_NODE), {
    value: UNSET,
    dirty: true,
    error: null,
    equal: defaultEquals,
    kind: "computed",
    producerMustRecompute(node) {
      return node.value === UNSET || node.value === COMPUTING;
    },
    producerRecomputeValue(node) {
      if (node.value === COMPUTING) {
        throw new Error(typeof ngDevMode !== "undefined" && ngDevMode ? "Detected cycle in computations." : "");
      }
      const oldValue = node.value;
      node.value = COMPUTING;
      const prevConsumer = consumerBeforeComputation(node);
      let newValue;
      let wasEqual = false;
      try {
        newValue = node.computation();
        setActiveConsumer(null);
        wasEqual = oldValue !== UNSET && oldValue !== ERRORED && newValue !== ERRORED && node.equal(oldValue, newValue);
      } catch (err) {
        newValue = ERRORED;
        node.error = err;
      } finally {
        consumerAfterComputation(node, prevConsumer);
      }
      if (wasEqual) {
        node.value = oldValue;
        return;
      }
      node.value = newValue;
      node.version++;
    }
  });
})();
function defaultThrowError() {
  throw new Error();
}
var throwInvalidWriteToSignalErrorFn = defaultThrowError;
function throwInvalidWriteToSignalError(node) {
  throwInvalidWriteToSignalErrorFn(node);
}
function setThrowInvalidWriteToSignalError(fn) {
  throwInvalidWriteToSignalErrorFn = fn;
}
var postSignalSetFn = null;
function createSignal(initialValue, equal) {
  const node = Object.create(SIGNAL_NODE);
  node.value = initialValue;
  if (equal !== void 0) {
    node.equal = equal;
  }
  const getter = () => signalGetFn(node);
  getter[SIGNAL] = node;
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    getter.toString = () => `[Signal${node.debugName ? " (" + node.debugName + ")" : ""}: ${String(node.value)}]`;
  }
  runPostProducerCreatedFn(node);
  const set = (newValue) => signalSetFn(node, newValue);
  const update = (updateFn) => signalUpdateFn(node, updateFn);
  return [getter, set, update];
}
function signalGetFn(node) {
  producerAccessed(node);
  return node.value;
}
function signalSetFn(node, newValue) {
  if (!producerUpdatesAllowed()) {
    throwInvalidWriteToSignalError(node);
  }
  if (!node.equal(node.value, newValue)) {
    node.value = newValue;
    signalValueChanged(node);
  }
}
function signalUpdateFn(node, updater) {
  if (!producerUpdatesAllowed()) {
    throwInvalidWriteToSignalError(node);
  }
  signalSetFn(node, updater(node.value));
}
var SIGNAL_NODE = /* @__PURE__ */ (() => {
  return __spreadProps(__spreadValues({}, REACTIVE_NODE), {
    equal: defaultEquals,
    value: void 0,
    kind: "signal"
  });
})();
function signalValueChanged(node) {
  node.version++;
  producerIncrementEpoch();
  producerNotifyConsumers(node);
  postSignalSetFn?.(node);
}
var BASE_EFFECT_NODE = /* @__PURE__ */ (() => __spreadProps(__spreadValues({}, REACTIVE_NODE), {
  consumerIsAlwaysLive: true,
  consumerAllowSignalWrites: true,
  dirty: true,
  kind: "effect"
}))();
function runEffect(node) {
  node.dirty = false;
  if (node.version > 0 && !consumerPollProducersForChange(node)) {
    return;
  }
  node.version++;
  const prevNode = consumerBeforeComputation(node);
  try {
    node.cleanup();
    node.fn();
  } finally {
    consumerAfterComputation(node, prevNode);
  }
}

// node_modules/rxjs/dist/esm/internal/util/isFunction.js
function isFunction(value) {
  return typeof value === "function";
}

// node_modules/rxjs/dist/esm/internal/util/createErrorClass.js
function createErrorClass(createImpl) {
  const _super = (instance) => {
    Error.call(instance);
    instance.stack = new Error().stack;
  };
  const ctorFunc = createImpl(_super);
  ctorFunc.prototype = Object.create(Error.prototype);
  ctorFunc.prototype.constructor = ctorFunc;
  return ctorFunc;
}

// node_modules/rxjs/dist/esm/internal/util/UnsubscriptionError.js
var UnsubscriptionError = createErrorClass((_super) => function UnsubscriptionErrorImpl(errors) {
  _super(this);
  this.message = errors ? `${errors.length} errors occurred during unsubscription:
${errors.map((err, i) => `${i + 1}) ${err.toString()}`).join("\n  ")}` : "";
  this.name = "UnsubscriptionError";
  this.errors = errors;
});

// node_modules/rxjs/dist/esm/internal/util/arrRemove.js
function arrRemove(arr, item) {
  if (arr) {
    const index = arr.indexOf(item);
    0 <= index && arr.splice(index, 1);
  }
}

// node_modules/rxjs/dist/esm/internal/Subscription.js
var Subscription = class _Subscription {
  constructor(initialTeardown) {
    this.initialTeardown = initialTeardown;
    this.closed = false;
    this._parentage = null;
    this._finalizers = null;
  }
  unsubscribe() {
    let errors;
    if (!this.closed) {
      this.closed = true;
      const { _parentage } = this;
      if (_parentage) {
        this._parentage = null;
        if (Array.isArray(_parentage)) {
          for (const parent of _parentage) {
            parent.remove(this);
          }
        } else {
          _parentage.remove(this);
        }
      }
      const { initialTeardown: initialFinalizer } = this;
      if (isFunction(initialFinalizer)) {
        try {
          initialFinalizer();
        } catch (e) {
          errors = e instanceof UnsubscriptionError ? e.errors : [e];
        }
      }
      const { _finalizers } = this;
      if (_finalizers) {
        this._finalizers = null;
        for (const finalizer of _finalizers) {
          try {
            execFinalizer(finalizer);
          } catch (err) {
            errors = errors !== null && errors !== void 0 ? errors : [];
            if (err instanceof UnsubscriptionError) {
              errors = [...errors, ...err.errors];
            } else {
              errors.push(err);
            }
          }
        }
      }
      if (errors) {
        throw new UnsubscriptionError(errors);
      }
    }
  }
  add(teardown) {
    var _a;
    if (teardown && teardown !== this) {
      if (this.closed) {
        execFinalizer(teardown);
      } else {
        if (teardown instanceof _Subscription) {
          if (teardown.closed || teardown._hasParent(this)) {
            return;
          }
          teardown._addParent(this);
        }
        (this._finalizers = (_a = this._finalizers) !== null && _a !== void 0 ? _a : []).push(teardown);
      }
    }
  }
  _hasParent(parent) {
    const { _parentage } = this;
    return _parentage === parent || Array.isArray(_parentage) && _parentage.includes(parent);
  }
  _addParent(parent) {
    const { _parentage } = this;
    this._parentage = Array.isArray(_parentage) ? (_parentage.push(parent), _parentage) : _parentage ? [_parentage, parent] : parent;
  }
  _removeParent(parent) {
    const { _parentage } = this;
    if (_parentage === parent) {
      this._parentage = null;
    } else if (Array.isArray(_parentage)) {
      arrRemove(_parentage, parent);
    }
  }
  remove(teardown) {
    const { _finalizers } = this;
    _finalizers && arrRemove(_finalizers, teardown);
    if (teardown instanceof _Subscription) {
      teardown._removeParent(this);
    }
  }
};
Subscription.EMPTY = (() => {
  const empty = new Subscription();
  empty.closed = true;
  return empty;
})();
var EMPTY_SUBSCRIPTION = Subscription.EMPTY;
function isSubscription(value) {
  return value instanceof Subscription || value && "closed" in value && isFunction(value.remove) && isFunction(value.add) && isFunction(value.unsubscribe);
}
function execFinalizer(finalizer) {
  if (isFunction(finalizer)) {
    finalizer();
  } else {
    finalizer.unsubscribe();
  }
}

// node_modules/rxjs/dist/esm/internal/config.js
var config = {
  onUnhandledError: null,
  onStoppedNotification: null,
  Promise: void 0,
  useDeprecatedSynchronousErrorHandling: false,
  useDeprecatedNextContext: false
};

// node_modules/rxjs/dist/esm/internal/scheduler/timeoutProvider.js
var timeoutProvider = {
  setTimeout(handler, timeout, ...args) {
    const { delegate } = timeoutProvider;
    if (delegate === null || delegate === void 0 ? void 0 : delegate.setTimeout) {
      return delegate.setTimeout(handler, timeout, ...args);
    }
    return setTimeout(handler, timeout, ...args);
  },
  clearTimeout(handle) {
    const { delegate } = timeoutProvider;
    return ((delegate === null || delegate === void 0 ? void 0 : delegate.clearTimeout) || clearTimeout)(handle);
  },
  delegate: void 0
};

// node_modules/rxjs/dist/esm/internal/util/reportUnhandledError.js
function reportUnhandledError(err) {
  timeoutProvider.setTimeout(() => {
    const { onUnhandledError } = config;
    if (onUnhandledError) {
      onUnhandledError(err);
    } else {
      throw err;
    }
  });
}

// node_modules/rxjs/dist/esm/internal/util/noop.js
function noop() {
}

// node_modules/rxjs/dist/esm/internal/NotificationFactories.js
var COMPLETE_NOTIFICATION = (() => createNotification("C", void 0, void 0))();
function errorNotification(error) {
  return createNotification("E", void 0, error);
}
function nextNotification(value) {
  return createNotification("N", value, void 0);
}
function createNotification(kind, value, error) {
  return {
    kind,
    value,
    error
  };
}

// node_modules/rxjs/dist/esm/internal/util/errorContext.js
var context = null;
function errorContext(cb) {
  if (config.useDeprecatedSynchronousErrorHandling) {
    const isRoot = !context;
    if (isRoot) {
      context = { errorThrown: false, error: null };
    }
    cb();
    if (isRoot) {
      const { errorThrown, error } = context;
      context = null;
      if (errorThrown) {
        throw error;
      }
    }
  } else {
    cb();
  }
}
function captureError(err) {
  if (config.useDeprecatedSynchronousErrorHandling && context) {
    context.errorThrown = true;
    context.error = err;
  }
}

// node_modules/rxjs/dist/esm/internal/Subscriber.js
var Subscriber = class extends Subscription {
  constructor(destination) {
    super();
    this.isStopped = false;
    if (destination) {
      this.destination = destination;
      if (isSubscription(destination)) {
        destination.add(this);
      }
    } else {
      this.destination = EMPTY_OBSERVER;
    }
  }
  static create(next, error, complete) {
    return new SafeSubscriber(next, error, complete);
  }
  next(value) {
    if (this.isStopped) {
      handleStoppedNotification(nextNotification(value), this);
    } else {
      this._next(value);
    }
  }
  error(err) {
    if (this.isStopped) {
      handleStoppedNotification(errorNotification(err), this);
    } else {
      this.isStopped = true;
      this._error(err);
    }
  }
  complete() {
    if (this.isStopped) {
      handleStoppedNotification(COMPLETE_NOTIFICATION, this);
    } else {
      this.isStopped = true;
      this._complete();
    }
  }
  unsubscribe() {
    if (!this.closed) {
      this.isStopped = true;
      super.unsubscribe();
      this.destination = null;
    }
  }
  _next(value) {
    this.destination.next(value);
  }
  _error(err) {
    try {
      this.destination.error(err);
    } finally {
      this.unsubscribe();
    }
  }
  _complete() {
    try {
      this.destination.complete();
    } finally {
      this.unsubscribe();
    }
  }
};
var _bind = Function.prototype.bind;
function bind(fn, thisArg) {
  return _bind.call(fn, thisArg);
}
var ConsumerObserver = class {
  constructor(partialObserver) {
    this.partialObserver = partialObserver;
  }
  next(value) {
    const { partialObserver } = this;
    if (partialObserver.next) {
      try {
        partialObserver.next(value);
      } catch (error) {
        handleUnhandledError(error);
      }
    }
  }
  error(err) {
    const { partialObserver } = this;
    if (partialObserver.error) {
      try {
        partialObserver.error(err);
      } catch (error) {
        handleUnhandledError(error);
      }
    } else {
      handleUnhandledError(err);
    }
  }
  complete() {
    const { partialObserver } = this;
    if (partialObserver.complete) {
      try {
        partialObserver.complete();
      } catch (error) {
        handleUnhandledError(error);
      }
    }
  }
};
var SafeSubscriber = class extends Subscriber {
  constructor(observerOrNext, error, complete) {
    super();
    let partialObserver;
    if (isFunction(observerOrNext) || !observerOrNext) {
      partialObserver = {
        next: observerOrNext !== null && observerOrNext !== void 0 ? observerOrNext : void 0,
        error: error !== null && error !== void 0 ? error : void 0,
        complete: complete !== null && complete !== void 0 ? complete : void 0
      };
    } else {
      let context2;
      if (this && config.useDeprecatedNextContext) {
        context2 = Object.create(observerOrNext);
        context2.unsubscribe = () => this.unsubscribe();
        partialObserver = {
          next: observerOrNext.next && bind(observerOrNext.next, context2),
          error: observerOrNext.error && bind(observerOrNext.error, context2),
          complete: observerOrNext.complete && bind(observerOrNext.complete, context2)
        };
      } else {
        partialObserver = observerOrNext;
      }
    }
    this.destination = new ConsumerObserver(partialObserver);
  }
};
function handleUnhandledError(error) {
  if (config.useDeprecatedSynchronousErrorHandling) {
    captureError(error);
  } else {
    reportUnhandledError(error);
  }
}
function defaultErrorHandler(err) {
  throw err;
}
function handleStoppedNotification(notification, subscriber) {
  const { onStoppedNotification } = config;
  onStoppedNotification && timeoutProvider.setTimeout(() => onStoppedNotification(notification, subscriber));
}
var EMPTY_OBSERVER = {
  closed: true,
  next: noop,
  error: defaultErrorHandler,
  complete: noop
};

// node_modules/rxjs/dist/esm/internal/symbol/observable.js
var observable = (() => typeof Symbol === "function" && Symbol.observable || "@@observable")();

// node_modules/rxjs/dist/esm/internal/util/identity.js
function identity(x) {
  return x;
}

// node_modules/rxjs/dist/esm/internal/util/pipe.js
function pipe(...fns) {
  return pipeFromArray(fns);
}
function pipeFromArray(fns) {
  if (fns.length === 0) {
    return identity;
  }
  if (fns.length === 1) {
    return fns[0];
  }
  return function piped(input2) {
    return fns.reduce((prev, fn) => fn(prev), input2);
  };
}

// node_modules/rxjs/dist/esm/internal/Observable.js
var Observable = class _Observable {
  constructor(subscribe) {
    if (subscribe) {
      this._subscribe = subscribe;
    }
  }
  lift(operator) {
    const observable2 = new _Observable();
    observable2.source = this;
    observable2.operator = operator;
    return observable2;
  }
  subscribe(observerOrNext, error, complete) {
    const subscriber = isSubscriber(observerOrNext) ? observerOrNext : new SafeSubscriber(observerOrNext, error, complete);
    errorContext(() => {
      const { operator, source } = this;
      subscriber.add(operator ? operator.call(subscriber, source) : source ? this._subscribe(subscriber) : this._trySubscribe(subscriber));
    });
    return subscriber;
  }
  _trySubscribe(sink) {
    try {
      return this._subscribe(sink);
    } catch (err) {
      sink.error(err);
    }
  }
  forEach(next, promiseCtor) {
    promiseCtor = getPromiseCtor(promiseCtor);
    return new promiseCtor((resolve, reject) => {
      const subscriber = new SafeSubscriber({
        next: (value) => {
          try {
            next(value);
          } catch (err) {
            reject(err);
            subscriber.unsubscribe();
          }
        },
        error: reject,
        complete: resolve
      });
      this.subscribe(subscriber);
    });
  }
  _subscribe(subscriber) {
    var _a;
    return (_a = this.source) === null || _a === void 0 ? void 0 : _a.subscribe(subscriber);
  }
  [observable]() {
    return this;
  }
  pipe(...operations) {
    return pipeFromArray(operations)(this);
  }
  toPromise(promiseCtor) {
    promiseCtor = getPromiseCtor(promiseCtor);
    return new promiseCtor((resolve, reject) => {
      let value;
      this.subscribe((x) => value = x, (err) => reject(err), () => resolve(value));
    });
  }
};
Observable.create = (subscribe) => {
  return new Observable(subscribe);
};
function getPromiseCtor(promiseCtor) {
  var _a;
  return (_a = promiseCtor !== null && promiseCtor !== void 0 ? promiseCtor : config.Promise) !== null && _a !== void 0 ? _a : Promise;
}
function isObserver(value) {
  return value && isFunction(value.next) && isFunction(value.error) && isFunction(value.complete);
}
function isSubscriber(value) {
  return value && value instanceof Subscriber || isObserver(value) && isSubscription(value);
}

// node_modules/rxjs/dist/esm/internal/util/lift.js
function hasLift(source) {
  return isFunction(source === null || source === void 0 ? void 0 : source.lift);
}
function operate(init) {
  return (source) => {
    if (hasLift(source)) {
      return source.lift(function(liftedSource) {
        try {
          return init(liftedSource, this);
        } catch (err) {
          this.error(err);
        }
      });
    }
    throw new TypeError("Unable to lift unknown Observable type");
  };
}

// node_modules/rxjs/dist/esm/internal/operators/OperatorSubscriber.js
function createOperatorSubscriber(destination, onNext, onComplete, onError, onFinalize) {
  return new OperatorSubscriber(destination, onNext, onComplete, onError, onFinalize);
}
var OperatorSubscriber = class extends Subscriber {
  constructor(destination, onNext, onComplete, onError, onFinalize, shouldUnsubscribe) {
    super(destination);
    this.onFinalize = onFinalize;
    this.shouldUnsubscribe = shouldUnsubscribe;
    this._next = onNext ? function(value) {
      try {
        onNext(value);
      } catch (err) {
        destination.error(err);
      }
    } : super._next;
    this._error = onError ? function(err) {
      try {
        onError(err);
      } catch (err2) {
        destination.error(err2);
      } finally {
        this.unsubscribe();
      }
    } : super._error;
    this._complete = onComplete ? function() {
      try {
        onComplete();
      } catch (err) {
        destination.error(err);
      } finally {
        this.unsubscribe();
      }
    } : super._complete;
  }
  unsubscribe() {
    var _a;
    if (!this.shouldUnsubscribe || this.shouldUnsubscribe()) {
      const { closed } = this;
      super.unsubscribe();
      !closed && ((_a = this.onFinalize) === null || _a === void 0 ? void 0 : _a.call(this));
    }
  }
};

// node_modules/rxjs/dist/esm/internal/util/ObjectUnsubscribedError.js
var ObjectUnsubscribedError = createErrorClass((_super) => function ObjectUnsubscribedErrorImpl() {
  _super(this);
  this.name = "ObjectUnsubscribedError";
  this.message = "object unsubscribed";
});

// node_modules/rxjs/dist/esm/internal/Subject.js
var Subject = class extends Observable {
  constructor() {
    super();
    this.closed = false;
    this.currentObservers = null;
    this.observers = [];
    this.isStopped = false;
    this.hasError = false;
    this.thrownError = null;
  }
  lift(operator) {
    const subject = new AnonymousSubject(this, this);
    subject.operator = operator;
    return subject;
  }
  _throwIfClosed() {
    if (this.closed) {
      throw new ObjectUnsubscribedError();
    }
  }
  next(value) {
    errorContext(() => {
      this._throwIfClosed();
      if (!this.isStopped) {
        if (!this.currentObservers) {
          this.currentObservers = Array.from(this.observers);
        }
        for (const observer of this.currentObservers) {
          observer.next(value);
        }
      }
    });
  }
  error(err) {
    errorContext(() => {
      this._throwIfClosed();
      if (!this.isStopped) {
        this.hasError = this.isStopped = true;
        this.thrownError = err;
        const { observers } = this;
        while (observers.length) {
          observers.shift().error(err);
        }
      }
    });
  }
  complete() {
    errorContext(() => {
      this._throwIfClosed();
      if (!this.isStopped) {
        this.isStopped = true;
        const { observers } = this;
        while (observers.length) {
          observers.shift().complete();
        }
      }
    });
  }
  unsubscribe() {
    this.isStopped = this.closed = true;
    this.observers = this.currentObservers = null;
  }
  get observed() {
    var _a;
    return ((_a = this.observers) === null || _a === void 0 ? void 0 : _a.length) > 0;
  }
  _trySubscribe(subscriber) {
    this._throwIfClosed();
    return super._trySubscribe(subscriber);
  }
  _subscribe(subscriber) {
    this._throwIfClosed();
    this._checkFinalizedStatuses(subscriber);
    return this._innerSubscribe(subscriber);
  }
  _innerSubscribe(subscriber) {
    const { hasError, isStopped, observers } = this;
    if (hasError || isStopped) {
      return EMPTY_SUBSCRIPTION;
    }
    this.currentObservers = null;
    observers.push(subscriber);
    return new Subscription(() => {
      this.currentObservers = null;
      arrRemove(observers, subscriber);
    });
  }
  _checkFinalizedStatuses(subscriber) {
    const { hasError, thrownError, isStopped } = this;
    if (hasError) {
      subscriber.error(thrownError);
    } else if (isStopped) {
      subscriber.complete();
    }
  }
  asObservable() {
    const observable2 = new Observable();
    observable2.source = this;
    return observable2;
  }
};
Subject.create = (destination, source) => {
  return new AnonymousSubject(destination, source);
};
var AnonymousSubject = class extends Subject {
  constructor(destination, source) {
    super();
    this.destination = destination;
    this.source = source;
  }
  next(value) {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.next) === null || _b === void 0 ? void 0 : _b.call(_a, value);
  }
  error(err) {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.error) === null || _b === void 0 ? void 0 : _b.call(_a, err);
  }
  complete() {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.complete) === null || _b === void 0 ? void 0 : _b.call(_a);
  }
  _subscribe(subscriber) {
    var _a, _b;
    return (_b = (_a = this.source) === null || _a === void 0 ? void 0 : _a.subscribe(subscriber)) !== null && _b !== void 0 ? _b : EMPTY_SUBSCRIPTION;
  }
};

// node_modules/rxjs/dist/esm/internal/BehaviorSubject.js
var BehaviorSubject = class extends Subject {
  constructor(_value) {
    super();
    this._value = _value;
  }
  get value() {
    return this.getValue();
  }
  _subscribe(subscriber) {
    const subscription = super._subscribe(subscriber);
    !subscription.closed && subscriber.next(this._value);
    return subscription;
  }
  getValue() {
    const { hasError, thrownError, _value } = this;
    if (hasError) {
      throw thrownError;
    }
    this._throwIfClosed();
    return _value;
  }
  next(value) {
    super.next(this._value = value);
  }
};

// node_modules/rxjs/dist/esm/internal/observable/empty.js
var EMPTY = new Observable((subscriber) => subscriber.complete());

// node_modules/rxjs/dist/esm/internal/util/isScheduler.js
function isScheduler(value) {
  return value && isFunction(value.schedule);
}

// node_modules/rxjs/dist/esm/internal/util/args.js
function last(arr) {
  return arr[arr.length - 1];
}
function popResultSelector(args) {
  return isFunction(last(args)) ? args.pop() : void 0;
}
function popScheduler(args) {
  return isScheduler(last(args)) ? args.pop() : void 0;
}

// node_modules/tslib/tslib.es6.mjs
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __values(o) {
  var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
  if (m) return m.call(o);
  if (o && typeof o.length === "number") return {
    next: function() {
      if (o && i >= o.length) o = void 0;
      return { value: o && o[i++], done: !o };
    }
  };
  throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function __await(v) {
  return this instanceof __await ? (this.v = v, this) : new __await(v);
}
function __asyncGenerator(thisArg, _arguments, generator) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var g = generator.apply(thisArg, _arguments || []), i, q = [];
  return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
    return this;
  }, i;
  function awaitReturn(f) {
    return function(v) {
      return Promise.resolve(v).then(f, reject);
    };
  }
  function verb(n, f) {
    if (g[n]) {
      i[n] = function(v) {
        return new Promise(function(a, b) {
          q.push([n, v, a, b]) > 1 || resume(n, v);
        });
      };
      if (f) i[n] = f(i[n]);
    }
  }
  function resume(n, v) {
    try {
      step(g[n](v));
    } catch (e) {
      settle(q[0][3], e);
    }
  }
  function step(r) {
    r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
  }
  function fulfill(value) {
    resume("next", value);
  }
  function reject(value) {
    resume("throw", value);
  }
  function settle(f, v) {
    if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
  }
}
function __asyncValues(o) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var m = o[Symbol.asyncIterator], i;
  return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
    return this;
  }, i);
  function verb(n) {
    i[n] = o[n] && function(v) {
      return new Promise(function(resolve, reject) {
        v = o[n](v), settle(resolve, reject, v.done, v.value);
      });
    };
  }
  function settle(resolve, reject, d, v) {
    Promise.resolve(v).then(function(v2) {
      resolve({ value: v2, done: d });
    }, reject);
  }
}

// node_modules/rxjs/dist/esm/internal/util/isArrayLike.js
var isArrayLike = ((x) => x && typeof x.length === "number" && typeof x !== "function");

// node_modules/rxjs/dist/esm/internal/util/isPromise.js
function isPromise(value) {
  return isFunction(value === null || value === void 0 ? void 0 : value.then);
}

// node_modules/rxjs/dist/esm/internal/util/isInteropObservable.js
function isInteropObservable(input2) {
  return isFunction(input2[observable]);
}

// node_modules/rxjs/dist/esm/internal/util/isAsyncIterable.js
function isAsyncIterable(obj) {
  return Symbol.asyncIterator && isFunction(obj === null || obj === void 0 ? void 0 : obj[Symbol.asyncIterator]);
}

// node_modules/rxjs/dist/esm/internal/util/throwUnobservableError.js
function createInvalidObservableTypeError(input2) {
  return new TypeError(`You provided ${input2 !== null && typeof input2 === "object" ? "an invalid object" : `'${input2}'`} where a stream was expected. You can provide an Observable, Promise, ReadableStream, Array, AsyncIterable, or Iterable.`);
}

// node_modules/rxjs/dist/esm/internal/symbol/iterator.js
function getSymbolIterator() {
  if (typeof Symbol !== "function" || !Symbol.iterator) {
    return "@@iterator";
  }
  return Symbol.iterator;
}
var iterator = getSymbolIterator();

// node_modules/rxjs/dist/esm/internal/util/isIterable.js
function isIterable(input2) {
  return isFunction(input2 === null || input2 === void 0 ? void 0 : input2[iterator]);
}

// node_modules/rxjs/dist/esm/internal/util/isReadableStreamLike.js
function readableStreamLikeToAsyncGenerator(readableStream) {
  return __asyncGenerator(this, arguments, function* readableStreamLikeToAsyncGenerator_1() {
    const reader = readableStream.getReader();
    try {
      while (true) {
        const { value, done } = yield __await(reader.read());
        if (done) {
          return yield __await(void 0);
        }
        yield yield __await(value);
      }
    } finally {
      reader.releaseLock();
    }
  });
}
function isReadableStreamLike(obj) {
  return isFunction(obj === null || obj === void 0 ? void 0 : obj.getReader);
}

// node_modules/rxjs/dist/esm/internal/observable/innerFrom.js
function innerFrom(input2) {
  if (input2 instanceof Observable) {
    return input2;
  }
  if (input2 != null) {
    if (isInteropObservable(input2)) {
      return fromInteropObservable(input2);
    }
    if (isArrayLike(input2)) {
      return fromArrayLike(input2);
    }
    if (isPromise(input2)) {
      return fromPromise(input2);
    }
    if (isAsyncIterable(input2)) {
      return fromAsyncIterable(input2);
    }
    if (isIterable(input2)) {
      return fromIterable(input2);
    }
    if (isReadableStreamLike(input2)) {
      return fromReadableStreamLike(input2);
    }
  }
  throw createInvalidObservableTypeError(input2);
}
function fromInteropObservable(obj) {
  return new Observable((subscriber) => {
    const obs = obj[observable]();
    if (isFunction(obs.subscribe)) {
      return obs.subscribe(subscriber);
    }
    throw new TypeError("Provided object does not correctly implement Symbol.observable");
  });
}
function fromArrayLike(array) {
  return new Observable((subscriber) => {
    for (let i = 0; i < array.length && !subscriber.closed; i++) {
      subscriber.next(array[i]);
    }
    subscriber.complete();
  });
}
function fromPromise(promise) {
  return new Observable((subscriber) => {
    promise.then((value) => {
      if (!subscriber.closed) {
        subscriber.next(value);
        subscriber.complete();
      }
    }, (err) => subscriber.error(err)).then(null, reportUnhandledError);
  });
}
function fromIterable(iterable) {
  return new Observable((subscriber) => {
    for (const value of iterable) {
      subscriber.next(value);
      if (subscriber.closed) {
        return;
      }
    }
    subscriber.complete();
  });
}
function fromAsyncIterable(asyncIterable) {
  return new Observable((subscriber) => {
    process(asyncIterable, subscriber).catch((err) => subscriber.error(err));
  });
}
function fromReadableStreamLike(readableStream) {
  return fromAsyncIterable(readableStreamLikeToAsyncGenerator(readableStream));
}
function process(asyncIterable, subscriber) {
  var asyncIterable_1, asyncIterable_1_1;
  var e_1, _a;
  return __awaiter(this, void 0, void 0, function* () {
    try {
      for (asyncIterable_1 = __asyncValues(asyncIterable); asyncIterable_1_1 = yield asyncIterable_1.next(), !asyncIterable_1_1.done; ) {
        const value = asyncIterable_1_1.value;
        subscriber.next(value);
        if (subscriber.closed) {
          return;
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (asyncIterable_1_1 && !asyncIterable_1_1.done && (_a = asyncIterable_1.return)) yield _a.call(asyncIterable_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    subscriber.complete();
  });
}

// node_modules/rxjs/dist/esm/internal/util/executeSchedule.js
function executeSchedule(parentSubscription, scheduler, work, delay = 0, repeat = false) {
  const scheduleSubscription = scheduler.schedule(function() {
    work();
    if (repeat) {
      parentSubscription.add(this.schedule(null, delay));
    } else {
      this.unsubscribe();
    }
  }, delay);
  parentSubscription.add(scheduleSubscription);
  if (!repeat) {
    return scheduleSubscription;
  }
}

// node_modules/rxjs/dist/esm/internal/operators/observeOn.js
function observeOn(scheduler, delay = 0) {
  return operate((source, subscriber) => {
    source.subscribe(createOperatorSubscriber(subscriber, (value) => executeSchedule(subscriber, scheduler, () => subscriber.next(value), delay), () => executeSchedule(subscriber, scheduler, () => subscriber.complete(), delay), (err) => executeSchedule(subscriber, scheduler, () => subscriber.error(err), delay)));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/subscribeOn.js
function subscribeOn(scheduler, delay = 0) {
  return operate((source, subscriber) => {
    subscriber.add(scheduler.schedule(() => source.subscribe(subscriber), delay));
  });
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduleObservable.js
function scheduleObservable(input2, scheduler) {
  return innerFrom(input2).pipe(subscribeOn(scheduler), observeOn(scheduler));
}

// node_modules/rxjs/dist/esm/internal/scheduled/schedulePromise.js
function schedulePromise(input2, scheduler) {
  return innerFrom(input2).pipe(subscribeOn(scheduler), observeOn(scheduler));
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduleArray.js
function scheduleArray(input2, scheduler) {
  return new Observable((subscriber) => {
    let i = 0;
    return scheduler.schedule(function() {
      if (i === input2.length) {
        subscriber.complete();
      } else {
        subscriber.next(input2[i++]);
        if (!subscriber.closed) {
          this.schedule();
        }
      }
    });
  });
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduleIterable.js
function scheduleIterable(input2, scheduler) {
  return new Observable((subscriber) => {
    let iterator2;
    executeSchedule(subscriber, scheduler, () => {
      iterator2 = input2[iterator]();
      executeSchedule(subscriber, scheduler, () => {
        let value;
        let done;
        try {
          ({ value, done } = iterator2.next());
        } catch (err) {
          subscriber.error(err);
          return;
        }
        if (done) {
          subscriber.complete();
        } else {
          subscriber.next(value);
        }
      }, 0, true);
    });
    return () => isFunction(iterator2 === null || iterator2 === void 0 ? void 0 : iterator2.return) && iterator2.return();
  });
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduleAsyncIterable.js
function scheduleAsyncIterable(input2, scheduler) {
  if (!input2) {
    throw new Error("Iterable cannot be null");
  }
  return new Observable((subscriber) => {
    executeSchedule(subscriber, scheduler, () => {
      const iterator2 = input2[Symbol.asyncIterator]();
      executeSchedule(subscriber, scheduler, () => {
        iterator2.next().then((result) => {
          if (result.done) {
            subscriber.complete();
          } else {
            subscriber.next(result.value);
          }
        });
      }, 0, true);
    });
  });
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduleReadableStreamLike.js
function scheduleReadableStreamLike(input2, scheduler) {
  return scheduleAsyncIterable(readableStreamLikeToAsyncGenerator(input2), scheduler);
}

// node_modules/rxjs/dist/esm/internal/scheduled/scheduled.js
function scheduled(input2, scheduler) {
  if (input2 != null) {
    if (isInteropObservable(input2)) {
      return scheduleObservable(input2, scheduler);
    }
    if (isArrayLike(input2)) {
      return scheduleArray(input2, scheduler);
    }
    if (isPromise(input2)) {
      return schedulePromise(input2, scheduler);
    }
    if (isAsyncIterable(input2)) {
      return scheduleAsyncIterable(input2, scheduler);
    }
    if (isIterable(input2)) {
      return scheduleIterable(input2, scheduler);
    }
    if (isReadableStreamLike(input2)) {
      return scheduleReadableStreamLike(input2, scheduler);
    }
  }
  throw createInvalidObservableTypeError(input2);
}

// node_modules/rxjs/dist/esm/internal/observable/from.js
function from(input2, scheduler) {
  return scheduler ? scheduled(input2, scheduler) : innerFrom(input2);
}

// node_modules/rxjs/dist/esm/internal/observable/of.js
function of(...args) {
  const scheduler = popScheduler(args);
  return from(args, scheduler);
}

// node_modules/rxjs/dist/esm/internal/observable/throwError.js
function throwError(errorOrErrorFactory, scheduler) {
  const errorFactory = isFunction(errorOrErrorFactory) ? errorOrErrorFactory : () => errorOrErrorFactory;
  const init = (subscriber) => subscriber.error(errorFactory());
  return new Observable(scheduler ? (subscriber) => scheduler.schedule(init, 0, subscriber) : init);
}

// node_modules/rxjs/dist/esm/internal/util/isObservable.js
function isObservable(obj) {
  return !!obj && (obj instanceof Observable || isFunction(obj.lift) && isFunction(obj.subscribe));
}

// node_modules/rxjs/dist/esm/internal/util/EmptyError.js
var EmptyError = createErrorClass((_super) => function EmptyErrorImpl() {
  _super(this);
  this.name = "EmptyError";
  this.message = "no elements in sequence";
});

// node_modules/rxjs/dist/esm/internal/operators/map.js
function map(project, thisArg) {
  return operate((source, subscriber) => {
    let index = 0;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      subscriber.next(project.call(thisArg, value, index++));
    }));
  });
}

// node_modules/rxjs/dist/esm/internal/util/mapOneOrManyArgs.js
var { isArray } = Array;
function callOrApply(fn, args) {
  return isArray(args) ? fn(...args) : fn(args);
}
function mapOneOrManyArgs(fn) {
  return map((args) => callOrApply(fn, args));
}

// node_modules/rxjs/dist/esm/internal/util/argsArgArrayOrObject.js
var { isArray: isArray2 } = Array;
var { getPrototypeOf, prototype: objectProto, keys: getKeys } = Object;
function argsArgArrayOrObject(args) {
  if (args.length === 1) {
    const first2 = args[0];
    if (isArray2(first2)) {
      return { args: first2, keys: null };
    }
    if (isPOJO(first2)) {
      const keys = getKeys(first2);
      return {
        args: keys.map((key) => first2[key]),
        keys
      };
    }
  }
  return { args, keys: null };
}
function isPOJO(obj) {
  return obj && typeof obj === "object" && getPrototypeOf(obj) === objectProto;
}

// node_modules/rxjs/dist/esm/internal/util/createObject.js
function createObject(keys, values) {
  return keys.reduce((result, key, i) => (result[key] = values[i], result), {});
}

// node_modules/rxjs/dist/esm/internal/observable/combineLatest.js
function combineLatest(...args) {
  const scheduler = popScheduler(args);
  const resultSelector = popResultSelector(args);
  const { args: observables, keys } = argsArgArrayOrObject(args);
  if (observables.length === 0) {
    return from([], scheduler);
  }
  const result = new Observable(combineLatestInit(observables, scheduler, keys ? (values) => createObject(keys, values) : identity));
  return resultSelector ? result.pipe(mapOneOrManyArgs(resultSelector)) : result;
}
function combineLatestInit(observables, scheduler, valueTransform = identity) {
  return (subscriber) => {
    maybeSchedule(scheduler, () => {
      const { length } = observables;
      const values = new Array(length);
      let active = length;
      let remainingFirstValues = length;
      for (let i = 0; i < length; i++) {
        maybeSchedule(scheduler, () => {
          const source = from(observables[i], scheduler);
          let hasFirstValue = false;
          source.subscribe(createOperatorSubscriber(subscriber, (value) => {
            values[i] = value;
            if (!hasFirstValue) {
              hasFirstValue = true;
              remainingFirstValues--;
            }
            if (!remainingFirstValues) {
              subscriber.next(valueTransform(values.slice()));
            }
          }, () => {
            if (!--active) {
              subscriber.complete();
            }
          }));
        }, subscriber);
      }
    }, subscriber);
  };
}
function maybeSchedule(scheduler, execute, subscription) {
  if (scheduler) {
    executeSchedule(subscription, scheduler, execute);
  } else {
    execute();
  }
}

// node_modules/rxjs/dist/esm/internal/operators/mergeInternals.js
function mergeInternals(source, subscriber, project, concurrent, onBeforeNext, expand, innerSubScheduler, additionalFinalizer) {
  const buffer = [];
  let active = 0;
  let index = 0;
  let isComplete = false;
  const checkComplete = () => {
    if (isComplete && !buffer.length && !active) {
      subscriber.complete();
    }
  };
  const outerNext = (value) => active < concurrent ? doInnerSub(value) : buffer.push(value);
  const doInnerSub = (value) => {
    expand && subscriber.next(value);
    active++;
    let innerComplete = false;
    innerFrom(project(value, index++)).subscribe(createOperatorSubscriber(subscriber, (innerValue) => {
      onBeforeNext === null || onBeforeNext === void 0 ? void 0 : onBeforeNext(innerValue);
      if (expand) {
        outerNext(innerValue);
      } else {
        subscriber.next(innerValue);
      }
    }, () => {
      innerComplete = true;
    }, void 0, () => {
      if (innerComplete) {
        try {
          active--;
          while (buffer.length && active < concurrent) {
            const bufferedValue = buffer.shift();
            if (innerSubScheduler) {
              executeSchedule(subscriber, innerSubScheduler, () => doInnerSub(bufferedValue));
            } else {
              doInnerSub(bufferedValue);
            }
          }
          checkComplete();
        } catch (err) {
          subscriber.error(err);
        }
      }
    }));
  };
  source.subscribe(createOperatorSubscriber(subscriber, outerNext, () => {
    isComplete = true;
    checkComplete();
  }));
  return () => {
    additionalFinalizer === null || additionalFinalizer === void 0 ? void 0 : additionalFinalizer();
  };
}

// node_modules/rxjs/dist/esm/internal/operators/mergeMap.js
function mergeMap(project, resultSelector, concurrent = Infinity) {
  if (isFunction(resultSelector)) {
    return mergeMap((a, i) => map((b, ii) => resultSelector(a, b, i, ii))(innerFrom(project(a, i))), concurrent);
  } else if (typeof resultSelector === "number") {
    concurrent = resultSelector;
  }
  return operate((source, subscriber) => mergeInternals(source, subscriber, project, concurrent));
}

// node_modules/rxjs/dist/esm/internal/operators/mergeAll.js
function mergeAll(concurrent = Infinity) {
  return mergeMap(identity, concurrent);
}

// node_modules/rxjs/dist/esm/internal/operators/concatAll.js
function concatAll() {
  return mergeAll(1);
}

// node_modules/rxjs/dist/esm/internal/observable/concat.js
function concat(...args) {
  return concatAll()(from(args, popScheduler(args)));
}

// node_modules/rxjs/dist/esm/internal/observable/defer.js
function defer(observableFactory) {
  return new Observable((subscriber) => {
    innerFrom(observableFactory()).subscribe(subscriber);
  });
}

// node_modules/rxjs/dist/esm/internal/observable/forkJoin.js
function forkJoin(...args) {
  const resultSelector = popResultSelector(args);
  const { args: sources, keys } = argsArgArrayOrObject(args);
  const result = new Observable((subscriber) => {
    const { length } = sources;
    if (!length) {
      subscriber.complete();
      return;
    }
    const values = new Array(length);
    let remainingCompletions = length;
    let remainingEmissions = length;
    for (let sourceIndex = 0; sourceIndex < length; sourceIndex++) {
      let hasValue = false;
      innerFrom(sources[sourceIndex]).subscribe(createOperatorSubscriber(subscriber, (value) => {
        if (!hasValue) {
          hasValue = true;
          remainingEmissions--;
        }
        values[sourceIndex] = value;
      }, () => remainingCompletions--, void 0, () => {
        if (!remainingCompletions || !hasValue) {
          if (!remainingEmissions) {
            subscriber.next(keys ? createObject(keys, values) : values);
          }
          subscriber.complete();
        }
      }));
    }
  });
  return resultSelector ? result.pipe(mapOneOrManyArgs(resultSelector)) : result;
}

// node_modules/rxjs/dist/esm/internal/operators/filter.js
function filter(predicate, thisArg) {
  return operate((source, subscriber) => {
    let index = 0;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => predicate.call(thisArg, value, index++) && subscriber.next(value)));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/catchError.js
function catchError(selector) {
  return operate((source, subscriber) => {
    let innerSub = null;
    let syncUnsub = false;
    let handledResult;
    innerSub = source.subscribe(createOperatorSubscriber(subscriber, void 0, void 0, (err) => {
      handledResult = innerFrom(selector(err, catchError(selector)(source)));
      if (innerSub) {
        innerSub.unsubscribe();
        innerSub = null;
        handledResult.subscribe(subscriber);
      } else {
        syncUnsub = true;
      }
    }));
    if (syncUnsub) {
      innerSub.unsubscribe();
      innerSub = null;
      handledResult.subscribe(subscriber);
    }
  });
}

// node_modules/rxjs/dist/esm/internal/operators/concatMap.js
function concatMap(project, resultSelector) {
  return isFunction(resultSelector) ? mergeMap(project, resultSelector, 1) : mergeMap(project, 1);
}

// node_modules/rxjs/dist/esm/internal/operators/defaultIfEmpty.js
function defaultIfEmpty(defaultValue) {
  return operate((source, subscriber) => {
    let hasValue = false;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      hasValue = true;
      subscriber.next(value);
    }, () => {
      if (!hasValue) {
        subscriber.next(defaultValue);
      }
      subscriber.complete();
    }));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/take.js
function take(count) {
  return count <= 0 ? () => EMPTY : operate((source, subscriber) => {
    let seen = 0;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      if (++seen <= count) {
        subscriber.next(value);
        if (count <= seen) {
          subscriber.complete();
        }
      }
    }));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/throwIfEmpty.js
function throwIfEmpty(errorFactory = defaultErrorFactory) {
  return operate((source, subscriber) => {
    let hasValue = false;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      hasValue = true;
      subscriber.next(value);
    }, () => hasValue ? subscriber.complete() : subscriber.error(errorFactory())));
  });
}
function defaultErrorFactory() {
  return new EmptyError();
}

// node_modules/rxjs/dist/esm/internal/operators/finalize.js
function finalize(callback) {
  return operate((source, subscriber) => {
    try {
      source.subscribe(subscriber);
    } finally {
      subscriber.add(callback);
    }
  });
}

// node_modules/rxjs/dist/esm/internal/operators/first.js
function first(predicate, defaultValue) {
  const hasDefaultValue = arguments.length >= 2;
  return (source) => source.pipe(predicate ? filter((v, i) => predicate(v, i, source)) : identity, take(1), hasDefaultValue ? defaultIfEmpty(defaultValue) : throwIfEmpty(() => new EmptyError()));
}

// node_modules/rxjs/dist/esm/internal/operators/takeLast.js
function takeLast(count) {
  return count <= 0 ? () => EMPTY : operate((source, subscriber) => {
    let buffer = [];
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      buffer.push(value);
      count < buffer.length && buffer.shift();
    }, () => {
      for (const value of buffer) {
        subscriber.next(value);
      }
      subscriber.complete();
    }, void 0, () => {
      buffer = null;
    }));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/startWith.js
function startWith(...values) {
  const scheduler = popScheduler(values);
  return operate((source, subscriber) => {
    (scheduler ? concat(values, source, scheduler) : concat(values, source)).subscribe(subscriber);
  });
}

// node_modules/rxjs/dist/esm/internal/operators/switchMap.js
function switchMap(project, resultSelector) {
  return operate((source, subscriber) => {
    let innerSubscriber = null;
    let index = 0;
    let isComplete = false;
    const checkComplete = () => isComplete && !innerSubscriber && subscriber.complete();
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      innerSubscriber === null || innerSubscriber === void 0 ? void 0 : innerSubscriber.unsubscribe();
      let innerIndex = 0;
      const outerIndex = index++;
      innerFrom(project(value, outerIndex)).subscribe(innerSubscriber = createOperatorSubscriber(subscriber, (innerValue) => subscriber.next(resultSelector ? resultSelector(value, innerValue, outerIndex, innerIndex++) : innerValue), () => {
        innerSubscriber = null;
        checkComplete();
      }));
    }, () => {
      isComplete = true;
      checkComplete();
    }));
  });
}

// node_modules/rxjs/dist/esm/internal/operators/takeUntil.js
function takeUntil(notifier) {
  return operate((source, subscriber) => {
    innerFrom(notifier).subscribe(createOperatorSubscriber(subscriber, () => subscriber.complete(), noop));
    !subscriber.closed && source.subscribe(subscriber);
  });
}

// node_modules/rxjs/dist/esm/internal/operators/tap.js
function tap(observerOrNext, error, complete) {
  const tapObserver = isFunction(observerOrNext) || error || complete ? { next: observerOrNext, error, complete } : observerOrNext;
  return tapObserver ? operate((source, subscriber) => {
    var _a;
    (_a = tapObserver.subscribe) === null || _a === void 0 ? void 0 : _a.call(tapObserver);
    let isUnsub = true;
    source.subscribe(createOperatorSubscriber(subscriber, (value) => {
      var _a2;
      (_a2 = tapObserver.next) === null || _a2 === void 0 ? void 0 : _a2.call(tapObserver, value);
      subscriber.next(value);
    }, () => {
      var _a2;
      isUnsub = false;
      (_a2 = tapObserver.complete) === null || _a2 === void 0 ? void 0 : _a2.call(tapObserver);
      subscriber.complete();
    }, (err) => {
      var _a2;
      isUnsub = false;
      (_a2 = tapObserver.error) === null || _a2 === void 0 ? void 0 : _a2.call(tapObserver, err);
      subscriber.error(err);
    }, () => {
      var _a2, _b;
      if (isUnsub) {
        (_a2 = tapObserver.unsubscribe) === null || _a2 === void 0 ? void 0 : _a2.call(tapObserver);
      }
      (_b = tapObserver.finalize) === null || _b === void 0 ? void 0 : _b.call(tapObserver);
    }));
  }) : identity;
}

// node_modules/@angular/core/fesm2022/_not_found-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var _currentInjector = void 0;
function getCurrentInjector() {
  return _currentInjector;
}
function setCurrentInjector(injector) {
  const former = _currentInjector;
  _currentInjector = injector;
  return former;
}
var NOT_FOUND = /* @__PURE__ */ Symbol("NotFound");
function isNotFound(e) {
  return e === NOT_FOUND || e?.name === "\u0275NotFound";
}

// node_modules/@angular/core/fesm2022/_untracked-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
function createLinkedSignal(sourceFn, computationFn, equalityFn) {
  const node = Object.create(LINKED_SIGNAL_NODE);
  node.source = sourceFn;
  node.computation = computationFn;
  if (equalityFn != void 0) {
    node.equal = equalityFn;
  }
  const linkedSignalGetter = () => {
    producerUpdateValueVersion(node);
    producerAccessed(node);
    if (node.value === ERRORED) {
      throw node.error;
    }
    return node.value;
  };
  const getter = linkedSignalGetter;
  getter[SIGNAL] = node;
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    getter.toString = () => `[LinkedSignal${node.debugName ? " (" + node.debugName + ")" : ""}: ${String(node.value)}]`;
  }
  runPostProducerCreatedFn(node);
  return getter;
}
function linkedSignalSetFn(node, newValue) {
  producerUpdateValueVersion(node);
  signalSetFn(node, newValue);
  producerMarkClean(node);
}
function linkedSignalUpdateFn(node, updater) {
  producerUpdateValueVersion(node);
  if (node.value === ERRORED) {
    throw node.error;
  }
  signalUpdateFn(node, updater);
  producerMarkClean(node);
}
var LINKED_SIGNAL_NODE = /* @__PURE__ */ (() => {
  return __spreadProps(__spreadValues({}, REACTIVE_NODE), {
    value: UNSET,
    dirty: true,
    error: null,
    equal: defaultEquals,
    kind: "linkedSignal",
    producerMustRecompute(node) {
      return node.value === UNSET || node.value === COMPUTING;
    },
    producerRecomputeValue(node) {
      if (node.value === COMPUTING) {
        throw new Error(typeof ngDevMode !== "undefined" && ngDevMode ? "Detected cycle in computations." : "");
      }
      const oldValue = node.value;
      node.value = COMPUTING;
      const prevConsumer = consumerBeforeComputation(node);
      let newValue;
      let wasEqual = false;
      try {
        const newSourceValue = node.source();
        const oldValueValid = oldValue !== UNSET && oldValue !== ERRORED;
        const prev = oldValueValid ? {
          source: node.sourceValue,
          value: oldValue
        } : void 0;
        newValue = node.computation(newSourceValue, prev);
        node.sourceValue = newSourceValue;
        setActiveConsumer(null);
        wasEqual = oldValueValid && newValue !== ERRORED && node.equal(oldValue, newValue);
      } catch (err) {
        newValue = ERRORED;
        node.error = err;
      } finally {
        consumerAfterComputation(node, prevConsumer);
      }
      if (wasEqual) {
        node.value = oldValue;
        return;
      }
      node.value = newValue;
      node.version++;
    }
  });
})();
function untracked(nonReactiveReadsFn) {
  const prevConsumer = setActiveConsumer(null);
  try {
    return nonReactiveReadsFn();
  } finally {
    setActiveConsumer(prevConsumer);
  }
}

// node_modules/@angular/core/fesm2022/primitives-signals.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var formatter = {
  header: (sig, config2) => {
    if (!isSignal(sig) || config2?.ngSkipFormatting) return null;
    let value;
    try {
      value = sig();
    } catch (e) {
      return ["span", `Signal(\u26A0\uFE0F Error)${e.message ? `: ${e.message}` : ""}`];
    }
    const kind = "computation" in sig[SIGNAL] ? "Computed" : "Signal";
    const isPrimitive = value === null || !Array.isArray(value) && typeof value !== "object";
    return ["span", {}, ["span", {}, `${kind}(`], (() => {
      if (isSignal(value)) {
        return formatter.header(value, config2);
      } else if (isPrimitive && value !== void 0 && typeof value !== "function") {
        return ["object", {
          object: value
        }];
      } else {
        return prettifyPreview(value);
      }
    })(), ["span", {}, `)`]];
  },
  hasBody: (sig, config2) => {
    if (!isSignal(sig)) return false;
    try {
      sig();
    } catch {
      return false;
    }
    return !config2?.ngSkipFormatting;
  },
  body: (sig, config2) => {
    const color = "var(--sys-color-primary)";
    return ["div", {
      style: `background: #FFFFFF10; padding-left: 4px; padding-top: 2px; padding-bottom: 2px;`
    }, ["div", {
      style: `color: ${color}`
    }, "Signal value: "], ["div", {
      style: `padding-left: .5rem;`
    }, ["object", {
      object: sig(),
      config: config2
    }]], ["div", {
      style: `color: ${color}`
    }, "Signal function: "], ["div", {
      style: `padding-left: .5rem;`
    }, ["object", {
      object: sig,
      config: __spreadProps(__spreadValues({}, config2), {
        ngSkipFormatting: true
      })
    }]]];
  }
};
function prettifyPreview(value) {
  if (value === null) return "null";
  if (Array.isArray(value)) return `Array(${value.length})`;
  if (value instanceof Element) return `<${value.tagName.toLowerCase()}>`;
  if (value instanceof URL) return `URL`;
  switch (typeof value) {
    case "undefined": {
      return "undefined";
    }
    case "function": {
      if ("prototype" in value) {
        return "class";
      } else {
        return "() => {\u2026}";
      }
    }
    case "object": {
      if (value.constructor.name === "Object") {
        return "{\u2026}";
      } else {
        return `${value.constructor.name} {}`;
      }
    }
    default: {
      return ["object", {
        object: value,
        config: {
          ngSkipFormatting: true
        }
      }];
    }
  }
}
function isSignal(value) {
  return value[SIGNAL] !== void 0;
}
function installDevToolsSignalFormatter() {
  globalThis.devtoolsFormatters ??= [];
  if (!globalThis.devtoolsFormatters.some((f) => f === formatter)) {
    globalThis.devtoolsFormatters.push(formatter);
  }
}
if (typeof ngDevMode === "undefined" || ngDevMode) {
  installDevToolsSignalFormatter();
}

// node_modules/@angular/core/fesm2022/primitives-di.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */

// node_modules/@angular/core/fesm2022/_effect-chunk2.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var Version = class {
  full;
  major;
  minor;
  patch;
  constructor(full) {
    this.full = full;
    const parts = full.split(".");
    this.major = parts[0];
    this.minor = parts[1];
    this.patch = parts.slice(2).join(".");
  }
};
var VERSION = /* @__PURE__ */ new Version("21.2.17");
var DOC_PAGE_BASE_URL = (() => {
  const full = VERSION.full;
  const isPreRelease = full.includes("-next") || full.includes("-rc") || full === "0.0.0-PLACEHOLDER";
  const prefix = isPreRelease ? "next" : `v${VERSION.major}`;
  return `https://${prefix}.angular.dev`;
})();
var ERROR_DETAILS_PAGE_BASE_URL = (() => {
  return `${DOC_PAGE_BASE_URL}/errors`;
})();
var XSS_SECURITY_URL = "https://angular.dev/best-practices/security#preventing-cross-site-scripting-xss";
var RuntimeError = class extends Error {
  code;
  constructor(code, message) {
    super(formatRuntimeError(code, message));
    this.code = code;
  }
};
function formatRuntimeErrorCode(code) {
  return `NG0${Math.abs(code)}`;
}
function formatRuntimeError(code, message) {
  const fullCode = formatRuntimeErrorCode(code);
  let errorMessage = `${fullCode}${message ? ": " + message : ""}`;
  if (ngDevMode && code < 0) {
    const addPeriodSeparator = !errorMessage.match(/[.,;!?\n]$/);
    const separator = addPeriodSeparator ? "." : "";
    errorMessage = `${errorMessage}${separator} Find more at ${ERROR_DETAILS_PAGE_BASE_URL}/${fullCode}`;
  }
  return errorMessage;
}
var _global = globalThis;
function ngDevModeResetPerfCounters() {
  const locationString = typeof location !== "undefined" ? location.toString() : "";
  const newCounters = {
    hydratedNodes: 0,
    hydratedComponents: 0,
    dehydratedViewsRemoved: 0,
    dehydratedViewsCleanupRuns: 0,
    componentsSkippedHydration: 0,
    deferBlocksWithIncrementalHydration: 0
  };
  const allowNgDevModeTrue = locationString.indexOf("ngDevMode=false") === -1;
  if (!allowNgDevModeTrue) {
    _global["ngDevMode"] = false;
  } else {
    if (typeof _global["ngDevMode"] !== "object") {
      _global["ngDevMode"] = {};
    }
    Object.assign(_global["ngDevMode"], newCounters);
  }
  return newCounters;
}
function initNgDevMode() {
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    if (typeof ngDevMode !== "object" || Object.keys(ngDevMode).length === 0) {
      ngDevModeResetPerfCounters();
    }
    return typeof ngDevMode !== "undefined" && !!ngDevMode;
  }
  return false;
}
function getClosureSafeProperty(objWithPropertyToExtract) {
  for (let key in objWithPropertyToExtract) {
    if (objWithPropertyToExtract[key] === getClosureSafeProperty) {
      return key;
    }
  }
  throw Error(typeof ngDevMode !== "undefined" && ngDevMode ? "Could not find renamed property on target object." : "");
}
function fillProperties(target, source) {
  for (const key in source) {
    if (source.hasOwnProperty(key) && !target.hasOwnProperty(key)) {
      target[key] = source[key];
    }
  }
}
function stringify(token) {
  if (typeof token === "string") {
    return token;
  }
  if (Array.isArray(token)) {
    return `[${token.map(stringify).join(", ")}]`;
  }
  if (token == null) {
    return "" + token;
  }
  const name = token.overriddenName || token.name;
  if (name) {
    return `${name}`;
  }
  const result = token.toString();
  if (result == null) {
    return "" + result;
  }
  const newLineIndex = result.indexOf("\n");
  return newLineIndex >= 0 ? result.slice(0, newLineIndex) : result;
}
function concatStringsWithSpace(before, after) {
  if (!before) return after || "";
  if (!after) return before;
  return `${before} ${after}`;
}
var __forward_ref__ = getClosureSafeProperty({
  __forward_ref__: getClosureSafeProperty
});
function forwardRef(forwardRefFn) {
  forwardRefFn.__forward_ref__ = forwardRef;
  if (ngDevMode) {
    forwardRefFn.toString = function() {
      return stringify(this());
    };
  }
  return forwardRefFn;
}
function resolveForwardRef(type) {
  return isForwardRef(type) ? type() : type;
}
function isForwardRef(fn) {
  return typeof fn === "function" && fn.hasOwnProperty(__forward_ref__) && fn.__forward_ref__ === forwardRef;
}
function assertNumber(actual, msg) {
  if (!(typeof actual === "number")) {
    throwError2(msg, typeof actual, "number", "===");
  }
}
function assertNumberInRange(actual, minInclusive, maxInclusive) {
  assertNumber(actual, "Expected a number");
  assertLessThanOrEqual(actual, maxInclusive, "Expected number to be less than or equal to");
  assertGreaterThanOrEqual(actual, minInclusive, "Expected number to be greater than or equal to");
}
function assertString(actual, msg) {
  if (!(typeof actual === "string")) {
    throwError2(msg, actual === null ? "null" : typeof actual, "string", "===");
  }
}
function assertFunction(actual, msg) {
  if (!(typeof actual === "function")) {
    throwError2(msg, actual === null ? "null" : typeof actual, "function", "===");
  }
}
function assertEqual(actual, expected, msg) {
  if (!(actual == expected)) {
    throwError2(msg, actual, expected, "==");
  }
}
function assertNotEqual(actual, expected, msg) {
  if (!(actual != expected)) {
    throwError2(msg, actual, expected, "!=");
  }
}
function assertSame(actual, expected, msg) {
  if (!(actual === expected)) {
    throwError2(msg, actual, expected, "===");
  }
}
function assertNotSame(actual, expected, msg) {
  if (!(actual !== expected)) {
    throwError2(msg, actual, expected, "!==");
  }
}
function assertLessThan(actual, expected, msg) {
  if (!(actual < expected)) {
    throwError2(msg, actual, expected, "<");
  }
}
function assertLessThanOrEqual(actual, expected, msg) {
  if (!(actual <= expected)) {
    throwError2(msg, actual, expected, "<=");
  }
}
function assertGreaterThan(actual, expected, msg) {
  if (!(actual > expected)) {
    throwError2(msg, actual, expected, ">");
  }
}
function assertGreaterThanOrEqual(actual, expected, msg) {
  if (!(actual >= expected)) {
    throwError2(msg, actual, expected, ">=");
  }
}
function assertDefined(actual, msg) {
  if (actual == null) {
    throwError2(msg, actual, null, "!=");
  }
}
function throwError2(msg, actual, expected, comparison) {
  throw new Error(`ASSERTION ERROR: ${msg}` + (comparison == null ? "" : ` [Expected=> ${expected} ${comparison} ${actual} <=Actual]`));
}
function assertDomNode(node) {
  if (!(node instanceof Node)) {
    throwError2(`The provided value must be an instance of a DOM Node but got ${stringify(node)}`);
  }
}
function assertElement(node) {
  if (!(node instanceof Element)) {
    throwError2(`The provided value must be an element but got ${stringify(node)}`);
  }
}
function assertIndexInRange(arr, index) {
  assertDefined(arr, "Array must be defined.");
  const maxLen = arr.length;
  if (index < 0 || index >= maxLen) {
    throwError2(`Index expected to be less than ${maxLen} but got ${index}`);
  }
}
function assertOneOf(value, ...validValues) {
  if (validValues.indexOf(value) !== -1) return true;
  throwError2(`Expected value to be one of ${JSON.stringify(validValues)} but was ${JSON.stringify(value)}.`);
}
function assertNotReactive(fn) {
  if (getActiveConsumer() !== null) {
    throwError2(`${fn}() should never be called in a reactive context.`);
  }
}
function \u0275\u0275defineInjectable(opts) {
  return {
    token: opts.token,
    providedIn: opts.providedIn || null,
    factory: opts.factory,
    value: void 0
  };
}
function \u0275\u0275defineInjector(options) {
  return {
    providers: options.providers || [],
    imports: options.imports || []
  };
}
function getInjectableDef(type) {
  return getOwnDefinition(type, NG_PROV_DEF);
}
function isInjectable(type) {
  return getInjectableDef(type) !== null;
}
function getOwnDefinition(type, field) {
  return type.hasOwnProperty(field) && type[field] || null;
}
function getInheritedInjectableDef(type) {
  const def = type?.[NG_PROV_DEF] ?? null;
  if (def) {
    ngDevMode && console.warn(`DEPRECATED: DI is instantiating a token "${type.name}" that inherits its @Injectable decorator but does not provide one itself.
This will become an error in a future version of Angular. Please add @Injectable() to the "${type.name}" class.`);
    return def;
  } else {
    return null;
  }
}
function getInjectorDef(type) {
  return type && type.hasOwnProperty(NG_INJ_DEF) ? type[NG_INJ_DEF] : null;
}
var NG_PROV_DEF = getClosureSafeProperty({
  \u0275prov: getClosureSafeProperty
});
var NG_INJ_DEF = getClosureSafeProperty({
  \u0275inj: getClosureSafeProperty
});
var InjectionToken = class {
  _desc;
  ngMetadataName = "InjectionToken";
  \u0275prov;
  constructor(_desc, options) {
    this._desc = _desc;
    this.\u0275prov = void 0;
    if (typeof options == "number") {
      (typeof ngDevMode === "undefined" || ngDevMode) && assertLessThan(options, 0, "Only negative numbers are supported here");
      this.__NG_ELEMENT_ID__ = options;
    } else if (options !== void 0) {
      this.\u0275prov = \u0275\u0275defineInjectable({
        token: this,
        providedIn: options.providedIn || "root",
        factory: options.factory
      });
    }
  }
  get multi() {
    return this;
  }
  toString() {
    return `InjectionToken ${this._desc}`;
  }
};
var _injectorProfilerContext;
function getInjectorProfilerContext() {
  !ngDevMode && throwError2("getInjectorProfilerContext should never be called in production mode");
  return _injectorProfilerContext;
}
function setInjectorProfilerContext(context2) {
  !ngDevMode && throwError2("setInjectorProfilerContext should never be called in production mode");
  const previous = _injectorProfilerContext;
  _injectorProfilerContext = context2;
  return previous;
}
var injectorProfilerCallbacks = [];
var NOOP_PROFILER_REMOVAL = () => {
};
function removeProfiler(profiler2) {
  const profilerIdx = injectorProfilerCallbacks.indexOf(profiler2);
  if (profilerIdx !== -1) {
    injectorProfilerCallbacks.splice(profilerIdx, 1);
  }
}
function setInjectorProfiler(injectorProfiler2) {
  !ngDevMode && throwError2("setInjectorProfiler should never be called in production mode");
  if (injectorProfiler2 !== null) {
    if (!injectorProfilerCallbacks.includes(injectorProfiler2)) {
      injectorProfilerCallbacks.push(injectorProfiler2);
    }
    return () => removeProfiler(injectorProfiler2);
  } else {
    injectorProfilerCallbacks.length = 0;
    return NOOP_PROFILER_REMOVAL;
  }
}
function injectorProfiler(event) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  for (let i = 0; i < injectorProfilerCallbacks.length; i++) {
    const injectorProfilerCallback = injectorProfilerCallbacks[i];
    injectorProfilerCallback(event);
  }
}
function emitProviderConfiguredEvent(eventProvider, isViewProvider = false) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  let token;
  if (typeof eventProvider === "function") {
    token = eventProvider;
  } else if (eventProvider instanceof InjectionToken) {
    token = eventProvider;
  } else {
    token = resolveForwardRef(eventProvider.provide);
  }
  let provider = eventProvider;
  if (eventProvider instanceof InjectionToken) {
    provider = eventProvider.\u0275prov || eventProvider;
  }
  injectorProfiler({
    type: 2,
    context: getInjectorProfilerContext(),
    providerRecord: {
      token,
      provider,
      isViewProvider
    }
  });
}
function emitInjectorToCreateInstanceEvent(token) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  injectorProfiler({
    type: 5,
    context: getInjectorProfilerContext(),
    token
  });
}
function emitInstanceCreatedByInjectorEvent(instance) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  injectorProfiler({
    type: 1,
    context: getInjectorProfilerContext(),
    instance: {
      value: instance
    }
  });
}
function emitInjectEvent(token, value, flags) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  injectorProfiler({
    type: 0,
    context: getInjectorProfilerContext(),
    service: {
      token,
      value,
      flags
    }
  });
}
function emitEffectCreatedEvent(effect2) {
  !ngDevMode && throwError2("Injector profiler should never be called in production mode");
  injectorProfiler({
    type: 3,
    context: getInjectorProfilerContext(),
    effect: effect2
  });
}
function runInInjectorProfilerContext(injector, token, callback) {
  !ngDevMode && throwError2("runInInjectorProfilerContext should never be called in production mode");
  const prevInjectContext = setInjectorProfilerContext({
    injector,
    token
  });
  try {
    callback();
  } finally {
    setInjectorProfilerContext(prevInjectContext);
  }
}
function isEnvironmentProviders(value) {
  return value && !!value.\u0275providers;
}
var NG_COMP_DEF = getClosureSafeProperty({
  \u0275cmp: getClosureSafeProperty
});
var NG_DIR_DEF = getClosureSafeProperty({
  \u0275dir: getClosureSafeProperty
});
var NG_PIPE_DEF = getClosureSafeProperty({
  \u0275pipe: getClosureSafeProperty
});
var NG_MOD_DEF = getClosureSafeProperty({
  \u0275mod: getClosureSafeProperty
});
var NG_FACTORY_DEF = getClosureSafeProperty({
  \u0275fac: getClosureSafeProperty
});
var NG_ELEMENT_ID = getClosureSafeProperty({
  __NG_ELEMENT_ID__: getClosureSafeProperty
});
var NG_ENV_ID = getClosureSafeProperty({
  __NG_ENV_ID__: getClosureSafeProperty
});
function getNgModuleDef(type) {
  assertTypeDefined(type, "@NgModule");
  return type[NG_MOD_DEF] || null;
}
function getNgModuleDefOrThrow(type) {
  const ngModuleDef = getNgModuleDef(type);
  if (!ngModuleDef) {
    throw new RuntimeError(915, (typeof ngDevMode === "undefined" || ngDevMode) && `Type ${stringify(type)} does not have '\u0275mod' property.`);
  }
  return ngModuleDef;
}
function getComponentDef(type) {
  assertTypeDefined(type, "@Component");
  return type[NG_COMP_DEF] || null;
}
function getDirectiveDefOrThrow(type) {
  const def = getDirectiveDef(type);
  if (!def) {
    throw new RuntimeError(916, (typeof ngDevMode === "undefined" || ngDevMode) && `Type ${stringify(type)} does not have '\u0275dir' property.`);
  }
  return def;
}
function getDirectiveDef(type) {
  assertTypeDefined(type, "@Directive");
  return type[NG_DIR_DEF] || null;
}
function getPipeDef(type) {
  assertTypeDefined(type, "@Pipe");
  return type[NG_PIPE_DEF] || null;
}
function assertTypeDefined(type, symbolType) {
  if (type == null) {
    throw new RuntimeError(-919, (typeof ngDevMode === "undefined" || ngDevMode) && `Cannot read ${symbolType} metadata. This can indicate a runtime circular dependency in your app that needs to be resolved.`);
  }
}
function isStandalone(type) {
  const def = getComponentDef(type) || getDirectiveDef(type) || getPipeDef(type);
  return def !== null && def.standalone;
}
function renderStringify(value) {
  if (typeof value === "string") return value;
  if (value == null) return "";
  return String(value);
}
function stringifyForError(value) {
  if (typeof value === "function") return value.name || value.toString();
  if (typeof value === "object" && value != null && typeof value.type === "function") {
    return value.type.name || value.type.toString();
  }
  return renderStringify(value);
}
function debugStringifyTypeForError(type) {
  const componentDef = getComponentDef(type);
  if (componentDef !== null && componentDef.debugInfo) {
    return stringifyTypeFromDebugInfo(componentDef.debugInfo);
  }
  return stringifyForError(type);
}
function stringifyTypeFromDebugInfo(debugInfo) {
  if (!debugInfo.filePath || !debugInfo.lineNumber) {
    return debugInfo.className;
  } else {
    return `${debugInfo.className} (at ${debugInfo.filePath}:${debugInfo.lineNumber})`;
  }
}
var NG_RUNTIME_ERROR_CODE = getClosureSafeProperty({
  "ngErrorCode": getClosureSafeProperty
});
var NG_RUNTIME_ERROR_MESSAGE = getClosureSafeProperty({
  "ngErrorMessage": getClosureSafeProperty
});
var NG_TOKEN_PATH = getClosureSafeProperty({
  "ngTokenPath": getClosureSafeProperty
});
function cyclicDependencyError(token, path) {
  const message = ngDevMode ? `Circular dependency detected for \`${token}\`.` : "";
  return createRuntimeError(message, -200, path);
}
function cyclicDependencyErrorWithDetails(token, path) {
  return augmentRuntimeError(cyclicDependencyError(token, path), null);
}
function throwMixedMultiProviderError() {
  throw new Error(`Cannot mix multi providers and regular providers`);
}
function throwInvalidProviderError(ngModuleType, providers, provider) {
  if (ngModuleType && providers) {
    const providerDetail = providers.map((v) => v == provider ? "?" + provider + "?" : "...");
    throw new Error(`Invalid provider for the NgModule '${stringify(ngModuleType)}' - only instances of Provider and Type are allowed, got: [${providerDetail.join(", ")}]`);
  } else if (isEnvironmentProviders(provider)) {
    if (provider.\u0275fromNgModule) {
      throw new RuntimeError(-207, `Invalid providers from 'importProvidersFrom' present in a non-environment injector. 'importProvidersFrom' can't be used for component providers.`);
    } else {
      throw new RuntimeError(-207, `Invalid providers present in a non-environment injector. 'EnvironmentProviders' can't be used for component providers.`);
    }
  } else {
    throw new Error("Invalid provider");
  }
}
function throwProviderNotFoundError(token, injectorName) {
  const errorMessage = ngDevMode && `No provider for ${stringifyForError(token)} found${injectorName ? ` in ${injectorName}` : ""}`;
  throw new RuntimeError(-201, errorMessage);
}
function prependTokenToDependencyPath(error, token) {
  error[NG_TOKEN_PATH] ??= [];
  const currentPath = error[NG_TOKEN_PATH];
  let pathStr;
  if (typeof token === "object" && "multi" in token && token?.multi === true) {
    assertDefined(token.provide, "Token with multi: true should have a provide property");
    pathStr = stringifyForError(token.provide);
  } else {
    pathStr = stringifyForError(token);
  }
  if (currentPath[0] !== pathStr) {
    error[NG_TOKEN_PATH].unshift(pathStr);
  }
}
function augmentRuntimeError(error, source) {
  const tokenPath = error[NG_TOKEN_PATH];
  const errorCode = error[NG_RUNTIME_ERROR_CODE];
  const message = error[NG_RUNTIME_ERROR_MESSAGE] || error.message;
  error.message = formatErrorMessage(message, errorCode, tokenPath, source);
  return error;
}
function createRuntimeError(message, code, path) {
  const error = new RuntimeError(code, message);
  error[NG_RUNTIME_ERROR_CODE] = code;
  error[NG_RUNTIME_ERROR_MESSAGE] = message;
  if (path) {
    error[NG_TOKEN_PATH] = path;
  }
  return error;
}
function getRuntimeErrorCode(error) {
  return error[NG_RUNTIME_ERROR_CODE];
}
function formatErrorMessage(text, code, path = [], source = null) {
  let pathDetails = "";
  if (path && path.length > 1) {
    pathDetails = ` Path: ${path.join(" -> ")}.`;
  }
  const sourceDetails = source ? ` Source: ${source}.` : "";
  return formatRuntimeError(code, `${text}${sourceDetails}${pathDetails}`);
}
var _injectImplementation;
function getInjectImplementation() {
  return _injectImplementation;
}
function setInjectImplementation(impl) {
  const previous = _injectImplementation;
  _injectImplementation = impl;
  return previous;
}
function injectRootLimpMode(token, notFoundValue, flags) {
  const injectableDef = getInjectableDef(token);
  if (injectableDef && injectableDef.providedIn == "root") {
    return injectableDef.value === void 0 ? injectableDef.value = injectableDef.factory() : injectableDef.value;
  }
  if (flags & 8) return null;
  if (notFoundValue !== void 0) return notFoundValue;
  throwProviderNotFoundError(token, typeof ngDevMode !== "undefined" && ngDevMode ? "Injector" : "");
}
function assertInjectImplementationNotEqual(fn) {
  ngDevMode && assertNotEqual(_injectImplementation, fn, "Calling \u0275\u0275inject would cause infinite recursion");
}
var _THROW_IF_NOT_FOUND = {};
var THROW_IF_NOT_FOUND = _THROW_IF_NOT_FOUND;
var DI_DECORATOR_FLAG = "__NG_DI_FLAG__";
var RetrievingInjector = class {
  injector;
  constructor(injector) {
    this.injector = injector;
  }
  retrieve(token, options) {
    const flags = convertToBitFlags(options) || 0;
    try {
      return this.injector.get(token, flags & 8 ? null : THROW_IF_NOT_FOUND, flags);
    } catch (e) {
      if (isNotFound(e)) {
        return e;
      }
      throw e;
    }
  }
};
function injectInjectorOnly(token, flags = 0) {
  const currentInjector = getCurrentInjector();
  if (currentInjector === void 0) {
    throw new RuntimeError(-203, ngDevMode && `The \`${stringify(token)}\` token injection failed. \`inject()\` function must be called from an injection context such as a constructor, a factory function, a field initializer, or a function used with \`runInInjectionContext\`.`);
  } else if (currentInjector === null) {
    return injectRootLimpMode(token, void 0, flags);
  } else {
    const options = convertToInjectOptions(flags);
    const value = currentInjector.retrieve(token, options);
    ngDevMode && emitInjectEvent(token, value, flags);
    if (isNotFound(value)) {
      if (options.optional) {
        return null;
      }
      throw value;
    }
    return value;
  }
}
function \u0275\u0275inject(token, flags = 0) {
  return (getInjectImplementation() || injectInjectorOnly)(resolveForwardRef(token), flags);
}
function \u0275\u0275invalidFactoryDep(index) {
  throw new RuntimeError(202, ngDevMode && `This constructor is not compatible with Angular Dependency Injection because its dependency at index ${index} of the parameter list is invalid.
This can happen if the dependency type is a primitive like a string or if an ancestor of this class is missing an Angular decorator.

Please check that 1) the type for the parameter at index ${index} is correct and 2) the correct Angular decorators are defined for this class and its ancestors.`);
}
function inject2(token, options) {
  return \u0275\u0275inject(token, convertToBitFlags(options));
}
function convertToBitFlags(flags) {
  if (typeof flags === "undefined" || typeof flags === "number") {
    return flags;
  }
  return 0 | (flags.optional && 8) | (flags.host && 1) | (flags.self && 2) | (flags.skipSelf && 4);
}
function convertToInjectOptions(flags) {
  return {
    optional: !!(flags & 8),
    host: !!(flags & 1),
    self: !!(flags & 2),
    skipSelf: !!(flags & 4)
  };
}
function injectArgs(types) {
  const args = [];
  for (let i = 0; i < types.length; i++) {
    const arg = resolveForwardRef(types[i]);
    if (Array.isArray(arg)) {
      if (arg.length === 0) {
        throw new RuntimeError(900, ngDevMode && "Arguments array must have arguments.");
      }
      let type = void 0;
      let flags = 0;
      for (let j = 0; j < arg.length; j++) {
        const meta = arg[j];
        const flag = getInjectFlag(meta);
        if (typeof flag === "number") {
          if (flag === -1) {
            type = meta.token;
          } else {
            flags |= flag;
          }
        } else {
          type = meta;
        }
      }
      args.push(\u0275\u0275inject(type, flags));
    } else {
      args.push(\u0275\u0275inject(arg));
    }
  }
  return args;
}
function attachInjectFlag(decorator, flag) {
  decorator[DI_DECORATOR_FLAG] = flag;
  decorator.prototype[DI_DECORATOR_FLAG] = flag;
  return decorator;
}
function getInjectFlag(token) {
  return token[DI_DECORATOR_FLAG];
}
function getFactoryDef(type, throwNotFound) {
  const hasFactoryDef = type.hasOwnProperty(NG_FACTORY_DEF);
  if (!hasFactoryDef && throwNotFound === true && ngDevMode) {
    throw new Error(`Type ${stringify(type)} does not have '\u0275fac' property.`);
  }
  return hasFactoryDef ? type[NG_FACTORY_DEF] : null;
}
function arrayEquals(a, b, identityAccessor) {
  if (a.length !== b.length) return false;
  for (let i = 0; i < a.length; i++) {
    let valueA = a[i];
    let valueB = b[i];
    if (identityAccessor) {
      valueA = identityAccessor(valueA);
      valueB = identityAccessor(valueB);
    }
    if (valueB !== valueA) {
      return false;
    }
  }
  return true;
}
function flatten(list) {
  return list.flat(Number.POSITIVE_INFINITY);
}
function deepForEach(input2, fn) {
  input2.forEach((value) => Array.isArray(value) ? deepForEach(value, fn) : fn(value));
}
function addToArray(arr, index, value) {
  if (index >= arr.length) {
    arr.push(value);
  } else {
    arr.splice(index, 0, value);
  }
}
function removeFromArray(arr, index) {
  if (index >= arr.length - 1) {
    return arr.pop();
  } else {
    return arr.splice(index, 1)[0];
  }
}
function newArray(size, value) {
  const list = [];
  for (let i = 0; i < size; i++) {
    list.push(value);
  }
  return list;
}
function arraySplice(array, index, count) {
  const length = array.length - count;
  while (index < length) {
    array[index] = array[index + count];
    index++;
  }
  while (count--) {
    array.pop();
  }
}
function arrayInsert2(array, index, value1, value2) {
  ngDevMode && assertLessThanOrEqual(index, array.length, "Can't insert past array end.");
  let end = array.length;
  if (end == index) {
    array.push(value1, value2);
  } else if (end === 1) {
    array.push(value2, array[0]);
    array[0] = value1;
  } else {
    end--;
    array.push(array[end - 1], array[end]);
    while (end > index) {
      const previousEnd = end - 2;
      array[end] = array[previousEnd];
      end--;
    }
    array[index] = value1;
    array[index + 1] = value2;
  }
}
function keyValueArraySet(keyValueArray, key, value) {
  let index = keyValueArrayIndexOf(keyValueArray, key);
  if (index >= 0) {
    keyValueArray[index | 1] = value;
  } else {
    index = ~index;
    arrayInsert2(keyValueArray, index, key, value);
  }
  return index;
}
function keyValueArrayGet(keyValueArray, key) {
  const index = keyValueArrayIndexOf(keyValueArray, key);
  if (index >= 0) {
    return keyValueArray[index | 1];
  }
  return void 0;
}
function keyValueArrayIndexOf(keyValueArray, key) {
  return _arrayIndexOfSorted(keyValueArray, key, 1);
}
function _arrayIndexOfSorted(array, value, shift) {
  ngDevMode && assertEqual(Array.isArray(array), true, "Expecting an array");
  let start = 0;
  let end = array.length >> shift;
  while (end !== start) {
    const middle = start + (end - start >> 1);
    const current = array[middle << shift];
    if (value === current) {
      return middle << shift;
    } else if (current > value) {
      end = middle;
    } else {
      start = middle + 1;
    }
  }
  return ~(end << shift);
}
var EMPTY_OBJ = {};
var EMPTY_ARRAY = [];
if ((typeof ngDevMode === "undefined" || ngDevMode) && initNgDevMode()) {
  Object.freeze(EMPTY_OBJ);
  Object.freeze(EMPTY_ARRAY);
}
var ENVIRONMENT_INITIALIZER = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "ENVIRONMENT_INITIALIZER" : "");
var INJECTOR$1 = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "INJECTOR" : "", -1);
var INJECTOR_DEF_TYPES = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "INJECTOR_DEF_TYPES" : "");
var NullInjector = class {
  get(token, notFoundValue = THROW_IF_NOT_FOUND) {
    if (notFoundValue === THROW_IF_NOT_FOUND) {
      const message = ngDevMode ? `No provider found for \`${stringify(token)}\`.` : "";
      const error = createRuntimeError(message, -201);
      error.name = "\u0275NotFound";
      throw error;
    }
    return notFoundValue;
  }
};
function makeEnvironmentProviders(providers) {
  return {
    \u0275providers: providers
  };
}
function provideEnvironmentInitializer(initializerFn) {
  return makeEnvironmentProviders([{
    provide: ENVIRONMENT_INITIALIZER,
    multi: true,
    useValue: initializerFn
  }]);
}
function importProvidersFrom(...sources) {
  return {
    \u0275providers: internalImportProvidersFrom(true, sources),
    \u0275fromNgModule: true
  };
}
function internalImportProvidersFrom(checkForStandaloneCmp, ...sources) {
  const providersOut = [];
  const dedup = /* @__PURE__ */ new Set();
  let injectorTypesWithProviders;
  const collectProviders = (provider) => {
    providersOut.push(provider);
  };
  deepForEach(sources, (source) => {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && checkForStandaloneCmp) {
      const cmpDef = getComponentDef(source);
      if (cmpDef?.standalone) {
        throw new RuntimeError(800, `Importing providers supports NgModule or ModuleWithProviders but got a standalone component "${stringifyForError(source)}"`);
      }
    }
    const internalSource = source;
    if (walkProviderTree(internalSource, collectProviders, [], dedup)) {
      injectorTypesWithProviders ||= [];
      injectorTypesWithProviders.push(internalSource);
    }
  });
  if (injectorTypesWithProviders !== void 0) {
    processInjectorTypesWithProviders(injectorTypesWithProviders, collectProviders);
  }
  return providersOut;
}
function processInjectorTypesWithProviders(typesWithProviders, visitor) {
  for (let i = 0; i < typesWithProviders.length; i++) {
    const {
      ngModule,
      providers
    } = typesWithProviders[i];
    deepForEachProvider(providers, (provider) => {
      ngDevMode && validateProvider(provider, providers || EMPTY_ARRAY, ngModule);
      visitor(provider, ngModule);
    });
  }
}
function walkProviderTree(container, visitor, parents, dedup) {
  container = resolveForwardRef(container);
  if (!container) return false;
  let defType = null;
  let injDef = getInjectorDef(container);
  const cmpDef = !injDef && getComponentDef(container);
  if (!injDef && !cmpDef) {
    const ngModule = container.ngModule;
    injDef = getInjectorDef(ngModule);
    if (injDef) {
      defType = ngModule;
    } else {
      return false;
    }
  } else if (cmpDef && !cmpDef.standalone) {
    return false;
  } else {
    defType = container;
  }
  if (ngDevMode && parents.indexOf(defType) !== -1) {
    const defName = stringify(defType);
    const path = parents.map(stringify).concat(defName);
    throw cyclicDependencyErrorWithDetails(defName, path);
  }
  const isDuplicate = dedup.has(defType);
  if (cmpDef) {
    if (isDuplicate) {
      return false;
    }
    dedup.add(defType);
    if (cmpDef.dependencies) {
      const deps = typeof cmpDef.dependencies === "function" ? cmpDef.dependencies() : cmpDef.dependencies;
      for (const dep of deps) {
        walkProviderTree(dep, visitor, parents, dedup);
      }
    }
  } else if (injDef) {
    if (injDef.imports != null && !isDuplicate) {
      ngDevMode && parents.push(defType);
      dedup.add(defType);
      let importTypesWithProviders;
      try {
        deepForEach(injDef.imports, (imported) => {
          if (walkProviderTree(imported, visitor, parents, dedup)) {
            importTypesWithProviders ||= [];
            importTypesWithProviders.push(imported);
          }
        });
      } finally {
        ngDevMode && parents.pop();
      }
      if (importTypesWithProviders !== void 0) {
        processInjectorTypesWithProviders(importTypesWithProviders, visitor);
      }
    }
    if (!isDuplicate) {
      const factory = getFactoryDef(defType) || (() => new defType());
      visitor({
        provide: defType,
        useFactory: factory,
        deps: EMPTY_ARRAY
      }, defType);
      visitor({
        provide: INJECTOR_DEF_TYPES,
        useValue: defType,
        multi: true
      }, defType);
      visitor({
        provide: ENVIRONMENT_INITIALIZER,
        useValue: () => \u0275\u0275inject(defType),
        multi: true
      }, defType);
    }
    const defProviders = injDef.providers;
    if (defProviders != null && !isDuplicate) {
      const injectorType = container;
      deepForEachProvider(defProviders, (provider) => {
        ngDevMode && validateProvider(provider, defProviders, injectorType);
        visitor(provider, injectorType);
      });
    }
  } else {
    return false;
  }
  return defType !== container && container.providers !== void 0;
}
function validateProvider(provider, providers, containerType) {
  if (isTypeProvider(provider) || isValueProvider(provider) || isFactoryProvider(provider) || isExistingProvider(provider)) {
    return;
  }
  const classRef = resolveForwardRef(provider && (provider.useClass || provider.provide));
  if (!classRef) {
    throwInvalidProviderError(containerType, providers, provider);
  }
}
function deepForEachProvider(providers, fn) {
  for (let provider of providers) {
    if (isEnvironmentProviders(provider)) {
      provider = provider.\u0275providers;
    }
    if (Array.isArray(provider)) {
      deepForEachProvider(provider, fn);
    } else {
      fn(provider);
    }
  }
}
var USE_VALUE = getClosureSafeProperty({
  provide: String,
  useValue: getClosureSafeProperty
});
function isValueProvider(value) {
  return value !== null && typeof value == "object" && USE_VALUE in value;
}
function isExistingProvider(value) {
  return !!(value && value.useExisting);
}
function isFactoryProvider(value) {
  return !!(value && value.useFactory);
}
function isTypeProvider(value) {
  return typeof value === "function";
}
function isClassProvider(value) {
  return !!value.useClass;
}
var INJECTOR_SCOPE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "Set Injector scope." : "");
var NOT_YET = {};
var CIRCULAR = {};
var NULL_INJECTOR = void 0;
function getNullInjector() {
  if (NULL_INJECTOR === void 0) {
    NULL_INJECTOR = new NullInjector();
  }
  return NULL_INJECTOR;
}
var EnvironmentInjector = class {
};
var R3Injector = class extends EnvironmentInjector {
  parent;
  source;
  scopes;
  records = /* @__PURE__ */ new Map();
  _ngOnDestroyHooks = /* @__PURE__ */ new Set();
  _onDestroyHooks = [];
  get destroyed() {
    return this._destroyed;
  }
  _destroyed = false;
  injectorDefTypes;
  constructor(providers, parent, source, scopes) {
    super();
    this.parent = parent;
    this.source = source;
    this.scopes = scopes;
    forEachSingleProvider(providers, (provider) => this.processProvider(provider));
    this.records.set(INJECTOR$1, makeRecord(void 0, this));
    if (scopes.has("environment")) {
      this.records.set(EnvironmentInjector, makeRecord(void 0, this));
    }
    const record = this.records.get(INJECTOR_SCOPE);
    if (record != null && typeof record.value === "string") {
      this.scopes.add(record.value);
    }
    this.injectorDefTypes = new Set(this.get(INJECTOR_DEF_TYPES, EMPTY_ARRAY, {
      self: true
    }));
  }
  retrieve(token, options) {
    const flags = convertToBitFlags(options) || 0;
    try {
      return this.get(token, THROW_IF_NOT_FOUND, flags);
    } catch (e) {
      if (isNotFound(e)) {
        return e;
      }
      throw e;
    }
  }
  destroy() {
    assertNotDestroyed(this);
    this._destroyed = true;
    const prevConsumer = setActiveConsumer(null);
    try {
      for (const service of this._ngOnDestroyHooks) {
        service.ngOnDestroy();
      }
      const onDestroyHooks = this._onDestroyHooks;
      this._onDestroyHooks = [];
      for (const hook of onDestroyHooks) {
        hook();
      }
    } finally {
      this.records.clear();
      this._ngOnDestroyHooks.clear();
      this.injectorDefTypes.clear();
      setActiveConsumer(prevConsumer);
    }
  }
  onDestroy(callback) {
    assertNotDestroyed(this);
    this._onDestroyHooks.push(callback);
    return () => this.removeOnDestroy(callback);
  }
  runInContext(fn) {
    assertNotDestroyed(this);
    const previousInjector = setCurrentInjector(this);
    const previousInjectImplementation = setInjectImplementation(void 0);
    let prevInjectContext;
    if (ngDevMode) {
      prevInjectContext = setInjectorProfilerContext({
        injector: this,
        token: null
      });
    }
    try {
      return fn();
    } finally {
      setCurrentInjector(previousInjector);
      setInjectImplementation(previousInjectImplementation);
      ngDevMode && setInjectorProfilerContext(prevInjectContext);
    }
  }
  get(token, notFoundValue = THROW_IF_NOT_FOUND, options) {
    assertNotDestroyed(this);
    if (token.hasOwnProperty(NG_ENV_ID)) {
      return token[NG_ENV_ID](this);
    }
    const flags = convertToBitFlags(options);
    let prevInjectContext;
    if (ngDevMode) {
      prevInjectContext = setInjectorProfilerContext({
        injector: this,
        token
      });
    }
    const previousInjector = setCurrentInjector(this);
    const previousInjectImplementation = setInjectImplementation(void 0);
    try {
      if (!(flags & 4)) {
        let record = this.records.get(token);
        if (record === void 0) {
          const def = couldBeInjectableType(token) && getInjectableDef(token);
          if (def && this.injectableDefInScope(def)) {
            if (ngDevMode) {
              runInInjectorProfilerContext(this, token, () => {
                emitProviderConfiguredEvent(token);
              });
            }
            record = makeRecord(injectableDefOrInjectorDefFactory(token), NOT_YET);
          } else {
            record = null;
          }
          this.records.set(token, record);
        }
        if (record != null) {
          return this.hydrate(token, record, flags);
        }
      }
      const nextInjector = !(flags & 2) ? this.parent : getNullInjector();
      notFoundValue = flags & 8 && notFoundValue === THROW_IF_NOT_FOUND ? null : notFoundValue;
      return nextInjector.get(token, notFoundValue);
    } catch (error) {
      const errorCode = getRuntimeErrorCode(error);
      if (errorCode === -200 || errorCode === -201) {
        if (ngDevMode) {
          prependTokenToDependencyPath(error, token);
          if (previousInjector) {
            throw error;
          } else {
            throw augmentRuntimeError(error, this.source);
          }
        } else {
          throw new RuntimeError(errorCode, null);
        }
      } else {
        throw error;
      }
    } finally {
      setInjectImplementation(previousInjectImplementation);
      setCurrentInjector(previousInjector);
      ngDevMode && setInjectorProfilerContext(prevInjectContext);
    }
  }
  resolveInjectorInitializers() {
    const prevConsumer = setActiveConsumer(null);
    const previousInjector = setCurrentInjector(this);
    const previousInjectImplementation = setInjectImplementation(void 0);
    let prevInjectContext;
    if (ngDevMode) {
      prevInjectContext = setInjectorProfilerContext({
        injector: this,
        token: null
      });
    }
    try {
      const initializers = this.get(ENVIRONMENT_INITIALIZER, EMPTY_ARRAY, {
        self: true
      });
      if (ngDevMode && !Array.isArray(initializers)) {
        throw new RuntimeError(-209, `Unexpected type of the \`ENVIRONMENT_INITIALIZER\` token value (expected an array, but got ${typeof initializers}). Please check that the \`ENVIRONMENT_INITIALIZER\` token is configured as a \`multi: true\` provider.`);
      }
      for (const initializer of initializers) {
        initializer();
      }
    } finally {
      setCurrentInjector(previousInjector);
      setInjectImplementation(previousInjectImplementation);
      ngDevMode && setInjectorProfilerContext(prevInjectContext);
      setActiveConsumer(prevConsumer);
    }
  }
  toString() {
    if (ngDevMode) {
      const tokens = [];
      const records = this.records;
      for (const token of records.keys()) {
        tokens.push(stringify(token));
      }
      return `R3Injector[${tokens.join(", ")}]`;
    }
    return "R3Injector[...]";
  }
  processProvider(provider) {
    provider = resolveForwardRef(provider);
    let token = isTypeProvider(provider) ? provider : resolveForwardRef(provider && provider.provide);
    const record = providerToRecord(provider);
    if (ngDevMode) {
      runInInjectorProfilerContext(this, token, () => {
        if (isValueProvider(provider)) {
          emitInjectorToCreateInstanceEvent(token);
          emitInstanceCreatedByInjectorEvent(provider.useValue);
        }
        emitProviderConfiguredEvent(provider);
      });
    }
    if (!isTypeProvider(provider) && provider.multi === true) {
      let multiRecord = this.records.get(token);
      if (multiRecord) {
        if (ngDevMode && multiRecord.multi === void 0) {
          throwMixedMultiProviderError();
        }
      } else {
        multiRecord = makeRecord(void 0, NOT_YET, true);
        multiRecord.factory = () => injectArgs(multiRecord.multi);
        this.records.set(token, multiRecord);
      }
      token = provider;
      multiRecord.multi.push(provider);
    } else {
      if (ngDevMode) {
        const existing = this.records.get(token);
        if (existing && existing.multi !== void 0) {
          throwMixedMultiProviderError();
        }
      }
    }
    this.records.set(token, record);
  }
  hydrate(token, record, flags) {
    const prevConsumer = setActiveConsumer(null);
    try {
      if (record.value === CIRCULAR) {
        throw cyclicDependencyError(ngDevMode ? stringify(token) : "");
      } else if (record.value === NOT_YET) {
        record.value = CIRCULAR;
        if (ngDevMode) {
          runInInjectorProfilerContext(this, token, () => {
            emitInjectorToCreateInstanceEvent(token);
            record.value = record.factory(void 0, flags);
            emitInstanceCreatedByInjectorEvent(record.value);
          });
        } else {
          record.value = record.factory(void 0, flags);
        }
      }
      if (typeof record.value === "object" && record.value && hasOnDestroy(record.value)) {
        this._ngOnDestroyHooks.add(record.value);
      }
      return record.value;
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
  injectableDefInScope(def) {
    if (!def.providedIn) {
      return false;
    }
    const providedIn = resolveForwardRef(def.providedIn);
    if (typeof providedIn === "string") {
      return providedIn === "any" || this.scopes.has(providedIn);
    } else {
      return this.injectorDefTypes.has(providedIn);
    }
  }
  removeOnDestroy(callback) {
    const destroyCBIdx = this._onDestroyHooks.indexOf(callback);
    if (destroyCBIdx !== -1) {
      this._onDestroyHooks.splice(destroyCBIdx, 1);
    }
  }
};
function injectableDefOrInjectorDefFactory(token) {
  const injectableDef = getInjectableDef(token);
  const factory = injectableDef !== null ? injectableDef.factory : getFactoryDef(token);
  if (factory !== null) {
    return factory;
  }
  if (token instanceof InjectionToken) {
    throw new RuntimeError(-204, ngDevMode && `Token ${stringify(token)} is missing a \u0275prov definition.`);
  }
  if (token instanceof Function) {
    return getUndecoratedInjectableFactory(token);
  }
  throw new RuntimeError(-204, ngDevMode && "unreachable");
}
function getUndecoratedInjectableFactory(token) {
  const paramLength = token.length;
  if (paramLength > 0) {
    throw new RuntimeError(-204, ngDevMode && `Can't resolve all parameters for ${stringify(token)}: (${newArray(paramLength, "?").join(", ")}).`);
  }
  const inheritedInjectableDef = getInheritedInjectableDef(token);
  if (inheritedInjectableDef !== null) {
    return () => inheritedInjectableDef.factory(token);
  } else {
    return () => new token();
  }
}
function providerToRecord(provider) {
  if (isValueProvider(provider)) {
    return makeRecord(void 0, provider.useValue);
  } else {
    const factory = providerToFactory(provider);
    return makeRecord(factory, NOT_YET);
  }
}
function providerToFactory(provider, ngModuleType, providers) {
  let factory = void 0;
  if (ngDevMode && isEnvironmentProviders(provider)) {
    throwInvalidProviderError(void 0, providers, provider);
  }
  if (isTypeProvider(provider)) {
    const unwrappedProvider = resolveForwardRef(provider);
    return getFactoryDef(unwrappedProvider) || injectableDefOrInjectorDefFactory(unwrappedProvider);
  } else {
    if (isValueProvider(provider)) {
      factory = () => resolveForwardRef(provider.useValue);
    } else if (isFactoryProvider(provider)) {
      factory = () => provider.useFactory(...injectArgs(provider.deps || []));
    } else if (isExistingProvider(provider)) {
      factory = (_, flags) => \u0275\u0275inject(resolveForwardRef(provider.useExisting), flags !== void 0 && flags & 8 ? 8 : void 0);
    } else {
      const classRef = resolveForwardRef(provider && (provider.useClass || provider.provide));
      if (ngDevMode && !classRef) {
        throwInvalidProviderError(ngModuleType, providers, provider);
      }
      if (hasDeps(provider)) {
        factory = () => new classRef(...injectArgs(provider.deps));
      } else {
        return getFactoryDef(classRef) || injectableDefOrInjectorDefFactory(classRef);
      }
    }
  }
  return factory;
}
function assertNotDestroyed(injector) {
  if (injector.destroyed) {
    throw new RuntimeError(-205, ngDevMode && "Injector has already been destroyed.");
  }
}
function makeRecord(factory, value, multi = false) {
  return {
    factory,
    value,
    multi: multi ? [] : void 0
  };
}
function hasDeps(value) {
  return !!value.deps;
}
function hasOnDestroy(value) {
  return value !== null && typeof value === "object" && typeof value.ngOnDestroy === "function";
}
function couldBeInjectableType(value) {
  return typeof value === "function" || typeof value === "object" && value.ngMetadataName === "InjectionToken";
}
function forEachSingleProvider(providers, fn) {
  for (const provider of providers) {
    if (Array.isArray(provider)) {
      forEachSingleProvider(provider, fn);
    } else if (provider && isEnvironmentProviders(provider)) {
      forEachSingleProvider(provider.\u0275providers, fn);
    } else {
      fn(provider);
    }
  }
}
function runInInjectionContext(injector, fn) {
  let internalInjector;
  if (injector instanceof R3Injector) {
    assertNotDestroyed(injector);
    internalInjector = injector;
  } else {
    internalInjector = new RetrievingInjector(injector);
  }
  let prevInjectorProfilerContext;
  if (ngDevMode) {
    prevInjectorProfilerContext = setInjectorProfilerContext({
      injector,
      token: null
    });
  }
  const prevInjector = setCurrentInjector(internalInjector);
  const previousInjectImplementation = setInjectImplementation(void 0);
  try {
    return fn();
  } finally {
    setCurrentInjector(prevInjector);
    ngDevMode && setInjectorProfilerContext(prevInjectorProfilerContext);
    setInjectImplementation(previousInjectImplementation);
  }
}
function isInInjectionContext() {
  return getInjectImplementation() !== void 0 || getCurrentInjector() != null;
}
function assertInInjectionContext(debugFn) {
  if (!isInInjectionContext()) {
    throw new RuntimeError(-203, ngDevMode && debugFn.name + "() can only be used within an injection context such as a constructor, a factory function, a field initializer, or a function used with `runInInjectionContext`");
  }
}
var HOST = 0;
var TVIEW = 1;
var FLAGS = 2;
var PARENT = 3;
var NEXT = 4;
var T_HOST = 5;
var HYDRATION = 6;
var CLEANUP = 7;
var CONTEXT = 8;
var INJECTOR = 9;
var ENVIRONMENT = 10;
var RENDERER = 11;
var CHILD_HEAD = 12;
var CHILD_TAIL = 13;
var DECLARATION_VIEW = 14;
var DECLARATION_COMPONENT_VIEW = 15;
var DECLARATION_LCONTAINER = 16;
var PREORDER_HOOK_FLAGS = 17;
var QUERIES = 18;
var ID = 19;
var EMBEDDED_VIEW_INJECTOR = 20;
var ON_DESTROY_HOOKS = 21;
var EFFECTS_TO_SCHEDULE = 22;
var EFFECTS = 23;
var REACTIVE_TEMPLATE_CONSUMER = 24;
var AFTER_RENDER_SEQUENCES_TO_ADD = 25;
var ANIMATIONS = 26;
var HEADER_OFFSET = 27;
var TYPE = 1;
var DEHYDRATED_VIEWS = 6;
var NATIVE = 7;
var VIEW_REFS = 8;
var MOVED_VIEWS = 9;
var CONTAINER_HEADER_OFFSET = 10;
function isLView(value) {
  return Array.isArray(value) && typeof value[TYPE] === "object";
}
function isLContainer(value) {
  return Array.isArray(value) && value[TYPE] === true;
}
function isContentQueryHost(tNode) {
  return (tNode.flags & 4) !== 0;
}
function isComponentHost(tNode) {
  return tNode.componentOffset > -1;
}
function isDirectiveHost(tNode) {
  return (tNode.flags & 1) === 1;
}
function isComponentDef(def) {
  return !!def.template;
}
function isRootView(target) {
  return (target[FLAGS] & 512) !== 0;
}
function isDestroyed(lView) {
  return (lView[FLAGS] & 256) === 256;
}
function assertTNodeForLView(tNode, lView) {
  assertTNodeForTView(tNode, lView[TVIEW]);
}
function assertTNodeCreationIndex(lView, index) {
  const adjustedIndex = index + HEADER_OFFSET;
  assertIndexInRange(lView, adjustedIndex);
  assertLessThan(adjustedIndex, lView[TVIEW].bindingStartIndex, "TNodes should be created before any bindings");
}
function assertTNodeForTView(tNode, tView) {
  assertTNode(tNode);
  const tData = tView.data;
  for (let i = HEADER_OFFSET; i < tData.length; i++) {
    if (tData[i] === tNode) {
      return;
    }
  }
  throwError2("This TNode does not belong to this TView.");
}
function assertTNode(tNode) {
  assertDefined(tNode, "TNode must be defined");
  if (!(tNode && typeof tNode === "object" && tNode.hasOwnProperty("directiveStylingLast"))) {
    throwError2("Not of type TNode, got: " + tNode);
  }
}
function assertTIcu(tIcu) {
  assertDefined(tIcu, "Expected TIcu to be defined");
  if (!(typeof tIcu.currentCaseLViewIndex === "number")) {
    throwError2("Object is not of TIcu type.");
  }
}
function assertComponentType(actual, msg = "Type passed in is not ComponentType, it does not have '\u0275cmp' property.") {
  if (!getComponentDef(actual)) {
    throwError2(msg);
  }
}
function assertNgModuleType(actual, msg = "Type passed in is not NgModuleType, it does not have '\u0275mod' property.") {
  if (!getNgModuleDef(actual)) {
    throwError2(msg);
  }
}
function assertHasParent(tNode) {
  assertDefined(tNode, "currentTNode should exist!");
  assertDefined(tNode.parent, "currentTNode should have a parent");
}
function assertLContainer(value) {
  assertDefined(value, "LContainer must be defined");
  assertEqual(isLContainer(value), true, "Expecting LContainer");
}
function assertLViewOrUndefined(value) {
  value && assertEqual(isLView(value), true, "Expecting LView or undefined or null");
}
function assertLView(value) {
  assertDefined(value, "LView must be defined");
  assertEqual(isLView(value), true, "Expecting LView");
}
function assertFirstCreatePass(tView, errMessage) {
  assertEqual(tView.firstCreatePass, true, errMessage || "Should only be called in first create pass.");
}
function assertFirstUpdatePass(tView, errMessage) {
  assertEqual(tView.firstUpdatePass, true, "Should only be called in first update pass.");
}
function assertDirectiveDef(obj) {
  if (obj.type === void 0 || obj.selectors == void 0 || obj.inputs === void 0) {
    throwError2(`Expected a DirectiveDef/ComponentDef and this object does not seem to have the expected shape.`);
  }
}
function assertIndexInDeclRange(tView, index) {
  assertBetween(HEADER_OFFSET, tView.bindingStartIndex, index);
}
function assertIndexInExpandoRange(lView, index) {
  const tView = lView[1];
  assertBetween(tView.expandoStartIndex, lView.length, index);
}
function assertBetween(lower, upper, index) {
  if (!(lower <= index && index < upper)) {
    throwError2(`Index out of range (expecting ${lower} <= ${index} < ${upper})`);
  }
}
function assertProjectionSlots(lView, errMessage) {
  assertDefined(lView[DECLARATION_COMPONENT_VIEW], "Component views should exist.");
  assertDefined(lView[DECLARATION_COMPONENT_VIEW][T_HOST].projection, "Components with projection nodes (<ng-content>) must have projection slots defined.");
}
function assertParentView(lView, errMessage) {
  assertDefined(lView, "Component views should always have a parent view (component's host view)");
}
function assertNodeInjector(lView, injectorIndex) {
  assertIndexInExpandoRange(lView, injectorIndex);
  assertIndexInExpandoRange(lView, injectorIndex + 8);
  assertNumber(lView[injectorIndex + 0], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 1], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 2], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 3], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 4], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 5], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 6], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 7], "injectorIndex should point to a bloom filter");
  assertNumber(lView[injectorIndex + 8], "injectorIndex should point to parent injector");
}
var SVG_NAMESPACE = "svg";
var MATH_ML_NAMESPACE = "math";
function unwrapRNode(value) {
  while (Array.isArray(value)) {
    value = value[HOST];
  }
  return value;
}
function unwrapLView(value) {
  while (Array.isArray(value)) {
    if (typeof value[TYPE] === "object") return value;
    value = value[HOST];
  }
  return null;
}
function getNativeByIndex(index, lView) {
  ngDevMode && assertIndexInRange(lView, index);
  ngDevMode && assertGreaterThanOrEqual(index, HEADER_OFFSET, "Expected to be past HEADER_OFFSET");
  return unwrapRNode(lView[index]);
}
function getNativeByTNode(tNode, lView) {
  ngDevMode && assertTNodeForLView(tNode, lView);
  ngDevMode && assertIndexInRange(lView, tNode.index);
  const node = unwrapRNode(lView[tNode.index]);
  return node;
}
function getTNode(tView, index) {
  ngDevMode && assertGreaterThan(index, -1, "wrong index for TNode");
  ngDevMode && assertLessThan(index, tView.data.length, "wrong index for TNode");
  const tNode = tView.data[index];
  ngDevMode && tNode !== null && assertTNode(tNode);
  return tNode;
}
function load(view, index) {
  ngDevMode && assertIndexInRange(view, index);
  return view[index];
}
function store(tView, lView, index, value) {
  if (index >= tView.data.length) {
    tView.data[index] = null;
    tView.blueprint[index] = null;
  }
  lView[index] = value;
}
function getComponentLViewByIndex(nodeIndex, hostView) {
  ngDevMode && assertIndexInRange(hostView, nodeIndex);
  const slotValue = hostView[nodeIndex];
  const lView = isLView(slotValue) ? slotValue : slotValue[HOST];
  return lView;
}
function isCreationMode(view) {
  return (view[FLAGS] & 4) === 4;
}
function viewAttachedToChangeDetector(view) {
  return (view[FLAGS] & 128) === 128;
}
function viewAttachedToContainer(view) {
  return isLContainer(view[PARENT]);
}
function getConstant(consts, index) {
  if (index === null || index === void 0) return null;
  ngDevMode && assertIndexInRange(consts, index);
  return consts[index];
}
function resetPreOrderHookFlags(lView) {
  lView[PREORDER_HOOK_FLAGS] = 0;
}
function markViewForRefresh(lView) {
  if (lView[FLAGS] & 1024) {
    return;
  }
  lView[FLAGS] |= 1024;
  if (viewAttachedToChangeDetector(lView)) {
    markAncestorsForTraversal(lView);
  }
}
function walkUpViews(nestingLevel, currentView) {
  while (nestingLevel > 0) {
    ngDevMode && assertDefined(currentView[DECLARATION_VIEW], "Declaration view should be defined if nesting level is greater than 0.");
    currentView = currentView[DECLARATION_VIEW];
    nestingLevel--;
  }
  return currentView;
}
function requiresRefreshOrTraversal(lView) {
  return !!(lView[FLAGS] & (1024 | 8192) || lView[REACTIVE_TEMPLATE_CONSUMER]?.dirty);
}
function updateAncestorTraversalFlagsOnAttach(lView) {
  lView[ENVIRONMENT].changeDetectionScheduler?.notify(8);
  if (lView[FLAGS] & 64) {
    lView[FLAGS] |= 1024;
  }
  if (requiresRefreshOrTraversal(lView)) {
    markAncestorsForTraversal(lView);
  }
}
function markAncestorsForTraversal(lView) {
  lView[ENVIRONMENT].changeDetectionScheduler?.notify(0);
  let parent = getLViewParent(lView);
  while (parent !== null) {
    if (parent[FLAGS] & 8192) {
      break;
    }
    parent[FLAGS] |= 8192;
    if (!viewAttachedToChangeDetector(parent)) {
      break;
    }
    parent = getLViewParent(parent);
  }
}
function storeLViewOnDestroy(lView, onDestroyCallback) {
  if (isDestroyed(lView)) {
    throw new RuntimeError(911, ngDevMode && "View has already been destroyed.");
  }
  if (lView[ON_DESTROY_HOOKS] === null) {
    lView[ON_DESTROY_HOOKS] = [];
  }
  lView[ON_DESTROY_HOOKS].push(onDestroyCallback);
}
function removeLViewOnDestroy(lView, onDestroyCallback) {
  if (lView[ON_DESTROY_HOOKS] === null) return;
  const destroyCBIdx = lView[ON_DESTROY_HOOKS].indexOf(onDestroyCallback);
  if (destroyCBIdx !== -1) {
    lView[ON_DESTROY_HOOKS].splice(destroyCBIdx, 1);
  }
}
function getLViewParent(lView) {
  ngDevMode && assertLView(lView);
  const parent = lView[PARENT];
  return isLContainer(parent) ? parent[PARENT] : parent;
}
function getOrCreateLViewCleanup(view) {
  return view[CLEANUP] ??= [];
}
function getOrCreateTViewCleanup(tView) {
  return tView.cleanup ??= [];
}
function storeCleanupWithContext(tView, lView, context2, cleanupFn) {
  const lCleanup = getOrCreateLViewCleanup(lView);
  ngDevMode && assertDefined(context2, "Cleanup context is mandatory when registering framework-level destroy hooks");
  lCleanup.push(context2);
  if (tView.firstCreatePass) {
    getOrCreateTViewCleanup(tView).push(cleanupFn, lCleanup.length - 1);
  } else {
    if (ngDevMode) {
      Object.freeze(getOrCreateTViewCleanup(tView));
    }
  }
}
var instructionState = {
  lFrame: createLFrame(null),
  bindingsEnabled: true,
  skipHydrationRootTNode: null
};
var CheckNoChangesMode;
(function(CheckNoChangesMode2) {
  CheckNoChangesMode2[CheckNoChangesMode2["Off"] = 0] = "Off";
  CheckNoChangesMode2[CheckNoChangesMode2["Exhaustive"] = 1] = "Exhaustive";
  CheckNoChangesMode2[CheckNoChangesMode2["OnlyDirtyViews"] = 2] = "OnlyDirtyViews";
})(CheckNoChangesMode || (CheckNoChangesMode = {}));
var _checkNoChangesMode = 0;
var _isRefreshingViews = false;
function getElementDepthCount() {
  return instructionState.lFrame.elementDepthCount;
}
function increaseElementDepthCount() {
  instructionState.lFrame.elementDepthCount++;
}
function decreaseElementDepthCount() {
  instructionState.lFrame.elementDepthCount--;
}
function getBindingsEnabled() {
  return instructionState.bindingsEnabled;
}
function isInSkipHydrationBlock() {
  return instructionState.skipHydrationRootTNode !== null;
}
function isSkipHydrationRootTNode(tNode) {
  return instructionState.skipHydrationRootTNode === tNode;
}
function \u0275\u0275enableBindings() {
  instructionState.bindingsEnabled = true;
}
function \u0275\u0275disableBindings() {
  instructionState.bindingsEnabled = false;
}
function leaveSkipHydrationBlock() {
  instructionState.skipHydrationRootTNode = null;
}
function getLView() {
  return instructionState.lFrame.lView;
}
function getTView() {
  return instructionState.lFrame.tView;
}
function \u0275\u0275restoreView(viewToRestore) {
  instructionState.lFrame.contextLView = viewToRestore;
  return viewToRestore[CONTEXT];
}
function \u0275\u0275resetView(value) {
  instructionState.lFrame.contextLView = null;
  return value;
}
function getCurrentTNode() {
  let currentTNode = getCurrentTNodePlaceholderOk();
  while (currentTNode !== null && currentTNode.type === 64) {
    currentTNode = currentTNode.parent;
  }
  return currentTNode;
}
function getCurrentTNodePlaceholderOk() {
  return instructionState.lFrame.currentTNode;
}
function getCurrentParentTNode() {
  const lFrame = instructionState.lFrame;
  const currentTNode = lFrame.currentTNode;
  return lFrame.isParent ? currentTNode : currentTNode.parent;
}
function setCurrentTNode(tNode, isParent) {
  ngDevMode && tNode && assertTNodeForTView(tNode, instructionState.lFrame.tView);
  const lFrame = instructionState.lFrame;
  lFrame.currentTNode = tNode;
  lFrame.isParent = isParent;
}
function isCurrentTNodeParent() {
  return instructionState.lFrame.isParent;
}
function setCurrentTNodeAsNotParent() {
  instructionState.lFrame.isParent = false;
}
function getContextLView() {
  const contextLView = instructionState.lFrame.contextLView;
  ngDevMode && assertDefined(contextLView, "contextLView must be defined.");
  return contextLView;
}
function isInCheckNoChangesMode() {
  !ngDevMode && throwError2("Must never be called in production mode");
  return _checkNoChangesMode !== CheckNoChangesMode.Off;
}
function isExhaustiveCheckNoChanges() {
  !ngDevMode && throwError2("Must never be called in production mode");
  return _checkNoChangesMode === CheckNoChangesMode.Exhaustive;
}
function setIsInCheckNoChangesMode(mode) {
  !ngDevMode && throwError2("Must never be called in production mode");
  _checkNoChangesMode = mode;
}
function isRefreshingViews() {
  return _isRefreshingViews;
}
function setIsRefreshingViews(mode) {
  const prev = _isRefreshingViews;
  _isRefreshingViews = mode;
  return prev;
}
function getBindingRoot() {
  const lFrame = instructionState.lFrame;
  let index = lFrame.bindingRootIndex;
  if (index === -1) {
    index = lFrame.bindingRootIndex = lFrame.tView.bindingStartIndex;
  }
  return index;
}
function getBindingIndex() {
  return instructionState.lFrame.bindingIndex;
}
function setBindingIndex(value) {
  return instructionState.lFrame.bindingIndex = value;
}
function nextBindingIndex() {
  return instructionState.lFrame.bindingIndex++;
}
function incrementBindingIndex(count) {
  const lFrame = instructionState.lFrame;
  const index = lFrame.bindingIndex;
  lFrame.bindingIndex = lFrame.bindingIndex + count;
  return index;
}
function isInI18nBlock() {
  return instructionState.lFrame.inI18n;
}
function setInI18nBlock(isInI18nBlock2) {
  instructionState.lFrame.inI18n = isInI18nBlock2;
}
function setBindingRootForHostBindings(bindingRootIndex, currentDirectiveIndex) {
  const lFrame = instructionState.lFrame;
  lFrame.bindingIndex = lFrame.bindingRootIndex = bindingRootIndex;
  setCurrentDirectiveIndex(currentDirectiveIndex);
}
function getCurrentDirectiveIndex() {
  return instructionState.lFrame.currentDirectiveIndex;
}
function setCurrentDirectiveIndex(currentDirectiveIndex) {
  instructionState.lFrame.currentDirectiveIndex = currentDirectiveIndex;
}
function getCurrentDirectiveDef(tData) {
  const currentDirectiveIndex = instructionState.lFrame.currentDirectiveIndex;
  return currentDirectiveIndex === -1 ? null : tData[currentDirectiveIndex];
}
function getCurrentQueryIndex() {
  return instructionState.lFrame.currentQueryIndex;
}
function setCurrentQueryIndex(value) {
  instructionState.lFrame.currentQueryIndex = value;
}
function getDeclarationTNode(lView) {
  const tView = lView[TVIEW];
  if (tView.type === 2) {
    ngDevMode && assertDefined(tView.declTNode, "Embedded TNodes should have declaration parents.");
    return tView.declTNode;
  }
  if (tView.type === 1) {
    return lView[T_HOST];
  }
  return null;
}
function enterDI(lView, tNode, flags) {
  ngDevMode && assertLViewOrUndefined(lView);
  if (flags & 4) {
    ngDevMode && assertTNodeForTView(tNode, lView[TVIEW]);
    let parentTNode = tNode;
    let parentLView = lView;
    while (true) {
      ngDevMode && assertDefined(parentTNode, "Parent TNode should be defined");
      parentTNode = parentTNode.parent;
      if (parentTNode === null && !(flags & 1)) {
        parentTNode = getDeclarationTNode(parentLView);
        if (parentTNode === null) break;
        ngDevMode && assertDefined(parentLView, "Parent LView should be defined");
        parentLView = parentLView[DECLARATION_VIEW];
        if (parentTNode.type & (2 | 8)) {
          break;
        }
      } else {
        break;
      }
    }
    if (parentTNode === null) {
      return false;
    } else {
      tNode = parentTNode;
      lView = parentLView;
    }
  }
  ngDevMode && assertTNodeForLView(tNode, lView);
  const lFrame = instructionState.lFrame = allocLFrame();
  lFrame.currentTNode = tNode;
  lFrame.lView = lView;
  return true;
}
function enterView(newView) {
  ngDevMode && assertNotEqual(newView[0], newView[1], "????");
  ngDevMode && assertLViewOrUndefined(newView);
  const newLFrame = allocLFrame();
  if (ngDevMode) {
    assertEqual(newLFrame.isParent, true, "Expected clean LFrame");
    assertEqual(newLFrame.lView, null, "Expected clean LFrame");
    assertEqual(newLFrame.tView, null, "Expected clean LFrame");
    assertEqual(newLFrame.selectedIndex, -1, "Expected clean LFrame");
    assertEqual(newLFrame.elementDepthCount, 0, "Expected clean LFrame");
    assertEqual(newLFrame.currentDirectiveIndex, -1, "Expected clean LFrame");
    assertEqual(newLFrame.currentNamespace, null, "Expected clean LFrame");
    assertEqual(newLFrame.bindingRootIndex, -1, "Expected clean LFrame");
    assertEqual(newLFrame.currentQueryIndex, 0, "Expected clean LFrame");
  }
  const tView = newView[TVIEW];
  instructionState.lFrame = newLFrame;
  ngDevMode && tView.firstChild && assertTNodeForTView(tView.firstChild, tView);
  newLFrame.currentTNode = tView.firstChild;
  newLFrame.lView = newView;
  newLFrame.tView = tView;
  newLFrame.contextLView = newView;
  newLFrame.bindingIndex = tView.bindingStartIndex;
  newLFrame.inI18n = false;
}
function allocLFrame() {
  const currentLFrame = instructionState.lFrame;
  const childLFrame = currentLFrame === null ? null : currentLFrame.child;
  const newLFrame = childLFrame === null ? createLFrame(currentLFrame) : childLFrame;
  return newLFrame;
}
function createLFrame(parent) {
  const lFrame = {
    currentTNode: null,
    isParent: true,
    lView: null,
    tView: null,
    selectedIndex: -1,
    contextLView: null,
    elementDepthCount: 0,
    currentNamespace: null,
    currentDirectiveIndex: -1,
    bindingRootIndex: -1,
    bindingIndex: -1,
    currentQueryIndex: 0,
    parent,
    child: null,
    inI18n: false
  };
  parent !== null && (parent.child = lFrame);
  return lFrame;
}
function leaveViewLight() {
  const oldLFrame = instructionState.lFrame;
  instructionState.lFrame = oldLFrame.parent;
  oldLFrame.currentTNode = null;
  oldLFrame.lView = null;
  return oldLFrame;
}
var leaveDI = leaveViewLight;
function leaveView() {
  const oldLFrame = leaveViewLight();
  oldLFrame.isParent = true;
  oldLFrame.tView = null;
  oldLFrame.selectedIndex = -1;
  oldLFrame.contextLView = null;
  oldLFrame.elementDepthCount = 0;
  oldLFrame.currentDirectiveIndex = -1;
  oldLFrame.currentNamespace = null;
  oldLFrame.bindingRootIndex = -1;
  oldLFrame.bindingIndex = -1;
  oldLFrame.currentQueryIndex = 0;
}
function nextContextImpl(level) {
  const contextLView = instructionState.lFrame.contextLView = walkUpViews(level, instructionState.lFrame.contextLView);
  return contextLView[CONTEXT];
}
function getSelectedIndex() {
  return instructionState.lFrame.selectedIndex;
}
function setSelectedIndex(index) {
  ngDevMode && index !== -1 && assertGreaterThanOrEqual(index, HEADER_OFFSET, "Index must be past HEADER_OFFSET (or -1).");
  ngDevMode && assertLessThan(index, instructionState.lFrame.lView.length, "Can't set index passed end of LView");
  instructionState.lFrame.selectedIndex = index;
}
function getSelectedTNode() {
  const lFrame = instructionState.lFrame;
  return getTNode(lFrame.tView, lFrame.selectedIndex);
}
function \u0275\u0275namespaceSVG() {
  instructionState.lFrame.currentNamespace = SVG_NAMESPACE;
}
function \u0275\u0275namespaceMathML() {
  instructionState.lFrame.currentNamespace = MATH_ML_NAMESPACE;
}
function \u0275\u0275namespaceHTML() {
  namespaceHTMLInternal();
}
function namespaceHTMLInternal() {
  instructionState.lFrame.currentNamespace = null;
}
function getNamespace() {
  return instructionState.lFrame.currentNamespace;
}
var _wasLastNodeCreated = true;
function wasLastNodeCreated() {
  return _wasLastNodeCreated;
}
function lastNodeWasCreated(flag) {
  _wasLastNodeCreated = flag;
}
function createInjector(defType, parent = null, additionalProviders = null, name) {
  const injector = createInjectorWithoutInjectorInstances(defType, parent, additionalProviders, name);
  injector.resolveInjectorInitializers();
  return injector;
}
function createInjectorWithoutInjectorInstances(defType, parent = null, additionalProviders = null, name, scopes = /* @__PURE__ */ new Set()) {
  const providers = [additionalProviders || EMPTY_ARRAY, importProvidersFrom(defType)];
  let source = void 0;
  if (ngDevMode) {
    source = name || (typeof defType === "object" ? void 0 : stringify(defType));
  }
  return new R3Injector(providers, parent || getNullInjector(), source || null, scopes);
}
var Injector = class _Injector {
  static THROW_IF_NOT_FOUND = THROW_IF_NOT_FOUND;
  static NULL = new NullInjector();
  static create(options, parent) {
    if (Array.isArray(options)) {
      return createInjector({
        name: ""
      }, parent, options, "");
    } else {
      const name = options.name ?? "";
      return createInjector({
        name
      }, options.parent, options.providers, name);
    }
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Injector,
    providedIn: "any",
    factory: () => \u0275\u0275inject(INJECTOR$1)
  });
  static __NG_ELEMENT_ID__ = -1;
};
var DOCUMENT = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DocumentToken" : "");
var DestroyRef = class {
  static __NG_ELEMENT_ID__ = injectDestroyRef;
  static __NG_ENV_ID__ = (injector) => injector;
};
var NodeInjectorDestroyRef = class extends DestroyRef {
  _lView;
  constructor(_lView) {
    super();
    this._lView = _lView;
  }
  get destroyed() {
    return isDestroyed(this._lView);
  }
  onDestroy(callback) {
    const lView = this._lView;
    storeLViewOnDestroy(lView, callback);
    return () => removeLViewOnDestroy(lView, callback);
  }
};
function injectDestroyRef() {
  return new NodeInjectorDestroyRef(getLView());
}
var SCHEDULE_IN_ROOT_ZONE_DEFAULT = false;
var DEBUG_TASK_TRACKER = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DEBUG_TASK_TRACKER" : "");
var PendingTasksInternal = class _PendingTasksInternal {
  taskId = 0;
  pendingTasks = /* @__PURE__ */ new Set();
  destroyed = false;
  pendingTask = new BehaviorSubject(false);
  debugTaskTracker = inject2(DEBUG_TASK_TRACKER, {
    optional: true
  });
  get hasPendingTasks() {
    return this.destroyed ? false : this.pendingTask.value;
  }
  get hasPendingTasksObservable() {
    if (this.destroyed) {
      return new Observable((subscriber) => {
        subscriber.next(false);
        subscriber.complete();
      });
    }
    return this.pendingTask;
  }
  add() {
    if (!this.hasPendingTasks && !this.destroyed) {
      this.pendingTask.next(true);
    }
    const taskId = this.taskId++;
    this.pendingTasks.add(taskId);
    this.debugTaskTracker?.add(taskId);
    return taskId;
  }
  has(taskId) {
    return this.pendingTasks.has(taskId);
  }
  remove(taskId) {
    this.pendingTasks.delete(taskId);
    this.debugTaskTracker?.remove(taskId);
    if (this.pendingTasks.size === 0 && this.hasPendingTasks) {
      this.pendingTask.next(false);
    }
  }
  ngOnDestroy() {
    this.pendingTasks.clear();
    if (this.hasPendingTasks) {
      this.pendingTask.next(false);
    }
    this.destroyed = true;
    this.pendingTask.unsubscribe();
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PendingTasksInternal,
    providedIn: "root",
    factory: () => new _PendingTasksInternal()
  });
};
var EventEmitter_ = class extends Subject {
  __isAsync;
  destroyRef = void 0;
  pendingTasks = void 0;
  constructor(isAsync = false) {
    super();
    this.__isAsync = isAsync;
    if (isInInjectionContext()) {
      this.destroyRef = inject2(DestroyRef, {
        optional: true
      }) ?? void 0;
      this.pendingTasks = inject2(PendingTasksInternal, {
        optional: true
      }) ?? void 0;
    }
  }
  emit(value) {
    const prevConsumer = setActiveConsumer(null);
    try {
      super.next(value);
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
  subscribe(observerOrNext, error, complete) {
    let nextFn = observerOrNext;
    let errorFn = error || (() => null);
    let completeFn = complete;
    if (observerOrNext && typeof observerOrNext === "object") {
      const observer = observerOrNext;
      nextFn = observer.next?.bind(observer);
      errorFn = observer.error?.bind(observer);
      completeFn = observer.complete?.bind(observer);
    }
    if (this.__isAsync) {
      errorFn = this.wrapInTimeout(errorFn);
      if (nextFn) {
        nextFn = this.wrapInTimeout(nextFn);
      }
      if (completeFn) {
        completeFn = this.wrapInTimeout(completeFn);
      }
    }
    const sink = super.subscribe({
      next: nextFn,
      error: errorFn,
      complete: completeFn
    });
    if (observerOrNext instanceof Subscription) {
      observerOrNext.add(sink);
    }
    return sink;
  }
  wrapInTimeout(fn) {
    return (value) => {
      const taskId = this.pendingTasks?.add();
      setTimeout(() => {
        try {
          fn(value);
        } finally {
          if (taskId !== void 0) {
            this.pendingTasks?.remove(taskId);
          }
        }
      });
    };
  }
};
var EventEmitter = EventEmitter_;
function noop2(...args) {
}
function scheduleCallbackWithRafRace(callback) {
  let timeoutId;
  let animationFrameId;
  function cleanup() {
    callback = noop2;
    try {
      if (animationFrameId !== void 0 && typeof cancelAnimationFrame === "function") {
        cancelAnimationFrame(animationFrameId);
      }
      if (timeoutId !== void 0) {
        clearTimeout(timeoutId);
      }
    } catch {
    }
  }
  timeoutId = setTimeout(() => {
    callback();
    cleanup();
  });
  if (typeof requestAnimationFrame === "function") {
    animationFrameId = requestAnimationFrame(() => {
      callback();
      cleanup();
    });
  }
  return () => cleanup();
}
function scheduleCallbackWithMicrotask(callback) {
  queueMicrotask(() => callback());
  return () => {
    callback = noop2;
  };
}
var AsyncStackTaggingZoneSpec = class {
  createTask;
  constructor(namePrefix, consoleAsyncStackTaggingImpl = console) {
    this.name = "asyncStackTagging for " + namePrefix;
    this.createTask = consoleAsyncStackTaggingImpl?.createTask ?? (() => null);
  }
  name;
  onScheduleTask(delegate, _current, target, task) {
    task.consoleTask = this.createTask(`Zone - ${task.source || task.type}`);
    return delegate.scheduleTask(target, task);
  }
  onInvokeTask(delegate, _currentZone, targetZone, task, applyThis, applyArgs) {
    let ret;
    if (task.consoleTask) {
      ret = task.consoleTask.run(() => delegate.invokeTask(targetZone, task, applyThis, applyArgs));
    } else {
      ret = delegate.invokeTask(targetZone, task, applyThis, applyArgs);
    }
    return ret;
  }
};
var isAngularZoneProperty = "isAngularZone";
var angularZoneInstanceIdProperty = isAngularZoneProperty + "_ID";
var ngZoneInstanceId = 0;
var NgZone = class _NgZone {
  hasPendingMacrotasks = false;
  hasPendingMicrotasks = false;
  isStable = true;
  onUnstable = new EventEmitter(false);
  onMicrotaskEmpty = new EventEmitter(false);
  onStable = new EventEmitter(false);
  onError = new EventEmitter(false);
  constructor(options) {
    const {
      enableLongStackTrace = false,
      shouldCoalesceEventChangeDetection = false,
      shouldCoalesceRunChangeDetection = false,
      scheduleInRootZone = SCHEDULE_IN_ROOT_ZONE_DEFAULT
    } = options;
    if (typeof Zone == "undefined") {
      throw new RuntimeError(908, ngDevMode && `In this configuration Angular requires Zone.js`);
    }
    Zone.assertZonePatched();
    const self = this;
    self._nesting = 0;
    self._outer = self._inner = Zone.current;
    if (ngDevMode) {
      self._inner = self._inner.fork(new AsyncStackTaggingZoneSpec("Angular"));
    }
    if (Zone["TaskTrackingZoneSpec"]) {
      self._inner = self._inner.fork(new Zone["TaskTrackingZoneSpec"]());
    }
    if (enableLongStackTrace && Zone["longStackTraceZoneSpec"]) {
      self._inner = self._inner.fork(Zone["longStackTraceZoneSpec"]);
    }
    self.shouldCoalesceEventChangeDetection = !shouldCoalesceRunChangeDetection && shouldCoalesceEventChangeDetection;
    self.shouldCoalesceRunChangeDetection = shouldCoalesceRunChangeDetection;
    self.callbackScheduled = false;
    self.scheduleInRootZone = scheduleInRootZone;
    forkInnerZoneWithAngularBehavior(self);
  }
  static isInAngularZone() {
    return typeof Zone !== "undefined" && Zone.current.get(isAngularZoneProperty) === true;
  }
  static assertInAngularZone() {
    if (!_NgZone.isInAngularZone()) {
      throw new RuntimeError(909, ngDevMode && "Expected to be in Angular Zone, but it is not!");
    }
  }
  static assertNotInAngularZone() {
    if (_NgZone.isInAngularZone()) {
      throw new RuntimeError(909, ngDevMode && "Expected to not be in Angular Zone, but it is!");
    }
  }
  run(fn, applyThis, applyArgs) {
    return this._inner.run(fn, applyThis, applyArgs);
  }
  runTask(fn, applyThis, applyArgs, name) {
    const zone = this._inner;
    const task = zone.scheduleEventTask("NgZoneEvent: " + name, fn, EMPTY_PAYLOAD, noop2, noop2);
    try {
      return zone.runTask(task, applyThis, applyArgs);
    } finally {
      zone.cancelTask(task);
    }
  }
  runGuarded(fn, applyThis, applyArgs) {
    return this._inner.runGuarded(fn, applyThis, applyArgs);
  }
  runOutsideAngular(fn) {
    return this._outer.run(fn);
  }
};
var EMPTY_PAYLOAD = {};
function checkStable(zone) {
  if (zone._nesting == 0 && !zone.hasPendingMicrotasks && !zone.isStable) {
    try {
      zone._nesting++;
      zone.onMicrotaskEmpty.emit(null);
    } finally {
      zone._nesting--;
      if (!zone.hasPendingMicrotasks) {
        try {
          zone.runOutsideAngular(() => zone.onStable.emit(null));
        } finally {
          zone.isStable = true;
        }
      }
    }
  }
}
function delayChangeDetectionForEvents(zone) {
  if (zone.isCheckStableRunning || zone.callbackScheduled) {
    return;
  }
  zone.callbackScheduled = true;
  function scheduleCheckStable() {
    scheduleCallbackWithRafRace(() => {
      zone.callbackScheduled = false;
      updateMicroTaskStatus(zone);
      zone.isCheckStableRunning = true;
      checkStable(zone);
      zone.isCheckStableRunning = false;
    });
  }
  if (zone.scheduleInRootZone) {
    Zone.root.run(() => {
      scheduleCheckStable();
    });
  } else {
    zone._outer.run(() => {
      scheduleCheckStable();
    });
  }
  updateMicroTaskStatus(zone);
}
function forkInnerZoneWithAngularBehavior(zone) {
  const delayChangeDetectionForEventsDelegate = () => {
    delayChangeDetectionForEvents(zone);
  };
  const instanceId = ngZoneInstanceId++;
  zone._inner = zone._inner.fork({
    name: "angular",
    properties: {
      [isAngularZoneProperty]: true,
      [angularZoneInstanceIdProperty]: instanceId,
      [angularZoneInstanceIdProperty + instanceId]: true
    },
    onInvokeTask: (delegate, current, target, task, applyThis, applyArgs) => {
      if (shouldBeIgnoredByZone(applyArgs)) {
        return delegate.invokeTask(target, task, applyThis, applyArgs);
      }
      try {
        onEnter(zone);
        return delegate.invokeTask(target, task, applyThis, applyArgs);
      } finally {
        if (zone.shouldCoalesceEventChangeDetection && task.type === "eventTask" || zone.shouldCoalesceRunChangeDetection) {
          delayChangeDetectionForEventsDelegate();
        }
        onLeave(zone);
      }
    },
    onInvoke: (delegate, current, target, callback, applyThis, applyArgs, source) => {
      try {
        onEnter(zone);
        return delegate.invoke(target, callback, applyThis, applyArgs, source);
      } finally {
        if (zone.shouldCoalesceRunChangeDetection && !zone.callbackScheduled && !isSchedulerTick(applyArgs)) {
          delayChangeDetectionForEventsDelegate();
        }
        onLeave(zone);
      }
    },
    onHasTask: (delegate, current, target, hasTaskState) => {
      delegate.hasTask(target, hasTaskState);
      if (current === target) {
        if (hasTaskState.change == "microTask") {
          zone._hasPendingMicrotasks = hasTaskState.microTask;
          updateMicroTaskStatus(zone);
          checkStable(zone);
        } else if (hasTaskState.change == "macroTask") {
          zone.hasPendingMacrotasks = hasTaskState.macroTask;
        }
      }
    },
    onHandleError: (delegate, current, target, error) => {
      delegate.handleError(target, error);
      zone.runOutsideAngular(() => zone.onError.emit(error));
      return false;
    }
  });
}
function updateMicroTaskStatus(zone) {
  if (zone._hasPendingMicrotasks || (zone.shouldCoalesceEventChangeDetection || zone.shouldCoalesceRunChangeDetection) && zone.callbackScheduled === true) {
    zone.hasPendingMicrotasks = true;
  } else {
    zone.hasPendingMicrotasks = false;
  }
}
function onEnter(zone) {
  zone._nesting++;
  if (zone.isStable) {
    zone.isStable = false;
    zone.onUnstable.emit(null);
  }
}
function onLeave(zone) {
  zone._nesting--;
  checkStable(zone);
}
var NoopNgZone = class {
  hasPendingMicrotasks = false;
  hasPendingMacrotasks = false;
  isStable = true;
  onUnstable = new EventEmitter();
  onMicrotaskEmpty = new EventEmitter();
  onStable = new EventEmitter();
  onError = new EventEmitter();
  run(fn, applyThis, applyArgs) {
    return fn.apply(applyThis, applyArgs);
  }
  runGuarded(fn, applyThis, applyArgs) {
    return fn.apply(applyThis, applyArgs);
  }
  runOutsideAngular(fn) {
    return fn();
  }
  runTask(fn, applyThis, applyArgs, name) {
    return fn.apply(applyThis, applyArgs);
  }
};
function shouldBeIgnoredByZone(applyArgs) {
  return hasApplyArgsData(applyArgs, "__ignore_ng_zone__");
}
function isSchedulerTick(applyArgs) {
  return hasApplyArgsData(applyArgs, "__scheduler_tick__");
}
function hasApplyArgsData(applyArgs, key) {
  if (!Array.isArray(applyArgs)) {
    return false;
  }
  if (applyArgs.length !== 1) {
    return false;
  }
  return applyArgs[0]?.data?.[key] === true;
}
var ErrorHandler = class {
  _console = console;
  handleError(error) {
    this._console.error("ERROR", error);
  }
};
var INTERNAL_APPLICATION_ERROR_HANDLER = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "internal error handler" : "", {
  factory: () => {
    const zone = inject2(NgZone);
    const injector = inject2(EnvironmentInjector);
    let userErrorHandler;
    return (e) => {
      zone.runOutsideAngular(() => {
        if (injector.destroyed && !userErrorHandler) {
          setTimeout(() => {
            throw e;
          });
        } else {
          userErrorHandler ??= injector.get(ErrorHandler);
          userErrorHandler.handleError(e);
        }
      });
    };
  }
});
var errorHandlerEnvironmentInitializer = {
  provide: ENVIRONMENT_INITIALIZER,
  useValue: () => {
    const handler = inject2(ErrorHandler, {
      optional: true
    });
    if ((typeof ngDevMode === "undefined" || ngDevMode) && handler === null) {
      throw new RuntimeError(402, `A required Injectable was not found in the dependency injection tree. If you are bootstrapping an NgModule, make sure that the \`BrowserModule\` is imported.`);
    }
  },
  multi: true
};
var globalErrorListeners = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "GlobalErrorListeners" : "", {
  factory: () => {
    if (false) {
      return;
    }
    const window2 = inject2(DOCUMENT).defaultView;
    if (!window2) {
      return;
    }
    const errorHandler2 = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
    const rejectionListener = (e) => {
      errorHandler2(e.reason);
      e.preventDefault();
    };
    const errorListener = (e) => {
      if (e.error) {
        errorHandler2(e.error);
      } else {
        errorHandler2(new Error(ngDevMode ? `An ErrorEvent with no error occurred. See Error.cause for details: ${e.message}` : e.message, {
          cause: e
        }));
      }
      e.preventDefault();
    };
    const setupEventListeners = () => {
      window2.addEventListener("unhandledrejection", rejectionListener);
      window2.addEventListener("error", errorListener);
    };
    if (typeof Zone !== "undefined") {
      Zone.root.run(setupEventListeners);
    } else {
      setupEventListeners();
    }
    inject2(DestroyRef).onDestroy(() => {
      window2.removeEventListener("error", errorListener);
      window2.removeEventListener("unhandledrejection", rejectionListener);
    });
  }
});
function provideBrowserGlobalErrorListeners() {
  return makeEnvironmentProviders([provideEnvironmentInitializer(() => void inject2(globalErrorListeners))]);
}
function signal(initialValue, options) {
  const [get, set, update] = createSignal(initialValue, options?.equal);
  const signalFn = get;
  const node = signalFn[SIGNAL];
  signalFn.set = set;
  signalFn.update = update;
  signalFn.asReadonly = signalAsReadonlyFn.bind(signalFn);
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    const debugName = options?.debugName;
    node.debugName = debugName;
    signalFn.toString = () => `[Signal${debugName ? " (" + debugName + ")" : ""}: ${signalFn()}]`;
  }
  return signalFn;
}
function signalAsReadonlyFn() {
  const node = this[SIGNAL];
  if (node.readonlyFn === void 0) {
    const readonlyFn = () => this();
    readonlyFn[SIGNAL] = node;
    node.readonlyFn = readonlyFn;
  }
  return node.readonlyFn;
}
function assertNotInReactiveContext(debugFn, extraContext) {
  if (getActiveConsumer() !== null) {
    throw new RuntimeError(-602, ngDevMode && `${debugFn.name}() cannot be called from within a reactive context.${extraContext ? ` ${extraContext}` : ""}`);
  }
}
var ViewContext = class {
  view;
  node;
  constructor(view, node) {
    this.view = view;
    this.node = node;
  }
  static __NG_ELEMENT_ID__ = injectViewContext;
};
function injectViewContext() {
  return new ViewContext(getLView(), getCurrentTNode());
}
var ChangeDetectionScheduler = class {
};
var ZONELESS_ENABLED = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "Zoneless enabled" : "", {
  factory: () => true
});
var PROVIDED_ZONELESS = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "Zoneless provided" : "", {
  factory: () => false
});
var SCHEDULE_IN_ROOT_ZONE = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "run changes outside zone in root" : "");
var PendingTasks = class _PendingTasks {
  internalPendingTasks = inject2(PendingTasksInternal);
  scheduler = inject2(ChangeDetectionScheduler);
  errorHandler = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
  add() {
    const taskId = this.internalPendingTasks.add();
    return () => {
      if (!this.internalPendingTasks.has(taskId)) {
        return;
      }
      this.scheduler.notify(11);
      this.internalPendingTasks.remove(taskId);
    };
  }
  run(fn) {
    const removeTask = this.add();
    fn().catch(this.errorHandler).finally(removeTask);
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PendingTasks,
    providedIn: "root",
    factory: () => new _PendingTasks()
  });
};
var EffectScheduler = class _EffectScheduler {
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _EffectScheduler,
    providedIn: "root",
    factory: () => new ZoneAwareEffectScheduler()
  });
};
var ZoneAwareEffectScheduler = class {
  dirtyEffectCount = 0;
  queues = /* @__PURE__ */ new Map();
  add(handle) {
    this.enqueue(handle);
    this.schedule(handle);
  }
  schedule(handle) {
    if (!handle.dirty) {
      return;
    }
    this.dirtyEffectCount++;
  }
  remove(handle) {
    const zone = handle.zone;
    const queue = this.queues.get(zone);
    if (!queue.has(handle)) {
      return;
    }
    queue.delete(handle);
    if (handle.dirty) {
      this.dirtyEffectCount--;
    }
  }
  enqueue(handle) {
    const zone = handle.zone;
    if (!this.queues.has(zone)) {
      this.queues.set(zone, /* @__PURE__ */ new Set());
    }
    const queue = this.queues.get(zone);
    if (queue.has(handle)) {
      return;
    }
    queue.add(handle);
  }
  flush() {
    while (this.dirtyEffectCount > 0) {
      let ranOneEffect = false;
      for (const [zone, queue] of this.queues) {
        if (zone === null) {
          ranOneEffect ||= this.flushQueue(queue);
        } else {
          ranOneEffect ||= zone.run(() => this.flushQueue(queue));
        }
      }
      if (!ranOneEffect) {
        this.dirtyEffectCount = 0;
      }
    }
  }
  flushQueue(queue) {
    let ranOneEffect = false;
    for (const handle of queue) {
      if (!handle.dirty) {
        continue;
      }
      this.dirtyEffectCount--;
      ranOneEffect = true;
      handle.run();
    }
    return ranOneEffect;
  }
};
var EffectRefImpl = class {
  [SIGNAL];
  constructor(node) {
    this[SIGNAL] = node;
  }
  destroy() {
    this[SIGNAL].destroy();
  }
};
function effect(effectFn, options) {
  ngDevMode && assertNotInReactiveContext(effect, "Call `effect` outside of a reactive context. For example, schedule the effect inside the component constructor.");
  if (ngDevMode && !options?.injector) {
    assertInInjectionContext(effect);
  }
  if (ngDevMode && options?.allowSignalWrites !== void 0) {
    console.warn(`The 'allowSignalWrites' flag is deprecated and no longer impacts effect() (writes are always allowed)`);
  }
  const injector = options?.injector ?? inject2(Injector);
  let destroyRef = options?.manualCleanup !== true ? injector.get(DestroyRef) : null;
  let node;
  const viewContext = injector.get(ViewContext, null, {
    optional: true
  });
  const notifier = injector.get(ChangeDetectionScheduler);
  if (viewContext !== null) {
    node = createViewEffect(viewContext.view, notifier, effectFn);
    if (destroyRef instanceof NodeInjectorDestroyRef && destroyRef._lView === viewContext.view) {
      destroyRef = null;
    }
  } else {
    node = createRootEffect(effectFn, injector.get(EffectScheduler), notifier);
  }
  node.injector = injector;
  if (destroyRef !== null) {
    node.onDestroyFns = [destroyRef.onDestroy(() => node.destroy())];
  }
  const effectRef = new EffectRefImpl(node);
  if (ngDevMode) {
    node.debugName = options?.debugName ?? "";
    const prevInjectorProfilerContext = setInjectorProfilerContext({
      injector,
      token: null
    });
    try {
      emitEffectCreatedEvent(effectRef);
    } finally {
      setInjectorProfilerContext(prevInjectorProfilerContext);
    }
  }
  return effectRef;
}
var EFFECT_NODE = /* @__PURE__ */ (() => __spreadProps(__spreadValues({}, BASE_EFFECT_NODE), {
  cleanupFns: void 0,
  zone: null,
  onDestroyFns: null,
  run() {
    if (ngDevMode && isInNotificationPhase()) {
      throw new Error(`Schedulers cannot synchronously execute watches while scheduling.`);
    }
    const prevRefreshingViews = setIsRefreshingViews(false);
    try {
      runEffect(this);
    } finally {
      setIsRefreshingViews(prevRefreshingViews);
    }
  },
  cleanup() {
    if (!this.cleanupFns?.length) {
      return;
    }
    const prevConsumer = setActiveConsumer(null);
    try {
      while (this.cleanupFns.length) {
        this.cleanupFns.pop()();
      }
    } finally {
      this.cleanupFns = [];
      setActiveConsumer(prevConsumer);
    }
  }
}))();
var ROOT_EFFECT_NODE = /* @__PURE__ */ (() => __spreadProps(__spreadValues({}, EFFECT_NODE), {
  consumerMarkedDirty() {
    this.scheduler.schedule(this);
    this.notifier.notify(12);
  },
  destroy() {
    consumerDestroy(this);
    if (this.onDestroyFns !== null) {
      for (const fn of this.onDestroyFns) {
        fn();
      }
    }
    this.cleanup();
    this.scheduler.remove(this);
  }
}))();
var VIEW_EFFECT_NODE = /* @__PURE__ */ (() => __spreadProps(__spreadValues({}, EFFECT_NODE), {
  consumerMarkedDirty() {
    this.view[FLAGS] |= 8192;
    markAncestorsForTraversal(this.view);
    this.notifier.notify(13);
  },
  destroy() {
    consumerDestroy(this);
    if (this.onDestroyFns !== null) {
      for (const fn of this.onDestroyFns) {
        fn();
      }
    }
    this.cleanup();
    this.view[EFFECTS]?.delete(this);
  }
}))();
function createViewEffect(view, notifier, fn) {
  const node = Object.create(VIEW_EFFECT_NODE);
  node.view = view;
  node.zone = typeof Zone !== "undefined" ? Zone.current : null;
  node.notifier = notifier;
  node.fn = createEffectFn(node, fn);
  view[EFFECTS] ??= /* @__PURE__ */ new Set();
  view[EFFECTS].add(node);
  node.consumerMarkedDirty(node);
  return node;
}
function createRootEffect(fn, scheduler, notifier) {
  const node = Object.create(ROOT_EFFECT_NODE);
  node.fn = createEffectFn(node, fn);
  node.scheduler = scheduler;
  node.notifier = notifier;
  node.zone = typeof Zone !== "undefined" ? Zone.current : null;
  node.scheduler.add(node);
  node.notifier.notify(12);
  return node;
}
function createEffectFn(node, fn) {
  return () => {
    fn((cleanupFn) => (node.cleanupFns ??= []).push(cleanupFn));
  };
}

// node_modules/@angular/core/fesm2022/_debug_node-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
function noSideEffects(fn) {
  return {
    toString: fn
  }.toString();
}
var ANNOTATIONS = "__annotations__";
var PARAMETERS = "__parameters__";
var PROP_METADATA = "__prop__metadata__";
function makeDecorator(name, props, parentClass, additionalProcessing, typeFn) {
  return noSideEffects(() => {
    const metaCtor = makeMetadataCtor(props);
    function DecoratorFactory(...args) {
      if (this instanceof DecoratorFactory) {
        metaCtor.call(this, ...args);
        return this;
      }
      const annotationInstance = new DecoratorFactory(...args);
      return function TypeDecorator(cls) {
        if (typeFn) typeFn(cls, ...args);
        const annotations = cls.hasOwnProperty(ANNOTATIONS) ? cls[ANNOTATIONS] : Object.defineProperty(cls, ANNOTATIONS, {
          value: []
        })[ANNOTATIONS];
        annotations.push(annotationInstance);
        return cls;
      };
    }
    if (parentClass) {
      DecoratorFactory.prototype = Object.create(parentClass.prototype);
    }
    DecoratorFactory.prototype.ngMetadataName = name;
    DecoratorFactory.annotationCls = DecoratorFactory;
    return DecoratorFactory;
  });
}
function makeMetadataCtor(props) {
  return function ctor(...args) {
    if (props) {
      const values = props(...args);
      for (const propName in values) {
        this[propName] = values[propName];
      }
    }
  };
}
function makeParamDecorator(name, props, parentClass) {
  return noSideEffects(() => {
    const metaCtor = makeMetadataCtor(props);
    function ParamDecoratorFactory(...args) {
      if (this instanceof ParamDecoratorFactory) {
        metaCtor.apply(this, args);
        return this;
      }
      const annotationInstance = new ParamDecoratorFactory(...args);
      ParamDecorator.annotation = annotationInstance;
      return ParamDecorator;
      function ParamDecorator(cls, unusedKey, index) {
        const parameters = cls.hasOwnProperty(PARAMETERS) ? cls[PARAMETERS] : Object.defineProperty(cls, PARAMETERS, {
          value: []
        })[PARAMETERS];
        while (parameters.length <= index) {
          parameters.push(null);
        }
        (parameters[index] = parameters[index] || []).push(annotationInstance);
        return cls;
      }
    }
    ParamDecoratorFactory.prototype.ngMetadataName = name;
    ParamDecoratorFactory.annotationCls = ParamDecoratorFactory;
    return ParamDecoratorFactory;
  });
}
function makePropDecorator(name, props, parentClass, additionalProcessing) {
  return noSideEffects(() => {
    const metaCtor = makeMetadataCtor(props);
    function PropDecoratorFactory(...args) {
      if (this instanceof PropDecoratorFactory) {
        metaCtor.apply(this, args);
        return this;
      }
      const decoratorInstance = new PropDecoratorFactory(...args);
      function PropDecorator(target, name2) {
        if (target === void 0) {
          throw new Error("Standard Angular field decorators are not supported in JIT mode.");
        }
        const constructor = target.constructor;
        const meta = constructor.hasOwnProperty(PROP_METADATA) ? constructor[PROP_METADATA] : Object.defineProperty(constructor, PROP_METADATA, {
          value: {}
        })[PROP_METADATA];
        meta[name2] = meta.hasOwnProperty(name2) && meta[name2] || [];
        meta[name2].unshift(decoratorInstance);
      }
      return PropDecorator;
    }
    if (parentClass) {
      PropDecoratorFactory.prototype = Object.create(parentClass.prototype);
    }
    PropDecoratorFactory.prototype.ngMetadataName = name;
    PropDecoratorFactory.annotationCls = PropDecoratorFactory;
    return PropDecoratorFactory;
  });
}
var Inject = attachInjectFlag(makeParamDecorator("Inject", (token) => ({
  token
})), -1);
var Optional = attachInjectFlag(makeParamDecorator("Optional"), 8);
var Self = attachInjectFlag(makeParamDecorator("Self"), 2);
var SkipSelf = attachInjectFlag(makeParamDecorator("SkipSelf"), 4);
var Host = attachInjectFlag(makeParamDecorator("Host"), 1);
function getCompilerFacade(request) {
  const globalNg = _global["ng"];
  if (globalNg && globalNg.\u0275compilerFacade) {
    return globalNg.\u0275compilerFacade;
  }
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    console.error(`JIT compilation failed for ${request.kind}`, request.type);
    let message = `The ${request.kind} '${request.type.name}' needs to be compiled using the JIT compiler, but '@angular/compiler' is not available.

`;
    if (request.usage === 1) {
      message += `The ${request.kind} is part of a library that has been partially compiled.
`;
      message += `However, the Angular Linker has not processed the library such that JIT compilation is used as fallback.
`;
      message += "\n";
      message += `Ideally, the library is processed using the Angular Linker to become fully AOT compiled.
`;
    } else {
      message += `JIT compilation is discouraged for production use-cases! Consider using AOT mode instead.
`;
    }
    message += `Alternatively, the JIT compiler should be loaded by bootstrapping using '@angular/platform-browser-dynamic' or '@angular/platform-server',
`;
    message += `or manually provide the compiler with 'import "@angular/compiler";' before bootstrapping.`;
    throw new Error(message);
  } else {
    throw new Error("JIT compiler unavailable");
  }
}
var angularCoreDiEnv = {
  "\u0275\u0275defineInjectable": \u0275\u0275defineInjectable,
  "\u0275\u0275defineInjector": \u0275\u0275defineInjector,
  "\u0275\u0275inject": \u0275\u0275inject,
  "\u0275\u0275invalidFactoryDep": \u0275\u0275invalidFactoryDep,
  "resolveForwardRef": resolveForwardRef
};
var Type = Function;
function isType(v) {
  return typeof v === "function";
}
var ES5_DELEGATE_CTOR = /^function\s+\S+\(\)\s*{[\s\S]+\.apply\(this,\s*(arguments|(?:[^()]+\(\[\],)?[^()]+\(arguments\).*)\)/;
var ES2015_INHERITED_CLASS = /^class\s+[A-Za-z\d$_]*\s*extends\s+[^{]+{/;
var ES2015_INHERITED_CLASS_WITH_CTOR = /^class\s+[A-Za-z\d$_]*\s*extends\s+[^{]+{[\s\S]*constructor\s*\(/;
var ES2015_INHERITED_CLASS_WITH_DELEGATE_CTOR = /^class\s+[A-Za-z\d$_]*\s*extends\s+[^{]+{[\s\S]*constructor\s*\(\)\s*{[^}]*super\(\.\.\.arguments\)/;
function isDelegateCtor(typeStr) {
  return ES5_DELEGATE_CTOR.test(typeStr) || ES2015_INHERITED_CLASS_WITH_DELEGATE_CTOR.test(typeStr) || ES2015_INHERITED_CLASS.test(typeStr) && !ES2015_INHERITED_CLASS_WITH_CTOR.test(typeStr);
}
var ReflectionCapabilities = class {
  _reflect;
  constructor(reflect) {
    this._reflect = reflect || _global["Reflect"];
  }
  factory(t) {
    return (...args) => new t(...args);
  }
  _zipTypesAndAnnotations(paramTypes, paramAnnotations) {
    let result;
    if (typeof paramTypes === "undefined") {
      result = newArray(paramAnnotations.length);
    } else {
      result = newArray(paramTypes.length);
    }
    for (let i = 0; i < result.length; i++) {
      if (typeof paramTypes === "undefined") {
        result[i] = [];
      } else if (paramTypes[i] && paramTypes[i] != Object) {
        result[i] = [paramTypes[i]];
      } else {
        result[i] = [];
      }
      if (paramAnnotations && paramAnnotations[i] != null) {
        result[i] = result[i].concat(paramAnnotations[i]);
      }
    }
    return result;
  }
  _ownParameters(type, parentCtor) {
    const typeStr = type.toString();
    if (isDelegateCtor(typeStr)) {
      return null;
    }
    if (type.parameters && type.parameters !== parentCtor.parameters) {
      return type.parameters;
    }
    const tsickleCtorParams = type.ctorParameters;
    if (tsickleCtorParams && tsickleCtorParams !== parentCtor.ctorParameters) {
      const ctorParameters = typeof tsickleCtorParams === "function" ? tsickleCtorParams() : tsickleCtorParams;
      const paramTypes2 = ctorParameters.map((ctorParam) => ctorParam && ctorParam.type);
      const paramAnnotations2 = ctorParameters.map((ctorParam) => ctorParam && convertTsickleDecoratorIntoMetadata(ctorParam.decorators));
      return this._zipTypesAndAnnotations(paramTypes2, paramAnnotations2);
    }
    const paramAnnotations = type.hasOwnProperty(PARAMETERS) && type[PARAMETERS];
    const paramTypes = this._reflect && this._reflect.getOwnMetadata && this._reflect.getOwnMetadata("design:paramtypes", type);
    if (paramTypes || paramAnnotations) {
      return this._zipTypesAndAnnotations(paramTypes, paramAnnotations);
    }
    return newArray(type.length);
  }
  parameters(type) {
    if (!isType(type)) {
      return [];
    }
    const parentCtor = getParentCtor(type);
    let parameters = this._ownParameters(type, parentCtor);
    if (!parameters && parentCtor !== Object) {
      parameters = this.parameters(parentCtor);
    }
    return parameters || [];
  }
  _ownAnnotations(typeOrFunc, parentCtor) {
    if (typeOrFunc.annotations && typeOrFunc.annotations !== parentCtor.annotations) {
      let annotations = typeOrFunc.annotations;
      if (typeof annotations === "function" && annotations.annotations) {
        annotations = annotations.annotations;
      }
      return annotations;
    }
    if (typeOrFunc.decorators && typeOrFunc.decorators !== parentCtor.decorators) {
      return convertTsickleDecoratorIntoMetadata(typeOrFunc.decorators);
    }
    if (typeOrFunc.hasOwnProperty(ANNOTATIONS)) {
      return typeOrFunc[ANNOTATIONS];
    }
    return null;
  }
  annotations(typeOrFunc) {
    if (!isType(typeOrFunc)) {
      return [];
    }
    const parentCtor = getParentCtor(typeOrFunc);
    const ownAnnotations = this._ownAnnotations(typeOrFunc, parentCtor) || [];
    const parentAnnotations = parentCtor !== Object ? this.annotations(parentCtor) : [];
    return parentAnnotations.concat(ownAnnotations);
  }
  _ownPropMetadata(typeOrFunc, parentCtor) {
    if (typeOrFunc.propMetadata && typeOrFunc.propMetadata !== parentCtor.propMetadata) {
      let propMetadata = typeOrFunc.propMetadata;
      if (typeof propMetadata === "function" && propMetadata.propMetadata) {
        propMetadata = propMetadata.propMetadata;
      }
      return propMetadata;
    }
    if (typeOrFunc.propDecorators && typeOrFunc.propDecorators !== parentCtor.propDecorators) {
      const propDecorators = typeOrFunc.propDecorators;
      const propMetadata = {};
      Object.keys(propDecorators).forEach((prop) => {
        propMetadata[prop] = convertTsickleDecoratorIntoMetadata(propDecorators[prop]);
      });
      return propMetadata;
    }
    if (typeOrFunc.hasOwnProperty(PROP_METADATA)) {
      return typeOrFunc[PROP_METADATA];
    }
    return null;
  }
  propMetadata(typeOrFunc) {
    if (!isType(typeOrFunc)) {
      return {};
    }
    const parentCtor = getParentCtor(typeOrFunc);
    const propMetadata = {};
    if (parentCtor !== Object) {
      const parentPropMetadata = this.propMetadata(parentCtor);
      Object.keys(parentPropMetadata).forEach((propName) => {
        propMetadata[propName] = parentPropMetadata[propName];
      });
    }
    const ownPropMetadata = this._ownPropMetadata(typeOrFunc, parentCtor);
    if (ownPropMetadata) {
      Object.keys(ownPropMetadata).forEach((propName) => {
        const decorators = [];
        if (propMetadata.hasOwnProperty(propName)) {
          decorators.push(...propMetadata[propName]);
        }
        decorators.push(...ownPropMetadata[propName]);
        propMetadata[propName] = decorators;
      });
    }
    return propMetadata;
  }
  ownPropMetadata(typeOrFunc) {
    if (!isType(typeOrFunc)) {
      return {};
    }
    return this._ownPropMetadata(typeOrFunc, getParentCtor(typeOrFunc)) || {};
  }
  hasLifecycleHook(type, lcProperty) {
    return type instanceof Type && lcProperty in type.prototype;
  }
};
function convertTsickleDecoratorIntoMetadata(decoratorInvocations) {
  if (!decoratorInvocations) {
    return [];
  }
  return decoratorInvocations.map((decoratorInvocation) => {
    const decoratorType = decoratorInvocation.type;
    const annotationCls = decoratorType.annotationCls;
    const annotationArgs = decoratorInvocation.args ? decoratorInvocation.args : [];
    return new annotationCls(...annotationArgs);
  });
}
function getParentCtor(ctor) {
  const parentProto = ctor.prototype ? Object.getPrototypeOf(ctor.prototype) : null;
  const parentCtor = parentProto ? parentProto.constructor : null;
  return parentCtor || Object;
}
function applyValueToInputField(instance, inputSignalNode, privateName, value) {
  if (inputSignalNode !== null) {
    inputSignalNode.applyValueToInputSignal(inputSignalNode, value);
  } else {
    instance[privateName] = value;
  }
}
var SimpleChange = class {
  previousValue;
  currentValue;
  firstChange;
  constructor(previousValue, currentValue, firstChange) {
    this.previousValue = previousValue;
    this.currentValue = currentValue;
    this.firstChange = firstChange;
  }
  isFirstChange() {
    return this.firstChange;
  }
};
var \u0275\u0275NgOnChangesFeature = /* @__PURE__ */ (() => {
  const \u0275\u0275NgOnChangesFeatureImpl = () => NgOnChangesFeatureImpl;
  \u0275\u0275NgOnChangesFeatureImpl.ngInherit = true;
  return \u0275\u0275NgOnChangesFeatureImpl;
})();
function NgOnChangesFeatureImpl(definition) {
  if (definition.type.prototype.ngOnChanges) {
    definition.setInput = ngOnChangesSetInput;
  }
  return rememberChangeHistoryAndInvokeOnChangesHook;
}
function rememberChangeHistoryAndInvokeOnChangesHook() {
  const simpleChangesStore = getSimpleChangesStore(this);
  const current = simpleChangesStore?.current;
  if (current) {
    const previous = simpleChangesStore.previous;
    if (previous === EMPTY_OBJ) {
      simpleChangesStore.previous = current;
    } else {
      for (let key in current) {
        previous[key] = current[key];
      }
    }
    simpleChangesStore.current = null;
    this.ngOnChanges(current);
  }
}
function ngOnChangesSetInput(instance, inputSignalNode, value, publicName, privateName) {
  const declaredName = this.declaredInputs[publicName];
  ngDevMode && assertString(declaredName, "Name of input in ngOnChanges has to be a string");
  const simpleChangesStore = getSimpleChangesStore(instance) || setSimpleChangesStore(instance, {
    previous: EMPTY_OBJ,
    current: null
  });
  const current = simpleChangesStore.current || (simpleChangesStore.current = {});
  const previous = simpleChangesStore.previous;
  const previousChange = previous[declaredName];
  current[declaredName] = new SimpleChange(previousChange && previousChange.currentValue, value, previous === EMPTY_OBJ);
  applyValueToInputField(instance, inputSignalNode, privateName, value);
}
var SIMPLE_CHANGES_STORE = "__ngSimpleChanges__";
function getSimpleChangesStore(instance) {
  return instance[SIMPLE_CHANGES_STORE] || null;
}
function setSimpleChangesStore(instance, store2) {
  return instance[SIMPLE_CHANGES_STORE] = store2;
}
var profilerCallbacks = [];
var NOOP_PROFILER_REMOVAL2 = () => {
};
function removeProfiler2(profiler2) {
  const profilerIdx = profilerCallbacks.indexOf(profiler2);
  if (profilerIdx !== -1) {
    profilerCallbacks.splice(profilerIdx, 1);
  }
}
function setProfiler(profiler2) {
  if (profiler2 !== null) {
    if (!profilerCallbacks.includes(profiler2)) {
      profilerCallbacks.push(profiler2);
    }
    return () => removeProfiler2(profiler2);
  } else {
    profilerCallbacks.length = 0;
    return NOOP_PROFILER_REMOVAL2;
  }
}
var profiler = function(event, instance = null, eventFn) {
  for (let i = 0; i < profilerCallbacks.length; i++) {
    const profilerCallback = profilerCallbacks[i];
    profilerCallback(event, instance, eventFn);
  }
};
var ProfilerEvent;
(function(ProfilerEvent2) {
  ProfilerEvent2[ProfilerEvent2["TemplateCreateStart"] = 0] = "TemplateCreateStart";
  ProfilerEvent2[ProfilerEvent2["TemplateCreateEnd"] = 1] = "TemplateCreateEnd";
  ProfilerEvent2[ProfilerEvent2["TemplateUpdateStart"] = 2] = "TemplateUpdateStart";
  ProfilerEvent2[ProfilerEvent2["TemplateUpdateEnd"] = 3] = "TemplateUpdateEnd";
  ProfilerEvent2[ProfilerEvent2["LifecycleHookStart"] = 4] = "LifecycleHookStart";
  ProfilerEvent2[ProfilerEvent2["LifecycleHookEnd"] = 5] = "LifecycleHookEnd";
  ProfilerEvent2[ProfilerEvent2["OutputStart"] = 6] = "OutputStart";
  ProfilerEvent2[ProfilerEvent2["OutputEnd"] = 7] = "OutputEnd";
  ProfilerEvent2[ProfilerEvent2["BootstrapApplicationStart"] = 8] = "BootstrapApplicationStart";
  ProfilerEvent2[ProfilerEvent2["BootstrapApplicationEnd"] = 9] = "BootstrapApplicationEnd";
  ProfilerEvent2[ProfilerEvent2["BootstrapComponentStart"] = 10] = "BootstrapComponentStart";
  ProfilerEvent2[ProfilerEvent2["BootstrapComponentEnd"] = 11] = "BootstrapComponentEnd";
  ProfilerEvent2[ProfilerEvent2["ChangeDetectionStart"] = 12] = "ChangeDetectionStart";
  ProfilerEvent2[ProfilerEvent2["ChangeDetectionEnd"] = 13] = "ChangeDetectionEnd";
  ProfilerEvent2[ProfilerEvent2["ChangeDetectionSyncStart"] = 14] = "ChangeDetectionSyncStart";
  ProfilerEvent2[ProfilerEvent2["ChangeDetectionSyncEnd"] = 15] = "ChangeDetectionSyncEnd";
  ProfilerEvent2[ProfilerEvent2["AfterRenderHooksStart"] = 16] = "AfterRenderHooksStart";
  ProfilerEvent2[ProfilerEvent2["AfterRenderHooksEnd"] = 17] = "AfterRenderHooksEnd";
  ProfilerEvent2[ProfilerEvent2["ComponentStart"] = 18] = "ComponentStart";
  ProfilerEvent2[ProfilerEvent2["ComponentEnd"] = 19] = "ComponentEnd";
  ProfilerEvent2[ProfilerEvent2["DeferBlockStateStart"] = 20] = "DeferBlockStateStart";
  ProfilerEvent2[ProfilerEvent2["DeferBlockStateEnd"] = 21] = "DeferBlockStateEnd";
  ProfilerEvent2[ProfilerEvent2["DynamicComponentStart"] = 22] = "DynamicComponentStart";
  ProfilerEvent2[ProfilerEvent2["DynamicComponentEnd"] = 23] = "DynamicComponentEnd";
  ProfilerEvent2[ProfilerEvent2["HostBindingsUpdateStart"] = 24] = "HostBindingsUpdateStart";
  ProfilerEvent2[ProfilerEvent2["HostBindingsUpdateEnd"] = 25] = "HostBindingsUpdateEnd";
})(ProfilerEvent || (ProfilerEvent = {}));
function registerPreOrderHooks(directiveIndex, directiveDef, tView) {
  ngDevMode && assertFirstCreatePass(tView);
  const {
    ngOnChanges,
    ngOnInit,
    ngDoCheck
  } = directiveDef.type.prototype;
  if (ngOnChanges) {
    const wrappedOnChanges = NgOnChangesFeatureImpl(directiveDef);
    (tView.preOrderHooks ??= []).push(directiveIndex, wrappedOnChanges);
    (tView.preOrderCheckHooks ??= []).push(directiveIndex, wrappedOnChanges);
  }
  if (ngOnInit) {
    (tView.preOrderHooks ??= []).push(0 - directiveIndex, ngOnInit);
  }
  if (ngDoCheck) {
    (tView.preOrderHooks ??= []).push(directiveIndex, ngDoCheck);
    (tView.preOrderCheckHooks ??= []).push(directiveIndex, ngDoCheck);
  }
}
function registerPostOrderHooks(tView, tNode) {
  ngDevMode && assertFirstCreatePass(tView);
  for (let i = tNode.directiveStart, end = tNode.directiveEnd; i < end; i++) {
    const directiveDef = tView.data[i];
    ngDevMode && assertDefined(directiveDef, "Expecting DirectiveDef");
    const lifecycleHooks = directiveDef.type.prototype;
    const {
      ngAfterContentInit,
      ngAfterContentChecked,
      ngAfterViewInit,
      ngAfterViewChecked,
      ngOnDestroy
    } = lifecycleHooks;
    if (ngAfterContentInit) {
      (tView.contentHooks ??= []).push(-i, ngAfterContentInit);
    }
    if (ngAfterContentChecked) {
      (tView.contentHooks ??= []).push(i, ngAfterContentChecked);
      (tView.contentCheckHooks ??= []).push(i, ngAfterContentChecked);
    }
    if (ngAfterViewInit) {
      (tView.viewHooks ??= []).push(-i, ngAfterViewInit);
    }
    if (ngAfterViewChecked) {
      (tView.viewHooks ??= []).push(i, ngAfterViewChecked);
      (tView.viewCheckHooks ??= []).push(i, ngAfterViewChecked);
    }
    if (ngOnDestroy != null) {
      (tView.destroyHooks ??= []).push(i, ngOnDestroy);
    }
  }
}
function executeCheckHooks(lView, hooks, nodeIndex) {
  callHooks(lView, hooks, 3, nodeIndex);
}
function executeInitAndCheckHooks(lView, hooks, initPhase, nodeIndex) {
  ngDevMode && assertNotEqual(initPhase, 3, "Init pre-order hooks should not be called more than once");
  if ((lView[FLAGS] & 3) === initPhase) {
    callHooks(lView, hooks, initPhase, nodeIndex);
  }
}
function incrementInitPhaseFlags(lView, initPhase) {
  ngDevMode && assertNotEqual(initPhase, 3, "Init hooks phase should not be incremented after all init hooks have been run.");
  let flags = lView[FLAGS];
  if ((flags & 3) === initPhase) {
    flags &= 16383;
    flags += 1;
    lView[FLAGS] = flags;
  }
}
function callHooks(currentView, arr, initPhase, currentNodeIndex) {
  ngDevMode && assertEqual(isInCheckNoChangesMode(), false, "Hooks should never be run when in check no changes mode.");
  const startIndex = currentNodeIndex !== void 0 ? currentView[PREORDER_HOOK_FLAGS] & 65535 : 0;
  const nodeIndexLimit = currentNodeIndex != null ? currentNodeIndex : -1;
  const max = arr.length - 1;
  let lastNodeIndexFound = 0;
  for (let i = startIndex; i < max; i++) {
    const hook = arr[i + 1];
    if (typeof hook === "number") {
      lastNodeIndexFound = arr[i];
      if (currentNodeIndex != null && lastNodeIndexFound >= currentNodeIndex) {
        break;
      }
    } else {
      const isInitHook = arr[i] < 0;
      if (isInitHook) {
        currentView[PREORDER_HOOK_FLAGS] += 65536;
      }
      if (lastNodeIndexFound < nodeIndexLimit || nodeIndexLimit == -1) {
        callHook(currentView, initPhase, arr, i);
        currentView[PREORDER_HOOK_FLAGS] = (currentView[PREORDER_HOOK_FLAGS] & 4294901760) + i + 2;
      }
      i++;
    }
  }
}
function callHookInternal(directive, hook) {
  profiler(ProfilerEvent.LifecycleHookStart, directive, hook);
  const prevConsumer = setActiveConsumer(null);
  try {
    hook.call(directive);
  } finally {
    setActiveConsumer(prevConsumer);
    profiler(ProfilerEvent.LifecycleHookEnd, directive, hook);
  }
}
function callHook(currentView, initPhase, arr, i) {
  const isInitHook = arr[i] < 0;
  const hook = arr[i + 1];
  const directiveIndex = isInitHook ? -arr[i] : arr[i];
  const directive = currentView[directiveIndex];
  if (isInitHook) {
    const indexWithintInitPhase = currentView[FLAGS] >> 14;
    if (indexWithintInitPhase < currentView[PREORDER_HOOK_FLAGS] >> 16 && (currentView[FLAGS] & 3) === initPhase) {
      currentView[FLAGS] += 16384;
      callHookInternal(directive, hook);
    }
  } else {
    callHookInternal(directive, hook);
  }
}
var NO_PARENT_INJECTOR = -1;
var NodeInjectorFactory = class {
  factory;
  name;
  injectImpl;
  resolving = false;
  canSeeViewProviders;
  multi;
  componentProviders;
  index;
  providerFactory;
  constructor(factory, isViewProvider, injectImplementation, name) {
    this.factory = factory;
    this.name = name;
    ngDevMode && assertDefined(factory, "Factory not specified");
    ngDevMode && assertEqual(typeof factory, "function", "Expected factory function.");
    this.canSeeViewProviders = isViewProvider;
    this.injectImpl = injectImplementation;
  }
};
function toTNodeTypeAsString(tNodeType) {
  let text = "";
  tNodeType & 1 && (text += "|Text");
  tNodeType & 2 && (text += "|Element");
  tNodeType & 4 && (text += "|Container");
  tNodeType & 8 && (text += "|ElementContainer");
  tNodeType & 16 && (text += "|Projection");
  tNodeType & 32 && (text += "|IcuContainer");
  tNodeType & 64 && (text += "|Placeholder");
  tNodeType & 128 && (text += "|LetDeclaration");
  return text.length > 0 ? text.substring(1) : text;
}
function isTNodeShape(value) {
  return value != null && typeof value === "object" && (value.insertBeforeIndex === null || typeof value.insertBeforeIndex === "number" || Array.isArray(value.insertBeforeIndex));
}
function hasClassInput(tNode) {
  return (tNode.flags & 8) !== 0;
}
function hasStyleInput(tNode) {
  return (tNode.flags & 16) !== 0;
}
function assertTNodeType(tNode, expectedTypes, message) {
  assertDefined(tNode, "should be called with a TNode");
  if ((tNode.type & expectedTypes) === 0) {
    throwError2(message || `Expected [${toTNodeTypeAsString(expectedTypes)}] but got ${toTNodeTypeAsString(tNode.type)}.`);
  }
}
function assertPureTNodeType(type) {
  if (!(type === 2 || type === 1 || type === 4 || type === 8 || type === 32 || type === 16 || type === 64 || type === 128)) {
    throwError2(`Expected TNodeType to have only a single type selected, but got ${toTNodeTypeAsString(type)}.`);
  }
}
function setUpAttributes(renderer, native, attrs) {
  let i = 0;
  while (i < attrs.length) {
    const value = attrs[i];
    if (typeof value === "number") {
      if (value !== 0) {
        break;
      }
      i++;
      const namespaceURI = attrs[i++];
      const attrName = attrs[i++];
      const attrVal = attrs[i++];
      renderer.setAttribute(native, attrName, attrVal, namespaceURI);
    } else {
      const attrName = value;
      const attrVal = attrs[++i];
      if (isAnimationProp(attrName)) {
        renderer.setProperty(native, attrName, attrVal);
      } else {
        renderer.setAttribute(native, attrName, attrVal);
      }
      i++;
    }
  }
  return i;
}
function isNameOnlyAttributeMarker(marker) {
  return marker === 3 || marker === 4 || marker === 6;
}
function isAnimationProp(name) {
  return name.charCodeAt(0) === 64;
}
function mergeHostAttrs(dst, src) {
  if (src === null || src.length === 0) ;
  else if (dst === null || dst.length === 0) {
    dst = src.slice();
  } else {
    let srcMarker = -1;
    for (let i = 0; i < src.length; i++) {
      const item = src[i];
      if (typeof item === "number") {
        srcMarker = item;
      } else {
        if (srcMarker === 0) ;
        else if (srcMarker === -1 || srcMarker === 2) {
          mergeHostAttribute(dst, srcMarker, item, null, src[++i]);
        } else {
          mergeHostAttribute(dst, srcMarker, item, null, null);
        }
      }
    }
  }
  return dst;
}
function mergeHostAttribute(dst, marker, key1, key2, value) {
  let i = 0;
  let markerInsertPosition = dst.length;
  if (marker === -1) {
    markerInsertPosition = -1;
  } else {
    while (i < dst.length) {
      const dstValue = dst[i++];
      if (typeof dstValue === "number") {
        if (dstValue === marker) {
          markerInsertPosition = -1;
          break;
        } else if (dstValue > marker) {
          markerInsertPosition = i - 1;
          break;
        }
      }
    }
  }
  while (i < dst.length) {
    const item = dst[i];
    if (typeof item === "number") {
      break;
    } else if (item === key1) {
      {
        if (value !== null) {
          dst[i + 1] = value;
        }
        return;
      }
    }
    i++;
    if (value !== null) i++;
  }
  if (markerInsertPosition !== -1) {
    dst.splice(markerInsertPosition, 0, marker);
    i = markerInsertPosition + 1;
  }
  dst.splice(i++, 0, key1);
  if (value !== null) {
    dst.splice(i++, 0, value);
  }
}
function hasParentInjector(parentLocation) {
  return parentLocation !== NO_PARENT_INJECTOR;
}
function getParentInjectorIndex(parentLocation) {
  if (ngDevMode) {
    assertNumber(parentLocation, "Number expected");
    assertNotEqual(parentLocation, -1, "Not a valid state.");
    const parentInjectorIndex = parentLocation & 32767;
    assertGreaterThan(parentInjectorIndex, HEADER_OFFSET, "Parent injector must be pointing past HEADER_OFFSET.");
  }
  return parentLocation & 32767;
}
function getParentInjectorViewOffset(parentLocation) {
  return parentLocation >> 16;
}
function getParentInjectorView(location2, startView) {
  let viewOffset = getParentInjectorViewOffset(location2);
  let parentView = startView;
  while (viewOffset > 0) {
    parentView = parentView[DECLARATION_VIEW];
    viewOffset--;
  }
  return parentView;
}
var includeViewProviders = true;
function setIncludeViewProviders(v) {
  const oldValue = includeViewProviders;
  includeViewProviders = v;
  return oldValue;
}
var BLOOM_SIZE = 256;
var BLOOM_MASK = BLOOM_SIZE - 1;
var BLOOM_BUCKET_BITS = 5;
var nextNgElementId = 0;
var NOT_FOUND2 = {};
function bloomAdd(injectorIndex, tView, type) {
  ngDevMode && assertEqual(tView.firstCreatePass, true, "expected firstCreatePass to be true");
  let id;
  if (typeof type === "string") {
    id = type.charCodeAt(0) || 0;
  } else if (type.hasOwnProperty(NG_ELEMENT_ID)) {
    id = type[NG_ELEMENT_ID];
  }
  if (id == null) {
    id = type[NG_ELEMENT_ID] = nextNgElementId++;
  }
  const bloomHash = id & BLOOM_MASK;
  const mask = 1 << bloomHash;
  tView.data[injectorIndex + (bloomHash >> BLOOM_BUCKET_BITS)] |= mask;
}
function getOrCreateNodeInjectorForNode(tNode, lView) {
  const existingInjectorIndex = getInjectorIndex(tNode, lView);
  if (existingInjectorIndex !== -1) {
    return existingInjectorIndex;
  }
  const tView = lView[TVIEW];
  if (tView.firstCreatePass) {
    tNode.injectorIndex = lView.length;
    insertBloom(tView.data, tNode);
    insertBloom(lView, null);
    insertBloom(tView.blueprint, null);
  }
  const parentLoc = getParentInjectorLocation(tNode, lView);
  const injectorIndex = tNode.injectorIndex;
  if (hasParentInjector(parentLoc)) {
    const parentIndex = getParentInjectorIndex(parentLoc);
    const parentLView = getParentInjectorView(parentLoc, lView);
    const parentData = parentLView[TVIEW].data;
    for (let i = 0; i < 8; i++) {
      lView[injectorIndex + i] = parentLView[parentIndex + i] | parentData[parentIndex + i];
    }
  }
  lView[injectorIndex + 8] = parentLoc;
  return injectorIndex;
}
function insertBloom(arr, footer) {
  arr.push(0, 0, 0, 0, 0, 0, 0, 0, footer);
}
function getInjectorIndex(tNode, lView) {
  if (tNode.injectorIndex === -1 || tNode.parent && tNode.parent.injectorIndex === tNode.injectorIndex || lView[tNode.injectorIndex + 8] === null) {
    return -1;
  } else {
    ngDevMode && assertIndexInRange(lView, tNode.injectorIndex);
    return tNode.injectorIndex;
  }
}
function getParentInjectorLocation(tNode, lView) {
  if (tNode.parent && tNode.parent.injectorIndex !== -1) {
    return tNode.parent.injectorIndex;
  }
  let declarationViewOffset = 0;
  let parentTNode = null;
  let lViewCursor = lView;
  while (lViewCursor !== null) {
    parentTNode = getTNodeFromLView(lViewCursor);
    if (parentTNode === null) {
      return NO_PARENT_INJECTOR;
    }
    ngDevMode && parentTNode && assertTNodeForLView(parentTNode, lViewCursor[DECLARATION_VIEW]);
    declarationViewOffset++;
    lViewCursor = lViewCursor[DECLARATION_VIEW];
    if (parentTNode.injectorIndex !== -1) {
      return parentTNode.injectorIndex | declarationViewOffset << 16;
    }
  }
  return NO_PARENT_INJECTOR;
}
function diPublicInInjector(injectorIndex, tView, token) {
  bloomAdd(injectorIndex, tView, token);
}
function injectAttributeImpl(tNode, attrNameToInject) {
  ngDevMode && assertTNodeType(tNode, 12 | 3);
  ngDevMode && assertDefined(tNode, "expecting tNode");
  if (attrNameToInject === "class") {
    return tNode.classes;
  }
  if (attrNameToInject === "style") {
    return tNode.styles;
  }
  const attrs = tNode.attrs;
  if (attrs) {
    const attrsLength = attrs.length;
    let i = 0;
    while (i < attrsLength) {
      const value = attrs[i];
      if (isNameOnlyAttributeMarker(value)) break;
      if (value === 0) {
        i = i + 2;
      } else if (typeof value === "number") {
        i++;
        while (i < attrsLength && typeof attrs[i] === "string") {
          i++;
        }
      } else if (value === attrNameToInject) {
        return attrs[i + 1];
      } else {
        i = i + 2;
      }
    }
  }
  return null;
}
function notFoundValueOrThrow(notFoundValue, token, flags) {
  if (flags & 8 || notFoundValue !== void 0) {
    return notFoundValue;
  } else {
    throwProviderNotFoundError(token, "NodeInjector");
  }
}
function lookupTokenUsingModuleInjector(lView, token, flags, notFoundValue) {
  if (flags & 8 && notFoundValue === void 0) {
    notFoundValue = null;
  }
  if ((flags & (2 | 1)) === 0) {
    const moduleInjector = lView[INJECTOR];
    const previousInjectImplementation = setInjectImplementation(void 0);
    try {
      if (moduleInjector) {
        return moduleInjector.get(token, notFoundValue, flags & 8);
      } else {
        return injectRootLimpMode(token, notFoundValue, flags & 8);
      }
    } finally {
      setInjectImplementation(previousInjectImplementation);
    }
  }
  return notFoundValueOrThrow(notFoundValue, token, flags);
}
function getOrCreateInjectable(tNode, lView, token, flags = 0, notFoundValue) {
  if (tNode !== null) {
    if (lView[FLAGS] & 2048 && !(flags & 2)) {
      const embeddedInjectorValue = lookupTokenUsingEmbeddedInjector(tNode, lView, token, flags, NOT_FOUND2);
      if (embeddedInjectorValue !== NOT_FOUND2) {
        return embeddedInjectorValue;
      }
    }
    const value = lookupTokenUsingNodeInjector(tNode, lView, token, flags, NOT_FOUND2);
    if (value !== NOT_FOUND2) {
      return value;
    }
  }
  return lookupTokenUsingModuleInjector(lView, token, flags, notFoundValue);
}
function lookupTokenUsingNodeInjector(tNode, lView, token, flags, notFoundValue) {
  const bloomHash = bloomHashBitOrFactory(token);
  if (typeof bloomHash === "function") {
    if (!enterDI(lView, tNode, flags)) {
      return flags & 1 ? notFoundValueOrThrow(notFoundValue, token, flags) : lookupTokenUsingModuleInjector(lView, token, flags, notFoundValue);
    }
    try {
      let value;
      if (ngDevMode) {
        runInInjectorProfilerContext(new NodeInjector(getCurrentTNode(), getLView()), token, () => {
          emitInjectorToCreateInstanceEvent(token);
          value = bloomHash(flags);
          emitInstanceCreatedByInjectorEvent(value);
        });
      } else {
        value = bloomHash(flags);
      }
      if (value == null && !(flags & 8)) {
        throwProviderNotFoundError(token);
      } else {
        return value;
      }
    } finally {
      leaveDI();
    }
  } else if (typeof bloomHash === "number") {
    let previousTView = null;
    let injectorIndex = getInjectorIndex(tNode, lView);
    let parentLocation = NO_PARENT_INJECTOR;
    let hostTElementNode = flags & 1 ? lView[DECLARATION_COMPONENT_VIEW][T_HOST] : null;
    if (injectorIndex === -1 || flags & 4) {
      parentLocation = injectorIndex === -1 ? getParentInjectorLocation(tNode, lView) : lView[injectorIndex + 8];
      if (parentLocation === NO_PARENT_INJECTOR || !shouldSearchParent(flags, false)) {
        injectorIndex = -1;
      } else {
        previousTView = lView[TVIEW];
        injectorIndex = getParentInjectorIndex(parentLocation);
        lView = getParentInjectorView(parentLocation, lView);
      }
    }
    while (injectorIndex !== -1) {
      ngDevMode && assertNodeInjector(lView, injectorIndex);
      const tView = lView[TVIEW];
      ngDevMode && assertTNodeForLView(tView.data[injectorIndex + 8], lView);
      if (bloomHasToken(bloomHash, injectorIndex, tView.data)) {
        const instance = searchTokensOnInjector(injectorIndex, lView, token, previousTView, flags, hostTElementNode);
        if (instance !== NOT_FOUND2) {
          return instance;
        }
      }
      parentLocation = lView[injectorIndex + 8];
      if (parentLocation !== NO_PARENT_INJECTOR && shouldSearchParent(flags, lView[TVIEW].data[injectorIndex + 8] === hostTElementNode) && bloomHasToken(bloomHash, injectorIndex, lView)) {
        previousTView = tView;
        injectorIndex = getParentInjectorIndex(parentLocation);
        lView = getParentInjectorView(parentLocation, lView);
      } else {
        injectorIndex = -1;
      }
    }
  }
  return notFoundValue;
}
function searchTokensOnInjector(injectorIndex, lView, token, previousTView, flags, hostTElementNode) {
  const currentTView = lView[TVIEW];
  const tNode = currentTView.data[injectorIndex + 8];
  const canAccessViewProviders = previousTView == null ? isComponentHost(tNode) && includeViewProviders : previousTView != currentTView && (tNode.type & 3) !== 0;
  const isHostSpecialCase = flags & 1 && hostTElementNode === tNode;
  const injectableIdx = locateDirectiveOrProvider(tNode, currentTView, token, canAccessViewProviders, isHostSpecialCase);
  if (injectableIdx !== null) {
    return getNodeInjectable(lView, currentTView, injectableIdx, tNode, flags);
  } else {
    return NOT_FOUND2;
  }
}
function locateDirectiveOrProvider(tNode, tView, token, canAccessViewProviders, isHostSpecialCase) {
  const nodeProviderIndexes = tNode.providerIndexes;
  const tInjectables = tView.data;
  const injectablesStart = nodeProviderIndexes & 1048575;
  const directivesStart = tNode.directiveStart;
  const directiveEnd = tNode.directiveEnd;
  const cptViewProvidersCount = nodeProviderIndexes >> 20;
  const startingIndex = canAccessViewProviders ? injectablesStart : injectablesStart + cptViewProvidersCount;
  const endIndex = isHostSpecialCase ? injectablesStart + cptViewProvidersCount : directiveEnd;
  for (let i = startingIndex; i < endIndex; i++) {
    const providerTokenOrDef = tInjectables[i];
    if (i < directivesStart && token === providerTokenOrDef || i >= directivesStart && providerTokenOrDef.type === token) {
      return i;
    }
  }
  if (isHostSpecialCase) {
    const dirDef = tInjectables[directivesStart];
    if (dirDef && isComponentDef(dirDef) && dirDef.type === token) {
      return directivesStart;
    }
  }
  return null;
}
var injectionPath = [];
function getNodeInjectable(lView, tView, index, tNode, flags) {
  let value = lView[index];
  const tData = tView.data;
  if (value instanceof NodeInjectorFactory) {
    const factory = value;
    ngDevMode && injectionPath.push(factory.name ?? "unknown");
    if (factory.resolving) {
      let token2 = "";
      if (ngDevMode) {
        token2 = stringifyForError(tData[index]);
        throw cyclicDependencyErrorWithDetails(token2, injectionPath);
      } else {
        throw cyclicDependencyError(token2);
      }
    }
    const previousIncludeViewProviders = setIncludeViewProviders(factory.canSeeViewProviders);
    factory.resolving = true;
    const token = tData[index].type || tData[index];
    let prevInjectContext;
    if (ngDevMode) {
      const injector = new NodeInjector(tNode, lView);
      prevInjectContext = setInjectorProfilerContext({
        injector,
        token
      });
    }
    const previousInjectImplementation = factory.injectImpl ? setInjectImplementation(factory.injectImpl) : null;
    const success = enterDI(lView, tNode, 0);
    ngDevMode && assertEqual(success, true, "Because flags do not contain `SkipSelf' we expect this to always succeed.");
    try {
      ngDevMode && emitInjectorToCreateInstanceEvent(token);
      value = lView[index] = factory.factory(void 0, flags, tData, lView, tNode);
      ngDevMode && emitInstanceCreatedByInjectorEvent(value);
      if (tView.firstCreatePass && index >= tNode.directiveStart) {
        ngDevMode && assertDirectiveDef(tData[index]);
        registerPreOrderHooks(index, tData[index], tView);
      }
    } finally {
      ngDevMode && setInjectorProfilerContext(prevInjectContext);
      previousInjectImplementation !== null && setInjectImplementation(previousInjectImplementation);
      setIncludeViewProviders(previousIncludeViewProviders);
      factory.resolving = false;
      leaveDI();
      ngDevMode && (injectionPath = []);
    }
  }
  return value;
}
function bloomHashBitOrFactory(token) {
  ngDevMode && assertDefined(token, "token must be defined");
  if (typeof token === "string") {
    return token.charCodeAt(0) || 0;
  }
  const tokenId = token.hasOwnProperty(NG_ELEMENT_ID) ? token[NG_ELEMENT_ID] : void 0;
  if (typeof tokenId === "number") {
    if (tokenId >= 0) {
      return tokenId & BLOOM_MASK;
    } else {
      ngDevMode && assertEqual(tokenId, -1, "Expecting to get Special Injector Id");
      return createNodeInjector;
    }
  } else {
    return tokenId;
  }
}
function bloomHasToken(bloomHash, injectorIndex, injectorView) {
  const mask = 1 << bloomHash;
  const value = injectorView[injectorIndex + (bloomHash >> BLOOM_BUCKET_BITS)];
  return !!(value & mask);
}
function shouldSearchParent(flags, isFirstHostTNode) {
  return !(flags & 2) && !(flags & 1 && isFirstHostTNode);
}
function getNodeInjectorLView(nodeInjector) {
  return nodeInjector._lView;
}
function getNodeInjectorTNode(nodeInjector) {
  return nodeInjector._tNode;
}
var NodeInjector = class {
  _tNode;
  _lView;
  constructor(_tNode, _lView) {
    this._tNode = _tNode;
    this._lView = _lView;
  }
  get(token, notFoundValue, flags) {
    return getOrCreateInjectable(this._tNode, this._lView, token, convertToBitFlags(flags), notFoundValue);
  }
};
function createNodeInjector() {
  return new NodeInjector(getCurrentTNode(), getLView());
}
function \u0275\u0275getInheritedFactory(type) {
  return noSideEffects(() => {
    const ownConstructor = type.prototype.constructor;
    const ownFactory = ownConstructor[NG_FACTORY_DEF] || getFactoryOf(ownConstructor);
    const objectPrototype = Object.prototype;
    let parent = Object.getPrototypeOf(type.prototype).constructor;
    while (parent && parent !== objectPrototype) {
      const factory = parent[NG_FACTORY_DEF] || getFactoryOf(parent);
      if (factory && factory !== ownFactory) {
        return factory;
      }
      parent = Object.getPrototypeOf(parent);
    }
    return (t) => new t();
  });
}
function getFactoryOf(type) {
  if (isForwardRef(type)) {
    return () => {
      const factory = getFactoryOf(resolveForwardRef(type));
      return factory && factory();
    };
  }
  return getFactoryDef(type);
}
function lookupTokenUsingEmbeddedInjector(tNode, lView, token, flags, notFoundValue) {
  let currentTNode = tNode;
  let currentLView = lView;
  while (currentTNode !== null && currentLView !== null && currentLView[FLAGS] & 2048 && !isRootView(currentLView)) {
    ngDevMode && assertTNodeForLView(currentTNode, currentLView);
    const nodeInjectorValue = lookupTokenUsingNodeInjector(currentTNode, currentLView, token, flags | 2, NOT_FOUND2);
    if (nodeInjectorValue !== NOT_FOUND2) {
      return nodeInjectorValue;
    }
    let parentTNode = currentTNode.parent;
    if (!parentTNode) {
      const embeddedViewInjector = currentLView[EMBEDDED_VIEW_INJECTOR];
      if (embeddedViewInjector) {
        const embeddedViewInjectorValue = embeddedViewInjector.get(token, NOT_FOUND2, flags & -5);
        if (embeddedViewInjectorValue !== NOT_FOUND2) {
          return embeddedViewInjectorValue;
        }
      }
      parentTNode = getTNodeFromLView(currentLView);
      currentLView = currentLView[DECLARATION_VIEW];
    }
    currentTNode = parentTNode;
  }
  return notFoundValue;
}
function getTNodeFromLView(lView) {
  const tView = lView[TVIEW];
  const tViewType = tView.type;
  if (tViewType === 2) {
    ngDevMode && assertDefined(tView.declTNode, "Embedded TNodes should have declaration parents.");
    return tView.declTNode;
  } else if (tViewType === 1) {
    return lView[T_HOST];
  }
  return null;
}
function \u0275\u0275injectAttribute(attrNameToInject) {
  return injectAttributeImpl(getCurrentTNode(), attrNameToInject);
}
var Attribute = makeParamDecorator("Attribute", (attributeName) => ({
  attributeName,
  __NG_ELEMENT_ID__: () => \u0275\u0275injectAttribute(attributeName)
}));
var _reflect = null;
function getReflect() {
  return _reflect = _reflect || new ReflectionCapabilities();
}
function reflectDependencies(type) {
  return convertDependencies(getReflect().parameters(type));
}
function convertDependencies(deps) {
  return deps.map((dep) => reflectDependency(dep));
}
function reflectDependency(dep) {
  const meta = {
    token: null,
    attribute: null,
    host: false,
    optional: false,
    self: false,
    skipSelf: false
  };
  if (Array.isArray(dep) && dep.length > 0) {
    for (let j = 0; j < dep.length; j++) {
      const param = dep[j];
      if (param === void 0) {
        continue;
      }
      const proto = Object.getPrototypeOf(param);
      if (param instanceof Optional || proto.ngMetadataName === "Optional") {
        meta.optional = true;
      } else if (param instanceof SkipSelf || proto.ngMetadataName === "SkipSelf") {
        meta.skipSelf = true;
      } else if (param instanceof Self || proto.ngMetadataName === "Self") {
        meta.self = true;
      } else if (param instanceof Host || proto.ngMetadataName === "Host") {
        meta.host = true;
      } else if (param instanceof Inject) {
        meta.token = param.token;
      } else if (param instanceof Attribute) {
        if (param.attributeName === void 0) {
          throw new RuntimeError(-204, ngDevMode && `Attribute name must be defined.`);
        }
        meta.attribute = param.attributeName;
      } else {
        meta.token = param;
      }
    }
  } else if (dep === void 0 || Array.isArray(dep) && dep.length === 0) {
    meta.token = null;
  } else {
    meta.token = dep;
  }
  return meta;
}
function compileInjectable(type, meta) {
  let ngInjectableDef = null;
  let ngFactoryDef = null;
  if (!type.hasOwnProperty(NG_PROV_DEF)) {
    Object.defineProperty(type, NG_PROV_DEF, {
      get: () => {
        if (ngInjectableDef === null) {
          const compiler = getCompilerFacade({
            usage: 0,
            kind: "injectable",
            type
          });
          ngInjectableDef = compiler.compileInjectable(angularCoreDiEnv, `ng:///${type.name}/\u0275prov.js`, getInjectableMetadata(type, meta));
        }
        return ngInjectableDef;
      }
    });
  }
  if (!type.hasOwnProperty(NG_FACTORY_DEF)) {
    Object.defineProperty(type, NG_FACTORY_DEF, {
      get: () => {
        if (ngFactoryDef === null) {
          const compiler = getCompilerFacade({
            usage: 0,
            kind: "injectable",
            type
          });
          ngFactoryDef = compiler.compileFactory(angularCoreDiEnv, `ng:///${type.name}/\u0275fac.js`, {
            name: type.name,
            type,
            typeArgumentCount: 0,
            deps: reflectDependencies(type),
            target: compiler.FactoryTarget.Injectable
          });
        }
        return ngFactoryDef;
      },
      configurable: true
    });
  }
}
var USE_VALUE2 = getClosureSafeProperty({
  provide: String,
  useValue: getClosureSafeProperty
});
function isUseClassProvider(meta) {
  return meta.useClass !== void 0;
}
function isUseValueProvider(meta) {
  return USE_VALUE2 in meta;
}
function isUseFactoryProvider(meta) {
  return meta.useFactory !== void 0;
}
function isUseExistingProvider(meta) {
  return meta.useExisting !== void 0;
}
function getInjectableMetadata(type, srcMeta) {
  const meta = srcMeta || {
    providedIn: null
  };
  const compilerMeta = {
    name: type.name,
    type,
    typeArgumentCount: 0,
    providedIn: meta.providedIn
  };
  if ((isUseClassProvider(meta) || isUseFactoryProvider(meta)) && meta.deps !== void 0) {
    compilerMeta.deps = convertDependencies(meta.deps);
  }
  if (isUseClassProvider(meta)) {
    compilerMeta.useClass = meta.useClass;
  } else if (isUseValueProvider(meta)) {
    compilerMeta.useValue = meta.useValue;
  } else if (isUseFactoryProvider(meta)) {
    compilerMeta.useFactory = meta.useFactory;
  } else if (isUseExistingProvider(meta)) {
    compilerMeta.useExisting = meta.useExisting;
  }
  return compilerMeta;
}
var Injectable = makeDecorator("Injectable", void 0, void 0, void 0, (type, meta) => compileInjectable(type, meta));
function injectElementRef() {
  return createElementRef(getCurrentTNode(), getLView());
}
function createElementRef(tNode, lView) {
  return new ElementRef(getNativeByTNode(tNode, lView));
}
var ElementRef = class {
  nativeElement;
  constructor(nativeElement) {
    this.nativeElement = nativeElement;
  }
  static __NG_ELEMENT_ID__ = injectElementRef;
};
function unwrapElementRef(value) {
  return value instanceof ElementRef ? value.nativeElement : value;
}
function symbolIterator() {
  return this._results[Symbol.iterator]();
}
var QueryList = class {
  _emitDistinctChangesOnly;
  dirty = true;
  _onDirty = void 0;
  _results = [];
  _changesDetected = false;
  _changes = void 0;
  length = 0;
  first = void 0;
  last = void 0;
  get changes() {
    return this._changes ??= new Subject();
  }
  constructor(_emitDistinctChangesOnly = false) {
    this._emitDistinctChangesOnly = _emitDistinctChangesOnly;
  }
  get(index) {
    return this._results[index];
  }
  map(fn) {
    return this._results.map(fn);
  }
  filter(fn) {
    return this._results.filter(fn);
  }
  find(fn) {
    return this._results.find(fn);
  }
  reduce(fn, init) {
    return this._results.reduce(fn, init);
  }
  forEach(fn) {
    this._results.forEach(fn);
  }
  some(fn) {
    return this._results.some(fn);
  }
  toArray() {
    return this._results.slice();
  }
  toString() {
    return this._results.toString();
  }
  reset(resultsTree, identityAccessor) {
    this.dirty = false;
    const newResultFlat = flatten(resultsTree);
    if (this._changesDetected = !arrayEquals(this._results, newResultFlat, identityAccessor)) {
      this._results = newResultFlat;
      this.length = newResultFlat.length;
      this.last = newResultFlat[this.length - 1];
      this.first = newResultFlat[0];
    }
  }
  notifyOnChanges() {
    if (this._changes !== void 0 && (this._changesDetected || !this._emitDistinctChangesOnly)) this._changes.next(this);
  }
  onDirty(cb) {
    this._onDirty = cb;
  }
  setDirty() {
    this.dirty = true;
    this._onDirty?.();
  }
  destroy() {
    if (this._changes !== void 0) {
      this._changes.complete();
      this._changes.unsubscribe();
    }
  }
  [Symbol.iterator] = /* @__PURE__ */ (() => symbolIterator)();
};
function hasInSkipHydrationBlockFlag(tNode) {
  return (tNode.flags & 128) === 128;
}
var ChangeDetectionStrategy;
(function(ChangeDetectionStrategy2) {
  ChangeDetectionStrategy2[ChangeDetectionStrategy2["OnPush"] = 0] = "OnPush";
  ChangeDetectionStrategy2[ChangeDetectionStrategy2["Eager"] = 1] = "Eager";
  ChangeDetectionStrategy2[ChangeDetectionStrategy2["Default"] = 1] = "Default";
})(ChangeDetectionStrategy || (ChangeDetectionStrategy = {}));
var TRACKED_LVIEWS = /* @__PURE__ */ new Map();
var uniqueIdCounter = 0;
function getUniqueLViewId() {
  return uniqueIdCounter++;
}
function registerLView(lView) {
  ngDevMode && assertNumber(lView[ID], "LView must have an ID in order to be registered");
  TRACKED_LVIEWS.set(lView[ID], lView);
}
function getLViewById(id) {
  ngDevMode && assertNumber(id, "ID used for LView lookup must be a number");
  return TRACKED_LVIEWS.get(id) || null;
}
function unregisterLView(lView) {
  ngDevMode && assertNumber(lView[ID], "Cannot stop tracking an LView that does not have an ID");
  TRACKED_LVIEWS.delete(lView[ID]);
}
function getTrackedLViews() {
  return TRACKED_LVIEWS;
}
var LContext = class {
  lViewId;
  nodeIndex;
  native;
  component;
  directives;
  localRefs;
  get lView() {
    return getLViewById(this.lViewId);
  }
  constructor(lViewId, nodeIndex, native) {
    this.lViewId = lViewId;
    this.nodeIndex = nodeIndex;
    this.native = native;
  }
};
function getLContext(target) {
  let mpValue = readPatchedData(target);
  if (mpValue) {
    if (isLView(mpValue)) {
      const lView = mpValue;
      let nodeIndex;
      let component = void 0;
      let directives = void 0;
      if (isComponentInstance(target)) {
        nodeIndex = findViaComponent(lView, target);
        if (nodeIndex == -1) {
          throw new Error("The provided component was not found in the application");
        }
        component = target;
      } else if (isDirectiveInstance(target)) {
        nodeIndex = findViaDirective(lView, target);
        if (nodeIndex == -1) {
          throw new Error("The provided directive was not found in the application");
        }
        directives = getDirectivesAtNodeIndex(nodeIndex, lView);
      } else {
        nodeIndex = findViaNativeElement(lView, target);
        if (nodeIndex == -1) {
          return null;
        }
      }
      const native = unwrapRNode(lView[nodeIndex]);
      const existingCtx = readPatchedData(native);
      const context2 = existingCtx && !Array.isArray(existingCtx) ? existingCtx : createLContext(lView, nodeIndex, native);
      if (component && context2.component === void 0) {
        context2.component = component;
        attachPatchData(context2.component, context2);
      }
      if (directives && context2.directives === void 0) {
        context2.directives = directives;
        for (let i = 0; i < directives.length; i++) {
          attachPatchData(directives[i], context2);
        }
      }
      attachPatchData(context2.native, context2);
      mpValue = context2;
    }
  } else {
    const rElement = target;
    ngDevMode && assertDomNode(rElement);
    let parent = rElement;
    while (parent = parent.parentNode) {
      const parentContext = readPatchedData(parent);
      if (parentContext) {
        const lView = Array.isArray(parentContext) ? parentContext : parentContext.lView;
        if (!lView) {
          return null;
        }
        const index = findViaNativeElement(lView, rElement);
        if (index >= 0) {
          const native = unwrapRNode(lView[index]);
          const context2 = createLContext(lView, index, native);
          attachPatchData(native, context2);
          mpValue = context2;
          break;
        }
      }
    }
  }
  return mpValue || null;
}
function createLContext(lView, nodeIndex, native) {
  return new LContext(lView[ID], nodeIndex, native);
}
function getComponentViewByInstance(componentInstance) {
  let patchedData = readPatchedData(componentInstance);
  let lView;
  if (isLView(patchedData)) {
    const contextLView = patchedData;
    const nodeIndex = findViaComponent(contextLView, componentInstance);
    lView = getComponentLViewByIndex(nodeIndex, contextLView);
    const context2 = createLContext(contextLView, nodeIndex, lView[HOST]);
    context2.component = componentInstance;
    attachPatchData(componentInstance, context2);
    attachPatchData(context2.native, context2);
  } else {
    const context2 = patchedData;
    const contextLView = context2.lView;
    ngDevMode && assertLView(contextLView);
    lView = getComponentLViewByIndex(context2.nodeIndex, contextLView);
  }
  return lView;
}
var MONKEY_PATCH_KEY_NAME = "__ngContext__";
function attachPatchData(target, data) {
  ngDevMode && assertDefined(target, "Target expected");
  if (isLView(data)) {
    target[MONKEY_PATCH_KEY_NAME] = data[ID];
    registerLView(data);
  } else {
    target[MONKEY_PATCH_KEY_NAME] = data;
  }
}
function readPatchedData(target) {
  ngDevMode && assertDefined(target, "Target expected");
  const data = target[MONKEY_PATCH_KEY_NAME];
  return typeof data === "number" ? getLViewById(data) : data || null;
}
function readPatchedLView(target) {
  const value = readPatchedData(target);
  if (value) {
    return isLView(value) ? value : value.lView;
  }
  return null;
}
function isComponentInstance(instance) {
  return instance && instance.constructor && instance.constructor.\u0275cmp;
}
function isDirectiveInstance(instance) {
  return instance && instance.constructor && instance.constructor.\u0275dir;
}
function findViaNativeElement(lView, target) {
  const tView = lView[TVIEW];
  for (let i = HEADER_OFFSET; i < tView.bindingStartIndex; i++) {
    if (unwrapRNode(lView[i]) === target) {
      return i;
    }
  }
  return -1;
}
function traverseNextElement(tNode) {
  if (tNode.child) {
    return tNode.child;
  } else if (tNode.next) {
    return tNode.next;
  } else {
    while (tNode.parent && !tNode.parent.next) {
      tNode = tNode.parent;
    }
    return tNode.parent && tNode.parent.next;
  }
}
function findViaComponent(lView, componentInstance) {
  const componentIndices = lView[TVIEW].components;
  if (componentIndices) {
    for (let i = 0; i < componentIndices.length; i++) {
      const elementComponentIndex = componentIndices[i];
      const componentView = getComponentLViewByIndex(elementComponentIndex, lView);
      if (componentView[CONTEXT] === componentInstance) {
        return elementComponentIndex;
      }
    }
  } else {
    const rootComponentView = getComponentLViewByIndex(HEADER_OFFSET, lView);
    const rootComponent = rootComponentView[CONTEXT];
    if (rootComponent === componentInstance) {
      return HEADER_OFFSET;
    }
  }
  return -1;
}
function findViaDirective(lView, directiveInstance) {
  let tNode = lView[TVIEW].firstChild;
  while (tNode) {
    const directiveIndexStart = tNode.directiveStart;
    const directiveIndexEnd = tNode.directiveEnd;
    for (let i = directiveIndexStart; i < directiveIndexEnd; i++) {
      if (lView[i] === directiveInstance) {
        return tNode.index;
      }
    }
    tNode = traverseNextElement(tNode);
  }
  return -1;
}
function getDirectivesAtNodeIndex(nodeIndex, lView) {
  const tNode = lView[TVIEW].data[nodeIndex];
  if (tNode.directiveStart === 0) return EMPTY_ARRAY;
  const results = [];
  for (let i = tNode.directiveStart; i < tNode.directiveEnd; i++) {
    const directiveInstance = lView[i];
    if (!isComponentInstance(directiveInstance)) {
      results.push(directiveInstance);
    }
  }
  return results;
}
function getComponentAtNodeIndex(nodeIndex, lView) {
  const tNode = lView[TVIEW].data[nodeIndex];
  return isComponentHost(tNode) ? lView[tNode.directiveStart + tNode.componentOffset] : null;
}
function getRootView(componentOrLView) {
  ngDevMode && assertDefined(componentOrLView, "component");
  let lView = isLView(componentOrLView) ? componentOrLView : readPatchedLView(componentOrLView);
  while (lView && !isRootView(lView)) {
    lView = getLViewParent(lView);
  }
  ngDevMode && assertLView(lView);
  return lView;
}
function getRootContext(viewOrComponent) {
  const rootView = getRootView(viewOrComponent);
  ngDevMode && assertDefined(rootView[CONTEXT], "Root view has no context. Perhaps it is disconnected?");
  return rootView[CONTEXT];
}
function getFirstLContainer(lView) {
  return getNearestLContainer(lView[CHILD_HEAD]);
}
function getNextLContainer(container) {
  return getNearestLContainer(container[NEXT]);
}
function getNearestLContainer(viewOrContainer) {
  while (viewOrContainer !== null && !isLContainer(viewOrContainer)) {
    viewOrContainer = viewOrContainer[NEXT];
  }
  return viewOrContainer;
}
function getComponent(element) {
  ngDevMode && assertDomElement(element);
  const context2 = getLContext(element);
  if (context2 === null) return null;
  if (context2.component === void 0) {
    const lView = context2.lView;
    if (lView === null) {
      return null;
    }
    context2.component = getComponentAtNodeIndex(context2.nodeIndex, lView);
  }
  return context2.component;
}
function getContext(element) {
  assertDomElement(element);
  const context2 = getLContext(element);
  const lView = context2 ? context2.lView : null;
  return lView === null ? null : lView[CONTEXT];
}
function getOwningComponent(elementOrDir) {
  const context2 = getLContext(elementOrDir);
  let lView = context2 ? context2.lView : null;
  if (lView === null) return null;
  let parent;
  while (lView[TVIEW].type === 2 && (parent = getLViewParent(lView))) {
    lView = parent;
  }
  return isRootView(lView) ? null : lView[CONTEXT];
}
function getRootComponents(elementOrDir) {
  const lView = readPatchedLView(elementOrDir);
  return lView !== null ? [getRootContext(lView)] : [];
}
function getInjector(elementOrDir) {
  const context2 = getLContext(elementOrDir);
  const lView = context2 ? context2.lView : null;
  if (lView === null) return Injector.NULL;
  const tNode = lView[TVIEW].data[context2.nodeIndex];
  return new NodeInjector(tNode, lView);
}
function getDirectives(node) {
  if (node instanceof Text) {
    return [];
  }
  const context2 = getLContext(node);
  const lView = context2 ? context2.lView : null;
  if (lView === null) {
    return [];
  }
  const tView = lView[TVIEW];
  const nodeIndex = context2.nodeIndex;
  if (!tView?.data[nodeIndex]) {
    return [];
  }
  if (context2.directives === void 0) {
    context2.directives = getDirectivesAtNodeIndex(nodeIndex, lView);
  }
  return context2.directives === null ? [] : [...context2.directives];
}
var AcxChangeDetectionStrategy;
(function(AcxChangeDetectionStrategy2) {
  AcxChangeDetectionStrategy2[AcxChangeDetectionStrategy2["Default"] = 0] = "Default";
  AcxChangeDetectionStrategy2[AcxChangeDetectionStrategy2["OnPush"] = 1] = "OnPush";
})(AcxChangeDetectionStrategy || (AcxChangeDetectionStrategy = {}));
var AcxViewEncapsulation;
(function(AcxViewEncapsulation2) {
  AcxViewEncapsulation2[AcxViewEncapsulation2["Emulated"] = 0] = "Emulated";
  AcxViewEncapsulation2[AcxViewEncapsulation2["None"] = 1] = "None";
})(AcxViewEncapsulation || (AcxViewEncapsulation = {}));
function getDirectiveMetadata$1(directiveOrComponentInstance) {
  const {
    constructor
  } = directiveOrComponentInstance;
  if (!constructor) {
    throw new Error("Unable to find the instance constructor");
  }
  const componentDef = getComponentDef(constructor);
  if (componentDef) {
    const inputs = extractInputDebugMetadata(componentDef.inputs);
    return {
      inputs,
      outputs: componentDef.outputs,
      encapsulation: componentDef.encapsulation,
      changeDetection: componentDef.onPush ? ChangeDetectionStrategy.OnPush : ChangeDetectionStrategy.Eager
    };
  }
  const directiveDef = getDirectiveDef(constructor);
  if (directiveDef) {
    const inputs = extractInputDebugMetadata(directiveDef.inputs);
    return {
      inputs,
      outputs: directiveDef.outputs
    };
  }
  return null;
}
function getHostElement(componentOrDirective) {
  return getLContext(componentOrDirective).native;
}
function getListeners(element) {
  ngDevMode && assertDomElement(element);
  const lContext = getLContext(element);
  const lView = lContext === null ? null : lContext.lView;
  if (lView === null) return [];
  const tView = lView[TVIEW];
  const lCleanup = lView[CLEANUP];
  const tCleanup = tView.cleanup;
  const listeners = [];
  if (tCleanup && lCleanup) {
    for (let i = 0; i < tCleanup.length; ) {
      const firstParam = tCleanup[i++];
      const secondParam = tCleanup[i++];
      if (typeof firstParam === "string") {
        const name = firstParam;
        const listenerElement = unwrapRNode(lView[secondParam]);
        const callback = lCleanup[tCleanup[i++]];
        const useCaptureOrIndx = tCleanup[i++];
        const type = typeof useCaptureOrIndx === "boolean" || useCaptureOrIndx >= 0 ? "dom" : "output";
        const useCapture = typeof useCaptureOrIndx === "boolean" ? useCaptureOrIndx : false;
        if (element == listenerElement) {
          listeners.push({
            element,
            name,
            callback,
            useCapture,
            type
          });
        }
      }
    }
  }
  listeners.sort(sortListeners);
  return listeners;
}
function sortListeners(a, b) {
  if (a.name == b.name) return 0;
  return a.name < b.name ? -1 : 1;
}
function assertDomElement(value) {
  if (typeof Element !== "undefined" && !(value instanceof Element)) {
    throw new Error("Expecting instance of DOM Element");
  }
}
function extractInputDebugMetadata(inputs) {
  const res = {};
  for (const key in inputs) {
    if (inputs.hasOwnProperty(key)) {
      const value = inputs[key];
      if (value !== void 0) {
        res[key] = value[0];
      }
    }
  }
  return res;
}
var DOCUMENT2 = void 0;
function setDocument(document2) {
  DOCUMENT2 = document2;
}
function getDocument() {
  if (DOCUMENT2 !== void 0) {
    return DOCUMENT2;
  } else if (typeof document !== "undefined") {
    return document;
  }
  throw new RuntimeError(210, (typeof ngDevMode === "undefined" || ngDevMode) && `The document object is not available in this context. Make sure the DOCUMENT injection token is provided.`);
}
var APP_ID = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "AppId" : "", {
  factory: () => DEFAULT_APP_ID
});
var DEFAULT_APP_ID = "ng";
var validAppIdInitializer = {
  provide: ENVIRONMENT_INITIALIZER,
  multi: true,
  useValue: () => {
    const appId = inject2(APP_ID);
    const isAlphanumeric = /^[a-zA-Z0-9\-_]+$/.test(appId);
    if (!isAlphanumeric) {
      throw new RuntimeError(211, `APP_ID value "${appId}" is not alphanumeric. The APP_ID must be a string of alphanumeric characters. (a-zA-Z0-9), hyphens (-) and underscores (_) are allowed.`);
    }
  }
};
var PLATFORM_INITIALIZER = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "Platform Initializer" : "");
var PLATFORM_ID = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "Platform ID" : "", {
  providedIn: "platform",
  factory: () => "unknown"
});
var ANIMATION_MODULE_TYPE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "AnimationModuleType" : "");
var CSP_NONCE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "CSP nonce" : "", {
  factory: () => {
    return inject2(DOCUMENT).body?.querySelector("[ngCspNonce]")?.getAttribute("ngCspNonce") || null;
  }
});
var IMAGE_CONFIG_DEFAULTS = {
  breakpoints: [16, 32, 48, 64, 96, 128, 256, 384, 640, 750, 828, 1080, 1200, 1920, 2048, 3840],
  placeholderResolution: 30,
  disableImageSizeWarning: false,
  disableImageLazyLoadWarning: false
};
var IMAGE_CONFIG = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "ImageConfig" : "", {
  factory: () => IMAGE_CONFIG_DEFAULTS
});
function makeStateKey(key) {
  return key;
}
var TransferState = class _TransferState {
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TransferState,
    providedIn: "root",
    factory: () => {
      const transferState = new _TransferState();
      if (true) {
        transferState.store = retrieveTransferredState(inject2(DOCUMENT), inject2(APP_ID));
      }
      return transferState;
    }
  });
  store = {};
  onSerializeCallbacks = {};
  get(key, defaultValue) {
    return this.store[key] !== void 0 ? this.store[key] : defaultValue;
  }
  set(key, value) {
    this.store[key] = value;
  }
  remove(key) {
    delete this.store[key];
  }
  hasKey(key) {
    return this.store.hasOwnProperty(key);
  }
  get isEmpty() {
    return Object.keys(this.store).length === 0;
  }
  onSerialize(key, callback) {
    this.onSerializeCallbacks[key] = callback;
  }
  toJson() {
    for (const key in this.onSerializeCallbacks) {
      if (this.onSerializeCallbacks.hasOwnProperty(key)) {
        try {
          this.store[key] = this.onSerializeCallbacks[key]();
        } catch (e) {
          console.warn("Exception in onSerialize callback: ", e);
        }
      }
    }
    return JSON.stringify(this.store).replace(/</g, "\\u003C").replace(/\//g, "\\u002F");
  }
};
function retrieveTransferredState(doc, appId) {
  const script = doc.getElementById(appId + "-state");
  if (script?.tagName === "SCRIPT" && script.textContent) {
    try {
      return JSON.parse(script.textContent);
    } catch (e) {
      console.warn("Exception while restoring TransferState for app " + appId, e);
    }
  }
  return {};
}
var NUM_ROOT_NODES = "r";
var DEFER_BLOCK_ID = "di";
var DEFER_BLOCK_STATE$1 = "s";
var DEFER_PARENT_BLOCK_ID = "p";
var IS_HYDRATION_DOM_REUSE_ENABLED = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "IS_HYDRATION_DOM_REUSE_ENABLED" : "");
var PRESERVE_HOST_CONTENT_DEFAULT = false;
var PRESERVE_HOST_CONTENT = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "PRESERVE_HOST_CONTENT" : "", {
  factory: () => PRESERVE_HOST_CONTENT_DEFAULT
});
var IS_I18N_HYDRATION_ENABLED = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "IS_I18N_HYDRATION_ENABLED" : "");
var IS_EVENT_REPLAY_ENABLED = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "IS_EVENT_REPLAY_ENABLED" : "");
var EVENT_REPLAY_QUEUE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "EVENT_REPLAY_QUEUE" : "", {
  factory: () => []
});
var IS_INCREMENTAL_HYDRATION_ENABLED = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "IS_INCREMENTAL_HYDRATION_ENABLED" : "");
var JSACTION_BLOCK_ELEMENT_MAP = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "JSACTION_BLOCK_ELEMENT_MAP" : "", {
  factory: () => /* @__PURE__ */ new Map()
});
var IS_ENABLED_BLOCKING_INITIAL_NAVIGATION = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "IS_ENABLED_BLOCKING_INITIAL_NAVIGATION" : "");
var eventListenerOptions = {
  passive: true,
  capture: true
};
var hoverTriggers = /* @__PURE__ */ new WeakMap();
var interactionTriggers = /* @__PURE__ */ new WeakMap();
var viewportTriggers = /* @__PURE__ */ new WeakMap();
var interactionEventNames = ["click", "keydown"];
var hoverEventNames = ["mouseenter", "mouseover", "focusin"];
var intersectionObservers = /* @__PURE__ */ new Map();
var DeferEventEntry = class {
  callbacks = /* @__PURE__ */ new Set();
  listener = () => {
    for (const callback of this.callbacks) {
      callback();
    }
  };
};
function onInteraction(trigger, callback) {
  let entry = interactionTriggers.get(trigger);
  if (!entry) {
    entry = new DeferEventEntry();
    interactionTriggers.set(trigger, entry);
    for (const name of interactionEventNames) {
      trigger.addEventListener(name, entry.listener, eventListenerOptions);
    }
  }
  entry.callbacks.add(callback);
  return () => {
    const {
      callbacks,
      listener
    } = entry;
    callbacks.delete(callback);
    if (callbacks.size === 0) {
      interactionTriggers.delete(trigger);
      for (const name of interactionEventNames) {
        trigger.removeEventListener(name, listener, eventListenerOptions);
      }
    }
  };
}
function onHover(trigger, callback) {
  let entry = hoverTriggers.get(trigger);
  if (!entry) {
    entry = new DeferEventEntry();
    hoverTriggers.set(trigger, entry);
    for (const name of hoverEventNames) {
      trigger.addEventListener(name, entry.listener, eventListenerOptions);
    }
  }
  entry.callbacks.add(callback);
  return () => {
    const {
      callbacks,
      listener
    } = entry;
    callbacks.delete(callback);
    if (callbacks.size === 0) {
      for (const name of hoverEventNames) {
        trigger.removeEventListener(name, listener, eventListenerOptions);
      }
      hoverTriggers.delete(trigger);
    }
  };
}
function createIntersectionObserver(options) {
  const key = getIntersectionObserverKey(options);
  return new IntersectionObserver((entries) => {
    for (const current of entries) {
      if (current.isIntersecting && viewportTriggers.has(current.target)) {
        viewportTriggers.get(current.target)?.get(key)?.listener();
      }
    }
  }, options);
}
function onViewport(trigger, callback, observerFactoryFn, options) {
  const key = getIntersectionObserverKey(options);
  let entry = viewportTriggers.get(trigger)?.get(key);
  if (!intersectionObservers.has(key)) {
    intersectionObservers.set(key, {
      observer: observerFactoryFn(options),
      count: 0
    });
  }
  const config2 = intersectionObservers.get(key);
  if (!entry) {
    entry = new DeferEventEntry();
    config2.observer.observe(trigger);
    let triggerConfig = viewportTriggers.get(trigger);
    if (triggerConfig) {
      triggerConfig.set(key, entry);
    } else {
      triggerConfig = /* @__PURE__ */ new Map();
      viewportTriggers.set(trigger, triggerConfig);
    }
    triggerConfig.set(key, entry);
    config2.count++;
  }
  entry.callbacks.add(callback);
  return () => {
    if (!viewportTriggers.get(trigger)?.has(key)) {
      return;
    }
    entry.callbacks.delete(callback);
    if (entry.callbacks.size === 0) {
      config2.observer.unobserve(trigger);
      config2.count--;
      const triggerConfig = viewportTriggers.get(trigger);
      if (triggerConfig) {
        triggerConfig.delete(key);
        if (triggerConfig.size === 0) {
          viewportTriggers.delete(trigger);
        }
      }
    }
    if (config2.count === 0) {
      config2.observer.disconnect();
      intersectionObservers.delete(key);
    }
  };
}
function getIntersectionObserverKey(options) {
  if (!options) {
    return "";
  }
  return `${options.rootMargin}/${typeof options.threshold === "number" ? options.threshold : options.threshold?.join("\n")}`;
}
var JSACTION_EVENT_CONTRACT = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "EVENT_CONTRACT_DETAILS" : "", {
  factory: () => ({})
});
var handledEventElements = /* @__PURE__ */ new WeakMap();
function markEventHandledForElement(event, element) {
  if (event == null || typeof event !== "object") return;
  let elements = handledEventElements.get(event);
  if (!elements) {
    elements = /* @__PURE__ */ new WeakSet();
    handledEventElements.set(event, elements);
  }
  elements.add(element);
}
var _stashEventListenerImpl = (lView, target, eventName, wrappedListener) => {
};
function stashEventListenerImpl(lView, target, eventName, wrappedListener) {
  _stashEventListenerImpl(lView, target, eventName, wrappedListener);
}
var DEHYDRATED_BLOCK_REGISTRY = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DEHYDRATED_BLOCK_REGISTRY" : "");
function isDetachedByI18n(tNode) {
  return (tNode.flags & 32) === 32;
}
var TRANSFER_STATE_TOKEN_ID = "__nghData__";
var NGH_DATA_KEY = makeStateKey(TRANSFER_STATE_TOKEN_ID);
var TRANSFER_STATE_DEFER_BLOCKS_INFO = "__nghDeferData__";
var NGH_DEFER_BLOCKS_KEY = makeStateKey(TRANSFER_STATE_DEFER_BLOCKS_INFO);
function isInternalHydrationTransferStateKey(key) {
  return key === TRANSFER_STATE_TOKEN_ID || key === TRANSFER_STATE_DEFER_BLOCKS_INFO;
}
var _retrieveHydrationInfoImpl = () => null;
function retrieveHydrationInfo(rNode, injector, isRootView2 = false) {
  return _retrieveHydrationInfoImpl(rNode, injector, isRootView2);
}
function getLNodeForHydration(viewRef) {
  let lView = viewRef._lView;
  const tView = lView[TVIEW];
  if (tView.type === 2) {
    return null;
  }
  if (isRootView(lView)) {
    lView = lView[HEADER_OFFSET];
  }
  return lView;
}
var HydrationStatus;
(function(HydrationStatus2) {
  HydrationStatus2["Hydrated"] = "hydrated";
  HydrationStatus2["Skipped"] = "skipped";
  HydrationStatus2["Mismatched"] = "mismatched";
})(HydrationStatus || (HydrationStatus = {}));
var HYDRATION_INFO_KEY = "__ngDebugHydrationInfo__";
function patchHydrationInfo(node, info) {
  node[HYDRATION_INFO_KEY] = info;
}
function markRNodeAsHavingHydrationMismatch(node, expectedNodeDetails = null, actualNodeDetails = null) {
  if (!ngDevMode) {
    throw new Error("Calling `markRNodeAsMismatchedByHydration` in prod mode is not supported and likely a mistake.");
  }
  while (node && !getComponent(node)) {
    node = node?.parentNode;
  }
  if (node) {
    patchHydrationInfo(node, {
      status: HydrationStatus.Mismatched,
      expectedNodeDetails,
      actualNodeDetails
    });
  }
}
function isIncrementalHydrationEnabled(injector) {
  return injector.get(IS_INCREMENTAL_HYDRATION_ENABLED, false, {
    optional: true
  });
}
var incrementalHydrationEnabledWarned = false;
function warnIncrementalHydrationNotConfigured() {
  if (!incrementalHydrationEnabledWarned) {
    incrementalHydrationEnabledWarned = true;
    console.warn(formatRuntimeError(508, "Angular has detected that some `@defer` blocks use `hydrate` triggers, but incremental hydration was not enabled. Please ensure that the `withIncrementalHydration()` call is added as an argument for the `provideClientHydration()` function call in your application config."));
  }
}
function assertSsrIdDefined(ssrUniqueId) {
  assertDefined(ssrUniqueId, "Internal error: expecting an SSR id for a defer block that should be hydrated, but the id is not present");
}
function getParentBlockHydrationQueue(deferBlockId, injector) {
  const dehydratedBlockRegistry = injector.get(DEHYDRATED_BLOCK_REGISTRY);
  const transferState = injector.get(TransferState);
  const deferBlockParents = transferState.get(NGH_DEFER_BLOCKS_KEY, {});
  let isTopMostDeferBlock = false;
  let currentBlockId = deferBlockId;
  let parentBlockPromise = null;
  const hydrationQueue = [];
  while (!isTopMostDeferBlock && currentBlockId) {
    ngDevMode && assertEqual(hydrationQueue.indexOf(currentBlockId), -1, "Internal error: defer block hierarchy has a cycle.");
    isTopMostDeferBlock = dehydratedBlockRegistry.has(currentBlockId);
    const hydratingParentBlock = dehydratedBlockRegistry.hydrating.get(currentBlockId);
    if (parentBlockPromise === null && hydratingParentBlock != null) {
      parentBlockPromise = hydratingParentBlock.promise;
      break;
    }
    hydrationQueue.unshift(currentBlockId);
    currentBlockId = deferBlockParents[currentBlockId][DEFER_PARENT_BLOCK_ID];
  }
  return {
    parentBlockPromise,
    hydrationQueue
  };
}
function refreshContentQueries(tView, lView) {
  const contentQueries = tView.contentQueries;
  if (contentQueries !== null) {
    const prevConsumer = setActiveConsumer(null);
    try {
      for (let i = 0; i < contentQueries.length; i += 2) {
        const queryStartIdx = contentQueries[i];
        const directiveDefIdx = contentQueries[i + 1];
        if (directiveDefIdx !== -1) {
          const directiveDef = tView.data[directiveDefIdx];
          ngDevMode && assertDefined(directiveDef, "DirectiveDef not found.");
          ngDevMode && assertDefined(directiveDef.contentQueries, "contentQueries function should be defined");
          setCurrentQueryIndex(queryStartIdx);
          directiveDef.contentQueries(2, lView[directiveDefIdx], directiveDefIdx);
        }
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
}
function executeViewQueryFn(flags, viewQueryFn, component) {
  ngDevMode && assertDefined(viewQueryFn, "View queries function to execute must be defined.");
  setCurrentQueryIndex(0);
  const prevConsumer = setActiveConsumer(null);
  try {
    viewQueryFn(flags, component);
  } finally {
    setActiveConsumer(prevConsumer);
  }
}
function executeContentQueries(tView, tNode, lView) {
  if (isContentQueryHost(tNode)) {
    const prevConsumer = setActiveConsumer(null);
    try {
      const start = tNode.directiveStart;
      const end = tNode.directiveEnd;
      for (let directiveIndex = start; directiveIndex < end; directiveIndex++) {
        const def = tView.data[directiveIndex];
        if (def.contentQueries) {
          const directiveInstance = lView[directiveIndex];
          ngDevMode && assertDefined(directiveIndex, "Incorrect reference to a directive defining a content query");
          def.contentQueries(1, directiveInstance, directiveIndex);
        }
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
}
var ViewEncapsulation;
(function(ViewEncapsulation3) {
  ViewEncapsulation3[ViewEncapsulation3["Emulated"] = 0] = "Emulated";
  ViewEncapsulation3[ViewEncapsulation3["None"] = 2] = "None";
  ViewEncapsulation3[ViewEncapsulation3["ShadowDom"] = 3] = "ShadowDom";
  ViewEncapsulation3[ViewEncapsulation3["ExperimentalIsolatedShadowDom"] = 4] = "ExperimentalIsolatedShadowDom";
})(ViewEncapsulation || (ViewEncapsulation = {}));
var CUSTOM_ELEMENTS_SCHEMA = {
  name: "custom-elements"
};
var NO_ERRORS_SCHEMA = {
  name: "no-errors-schema"
};
var shouldThrowErrorOnUnknownElement = false;
var shouldThrowErrorOnUnknownProperty = false;
function validateElementIsKnown(lView, tNode) {
  const tView = lView[TVIEW];
  if (tView.schemas === null) return;
  const tagName = tNode.value;
  if (!isDirectiveHost(tNode) && tagName !== null) {
    const isUnknown = typeof HTMLUnknownElement !== "undefined" && HTMLUnknownElement && getNativeByTNode(tNode, lView) instanceof HTMLUnknownElement || typeof customElements !== "undefined" && tagName.indexOf("-") > -1 && !customElements.get(tagName);
    if (isUnknown && !matchingSchemas(tView.schemas, tagName)) {
      const isHostStandalone = isHostComponentStandalone(lView);
      const templateLocation = getTemplateLocationDetails(lView);
      const schemas = `'${isHostStandalone ? "@Component" : "@NgModule"}.schemas'`;
      let message = `'${tagName}' is not a known element${templateLocation}:
`;
      message += `1. If '${tagName}' is an Angular component, then verify that it is ${isHostStandalone ? "included in the '@Component.imports' of this component" : "a part of an @NgModule where this component is declared"}.
`;
      if (tagName && tagName.indexOf("-") > -1) {
        message += `2. If '${tagName}' is a Web Component then add 'CUSTOM_ELEMENTS_SCHEMA' to the ${schemas} of this component to suppress this message.`;
      } else {
        message += `2. To allow any element add 'NO_ERRORS_SCHEMA' to the ${schemas} of this component.`;
      }
      if (shouldThrowErrorOnUnknownElement) {
        throw new RuntimeError(304, message);
      } else {
        console.error(formatRuntimeError(304, message));
      }
    }
  }
}
function isPropertyValid(element, propName, tagName, schemas) {
  if (schemas === null) return true;
  if (matchingSchemas(schemas, tagName) || propName in element || isAnimationProp(propName)) {
    return true;
  }
  return typeof Node === "undefined" || Node === null || !(element instanceof Node);
}
function handleUnknownPropertyError(propName, tagName, nodeType, lView) {
  if (!tagName && nodeType === 4) {
    tagName = "ng-template";
  }
  const isHostStandalone = isHostComponentStandalone(lView);
  const templateLocation = getTemplateLocationDetails(lView);
  let message = `Can't bind to '${propName}' since it isn't a known property of '${tagName}'${templateLocation}.`;
  const schemas = `'${isHostStandalone ? "@Component" : "@NgModule"}.schemas'`;
  const importLocation = isHostStandalone ? "included in the '@Component.imports' of this component" : "a part of an @NgModule where this component is declared";
  if (KNOWN_CONTROL_FLOW_DIRECTIVES.has(propName)) {
    const correspondingImport = KNOWN_CONTROL_FLOW_DIRECTIVES.get(propName);
    message += `
If the '${propName}' is an Angular control flow directive, please make sure that either the '${correspondingImport}' directive or the 'CommonModule' is ${importLocation}.`;
  } else {
    message += `
1. If '${tagName}' is an Angular component and it has the '${propName}' input, then verify that it is ${importLocation}.`;
    if (tagName && tagName.indexOf("-") > -1) {
      message += `
2. If '${tagName}' is a Web Component then add 'CUSTOM_ELEMENTS_SCHEMA' to the ${schemas} of this component to suppress this message.`;
      message += `
3. To allow any property add 'NO_ERRORS_SCHEMA' to the ${schemas} of this component.`;
    } else {
      message += `
2. To allow any property add 'NO_ERRORS_SCHEMA' to the ${schemas} of this component.`;
    }
  }
  reportUnknownPropertyError(message);
}
function reportUnknownPropertyError(message) {
  if (shouldThrowErrorOnUnknownProperty) {
    throw new RuntimeError(303, message);
  } else {
    console.error(formatRuntimeError(303, message));
  }
}
function getDeclarationComponentDef(lView) {
  !ngDevMode && throwError2("Must never be called in production mode");
  const declarationLView = lView[DECLARATION_COMPONENT_VIEW];
  const context2 = declarationLView[CONTEXT];
  if (!context2) return null;
  return context2.constructor ? getComponentDef(context2.constructor) : null;
}
function isHostComponentStandalone(lView) {
  !ngDevMode && throwError2("Must never be called in production mode");
  const componentDef = getDeclarationComponentDef(lView);
  return !!componentDef?.standalone;
}
function getTemplateLocationDetails(lView) {
  !ngDevMode && throwError2("Must never be called in production mode");
  const hostComponentDef = getDeclarationComponentDef(lView);
  const componentClassName = hostComponentDef?.type?.name;
  return componentClassName ? ` (used in the '${componentClassName}' component template)` : "";
}
var KNOWN_CONTROL_FLOW_DIRECTIVES = /* @__PURE__ */ new Map([["ngIf", "NgIf"], ["ngFor", "NgFor"], ["ngSwitchCase", "NgSwitchCase"], ["ngSwitchDefault", "NgSwitchDefault"]]);
function matchingSchemas(schemas, tagName) {
  if (schemas !== null) {
    for (let i = 0; i < schemas.length; i++) {
      const schema = schemas[i];
      if (schema === NO_ERRORS_SCHEMA || schema === CUSTOM_ELEMENTS_SCHEMA && tagName && tagName.indexOf("-") > -1) {
        return true;
      }
    }
  }
  return false;
}
var policy$1;
function getPolicy$1() {
  if (policy$1 === void 0) {
    policy$1 = null;
    if (_global.trustedTypes) {
      try {
        policy$1 = _global.trustedTypes.createPolicy("angular", {
          createHTML: (s) => s,
          createScript: (s) => s,
          createScriptURL: (s) => s
        });
      } catch {
      }
    }
  }
  return policy$1;
}
function trustedHTMLFromString(html) {
  return getPolicy$1()?.createHTML(html) || html;
}
function trustedScriptURLFromString(url) {
  return getPolicy$1()?.createScriptURL(url) || url;
}
var policy;
function getPolicy() {
  if (policy === void 0) {
    policy = null;
    if (_global.trustedTypes) {
      try {
        policy = _global.trustedTypes.createPolicy("angular#unsafe-bypass", {
          createHTML: (s) => s,
          createScript: (s) => s,
          createScriptURL: (s) => s
        });
      } catch {
      }
    }
  }
  return policy;
}
function trustedHTMLFromStringBypass(html) {
  return getPolicy()?.createHTML(html) || html;
}
function trustedScriptFromStringBypass(script) {
  return getPolicy()?.createScript(script) || script;
}
function trustedScriptURLFromStringBypass(url) {
  return getPolicy()?.createScriptURL(url) || url;
}
var SafeValueImpl = class {
  changingThisBreaksApplicationSecurity;
  constructor(changingThisBreaksApplicationSecurity) {
    this.changingThisBreaksApplicationSecurity = changingThisBreaksApplicationSecurity;
  }
  toString() {
    return `SafeValue must use [property]=binding: ${this.changingThisBreaksApplicationSecurity} (see ${XSS_SECURITY_URL})`;
  }
};
var SafeHtmlImpl = class extends SafeValueImpl {
  getTypeName() {
    return "HTML";
  }
};
var SafeStyleImpl = class extends SafeValueImpl {
  getTypeName() {
    return "Style";
  }
};
var SafeScriptImpl = class extends SafeValueImpl {
  getTypeName() {
    return "Script";
  }
};
var SafeUrlImpl = class extends SafeValueImpl {
  getTypeName() {
    return "URL";
  }
};
var SafeResourceUrlImpl = class extends SafeValueImpl {
  getTypeName() {
    return "ResourceURL";
  }
};
function unwrapSafeValue(value) {
  return value instanceof SafeValueImpl ? value.changingThisBreaksApplicationSecurity : value;
}
function allowSanitizationBypassAndThrow(value, type) {
  const actualType = getSanitizationBypassType(value);
  if (actualType != null && actualType !== type) {
    if (actualType === "ResourceURL" && type === "URL") return true;
    throw new Error(`Required a safe ${type}, got a ${actualType} (see ${XSS_SECURITY_URL})`);
  }
  return actualType === type;
}
function getSanitizationBypassType(value) {
  return value instanceof SafeValueImpl && value.getTypeName() || null;
}
function bypassSanitizationTrustHtml(trustedHtml) {
  return new SafeHtmlImpl(trustedHtml);
}
function bypassSanitizationTrustStyle(trustedStyle) {
  return new SafeStyleImpl(trustedStyle);
}
function bypassSanitizationTrustScript(trustedScript) {
  return new SafeScriptImpl(trustedScript);
}
function bypassSanitizationTrustUrl(trustedUrl) {
  return new SafeUrlImpl(trustedUrl);
}
function bypassSanitizationTrustResourceUrl(trustedResourceUrl) {
  return new SafeResourceUrlImpl(trustedResourceUrl);
}
function getInertBodyHelper(defaultDoc) {
  const inertDocumentHelper = new InertDocumentHelper(defaultDoc);
  return isDOMParserAvailable() ? new DOMParserHelper(inertDocumentHelper) : inertDocumentHelper;
}
var DOMParserHelper = class {
  inertDocumentHelper;
  constructor(inertDocumentHelper) {
    this.inertDocumentHelper = inertDocumentHelper;
  }
  getInertBodyElement(html) {
    html = "<body><remove></remove>" + html;
    try {
      const body = new window.DOMParser().parseFromString(trustedHTMLFromString(html), "text/html").body;
      if (body === null) {
        return this.inertDocumentHelper.getInertBodyElement(html);
      }
      body.firstChild?.remove();
      return body;
    } catch {
      return null;
    }
  }
};
var InertDocumentHelper = class {
  defaultDoc;
  inertDocument;
  constructor(defaultDoc) {
    this.defaultDoc = defaultDoc;
    this.inertDocument = this.defaultDoc.implementation.createHTMLDocument("sanitization-inert");
  }
  getInertBodyElement(html) {
    const templateEl = this.inertDocument.createElement("template");
    templateEl.innerHTML = trustedHTMLFromString(html);
    return templateEl;
  }
};
function isDOMParserAvailable() {
  try {
    return !!new window.DOMParser().parseFromString(trustedHTMLFromString(""), "text/html");
  } catch {
    return false;
  }
}
var SAFE_URL_PATTERN = /^(?!javascript:)(?:[a-z0-9+.-]+:|[^&:\/?#]*(?:[\/?#]|$))/i;
function _sanitizeUrl(url) {
  url = String(url);
  if (url.match(SAFE_URL_PATTERN)) return url;
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    console.warn(`WARNING: sanitizing unsafe URL value ${url} (see ${XSS_SECURITY_URL})`);
  }
  return "unsafe:" + url;
}
function tagSet(tags) {
  const res = {};
  for (const t of tags.split(",")) res[t] = true;
  return res;
}
function merge(...sets) {
  const res = {};
  for (const s of sets) {
    for (const v in s) {
      if (s.hasOwnProperty(v)) res[v] = true;
    }
  }
  return res;
}
var VOID_ELEMENTS = tagSet("area,br,col,hr,img,wbr");
var OPTIONAL_END_TAG_BLOCK_ELEMENTS = tagSet("colgroup,dd,dt,li,p,tbody,td,tfoot,th,thead,tr");
var OPTIONAL_END_TAG_INLINE_ELEMENTS = tagSet("rp,rt");
var OPTIONAL_END_TAG_ELEMENTS = merge(OPTIONAL_END_TAG_INLINE_ELEMENTS, OPTIONAL_END_TAG_BLOCK_ELEMENTS);
var BLOCK_ELEMENTS = merge(OPTIONAL_END_TAG_BLOCK_ELEMENTS, tagSet("address,article,aside,blockquote,caption,center,del,details,dialog,dir,div,dl,figure,figcaption,footer,h1,h2,h3,h4,h5,h6,header,hgroup,hr,ins,main,map,menu,nav,ol,pre,section,summary,table,ul"));
var INLINE_ELEMENTS = merge(OPTIONAL_END_TAG_INLINE_ELEMENTS, tagSet("a,abbr,acronym,audio,b,bdi,bdo,big,br,cite,code,del,dfn,em,font,i,img,ins,kbd,label,map,mark,picture,q,ruby,rp,rt,s,samp,small,source,span,strike,strong,sub,sup,time,track,tt,u,var,video"));
var VALID_ELEMENTS = merge(VOID_ELEMENTS, BLOCK_ELEMENTS, INLINE_ELEMENTS, OPTIONAL_END_TAG_ELEMENTS);
var URI_ATTRS = tagSet("background,cite,href,itemtype,longdesc,poster,src,xlink:href");
var HTML_ATTRS = tagSet("abbr,accesskey,align,alt,autoplay,axis,bgcolor,border,cellpadding,cellspacing,class,clear,color,cols,colspan,compact,controls,coords,datetime,default,dir,download,face,headers,height,hidden,hreflang,hspace,ismap,itemscope,itemprop,kind,label,lang,language,loop,media,muted,nohref,nowrap,open,preload,rel,rev,role,rows,rowspan,rules,scope,scrolling,shape,size,sizes,span,srclang,srcset,start,summary,tabindex,target,title,translate,type,usemap,valign,value,vspace,width");
var ARIA_ATTRS = tagSet("aria-activedescendant,aria-atomic,aria-autocomplete,aria-busy,aria-checked,aria-colcount,aria-colindex,aria-colspan,aria-controls,aria-current,aria-describedby,aria-details,aria-disabled,aria-dropeffect,aria-errormessage,aria-expanded,aria-flowto,aria-grabbed,aria-haspopup,aria-hidden,aria-invalid,aria-keyshortcuts,aria-label,aria-labelledby,aria-level,aria-live,aria-modal,aria-multiline,aria-multiselectable,aria-orientation,aria-owns,aria-placeholder,aria-posinset,aria-pressed,aria-readonly,aria-relevant,aria-required,aria-roledescription,aria-rowcount,aria-rowindex,aria-rowspan,aria-selected,aria-setsize,aria-sort,aria-valuemax,aria-valuemin,aria-valuenow,aria-valuetext");
var VALID_ATTRS = merge(URI_ATTRS, HTML_ATTRS, ARIA_ATTRS);
var SKIP_TRAVERSING_CONTENT_IF_INVALID_ELEMENTS = tagSet("script,style,template");
var SanitizingHtmlSerializer = class {
  sanitizedSomething = false;
  buf = [];
  sanitizeChildren(el) {
    let current = el.firstChild;
    let traverseContent = true;
    let parentNodes = [];
    while (current) {
      if (current.nodeType === Node.ELEMENT_NODE) {
        traverseContent = this.startElement(current);
      } else if (current.nodeType === Node.TEXT_NODE) {
        this.chars(current.nodeValue);
      } else {
        this.sanitizedSomething = true;
      }
      if (traverseContent && current.firstChild) {
        parentNodes.push(current);
        current = getFirstChild(current);
        continue;
      }
      while (current) {
        if (current.nodeType === Node.ELEMENT_NODE) {
          this.endElement(current);
        }
        let next = getNextSibling(current);
        if (next) {
          current = next;
          break;
        }
        current = parentNodes.pop();
      }
    }
    return this.buf.join("");
  }
  startElement(element) {
    const tagName = getNodeName(element).toLowerCase();
    if (!VALID_ELEMENTS.hasOwnProperty(tagName)) {
      this.sanitizedSomething = true;
      return !SKIP_TRAVERSING_CONTENT_IF_INVALID_ELEMENTS.hasOwnProperty(tagName);
    }
    this.buf.push("<");
    this.buf.push(tagName);
    const elAttrs = element.attributes;
    for (let i = 0; i < elAttrs.length; i++) {
      const elAttr = elAttrs.item(i);
      const attrName = elAttr.name;
      const lower = attrName.toLowerCase();
      if (!VALID_ATTRS.hasOwnProperty(lower)) {
        this.sanitizedSomething = true;
        continue;
      }
      let value = elAttr.value;
      if (URI_ATTRS[lower]) value = _sanitizeUrl(value);
      this.buf.push(" ", attrName, '="', encodeEntities(value), '"');
    }
    this.buf.push(">");
    return true;
  }
  endElement(current) {
    const tagName = getNodeName(current).toLowerCase();
    if (VALID_ELEMENTS.hasOwnProperty(tagName) && !VOID_ELEMENTS.hasOwnProperty(tagName)) {
      this.buf.push("</");
      this.buf.push(tagName);
      this.buf.push(">");
    }
  }
  chars(chars) {
    this.buf.push(encodeEntities(chars));
  }
};
function isClobberedElement(parentNode, childNode) {
  return (parentNode.compareDocumentPosition(childNode) & Node.DOCUMENT_POSITION_CONTAINED_BY) !== Node.DOCUMENT_POSITION_CONTAINED_BY;
}
function getNextSibling(node) {
  const nextSibling = node.nextSibling;
  if (nextSibling && node !== nextSibling.previousSibling) {
    throw clobberedElementError(nextSibling);
  }
  return nextSibling;
}
function getFirstChild(node) {
  const firstChild = node.firstChild;
  if (firstChild && isClobberedElement(node, firstChild)) {
    throw clobberedElementError(firstChild);
  }
  return firstChild;
}
function getNodeName(node) {
  const nodeName = node.nodeName;
  return typeof nodeName === "string" ? nodeName : "FORM";
}
function clobberedElementError(node) {
  return new Error(`Failed to sanitize html because the element is clobbered: ${node.outerHTML}`);
}
var SURROGATE_PAIR_REGEXP = /[\uD800-\uDBFF][\uDC00-\uDFFF]/g;
var NON_ALPHANUMERIC_REGEXP = /([^\#-~ |!])/g;
function encodeEntities(value) {
  return value.replace(/&/g, "&amp;").replace(SURROGATE_PAIR_REGEXP, function(match) {
    const hi = match.charCodeAt(0);
    const low = match.charCodeAt(1);
    return "&#" + ((hi - 55296) * 1024 + (low - 56320) + 65536) + ";";
  }).replace(NON_ALPHANUMERIC_REGEXP, function(match) {
    return "&#" + match.charCodeAt(0) + ";";
  }).replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
var inertBodyHelper;
function _sanitizeHtml(defaultDoc, unsafeHtmlInput) {
  let inertBodyElement = null;
  try {
    inertBodyHelper = inertBodyHelper || getInertBodyHelper(defaultDoc);
    let unsafeHtml = unsafeHtmlInput ? String(unsafeHtmlInput) : "";
    inertBodyElement = inertBodyHelper.getInertBodyElement(unsafeHtml);
    let mXSSAttempts = 5;
    let parsedHtml = unsafeHtml;
    do {
      if (mXSSAttempts === 0) {
        throw new Error("Failed to sanitize html because the input is unstable");
      }
      mXSSAttempts--;
      unsafeHtml = parsedHtml;
      parsedHtml = inertBodyElement.innerHTML;
      inertBodyElement = inertBodyHelper.getInertBodyElement(unsafeHtml);
    } while (unsafeHtml !== parsedHtml);
    const sanitizer = new SanitizingHtmlSerializer();
    const safeHtml = sanitizer.sanitizeChildren(getTemplateContent(inertBodyElement) || inertBodyElement);
    if ((typeof ngDevMode === "undefined" || ngDevMode) && sanitizer.sanitizedSomething) {
      console.warn(`WARNING: sanitizing HTML stripped some content, see ${XSS_SECURITY_URL}`);
    }
    return trustedHTMLFromString(safeHtml);
  } finally {
    if (inertBodyElement) {
      const parent = getTemplateContent(inertBodyElement) || inertBodyElement;
      while (parent.firstChild) {
        parent.firstChild.remove();
      }
    }
  }
}
function getTemplateContent(el) {
  return "content" in el && isTemplateElement(el) ? el.content : null;
}
function isTemplateElement(el) {
  return el.nodeType === Node.ELEMENT_NODE && el.nodeName === "TEMPLATE";
}
var COMMENT_DISALLOWED = /^>|^->|<!--|-->|--!>|<!-$/g;
var COMMENT_DELIMITER = /(<|>)/g;
var COMMENT_DELIMITER_ESCAPED = "\u200B$1\u200B";
function escapeCommentText(value) {
  return value.replace(COMMENT_DISALLOWED, (text) => text.replace(COMMENT_DELIMITER, COMMENT_DELIMITER_ESCAPED));
}
function createTextNode(renderer, value) {
  return renderer.createText(value);
}
function updateTextNode(renderer, rNode, value) {
  renderer.setValue(rNode, value);
}
function createCommentNode(renderer, value) {
  return renderer.createComment(escapeCommentText(value));
}
function createElementNode(renderer, name, namespace) {
  return renderer.createElement(name, namespace);
}
function nativeInsertBefore(renderer, parent, child, beforeNode, isMove) {
  renderer.insertBefore(parent, child, beforeNode, isMove);
}
function nativeAppendChild(renderer, parent, child) {
  ngDevMode && assertDefined(parent, "parent node must be defined");
  renderer.appendChild(parent, child);
}
function nativeAppendOrInsertBefore(renderer, parent, child, beforeNode, isMove) {
  if (beforeNode !== null) {
    nativeInsertBefore(renderer, parent, child, beforeNode, isMove);
  } else {
    nativeAppendChild(renderer, parent, child);
  }
}
function nativeRemoveNode(renderer, rNode, isHostElement, requireSynchronousElementRemoval) {
  renderer.removeChild(null, rNode, isHostElement, requireSynchronousElementRemoval);
}
function writeDirectStyle(renderer, element, newValue) {
  ngDevMode && assertString(newValue, "'newValue' should be a string");
  renderer.setAttribute(element, "style", newValue);
}
function writeDirectClass(renderer, element, newValue) {
  ngDevMode && assertString(newValue, "'newValue' should be a string");
  if (newValue === "") {
    renderer.removeAttribute(element, "class");
  } else {
    renderer.setAttribute(element, "class", newValue);
  }
}
function setupStaticAttributes(renderer, element, tNode) {
  const {
    mergedAttrs,
    classes,
    styles
  } = tNode;
  if (mergedAttrs !== null) {
    setUpAttributes(renderer, element, mergedAttrs);
  }
  if (classes !== null) {
    writeDirectClass(renderer, element, classes);
  }
  if (styles !== null) {
    writeDirectStyle(renderer, element, styles);
  }
}
function enforceIframeSecurity(iframe) {
  const lView = getLView();
  iframe.src = "";
  iframe.srcdoc = trustedHTMLFromString("");
  nativeRemoveNode(lView[RENDERER], iframe);
}
var SecurityContext;
(function(SecurityContext2) {
  SecurityContext2[SecurityContext2["NONE"] = 0] = "NONE";
  SecurityContext2[SecurityContext2["HTML"] = 1] = "HTML";
  SecurityContext2[SecurityContext2["STYLE"] = 2] = "STYLE";
  SecurityContext2[SecurityContext2["SCRIPT"] = 3] = "SCRIPT";
  SecurityContext2[SecurityContext2["URL"] = 4] = "URL";
  SecurityContext2[SecurityContext2["RESOURCE_URL"] = 5] = "RESOURCE_URL";
  SecurityContext2[SecurityContext2["ATTRIBUTE_NO_BINDING"] = 6] = "ATTRIBUTE_NO_BINDING";
})(SecurityContext || (SecurityContext = {}));
var _SECURITY_SCHEMA;
var SVG_NAMESPACE2 = "svg";
var MATH_ML_NAMESPACE2 = "math";
function SECURITY_SCHEMA() {
  if (!_SECURITY_SCHEMA) {
    _SECURITY_SCHEMA = {};
    registerContext(SecurityContext.HTML, void 0, [["iframe", ["srcdoc"]], ["*", ["innerHTML", "outerHTML"]]]);
    registerContext(SecurityContext.STYLE, void 0, [["*", ["style"]]]);
    registerContext(SecurityContext.URL, void 0, [["*", ["formAction"]], ["area", ["href"]], ["a", ["href", "xlink:href"]], ["form", ["action"]], ["img", ["src"]], ["video", ["src"]]]);
    registerContext(SecurityContext.URL, MATH_ML_NAMESPACE2, [["annotation", ["href", "xlink:href"]], ["annotation-xml", ["href", "xlink:href"]], ["maction", ["href", "xlink:href"]], ["malignmark", ["href", "xlink:href"]], ["math", ["href", "xlink:href"]], ["mroot", ["href", "xlink:href"]], ["msqrt", ["href", "xlink:href"]], ["merror", ["href", "xlink:href"]], ["mfrac", ["href", "xlink:href"]], ["mglyph", ["href", "xlink:href"]], ["msub", ["href", "xlink:href"]], ["msup", ["href", "xlink:href"]], ["msubsup", ["href", "xlink:href"]], ["mmultiscripts", ["href", "xlink:href"]], ["mprescripts", ["href", "xlink:href"]], ["mi", ["href", "xlink:href"]], ["mn", ["href", "xlink:href"]], ["mo", ["href", "xlink:href"]], ["mpadded", ["href", "xlink:href"]], ["mphantom", ["href", "xlink:href"]], ["mrow", ["href", "xlink:href"]], ["ms", ["href", "xlink:href"]], ["mspace", ["href", "xlink:href"]], ["mstyle", ["href", "xlink:href"]], ["mtable", ["href", "xlink:href"]], ["mtd", ["href", "xlink:href"]], ["mtr", ["href", "xlink:href"]], ["mtext", ["href", "xlink:href"]], ["mover", ["href", "xlink:href"]], ["munder", ["href", "xlink:href"]], ["munderover", ["href", "xlink:href"]], ["semantics", ["href", "xlink:href"]], ["none", ["href", "xlink:href"]]]);
    registerContext(SecurityContext.RESOURCE_URL, void 0, [["base", ["href"]], ["embed", ["src"]], ["frame", ["src"]], ["iframe", ["src"]], ["link", ["href"]], ["object", ["codebase", "data"]]]);
    registerContext(SecurityContext.URL, SVG_NAMESPACE2, [["a", ["href", "xlink:href"]]]);
    registerContext(SecurityContext.ATTRIBUTE_NO_BINDING, SVG_NAMESPACE2, [["animate", ["attributeName", "values", "to", "from"]], ["set", ["to", "attributeName"]], ["animateMotion", ["attributeName"]], ["animateTransform", ["attributeName"]]]);
    registerContext(SecurityContext.ATTRIBUTE_NO_BINDING, void 0, [["unknown", ["attributeName", "values", "to", "from", "sandbox", "allow", "allowFullscreen", "referrerPolicy", "csp", "fetchPriority"]], ["iframe", ["sandbox", "allow", "allowFullscreen", "referrerPolicy", "csp", "fetchPriority"]]]);
  }
  return _SECURITY_SCHEMA;
}
function registerContext(ctx, namespace, specs) {
  for (const [element, attributeNames] of specs) {
    let tagName = namespace && element !== "*" && element !== "unknown" ? `:${namespace}:${element}` : element;
    tagName = tagName.toLowerCase();
    for (const attr of attributeNames) {
      _SECURITY_SCHEMA[`${tagName}|${attr.toLowerCase()}`] = ctx;
    }
  }
}
function \u0275\u0275sanitizeHtml(unsafeHtml) {
  const sanitizer = getSanitizer();
  if (sanitizer) {
    return trustedHTMLFromStringBypass(sanitizer.sanitize(SecurityContext.HTML, unsafeHtml) || "");
  }
  if (allowSanitizationBypassAndThrow(unsafeHtml, "HTML")) {
    return trustedHTMLFromStringBypass(unwrapSafeValue(unsafeHtml));
  }
  return _sanitizeHtml(getDocument(), renderStringify(unsafeHtml));
}
function \u0275\u0275sanitizeStyle(unsafeStyle) {
  const sanitizer = getSanitizer();
  if (sanitizer) {
    return sanitizer.sanitize(SecurityContext.STYLE, unsafeStyle) || "";
  }
  if (allowSanitizationBypassAndThrow(unsafeStyle, "Style")) {
    return unwrapSafeValue(unsafeStyle);
  }
  return renderStringify(unsafeStyle);
}
function \u0275\u0275sanitizeUrl(unsafeUrl) {
  const sanitizer = getSanitizer();
  if (sanitizer) {
    return sanitizer.sanitize(SecurityContext.URL, unsafeUrl) || "";
  }
  if (allowSanitizationBypassAndThrow(unsafeUrl, "URL")) {
    return unwrapSafeValue(unsafeUrl);
  }
  return _sanitizeUrl(renderStringify(unsafeUrl));
}
function \u0275\u0275sanitizeResourceUrl(unsafeResourceUrl) {
  const sanitizer = getSanitizer();
  if (sanitizer) {
    return trustedScriptURLFromStringBypass(sanitizer.sanitize(SecurityContext.RESOURCE_URL, unsafeResourceUrl) || "");
  }
  if (allowSanitizationBypassAndThrow(unsafeResourceUrl, "ResourceURL")) {
    return trustedScriptURLFromStringBypass(unwrapSafeValue(unsafeResourceUrl));
  }
  throw new RuntimeError(904, ngDevMode && `unsafe value used in a resource URL context (see ${XSS_SECURITY_URL})`);
}
function \u0275\u0275sanitizeScript(unsafeScript) {
  const sanitizer = getSanitizer();
  if (sanitizer) {
    return trustedScriptFromStringBypass(sanitizer.sanitize(SecurityContext.SCRIPT, unsafeScript) || "");
  }
  if (allowSanitizationBypassAndThrow(unsafeScript, "Script")) {
    return trustedScriptFromStringBypass(unwrapSafeValue(unsafeScript));
  }
  throw new RuntimeError(905, ngDevMode && "unsafe value used in a script context");
}
function \u0275\u0275trustConstantHtml(html) {
  if (ngDevMode && (!Array.isArray(html) || !Array.isArray(html.raw) || html.length !== 1)) {
    throw new Error(`Unexpected interpolation in trusted HTML constant: ${html.join("?")}`);
  }
  return trustedHTMLFromString(html[0]);
}
function \u0275\u0275trustConstantResourceUrl(url) {
  if (ngDevMode && (!Array.isArray(url) || !Array.isArray(url.raw) || url.length !== 1)) {
    throw new Error(`Unexpected interpolation in trusted URL constant: ${url.join("?")}`);
  }
  return trustedScriptURLFromString(url[0]);
}
var RESOURCE_MAP = {
  "embed": {
    "src": true
  },
  "frame": {
    "src": true
  },
  "iframe": {
    "src": true
  },
  "media": {
    "src": true
  },
  "base": {
    "href": true
  },
  "link": {
    "href": true
  },
  "object": {
    "data": true,
    "codebase": true
  }
};
function getUrlSanitizer(tag, prop) {
  const isResource = RESOURCE_MAP[tag.toLowerCase()]?.[prop.toLowerCase()] === true;
  return isResource ? \u0275\u0275sanitizeResourceUrl : \u0275\u0275sanitizeUrl;
}
function \u0275\u0275sanitizeUrlOrResourceUrl(unsafeUrl, tag, prop) {
  return getUrlSanitizer(tag, prop)(unsafeUrl);
}
function validateAgainstEventProperties(name) {
  if (name.toLowerCase().startsWith("on")) {
    const errorMessage = `Binding to event property '${name}' is disallowed for security reasons, please use (${name.slice(2)})=...
If '${name}' is a directive input, make sure the directive is imported by the current module.`;
    throw new RuntimeError(306, errorMessage);
  }
}
function getSanitizer() {
  const lView = getLView();
  return lView && lView[ENVIRONMENT].sanitizer;
}
var SECURITY_SENSITIVE_ATTRIBUTE_NAMES = /* @__PURE__ */ new Set(["href", "xlink:href"]);
var SVG_ANIMATION_ATTRIBUTE_NAME_CANDIDATES = ["attributeName", "attributename"];
var SECURITY_SENSITIVE_ELEMENTS = {
  "iframe": {
    "sandbox": true,
    "allow": true,
    "allowfullscreen": true,
    "referrerpolicy": true,
    "csp": true,
    "fetchpriority": true
  },
  ":svg:animate": {
    "attributename": true,
    "to": SECURITY_SENSITIVE_ATTRIBUTE_NAMES,
    "values": SECURITY_SENSITIVE_ATTRIBUTE_NAMES,
    "from": SECURITY_SENSITIVE_ATTRIBUTE_NAMES
  },
  ":svg:set": {
    "attributename": true,
    "to": SECURITY_SENSITIVE_ATTRIBUTE_NAMES
  },
  ":svg:animatemotion": {
    "attributename": true
  },
  ":svg:animatetransform": {
    "attributename": true
  }
};
function \u0275\u0275validateAttribute(value, tagName, attributeName) {
  const lowerCaseTagName = tagName.toLowerCase();
  const lowerCaseAttrName = attributeName.toLowerCase();
  const index = getSelectedIndex();
  const tNode = index === -1 ? null : getSelectedTNode();
  if (tNode && tNode.type !== 2) {
    return value;
  }
  const fullTagName = lowerCaseTagName[0] !== ":" && tNode?.namespace ? `:${tNode.namespace}:${lowerCaseTagName}` : lowerCaseTagName;
  const validationConfig = SECURITY_SENSITIVE_ELEMENTS[fullTagName]?.[lowerCaseAttrName];
  if (!validationConfig) {
    return value;
  }
  const lView = getLView();
  if (tNode && lowerCaseTagName === "iframe") {
    const element = getNativeByTNode(tNode, lView);
    enforceIframeSecurity(element);
  }
  const displayTagName = tagName[0] === ":" ? tagName.split(":").pop() : tagName;
  if (typeof validationConfig !== "boolean") {
    if (!tNode) {
      const errorMessage2 = ngDevMode && `Angular has detected that the \`${attributeName}\` was applied as a binding to the <${tagName}> element. For security reasons, the \`${attributeName}\` can be set on the <${tagName}> element as a static attribute only. 
To fix this, switch the \`${attributeName}\` binding to a static attribute in a template or in host bindings section.`;
      throw new RuntimeError(-910, errorMessage2);
    }
    const element = getNativeByTNode(tNode, lView);
    const attributeNameValue = getSecuritySensitiveSVGAnimationAttributeName(element, validationConfig);
    if (attributeNameValue) {
      const errorMessage2 = ngDevMode && `Angular has detected that the \`${attributeName}\` was applied as a binding to the <${displayTagName}> element${getTemplateLocationDetails(lView)}. For security reasons, the \`${attributeName}\` can be set on the <${displayTagName}> element as a static attribute only when the "attributeName" is set to '${attributeNameValue}'. 
To fix this, switch the \`${attributeNameValue}\` binding to a static attribute in a template or in host bindings section.`;
      throw new RuntimeError(-910, errorMessage2);
    }
    return value;
  }
  const errorMessage = ngDevMode && `Angular has detected that the \`${attributeName}\` was applied as a binding to the <${displayTagName}> element${tNode ? getTemplateLocationDetails(lView) : ""}. For security reasons, the \`${attributeName}\` can be set on the <${displayTagName}> element as a static attribute only. 
To fix this, switch the \`${attributeName}\` binding to a static attribute in a template or in host bindings section.`;
  throw new RuntimeError(-910, errorMessage);
}
function getSecuritySensitiveSVGAnimationAttributeName(element, validationConfig) {
  for (const attributeName of SVG_ANIMATION_ATTRIBUTE_NAME_CANDIDATES) {
    const attributeNameValue = element.getAttribute(attributeName);
    if (attributeNameValue !== null && validationConfig.has(attributeNameValue.toLowerCase())) {
      return attributeNameValue;
    }
  }
  return null;
}
var NG_REFLECT_ATTRS_FLAG_DEFAULT = false;
var NG_REFLECT_ATTRS_FLAG = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "NG_REFLECT_FLAG" : "", {
  factory: () => NG_REFLECT_ATTRS_FLAG_DEFAULT
});
function normalizeDebugBindingName(name) {
  name = camelCaseToDashCase(name.replace(/[$@]/g, "_"));
  return `ng-reflect-${name}`;
}
var CAMEL_CASE_REGEXP = /([A-Z])/g;
function camelCaseToDashCase(input2) {
  return input2.replace(CAMEL_CASE_REGEXP, (...m) => "-" + m[1].toLowerCase());
}
function normalizeDebugBindingValue(value) {
  try {
    return value != null ? value.toString().slice(0, 30) : value;
  } catch (e) {
    return "[ERROR] Exception while trying to serialize the value";
  }
}
function \u0275\u0275resolveWindow(element) {
  return element.ownerDocument.defaultView;
}
function \u0275\u0275resolveDocument(element) {
  return element.ownerDocument;
}
function \u0275\u0275resolveBody(element) {
  return element.ownerDocument.body;
}
var INTERPOLATION_DELIMITER = `\uFFFD`;
function maybeUnwrapFn(value) {
  if (value instanceof Function) {
    return value();
  } else {
    return value;
  }
}
var VALUE_STRING_LENGTH_LIMIT = 200;
function assertStandaloneComponentType(type) {
  assertComponentDef(type);
  const componentDef = getComponentDef(type);
  if (!componentDef.standalone) {
    throw new RuntimeError(907, `The ${stringifyForError(type)} component is not marked as standalone, but Angular expects to have a standalone component here. Please make sure the ${stringifyForError(type)} component does not have the \`standalone: false\` flag in the decorator.`);
  }
}
function assertComponentDef(type) {
  if (!getComponentDef(type)) {
    throw new RuntimeError(906, `The ${stringifyForError(type)} is not an Angular component, make sure it has the \`@Component\` decorator.`);
  }
}
function throwMultipleComponentError(tNode, first2, second) {
  throw new RuntimeError(-300, `Multiple components match node with tagname ${tNode.value}: ${stringifyForError(first2)} and ${stringifyForError(second)}`);
}
function throwErrorIfNoChangesMode(creationMode, oldValue, currValue, propName, lView) {
  const hostComponentDef = getDeclarationComponentDef(lView);
  const componentClassName = hostComponentDef?.type?.name;
  const field = propName ? ` for '${propName}'` : "";
  let msg = `ExpressionChangedAfterItHasBeenCheckedError: Expression has changed after it was checked. Previous value${field}: '${formatValue(oldValue)}'. Current value: '${formatValue(currValue)}'.${componentClassName ? ` Expression location: ${componentClassName} component` : ""}`;
  if (creationMode) {
    msg += ` It seems like the view has been created after its parent and its children have been dirty checked. Has it been created in a change detection hook?`;
  }
  throw new RuntimeError(-100, msg);
}
function formatValue(value) {
  let strValue = String(value);
  try {
    if (Array.isArray(value) || strValue === "[object Object]") {
      strValue = JSON.stringify(value);
    }
  } catch (error) {
  }
  return strValue.length > VALUE_STRING_LENGTH_LIMIT ? strValue.substring(0, VALUE_STRING_LENGTH_LIMIT) + "\u2026" : strValue;
}
function constructDetailsForInterpolation(lView, rootIndex, expressionIndex, meta, changedValue) {
  const [propName, prefix, ...chunks] = meta.split(INTERPOLATION_DELIMITER);
  let oldValue = prefix, newValue = prefix;
  for (let i = 0; i < chunks.length; i++) {
    const slotIdx = rootIndex + i;
    oldValue += `${lView[slotIdx]}${chunks[i]}`;
    newValue += `${slotIdx === expressionIndex ? changedValue : lView[slotIdx]}${chunks[i]}`;
  }
  return {
    propName,
    oldValue,
    newValue
  };
}
function getExpressionChangedErrorDetails(lView, bindingIndex, oldValue, newValue) {
  const tData = lView[TVIEW].data;
  const metadata = tData[bindingIndex];
  if (typeof metadata === "string") {
    if (metadata.indexOf(INTERPOLATION_DELIMITER) > -1) {
      return constructDetailsForInterpolation(lView, bindingIndex, bindingIndex, metadata, newValue);
    }
    return {
      propName: metadata,
      oldValue,
      newValue
    };
  }
  if (metadata === null) {
    let idx = bindingIndex - 1;
    while (typeof tData[idx] !== "string" && tData[idx + 1] === null) {
      idx--;
    }
    const meta = tData[idx];
    if (typeof meta === "string") {
      const matches = meta.match(new RegExp(INTERPOLATION_DELIMITER, "g"));
      if (matches && matches.length - 1 > bindingIndex - idx) {
        return constructDetailsForInterpolation(lView, idx, bindingIndex, meta, newValue);
      }
    }
  }
  return {
    propName: void 0,
    oldValue,
    newValue
  };
}
function classIndexOf(className, classToSearch, startingIndex) {
  ngDevMode && assertNotEqual(classToSearch, "", 'can not look for "" string.');
  let end = className.length;
  while (true) {
    const foundIndex = className.indexOf(classToSearch, startingIndex);
    if (foundIndex === -1) return foundIndex;
    if (foundIndex === 0 || className.charCodeAt(foundIndex - 1) <= 32) {
      const length = classToSearch.length;
      if (foundIndex + length === end || className.charCodeAt(foundIndex + length) <= 32) {
        return foundIndex;
      }
    }
    startingIndex = foundIndex + 1;
  }
}
var NG_TEMPLATE_SELECTOR = "ng-template";
function isCssClassMatching(tNode, attrs, cssClassToMatch, isProjectionMode) {
  ngDevMode && assertEqual(cssClassToMatch, cssClassToMatch.toLowerCase(), "Class name expected to be lowercase.");
  let i = 0;
  if (isProjectionMode) {
    for (; i < attrs.length && typeof attrs[i] === "string"; i += 2) {
      if (attrs[i] === "class" && classIndexOf(attrs[i + 1].toLowerCase(), cssClassToMatch, 0) !== -1) {
        return true;
      }
    }
  } else if (isInlineTemplate(tNode)) {
    return false;
  }
  i = attrs.indexOf(1, i);
  if (i > -1) {
    let item;
    while (++i < attrs.length && typeof (item = attrs[i]) === "string") {
      if (item.toLowerCase() === cssClassToMatch) {
        return true;
      }
    }
  }
  return false;
}
function isInlineTemplate(tNode) {
  return tNode.type === 4 && tNode.value !== NG_TEMPLATE_SELECTOR;
}
function hasTagAndTypeMatch(tNode, currentSelector, isProjectionMode) {
  const tagNameToCompare = tNode.type === 4 && !isProjectionMode ? NG_TEMPLATE_SELECTOR : tNode.value;
  return currentSelector === tagNameToCompare;
}
function isNodeMatchingSelector(tNode, selector, isProjectionMode) {
  ngDevMode && assertDefined(selector[0], "Selector should have a tag name");
  let mode = 4;
  const nodeAttrs = tNode.attrs;
  const nameOnlyMarkerIdx = nodeAttrs !== null ? getNameOnlyMarkerIndex(nodeAttrs) : 0;
  let skipToNextSelector = false;
  for (let i = 0; i < selector.length; i++) {
    const current = selector[i];
    if (typeof current === "number") {
      if (!skipToNextSelector && !isPositive(mode) && !isPositive(current)) {
        return false;
      }
      if (skipToNextSelector && isPositive(current)) continue;
      skipToNextSelector = false;
      mode = current | mode & 1;
      continue;
    }
    if (skipToNextSelector) continue;
    if (mode & 4) {
      mode = 2 | mode & 1;
      if (current !== "" && !hasTagAndTypeMatch(tNode, current, isProjectionMode) || current === "" && selector.length === 1) {
        if (isPositive(mode)) return false;
        skipToNextSelector = true;
      }
    } else if (mode & 8) {
      if (nodeAttrs === null || !isCssClassMatching(tNode, nodeAttrs, current, isProjectionMode)) {
        if (isPositive(mode)) return false;
        skipToNextSelector = true;
      }
    } else {
      const selectorAttrValue = selector[++i];
      const attrIndexInNode = findAttrIndexInNode(current, nodeAttrs, isInlineTemplate(tNode), isProjectionMode);
      if (attrIndexInNode === -1) {
        if (isPositive(mode)) return false;
        skipToNextSelector = true;
        continue;
      }
      if (selectorAttrValue !== "") {
        let nodeAttrValue;
        if (attrIndexInNode > nameOnlyMarkerIdx) {
          nodeAttrValue = "";
        } else {
          ngDevMode && assertNotEqual(nodeAttrs[attrIndexInNode], 0, "We do not match directives on namespaced attributes");
          nodeAttrValue = nodeAttrs[attrIndexInNode + 1].toLowerCase();
        }
        if (mode & 2 && selectorAttrValue !== nodeAttrValue) {
          if (isPositive(mode)) return false;
          skipToNextSelector = true;
        }
      }
    }
  }
  return isPositive(mode) || skipToNextSelector;
}
function isPositive(mode) {
  return (mode & 1) === 0;
}
function findAttrIndexInNode(name, attrs, isInlineTemplate2, isProjectionMode) {
  if (attrs === null) return -1;
  let i = 0;
  if (isProjectionMode || !isInlineTemplate2) {
    let bindingsMode = false;
    while (i < attrs.length) {
      const maybeAttrName = attrs[i];
      if (maybeAttrName === name) {
        return i;
      } else if (maybeAttrName === 3 || maybeAttrName === 6) {
        bindingsMode = true;
      } else if (maybeAttrName === 1 || maybeAttrName === 2) {
        let value = attrs[++i];
        while (typeof value === "string") {
          value = attrs[++i];
        }
        continue;
      } else if (maybeAttrName === 4) {
        break;
      } else if (maybeAttrName === 0) {
        i += 4;
        continue;
      }
      i += bindingsMode ? 1 : 2;
    }
    return -1;
  } else {
    return matchTemplateAttribute(attrs, name);
  }
}
function isNodeMatchingSelectorList(tNode, selector, isProjectionMode = false) {
  for (let i = 0; i < selector.length; i++) {
    if (isNodeMatchingSelector(tNode, selector[i], isProjectionMode)) {
      return true;
    }
  }
  return false;
}
function getProjectAsAttrValue(tNode) {
  const nodeAttrs = tNode.attrs;
  if (nodeAttrs != null) {
    const ngProjectAsAttrIdx = nodeAttrs.indexOf(5);
    if ((ngProjectAsAttrIdx & 1) === 0) {
      return nodeAttrs[ngProjectAsAttrIdx + 1];
    }
  }
  return null;
}
function getNameOnlyMarkerIndex(nodeAttrs) {
  for (let i = 0; i < nodeAttrs.length; i++) {
    const nodeAttr = nodeAttrs[i];
    if (isNameOnlyAttributeMarker(nodeAttr)) {
      return i;
    }
  }
  return nodeAttrs.length;
}
function matchTemplateAttribute(attrs, name) {
  let i = attrs.indexOf(4);
  if (i > -1) {
    i++;
    while (i < attrs.length) {
      const attr = attrs[i];
      if (typeof attr === "number") return -1;
      if (attr === name) return i;
      i++;
    }
  }
  return -1;
}
function isSelectorInSelectorList(selector, list) {
  selectorListLoop: for (let i = 0; i < list.length; i++) {
    const currentSelectorInList = list[i];
    if (selector.length !== currentSelectorInList.length) {
      continue;
    }
    for (let j = 0; j < selector.length; j++) {
      if (selector[j] !== currentSelectorInList[j]) {
        continue selectorListLoop;
      }
    }
    return true;
  }
  return false;
}
function maybeWrapInNotSelector(isNegativeMode, chunk) {
  return isNegativeMode ? ":not(" + chunk.trim() + ")" : chunk;
}
function stringifyCSSSelector(selector) {
  let result = selector[0];
  let i = 1;
  let mode = 2;
  let currentChunk = "";
  let isNegativeMode = false;
  while (i < selector.length) {
    let valueOrMarker = selector[i];
    if (typeof valueOrMarker === "string") {
      if (mode & 2) {
        const attrValue = selector[++i];
        currentChunk += "[" + valueOrMarker + (attrValue.length > 0 ? '="' + attrValue + '"' : "") + "]";
      } else if (mode & 8) {
        currentChunk += "." + valueOrMarker;
      } else if (mode & 4) {
        currentChunk += " " + valueOrMarker;
      }
    } else {
      if (currentChunk !== "" && !isPositive(valueOrMarker)) {
        result += maybeWrapInNotSelector(isNegativeMode, currentChunk);
        currentChunk = "";
      }
      mode = valueOrMarker;
      isNegativeMode = isNegativeMode || !isPositive(mode);
    }
    i++;
  }
  if (currentChunk !== "") {
    result += maybeWrapInNotSelector(isNegativeMode, currentChunk);
  }
  return result;
}
function stringifyCSSSelectorList(selectorList) {
  return selectorList.map(stringifyCSSSelector).join(",");
}
function extractAttrsAndClassesFromSelector(selector) {
  const attrs = [];
  const classes = [];
  let i = 1;
  let mode = 2;
  while (i < selector.length) {
    let valueOrMarker = selector[i];
    if (typeof valueOrMarker === "string") {
      if (mode === 2) {
        if (valueOrMarker !== "") {
          attrs.push(valueOrMarker, selector[++i]);
        }
      } else if (mode === 8) {
        classes.push(valueOrMarker);
      }
    } else {
      if (!isPositive(mode)) break;
      mode = valueOrMarker;
    }
    i++;
  }
  if (classes.length) {
    attrs.push(1, ...classes);
  }
  return attrs;
}
var NO_CHANGE = typeof ngDevMode === "undefined" || ngDevMode ? {
  __brand__: "NO_CHANGE"
} : {};
function createTView(type, declTNode, templateFn, decls, vars, directives, pipes, viewQuery, schemas, constsOrFactory, ssrId) {
  const bindingStartIndex = HEADER_OFFSET + decls;
  const initialViewLength = bindingStartIndex + vars;
  const blueprint = createViewBlueprint(bindingStartIndex, initialViewLength);
  const consts = typeof constsOrFactory === "function" ? constsOrFactory() : constsOrFactory;
  const tView = blueprint[TVIEW] = {
    type,
    blueprint,
    template: templateFn,
    queries: null,
    viewQuery,
    declTNode,
    data: blueprint.slice().fill(null, bindingStartIndex),
    bindingStartIndex,
    expandoStartIndex: initialViewLength,
    hostBindingOpCodes: null,
    firstCreatePass: true,
    firstUpdatePass: true,
    staticViewQueries: false,
    staticContentQueries: false,
    preOrderHooks: null,
    preOrderCheckHooks: null,
    contentHooks: null,
    contentCheckHooks: null,
    viewHooks: null,
    viewCheckHooks: null,
    destroyHooks: null,
    cleanup: null,
    contentQueries: null,
    components: null,
    directiveRegistry: typeof directives === "function" ? directives() : directives,
    pipeRegistry: typeof pipes === "function" ? pipes() : pipes,
    firstChild: null,
    schemas,
    consts,
    incompleteFirstPass: false,
    ssrId
  };
  if (ngDevMode) {
    Object.seal(tView);
  }
  return tView;
}
function createViewBlueprint(bindingStartIndex, initialViewLength) {
  const blueprint = [];
  for (let i = 0; i < initialViewLength; i++) {
    blueprint.push(i < bindingStartIndex ? null : NO_CHANGE);
  }
  return blueprint;
}
function getOrCreateComponentTView(def) {
  const tView = def.tView;
  if (tView === null || tView.incompleteFirstPass) {
    const declTNode = null;
    return def.tView = createTView(1, declTNode, def.template, def.decls, def.vars, def.directiveDefs, def.pipeDefs, def.viewQuery, def.schemas, def.consts, def.id);
  }
  return tView;
}
function createLView(parentLView, tView, context2, flags, host, tHostNode, environment, renderer, injector, embeddedViewInjector, hydrationInfo) {
  const lView = tView.blueprint.slice();
  lView[HOST] = host;
  lView[FLAGS] = flags | 4 | 128 | 8 | 64 | 1024;
  if (embeddedViewInjector !== null || parentLView && parentLView[FLAGS] & 2048) {
    lView[FLAGS] |= 2048;
  }
  resetPreOrderHookFlags(lView);
  ngDevMode && tView.declTNode && parentLView && assertTNodeForLView(tView.declTNode, parentLView);
  lView[PARENT] = lView[DECLARATION_VIEW] = parentLView;
  lView[CONTEXT] = context2;
  lView[ENVIRONMENT] = environment || parentLView && parentLView[ENVIRONMENT];
  ngDevMode && assertDefined(lView[ENVIRONMENT], "LViewEnvironment is required");
  lView[RENDERER] = renderer || parentLView && parentLView[RENDERER];
  ngDevMode && assertDefined(lView[RENDERER], "Renderer is required");
  lView[INJECTOR] = injector || parentLView && parentLView[INJECTOR] || null;
  lView[T_HOST] = tHostNode;
  lView[ID] = getUniqueLViewId();
  lView[HYDRATION] = hydrationInfo;
  lView[EMBEDDED_VIEW_INJECTOR] = embeddedViewInjector;
  ngDevMode && assertEqual(tView.type == 2 ? parentLView !== null : true, true, "Embedded views must have parentLView");
  lView[DECLARATION_COMPONENT_VIEW] = tView.type == 2 ? parentLView[DECLARATION_COMPONENT_VIEW] : lView;
  return lView;
}
function createComponentLView(lView, hostTNode, def) {
  const native = getNativeByTNode(hostTNode, lView);
  const tView = getOrCreateComponentTView(def);
  const rendererFactory = lView[ENVIRONMENT].rendererFactory;
  const componentView = addToEndOfViewTree(lView, createLView(lView, tView, null, getInitialLViewFlagsFromDef(def), native, hostTNode, null, rendererFactory.createRenderer(native, def), null, null, null));
  return lView[hostTNode.index] = componentView;
}
function getInitialLViewFlagsFromDef(def) {
  let flags = 16;
  if (def.signals) {
    flags = 4096;
  } else if (def.onPush) {
    flags = 64;
  }
  return flags;
}
function allocExpando(tView, lView, numSlotsToAlloc, initialValue) {
  if (numSlotsToAlloc === 0) return -1;
  if (ngDevMode) {
    assertFirstCreatePass(tView);
    assertSame(tView, lView[TVIEW], "`LView` must be associated with `TView`!");
    assertEqual(tView.data.length, lView.length, "Expecting LView to be same size as TView");
    assertEqual(tView.data.length, tView.blueprint.length, "Expecting Blueprint to be same size as TView");
    assertFirstUpdatePass(tView);
  }
  const allocIdx = lView.length;
  for (let i = 0; i < numSlotsToAlloc; i++) {
    lView.push(initialValue);
    tView.blueprint.push(initialValue);
    tView.data.push(null);
  }
  return allocIdx;
}
function addToEndOfViewTree(lView, lViewOrLContainer) {
  if (lView[CHILD_HEAD]) {
    lView[CHILD_TAIL][NEXT] = lViewOrLContainer;
  } else {
    lView[CHILD_HEAD] = lViewOrLContainer;
  }
  lView[CHILD_TAIL] = lViewOrLContainer;
  return lViewOrLContainer;
}
function \u0275\u0275advance(delta = 1) {
  ngDevMode && assertGreaterThan(delta, 0, "Can only advance forward");
  selectIndexInternal(getTView(), getLView(), getSelectedIndex() + delta, !!ngDevMode && isInCheckNoChangesMode());
}
function selectIndexInternal(tView, lView, index, checkNoChangesMode) {
  ngDevMode && assertIndexInDeclRange(lView[TVIEW], index);
  if (!checkNoChangesMode) {
    const hooksInitPhaseCompleted = (lView[FLAGS] & 3) === 3;
    if (hooksInitPhaseCompleted) {
      const preOrderCheckHooks = tView.preOrderCheckHooks;
      if (preOrderCheckHooks !== null) {
        executeCheckHooks(lView, preOrderCheckHooks, index);
      }
    } else {
      const preOrderHooks = tView.preOrderHooks;
      if (preOrderHooks !== null) {
        executeInitAndCheckHooks(lView, preOrderHooks, 0, index);
      }
    }
  }
  setSelectedIndex(index);
}
var InputFlags;
(function(InputFlags2) {
  InputFlags2[InputFlags2["None"] = 0] = "None";
  InputFlags2[InputFlags2["SignalBased"] = 1] = "SignalBased";
  InputFlags2[InputFlags2["HasDecoratorInputTransform"] = 2] = "HasDecoratorInputTransform";
})(InputFlags || (InputFlags = {}));
function writeToDirectiveInput(def, instance, publicName, value) {
  const prevConsumer = setActiveConsumer(null);
  try {
    if (ngDevMode) {
      if (!def.inputs.hasOwnProperty(publicName)) {
        throw new Error(`ASSERTION ERROR: Directive ${def.type.name} does not have an input with a public name of "${publicName}"`);
      }
      if (instance instanceof NodeInjectorFactory) {
        throw new Error(`ASSERTION ERROR: Cannot write input to factory for type ${def.type.name}. Directive has not been created yet.`);
      }
    }
    const [privateName, flags, transform] = def.inputs[publicName];
    let inputSignalNode = null;
    if ((flags & InputFlags.SignalBased) !== 0) {
      const field = instance[privateName];
      inputSignalNode = field[SIGNAL];
    }
    if (inputSignalNode !== null && inputSignalNode.transformFn !== void 0) {
      value = inputSignalNode.transformFn(value);
    } else if (transform !== null) {
      value = transform.call(instance, value);
    }
    if (def.setInput !== null) {
      def.setInput(instance, inputSignalNode, value, publicName, privateName);
    } else {
      applyValueToInputField(instance, inputSignalNode, privateName, value);
    }
  } finally {
    setActiveConsumer(prevConsumer);
  }
}
var RendererStyleFlags2;
(function(RendererStyleFlags22) {
  RendererStyleFlags22[RendererStyleFlags22["Important"] = 1] = "Important";
  RendererStyleFlags22[RendererStyleFlags22["DashCase"] = 2] = "DashCase";
})(RendererStyleFlags2 || (RendererStyleFlags2 = {}));
var _icuContainerIterate;
function icuContainerIterate(tIcuContainerNode, lView) {
  return _icuContainerIterate(tIcuContainerNode, lView);
}
function ensureIcuContainerVisitorLoaded(loader) {
  if (_icuContainerIterate === void 0) {
    _icuContainerIterate = loader();
  }
}
var ANIMATIONS_DISABLED = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "AnimationsDisabled" : "", {
  factory: () => false
});
var MAX_ANIMATION_TIMEOUT = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "MaxAnimationTimeout" : "", {
  factory: () => MAX_ANIMATION_TIMEOUT_DEFAULT
});
var MAX_ANIMATION_TIMEOUT_DEFAULT = 4e3;
var DEFAULT_ANIMATIONS_DISABLED = false;
var areAnimationSupported = typeof document !== "undefined" && typeof document?.documentElement?.getAnimations === "function";
function areAnimationsDisabled(lView) {
  const injector = lView[INJECTOR];
  return injector.get(ANIMATIONS_DISABLED, DEFAULT_ANIMATIONS_DISABLED);
}
function assertAnimationTypes(value, instruction) {
  if (value == null || typeof value !== "string" && typeof value !== "function") {
    throw new RuntimeError(650, `'${instruction}' value must be a string of CSS classes or an animation function, got ${stringify(value)}`);
  }
}
function assertElementNodes(nativeElement, instruction) {
  if (nativeElement.nodeType !== Node.ELEMENT_NODE) {
    throw new RuntimeError(650, `'${instruction}' can only be used on an element node, got ${stringify(nativeElement.nodeType)}`);
  }
}
function trackEnterClasses(el, classList, cleanupFns) {
  const elementData = enterClassMap.get(el);
  if (elementData) {
    for (const klass of classList) {
      elementData.classList.push(klass);
    }
    for (const fn of cleanupFns) {
      elementData.cleanupFns.push(fn);
    }
  } else {
    enterClassMap.set(el, {
      classList,
      cleanupFns
    });
  }
}
function cleanupEnterClassData(element) {
  const elementData = enterClassMap.get(element);
  if (elementData) {
    for (const fn of elementData.cleanupFns) {
      fn();
    }
    enterClassMap.delete(element);
  }
  longestAnimations.delete(element);
}
var noOpAnimationComplete = () => {
};
var enterClassMap = /* @__PURE__ */ new WeakMap();
var longestAnimations = /* @__PURE__ */ new WeakMap();
var leavingNodes = /* @__PURE__ */ new WeakMap();
var reusedNodes = /* @__PURE__ */ new WeakSet();
function clearLeavingNodes(tNode, el) {
  const nodes = leavingNodes.get(tNode);
  if (nodes && nodes.length > 0) {
    const ix = nodes.findIndex((node) => node === el);
    if (ix > -1) nodes.splice(ix, 1);
  }
  if (nodes?.length === 0) {
    leavingNodes.delete(tNode);
  }
}
function cancelLeavingNodes(tNode, newElement) {
  const nodes = leavingNodes.get(tNode);
  if (!nodes || nodes.length === 0) return;
  const newParent = newElement.parentNode;
  const prevSibling = newElement.previousSibling;
  for (let i = nodes.length - 1; i >= 0; i--) {
    const leavingEl = nodes[i];
    const leavingParent = leavingEl.parentNode;
    if (leavingEl === newElement) {
      nodes.splice(i, 1);
      reusedNodes.add(leavingEl);
      leavingEl.dispatchEvent(new CustomEvent("animationend", {
        detail: {
          cancel: true
        }
      }));
    } else if (prevSibling && leavingEl === prevSibling || leavingParent && newParent && leavingParent !== newParent) {
      nodes.splice(i, 1);
      leavingEl.dispatchEvent(new CustomEvent("animationend", {
        detail: {
          cancel: true
        }
      }));
      leavingEl.parentNode?.removeChild(leavingEl);
    }
  }
}
function trackLeavingNodes(tNode, el) {
  const nodes = leavingNodes.get(tNode);
  if (nodes) {
    if (!nodes.includes(el)) {
      nodes.push(el);
    }
  } else {
    leavingNodes.set(tNode, [el]);
  }
}
function getLViewEnterAnimations(lView) {
  const animationData = lView[ANIMATIONS] ??= {};
  return animationData.enter ??= /* @__PURE__ */ new Map();
}
function getLViewLeaveAnimations(lView) {
  const animationData = lView[ANIMATIONS] ??= {};
  return animationData.leave ??= /* @__PURE__ */ new Map();
}
function getClassListFromValue(value) {
  const classes = typeof value === "function" ? value() : value;
  let classList = Array.isArray(classes) ? classes : null;
  if (typeof classes === "string") {
    classList = classes.trim().split(/\s+/).filter((k) => k);
  }
  return classList;
}
function cancelAnimationsIfRunning(element, renderer) {
  if (!areAnimationSupported) return;
  const elementData = enterClassMap.get(element);
  if (elementData && elementData.classList.length > 0 && elementHasClassList(element, elementData.classList)) {
    for (const klass of elementData.classList) {
      renderer.removeClass(element, klass);
    }
  }
  cleanupEnterClassData(element);
}
function elementHasClassList(element, classList) {
  for (const className of classList) {
    if (element.classList.contains(className)) return true;
  }
  return false;
}
function getEventTarget(event) {
  return event.composedPath ? event.composedPath()[0] : event.target;
}
function isLongestAnimation(event, nativeElement) {
  const longestAnimation = longestAnimations.get(nativeElement);
  if (longestAnimation === void 0) return true;
  return nativeElement === getEventTarget(event) && (longestAnimation.animationName !== void 0 && event.animationName === longestAnimation.animationName || longestAnimation.propertyName !== void 0 && (longestAnimation.propertyName === "all" || event.propertyName === longestAnimation.propertyName));
}
function addAnimationToLView(animations, tNode, fn) {
  const nodeAnimations = animations.get(tNode.index) ?? {
    animateFns: []
  };
  nodeAnimations.animateFns.push(fn);
  animations.set(tNode.index, nodeAnimations);
}
function cleanupAfterLeaveAnimations(resolvers, cleanupFns) {
  if (resolvers) {
    for (const fn of resolvers) {
      fn();
    }
  }
  for (const fn of cleanupFns) {
    fn();
  }
}
function clearLViewNodeAnimationResolvers(lView, tNode) {
  const nodeAnimations = getLViewLeaveAnimations(lView).get(tNode.index);
  if (nodeAnimations) nodeAnimations.resolvers = void 0;
}
function leaveAnimationFunctionCleanup(lView, tNode, nativeElement, resolvers, cleanupFns) {
  clearLeavingNodes(tNode, nativeElement);
  cleanupAfterLeaveAnimations(resolvers, cleanupFns);
  clearLViewNodeAnimationResolvers(lView, tNode);
}
function parseCssTimeUnitsToMs(value) {
  if (!value) return 0;
  const multiplier = value.toLowerCase().indexOf("ms") > -1 ? 1 : 1e3;
  return parseFloat(value) * multiplier;
}
function parseCssPropertyValue(computedStyle, name) {
  const value = computedStyle.getPropertyValue(name);
  return value.split(",").map((part) => part.trim());
}
function getLongestComputedTransition(computedStyle) {
  const transitionedProperties = parseCssPropertyValue(computedStyle, "transition-property");
  const rawDurations = parseCssPropertyValue(computedStyle, "transition-duration");
  const rawDelays = parseCssPropertyValue(computedStyle, "transition-delay");
  const longest = {
    propertyName: "",
    duration: 0,
    animationName: void 0
  };
  for (let i = 0; i < transitionedProperties.length; i++) {
    const duration = parseCssTimeUnitsToMs(rawDelays[i]) + parseCssTimeUnitsToMs(rawDurations[i]);
    if (duration > longest.duration) {
      longest.propertyName = transitionedProperties[i];
      longest.duration = duration;
    }
  }
  return longest;
}
function getLongestComputedAnimation(computedStyle) {
  const rawNames = parseCssPropertyValue(computedStyle, "animation-name");
  const rawDelays = parseCssPropertyValue(computedStyle, "animation-delay");
  const rawDurations = parseCssPropertyValue(computedStyle, "animation-duration");
  const rawIterationCounts = parseCssPropertyValue(computedStyle, "animation-iteration-count");
  const longest = {
    animationName: "",
    propertyName: void 0,
    duration: 0
  };
  for (let i = 0; i < rawNames.length; i++) {
    const duration = parseCssTimeUnitsToMs(rawDelays[i]) + parseCssTimeUnitsToMs(rawDurations[i]);
    const iterationCount = rawIterationCounts[i];
    if (duration > longest.duration && iterationCount !== "infinite") {
      longest.animationName = rawNames[i];
      longest.duration = duration;
    }
  }
  return longest;
}
function isShorterThanExistingAnimation(existing, longest) {
  return existing !== void 0 && existing.duration > longest.duration;
}
function longestExists(longest) {
  return (longest.animationName != void 0 || longest.propertyName != void 0) && longest.duration > 0;
}
function determineLongestAnimationFromComputedStyles(el, animationsMap) {
  const computedStyle = getComputedStyle(el);
  const longestAnimation = getLongestComputedAnimation(computedStyle);
  const longestTransition = getLongestComputedTransition(computedStyle);
  const longest = longestAnimation.duration > longestTransition.duration ? longestAnimation : longestTransition;
  if (isShorterThanExistingAnimation(animationsMap.get(el), longest)) return;
  if (longestExists(longest)) {
    animationsMap.set(el, longest);
  }
}
function determineLongestAnimation(el, animationsMap, areAnimationSupported2) {
  if (!areAnimationSupported2) return;
  const animations = el.getAnimations();
  return animations.length === 0 ? determineLongestAnimationFromComputedStyles(el, animationsMap) : determineLongestAnimationFromElementAnimations(el, animationsMap, animations);
}
function determineLongestAnimationFromElementAnimations(el, animationsMap, animations) {
  let longest = {
    animationName: void 0,
    propertyName: void 0,
    duration: 0
  };
  for (const animation of animations) {
    const timing = animation.effect?.getTiming();
    if (timing?.iterations === Infinity) {
      continue;
    }
    const animDuration = typeof timing?.duration === "number" ? timing.duration : 0;
    let duration = (timing?.delay ?? 0) + animDuration;
    const playbackRate = animation.playbackRate;
    if (playbackRate !== void 0 && playbackRate !== 0 && playbackRate !== 1) {
      duration /= Math.abs(playbackRate);
    }
    let propertyName;
    let animationName;
    if (animation.animationName) {
      animationName = animation.animationName;
    } else {
      propertyName = animation.transitionProperty;
    }
    if (duration >= longest.duration) {
      longest = {
        animationName,
        propertyName,
        duration
      };
    }
  }
  if (isShorterThanExistingAnimation(animationsMap.get(el), longest)) return;
  if (longestExists(longest)) {
    animationsMap.set(el, longest);
  }
}
var allLeavingAnimations = /* @__PURE__ */ new Set();
var TracingAction;
(function(TracingAction2) {
  TracingAction2[TracingAction2["CHANGE_DETECTION"] = 0] = "CHANGE_DETECTION";
  TracingAction2[TracingAction2["AFTER_NEXT_RENDER"] = 1] = "AFTER_NEXT_RENDER";
})(TracingAction || (TracingAction = {}));
var TracingService = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "TracingService" : "");
var markedFeatures = /* @__PURE__ */ new Set();
function performanceMarkFeature(feature) {
  if (markedFeatures.has(feature)) {
    return;
  }
  markedFeatures.add(feature);
  performance?.mark?.("mark_feature_usage", {
    detail: {
      feature
    }
  });
}
var AfterRenderManager = class _AfterRenderManager {
  impl = null;
  execute() {
    this.impl?.execute();
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _AfterRenderManager,
    providedIn: "root",
    factory: () => new _AfterRenderManager()
  });
};
var AFTER_RENDER_PHASES = /* @__PURE__ */ (() => [0, 1, 2, 3])();
var AfterRenderImpl = class _AfterRenderImpl {
  ngZone = inject2(NgZone);
  scheduler = inject2(ChangeDetectionScheduler);
  errorHandler = inject2(ErrorHandler, {
    optional: true
  });
  sequences = /* @__PURE__ */ new Set();
  deferredRegistrations = /* @__PURE__ */ new Set();
  executing = false;
  constructor() {
    inject2(TracingService, {
      optional: true
    });
  }
  execute() {
    const hasSequencesToExecute = this.sequences.size > 0;
    if (hasSequencesToExecute) {
      profiler(ProfilerEvent.AfterRenderHooksStart);
    }
    this.executing = true;
    for (const phase of AFTER_RENDER_PHASES) {
      for (const sequence of this.sequences) {
        if (sequence.erroredOrDestroyed || !sequence.hooks[phase]) {
          continue;
        }
        try {
          sequence.pipelinedValue = this.ngZone.runOutsideAngular(() => this.maybeTrace(() => {
            const hookFn = sequence.hooks[phase];
            const value = hookFn(sequence.pipelinedValue);
            return value;
          }, sequence.snapshot));
        } catch (err) {
          sequence.erroredOrDestroyed = true;
          this.errorHandler?.handleError(err);
        }
      }
    }
    this.executing = false;
    for (const sequence of this.sequences) {
      sequence.afterRun();
      if (sequence.once) {
        this.sequences.delete(sequence);
        sequence.destroy();
      }
    }
    for (const sequence of this.deferredRegistrations) {
      this.sequences.add(sequence);
    }
    if (this.deferredRegistrations.size > 0) {
      this.scheduler.notify(7);
    }
    this.deferredRegistrations.clear();
    if (hasSequencesToExecute) {
      profiler(ProfilerEvent.AfterRenderHooksEnd);
    }
  }
  register(sequence) {
    const {
      view
    } = sequence;
    if (view !== void 0) {
      (view[AFTER_RENDER_SEQUENCES_TO_ADD] ??= []).push(sequence);
      markAncestorsForTraversal(view);
      view[FLAGS] |= 8192;
    } else if (!this.executing) {
      this.addSequence(sequence);
    } else {
      this.deferredRegistrations.add(sequence);
    }
  }
  addSequence(sequence) {
    this.sequences.add(sequence);
    this.scheduler.notify(7);
  }
  unregister(sequence) {
    if (this.executing && this.sequences.has(sequence)) {
      sequence.erroredOrDestroyed = true;
      sequence.pipelinedValue = void 0;
      sequence.once = true;
    } else {
      this.sequences.delete(sequence);
      this.deferredRegistrations.delete(sequence);
    }
  }
  maybeTrace(fn, snapshot) {
    return snapshot ? snapshot.run(TracingAction.AFTER_NEXT_RENDER, fn) : fn();
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _AfterRenderImpl,
    providedIn: "root",
    factory: () => new _AfterRenderImpl()
  });
};
var AfterRenderSequence = class {
  impl;
  hooks;
  view;
  once;
  snapshot;
  erroredOrDestroyed = false;
  pipelinedValue = void 0;
  unregisterOnDestroy;
  constructor(impl, hooks, view, once, destroyRef, snapshot = null) {
    this.impl = impl;
    this.hooks = hooks;
    this.view = view;
    this.once = once;
    this.snapshot = snapshot;
    this.unregisterOnDestroy = destroyRef?.onDestroy(() => this.destroy());
  }
  afterRun() {
    this.erroredOrDestroyed = false;
    this.pipelinedValue = void 0;
    this.snapshot?.dispose();
    this.snapshot = null;
  }
  destroy() {
    this.impl.unregister(this);
    this.unregisterOnDestroy?.();
    const scheduled2 = this.view?.[AFTER_RENDER_SEQUENCES_TO_ADD];
    if (scheduled2) {
      this.view[AFTER_RENDER_SEQUENCES_TO_ADD] = scheduled2.filter((s) => s !== this);
    }
  }
};
function afterEveryRender(callbackOrSpec, options) {
  ngDevMode && assertNotInReactiveContext(afterEveryRender, "Call `afterEveryRender` outside of a reactive context. For example, schedule the render callback inside the component constructor`.");
  if (ngDevMode && !options?.injector) {
    assertInInjectionContext(afterEveryRender);
  }
  const injector = options?.injector ?? inject2(Injector);
  if (false) {
    return NOOP_AFTER_RENDER_REF;
  }
  performanceMarkFeature("NgAfterRender");
  return afterEveryRenderImpl(callbackOrSpec, injector, options, false);
}
function afterNextRender(callbackOrSpec, options) {
  if (ngDevMode && !options?.injector) {
    assertInInjectionContext(afterNextRender);
  }
  const injector = options?.injector ?? inject2(Injector);
  if (false) {
    return NOOP_AFTER_RENDER_REF;
  }
  performanceMarkFeature("NgAfterNextRender");
  return afterEveryRenderImpl(callbackOrSpec, injector, options, true);
}
function getHooks(callbackOrSpec) {
  if (callbackOrSpec instanceof Function) {
    return [void 0, void 0, callbackOrSpec, void 0];
  } else {
    return [callbackOrSpec.earlyRead, callbackOrSpec.write, callbackOrSpec.mixedReadWrite, callbackOrSpec.read];
  }
}
function afterEveryRenderImpl(callbackOrSpec, injector, options, once) {
  const manager = injector.get(AfterRenderManager);
  manager.impl ??= injector.get(AfterRenderImpl);
  const tracing = injector.get(TracingService, null, {
    optional: true
  });
  const destroyRef = options?.manualCleanup !== true ? injector.get(DestroyRef) : null;
  const viewContext = injector.get(ViewContext, null, {
    optional: true
  });
  const sequence = new AfterRenderSequence(manager.impl, getHooks(callbackOrSpec), viewContext?.view, once, destroyRef, tracing?.snapshot(null));
  manager.impl.register(sequence);
  return sequence;
}
var ANIMATION_QUEUE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "AnimationQueue" : "", {
  factory: () => {
    return {
      queue: /* @__PURE__ */ new Set(),
      isScheduled: false,
      scheduler: null,
      injector: inject2(EnvironmentInjector)
    };
  }
});
function addToAnimationQueue(injector, animationFns, animationData) {
  const animationQueue = injector.get(ANIMATION_QUEUE);
  if (Array.isArray(animationFns)) {
    for (const animateFn of animationFns) {
      animationQueue.queue.add(animateFn);
      animationData?.detachedLeaveAnimationFns?.push(animateFn);
    }
  } else {
    animationQueue.queue.add(animationFns);
    animationData?.detachedLeaveAnimationFns?.push(animationFns);
  }
  animationQueue.scheduler && animationQueue.scheduler(injector);
}
function removeFromAnimationQueue(injector, animationData) {
  const animationQueue = injector.get(ANIMATION_QUEUE);
  if (animationData.detachedLeaveAnimationFns) {
    for (const animationFn of animationData.detachedLeaveAnimationFns) {
      animationQueue.queue.delete(animationFn);
    }
    animationData.detachedLeaveAnimationFns = void 0;
  }
}
function scheduleAnimationQueue(injector) {
  const animationQueue = injector.get(ANIMATION_QUEUE);
  if (!animationQueue.isScheduled) {
    afterNextRender(() => {
      animationQueue.isScheduled = false;
      for (let animateFn of animationQueue.queue) {
        animateFn();
      }
      animationQueue.queue.clear();
    }, {
      injector: animationQueue.injector
    });
    animationQueue.isScheduled = true;
  }
}
function initializeAnimationQueueScheduler(injector) {
  const animationQueue = injector.get(ANIMATION_QUEUE);
  animationQueue.scheduler = scheduleAnimationQueue;
  animationQueue.scheduler(injector);
}
function queueEnterAnimations(injector, enterAnimations) {
  for (const [_, nodeAnimations] of enterAnimations) {
    addToAnimationQueue(injector, nodeAnimations.animateFns);
  }
}
function maybeQueueEnterAnimation(parentLView, parent, tNode, injector) {
  const enterAnimations = parentLView?.[ANIMATIONS]?.enter;
  if (parent !== null && enterAnimations && enterAnimations.has(tNode.index)) {
    queueEnterAnimations(injector, enterAnimations);
  }
}
function applyToElementOrContainer(action, renderer, injector, parent, lNodeToHandle, tNode, beforeNode, parentLView) {
  if (lNodeToHandle != null) {
    let lContainer;
    let isComponent2 = false;
    if (isLContainer(lNodeToHandle)) {
      lContainer = lNodeToHandle;
    } else if (isLView(lNodeToHandle)) {
      isComponent2 = true;
      ngDevMode && assertDefined(lNodeToHandle[HOST], "HOST must be defined for a component LView");
      lNodeToHandle = lNodeToHandle[HOST];
    }
    const rNode = unwrapRNode(lNodeToHandle);
    if (action === 0 && parent !== null) {
      maybeQueueEnterAnimation(parentLView, parent, tNode, injector);
      if (beforeNode == null) {
        nativeAppendChild(renderer, parent, rNode);
      } else {
        nativeInsertBefore(renderer, parent, rNode, beforeNode || null, true);
      }
    } else if (action === 1 && parent !== null) {
      maybeQueueEnterAnimation(parentLView, parent, tNode, injector);
      nativeInsertBefore(renderer, parent, rNode, beforeNode || null, true);
      cancelLeavingNodes(tNode, rNode);
    } else if (action === 2) {
      if (parentLView?.[ANIMATIONS]?.leave?.has(tNode.index)) {
        trackLeavingNodes(tNode, rNode);
      }
      reusedNodes.delete(rNode);
      runLeaveAnimationsWithCallback(parentLView, tNode, injector, (nodeHasLeaveAnimations) => {
        if (reusedNodes.has(rNode)) {
          reusedNodes.delete(rNode);
          return;
        }
        nativeRemoveNode(renderer, rNode, isComponent2, nodeHasLeaveAnimations);
      });
    } else if (action === 3) {
      reusedNodes.delete(rNode);
      runLeaveAnimationsWithCallback(parentLView, tNode, injector, () => {
        renderer.destroyNode(rNode);
      });
    }
    if (lContainer != null) {
      applyContainer(renderer, action, injector, lContainer, tNode, parent, beforeNode);
    }
  }
}
function removeViewFromDOM(tView, lView) {
  detachViewFromDOM(tView, lView);
  lView[HOST] = null;
  lView[T_HOST] = null;
}
function addViewToDOM(tView, parentTNode, renderer, lView, parentNativeNode, beforeNode) {
  lView[HOST] = parentNativeNode;
  lView[T_HOST] = parentTNode;
  applyView(tView, lView, renderer, 1, parentNativeNode, beforeNode);
}
function detachViewFromDOM(tView, lView) {
  lView[ENVIRONMENT].changeDetectionScheduler?.notify(9);
  applyView(tView, lView, lView[RENDERER], 2, null, null);
}
function destroyViewTree(rootView) {
  let lViewOrLContainer = rootView[CHILD_HEAD];
  if (!lViewOrLContainer) {
    return cleanUpView(rootView[TVIEW], rootView);
  }
  while (lViewOrLContainer) {
    let next = null;
    if (isLView(lViewOrLContainer)) {
      next = lViewOrLContainer[CHILD_HEAD];
    } else {
      ngDevMode && assertLContainer(lViewOrLContainer);
      const firstView = lViewOrLContainer[CONTAINER_HEADER_OFFSET];
      if (firstView) next = firstView;
    }
    if (!next) {
      while (lViewOrLContainer && !lViewOrLContainer[NEXT] && lViewOrLContainer !== rootView) {
        if (isLView(lViewOrLContainer)) {
          cleanUpView(lViewOrLContainer[TVIEW], lViewOrLContainer);
        }
        lViewOrLContainer = lViewOrLContainer[PARENT];
      }
      if (lViewOrLContainer === null) lViewOrLContainer = rootView;
      if (isLView(lViewOrLContainer)) {
        cleanUpView(lViewOrLContainer[TVIEW], lViewOrLContainer);
      }
      next = lViewOrLContainer && lViewOrLContainer[NEXT];
    }
    lViewOrLContainer = next;
  }
}
function detachMovedView(declarationContainer, lView) {
  ngDevMode && assertLContainer(declarationContainer);
  ngDevMode && assertDefined(declarationContainer[MOVED_VIEWS], "A projected view should belong to a non-empty projected views collection");
  const movedViews = declarationContainer[MOVED_VIEWS];
  const declarationViewIndex = movedViews.indexOf(lView);
  movedViews.splice(declarationViewIndex, 1);
}
function destroyLView(tView, lView) {
  if (isDestroyed(lView)) {
    return;
  }
  const renderer = lView[RENDERER];
  if (renderer.destroyNode) {
    applyView(tView, lView, renderer, 3, null, null);
  }
  destroyViewTree(lView);
}
function cleanUpView(tView, lView) {
  if (isDestroyed(lView)) {
    return;
  }
  const prevConsumer = setActiveConsumer(null);
  try {
    lView[FLAGS] &= ~128;
    lView[FLAGS] |= 256;
    lView[REACTIVE_TEMPLATE_CONSUMER] && consumerDestroy(lView[REACTIVE_TEMPLATE_CONSUMER]);
    executeOnDestroys(tView, lView);
    processCleanups(tView, lView);
    if (lView[TVIEW].type === 1) {
      lView[RENDERER].destroy();
    }
    const declarationContainer = lView[DECLARATION_LCONTAINER];
    if (declarationContainer !== null && isLContainer(lView[PARENT])) {
      if (declarationContainer !== lView[PARENT]) {
        detachMovedView(declarationContainer, lView);
      }
      const lQueries = lView[QUERIES];
      if (lQueries !== null) {
        lQueries.detachView(tView);
      }
    }
    unregisterLView(lView);
  } finally {
    setActiveConsumer(prevConsumer);
  }
}
function runLeaveAnimationsWithCallback(lView, tNode, injector, callback) {
  const animations = lView?.[ANIMATIONS];
  if (animations == null || animations.leave == void 0 || !animations.leave.has(tNode.index)) return callback(false);
  if (lView) allLeavingAnimations.add(lView[ID]);
  addToAnimationQueue(injector, () => {
    if (animations.leave && animations.leave.has(tNode.index)) {
      const leaveAnimationMap = animations.leave;
      const leaveAnimations = leaveAnimationMap.get(tNode.index);
      const runningAnimations = [];
      if (leaveAnimations) {
        for (let index = 0; index < leaveAnimations.animateFns.length; index++) {
          const animationFn = leaveAnimations.animateFns[index];
          const {
            promise
          } = animationFn();
          runningAnimations.push(promise);
        }
        animations.detachedLeaveAnimationFns = void 0;
      }
      animations.running = Promise.allSettled(runningAnimations);
      runAfterLeaveAnimations(lView, callback);
    } else {
      if (lView) allLeavingAnimations.delete(lView[ID]);
      callback(false);
    }
  }, animations);
}
function runAfterLeaveAnimations(lView, callback) {
  const runningAnimations = lView[ANIMATIONS]?.running;
  if (runningAnimations) {
    runningAnimations.then(() => {
      lView[ANIMATIONS].running = void 0;
      allLeavingAnimations.delete(lView[ID]);
      callback(true);
    });
    return;
  }
  callback(false);
}
function processCleanups(tView, lView) {
  ngDevMode && assertNotReactive(processCleanups.name);
  const tCleanup = tView.cleanup;
  const lCleanup = lView[CLEANUP];
  if (tCleanup !== null) {
    for (let i = 0; i < tCleanup.length - 1; i += 2) {
      if (typeof tCleanup[i] === "string") {
        const targetIdx = tCleanup[i + 3];
        ngDevMode && assertNumber(targetIdx, "cleanup target must be a number");
        if (targetIdx >= 0) {
          lCleanup[targetIdx]();
        } else {
          lCleanup[-targetIdx].unsubscribe();
        }
        i += 2;
      } else {
        const context2 = lCleanup[tCleanup[i + 1]];
        tCleanup[i].call(context2);
      }
    }
  }
  if (lCleanup !== null) {
    lView[CLEANUP] = null;
  }
  const destroyHooks = lView[ON_DESTROY_HOOKS];
  if (destroyHooks !== null) {
    lView[ON_DESTROY_HOOKS] = null;
    for (let i = 0; i < destroyHooks.length; i++) {
      const destroyHooksFn = destroyHooks[i];
      ngDevMode && assertFunction(destroyHooksFn, "Expecting destroy hook to be a function.");
      destroyHooksFn();
    }
  }
  const effects = lView[EFFECTS];
  if (effects !== null) {
    lView[EFFECTS] = null;
    for (const effect2 of effects) {
      effect2.destroy();
    }
  }
}
function executeOnDestroys(tView, lView) {
  ngDevMode && assertNotReactive(executeOnDestroys.name);
  let destroyHooks;
  if (tView != null && (destroyHooks = tView.destroyHooks) != null) {
    for (let i = 0; i < destroyHooks.length; i += 2) {
      const context2 = lView[destroyHooks[i]];
      if (!(context2 instanceof NodeInjectorFactory)) {
        const toCall = destroyHooks[i + 1];
        if (Array.isArray(toCall)) {
          for (let j = 0; j < toCall.length; j += 2) {
            const callContext = context2[toCall[j]];
            const hook = toCall[j + 1];
            profiler(ProfilerEvent.LifecycleHookStart, callContext, hook);
            try {
              hook.call(callContext);
            } finally {
              profiler(ProfilerEvent.LifecycleHookEnd, callContext, hook);
            }
          }
        } else {
          profiler(ProfilerEvent.LifecycleHookStart, context2, toCall);
          try {
            toCall.call(context2);
          } finally {
            profiler(ProfilerEvent.LifecycleHookEnd, context2, toCall);
          }
        }
      }
    }
  }
}
function getParentRElement(tView, tNode, lView) {
  return getClosestRElement(tView, tNode.parent, lView);
}
function getClosestRElement(tView, tNode, lView) {
  let parentTNode = tNode;
  while (parentTNode !== null && parentTNode.type & (8 | 32 | 128)) {
    tNode = parentTNode;
    parentTNode = tNode.parent;
  }
  if (parentTNode === null) {
    return lView[HOST];
  } else {
    ngDevMode && assertTNodeType(parentTNode, 3 | 4);
    if (isComponentHost(parentTNode)) {
      ngDevMode && assertTNodeForLView(parentTNode, lView);
      const {
        encapsulation
      } = tView.data[parentTNode.directiveStart + parentTNode.componentOffset];
      if (encapsulation === ViewEncapsulation.None || encapsulation === ViewEncapsulation.Emulated) {
        return null;
      }
    }
    return getNativeByTNode(parentTNode, lView);
  }
}
function getInsertInFrontOfRNode(parentTNode, currentTNode, lView) {
  return _getInsertInFrontOfRNodeWithI18n(parentTNode, currentTNode, lView);
}
function getInsertInFrontOfRNodeWithNoI18n(parentTNode, currentTNode, lView) {
  if (parentTNode.type & (8 | 32)) {
    return getNativeByTNode(parentTNode, lView);
  }
  return null;
}
var _getInsertInFrontOfRNodeWithI18n = getInsertInFrontOfRNodeWithNoI18n;
var _processI18nInsertBefore;
function setI18nHandling(getInsertInFrontOfRNodeWithI18n2, processI18nInsertBefore2) {
  _getInsertInFrontOfRNodeWithI18n = getInsertInFrontOfRNodeWithI18n2;
  _processI18nInsertBefore = processI18nInsertBefore2;
}
function appendChild(tView, lView, childRNode, childTNode) {
  const parentRNode = getParentRElement(tView, childTNode, lView);
  const renderer = lView[RENDERER];
  const parentTNode = childTNode.parent || lView[T_HOST];
  const anchorNode = getInsertInFrontOfRNode(parentTNode, childTNode, lView);
  if (parentRNode != null) {
    if (Array.isArray(childRNode)) {
      for (let i = 0; i < childRNode.length; i++) {
        nativeAppendOrInsertBefore(renderer, parentRNode, childRNode[i], anchorNode, false);
      }
    } else {
      nativeAppendOrInsertBefore(renderer, parentRNode, childRNode, anchorNode, false);
    }
  }
  _processI18nInsertBefore !== void 0 && _processI18nInsertBefore(renderer, childTNode, lView, childRNode, parentRNode);
}
function getFirstNativeNode(lView, tNode) {
  if (tNode !== null) {
    ngDevMode && assertTNodeType(tNode, 3 | 12 | 32 | 16 | 128);
    const tNodeType = tNode.type;
    if (tNodeType & 3) {
      return getNativeByTNode(tNode, lView);
    } else if (tNodeType & 4) {
      return getBeforeNodeForView(-1, lView[tNode.index]);
    } else if (tNodeType & 8) {
      const elIcuContainerChild = tNode.child;
      if (elIcuContainerChild !== null) {
        return getFirstNativeNode(lView, elIcuContainerChild);
      } else {
        const rNodeOrLContainer = lView[tNode.index];
        if (isLContainer(rNodeOrLContainer)) {
          return getBeforeNodeForView(-1, rNodeOrLContainer);
        } else {
          return unwrapRNode(rNodeOrLContainer);
        }
      }
    } else if (tNodeType & 128) {
      return getFirstNativeNode(lView, tNode.next);
    } else if (tNodeType & 32) {
      let nextRNode = icuContainerIterate(tNode, lView);
      let rNode = nextRNode();
      return rNode || unwrapRNode(lView[tNode.index]);
    } else {
      const projectionNodes = getProjectionNodes(lView, tNode);
      if (projectionNodes !== null) {
        if (Array.isArray(projectionNodes)) {
          return projectionNodes[0];
        }
        const parentView = getLViewParent(lView[DECLARATION_COMPONENT_VIEW]);
        ngDevMode && assertParentView(parentView);
        return getFirstNativeNode(parentView, projectionNodes);
      } else {
        return getFirstNativeNode(lView, tNode.next);
      }
    }
  }
  return null;
}
function getProjectionNodes(lView, tNode) {
  if (tNode !== null) {
    const componentView = lView[DECLARATION_COMPONENT_VIEW];
    const componentHost = componentView[T_HOST];
    const slotIdx = tNode.projection;
    ngDevMode && assertProjectionSlots(lView);
    return componentHost.projection[slotIdx];
  }
  return null;
}
function getBeforeNodeForView(viewIndexInContainer, lContainer) {
  const nextViewIndex = CONTAINER_HEADER_OFFSET + viewIndexInContainer + 1;
  if (nextViewIndex < lContainer.length) {
    const lView = lContainer[nextViewIndex];
    const firstTNodeOfView = lView[TVIEW].firstChild;
    if (firstTNodeOfView !== null) {
      return getFirstNativeNode(lView, firstTNodeOfView);
    }
  }
  return lContainer[NATIVE];
}
function applyNodes(renderer, action, tNode, lView, parentRElement, beforeNode, isProjection) {
  while (tNode != null) {
    ngDevMode && assertTNodeForLView(tNode, lView);
    const injector = lView[INJECTOR];
    if (tNode.type === 128) {
      tNode = tNode.next;
      continue;
    }
    ngDevMode && assertTNodeType(tNode, 3 | 12 | 16 | 32);
    const rawSlotValue = lView[tNode.index];
    const tNodeType = tNode.type;
    if (isProjection) {
      if (action === 0) {
        rawSlotValue && attachPatchData(unwrapRNode(rawSlotValue), lView);
        tNode.flags |= 2;
      }
    }
    if (!isDetachedByI18n(tNode)) {
      if (tNodeType & 8) {
        applyNodes(renderer, action, tNode.child, lView, parentRElement, beforeNode, false);
        applyToElementOrContainer(action, renderer, injector, parentRElement, rawSlotValue, tNode, beforeNode, lView);
      } else if (tNodeType & 32) {
        const nextRNode = icuContainerIterate(tNode, lView);
        let rNode;
        while (rNode = nextRNode()) {
          applyToElementOrContainer(action, renderer, injector, parentRElement, rNode, tNode, beforeNode, lView);
        }
        applyToElementOrContainer(action, renderer, injector, parentRElement, rawSlotValue, tNode, beforeNode, lView);
      } else if (tNodeType & 16) {
        applyProjectionRecursive(renderer, action, lView, tNode, parentRElement, beforeNode);
      } else {
        ngDevMode && assertTNodeType(tNode, 3 | 4);
        applyToElementOrContainer(action, renderer, injector, parentRElement, rawSlotValue, tNode, beforeNode, lView);
      }
    }
    tNode = isProjection ? tNode.projectionNext : tNode.next;
  }
}
function applyView(tView, lView, renderer, action, parentRElement, beforeNode) {
  applyNodes(renderer, action, tView.firstChild, lView, parentRElement, beforeNode, false);
}
function applyProjection(tView, lView, tProjectionNode) {
  const renderer = lView[RENDERER];
  const parentRNode = getParentRElement(tView, tProjectionNode, lView);
  const parentTNode = tProjectionNode.parent || lView[T_HOST];
  let beforeNode = getInsertInFrontOfRNode(parentTNode, tProjectionNode, lView);
  applyProjectionRecursive(renderer, 0, lView, tProjectionNode, parentRNode, beforeNode);
}
function applyProjectionRecursive(renderer, action, lView, tProjectionNode, parentRElement, beforeNode) {
  const componentLView = lView[DECLARATION_COMPONENT_VIEW];
  const componentNode = componentLView[T_HOST];
  ngDevMode && assertEqual(typeof tProjectionNode.projection, "number", "expecting projection index");
  const nodeToProjectOrRNodes = componentNode.projection[tProjectionNode.projection];
  if (Array.isArray(nodeToProjectOrRNodes)) {
    for (let i = 0; i < nodeToProjectOrRNodes.length; i++) {
      const rNode = nodeToProjectOrRNodes[i];
      applyToElementOrContainer(action, renderer, lView[INJECTOR], parentRElement, rNode, tProjectionNode, beforeNode, lView);
    }
  } else {
    let nodeToProject = nodeToProjectOrRNodes;
    const projectedComponentLView = componentLView[PARENT];
    if (hasInSkipHydrationBlockFlag(tProjectionNode)) {
      nodeToProject.flags |= 128;
    }
    applyNodes(renderer, action, nodeToProject, projectedComponentLView, parentRElement, beforeNode, true);
  }
}
function applyContainer(renderer, action, injector, lContainer, tNode, parentRElement, beforeNode) {
  ngDevMode && assertLContainer(lContainer);
  const anchor = lContainer[NATIVE];
  const native = unwrapRNode(lContainer);
  if (anchor !== native) {
    applyToElementOrContainer(action, renderer, injector, parentRElement, anchor, tNode, beforeNode);
  }
  for (let i = CONTAINER_HEADER_OFFSET; i < lContainer.length; i++) {
    const lView = lContainer[i];
    applyView(lView[TVIEW], lView, renderer, action, parentRElement, anchor);
  }
}
function applyStyling(renderer, isClassBased, rNode, prop, value) {
  if (isClassBased) {
    if (!value) {
      renderer.removeClass(rNode, prop);
    } else {
      renderer.addClass(rNode, prop);
    }
  } else {
    let flags = prop.indexOf("-") === -1 ? void 0 : RendererStyleFlags2.DashCase;
    if (value == null) {
      renderer.removeStyle(rNode, prop, flags);
    } else {
      const isImportant = typeof value === "string" ? value.endsWith("!important") : false;
      if (isImportant) {
        value = value.slice(0, -10);
        flags |= RendererStyleFlags2.Important;
      }
      renderer.setStyle(rNode, prop, value, flags);
    }
  }
}
function executeTemplate(tView, lView, templateFn, rf, context2) {
  const prevSelectedIndex = getSelectedIndex();
  const isUpdatePhase = rf & 2;
  try {
    setSelectedIndex(-1);
    if (isUpdatePhase && lView.length > HEADER_OFFSET) {
      selectIndexInternal(tView, lView, HEADER_OFFSET, !!ngDevMode && isInCheckNoChangesMode());
    }
    const preHookType = isUpdatePhase ? ProfilerEvent.TemplateUpdateStart : ProfilerEvent.TemplateCreateStart;
    profiler(preHookType, context2, templateFn);
    templateFn(rf, context2);
  } finally {
    setSelectedIndex(prevSelectedIndex);
    const postHookType = isUpdatePhase ? ProfilerEvent.TemplateUpdateEnd : ProfilerEvent.TemplateCreateEnd;
    profiler(postHookType, context2, templateFn);
  }
}
function createDirectivesInstances(tView, lView, tNode) {
  instantiateAllDirectives(tView, lView, tNode);
  if ((tNode.flags & 64) === 64) {
    invokeDirectivesHostBindings(tView, lView, tNode);
  }
}
function saveResolvedLocalsInData(viewData, tNode, localRefExtractor = getNativeByTNode) {
  const localNames = tNode.localNames;
  if (localNames !== null) {
    let localIndex = tNode.index + 1;
    for (let i = 0; i < localNames.length; i += 2) {
      const index = localNames[i + 1];
      const value = index === -1 ? localRefExtractor(tNode, viewData) : viewData[index];
      viewData[localIndex++] = value;
    }
  }
}
function locateHostElement(renderer, elementOrSelector, encapsulation, injector) {
  const preserveHostContent = injector.get(PRESERVE_HOST_CONTENT, PRESERVE_HOST_CONTENT_DEFAULT);
  const preserveContent = preserveHostContent || encapsulation === ViewEncapsulation.ShadowDom || encapsulation === ViewEncapsulation.ExperimentalIsolatedShadowDom;
  const rootElement = renderer.selectRootElement(elementOrSelector, preserveContent);
  if (rootElement.tagName.toLowerCase() === "script") {
    throw new RuntimeError(905, ngDevMode && `"<script>" tag is not allowed as a component host element.`);
  }
  applyRootElementTransform(rootElement);
  return rootElement;
}
function applyRootElementTransform(rootElement) {
  _applyRootElementTransformImpl(rootElement);
}
var _applyRootElementTransformImpl = () => null;
function mapPropName(name) {
  if (name === "class") return "className";
  if (name === "for") return "htmlFor";
  if (name === "formaction") return "formAction";
  if (name === "innerHtml") return "innerHTML";
  if (name === "readonly") return "readOnly";
  if (name === "tabindex") return "tabIndex";
  return name;
}
function setPropertyAndInputs(tNode, lView, propName, value, renderer, sanitizer) {
  ngDevMode && assertNotSame(value, NO_CHANGE, "Incoming value should never be NO_CHANGE.");
  const tView = lView[TVIEW];
  const hasSetInput = setAllInputsForProperty(tNode, tView, lView, propName, value);
  if (hasSetInput) {
    isComponentHost(tNode) && markDirtyIfOnPush(lView, tNode.index);
    ngDevMode && setNgReflectProperties(lView, tView, tNode, propName, value);
    return;
  }
  if (tNode.type & 3) {
    propName = mapPropName(propName);
  }
  setDomProperty(tNode, lView, propName, value, renderer, sanitizer);
}
function setDomProperty(tNode, lView, propName, value, renderer, sanitizer) {
  if (tNode.type & 3) {
    const element = getNativeByTNode(tNode, lView);
    if (ngDevMode) {
      if (lView[TVIEW].firstUpdatePass) {
        validateAgainstEventProperties(propName);
      }
      if (!isPropertyValid(element, propName, tNode.value, lView[TVIEW].schemas)) {
        handleUnknownPropertyError(propName, tNode.value, tNode.type, lView);
      }
    }
    value = sanitizer != null ? sanitizer(value, tNode.value || "", propName) : value;
    renderer.setProperty(element, propName, value);
  } else if (tNode.type & 12) {
    if (ngDevMode && !matchingSchemas(lView[TVIEW].schemas, tNode.value)) {
      handleUnknownPropertyError(propName, tNode.value, tNode.type, lView);
    }
  }
}
function markDirtyIfOnPush(lView, viewIndex) {
  ngDevMode && assertLView(lView);
  const childComponentLView = getComponentLViewByIndex(viewIndex, lView);
  if (!(childComponentLView[FLAGS] & 16)) {
    childComponentLView[FLAGS] |= 64;
  }
}
function setNgReflectProperty(lView, tNode, attrName, value) {
  const environment = lView[ENVIRONMENT];
  if (!environment.ngReflect) {
    return;
  }
  const element = getNativeByTNode(tNode, lView);
  const renderer = lView[RENDERER];
  attrName = normalizeDebugBindingName(attrName);
  const debugValue = normalizeDebugBindingValue(value);
  if (tNode.type & 3) {
    if (value == null) {
      renderer.removeAttribute(element, attrName);
    } else {
      renderer.setAttribute(element, attrName, debugValue);
    }
  } else {
    const textContent = escapeCommentText(`bindings=${JSON.stringify({
      [attrName]: debugValue
    }, null, 2)}`);
    renderer.setValue(element, textContent);
  }
}
function setNgReflectProperties(lView, tView, tNode, publicName, value) {
  const environment = lView[ENVIRONMENT];
  if (!environment.ngReflect || !(tNode.type & (3 | 4))) {
    return;
  }
  const inputConfig = tNode.inputs?.[publicName];
  const hostInputConfig = tNode.hostDirectiveInputs?.[publicName];
  if (hostInputConfig) {
    for (let i = 0; i < hostInputConfig.length; i += 2) {
      const index = hostInputConfig[i];
      const publicName2 = hostInputConfig[i + 1];
      const def = tView.data[index];
      setNgReflectProperty(lView, tNode, def.inputs[publicName2][0], value);
    }
  }
  if (inputConfig) {
    for (const index of inputConfig) {
      const def = tView.data[index];
      setNgReflectProperty(lView, tNode, def.inputs[publicName][0], value);
    }
  }
}
function instantiateAllDirectives(tView, lView, tNode) {
  const start = tNode.directiveStart;
  const end = tNode.directiveEnd;
  if (isComponentHost(tNode)) {
    ngDevMode && assertTNodeType(tNode, 3);
    createComponentLView(lView, tNode, tView.data[start + tNode.componentOffset]);
  }
  if (!tView.firstCreatePass) {
    getOrCreateNodeInjectorForNode(tNode, lView);
  }
  const initialInputs = tNode.initialInputs;
  for (let i = start; i < end; i++) {
    const def = tView.data[i];
    const directive = getNodeInjectable(lView, tView, i, tNode);
    attachPatchData(directive, lView);
    if (initialInputs !== null) {
      setInputsFromAttrs(lView, i - start, directive, def, tNode, initialInputs);
    }
    if (isComponentDef(def)) {
      const componentView = getComponentLViewByIndex(tNode.index, lView);
      componentView[CONTEXT] = getNodeInjectable(lView, tView, i, tNode);
    }
  }
}
function invokeDirectivesHostBindings(tView, lView, tNode) {
  const start = tNode.directiveStart;
  const end = tNode.directiveEnd;
  const elementIndex = tNode.index;
  const currentDirectiveIndex = getCurrentDirectiveIndex();
  try {
    setSelectedIndex(elementIndex);
    for (let dirIndex = start; dirIndex < end; dirIndex++) {
      const def = tView.data[dirIndex];
      const directive = lView[dirIndex];
      setCurrentDirectiveIndex(dirIndex);
      if (def.hostBindings !== null || def.hostVars !== 0 || def.hostAttrs !== null) {
        invokeHostBindingsInCreationMode(def, directive);
      }
    }
  } finally {
    setSelectedIndex(-1);
    setCurrentDirectiveIndex(currentDirectiveIndex);
  }
}
function invokeHostBindingsInCreationMode(def, directive) {
  if (def.hostBindings !== null) {
    def.hostBindings(1, directive);
  }
}
function findDirectiveDefMatches(tView, tNode) {
  ngDevMode && assertFirstCreatePass(tView);
  ngDevMode && assertTNodeType(tNode, 3 | 12);
  const registry = tView.directiveRegistry;
  let matches = null;
  if (registry) {
    for (let i = 0; i < registry.length; i++) {
      const def = registry[i];
      if (isNodeMatchingSelectorList(tNode, def.selectors, false)) {
        matches ??= [];
        if (isComponentDef(def)) {
          if (ngDevMode) {
            assertTNodeType(tNode, 2, `"${tNode.value}" tags cannot be used as component hosts. Please use a different tag to activate the ${stringify(def.type)} component.`);
            if (matches.length && isComponentDef(matches[0])) {
              throwMultipleComponentError(tNode, matches.find(isComponentDef).type, def.type);
            }
          }
          matches.unshift(def);
        } else {
          matches.push(def);
        }
      }
    }
  }
  return matches;
}
function elementAttributeInternal(tNode, lView, name, value, sanitizer, namespace) {
  if (ngDevMode) {
    assertNotSame(value, NO_CHANGE, "Incoming value should never be NO_CHANGE.");
    assertTNodeType(tNode, 2, `Attempted to set attribute \`${name}\` on a container node. Host bindings are not valid on ng-container or ng-template.`);
  }
  const element = getNativeByTNode(tNode, lView);
  setElementAttribute(lView[RENDERER], element, namespace, tNode.value, name, value, sanitizer);
}
function setElementAttribute(renderer, element, namespace, tagName, name, value, sanitizer) {
  if (value == null) {
    renderer.removeAttribute(element, name, namespace);
  } else {
    const strValue = sanitizer == null ? renderStringify(value) : sanitizer(value, tagName || "", name);
    renderer.setAttribute(element, name, strValue, namespace);
  }
}
function setInputsFromAttrs(lView, directiveIndex, instance, def, tNode, initialInputData) {
  const initialInputs = initialInputData[directiveIndex];
  if (initialInputs !== null) {
    for (let i = 0; i < initialInputs.length; i += 2) {
      const lookupName = initialInputs[i];
      const value = initialInputs[i + 1];
      writeToDirectiveInput(def, instance, lookupName, value);
      if (ngDevMode) {
        setNgReflectProperty(lView, tNode, def.inputs[lookupName][0], value);
      }
    }
  }
}
function elementLikeStartShared(tNode, lView, index, name, locateOrCreateNativeNode) {
  const adjustedIndex = HEADER_OFFSET + index;
  const tView = lView[TVIEW];
  const native = locateOrCreateNativeNode(tView, lView, tNode, name, index);
  lView[adjustedIndex] = native;
  setCurrentTNode(tNode, true);
  const isElement = tNode.type === 2;
  if (isElement) {
    setupStaticAttributes(lView[RENDERER], native, tNode);
    if (getElementDepthCount() === 0 || isDirectiveHost(tNode)) {
      attachPatchData(native, lView);
    }
    increaseElementDepthCount();
  } else {
    attachPatchData(native, lView);
  }
  if (wasLastNodeCreated() && (!isElement || !isDetachedByI18n(tNode))) {
    appendChild(tView, lView, native, tNode);
  }
  return tNode;
}
function elementLikeEndShared(tNode) {
  let currentTNode = tNode;
  if (isCurrentTNodeParent()) {
    setCurrentTNodeAsNotParent();
  } else {
    ngDevMode && assertHasParent(getCurrentTNode());
    currentTNode = currentTNode.parent;
    setCurrentTNode(currentTNode, false);
  }
  return currentTNode;
}
function storePropertyBindingMetadata(tData, tNode, propertyName, bindingIndex, ...interpolationParts) {
  if (tData[bindingIndex] === null) {
    if (!tNode.inputs?.[propertyName] && !tNode.hostDirectiveInputs?.[propertyName]) {
      const propBindingIdxs = tNode.propertyBindings || (tNode.propertyBindings = []);
      propBindingIdxs.push(bindingIndex);
      let bindingMetadata = propertyName;
      if (interpolationParts.length > 0) {
        bindingMetadata += INTERPOLATION_DELIMITER + interpolationParts.join(INTERPOLATION_DELIMITER);
      }
      tData[bindingIndex] = bindingMetadata;
    }
  }
}
function loadComponentRenderer(currentDef, tNode, lView) {
  if (currentDef === null || isComponentDef(currentDef)) {
    lView = unwrapLView(lView[tNode.index]);
  }
  return lView[RENDERER];
}
function handleUncaughtError(lView, error) {
  const injector = lView[INJECTOR];
  if (!injector) {
    return;
  }
  let errorHandler2;
  try {
    errorHandler2 = injector.get(INTERNAL_APPLICATION_ERROR_HANDLER, null);
  } catch {
    errorHandler2 = null;
  }
  errorHandler2?.(error);
}
function setAllInputsForProperty(tNode, tView, lView, publicName, value) {
  const inputs = tNode.inputs?.[publicName];
  const hostDirectiveInputs = tNode.hostDirectiveInputs?.[publicName];
  let hasMatch = false;
  if (hostDirectiveInputs) {
    for (let i = 0; i < hostDirectiveInputs.length; i += 2) {
      const index = hostDirectiveInputs[i];
      ngDevMode && assertIndexInRange(lView, index);
      const publicName2 = hostDirectiveInputs[i + 1];
      const def = tView.data[index];
      writeToDirectiveInput(def, lView[index], publicName2, value);
      hasMatch = true;
    }
  }
  if (inputs) {
    for (const index of inputs) {
      ngDevMode && assertIndexInRange(lView, index);
      const instance = lView[index];
      const def = tView.data[index];
      writeToDirectiveInput(def, instance, publicName, value);
      hasMatch = true;
    }
  }
  return hasMatch;
}
function renderComponent(hostLView, componentHostIdx) {
  ngDevMode && assertEqual(isCreationMode(hostLView), true, "Should be run in creation mode");
  const componentView = getComponentLViewByIndex(componentHostIdx, hostLView);
  const componentTView = componentView[TVIEW];
  syncViewWithBlueprint(componentTView, componentView);
  const hostRNode = componentView[HOST];
  if (hostRNode !== null && componentView[HYDRATION] === null) {
    componentView[HYDRATION] = retrieveHydrationInfo(hostRNode, componentView[INJECTOR]);
  }
  profiler(ProfilerEvent.ComponentStart);
  try {
    renderView(componentTView, componentView, componentView[CONTEXT]);
  } finally {
    profiler(ProfilerEvent.ComponentEnd, componentView[CONTEXT]);
  }
}
function syncViewWithBlueprint(tView, lView) {
  for (let i = lView.length; i < tView.blueprint.length; i++) {
    lView.push(tView.blueprint[i]);
  }
}
function renderView(tView, lView, context2) {
  ngDevMode && assertEqual(isCreationMode(lView), true, "Should be run in creation mode");
  ngDevMode && assertNotReactive(renderView.name);
  enterView(lView);
  try {
    const viewQuery = tView.viewQuery;
    if (viewQuery !== null) {
      executeViewQueryFn(1, viewQuery, context2);
    }
    const templateFn = tView.template;
    if (templateFn !== null) {
      executeTemplate(tView, lView, templateFn, 1, context2);
    }
    if (tView.firstCreatePass) {
      tView.firstCreatePass = false;
    }
    lView[QUERIES]?.finishViewCreation(tView);
    if (tView.staticContentQueries) {
      refreshContentQueries(tView, lView);
    }
    if (tView.staticViewQueries) {
      executeViewQueryFn(2, tView.viewQuery, context2);
    }
    const components = tView.components;
    if (components !== null) {
      renderChildComponents(lView, components);
    }
  } catch (error) {
    if (tView.firstCreatePass) {
      tView.incompleteFirstPass = true;
      tView.firstCreatePass = false;
    }
    throw error;
  } finally {
    lView[FLAGS] &= -5;
    leaveView();
  }
}
function renderChildComponents(hostLView, components) {
  for (let i = 0; i < components.length; i++) {
    renderComponent(hostLView, components[i]);
  }
}
function createAndRenderEmbeddedLView(declarationLView, templateTNode, context2, options) {
  const prevConsumer = setActiveConsumer(null);
  try {
    const embeddedTView = templateTNode.tView;
    ngDevMode && assertDefined(embeddedTView, "TView must be defined for a template node.");
    ngDevMode && assertTNodeForLView(templateTNode, declarationLView);
    const isSignalView = declarationLView[FLAGS] & 4096;
    const viewFlags = isSignalView ? 4096 : 16;
    const embeddedLView = createLView(declarationLView, embeddedTView, context2, viewFlags, null, templateTNode, null, null, options?.injector ?? null, options?.embeddedViewInjector ?? null, options?.dehydratedView ?? null);
    const declarationLContainer = declarationLView[templateTNode.index];
    ngDevMode && assertLContainer(declarationLContainer);
    embeddedLView[DECLARATION_LCONTAINER] = declarationLContainer;
    const declarationViewLQueries = declarationLView[QUERIES];
    if (declarationViewLQueries !== null) {
      embeddedLView[QUERIES] = declarationViewLQueries.createEmbeddedView(embeddedTView);
    }
    renderView(embeddedTView, embeddedLView, context2);
    return embeddedLView;
  } finally {
    setActiveConsumer(prevConsumer);
  }
}
function shouldAddViewToDom(tNode, dehydratedView) {
  return !dehydratedView || dehydratedView.firstChild === null || hasInSkipHydrationBlockFlag(tNode);
}
var USE_EXHAUSTIVE_CHECK_NO_CHANGES_DEFAULT = false;
var UseExhaustiveCheckNoChanges = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "exhaustive checkNoChanges" : "");
function collectNativeNodes(tView, lView, tNode, result, isProjection = false) {
  while (tNode !== null) {
    if (tNode.type === 128) {
      tNode = isProjection ? tNode.projectionNext : tNode.next;
      continue;
    }
    ngDevMode && assertTNodeType(tNode, 3 | 12 | 16 | 32);
    const lNode = lView[tNode.index];
    if (lNode !== null) {
      result.push(unwrapRNode(lNode));
    }
    if (isLContainer(lNode)) {
      collectNativeNodesInLContainer(lNode, result);
    }
    const tNodeType = tNode.type;
    if (tNodeType & 8) {
      collectNativeNodes(tView, lView, tNode.child, result);
    } else if (tNodeType & 32) {
      const nextRNode = icuContainerIterate(tNode, lView);
      let rNode;
      while (rNode = nextRNode()) {
        result.push(rNode);
      }
    } else if (tNodeType & 16) {
      const nodesInSlot = getProjectionNodes(lView, tNode);
      if (Array.isArray(nodesInSlot)) {
        result.push(...nodesInSlot);
      } else {
        const parentView = getLViewParent(lView[DECLARATION_COMPONENT_VIEW]);
        ngDevMode && assertParentView(parentView);
        collectNativeNodes(parentView[TVIEW], parentView, nodesInSlot, result, true);
      }
    }
    tNode = isProjection ? tNode.projectionNext : tNode.next;
  }
  return result;
}
function collectNativeNodesInLContainer(lContainer, result) {
  for (let i = CONTAINER_HEADER_OFFSET; i < lContainer.length; i++) {
    const lViewInAContainer = lContainer[i];
    const lViewFirstChildTNode = lViewInAContainer[TVIEW].firstChild;
    if (lViewFirstChildTNode !== null) {
      collectNativeNodes(lViewInAContainer[TVIEW], lViewInAContainer, lViewFirstChildTNode, result);
    }
  }
  if (lContainer[NATIVE] !== lContainer[HOST]) {
    result.push(lContainer[NATIVE]);
  }
}
function addAfterRenderSequencesForView(lView) {
  if (lView[AFTER_RENDER_SEQUENCES_TO_ADD] !== null) {
    for (const sequence of lView[AFTER_RENDER_SEQUENCES_TO_ADD]) {
      sequence.impl.addSequence(sequence);
    }
    lView[AFTER_RENDER_SEQUENCES_TO_ADD].length = 0;
  }
}
var freeConsumers = [];
function getOrBorrowReactiveLViewConsumer(lView) {
  return lView[REACTIVE_TEMPLATE_CONSUMER] ?? borrowReactiveLViewConsumer(lView);
}
function borrowReactiveLViewConsumer(lView) {
  const consumer = freeConsumers.pop() ?? Object.create(REACTIVE_LVIEW_CONSUMER_NODE);
  consumer.lView = lView;
  return consumer;
}
function maybeReturnReactiveLViewConsumer(consumer) {
  if (consumer.lView[REACTIVE_TEMPLATE_CONSUMER] === consumer) {
    return;
  }
  consumer.lView = null;
  freeConsumers.push(consumer);
}
var REACTIVE_LVIEW_CONSUMER_NODE = __spreadProps(__spreadValues({}, REACTIVE_NODE), {
  consumerIsAlwaysLive: true,
  kind: "template",
  consumerMarkedDirty: (node) => {
    markAncestorsForTraversal(node.lView);
  },
  consumerOnSignalRead() {
    this.lView[REACTIVE_TEMPLATE_CONSUMER] = this;
  }
});
function getOrCreateTemporaryConsumer(lView) {
  const consumer = lView[REACTIVE_TEMPLATE_CONSUMER] ?? Object.create(TEMPORARY_CONSUMER_NODE);
  consumer.lView = lView;
  return consumer;
}
var TEMPORARY_CONSUMER_NODE = __spreadProps(__spreadValues({}, REACTIVE_NODE), {
  consumerIsAlwaysLive: true,
  kind: "template",
  consumerMarkedDirty: (node) => {
    let parent = getLViewParent(node.lView);
    while (parent && !viewShouldHaveReactiveConsumer(parent[TVIEW])) {
      parent = getLViewParent(parent);
    }
    if (!parent) {
      return;
    }
    markViewForRefresh(parent);
  },
  consumerOnSignalRead() {
    this.lView[REACTIVE_TEMPLATE_CONSUMER] = this;
  }
});
function viewShouldHaveReactiveConsumer(tView) {
  return tView.type !== 2;
}
function isReactiveLViewConsumer(node) {
  return node.kind === "template";
}
function runEffectsInView(view) {
  if (view[EFFECTS] === null) {
    return;
  }
  let tryFlushEffects = true;
  while (tryFlushEffects) {
    let foundDirtyEffect = false;
    for (const effect2 of view[EFFECTS]) {
      if (!effect2.dirty) {
        continue;
      }
      foundDirtyEffect = true;
      if (effect2.zone === null || Zone.current === effect2.zone) {
        effect2.run();
      } else {
        effect2.zone.run(() => effect2.run());
      }
    }
    tryFlushEffects = foundDirtyEffect && !!(view[FLAGS] & 8192);
  }
}
var MAXIMUM_REFRESH_RERUNS$1 = 100;
function detectChangesInternal(lView, mode = 0) {
  const environment = lView[ENVIRONMENT];
  const rendererFactory = environment.rendererFactory;
  const checkNoChangesMode = !!ngDevMode && isInCheckNoChangesMode();
  if (!checkNoChangesMode) {
    rendererFactory.begin?.();
  }
  try {
    detectChangesInViewWhileDirty(lView, mode);
  } finally {
    if (!checkNoChangesMode) {
      rendererFactory.end?.();
    }
  }
}
function detectChangesInViewWhileDirty(lView, mode) {
  const lastIsRefreshingViewsValue = isRefreshingViews();
  try {
    setIsRefreshingViews(true);
    detectChangesInView(lView, mode);
    if (ngDevMode && isExhaustiveCheckNoChanges()) {
      return;
    }
    let retries = 0;
    while (requiresRefreshOrTraversal(lView)) {
      if (retries === MAXIMUM_REFRESH_RERUNS$1) {
        throw new RuntimeError(103, ngDevMode && "Infinite change detection while trying to refresh views. There may be components which each cause the other to require a refresh, causing an infinite loop.");
      }
      retries++;
      detectChangesInView(lView, 1);
    }
  } finally {
    setIsRefreshingViews(lastIsRefreshingViewsValue);
  }
}
function checkNoChangesInternal(lView, exhaustive) {
  setIsInCheckNoChangesMode(exhaustive ? CheckNoChangesMode.Exhaustive : CheckNoChangesMode.OnlyDirtyViews);
  try {
    detectChangesInternal(lView);
  } finally {
    setIsInCheckNoChangesMode(CheckNoChangesMode.Off);
  }
}
function refreshView(tView, lView, templateFn, context2) {
  ngDevMode && assertEqual(isCreationMode(lView), false, "Should be run in update mode");
  if (isDestroyed(lView)) return;
  const flags = lView[FLAGS];
  const isInCheckNoChangesPass = ngDevMode && isInCheckNoChangesMode();
  const isInExhaustiveCheckNoChangesPass = ngDevMode && isExhaustiveCheckNoChanges();
  enterView(lView);
  let returnConsumerToPool = true;
  let prevConsumer = null;
  let currentConsumer = null;
  if (!isInCheckNoChangesPass) {
    if (viewShouldHaveReactiveConsumer(tView)) {
      currentConsumer = getOrBorrowReactiveLViewConsumer(lView);
      prevConsumer = consumerBeforeComputation(currentConsumer);
    } else if (getActiveConsumer() === null) {
      returnConsumerToPool = false;
      currentConsumer = getOrCreateTemporaryConsumer(lView);
      prevConsumer = consumerBeforeComputation(currentConsumer);
    } else if (lView[REACTIVE_TEMPLATE_CONSUMER]) {
      consumerDestroy(lView[REACTIVE_TEMPLATE_CONSUMER]);
      lView[REACTIVE_TEMPLATE_CONSUMER] = null;
    }
  }
  try {
    resetPreOrderHookFlags(lView);
    setBindingIndex(tView.bindingStartIndex);
    if (templateFn !== null) {
      executeTemplate(tView, lView, templateFn, 2, context2);
    }
    const hooksInitPhaseCompleted = (flags & 3) === 3;
    if (!isInCheckNoChangesPass) {
      if (hooksInitPhaseCompleted) {
        const preOrderCheckHooks = tView.preOrderCheckHooks;
        if (preOrderCheckHooks !== null) {
          executeCheckHooks(lView, preOrderCheckHooks, null);
        }
      } else {
        const preOrderHooks = tView.preOrderHooks;
        if (preOrderHooks !== null) {
          executeInitAndCheckHooks(lView, preOrderHooks, 0, null);
        }
        incrementInitPhaseFlags(lView, 0);
      }
    }
    if (!isInExhaustiveCheckNoChangesPass) {
      markTransplantedViewsForRefresh(lView);
    }
    runEffectsInView(lView);
    detectChangesInEmbeddedViews(lView, 0);
    if (tView.contentQueries !== null) {
      refreshContentQueries(tView, lView);
    }
    if (!isInCheckNoChangesPass) {
      if (hooksInitPhaseCompleted) {
        const contentCheckHooks = tView.contentCheckHooks;
        if (contentCheckHooks !== null) {
          executeCheckHooks(lView, contentCheckHooks);
        }
      } else {
        const contentHooks = tView.contentHooks;
        if (contentHooks !== null) {
          executeInitAndCheckHooks(lView, contentHooks, 1);
        }
        incrementInitPhaseFlags(lView, 1);
      }
    }
    processHostBindingOpCodes(tView, lView);
    const components = tView.components;
    if (components !== null) {
      detectChangesInChildComponents(lView, components, 0);
    }
    const viewQuery = tView.viewQuery;
    if (viewQuery !== null) {
      executeViewQueryFn(2, viewQuery, context2);
    }
    if (!isInCheckNoChangesPass) {
      if (hooksInitPhaseCompleted) {
        const viewCheckHooks = tView.viewCheckHooks;
        if (viewCheckHooks !== null) {
          executeCheckHooks(lView, viewCheckHooks);
        }
      } else {
        const viewHooks = tView.viewHooks;
        if (viewHooks !== null) {
          executeInitAndCheckHooks(lView, viewHooks, 2);
        }
        incrementInitPhaseFlags(lView, 2);
      }
    }
    if (tView.firstUpdatePass === true) {
      tView.firstUpdatePass = false;
    }
    if (lView[EFFECTS_TO_SCHEDULE]) {
      for (const notifyEffect of lView[EFFECTS_TO_SCHEDULE]) {
        notifyEffect();
      }
      lView[EFFECTS_TO_SCHEDULE] = null;
    }
    if (!isInCheckNoChangesPass) {
      addAfterRenderSequencesForView(lView);
      lView[FLAGS] &= ~(64 | 8);
    }
  } catch (e) {
    if (!isInCheckNoChangesPass) {
      markAncestorsForTraversal(lView);
    }
    throw e;
  } finally {
    if (currentConsumer !== null) {
      consumerAfterComputation(currentConsumer, prevConsumer);
      if (returnConsumerToPool) {
        maybeReturnReactiveLViewConsumer(currentConsumer);
      }
    }
    leaveView();
  }
}
function detectChangesInEmbeddedViews(lView, mode) {
  for (let lContainer = getFirstLContainer(lView); lContainer !== null; lContainer = getNextLContainer(lContainer)) {
    for (let i = CONTAINER_HEADER_OFFSET; i < lContainer.length; i++) {
      const embeddedLView = lContainer[i];
      detectChangesInViewIfAttached(embeddedLView, mode);
    }
  }
}
function markTransplantedViewsForRefresh(lView) {
  for (let lContainer = getFirstLContainer(lView); lContainer !== null; lContainer = getNextLContainer(lContainer)) {
    if (!(lContainer[FLAGS] & 2)) continue;
    const movedViews = lContainer[MOVED_VIEWS];
    ngDevMode && assertDefined(movedViews, "Transplanted View flags set but missing MOVED_VIEWS");
    for (let i = 0; i < movedViews.length; i++) {
      const movedLView = movedViews[i];
      markViewForRefresh(movedLView);
    }
  }
}
function detectChangesInComponent(hostLView, componentHostIdx, mode) {
  ngDevMode && assertEqual(isCreationMode(hostLView), false, "Should be run in update mode");
  profiler(ProfilerEvent.ComponentStart);
  const componentView = getComponentLViewByIndex(componentHostIdx, hostLView);
  try {
    detectChangesInViewIfAttached(componentView, mode);
  } finally {
    profiler(ProfilerEvent.ComponentEnd, componentView[CONTEXT]);
  }
}
function detectChangesInViewIfAttached(lView, mode) {
  if (!viewAttachedToChangeDetector(lView)) {
    return;
  }
  detectChangesInView(lView, mode);
}
function detectChangesInView(lView, mode) {
  const isInCheckNoChangesPass = ngDevMode && isInCheckNoChangesMode();
  const tView = lView[TVIEW];
  const flags = lView[FLAGS];
  const consumer = lView[REACTIVE_TEMPLATE_CONSUMER];
  let shouldRefreshView = !!(mode === 0 && flags & 16);
  shouldRefreshView ||= !!(flags & 64 && mode === 0 && !isInCheckNoChangesPass);
  shouldRefreshView ||= !!(flags & 1024);
  shouldRefreshView ||= !!(consumer?.dirty && consumerPollProducersForChange(consumer));
  shouldRefreshView ||= !!(ngDevMode && isExhaustiveCheckNoChanges());
  if (consumer) {
    consumer.dirty = false;
  }
  lView[FLAGS] &= -9217;
  if (shouldRefreshView) {
    refreshView(tView, lView, tView.template, lView[CONTEXT]);
  } else if (flags & 8192) {
    const prevConsumer = setActiveConsumer(null);
    try {
      if (!isInCheckNoChangesPass) {
        runEffectsInView(lView);
      }
      detectChangesInEmbeddedViews(lView, 1);
      const components = tView.components;
      if (components !== null) {
        detectChangesInChildComponents(lView, components, 1);
      }
      if (!isInCheckNoChangesPass) {
        addAfterRenderSequencesForView(lView);
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
}
function detectChangesInChildComponents(hostLView, components, mode) {
  for (let i = 0; i < components.length; i++) {
    detectChangesInComponent(hostLView, components[i], mode);
  }
}
function processHostBindingOpCodes(tView, lView) {
  const hostBindingOpCodes = tView.hostBindingOpCodes;
  if (hostBindingOpCodes === null) return;
  try {
    for (let i = 0; i < hostBindingOpCodes.length; i++) {
      const opCode = hostBindingOpCodes[i];
      if (opCode < 0) {
        setSelectedIndex(~opCode);
      } else {
        const directiveIdx = opCode;
        const bindingRootIndx = hostBindingOpCodes[++i];
        const hostBindingFn = hostBindingOpCodes[++i];
        setBindingRootForHostBindings(bindingRootIndx, directiveIdx);
        const context2 = lView[directiveIdx];
        profiler(ProfilerEvent.HostBindingsUpdateStart, context2);
        try {
          hostBindingFn(2, context2);
        } finally {
          profiler(ProfilerEvent.HostBindingsUpdateEnd, context2);
        }
      }
    }
  } finally {
    setSelectedIndex(-1);
  }
}
function markViewDirty(lView, source) {
  const dirtyBitsToUse = isRefreshingViews() ? 64 : 1024 | 64;
  lView[ENVIRONMENT].changeDetectionScheduler?.notify(source);
  while (lView) {
    lView[FLAGS] |= dirtyBitsToUse;
    const parent = getLViewParent(lView);
    if (isRootView(lView) && !parent) {
      return lView;
    }
    lView = parent;
  }
  return null;
}
function createLContainer(hostNative, currentView, native, tNode) {
  ngDevMode && assertLView(currentView);
  const lContainer = [hostNative, true, 0, currentView, null, tNode, null, native, null, null];
  ngDevMode && assertEqual(lContainer.length, CONTAINER_HEADER_OFFSET, "Should allocate correct number of slots for LContainer header.");
  return lContainer;
}
function getLViewFromLContainer(lContainer, index) {
  const adjustedIndex = CONTAINER_HEADER_OFFSET + index;
  if (adjustedIndex < lContainer.length) {
    const lView = lContainer[adjustedIndex];
    ngDevMode && assertLView(lView);
    return lView;
  }
  return void 0;
}
function addLViewToLContainer(lContainer, lView, index, addToDOM = true) {
  const tView = lView[TVIEW];
  insertView(tView, lView, lContainer, index);
  if (addToDOM) {
    const beforeNode = getBeforeNodeForView(index, lContainer);
    const renderer = lView[RENDERER];
    const parentRNode = renderer.parentNode(lContainer[NATIVE]);
    if (parentRNode !== null) {
      addViewToDOM(tView, lContainer[T_HOST], renderer, lView, parentRNode, beforeNode);
    }
  }
  const hydrationInfo = lView[HYDRATION];
  if (hydrationInfo !== null && hydrationInfo.firstChild !== null) {
    hydrationInfo.firstChild = null;
  }
}
function removeLViewFromLContainer(lContainer, index) {
  const lView = detachView(lContainer, index);
  if (lView !== void 0) {
    destroyLView(lView[TVIEW], lView);
  }
  return lView;
}
function detachView(lContainer, removeIndex) {
  if (lContainer.length <= CONTAINER_HEADER_OFFSET) return;
  const indexInContainer = CONTAINER_HEADER_OFFSET + removeIndex;
  const viewToDetach = lContainer[indexInContainer];
  if (viewToDetach) {
    const declarationLContainer = viewToDetach[DECLARATION_LCONTAINER];
    if (declarationLContainer !== null && declarationLContainer !== lContainer) {
      detachMovedView(declarationLContainer, viewToDetach);
    }
    if (removeIndex > 0) {
      lContainer[indexInContainer - 1][NEXT] = viewToDetach[NEXT];
    }
    const removedLView = removeFromArray(lContainer, CONTAINER_HEADER_OFFSET + removeIndex);
    removeViewFromDOM(viewToDetach[TVIEW], viewToDetach);
    const lQueries = removedLView[QUERIES];
    if (lQueries !== null) {
      lQueries.detachView(removedLView[TVIEW]);
    }
    viewToDetach[PARENT] = null;
    viewToDetach[NEXT] = null;
    viewToDetach[FLAGS] &= -129;
  }
  return viewToDetach;
}
function insertView(tView, lView, lContainer, index) {
  ngDevMode && assertLView(lView);
  ngDevMode && assertLContainer(lContainer);
  const indexInContainer = CONTAINER_HEADER_OFFSET + index;
  const containerLength = lContainer.length;
  if (index > 0) {
    lContainer[indexInContainer - 1][NEXT] = lView;
  }
  if (index < containerLength - CONTAINER_HEADER_OFFSET) {
    lView[NEXT] = lContainer[indexInContainer];
    addToArray(lContainer, CONTAINER_HEADER_OFFSET + index, lView);
  } else {
    lContainer.push(lView);
    lView[NEXT] = null;
  }
  lView[PARENT] = lContainer;
  const declarationLContainer = lView[DECLARATION_LCONTAINER];
  if (declarationLContainer !== null && lContainer !== declarationLContainer) {
    trackMovedView(declarationLContainer, lView);
  }
  const lQueries = lView[QUERIES];
  if (lQueries !== null) {
    lQueries.insertView(tView);
  }
  updateAncestorTraversalFlagsOnAttach(lView);
  lView[FLAGS] |= 128;
}
function trackMovedView(declarationContainer, lView) {
  ngDevMode && assertDefined(lView, "LView required");
  ngDevMode && assertLContainer(declarationContainer);
  const movedViews = declarationContainer[MOVED_VIEWS];
  const parent = lView[PARENT];
  ngDevMode && assertDefined(parent, "missing parent");
  if (isLView(parent)) {
    declarationContainer[FLAGS] |= 2;
  } else {
    const insertedComponentLView = parent[PARENT][DECLARATION_COMPONENT_VIEW];
    ngDevMode && assertDefined(insertedComponentLView, "Missing insertedComponentLView");
    const declaredComponentLView = lView[DECLARATION_COMPONENT_VIEW];
    ngDevMode && assertDefined(declaredComponentLView, "Missing declaredComponentLView");
    if (declaredComponentLView !== insertedComponentLView) {
      declarationContainer[FLAGS] |= 2;
    }
  }
  if (movedViews === null) {
    declarationContainer[MOVED_VIEWS] = [lView];
  } else {
    movedViews.push(lView);
  }
}
var ViewRef = class {
  _lView;
  _cdRefInjectingView;
  _appRef = null;
  _attachedToViewContainer = false;
  exhaustive;
  get rootNodes() {
    const lView = this._lView;
    const tView = lView[TVIEW];
    return collectNativeNodes(tView, lView, tView.firstChild, []);
  }
  constructor(_lView, _cdRefInjectingView) {
    this._lView = _lView;
    this._cdRefInjectingView = _cdRefInjectingView;
  }
  get context() {
    return this._lView[CONTEXT];
  }
  set context(value) {
    if (ngDevMode) {
      console.warn("Angular: Replacing the `context` object of an `EmbeddedViewRef` is deprecated.");
    }
    this._lView[CONTEXT] = value;
  }
  get destroyed() {
    return isDestroyed(this._lView);
  }
  destroy() {
    if (this._appRef) {
      this._appRef.detachView(this);
    } else if (this._attachedToViewContainer) {
      const parent = this._lView[PARENT];
      if (isLContainer(parent)) {
        const viewRefs = parent[VIEW_REFS];
        const index = viewRefs ? viewRefs.indexOf(this) : -1;
        if (index > -1) {
          ngDevMode && assertEqual(index, parent.indexOf(this._lView) - CONTAINER_HEADER_OFFSET, "An attached view should be in the same position within its container as its ViewRef in the VIEW_REFS array.");
          detachView(parent, index);
          removeFromArray(viewRefs, index);
        }
      }
      this._attachedToViewContainer = false;
    }
    destroyLView(this._lView[TVIEW], this._lView);
  }
  onDestroy(callback) {
    storeLViewOnDestroy(this._lView, callback);
  }
  markForCheck() {
    markViewDirty(this._cdRefInjectingView || this._lView, 4);
  }
  detach() {
    this._lView[FLAGS] &= -129;
  }
  reattach() {
    updateAncestorTraversalFlagsOnAttach(this._lView);
    this._lView[FLAGS] |= 128;
  }
  detectChanges() {
    this._lView[FLAGS] |= 1024;
    detectChangesInternal(this._lView);
  }
  checkNoChanges() {
    if (ngDevMode) {
      try {
        this.exhaustive ??= this._lView[INJECTOR].get(UseExhaustiveCheckNoChanges, USE_EXHAUSTIVE_CHECK_NO_CHANGES_DEFAULT);
      } catch {
        this.exhaustive = USE_EXHAUSTIVE_CHECK_NO_CHANGES_DEFAULT;
      }
      checkNoChangesInternal(this._lView, this.exhaustive);
    }
  }
  attachToViewContainerRef() {
    if (this._appRef) {
      throw new RuntimeError(902, ngDevMode && "This view is already attached directly to the ApplicationRef!");
    }
    this._attachedToViewContainer = true;
  }
  detachFromAppRef() {
    this._appRef = null;
    const isRoot = isRootView(this._lView);
    const declarationContainer = this._lView[DECLARATION_LCONTAINER];
    if (declarationContainer !== null && !isRoot) {
      detachMovedView(declarationContainer, this._lView);
    }
    detachViewFromDOM(this._lView[TVIEW], this._lView);
  }
  attachToAppRef(appRef) {
    if (this._attachedToViewContainer) {
      throw new RuntimeError(902, ngDevMode && "This view is already attached to a ViewContainer!");
    }
    this._appRef = appRef;
    const isRoot = isRootView(this._lView);
    const declarationContainer = this._lView[DECLARATION_LCONTAINER];
    if (declarationContainer !== null && !isRoot) {
      trackMovedView(declarationContainer, this._lView);
    }
    updateAncestorTraversalFlagsOnAttach(this._lView);
  }
};
var TemplateRef = class {
  _declarationLView;
  _declarationTContainer;
  elementRef;
  static __NG_ELEMENT_ID__ = injectTemplateRef;
  constructor(_declarationLView, _declarationTContainer, elementRef) {
    this._declarationLView = _declarationLView;
    this._declarationTContainer = _declarationTContainer;
    this.elementRef = elementRef;
  }
  get ssrId() {
    return this._declarationTContainer.tView?.ssrId || null;
  }
  createEmbeddedView(context2, injector) {
    return this.createEmbeddedViewImpl(context2, injector);
  }
  createEmbeddedViewImpl(context2, injector, dehydratedView) {
    const embeddedLView = createAndRenderEmbeddedLView(this._declarationLView, this._declarationTContainer, context2, {
      embeddedViewInjector: injector,
      dehydratedView
    });
    return new ViewRef(embeddedLView);
  }
};
function injectTemplateRef() {
  return createTemplateRef(getCurrentTNode(), getLView());
}
function createTemplateRef(hostTNode, hostLView) {
  if (hostTNode.type & 4) {
    ngDevMode && assertDefined(hostTNode.tView, "TView must be allocated");
    return new TemplateRef(hostLView, hostTNode, createElementRef(hostTNode, hostLView));
  }
  return null;
}
var AT_THIS_LOCATION = "<-- AT THIS LOCATION";
var THIRD_PARTY_SCRIPTS_URL = `/guide/hydration#third-party-scripts-with-dom-manipulation`;
function getFriendlyStringFromTNodeType(tNodeType) {
  switch (tNodeType) {
    case 4:
      return "view container";
    case 2:
      return "element";
    case 8:
      return "ng-container";
    case 32:
      return "icu";
    case 64:
      return "i18n";
    case 16:
      return "projection";
    case 1:
      return "text";
    case 128:
      return "@let";
    default:
      return "<unknown>";
  }
}
function validateSiblingNodeExists(node) {
  validateNodeExists(node);
  if (!node.nextSibling) {
    const header = "During hydration Angular expected more sibling nodes to be present.\n\n";
    const actual = `Actual DOM is:

${describeDomFromNode(node)}

`;
    const footer = getHydrationErrorFooter();
    const message = header + actual + footer;
    markRNodeAsHavingHydrationMismatch(node, "", actual);
    throw new RuntimeError(-501, message);
  }
}
function validateNodeExists(node, lView = null, tNode = null) {
  if (!node) {
    const header = "During hydration, Angular expected an element to be present at this location.\n\n";
    let expected = "";
    let footer = "";
    if (lView !== null && tNode !== null) {
      expected = describeExpectedDom(lView, tNode, false);
      footer = getHydrationErrorFooter();
      markRNodeAsHavingHydrationMismatch(unwrapRNode(lView[HOST]), expected, "");
    }
    throw new RuntimeError(-502, `${header}${expected}

${footer}`);
  }
}
function stringifyTNodeAttrs(tNode) {
  const results = [];
  if (tNode.attrs) {
    for (let i = 0; i < tNode.attrs.length; ) {
      const attrName = tNode.attrs[i++];
      if (typeof attrName == "number") {
        break;
      }
      const attrValue = tNode.attrs[i++];
      results.push(`${attrName}="${shorten(attrValue)}"`);
    }
  }
  return results.join(" ");
}
var internalAttrs = /* @__PURE__ */ new Set(["ngh", "ng-version", "ng-server-context"]);
function stringifyRNodeAttrs(rNode) {
  const results = [];
  for (let i = 0; i < rNode.attributes.length; i++) {
    const attr = rNode.attributes[i];
    if (internalAttrs.has(attr.name)) continue;
    results.push(`${attr.name}="${shorten(attr.value)}"`);
  }
  return results.join(" ");
}
function describeTNode(tNode, innerContent = "\u2026") {
  switch (tNode.type) {
    case 1:
      const content = tNode.value ? `(${tNode.value})` : "";
      return `#text${content}`;
    case 2:
      const attrs = stringifyTNodeAttrs(tNode);
      const tag = tNode.value.toLowerCase();
      return `<${tag}${attrs ? " " + attrs : ""}>${innerContent}</${tag}>`;
    case 8:
      return "<!-- ng-container -->";
    case 4:
      return "<!-- container -->";
    default:
      const typeAsString = getFriendlyStringFromTNodeType(tNode.type);
      return `#node(${typeAsString})`;
  }
}
function describeRNode(rNode, innerContent = "\u2026") {
  const node = rNode;
  switch (node.nodeType) {
    case Node.ELEMENT_NODE:
      const tag = node.tagName.toLowerCase();
      const attrs = stringifyRNodeAttrs(node);
      return `<${tag}${attrs ? " " + attrs : ""}>${innerContent}</${tag}>`;
    case Node.TEXT_NODE:
      const content = node.textContent ? shorten(node.textContent) : "";
      return `#text${content ? `(${content})` : ""}`;
    case Node.COMMENT_NODE:
      return `<!-- ${shorten(node.textContent ?? "")} -->`;
    default:
      return `#node(${node.nodeType})`;
  }
}
function describeExpectedDom(lView, tNode, isViewContainerAnchor) {
  const spacer = "  ";
  let content = "";
  if (tNode.prev) {
    content += spacer + "\u2026\n";
    content += spacer + describeTNode(tNode.prev) + "\n";
  } else if (tNode.type && tNode.type & 12) {
    content += spacer + "\u2026\n";
  }
  if (isViewContainerAnchor) {
    content += spacer + describeTNode(tNode) + "\n";
    content += spacer + `<!-- container -->  ${AT_THIS_LOCATION}
`;
  } else {
    content += spacer + describeTNode(tNode) + `  ${AT_THIS_LOCATION}
`;
  }
  content += spacer + "\u2026\n";
  const parentRNode = tNode.type ? getParentRElement(lView[TVIEW], tNode, lView) : null;
  if (parentRNode) {
    content = describeRNode(parentRNode, "\n" + content);
  }
  return content;
}
function describeDomFromNode(node) {
  const spacer = "  ";
  let content = "";
  const currentNode = node;
  if (currentNode.previousSibling) {
    content += spacer + "\u2026\n";
    content += spacer + describeRNode(currentNode.previousSibling) + "\n";
  }
  content += spacer + describeRNode(currentNode) + `  ${AT_THIS_LOCATION}
`;
  if (node.nextSibling) {
    content += spacer + "\u2026\n";
  }
  if (node.parentNode) {
    content = describeRNode(currentNode.parentNode, "\n" + content);
  }
  return content;
}
function getHydrationErrorFooter(componentClassName) {
  const componentInfo = componentClassName ? `the "${componentClassName}"` : "corresponding";
  return `To fix this problem:
  * check ${componentInfo} component for hydration-related issues
  * check to see if your template has valid HTML structure
  * check if there are any third-party scripts that manipulate the DOM. More info: ${DOC_PAGE_BASE_URL}${THIRD_PARTY_SCRIPTS_URL}
  * or skip hydration by adding the \`ngSkipHydration\` attribute to its host node in a template

`;
}
function stripNewlines(input2) {
  return input2.replace(/\s+/gm, "");
}
function shorten(input2, maxLength = 50) {
  if (!input2) {
    return "";
  }
  input2 = stripNewlines(input2);
  return input2.length > maxLength ? `${input2.substring(0, maxLength - 1)}\u2026` : input2;
}
function getInsertInFrontOfRNodeWithI18n(parentTNode, currentTNode, lView) {
  const tNodeInsertBeforeIndex = currentTNode.insertBeforeIndex;
  const insertBeforeIndex = Array.isArray(tNodeInsertBeforeIndex) ? tNodeInsertBeforeIndex[0] : tNodeInsertBeforeIndex;
  if (insertBeforeIndex === null) {
    return getInsertInFrontOfRNodeWithNoI18n(parentTNode, currentTNode, lView);
  } else {
    ngDevMode && assertIndexInRange(lView, insertBeforeIndex);
    return unwrapRNode(lView[insertBeforeIndex]);
  }
}
function processI18nInsertBefore(renderer, childTNode, lView, childRNode, parentRElement) {
  const tNodeInsertBeforeIndex = childTNode.insertBeforeIndex;
  if (Array.isArray(tNodeInsertBeforeIndex)) {
    ngDevMode && assertDomNode(childRNode);
    let i18nParent = childRNode;
    let anchorRNode = null;
    if (!(childTNode.type & 3)) {
      anchorRNode = i18nParent;
      i18nParent = parentRElement;
    }
    if (i18nParent !== null && childTNode.componentOffset === -1) {
      for (let i = 1; i < tNodeInsertBeforeIndex.length; i++) {
        const i18nChild = lView[tNodeInsertBeforeIndex[i]];
        nativeInsertBefore(renderer, i18nParent, i18nChild, anchorRNode, false);
      }
    }
  }
}
function getOrCreateTNode(tView, index, type, name, attrs) {
  ngDevMode && index !== 0 && assertGreaterThanOrEqual(index, HEADER_OFFSET, "TNodes can't be in the LView header.");
  ngDevMode && assertPureTNodeType(type);
  let tNode = tView.data[index];
  if (tNode === null) {
    tNode = createTNodeAtIndex(tView, index, type, name, attrs);
    if (isInI18nBlock()) {
      tNode.flags |= 32;
    }
  } else if (tNode.type & 64) {
    tNode.type = type;
    tNode.value = name;
    tNode.attrs = attrs;
    const parent = getCurrentParentTNode();
    tNode.injectorIndex = parent === null ? -1 : parent.injectorIndex;
    ngDevMode && assertTNodeForTView(tNode, tView);
    ngDevMode && assertEqual(index, tNode.index, "Expecting same index");
  }
  setCurrentTNode(tNode, true);
  return tNode;
}
function createTNodeAtIndex(tView, index, type, name, attrs) {
  const currentTNode = getCurrentTNodePlaceholderOk();
  const isParent = isCurrentTNodeParent();
  const parent = isParent ? currentTNode : currentTNode && currentTNode.parent;
  const tNode = tView.data[index] = createTNode(tView, parent, type, index, name, attrs);
  linkTNodeInTView(tView, tNode, currentTNode, isParent);
  return tNode;
}
function linkTNodeInTView(tView, tNode, currentTNode, isParent) {
  if (tView.firstChild === null) {
    tView.firstChild = tNode;
  }
  if (currentTNode !== null) {
    if (isParent) {
      if (currentTNode.child == null && tNode.parent !== null) {
        currentTNode.child = tNode;
      }
    } else {
      if (currentTNode.next === null) {
        currentTNode.next = tNode;
        tNode.prev = currentTNode;
      }
    }
  }
}
function createTNode(tView, tParent, type, index, value, attrs) {
  ngDevMode && index !== 0 && assertGreaterThanOrEqual(index, HEADER_OFFSET, "TNodes can't be in the LView header.");
  ngDevMode && assertNotSame(attrs, void 0, "'undefined' is not valid value for 'attrs'");
  ngDevMode && tParent && assertTNodeForTView(tParent, tView);
  let injectorIndex = tParent ? tParent.injectorIndex : -1;
  let flags = 0;
  if (isInSkipHydrationBlock()) {
    flags |= 128;
  }
  const tNode = {
    type,
    index,
    insertBeforeIndex: null,
    injectorIndex,
    directiveStart: -1,
    directiveEnd: -1,
    directiveStylingLast: -1,
    componentOffset: -1,
    controlDirectiveIndex: -1,
    customControlIndex: -1,
    propertyBindings: null,
    flags,
    providerIndexes: 0,
    value,
    namespace: getNamespace(),
    attrs,
    mergedAttrs: null,
    localNames: null,
    initialInputs: null,
    inputs: null,
    hostDirectiveInputs: null,
    outputs: null,
    hostDirectiveOutputs: null,
    directiveToIndex: null,
    tView: null,
    next: null,
    prev: null,
    projectionNext: null,
    child: null,
    parent: tParent,
    projection: null,
    styles: null,
    stylesWithoutHost: null,
    residualStyles: void 0,
    classes: null,
    classesWithoutHost: null,
    residualClasses: void 0,
    classBindings: 0,
    styleBindings: 0
  };
  if (ngDevMode) {
    Object.seal(tNode);
  }
  return tNode;
}
function addTNodeAndUpdateInsertBeforeIndex(previousTNodes, newTNode) {
  ngDevMode && assertEqual(newTNode.insertBeforeIndex, null, "We expect that insertBeforeIndex is not set");
  previousTNodes.push(newTNode);
  if (previousTNodes.length > 1) {
    for (let i = previousTNodes.length - 2; i >= 0; i--) {
      const existingTNode = previousTNodes[i];
      if (!isI18nText(existingTNode)) {
        if (isNewTNodeCreatedBefore(existingTNode, newTNode) && getInsertBeforeIndex(existingTNode) === null) {
          setInsertBeforeIndex(existingTNode, newTNode.index);
        }
      }
    }
  }
}
function isI18nText(tNode) {
  return !(tNode.type & 64);
}
function isNewTNodeCreatedBefore(existingTNode, newTNode) {
  return isI18nText(newTNode) || existingTNode.index > newTNode.index;
}
function getInsertBeforeIndex(tNode) {
  const index = tNode.insertBeforeIndex;
  return Array.isArray(index) ? index[0] : index;
}
function setInsertBeforeIndex(tNode, value) {
  const index = tNode.insertBeforeIndex;
  if (Array.isArray(index)) {
    index[0] = value;
  } else {
    setI18nHandling(getInsertInFrontOfRNodeWithI18n, processI18nInsertBefore);
    tNode.insertBeforeIndex = value;
  }
}
function getTIcu(tView, index) {
  const value = tView.data[index];
  if (value === null || typeof value === "string") return null;
  if (ngDevMode && !(value.hasOwnProperty("tView") || value.hasOwnProperty("currentCaseLViewIndex"))) {
    throwError2("We expect to get 'null'|'TIcu'|'TIcuContainer', but got: " + value);
  }
  const tIcu = value.hasOwnProperty("currentCaseLViewIndex") ? value : value.value;
  ngDevMode && assertTIcu(tIcu);
  return tIcu;
}
function setTIcu(tView, index, tIcu) {
  const tNode = tView.data[index];
  ngDevMode && assertEqual(tNode === null || tNode.hasOwnProperty("tView"), true, "We expect to get 'null'|'TIcuContainer'");
  if (tNode === null) {
    tView.data[index] = tIcu;
  } else {
    ngDevMode && assertTNodeType(tNode, 32);
    tNode.value = tIcu;
  }
}
function setTNodeInsertBeforeIndex(tNode, index) {
  ngDevMode && assertTNode(tNode);
  let insertBeforeIndex = tNode.insertBeforeIndex;
  if (insertBeforeIndex === null) {
    setI18nHandling(getInsertInFrontOfRNodeWithI18n, processI18nInsertBefore);
    insertBeforeIndex = tNode.insertBeforeIndex = [null, index];
  } else {
    assertEqual(Array.isArray(insertBeforeIndex), true, "Expecting array here");
    insertBeforeIndex.push(index);
  }
}
function createTNodePlaceholder(tView, previousTNodes, index) {
  const tNode = createTNodeAtIndex(tView, index, 64, null, null);
  addTNodeAndUpdateInsertBeforeIndex(previousTNodes, tNode);
  return tNode;
}
function getCurrentICUCaseIndex(tIcu, lView) {
  const currentCase = lView[tIcu.currentCaseLViewIndex];
  return currentCase === null ? currentCase : currentCase < 0 ? ~currentCase : currentCase;
}
function getParentFromIcuCreateOpCode(mergedCode) {
  return mergedCode >>> 17;
}
function getRefFromIcuCreateOpCode(mergedCode) {
  return (mergedCode & 131070) >>> 1;
}
function getInstructionFromIcuCreateOpCode(mergedCode) {
  return mergedCode & 1;
}
function icuCreateOpCode(opCode, parentIdx, refIdx) {
  ngDevMode && assertGreaterThanOrEqual(parentIdx, 0, "Missing parent index");
  ngDevMode && assertGreaterThan(refIdx, 0, "Missing ref index");
  return opCode | parentIdx << 17 | refIdx << 1;
}
function isRootTemplateMessage(subTemplateIndex) {
  return subTemplateIndex === -1;
}
function enterIcu(state, tIcu, lView) {
  state.index = 0;
  const currentCase = getCurrentICUCaseIndex(tIcu, lView);
  if (currentCase !== null) {
    ngDevMode && assertNumberInRange(currentCase, 0, tIcu.cases.length - 1);
    state.removes = tIcu.remove[currentCase];
  } else {
    state.removes = EMPTY_ARRAY;
  }
}
function icuContainerIteratorNext(state) {
  if (state.index < state.removes.length) {
    const removeOpCode = state.removes[state.index++];
    ngDevMode && assertNumber(removeOpCode, "Expecting OpCode number");
    if (removeOpCode > 0) {
      const rNode = state.lView[removeOpCode];
      ngDevMode && assertDomNode(rNode);
      return rNode;
    } else {
      state.stack.push(state.index, state.removes);
      const tIcuIndex = ~removeOpCode;
      const tIcu = state.lView[TVIEW].data[tIcuIndex];
      ngDevMode && assertTIcu(tIcu);
      enterIcu(state, tIcu, state.lView);
      return icuContainerIteratorNext(state);
    }
  } else {
    if (state.stack.length === 0) {
      state.lView = void 0;
      return null;
    } else {
      state.removes = state.stack.pop();
      state.index = state.stack.pop();
      return icuContainerIteratorNext(state);
    }
  }
}
function loadIcuContainerVisitor() {
  const _state = {
    stack: [],
    index: -1
  };
  function icuContainerIteratorStart(tIcuContainerNode, lView) {
    _state.lView = lView;
    while (_state.stack.length) _state.stack.pop();
    ngDevMode && assertTNodeForLView(tIcuContainerNode, lView);
    enterIcu(_state, tIcuContainerNode.value, lView);
    return icuContainerIteratorNext.bind(null, _state);
  }
  return icuContainerIteratorStart;
}
var _prepareI18nBlockForHydrationImpl = () => {
};
function prepareI18nBlockForHydration(lView, index, parentTNode, subTemplateIndex) {
  _prepareI18nBlockForHydrationImpl(lView, index, parentTNode, subTemplateIndex);
}
var _claimDehydratedIcuCaseImpl = () => {
};
function claimDehydratedIcuCase(lView, icuIndex, caseIndex) {
  _claimDehydratedIcuCaseImpl(lView, icuIndex, caseIndex);
}
function cleanupI18nHydrationData(lView) {
  const hydrationInfo = lView[HYDRATION];
  if (hydrationInfo) {
    const {
      i18nNodes,
      dehydratedIcuData: dehydratedIcuDataMap
    } = hydrationInfo;
    if (i18nNodes && dehydratedIcuDataMap) {
      const renderer = lView[RENDERER];
      for (const dehydratedIcuData of dehydratedIcuDataMap.values()) {
        cleanupDehydratedIcuData(renderer, i18nNodes, dehydratedIcuData);
      }
    }
    hydrationInfo.i18nNodes = void 0;
    hydrationInfo.dehydratedIcuData = void 0;
  }
}
function cleanupDehydratedIcuData(renderer, i18nNodes, dehydratedIcuData) {
  for (const node of dehydratedIcuData.node.cases[dehydratedIcuData.case]) {
    const rNode = i18nNodes.get(node.index - HEADER_OFFSET);
    if (rNode) {
      nativeRemoveNode(renderer, rNode, false);
    }
  }
}
function removeDehydratedViews(lContainer) {
  const views = lContainer[DEHYDRATED_VIEWS] ?? [];
  const parentLView = lContainer[PARENT];
  const renderer = parentLView[RENDERER];
  const retainedViews = [];
  for (const view of views) {
    if (view.data[DEFER_BLOCK_ID] !== void 0) {
      retainedViews.push(view);
    } else {
      removeDehydratedView(view, renderer);
      ngDevMode && ngDevMode.dehydratedViewsRemoved++;
    }
  }
  lContainer[DEHYDRATED_VIEWS] = retainedViews;
}
function removeDehydratedViewList(deferBlock) {
  const {
    lContainer
  } = deferBlock;
  const dehydratedViews = lContainer[DEHYDRATED_VIEWS];
  if (dehydratedViews === null) return;
  const parentLView = lContainer[PARENT];
  const renderer = parentLView[RENDERER];
  for (const view of dehydratedViews) {
    removeDehydratedView(view, renderer);
    ngDevMode && ngDevMode.dehydratedViewsRemoved++;
  }
}
function removeDehydratedView(dehydratedView, renderer) {
  let nodesRemoved = 0;
  let currentRNode = dehydratedView.firstChild;
  if (currentRNode) {
    const numNodes = dehydratedView.data[NUM_ROOT_NODES];
    while (nodesRemoved < numNodes) {
      ngDevMode && validateSiblingNodeExists(currentRNode);
      const nextSibling = currentRNode.nextSibling;
      nativeRemoveNode(renderer, currentRNode, false);
      currentRNode = nextSibling;
      nodesRemoved++;
    }
  }
}
function cleanupLContainer(lContainer) {
  removeDehydratedViews(lContainer);
  const hostLView = lContainer[HOST];
  if (isLView(hostLView)) {
    cleanupLView(hostLView);
  }
  for (let i = CONTAINER_HEADER_OFFSET; i < lContainer.length; i++) {
    cleanupLView(lContainer[i]);
  }
}
function cleanupLView(lView) {
  cleanupI18nHydrationData(lView);
  const tView = lView[TVIEW];
  for (let i = HEADER_OFFSET; i < tView.bindingStartIndex; i++) {
    if (isLContainer(lView[i])) {
      const lContainer = lView[i];
      cleanupLContainer(lContainer);
    } else if (isLView(lView[i])) {
      cleanupLView(lView[i]);
    }
  }
}
function cleanupDehydratedViews(appRef) {
  const viewRefs = appRef._views;
  for (const viewRef of viewRefs) {
    const lNode = getLNodeForHydration(viewRef);
    if (lNode !== null && lNode[HOST] !== null) {
      if (isLView(lNode)) {
        cleanupLView(lNode);
      } else {
        cleanupLContainer(lNode);
      }
      ngDevMode && ngDevMode.dehydratedViewsCleanupRuns++;
    }
  }
}
function cleanupHydratedDeferBlocks(deferBlock, hydratedBlocks, registry, appRef) {
  if (deferBlock !== null) {
    registry.cleanup(hydratedBlocks);
    cleanupLContainer(deferBlock.lContainer);
    cleanupDehydratedViews(appRef);
  }
}
var _findMatchingDehydratedViewImpl = () => null;
var _findAndReconcileMatchingDehydratedViewsImpl = () => null;
function findMatchingDehydratedView(lContainer, template) {
  return _findMatchingDehydratedViewImpl(lContainer, template);
}
function findAndReconcileMatchingDehydratedViews(lContainer, templateTNode, hostLView) {
  return _findAndReconcileMatchingDehydratedViewsImpl(lContainer, templateTNode, hostLView);
}
var ComponentRef$1 = class ComponentRef {
};
var ComponentFactory$1 = class ComponentFactory {
};
var _NullComponentFactoryResolver = class {
  resolveComponentFactory(component) {
    throw new RuntimeError(917, typeof ngDevMode !== "undefined" && ngDevMode && `No component factory found for ${stringify(component)}.`);
  }
};
var ComponentFactoryResolver$1 = class ComponentFactoryResolver {
  static NULL = new _NullComponentFactoryResolver();
};
var RendererFactory2 = class {
};
var Renderer2 = class {
  destroyNode = null;
  static __NG_ELEMENT_ID__ = () => injectRenderer2();
};
function injectRenderer2() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  const nodeAtIndex = getComponentLViewByIndex(tNode.index, lView);
  return (isLView(nodeAtIndex) ? nodeAtIndex : lView)[RENDERER];
}
var Sanitizer = class _Sanitizer {
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Sanitizer,
    providedIn: "root",
    factory: () => null
  });
};
function isModuleWithProviders(value) {
  return value.ngModule !== void 0;
}
function isNgModule(value) {
  return !!getNgModuleDef(value);
}
function isPipe(value) {
  return !!getPipeDef(value);
}
function isDirective(value) {
  return !!getDirectiveDef(value);
}
function isComponent(value) {
  return !!getComponentDef(value);
}
function getDependencyTypeForError(type) {
  if (getComponentDef(type)) return "component";
  if (getDirectiveDef(type)) return "directive";
  if (getPipeDef(type)) return "pipe";
  return "type";
}
function verifyStandaloneImport(depType, importingType) {
  if (isForwardRef(depType)) {
    depType = resolveForwardRef(depType);
    if (!depType) {
      throw new Error(`Expected forwardRef function, imported from "${stringifyForError(importingType)}", to return a standalone entity or NgModule but got "${stringifyForError(depType) || depType}".`);
    }
  }
  if (getNgModuleDef(depType) == null) {
    const def = getComponentDef(depType) || getDirectiveDef(depType) || getPipeDef(depType);
    if (def != null) {
      if (!def.standalone) {
        const type = getDependencyTypeForError(depType);
        throw new Error(`The "${stringifyForError(depType)}" ${type}, imported from "${stringifyForError(importingType)}", is not standalone. Does the ${type} have the standalone: false flag?`);
      }
    } else {
      if (isModuleWithProviders(depType)) {
        throw new Error(`A module with providers was imported from "${stringifyForError(importingType)}". Modules with providers are not supported in standalone components imports.`);
      } else {
        throw new Error(`The "${stringifyForError(depType)}" type, imported from "${stringifyForError(importingType)}", must be a standalone component / directive / pipe or an NgModule. Did you forget to add the required @Component / @Directive / @Pipe or @NgModule annotation?`);
      }
    }
  }
}
var DepsTracker = class {
  ownerNgModule = /* @__PURE__ */ new WeakMap();
  ngModulesWithSomeUnresolvedDecls = /* @__PURE__ */ new Set();
  ngModulesScopeCache = /* @__PURE__ */ new WeakMap();
  standaloneComponentsScopeCache = /* @__PURE__ */ new WeakMap();
  resolveNgModulesDecls() {
    if (this.ngModulesWithSomeUnresolvedDecls.size === 0) {
      return;
    }
    for (const moduleType of this.ngModulesWithSomeUnresolvedDecls) {
      const def = getNgModuleDef(moduleType);
      if (def?.declarations) {
        for (const decl of maybeUnwrapFn(def.declarations)) {
          if (isComponent(decl)) {
            this.ownerNgModule.set(decl, moduleType);
          }
        }
      }
    }
    this.ngModulesWithSomeUnresolvedDecls.clear();
  }
  getComponentDependencies(type, rawImports) {
    this.resolveNgModulesDecls();
    const def = getComponentDef(type);
    if (def === null) {
      throw new Error(`Attempting to get component dependencies for a type that is not a component: ${type}`);
    }
    if (def.standalone) {
      const scope = this.getStandaloneComponentScope(type, rawImports);
      if (scope.compilation.isPoisoned) {
        return {
          dependencies: []
        };
      }
      return {
        dependencies: [...scope.compilation.directives, ...scope.compilation.pipes, ...scope.compilation.ngModules]
      };
    } else {
      if (!this.ownerNgModule.has(type)) {
        return {
          dependencies: []
        };
      }
      const scope = this.getNgModuleScope(this.ownerNgModule.get(type));
      if (scope.compilation.isPoisoned) {
        return {
          dependencies: []
        };
      }
      return {
        dependencies: [...scope.compilation.directives, ...scope.compilation.pipes]
      };
    }
  }
  registerNgModule(type, scopeInfo) {
    if (!isNgModule(type)) {
      throw new Error(`Attempting to register a Type which is not NgModule as NgModule: ${type}`);
    }
    this.ngModulesWithSomeUnresolvedDecls.add(type);
  }
  clearScopeCacheFor(type) {
    this.ngModulesScopeCache.delete(type);
    this.standaloneComponentsScopeCache.delete(type);
  }
  getNgModuleScope(type) {
    if (this.ngModulesScopeCache.has(type)) {
      return this.ngModulesScopeCache.get(type);
    }
    const scope = this.computeNgModuleScope(type);
    this.ngModulesScopeCache.set(type, scope);
    return scope;
  }
  computeNgModuleScope(type) {
    const def = getNgModuleDefOrThrow(type);
    const scope = {
      exported: {
        directives: /* @__PURE__ */ new Set(),
        pipes: /* @__PURE__ */ new Set()
      },
      compilation: {
        directives: /* @__PURE__ */ new Set(),
        pipes: /* @__PURE__ */ new Set()
      }
    };
    for (const imported of maybeUnwrapFn(def.imports)) {
      if (isNgModule(imported)) {
        const importedScope = this.getNgModuleScope(imported);
        addSet(importedScope.exported.directives, scope.compilation.directives);
        addSet(importedScope.exported.pipes, scope.compilation.pipes);
      } else if (isStandalone(imported)) {
        if (isDirective(imported) || isComponent(imported)) {
          scope.compilation.directives.add(imported);
        } else if (isPipe(imported)) {
          scope.compilation.pipes.add(imported);
        } else {
          throw new RuntimeError(980, "The standalone imported type is neither a component nor a directive nor a pipe");
        }
      } else {
        scope.compilation.isPoisoned = true;
        break;
      }
    }
    if (!scope.compilation.isPoisoned) {
      for (const decl of maybeUnwrapFn(def.declarations)) {
        if (isNgModule(decl) || isStandalone(decl)) {
          scope.compilation.isPoisoned = true;
          break;
        }
        if (isPipe(decl)) {
          scope.compilation.pipes.add(decl);
        } else {
          scope.compilation.directives.add(decl);
        }
      }
    }
    for (const exported of maybeUnwrapFn(def.exports)) {
      if (isNgModule(exported)) {
        const exportedScope = this.getNgModuleScope(exported);
        addSet(exportedScope.exported.directives, scope.exported.directives);
        addSet(exportedScope.exported.pipes, scope.exported.pipes);
        addSet(exportedScope.exported.directives, scope.compilation.directives);
        addSet(exportedScope.exported.pipes, scope.compilation.pipes);
      } else if (isPipe(exported)) {
        scope.exported.pipes.add(exported);
      } else {
        scope.exported.directives.add(exported);
      }
    }
    return scope;
  }
  getStandaloneComponentScope(type, rawImports) {
    if (this.standaloneComponentsScopeCache.has(type)) {
      return this.standaloneComponentsScopeCache.get(type);
    }
    const ans = this.computeStandaloneComponentScope(type, rawImports);
    this.standaloneComponentsScopeCache.set(type, ans);
    return ans;
  }
  computeStandaloneComponentScope(type, rawImports) {
    const ans = {
      compilation: {
        directives: /* @__PURE__ */ new Set([type]),
        pipes: /* @__PURE__ */ new Set(),
        ngModules: /* @__PURE__ */ new Set()
      }
    };
    for (const rawImport of flatten(rawImports ?? [])) {
      const imported = resolveForwardRef(rawImport);
      try {
        verifyStandaloneImport(imported, type);
      } catch (e) {
        ans.compilation.isPoisoned = true;
        return ans;
      }
      if (isNgModule(imported)) {
        ans.compilation.ngModules.add(imported);
        const importedScope = this.getNgModuleScope(imported);
        if (importedScope.exported.isPoisoned) {
          ans.compilation.isPoisoned = true;
          return ans;
        }
        addSet(importedScope.exported.directives, ans.compilation.directives);
        addSet(importedScope.exported.pipes, ans.compilation.pipes);
      } else if (isPipe(imported)) {
        ans.compilation.pipes.add(imported);
      } else if (isDirective(imported) || isComponent(imported)) {
        ans.compilation.directives.add(imported);
      } else {
        ans.compilation.isPoisoned = true;
        return ans;
      }
    }
    return ans;
  }
  isOrphanComponent(cmp) {
    const def = getComponentDef(cmp);
    if (!def || def.standalone) {
      return false;
    }
    this.resolveNgModulesDecls();
    return !this.ownerNgModule.has(cmp);
  }
};
function addSet(sourceSet, targetSet) {
  for (const m of sourceSet) {
    targetSet.add(m);
  }
}
var depsTracker = new DepsTracker();
var NOT_FOUND_CHECK_ONLY_ELEMENT_INJECTOR = {};
var ChainedInjector = class {
  injector;
  parentInjector;
  constructor(injector, parentInjector) {
    this.injector = injector;
    this.parentInjector = parentInjector;
  }
  get(token, notFoundValue, options) {
    const value = this.injector.get(token, NOT_FOUND_CHECK_ONLY_ELEMENT_INJECTOR, options);
    if (value !== NOT_FOUND_CHECK_ONLY_ELEMENT_INJECTOR || notFoundValue === NOT_FOUND_CHECK_ONLY_ELEMENT_INJECTOR) {
      return value;
    }
    return this.parentInjector.get(token, notFoundValue, options);
  }
};
function computeStaticStyling(tNode, attrs, writeToHost) {
  ngDevMode && assertFirstCreatePass(getTView(), "Expecting to be called in first template pass only");
  let styles = writeToHost ? tNode.styles : null;
  let classes = writeToHost ? tNode.classes : null;
  let mode = 0;
  if (attrs !== null) {
    for (let i = 0; i < attrs.length; i++) {
      const value = attrs[i];
      if (typeof value === "number") {
        mode = value;
      } else if (mode == 1) {
        classes = concatStringsWithSpace(classes, value);
      } else if (mode == 2) {
        const style = value;
        const styleValue = attrs[++i];
        styles = concatStringsWithSpace(styles, style + ": " + styleValue + ";");
      }
    }
  }
  writeToHost ? tNode.styles = styles : tNode.stylesWithoutHost = styles;
  writeToHost ? tNode.classes = classes : tNode.classesWithoutHost = classes;
}
function \u0275\u0275directiveInject(token, flags = 0) {
  const lView = getLView();
  if (lView === null) {
    ngDevMode && assertInjectImplementationNotEqual(\u0275\u0275directiveInject);
    return \u0275\u0275inject(token, flags);
  }
  const tNode = getCurrentTNode();
  const value = getOrCreateInjectable(tNode, lView, resolveForwardRef(token), flags);
  ngDevMode && emitInjectEvent(token, value, flags);
  return value;
}
function \u0275\u0275invalidFactory() {
  const msg = ngDevMode ? `This constructor was not compatible with Dependency Injection.` : "invalid";
  throw new Error(msg);
}
function resolveDirectives(tView, lView, tNode, localRefs, directiveMatcher) {
  ngDevMode && assertFirstCreatePass(tView);
  const exportsMap = localRefs === null ? null : {
    "": -1
  };
  const matchedDirectiveDefs = directiveMatcher(tView, tNode);
  if (matchedDirectiveDefs !== null) {
    let directiveDefs = matchedDirectiveDefs;
    let hostDirectiveDefs = null;
    let hostDirectiveRanges = null;
    for (const def of matchedDirectiveDefs) {
      if (def.resolveHostDirectives !== null) {
        [directiveDefs, hostDirectiveDefs, hostDirectiveRanges] = def.resolveHostDirectives(matchedDirectiveDefs);
        break;
      }
    }
    ngDevMode && assertNoDuplicateDirectives(directiveDefs);
    initializeDirectives(tView, lView, tNode, directiveDefs, exportsMap, hostDirectiveDefs, hostDirectiveRanges);
  }
  if (exportsMap !== null && localRefs !== null) {
    cacheMatchingLocalNames(tNode, localRefs, exportsMap);
  }
}
function cacheMatchingLocalNames(tNode, localRefs, exportsMap) {
  const localNames = tNode.localNames = [];
  for (let i = 0; i < localRefs.length; i += 2) {
    const index = exportsMap[localRefs[i + 1]];
    if (index == null) throw new RuntimeError(-301, ngDevMode && `Export of name '${localRefs[i + 1]}' not found!`);
    localNames.push(localRefs[i], index);
  }
}
function markAsComponentHost(tView, hostTNode, componentOffset) {
  ngDevMode && assertFirstCreatePass(tView);
  ngDevMode && assertGreaterThan(componentOffset, -1, "componentOffset must be great than -1");
  hostTNode.componentOffset = componentOffset;
  (tView.components ??= []).push(hostTNode.index);
}
function initializeDirectives(tView, lView, tNode, directives, exportsMap, hostDirectiveDefs, hostDirectiveRanges) {
  ngDevMode && assertFirstCreatePass(tView);
  const directivesLength = directives.length;
  let componentDef = null;
  for (let i = 0; i < directivesLength; i++) {
    const def = directives[i];
    if (componentDef === null && isComponentDef(def)) {
      componentDef = def;
      markAsComponentHost(tView, tNode, i);
    }
    diPublicInInjector(getOrCreateNodeInjectorForNode(tNode, lView), tView, def.type);
  }
  initTNodeFlags(tNode, tView.data.length, directivesLength);
  if (componentDef?.viewProvidersResolver) {
    componentDef.viewProvidersResolver(componentDef);
  }
  for (let i = 0; i < directivesLength; i++) {
    const def = directives[i];
    if (def.providersResolver) {
      def.providersResolver(def);
    }
  }
  let preOrderHooksFound = false;
  let preOrderCheckHooksFound = false;
  let directiveIdx = allocExpando(tView, lView, directivesLength, null);
  ngDevMode && assertSame(directiveIdx, tNode.directiveStart, "TNode.directiveStart should point to just allocated space");
  if (directivesLength > 0) {
    tNode.directiveToIndex = /* @__PURE__ */ new Map();
  }
  for (let i = 0; i < directivesLength; i++) {
    const def = directives[i];
    tNode.mergedAttrs = mergeHostAttrs(tNode.mergedAttrs, def.hostAttrs);
    configureViewWithDirective(tView, tNode, lView, directiveIdx, def);
    saveNameToExportMap(directiveIdx, def, exportsMap);
    if (hostDirectiveRanges !== null && hostDirectiveRanges.has(def)) {
      const [start, end] = hostDirectiveRanges.get(def);
      tNode.directiveToIndex.set(def.type, [directiveIdx, start + tNode.directiveStart, end + tNode.directiveStart]);
    } else if (hostDirectiveDefs === null || !hostDirectiveDefs.has(def)) {
      tNode.directiveToIndex.set(def.type, directiveIdx);
    }
    if (def.contentQueries !== null) tNode.flags |= 4;
    if (def.hostBindings !== null || def.hostAttrs !== null || def.hostVars !== 0) tNode.flags |= 64;
    const lifeCycleHooks = def.type.prototype;
    if (!preOrderHooksFound && (lifeCycleHooks.ngOnChanges || lifeCycleHooks.ngOnInit || lifeCycleHooks.ngDoCheck)) {
      (tView.preOrderHooks ??= []).push(tNode.index);
      preOrderHooksFound = true;
    }
    if (!preOrderCheckHooksFound && (lifeCycleHooks.ngOnChanges || lifeCycleHooks.ngDoCheck)) {
      (tView.preOrderCheckHooks ??= []).push(tNode.index);
      preOrderCheckHooksFound = true;
    }
    directiveIdx++;
  }
  initializeInputAndOutputAliases(tView, tNode, hostDirectiveDefs);
}
function initializeInputAndOutputAliases(tView, tNode, hostDirectiveDefs) {
  ngDevMode && assertFirstCreatePass(tView);
  for (let index = tNode.directiveStart; index < tNode.directiveEnd; index++) {
    const directiveDef = tView.data[index];
    if (hostDirectiveDefs === null || !hostDirectiveDefs.has(directiveDef)) {
      setupSelectorMatchedInputsOrOutputs(0, tNode, directiveDef, index);
      setupSelectorMatchedInputsOrOutputs(1, tNode, directiveDef, index);
      setupInitialInputs(tNode, index, false);
    } else {
      const hostDirectiveDef = hostDirectiveDefs.get(directiveDef);
      setupHostDirectiveInputsOrOutputs(0, tNode, hostDirectiveDef, index);
      setupHostDirectiveInputsOrOutputs(1, tNode, hostDirectiveDef, index);
      setupInitialInputs(tNode, index, true);
    }
  }
}
function setupSelectorMatchedInputsOrOutputs(mode, tNode, def, directiveIndex) {
  const aliasMap = mode === 0 ? def.inputs : def.outputs;
  for (const publicName in aliasMap) {
    if (aliasMap.hasOwnProperty(publicName)) {
      let bindings;
      if (mode === 0) {
        bindings = tNode.inputs ??= {};
      } else {
        bindings = tNode.outputs ??= {};
      }
      bindings[publicName] ??= [];
      bindings[publicName].push(directiveIndex);
      setShadowStylingInputFlags(tNode, publicName);
    }
  }
}
function setupHostDirectiveInputsOrOutputs(mode, tNode, config2, directiveIndex) {
  const aliasMap = mode === 0 ? config2.inputs : config2.outputs;
  for (const initialName in aliasMap) {
    if (aliasMap.hasOwnProperty(initialName)) {
      const publicName = aliasMap[initialName];
      let bindings;
      if (mode === 0) {
        bindings = tNode.hostDirectiveInputs ??= {};
      } else {
        bindings = tNode.hostDirectiveOutputs ??= {};
      }
      bindings[publicName] ??= [];
      bindings[publicName].push(directiveIndex, initialName);
      setShadowStylingInputFlags(tNode, publicName);
    }
  }
}
function setShadowStylingInputFlags(tNode, publicName) {
  if (publicName === "class") {
    tNode.flags |= 8;
  } else if (publicName === "style") {
    tNode.flags |= 16;
  }
}
function setupInitialInputs(tNode, directiveIndex, isHostDirective) {
  const {
    attrs,
    inputs,
    hostDirectiveInputs
  } = tNode;
  if (attrs === null || !isHostDirective && inputs === null || isHostDirective && hostDirectiveInputs === null || isInlineTemplate(tNode)) {
    tNode.initialInputs ??= [];
    tNode.initialInputs.push(null);
    return;
  }
  let inputsToStore = null;
  let i = 0;
  while (i < attrs.length) {
    const attrName = attrs[i];
    if (attrName === 0) {
      i += 4;
      continue;
    } else if (attrName === 5) {
      i += 2;
      continue;
    } else if (typeof attrName === "number") {
      break;
    }
    if (!isHostDirective && inputs.hasOwnProperty(attrName)) {
      const inputConfig = inputs[attrName];
      for (const index of inputConfig) {
        if (index === directiveIndex) {
          inputsToStore ??= [];
          inputsToStore.push(attrName, attrs[i + 1]);
          break;
        }
      }
    } else if (isHostDirective && hostDirectiveInputs.hasOwnProperty(attrName)) {
      const config2 = hostDirectiveInputs[attrName];
      for (let j = 0; j < config2.length; j += 2) {
        if (config2[j] === directiveIndex) {
          inputsToStore ??= [];
          inputsToStore.push(config2[j + 1], attrs[i + 1]);
          break;
        }
      }
    }
    i += 2;
  }
  tNode.initialInputs ??= [];
  tNode.initialInputs.push(inputsToStore);
}
function configureViewWithDirective(tView, tNode, lView, directiveIndex, def) {
  ngDevMode && assertGreaterThanOrEqual(directiveIndex, HEADER_OFFSET, "Must be in Expando section");
  tView.data[directiveIndex] = def;
  const directiveFactory = def.factory || (def.factory = getFactoryDef(def.type, true));
  const nodeInjectorFactory = new NodeInjectorFactory(directiveFactory, isComponentDef(def), \u0275\u0275directiveInject, ngDevMode ? def.type.name : null);
  tView.blueprint[directiveIndex] = nodeInjectorFactory;
  lView[directiveIndex] = nodeInjectorFactory;
  registerHostBindingOpCodes(tView, tNode, directiveIndex, allocExpando(tView, lView, def.hostVars, NO_CHANGE), def);
}
function registerHostBindingOpCodes(tView, tNode, directiveIdx, directiveVarsIdx, def) {
  ngDevMode && assertFirstCreatePass(tView);
  const hostBindings = def.hostBindings;
  if (hostBindings) {
    let hostBindingOpCodes = tView.hostBindingOpCodes;
    if (hostBindingOpCodes === null) {
      hostBindingOpCodes = tView.hostBindingOpCodes = [];
    }
    const elementIndx = ~tNode.index;
    if (lastSelectedElementIdx(hostBindingOpCodes) != elementIndx) {
      hostBindingOpCodes.push(elementIndx);
    }
    hostBindingOpCodes.push(directiveIdx, directiveVarsIdx, hostBindings);
  }
}
function lastSelectedElementIdx(hostBindingOpCodes) {
  let i = hostBindingOpCodes.length;
  while (i > 0) {
    const value = hostBindingOpCodes[--i];
    if (typeof value === "number" && value < 0) {
      return value;
    }
  }
  return 0;
}
function saveNameToExportMap(directiveIdx, def, exportsMap) {
  if (exportsMap) {
    if (def.exportAs) {
      for (let i = 0; i < def.exportAs.length; i++) {
        exportsMap[def.exportAs[i]] = directiveIdx;
      }
    }
    if (isComponentDef(def)) exportsMap[""] = directiveIdx;
  }
}
function initTNodeFlags(tNode, index, numberOfDirectives) {
  ngDevMode && assertNotEqual(numberOfDirectives, tNode.directiveEnd - tNode.directiveStart, "Reached the max number of directives");
  tNode.flags |= 1;
  tNode.directiveStart = index;
  tNode.directiveEnd = index + numberOfDirectives;
  tNode.providerIndexes = index;
}
function assertNoDuplicateDirectives(directives) {
  if (directives.length < 2) {
    return;
  }
  const seenDirectives = /* @__PURE__ */ new Set();
  for (const current of directives) {
    if (seenDirectives.has(current)) {
      throw new RuntimeError(309, `Directive ${current.type.name} matches multiple times on the same element. Directives can only match an element once.`);
    }
    seenDirectives.add(current);
  }
}
function directiveHostFirstCreatePass(index, lView, type, name, directiveMatcher, bindingsEnabled, attrsIndex, localRefsIndex) {
  const tView = lView[TVIEW];
  ngDevMode && assertFirstCreatePass(tView);
  const tViewConsts = tView.consts;
  const attrs = getConstant(tViewConsts, attrsIndex);
  const tNode = getOrCreateTNode(tView, index, type, name, attrs);
  if (bindingsEnabled) {
    resolveDirectives(tView, lView, tNode, getConstant(tViewConsts, localRefsIndex), directiveMatcher);
  }
  tNode.mergedAttrs = mergeHostAttrs(tNode.mergedAttrs, tNode.attrs);
  if (tNode.attrs !== null) {
    computeStaticStyling(tNode, tNode.attrs, false);
  }
  if (tNode.mergedAttrs !== null) {
    computeStaticStyling(tNode, tNode.mergedAttrs, true);
  }
  if (tView.queries !== null) {
    tView.queries.elementStart(tView, tNode);
  }
  return tNode;
}
function directiveHostEndFirstCreatePass(tView, tNode) {
  ngDevMode && assertFirstCreatePass(tView);
  registerPostOrderHooks(tView, tNode);
  if (isContentQueryHost(tNode)) {
    tView.queries.elementEnd(tNode);
  }
}
function domOnlyFirstCreatePass(index, tView, type, name, attrsIndex, localRefsIndex) {
  ngDevMode && assertFirstCreatePass(tView);
  const tViewConsts = tView.consts;
  const attrs = getConstant(tViewConsts, attrsIndex);
  const tNode = getOrCreateTNode(tView, index, type, name, attrs);
  tNode.mergedAttrs = mergeHostAttrs(tNode.mergedAttrs, tNode.attrs);
  if (localRefsIndex != null) {
    const refs = getConstant(tViewConsts, localRefsIndex);
    tNode.localNames = [];
    for (let i = 0; i < refs.length; i += 2) {
      tNode.localNames.push(refs[i], -1);
    }
  }
  if (tNode.attrs !== null) {
    computeStaticStyling(tNode, tNode.attrs, false);
  }
  if (tNode.mergedAttrs !== null) {
    computeStaticStyling(tNode, tNode.mergedAttrs, true);
  }
  if (tView.queries !== null) {
    tView.queries.elementStart(tView, tNode);
  }
  return tNode;
}
function isListLikeIterable(obj) {
  if (!isJsObject(obj)) return false;
  return Array.isArray(obj) || !(obj instanceof Map) && Symbol.iterator in obj;
}
function areIterablesEqual(a, b, comparator) {
  const iterator1 = a[Symbol.iterator]();
  const iterator2 = b[Symbol.iterator]();
  while (true) {
    const item1 = iterator1.next();
    const item2 = iterator2.next();
    if (item1.done && item2.done) return true;
    if (item1.done || item2.done) return false;
    if (!comparator(item1.value, item2.value)) return false;
  }
}
function iterateListLike(obj, fn) {
  if (Array.isArray(obj)) {
    for (let i = 0; i < obj.length; i++) {
      fn(obj[i]);
    }
  } else {
    const iterator2 = obj[Symbol.iterator]();
    let item;
    while (!(item = iterator2.next()).done) {
      fn(item.value);
    }
  }
}
function isJsObject(o) {
  return o !== null && (typeof o === "function" || typeof o === "object");
}
function devModeEqual(a, b) {
  const isListLikeIterableA = isListLikeIterable(a);
  const isListLikeIterableB = isListLikeIterable(b);
  if (isListLikeIterableA && isListLikeIterableB) {
    return areIterablesEqual(a, b, devModeEqual);
  } else {
    const isAObject = a && (typeof a === "object" || typeof a === "function");
    const isBObject = b && (typeof b === "object" || typeof b === "function");
    if (!isListLikeIterableA && isAObject && !isListLikeIterableB && isBObject) {
      return true;
    } else {
      return Object.is(a, b);
    }
  }
}
function updateBinding(lView, bindingIndex, value) {
  return lView[bindingIndex] = value;
}
function getBinding(lView, bindingIndex) {
  ngDevMode && assertIndexInRange(lView, bindingIndex);
  ngDevMode && assertNotSame(lView[bindingIndex], NO_CHANGE, "Stored value should never be NO_CHANGE.");
  return lView[bindingIndex];
}
function bindingUpdated(lView, bindingIndex, value) {
  ngDevMode && assertLessThan(bindingIndex, lView.length, `Slot should have been initialized to NO_CHANGE`);
  if (value === NO_CHANGE) {
    return false;
  }
  const oldValue = lView[bindingIndex];
  if (Object.is(oldValue, value)) {
    return false;
  } else {
    if (ngDevMode && isInCheckNoChangesMode()) {
      const oldValueToCompare = oldValue !== NO_CHANGE ? oldValue : void 0;
      if (!devModeEqual(oldValueToCompare, value)) {
        const details = getExpressionChangedErrorDetails(lView, bindingIndex, oldValueToCompare, value);
        throwErrorIfNoChangesMode(oldValue === NO_CHANGE, details.oldValue, details.newValue, details.propName, lView);
      }
      return false;
    }
    lView[bindingIndex] = value;
    return true;
  }
}
function bindingUpdated2(lView, bindingIndex, exp1, exp2) {
  const different = bindingUpdated(lView, bindingIndex, exp1);
  return bindingUpdated(lView, bindingIndex + 1, exp2) || different;
}
function bindingUpdated3(lView, bindingIndex, exp1, exp2, exp3) {
  const different = bindingUpdated2(lView, bindingIndex, exp1, exp2);
  return bindingUpdated(lView, bindingIndex + 2, exp3) || different;
}
function bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4) {
  const different = bindingUpdated2(lView, bindingIndex, exp1, exp2);
  return bindingUpdated2(lView, bindingIndex + 2, exp3, exp4) || different;
}
function wrapListener(tNode, lView, listenerFn) {
  return function wrapListenerIn_markDirtyAndPreventDefault(event) {
    const nativeEl = wrapListenerIn_markDirtyAndPreventDefault.__ngNativeEl__;
    if (nativeEl !== void 0) {
      markEventHandledForElement(event, nativeEl);
    }
    const startView = isComponentHost(tNode) ? getComponentLViewByIndex(tNode.index, lView) : lView;
    markViewDirty(startView, 5);
    const context2 = lView[CONTEXT];
    let result = executeListenerWithErrorHandling(lView, context2, listenerFn, event);
    let nextListenerFn = wrapListenerIn_markDirtyAndPreventDefault.__ngNextListenerFn__;
    while (nextListenerFn) {
      result = executeListenerWithErrorHandling(lView, context2, nextListenerFn, event) && result;
      nextListenerFn = nextListenerFn.__ngNextListenerFn__;
    }
    return result;
  };
}
function executeListenerWithErrorHandling(lView, context2, listenerFn, e) {
  const prevConsumer = setActiveConsumer(null);
  try {
    profiler(ProfilerEvent.OutputStart, context2, listenerFn);
    return listenerFn(e) !== false;
  } catch (error) {
    handleUncaughtError(lView, error);
    return false;
  } finally {
    profiler(ProfilerEvent.OutputEnd, context2, listenerFn);
    setActiveConsumer(prevConsumer);
  }
}
function listenToDomEvent(tNode, tView, lView, eventTargetResolver, renderer, eventName, originalListener, wrappedListener) {
  ngDevMode && assertNotSame(wrappedListener, originalListener, "Expected wrapped and original listeners to be different.");
  const isTNodeDirectiveHost = isDirectiveHost(tNode);
  let hasCoalesced = false;
  let existingListener = null;
  if (!eventTargetResolver && isTNodeDirectiveHost) {
    existingListener = findExistingListener(tView, lView, eventName, tNode.index);
  }
  if (existingListener !== null) {
    const lastListenerFn = existingListener.__ngLastListenerFn__ || existingListener;
    lastListenerFn.__ngNextListenerFn__ = originalListener;
    existingListener.__ngLastListenerFn__ = originalListener;
    hasCoalesced = true;
  } else {
    const native = getNativeByTNode(tNode, lView);
    const target = eventTargetResolver ? eventTargetResolver(native) : native;
    stashEventListenerImpl(lView, target, eventName, wrappedListener);
    if (!eventTargetResolver) {
      wrappedListener.__ngNativeEl__ = native;
    }
    const cleanupFn = renderer.listen(target, eventName, wrappedListener);
    if (!isAnimationEventType(eventName)) {
      const idxOrTargetGetter = eventTargetResolver ? (_lView) => eventTargetResolver(unwrapRNode(_lView[tNode.index])) : tNode.index;
      storeListenerCleanup(idxOrTargetGetter, tView, lView, eventName, wrappedListener, cleanupFn, false);
    }
  }
  return hasCoalesced;
}
function isAnimationEventType(eventName) {
  return eventName.startsWith("animation") || eventName.startsWith("transition");
}
function findExistingListener(tView, lView, eventName, tNodeIndex) {
  const tCleanup = tView.cleanup;
  if (tCleanup != null) {
    for (let i = 0; i < tCleanup.length - 1; i += 2) {
      const cleanupEventName = tCleanup[i];
      if (cleanupEventName === eventName && tCleanup[i + 1] === tNodeIndex) {
        const lCleanup = lView[CLEANUP];
        const listenerIdxInLCleanup = tCleanup[i + 2];
        return lCleanup && lCleanup.length > listenerIdxInLCleanup ? lCleanup[listenerIdxInLCleanup] : null;
      }
      if (typeof cleanupEventName === "string") {
        i += 2;
      }
    }
  }
  return null;
}
function storeListenerCleanup(indexOrTargetGetter, tView, lView, eventName, listenerFn, cleanup, isOutput) {
  const tCleanup = tView.firstCreatePass ? getOrCreateTViewCleanup(tView) : null;
  const lCleanup = getOrCreateLViewCleanup(lView);
  const index = lCleanup.length;
  lCleanup.push(listenerFn, cleanup);
  tCleanup && tCleanup.push(eventName, indexOrTargetGetter, index, (index + 1) * (isOutput ? -1 : 1));
}
function listenToOutput(tNode, lView, directiveIndex, lookupName, eventName, listenerFn) {
  ngDevMode && assertIndexInRange(lView, directiveIndex);
  const instance = lView[directiveIndex];
  const tView = lView[TVIEW];
  const def = tView.data[directiveIndex];
  const propertyName = def.outputs[lookupName];
  const output = instance[propertyName];
  if (ngDevMode && !isOutputSubscribable(output)) {
    throw new Error(`@Output ${propertyName} not initialized in '${instance.constructor.name}'.`);
  }
  const subscription = output.subscribe(listenerFn);
  storeListenerCleanup(tNode.index, tView, lView, eventName, listenerFn, subscription, true);
}
function isOutputSubscribable(value) {
  return value != null && typeof value.subscribe === "function";
}
function \u0275\u0275controlCreate() {
  controlCreateInternal();
}
function controlCreateInternal() {
  const lView = getLView();
  const tView = getTView();
  const tNode = getCurrentTNode();
  if (tView.firstCreatePass) {
    initializeControlFirstCreatePass(tView, tNode);
  }
  if (tNode.controlDirectiveIndex === -1) {
    return;
  }
  performanceMarkFeature("NgSignalForms");
  const instance = lView[tNode.controlDirectiveIndex];
  const controlDef = tView.data[tNode.controlDirectiveIndex].controlDef;
  controlDef.create(instance, new ControlDirectiveHostImpl(lView, tView, tNode));
}
function \u0275\u0275control() {
  controlUpdateInternal();
}
function controlUpdateInternal() {
  if (ngDevMode && isInCheckNoChangesMode()) {
    return;
  }
  const lView = getLView();
  const tView = getTView();
  const tNode = getSelectedTNode();
  if (tNode.controlDirectiveIndex === -1) {
    return;
  }
  const controlDef = tView.data[tNode.controlDirectiveIndex].controlDef;
  const instance = lView[tNode.controlDirectiveIndex];
  controlDef.update(instance, new ControlDirectiveHostImpl(lView, tView, tNode));
}
var ControlDirectiveHostImpl = class {
  lView;
  tView;
  tNode;
  hasPassThrough;
  constructor(lView, tView, tNode) {
    this.lView = lView;
    this.tView = tView;
    this.tNode = tNode;
    this.hasPassThrough = !!(tNode.flags & 4096);
  }
  get customControl() {
    return this.tNode.customControlIndex !== -1 ? this.lView[this.tNode.customControlIndex] : void 0;
  }
  get descriptor() {
    if (ngDevMode && isComponentHost(this.tNode)) {
      const componentIndex = this.tNode.directiveStart + this.tNode.componentOffset;
      const componentDef = this.tView.data[componentIndex];
      return `Component ${debugStringifyTypeForError(componentDef.type)}`;
    }
    return `<${this.tNode.value}>`;
  }
  listenToCustomControlOutput(outputName, callback) {
    if (!hasOutput(this.tView.data[this.tNode.customControlIndex], outputName)) {
      return;
    }
    listenToOutput(this.tNode, this.lView, this.tNode.customControlIndex, outputName, outputName, wrapListener(this.tNode, this.lView, callback));
  }
  listenToCustomControlModel(listener) {
    const modelName = this.tNode.flags & 1024 ? "valueChange" : "checkedChange";
    listenToOutput(this.tNode, this.lView, this.tNode.customControlIndex, modelName, modelName, wrapListener(this.tNode, this.lView, listener));
  }
  listenToDom(eventName, listener) {
    listenToDomEvent(this.tNode, this.tView, this.lView, void 0, this.lView[RENDERER], eventName, listener, wrapListener(this.tNode, this.lView, listener));
  }
  setInputOnDirectives(inputName, value) {
    const directiveIndices = this.tNode.inputs?.[inputName];
    const hostDirectiveInputs = this.tNode.hostDirectiveInputs?.[inputName];
    if (!directiveIndices && !hostDirectiveInputs) {
      return false;
    }
    if (directiveIndices) {
      for (const index of directiveIndices) {
        const directiveDef = this.tView.data[index];
        const directive = this.lView[index];
        writeToDirectiveInput(directiveDef, directive, inputName, value);
      }
    }
    if (hostDirectiveInputs) {
      for (let i = 0; i < hostDirectiveInputs.length; i += 2) {
        const index = hostDirectiveInputs[i];
        const internalName = hostDirectiveInputs[i + 1];
        const directiveDef = this.tView.data[index];
        const directive = this.lView[index];
        writeToDirectiveInput(directiveDef, directive, internalName, value);
      }
    }
    return true;
  }
  setCustomControlModelInput(value) {
    const directive = this.lView[this.tNode.customControlIndex];
    const directiveDef = this.tView.data[this.tNode.customControlIndex];
    const modelName = this.tNode.flags & 1024 ? "value" : "checked";
    writeToDirectiveInput(directiveDef, directive, modelName, value);
  }
  customControlHasInput(inputName) {
    if (this.tNode.customControlIndex === -1) {
      return false;
    }
    const directiveDef = this.tView.data[this.tNode.customControlIndex];
    return directiveDef.inputs[inputName] != void 0;
  }
};
function initializeControlFirstCreatePass(tView, tNode, lView) {
  ngDevMode && assertFirstCreatePass(tView);
  for (let i = tNode.directiveStart; i < tNode.directiveEnd; i++) {
    const directiveDef = tView.data[i];
    if (directiveDef.controlDef) {
      tNode.controlDirectiveIndex = i;
      break;
    }
  }
  if (tNode.controlDirectiveIndex === -1) {
    return;
  }
  const controlDef = tView.data[tNode.controlDirectiveIndex].controlDef;
  if (controlDef.passThroughInput) {
    if ((tNode.inputs?.[controlDef.passThroughInput]?.length ?? 0) > 1) {
      tNode.flags |= 4096;
      return;
    }
  }
  initializeCustomControlStatus(tView, tNode);
}
function initializeCustomControlStatus(tView, tNode) {
  for (let i = tNode.directiveStart; i < tNode.directiveEnd; i++) {
    const directiveDef = tView.data[i];
    if (hasModelInput(directiveDef, "value")) {
      tNode.flags |= 1024;
      tNode.customControlIndex = i;
      return;
    }
    if (hasModelInput(directiveDef, "checked")) {
      tNode.flags |= 2048;
      tNode.customControlIndex = i;
      return;
    }
  }
}
function hasModelInput(directiveDef, name) {
  return hasInput(directiveDef, name) && hasOutput(directiveDef, name + "Change");
}
function hasInput(directiveDef, name) {
  return name in directiveDef.inputs;
}
function hasOutput(directiveDef, name) {
  return name in directiveDef.outputs;
}
var BINDING = /* @__PURE__ */ Symbol("BINDING");
function getComponentName(def) {
  return def.debugInfo?.className || def.type.name || null;
}
var ComponentFactoryResolver2 = class extends ComponentFactoryResolver$1 {
  ngModule;
  constructor(ngModule) {
    super();
    this.ngModule = ngModule;
  }
  resolveComponentFactory(component) {
    ngDevMode && assertComponentType(component);
    const componentDef = getComponentDef(component);
    return new ComponentFactory2(componentDef, this.ngModule);
  }
};
function toInputRefArray(map2) {
  return Object.keys(map2).map((name) => {
    const [propName, flags, transform] = map2[name];
    const inputData = {
      propName,
      templateName: name,
      isSignal: (flags & InputFlags.SignalBased) !== 0
    };
    if (transform) {
      inputData.transform = transform;
    }
    return inputData;
  });
}
function toOutputRefArray(map2) {
  return Object.keys(map2).map((name) => ({
    propName: map2[name],
    templateName: name
  }));
}
function verifyNotAnOrphanComponent(componentDef) {
  if (false) {
    if (depsTracker.isOrphanComponent(componentDef.type)) {
      throw new RuntimeError(981, `Orphan component found! Trying to render the component ${debugStringifyTypeForError(componentDef.type)} without first loading the NgModule that declares it. It is recommended to make this component standalone in order to avoid this error. If this is not possible now, import the component's NgModule in the appropriate NgModule, or the standalone component in which you are trying to render this component. If this is a lazy import, load the NgModule lazily as well and use its module injector.`);
    }
  }
}
function createRootViewInjector(componentDef, environmentInjector, injector) {
  let realEnvironmentInjector = environmentInjector instanceof EnvironmentInjector ? environmentInjector : environmentInjector?.injector;
  if (realEnvironmentInjector && componentDef.getStandaloneInjector !== null) {
    realEnvironmentInjector = componentDef.getStandaloneInjector(realEnvironmentInjector) || realEnvironmentInjector;
  }
  const rootViewInjector = realEnvironmentInjector ? new ChainedInjector(injector, realEnvironmentInjector) : injector;
  return rootViewInjector;
}
function createRootLViewEnvironment(rootLViewInjector) {
  const rendererFactory = rootLViewInjector.get(RendererFactory2, null);
  if (rendererFactory === null) {
    throw new RuntimeError(407, ngDevMode && "Angular was not able to inject a renderer (RendererFactory2). Likely this is due to a broken DI hierarchy. Make sure that any injector used to create this component has a correct parent.");
  }
  const sanitizer = rootLViewInjector.get(Sanitizer, null);
  const changeDetectionScheduler = rootLViewInjector.get(ChangeDetectionScheduler, null);
  const tracingService = rootLViewInjector.get(TracingService, null, {
    optional: true
  });
  let ngReflect = false;
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    ngReflect = rootLViewInjector.get(NG_REFLECT_ATTRS_FLAG, NG_REFLECT_ATTRS_FLAG_DEFAULT);
  }
  return {
    rendererFactory,
    sanitizer,
    changeDetectionScheduler,
    ngReflect,
    tracingService
  };
}
function createHostElement(componentDef, renderer) {
  const tagName = inferTagNameFromDefinition(componentDef);
  const namespace = tagName === "svg" ? SVG_NAMESPACE : tagName === "math" ? MATH_ML_NAMESPACE : null;
  return createElementNode(renderer, tagName, namespace);
}
function inferTagNameFromDefinition(componentDef) {
  return (componentDef.selectors[0][0] || "div").toLowerCase();
}
var ComponentFactory2 = class extends ComponentFactory$1 {
  componentDef;
  ngModule;
  selector;
  componentType;
  ngContentSelectors;
  isBoundToModule;
  cachedInputs = null;
  cachedOutputs = null;
  get inputs() {
    this.cachedInputs ??= toInputRefArray(this.componentDef.inputs);
    return this.cachedInputs;
  }
  get outputs() {
    this.cachedOutputs ??= toOutputRefArray(this.componentDef.outputs);
    return this.cachedOutputs;
  }
  constructor(componentDef, ngModule) {
    super();
    this.componentDef = componentDef;
    this.ngModule = ngModule;
    this.componentType = componentDef.type;
    this.selector = stringifyCSSSelectorList(componentDef.selectors);
    this.ngContentSelectors = componentDef.ngContentSelectors ?? [];
    this.isBoundToModule = !!ngModule;
  }
  create(injector, projectableNodes, rootSelectorOrNode, environmentInjector, directives, componentBindings) {
    profiler(ProfilerEvent.DynamicComponentStart);
    const prevConsumer = setActiveConsumer(null);
    try {
      const cmpDef = this.componentDef;
      ngDevMode && verifyNotAnOrphanComponent(cmpDef);
      const rootViewInjector = createRootViewInjector(cmpDef, environmentInjector || this.ngModule, injector);
      const environment = createRootLViewEnvironment(rootViewInjector);
      const tracingService = environment.tracingService;
      if (tracingService && tracingService.componentCreate) {
        return tracingService.componentCreate(getComponentName(cmpDef), () => this.createComponentRef(environment, rootViewInjector, projectableNodes, rootSelectorOrNode, directives, componentBindings));
      } else {
        return this.createComponentRef(environment, rootViewInjector, projectableNodes, rootSelectorOrNode, directives, componentBindings);
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
  createComponentRef(environment, rootViewInjector, projectableNodes, rootSelectorOrNode, directives, componentBindings) {
    const cmpDef = this.componentDef;
    const rootTView = createRootTView(rootSelectorOrNode, cmpDef, componentBindings, directives);
    const hostRenderer = environment.rendererFactory.createRenderer(null, cmpDef);
    const hostElement = rootSelectorOrNode ? locateHostElement(hostRenderer, rootSelectorOrNode, cmpDef.encapsulation, rootViewInjector) : createHostElement(cmpDef, hostRenderer);
    const hasInputBindings = componentBindings?.some(isInputBinding) || directives?.some((d) => typeof d !== "function" && d.bindings.some(isInputBinding));
    const rootLView = createLView(null, rootTView, null, 512 | getInitialLViewFlagsFromDef(cmpDef), null, null, environment, hostRenderer, rootViewInjector, null, retrieveHydrationInfo(hostElement, rootViewInjector, true));
    rootLView[HEADER_OFFSET] = hostElement;
    enterView(rootLView);
    let componentView = null;
    try {
      const hostTNode = directiveHostFirstCreatePass(HEADER_OFFSET, rootLView, 2, "#host", () => rootTView.directiveRegistry, true, 0);
      setupStaticAttributes(hostRenderer, hostElement, hostTNode);
      attachPatchData(hostElement, rootLView);
      createDirectivesInstances(rootTView, rootLView, hostTNode);
      executeContentQueries(rootTView, hostTNode, rootLView);
      directiveHostEndFirstCreatePass(rootTView, hostTNode);
      if (projectableNodes !== void 0) {
        projectNodes(hostTNode, this.ngContentSelectors, projectableNodes);
      }
      componentView = getComponentLViewByIndex(hostTNode.index, rootLView);
      rootLView[CONTEXT] = componentView[CONTEXT];
      renderView(rootTView, rootLView, null);
    } catch (e) {
      if (componentView !== null) {
        unregisterLView(componentView);
      }
      unregisterLView(rootLView);
      throw e;
    } finally {
      profiler(ProfilerEvent.DynamicComponentEnd);
      leaveView();
    }
    return new ComponentRef2(this.componentType, rootLView, !!hasInputBindings);
  }
};
function createRootTView(rootSelectorOrNode, componentDef, componentBindings, directives) {
  const tAttributes = rootSelectorOrNode ? ["ng-version", "21.2.17"] : extractAttrsAndClassesFromSelector(componentDef.selectors[0]);
  let creationBindings = null;
  let updateBindings = null;
  let varsToAllocate = 0;
  if (componentBindings) {
    for (const binding of componentBindings) {
      varsToAllocate += binding[BINDING].requiredVars;
      if (binding.create) {
        binding.targetIdx = 0;
        (creationBindings ??= []).push(binding);
      }
      if (binding.update) {
        binding.targetIdx = 0;
        (updateBindings ??= []).push(binding);
      }
    }
  }
  if (directives) {
    for (let i = 0; i < directives.length; i++) {
      const directive = directives[i];
      if (typeof directive !== "function") {
        for (const binding of directive.bindings) {
          varsToAllocate += binding[BINDING].requiredVars;
          const targetDirectiveIdx = i + 1;
          if (binding.create) {
            binding.targetIdx = targetDirectiveIdx;
            (creationBindings ??= []).push(binding);
          }
          if (binding.update) {
            binding.targetIdx = targetDirectiveIdx;
            (updateBindings ??= []).push(binding);
          }
        }
      }
    }
  }
  const directivesToApply = [componentDef];
  if (directives) {
    for (const directive of directives) {
      const directiveType = typeof directive === "function" ? directive : directive.type;
      const directiveDef = ngDevMode ? getDirectiveDefOrThrow(directiveType) : getDirectiveDef(directiveType);
      if (ngDevMode && !directiveDef.standalone) {
        throw new RuntimeError(907, `The ${stringifyForError(directiveType)} directive must be standalone in order to be applied to a dynamically-created component.`);
      }
      directivesToApply.push(directiveDef);
    }
  }
  const rootTView = createTView(0, null, getRootTViewTemplate(creationBindings, updateBindings), 1, varsToAllocate, directivesToApply, null, null, null, [tAttributes], null);
  return rootTView;
}
function getRootTViewTemplate(creationBindings, updateBindings) {
  if (!creationBindings && !updateBindings) {
    return null;
  }
  return (flags) => {
    if (flags & 1 && creationBindings) {
      for (const binding of creationBindings) {
        binding.create();
      }
    }
    if (flags & 2 && updateBindings) {
      for (const binding of updateBindings) {
        binding.update();
      }
    }
  };
}
function isInputBinding(binding) {
  const kind = binding[BINDING].kind;
  return kind === "input" || kind === "twoWay";
}
var ComponentRef2 = class extends ComponentRef$1 {
  _rootLView;
  _hasInputBindings;
  instance;
  hostView;
  changeDetectorRef;
  componentType;
  location;
  previousInputValues = null;
  _tNode;
  constructor(componentType, _rootLView, _hasInputBindings) {
    super();
    this._rootLView = _rootLView;
    this._hasInputBindings = _hasInputBindings;
    this._tNode = getTNode(_rootLView[TVIEW], HEADER_OFFSET);
    this.location = createElementRef(this._tNode, _rootLView);
    this.instance = getComponentLViewByIndex(this._tNode.index, _rootLView)[CONTEXT];
    this.hostView = this.changeDetectorRef = new ViewRef(_rootLView, void 0);
    this.componentType = componentType;
  }
  setInput(name, value) {
    if (this._hasInputBindings && ngDevMode) {
      throw new RuntimeError(317, "Cannot call `setInput` on a component that is using the `inputBinding` or `twoWayBinding` functions.");
    }
    const tNode = this._tNode;
    this.previousInputValues ??= /* @__PURE__ */ new Map();
    if (this.previousInputValues.has(name) && Object.is(this.previousInputValues.get(name), value)) {
      return;
    }
    const lView = this._rootLView;
    const hasSetInput = setAllInputsForProperty(tNode, lView[TVIEW], lView, name, value);
    this.previousInputValues.set(name, value);
    const childComponentLView = getComponentLViewByIndex(tNode.index, lView);
    markViewDirty(childComponentLView, 1);
    if (ngDevMode && !hasSetInput) {
      const cmpNameForError = stringifyForError(this.componentType);
      let message = `Can't set value of the '${name}' input on the '${cmpNameForError}' component. `;
      message += `Make sure that the '${name}' property is declared as an input using the input() or model() function or the @Input() decorator.`;
      reportUnknownPropertyError(message);
    }
  }
  get injector() {
    return new NodeInjector(this._tNode, this._rootLView);
  }
  destroy() {
    this.hostView.destroy();
  }
  onDestroy(callback) {
    this.hostView.onDestroy(callback);
  }
};
function projectNodes(tNode, ngContentSelectors, projectableNodes) {
  const projection = tNode.projection = [];
  for (let i = 0; i < ngContentSelectors.length; i++) {
    const nodesforSlot = projectableNodes[i];
    projection.push(nodesforSlot != null && nodesforSlot.length ? Array.from(nodesforSlot) : null);
  }
}
var ViewContainerRef = class {
  static __NG_ELEMENT_ID__ = injectViewContainerRef;
};
function injectViewContainerRef() {
  const previousTNode = getCurrentTNode();
  return createContainerRef(previousTNode, getLView());
}
var R3ViewContainerRef = class _R3ViewContainerRef extends ViewContainerRef {
  _lContainer;
  _hostTNode;
  _hostLView;
  constructor(_lContainer, _hostTNode, _hostLView) {
    super();
    this._lContainer = _lContainer;
    this._hostTNode = _hostTNode;
    this._hostLView = _hostLView;
  }
  get element() {
    return createElementRef(this._hostTNode, this._hostLView);
  }
  get injector() {
    return new NodeInjector(this._hostTNode, this._hostLView);
  }
  get parentInjector() {
    const parentLocation = getParentInjectorLocation(this._hostTNode, this._hostLView);
    if (hasParentInjector(parentLocation)) {
      const parentView = getParentInjectorView(parentLocation, this._hostLView);
      const injectorIndex = getParentInjectorIndex(parentLocation);
      ngDevMode && assertNodeInjector(parentView, injectorIndex);
      const parentTNode = parentView[TVIEW].data[injectorIndex + 8];
      return new NodeInjector(parentTNode, parentView);
    } else {
      return new NodeInjector(null, this._hostLView);
    }
  }
  clear() {
    while (this.length > 0) {
      this.remove(this.length - 1);
    }
  }
  get(index) {
    const viewRefs = getViewRefs(this._lContainer);
    return viewRefs !== null && viewRefs[index] || null;
  }
  get length() {
    return this._lContainer.length - CONTAINER_HEADER_OFFSET;
  }
  createEmbeddedView(templateRef, context2, indexOrOptions) {
    let index;
    let injector;
    if (typeof indexOrOptions === "number") {
      index = indexOrOptions;
    } else if (indexOrOptions != null) {
      index = indexOrOptions.index;
      injector = indexOrOptions.injector;
    }
    const dehydratedView = findMatchingDehydratedView(this._lContainer, templateRef.ssrId);
    const viewRef = templateRef.createEmbeddedViewImpl(context2 || {}, injector, dehydratedView);
    this.insertImpl(viewRef, index, shouldAddViewToDom(this._hostTNode, dehydratedView));
    return viewRef;
  }
  createComponent(componentFactoryOrType, indexOrOptions, injector, projectableNodes, environmentInjector, directives, bindings) {
    const isComponentFactory = componentFactoryOrType && !isType(componentFactoryOrType);
    let index;
    if (isComponentFactory) {
      if (ngDevMode) {
        assertEqual(typeof indexOrOptions !== "object", true, "It looks like Component factory was provided as the first argument and an options object as the second argument. This combination of arguments is incompatible. You can either change the first argument to provide Component type or change the second argument to be a number (representing an index at which to insert the new component's host view into this container)");
      }
      index = indexOrOptions;
    } else {
      if (ngDevMode) {
        assertDefined(getComponentDef(componentFactoryOrType), `Provided Component class doesn't contain Component definition. Please check whether provided class has @Component decorator.`);
        assertEqual(typeof indexOrOptions !== "number", true, "It looks like Component type was provided as the first argument and a number (representing an index at which to insert the new component's host view into this container as the second argument. This combination of arguments is incompatible. Please use an object as the second argument instead.");
      }
      const options = indexOrOptions || {};
      if (ngDevMode && options.environmentInjector && options.ngModuleRef) {
        throwError2(`Cannot pass both environmentInjector and ngModuleRef options to createComponent().`);
      }
      index = options.index;
      injector = options.injector;
      projectableNodes = options.projectableNodes;
      environmentInjector = options.environmentInjector || options.ngModuleRef;
      directives = options.directives;
      bindings = options.bindings;
    }
    const componentFactory = isComponentFactory ? componentFactoryOrType : new ComponentFactory2(getComponentDef(componentFactoryOrType));
    const contextInjector = injector || this.parentInjector;
    if (!environmentInjector && componentFactory.ngModule == null) {
      const _injector = isComponentFactory ? contextInjector : this.parentInjector;
      const result = _injector.get(EnvironmentInjector, null);
      if (result) {
        environmentInjector = result;
      }
    }
    const componentDef = getComponentDef(componentFactory.componentType ?? {});
    const dehydratedView = findMatchingDehydratedView(this._lContainer, componentDef?.id ?? null);
    const rNode = dehydratedView?.firstChild ?? null;
    const componentRef = componentFactory.create(contextInjector, projectableNodes, rNode, environmentInjector, directives, bindings);
    this.insertImpl(componentRef.hostView, index, shouldAddViewToDom(this._hostTNode, dehydratedView));
    return componentRef;
  }
  insert(viewRef, index) {
    return this.insertImpl(viewRef, index, true);
  }
  insertImpl(viewRef, index, addToDOM) {
    const lView = viewRef._lView;
    if (ngDevMode && viewRef.destroyed) {
      throw new RuntimeError(922, ngDevMode && "Cannot insert a destroyed View in a ViewContainer!");
    }
    if (viewAttachedToContainer(lView)) {
      const prevIdx = this.indexOf(viewRef);
      if (prevIdx !== -1) {
        this.detach(prevIdx);
      } else {
        const prevLContainer = lView[PARENT];
        ngDevMode && assertEqual(isLContainer(prevLContainer), true, "An attached view should have its PARENT point to a container.");
        const prevVCRef = new _R3ViewContainerRef(prevLContainer, prevLContainer[T_HOST], prevLContainer[PARENT]);
        prevVCRef.detach(prevVCRef.indexOf(viewRef));
      }
    }
    const adjustedIdx = this._adjustIndex(index);
    const lContainer = this._lContainer;
    addLViewToLContainer(lContainer, lView, adjustedIdx, addToDOM);
    viewRef.attachToViewContainerRef();
    addToArray(getOrCreateViewRefs(lContainer), adjustedIdx, viewRef);
    return viewRef;
  }
  move(viewRef, newIndex) {
    if (ngDevMode && viewRef.destroyed) {
      throw new RuntimeError(923, ngDevMode && "Cannot move a destroyed View in a ViewContainer!");
    }
    return this.insert(viewRef, newIndex);
  }
  indexOf(viewRef) {
    const viewRefsArr = getViewRefs(this._lContainer);
    return viewRefsArr !== null ? viewRefsArr.indexOf(viewRef) : -1;
  }
  remove(index) {
    const adjustedIdx = this._adjustIndex(index, -1);
    const detachedView = detachView(this._lContainer, adjustedIdx);
    if (detachedView) {
      removeFromArray(getOrCreateViewRefs(this._lContainer), adjustedIdx);
      destroyLView(detachedView[TVIEW], detachedView);
    }
  }
  detach(index) {
    const adjustedIdx = this._adjustIndex(index, -1);
    const view = detachView(this._lContainer, adjustedIdx);
    const wasDetached = view && removeFromArray(getOrCreateViewRefs(this._lContainer), adjustedIdx) != null;
    return wasDetached ? new ViewRef(view) : null;
  }
  _adjustIndex(index, shift = 0) {
    if (index == null) {
      return this.length + shift;
    }
    if (ngDevMode) {
      assertGreaterThan(index, -1, `ViewRef index must be positive, got ${index}`);
      assertLessThan(index, this.length + 1 + shift, "index");
    }
    return index;
  }
};
function getViewRefs(lContainer) {
  return lContainer[VIEW_REFS];
}
function getOrCreateViewRefs(lContainer) {
  return lContainer[VIEW_REFS] || (lContainer[VIEW_REFS] = []);
}
function createContainerRef(hostTNode, hostLView) {
  ngDevMode && assertTNodeType(hostTNode, 12 | 3);
  let lContainer;
  const slotValue = hostLView[hostTNode.index];
  if (isLContainer(slotValue)) {
    lContainer = slotValue;
  } else {
    lContainer = createLContainer(slotValue, hostLView, null, hostTNode);
    hostLView[hostTNode.index] = lContainer;
    addToEndOfViewTree(hostLView, lContainer);
  }
  _locateOrCreateAnchorNode(lContainer, hostLView, hostTNode, slotValue);
  return new R3ViewContainerRef(lContainer, hostTNode, hostLView);
}
function insertAnchorNode(hostLView, hostTNode) {
  const renderer = hostLView[RENDERER];
  const commentNode = renderer.createComment(ngDevMode ? "container" : "");
  const hostNative = getNativeByTNode(hostTNode, hostLView);
  const parentOfHostNative = renderer.parentNode(hostNative);
  nativeInsertBefore(renderer, parentOfHostNative, commentNode, renderer.nextSibling(hostNative), false);
  return commentNode;
}
var _locateOrCreateAnchorNode = createAnchorNode;
var _populateDehydratedViewsInLContainer = () => false;
function populateDehydratedViewsInLContainer(lContainer, tNode, hostLView) {
  return _populateDehydratedViewsInLContainer(lContainer, tNode, hostLView);
}
function createAnchorNode(lContainer, hostLView, hostTNode, slotValue) {
  if (lContainer[NATIVE]) return;
  let commentNode;
  if (hostTNode.type & 8) {
    commentNode = unwrapRNode(slotValue);
  } else {
    commentNode = insertAnchorNode(hostLView, hostTNode);
  }
  lContainer[NATIVE] = commentNode;
}
var LQuery_ = class _LQuery_ {
  queryList;
  matches = null;
  constructor(queryList) {
    this.queryList = queryList;
  }
  clone() {
    return new _LQuery_(this.queryList);
  }
  setDirty() {
    this.queryList.setDirty();
  }
};
var LQueries_ = class _LQueries_ {
  queries;
  constructor(queries = []) {
    this.queries = queries;
  }
  createEmbeddedView(tView) {
    const tQueries = tView.queries;
    if (tQueries !== null) {
      const noOfInheritedQueries = tView.contentQueries !== null ? tView.contentQueries[0] : tQueries.length;
      const viewLQueries = [];
      for (let i = 0; i < noOfInheritedQueries; i++) {
        const tQuery = tQueries.getByIndex(i);
        const parentLQuery = this.queries[tQuery.indexInDeclarationView];
        viewLQueries.push(parentLQuery.clone());
      }
      return new _LQueries_(viewLQueries);
    }
    return null;
  }
  insertView(tView) {
    this.dirtyQueriesWithMatches(tView);
  }
  detachView(tView) {
    this.dirtyQueriesWithMatches(tView);
  }
  finishViewCreation(tView) {
    this.dirtyQueriesWithMatches(tView);
  }
  dirtyQueriesWithMatches(tView) {
    for (let i = 0; i < this.queries.length; i++) {
      if (getTQuery(tView, i).matches !== null) {
        this.queries[i].setDirty();
      }
    }
  }
};
var TQueryMetadata_ = class {
  flags;
  read;
  predicate;
  constructor(predicate, flags, read = null) {
    this.flags = flags;
    this.read = read;
    if (typeof predicate === "string") {
      this.predicate = splitQueryMultiSelectors(predicate);
    } else {
      this.predicate = predicate;
    }
  }
};
var TQueries_ = class _TQueries_ {
  queries;
  constructor(queries = []) {
    this.queries = queries;
  }
  elementStart(tView, tNode) {
    ngDevMode && assertFirstCreatePass(tView, "Queries should collect results on the first template pass only");
    for (let i = 0; i < this.queries.length; i++) {
      this.queries[i].elementStart(tView, tNode);
    }
  }
  elementEnd(tNode) {
    for (let i = 0; i < this.queries.length; i++) {
      this.queries[i].elementEnd(tNode);
    }
  }
  embeddedTView(tNode) {
    let queriesForTemplateRef = null;
    for (let i = 0; i < this.length; i++) {
      const childQueryIndex = queriesForTemplateRef !== null ? queriesForTemplateRef.length : 0;
      const tqueryClone = this.getByIndex(i).embeddedTView(tNode, childQueryIndex);
      if (tqueryClone) {
        tqueryClone.indexInDeclarationView = i;
        if (queriesForTemplateRef !== null) {
          queriesForTemplateRef.push(tqueryClone);
        } else {
          queriesForTemplateRef = [tqueryClone];
        }
      }
    }
    return queriesForTemplateRef !== null ? new _TQueries_(queriesForTemplateRef) : null;
  }
  template(tView, tNode) {
    ngDevMode && assertFirstCreatePass(tView, "Queries should collect results on the first template pass only");
    for (let i = 0; i < this.queries.length; i++) {
      this.queries[i].template(tView, tNode);
    }
  }
  getByIndex(index) {
    ngDevMode && assertIndexInRange(this.queries, index);
    return this.queries[index];
  }
  get length() {
    return this.queries.length;
  }
  track(tquery) {
    this.queries.push(tquery);
  }
};
var TQuery_ = class _TQuery_ {
  metadata;
  matches = null;
  indexInDeclarationView = -1;
  crossesNgTemplate = false;
  _declarationNodeIndex;
  _appliesToNextNode = true;
  constructor(metadata, nodeIndex = -1) {
    this.metadata = metadata;
    this._declarationNodeIndex = nodeIndex;
  }
  elementStart(tView, tNode) {
    if (this.isApplyingToNode(tNode)) {
      this.matchTNode(tView, tNode);
    }
  }
  elementEnd(tNode) {
    if (this._declarationNodeIndex === tNode.index) {
      this._appliesToNextNode = false;
    }
  }
  template(tView, tNode) {
    this.elementStart(tView, tNode);
  }
  embeddedTView(tNode, childQueryIndex) {
    if (this.isApplyingToNode(tNode)) {
      this.crossesNgTemplate = true;
      this.addMatch(-tNode.index, childQueryIndex);
      return new _TQuery_(this.metadata);
    }
    return null;
  }
  isApplyingToNode(tNode) {
    if (this._appliesToNextNode && (this.metadata.flags & 1) !== 1) {
      const declarationNodeIdx = this._declarationNodeIndex;
      let parent = tNode.parent;
      while (parent !== null && parent.type & 8 && parent.index !== declarationNodeIdx) {
        parent = parent.parent;
      }
      return declarationNodeIdx === (parent !== null ? parent.index : -1);
    }
    return this._appliesToNextNode;
  }
  matchTNode(tView, tNode) {
    const predicate = this.metadata.predicate;
    if (Array.isArray(predicate)) {
      for (let i = 0; i < predicate.length; i++) {
        const name = predicate[i];
        this.matchTNodeWithReadOption(tView, tNode, getIdxOfMatchingSelector(tNode, name));
        this.matchTNodeWithReadOption(tView, tNode, locateDirectiveOrProvider(tNode, tView, name, false, false));
      }
    } else {
      if (predicate === TemplateRef) {
        if (tNode.type & 4) {
          this.matchTNodeWithReadOption(tView, tNode, -1);
        }
      } else {
        this.matchTNodeWithReadOption(tView, tNode, locateDirectiveOrProvider(tNode, tView, predicate, false, false));
      }
    }
  }
  matchTNodeWithReadOption(tView, tNode, nodeMatchIdx) {
    if (nodeMatchIdx !== null) {
      const read = this.metadata.read;
      if (read !== null) {
        if (read === ElementRef || read === ViewContainerRef || read === TemplateRef && tNode.type & 4) {
          this.addMatch(tNode.index, -2);
        } else {
          const directiveOrProviderIdx = locateDirectiveOrProvider(tNode, tView, read, false, false);
          if (directiveOrProviderIdx !== null) {
            this.addMatch(tNode.index, directiveOrProviderIdx);
          }
        }
      } else {
        this.addMatch(tNode.index, nodeMatchIdx);
      }
    }
  }
  addMatch(tNodeIdx, matchIdx) {
    if (this.matches === null) {
      this.matches = [tNodeIdx, matchIdx];
    } else {
      this.matches.push(tNodeIdx, matchIdx);
    }
  }
};
function getIdxOfMatchingSelector(tNode, selector) {
  const localNames = tNode.localNames;
  if (localNames !== null) {
    for (let i = 0; i < localNames.length; i += 2) {
      if (localNames[i] === selector) {
        return localNames[i + 1];
      }
    }
  }
  return null;
}
function createResultByTNodeType(tNode, currentView) {
  if (tNode.type & (3 | 8)) {
    return createElementRef(tNode, currentView);
  } else if (tNode.type & 4) {
    return createTemplateRef(tNode, currentView);
  }
  return null;
}
function createResultForNode(lView, tNode, matchingIdx, read) {
  if (matchingIdx === -1) {
    return createResultByTNodeType(tNode, lView);
  } else if (matchingIdx === -2) {
    return createSpecialToken(lView, tNode, read);
  } else {
    return getNodeInjectable(lView, lView[TVIEW], matchingIdx, tNode);
  }
}
function createSpecialToken(lView, tNode, read) {
  if (read === ElementRef) {
    return createElementRef(tNode, lView);
  } else if (read === TemplateRef) {
    return createTemplateRef(tNode, lView);
  } else if (read === ViewContainerRef) {
    ngDevMode && assertTNodeType(tNode, 3 | 12);
    return createContainerRef(tNode, lView);
  } else {
    ngDevMode && throwError2(`Special token to read should be one of ElementRef, TemplateRef or ViewContainerRef but got ${stringify(read)}.`);
  }
}
function materializeViewResults(tView, lView, tQuery, queryIndex) {
  const lQuery = lView[QUERIES].queries[queryIndex];
  if (lQuery.matches === null) {
    const tViewData = tView.data;
    const tQueryMatches = tQuery.matches;
    const result = [];
    for (let i = 0; tQueryMatches !== null && i < tQueryMatches.length; i += 2) {
      const matchedNodeIdx = tQueryMatches[i];
      if (matchedNodeIdx < 0) {
        result.push(null);
      } else {
        ngDevMode && assertIndexInRange(tViewData, matchedNodeIdx);
        const tNode = tViewData[matchedNodeIdx];
        result.push(createResultForNode(lView, tNode, tQueryMatches[i + 1], tQuery.metadata.read));
      }
    }
    lQuery.matches = result;
  }
  return lQuery.matches;
}
function collectQueryResults(tView, lView, queryIndex, result) {
  const tQuery = tView.queries.getByIndex(queryIndex);
  const tQueryMatches = tQuery.matches;
  if (tQueryMatches !== null) {
    const lViewResults = materializeViewResults(tView, lView, tQuery, queryIndex);
    for (let i = 0; i < tQueryMatches.length; i += 2) {
      const tNodeIdx = tQueryMatches[i];
      if (tNodeIdx > 0) {
        result.push(lViewResults[i / 2]);
      } else {
        const childQueryIndex = tQueryMatches[i + 1];
        const declarationLContainer = lView[-tNodeIdx];
        ngDevMode && assertLContainer(declarationLContainer);
        for (let i2 = CONTAINER_HEADER_OFFSET; i2 < declarationLContainer.length; i2++) {
          const embeddedLView = declarationLContainer[i2];
          if (embeddedLView[DECLARATION_LCONTAINER] === embeddedLView[PARENT]) {
            collectQueryResults(embeddedLView[TVIEW], embeddedLView, childQueryIndex, result);
          }
        }
        if (declarationLContainer[MOVED_VIEWS] !== null) {
          const embeddedLViews = declarationLContainer[MOVED_VIEWS];
          for (let i2 = 0; i2 < embeddedLViews.length; i2++) {
            const embeddedLView = embeddedLViews[i2];
            collectQueryResults(embeddedLView[TVIEW], embeddedLView, childQueryIndex, result);
          }
        }
      }
    }
  }
  return result;
}
function loadQueryInternal(lView, queryIndex) {
  ngDevMode && assertDefined(lView[QUERIES], "LQueries should be defined when trying to load a query");
  ngDevMode && assertIndexInRange(lView[QUERIES].queries, queryIndex);
  return lView[QUERIES].queries[queryIndex].queryList;
}
function createLQuery(tView, lView, flags) {
  const queryList = new QueryList((flags & 4) === 4);
  storeCleanupWithContext(tView, lView, queryList, queryList.destroy);
  const lQueries = (lView[QUERIES] ??= new LQueries_()).queries;
  return lQueries.push(new LQuery_(queryList)) - 1;
}
function createViewQuery(predicate, flags, read) {
  ngDevMode && assertNumber(flags, "Expecting flags");
  const tView = getTView();
  if (tView.firstCreatePass) {
    createTQuery(tView, new TQueryMetadata_(predicate, flags, read), -1);
    if ((flags & 2) === 2) {
      tView.staticViewQueries = true;
    }
  }
  return createLQuery(tView, getLView(), flags);
}
function createContentQuery(directiveIndex, predicate, flags, read) {
  ngDevMode && assertNumber(flags, "Expecting flags");
  const tView = getTView();
  if (tView.firstCreatePass) {
    const tNode = getCurrentTNode();
    createTQuery(tView, new TQueryMetadata_(predicate, flags, read), tNode.index);
    saveContentQueryAndDirectiveIndex(tView, directiveIndex);
    if ((flags & 2) === 2) {
      tView.staticContentQueries = true;
    }
  }
  return createLQuery(tView, getLView(), flags);
}
function splitQueryMultiSelectors(locator) {
  return locator.split(",").map((s) => s.trim());
}
function createTQuery(tView, metadata, nodeIndex) {
  if (tView.queries === null) tView.queries = new TQueries_();
  tView.queries.track(new TQuery_(metadata, nodeIndex));
}
function saveContentQueryAndDirectiveIndex(tView, directiveIndex) {
  const tViewContentQueries = tView.contentQueries || (tView.contentQueries = []);
  const lastSavedDirectiveIndex = tViewContentQueries.length ? tViewContentQueries[tViewContentQueries.length - 1] : -1;
  if (directiveIndex !== lastSavedDirectiveIndex) {
    tViewContentQueries.push(tView.queries.length - 1, directiveIndex);
  }
}
function getTQuery(tView, index) {
  ngDevMode && assertDefined(tView.queries, "TQueries must be defined to retrieve a TQuery");
  return tView.queries.getByIndex(index);
}
function getQueryResults(lView, queryIndex) {
  const tView = lView[TVIEW];
  const tQuery = getTQuery(tView, queryIndex);
  return tQuery.crossesNgTemplate ? collectQueryResults(tView, lView, queryIndex, []) : materializeViewResults(tView, lView, tQuery, queryIndex);
}
function createQuerySignalFn(firstOnly, required, opts) {
  let node;
  const signalFn = createComputed(() => {
    node._dirtyCounter();
    const value = refreshSignalQuery(node, firstOnly);
    if (required && value === void 0) {
      throw new RuntimeError(-951, ngDevMode && "Child query result is required but no value is available.");
    }
    return value;
  });
  node = signalFn[SIGNAL];
  node._dirtyCounter = signal(0);
  node._flatValue = void 0;
  if (ngDevMode) {
    signalFn.toString = () => `[Query Signal]`;
    node.debugName = opts?.debugName;
  }
  return signalFn;
}
function createSingleResultOptionalQuerySignalFn(opts) {
  return createQuerySignalFn(true, false, opts);
}
function createSingleResultRequiredQuerySignalFn(opts) {
  return createQuerySignalFn(true, true, opts);
}
function createMultiResultQuerySignalFn(opts) {
  return createQuerySignalFn(false, false, opts);
}
function bindQueryToSignal(target, queryIndex) {
  const node = target[SIGNAL];
  node._lView = getLView();
  node._queryIndex = queryIndex;
  node._queryList = loadQueryInternal(node._lView, queryIndex);
  node._queryList.onDirty(() => node._dirtyCounter.update((v) => v + 1));
}
function refreshSignalQuery(node, firstOnly) {
  const lView = node._lView;
  const queryIndex = node._queryIndex;
  if (lView === void 0 || queryIndex === void 0 || lView[FLAGS] & 4) {
    return firstOnly ? void 0 : EMPTY_ARRAY;
  }
  const queryList = loadQueryInternal(lView, queryIndex);
  const results = getQueryResults(lView, queryIndex);
  queryList.reset(results, unwrapElementRef);
  if (firstOnly) {
    return queryList.first;
  } else {
    const resultChanged = queryList._changesDetected;
    if (resultChanged || node._flatValue === void 0) {
      return node._flatValue = queryList.toArray();
    }
    return node._flatValue;
  }
}
var componentResourceResolutionQueue = /* @__PURE__ */ new Map();
var componentDefPendingResolution = /* @__PURE__ */ new Set();
async function resolveComponentResources(resourceResolver) {
  const currentQueue = componentResourceResolutionQueue;
  componentResourceResolutionQueue = /* @__PURE__ */ new Map();
  const urlCache = /* @__PURE__ */ new Map();
  function cachedResourceResolve(url) {
    const promiseCached = urlCache.get(url);
    if (promiseCached) {
      return promiseCached;
    }
    const promise = resourceResolver(url).then((response) => unwrapResponse(url, response));
    urlCache.set(url, promise);
    return promise;
  }
  const resolutionPromises = Array.from(currentQueue).map(async ([type, component]) => {
    if (component.styleUrl && component.styleUrls?.length) {
      throw new Error("@Component cannot define both `styleUrl` and `styleUrls`. Use `styleUrl` if the component has one stylesheet, or `styleUrls` if it has multiple");
    }
    const componentTasks = [];
    if (component.templateUrl) {
      componentTasks.push(cachedResourceResolve(component.templateUrl).then((template) => {
        component.template = template;
      }));
    }
    const styles = typeof component.styles === "string" ? [component.styles] : component.styles ?? [];
    component.styles = styles;
    let {
      styleUrl,
      styleUrls
    } = component;
    if (styleUrl) {
      styleUrls = [styleUrl];
      component.styleUrl = void 0;
    }
    if (styleUrls?.length) {
      const allFetched = Promise.all(styleUrls.map((url) => cachedResourceResolve(url))).then((fetchedStyles) => {
        styles.push(...fetchedStyles);
        component.styleUrls = void 0;
      });
      componentTasks.push(allFetched);
    }
    await Promise.all(componentTasks);
    componentDefPendingResolution.delete(type);
  });
  await Promise.all(resolutionPromises);
}
function maybeQueueResolutionOfComponentResources(type, metadata) {
  if (componentNeedsResolution(metadata)) {
    componentResourceResolutionQueue.set(type, metadata);
    componentDefPendingResolution.add(type);
  }
}
function componentNeedsResolution(component) {
  return !!(component.templateUrl && !component.hasOwnProperty("template") || component.styleUrls?.length || component.styleUrl);
}
function isComponentResourceResolutionQueueEmpty() {
  return componentResourceResolutionQueue.size === 0;
}
async function unwrapResponse(url, response) {
  if (typeof response === "string") {
    return response;
  }
  if (response.status !== void 0 && response.status !== 200) {
    throw new RuntimeError(918, ngDevMode && `Could not load resource: ${url}. Response status: ${response.status}`);
  }
  return response.text();
}
var modules = /* @__PURE__ */ new Map();
var checkForDuplicateNgModules = true;
function assertSameOrNotExisting(id, type, incoming) {
  if (type && type !== incoming && checkForDuplicateNgModules) {
    throw new RuntimeError(921, ngDevMode && `Duplicate module registered for ${id} - ${stringify(type)} vs ${stringify(type.name)}`);
  }
}
function registerNgModuleType(ngModuleType, id) {
  const existing = modules.get(id) || null;
  assertSameOrNotExisting(id, existing, ngModuleType);
  modules.set(id, ngModuleType);
}
var NgModuleRef$1 = class NgModuleRef {
};
var NgModuleFactory$1 = class NgModuleFactory {
};
function createNgModule(ngModule, parentInjector) {
  return new NgModuleRef2(ngModule, parentInjector ?? null, []);
}
var NgModuleRef2 = class extends NgModuleRef$1 {
  ngModuleType;
  _parent;
  _bootstrapComponents = [];
  _r3Injector;
  instance;
  destroyCbs = [];
  componentFactoryResolver = new ComponentFactoryResolver2(this);
  constructor(ngModuleType, _parent, additionalProviders, runInjectorInitializers = true) {
    super();
    this.ngModuleType = ngModuleType;
    this._parent = _parent;
    const ngModuleDef = getNgModuleDef(ngModuleType);
    ngDevMode && assertDefined(ngModuleDef, `NgModule '${stringify(ngModuleType)}' is not a subtype of 'NgModuleType'.`);
    this._bootstrapComponents = maybeUnwrapFn(ngModuleDef.bootstrap);
    this._r3Injector = createInjectorWithoutInjectorInstances(ngModuleType, _parent, [{
      provide: NgModuleRef$1,
      useValue: this
    }, {
      provide: ComponentFactoryResolver$1,
      useValue: this.componentFactoryResolver
    }, ...additionalProviders], stringify(ngModuleType), /* @__PURE__ */ new Set(["environment"]));
    if (runInjectorInitializers) {
      this.resolveInjectorInitializers();
    }
  }
  resolveInjectorInitializers() {
    this._r3Injector.resolveInjectorInitializers();
    this.instance = this._r3Injector.get(this.ngModuleType);
  }
  get injector() {
    return this._r3Injector;
  }
  destroy() {
    ngDevMode && assertDefined(this.destroyCbs, "NgModule already destroyed");
    const injector = this._r3Injector;
    !injector.destroyed && injector.destroy();
    this.destroyCbs.forEach((fn) => fn());
    this.destroyCbs = null;
  }
  onDestroy(callback) {
    ngDevMode && assertDefined(this.destroyCbs, "NgModule already destroyed");
    this.destroyCbs.push(callback);
  }
};
var NgModuleFactory2 = class extends NgModuleFactory$1 {
  moduleType;
  constructor(moduleType) {
    super();
    this.moduleType = moduleType;
  }
  create(parentInjector) {
    return new NgModuleRef2(this.moduleType, parentInjector, []);
  }
};
function createNgModuleRefWithProviders(moduleType, parentInjector, additionalProviders) {
  return new NgModuleRef2(moduleType, parentInjector, additionalProviders, false);
}
var EnvironmentNgModuleRefAdapter = class extends NgModuleRef$1 {
  injector;
  componentFactoryResolver = new ComponentFactoryResolver2(this);
  instance = null;
  constructor(config2) {
    super();
    const injector = new R3Injector([...config2.providers, {
      provide: NgModuleRef$1,
      useValue: this
    }, {
      provide: ComponentFactoryResolver$1,
      useValue: this.componentFactoryResolver
    }], config2.parent || getNullInjector(), config2.debugName, /* @__PURE__ */ new Set(["environment"]));
    this.injector = injector;
    if (config2.runEnvironmentInitializers) {
      injector.resolveInjectorInitializers();
    }
  }
  destroy() {
    this.injector.destroy();
  }
  onDestroy(callback) {
    this.injector.onDestroy(callback);
  }
};
function createEnvironmentInjector(providers, parent, debugName = null) {
  const adapter = new EnvironmentNgModuleRefAdapter({
    providers,
    parent,
    debugName,
    runEnvironmentInitializers: true
  });
  return adapter.injector;
}
var StandaloneService = class _StandaloneService {
  _injector;
  cachedInjectors = /* @__PURE__ */ new Map();
  constructor(_injector) {
    this._injector = _injector;
  }
  getOrCreateStandaloneInjector(componentDef) {
    if (!componentDef.standalone) {
      return null;
    }
    if (!this.cachedInjectors.has(componentDef)) {
      const providers = internalImportProvidersFrom(false, componentDef.type);
      const standaloneInjector = providers.length > 0 ? createEnvironmentInjector([providers], this._injector, typeof ngDevMode !== "undefined" && ngDevMode ? `Standalone[${componentDef.type.name}]` : "") : null;
      this.cachedInjectors.set(componentDef, standaloneInjector);
    }
    return this.cachedInjectors.get(componentDef);
  }
  ngOnDestroy() {
    try {
      for (const injector of this.cachedInjectors.values()) {
        if (injector !== null) {
          injector.destroy();
        }
      }
    } finally {
      this.cachedInjectors.clear();
    }
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _StandaloneService,
    providedIn: "environment",
    factory: () => new _StandaloneService(\u0275\u0275inject(EnvironmentInjector))
  });
};
function \u0275\u0275defineComponent(componentDefinition) {
  return noSideEffects(() => {
    (typeof ngDevMode === "undefined" || ngDevMode) && initNgDevMode();
    const baseDef = getNgDirectiveDef(componentDefinition);
    const def = __spreadProps(__spreadValues({}, baseDef), {
      decls: componentDefinition.decls,
      vars: componentDefinition.vars,
      template: componentDefinition.template,
      consts: componentDefinition.consts || null,
      ngContentSelectors: componentDefinition.ngContentSelectors,
      onPush: componentDefinition.changeDetection === ChangeDetectionStrategy.OnPush,
      directiveDefs: null,
      pipeDefs: null,
      dependencies: baseDef.standalone && componentDefinition.dependencies || null,
      getStandaloneInjector: baseDef.standalone ? (parentInjector) => {
        return parentInjector.get(StandaloneService).getOrCreateStandaloneInjector(def);
      } : null,
      getExternalStyles: null,
      signals: componentDefinition.signals ?? false,
      data: componentDefinition.data || {},
      encapsulation: componentDefinition.encapsulation || ViewEncapsulation.Emulated,
      styles: componentDefinition.styles || EMPTY_ARRAY,
      _: null,
      schemas: componentDefinition.schemas || null,
      tView: null,
      id: ""
    });
    if (baseDef.standalone) {
      performanceMarkFeature("NgStandalone");
    }
    initFeatures(def);
    const dependencies = componentDefinition.dependencies;
    def.directiveDefs = extractDefListOrFactory(dependencies, extractDirectiveDef);
    def.pipeDefs = extractDefListOrFactory(dependencies, getPipeDef);
    def.id = getComponentId(def);
    return def;
  });
}
function extractDirectiveDef(type) {
  return getComponentDef(type) || getDirectiveDef(type);
}
function \u0275\u0275defineNgModule(def) {
  return noSideEffects(() => {
    const res = {
      type: def.type,
      bootstrap: def.bootstrap || EMPTY_ARRAY,
      declarations: def.declarations || EMPTY_ARRAY,
      imports: def.imports || EMPTY_ARRAY,
      exports: def.exports || EMPTY_ARRAY,
      transitiveCompileScopes: null,
      schemas: def.schemas || null,
      id: def.id || null
    };
    return res;
  });
}
function parseAndConvertInputsForDefinition(obj, declaredInputs) {
  if (obj == null) return EMPTY_OBJ;
  const newLookup = {};
  for (const minifiedKey in obj) {
    if (obj.hasOwnProperty(minifiedKey)) {
      const value = obj[minifiedKey];
      let publicName;
      let declaredName;
      let inputFlags;
      let transform;
      if (Array.isArray(value)) {
        inputFlags = value[0];
        publicName = value[1];
        declaredName = value[2] ?? publicName;
        transform = value[3] || null;
      } else {
        publicName = value;
        declaredName = value;
        inputFlags = InputFlags.None;
        transform = null;
      }
      newLookup[publicName] = [minifiedKey, inputFlags, transform];
      declaredInputs[publicName] = declaredName;
    }
  }
  return newLookup;
}
function parseAndConvertOutputsForDefinition(obj) {
  if (obj == null) return EMPTY_OBJ;
  const newLookup = {};
  for (const minifiedKey in obj) {
    if (obj.hasOwnProperty(minifiedKey)) {
      newLookup[obj[minifiedKey]] = minifiedKey;
    }
  }
  return newLookup;
}
function \u0275\u0275defineDirective(directiveDefinition) {
  return noSideEffects(() => {
    const def = getNgDirectiveDef(directiveDefinition);
    initFeatures(def);
    return def;
  });
}
function \u0275\u0275definePipe(pipeDef) {
  return {
    type: pipeDef.type,
    name: pipeDef.name,
    factory: null,
    pure: pipeDef.pure !== false,
    standalone: pipeDef.standalone ?? true,
    onDestroy: pipeDef.type.prototype.ngOnDestroy || null
  };
}
function getNgDirectiveDef(directiveDefinition) {
  const declaredInputs = {};
  return {
    type: directiveDefinition.type,
    providersResolver: null,
    viewProvidersResolver: null,
    factory: null,
    hostBindings: directiveDefinition.hostBindings || null,
    hostVars: directiveDefinition.hostVars || 0,
    hostAttrs: directiveDefinition.hostAttrs || null,
    contentQueries: directiveDefinition.contentQueries || null,
    declaredInputs,
    inputConfig: directiveDefinition.inputs || EMPTY_OBJ,
    exportAs: directiveDefinition.exportAs || null,
    standalone: directiveDefinition.standalone ?? true,
    signals: directiveDefinition.signals === true,
    selectors: directiveDefinition.selectors || EMPTY_ARRAY,
    viewQuery: directiveDefinition.viewQuery || null,
    features: directiveDefinition.features || null,
    setInput: null,
    resolveHostDirectives: null,
    hostDirectives: null,
    controlDef: null,
    inputs: parseAndConvertInputsForDefinition(directiveDefinition.inputs, declaredInputs),
    outputs: parseAndConvertOutputsForDefinition(directiveDefinition.outputs),
    debugInfo: null
  };
}
function initFeatures(definition) {
  definition.features?.forEach((fn) => fn(definition));
}
function extractDefListOrFactory(dependencies, defExtractor) {
  if (!dependencies) {
    return null;
  }
  return () => {
    const resolvedDependencies = typeof dependencies === "function" ? dependencies() : dependencies;
    const result = [];
    for (const dep of resolvedDependencies) {
      const definition = defExtractor(dep);
      if (definition !== null) {
        result.push(definition);
      }
    }
    return result;
  };
}
var GENERATED_COMP_IDS = /* @__PURE__ */ new Map();
function getComponentId(componentDef) {
  let hash = 0;
  const componentDefConsts = typeof componentDef.consts === "function" ? "" : componentDef.consts;
  const hashSelectors = [componentDef.selectors, componentDef.ngContentSelectors, componentDef.hostVars, componentDef.hostAttrs, componentDefConsts, componentDef.vars, componentDef.decls, componentDef.encapsulation, componentDef.standalone, componentDef.signals, componentDef.exportAs, JSON.stringify(componentDef.inputs), JSON.stringify(componentDef.outputs), Object.getOwnPropertyNames(componentDef.type.prototype), !!componentDef.contentQueries, !!componentDef.viewQuery];
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    for (const item of hashSelectors) {
      assertNotEqual(typeof item, "function", "Internal error: attempting to use a function in component id computation logic.");
    }
  }
  for (const char of hashSelectors.join("|")) {
    hash = Math.imul(31, hash) + char.charCodeAt(0) << 0;
  }
  hash += 2147483647 + 1;
  const compId = "c" + hash;
  if ((typeof ngDevMode === "undefined" || ngDevMode) && true) {
    if (GENERATED_COMP_IDS.has(compId)) {
      const previousCompDefType = GENERATED_COMP_IDS.get(compId);
      if (previousCompDefType !== componentDef.type) {
        console.warn(formatRuntimeError(-912, `Component ID generation collision detected. Components '${previousCompDefType.name}' and '${componentDef.type.name}' with selector '${stringifyCSSSelectorList(componentDef.selectors)}' generated the same component ID. To fix this, you can change the selector of one of those components or add an extra host attribute to force a different ID.`));
      }
    } else {
      GENERATED_COMP_IDS.set(compId, componentDef.type);
    }
  }
  return compId;
}
function \u0275\u0275ControlFeature(passThroughInput) {
  return (definition) => {
    definition.controlDef = {
      create: (inst, host) => {
        inst?.\u0275ngControlCreate(host);
      },
      update: (inst, host) => {
        inst?.\u0275ngControlUpdate?.(host);
      },
      passThroughInput
    };
  };
}
function \u0275\u0275HostDirectivesFeature(rawHostDirectives) {
  const feature = (definition) => {
    const isEager = Array.isArray(rawHostDirectives);
    if (definition.hostDirectives === null) {
      definition.resolveHostDirectives = resolveHostDirectives;
      definition.hostDirectives = isEager ? rawHostDirectives.map(createHostDirectiveDef) : [rawHostDirectives];
    } else if (isEager) {
      definition.hostDirectives.unshift(...rawHostDirectives.map(createHostDirectiveDef));
    } else {
      definition.hostDirectives.unshift(rawHostDirectives);
    }
  };
  feature.ngInherit = true;
  return feature;
}
function resolveHostDirectives(matches) {
  const allDirectiveDefs = [];
  let hasComponent = false;
  let hostDirectiveDefs = null;
  let hostDirectiveRanges = null;
  for (let i = 0; i < matches.length; i++) {
    const def = matches[i];
    if (def.hostDirectives !== null) {
      const start = allDirectiveDefs.length;
      hostDirectiveDefs ??= /* @__PURE__ */ new Map();
      hostDirectiveRanges ??= /* @__PURE__ */ new Map();
      findHostDirectiveDefs(def, allDirectiveDefs, hostDirectiveDefs);
      hostDirectiveRanges.set(def, [start, allDirectiveDefs.length - 1]);
    }
    if (i === 0 && isComponentDef(def)) {
      hasComponent = true;
      allDirectiveDefs.push(def);
    }
  }
  for (let i = hasComponent ? 1 : 0; i < matches.length; i++) {
    allDirectiveDefs.push(matches[i]);
  }
  return [allDirectiveDefs, hostDirectiveDefs, hostDirectiveRanges];
}
function findHostDirectiveDefs(currentDef, matchedDefs, hostDirectiveDefs) {
  if (currentDef.hostDirectives !== null) {
    for (const configOrFn of currentDef.hostDirectives) {
      if (typeof configOrFn === "function") {
        const resolved = configOrFn();
        for (const config2 of resolved) {
          trackHostDirectiveDef(createHostDirectiveDef(config2), matchedDefs, hostDirectiveDefs);
        }
      } else {
        trackHostDirectiveDef(configOrFn, matchedDefs, hostDirectiveDefs);
      }
    }
  }
}
function trackHostDirectiveDef(def, matchedDefs, hostDirectiveDefs) {
  const hostDirectiveDef = getDirectiveDef(def.directive);
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    validateHostDirective(def, hostDirectiveDef);
  }
  patchDeclaredInputs(hostDirectiveDef.declaredInputs, def.inputs);
  findHostDirectiveDefs(hostDirectiveDef, matchedDefs, hostDirectiveDefs);
  hostDirectiveDefs.set(hostDirectiveDef, def);
  matchedDefs.push(hostDirectiveDef);
}
function createHostDirectiveDef(config2) {
  return typeof config2 === "function" ? {
    directive: resolveForwardRef(config2),
    inputs: EMPTY_OBJ,
    outputs: EMPTY_OBJ
  } : {
    directive: resolveForwardRef(config2.directive),
    inputs: bindingArrayToMap(config2.inputs),
    outputs: bindingArrayToMap(config2.outputs)
  };
}
function bindingArrayToMap(bindings) {
  if (bindings === void 0 || bindings.length === 0) {
    return EMPTY_OBJ;
  }
  const result = {};
  for (let i = 0; i < bindings.length; i += 2) {
    result[bindings[i]] = bindings[i + 1];
  }
  return result;
}
function patchDeclaredInputs(declaredInputs, exposedInputs) {
  for (const publicName in exposedInputs) {
    if (exposedInputs.hasOwnProperty(publicName)) {
      const remappedPublicName = exposedInputs[publicName];
      const privateName = declaredInputs[publicName];
      if ((typeof ngDevMode === "undefined" || ngDevMode) && declaredInputs.hasOwnProperty(remappedPublicName)) {
        assertEqual(declaredInputs[remappedPublicName], declaredInputs[publicName], `Conflicting host directive input alias ${publicName}.`);
      }
      declaredInputs[remappedPublicName] = privateName;
    }
  }
}
function validateHostDirective(hostDirectiveConfig, directiveDef) {
  const type = hostDirectiveConfig.directive;
  if (directiveDef === null) {
    if (getComponentDef(type) !== null) {
      throw new RuntimeError(310, `Host directive ${type.name} cannot be a component.`);
    }
    throw new RuntimeError(307, `Could not resolve metadata for host directive ${type.name}. Make sure that the ${type.name} class is annotated with an @Directive decorator.`);
  }
  if (!directiveDef.standalone) {
    throw new RuntimeError(308, `Host directive ${directiveDef.type.name} must be standalone.`);
  }
  validateMappings("input", directiveDef, hostDirectiveConfig.inputs);
  validateMappings("output", directiveDef, hostDirectiveConfig.outputs);
}
function validateMappings(bindingType, def, hostDirectiveBindings) {
  const className = def.type.name;
  const bindings = bindingType === "input" ? def.inputs : def.outputs;
  for (const publicName in hostDirectiveBindings) {
    if (hostDirectiveBindings.hasOwnProperty(publicName)) {
      if (!bindings.hasOwnProperty(publicName)) {
        throw new RuntimeError(311, `Directive ${className} does not have an ${bindingType} with a public name of ${publicName}.`);
      }
      const remappedPublicName = hostDirectiveBindings[publicName];
      if (bindings.hasOwnProperty(remappedPublicName) && remappedPublicName !== publicName) {
        throw new RuntimeError(312, `Cannot alias ${bindingType} ${publicName} of host directive ${className} to ${remappedPublicName}, because it already has a different ${bindingType} with the same public name.`);
      }
    }
  }
}
function getSuperType(type) {
  return Object.getPrototypeOf(type.prototype).constructor;
}
function \u0275\u0275InheritDefinitionFeature(definition) {
  let superType = getSuperType(definition.type);
  let shouldInheritFields = true;
  const inheritanceChain = [definition];
  while (superType && superType !== Function.prototype && superType !== Object.prototype) {
    let superDef = void 0;
    const cmpDef = Object.hasOwn(superType, NG_COMP_DEF) ? superType[NG_COMP_DEF] : void 0;
    const dirDef = Object.hasOwn(superType, NG_DIR_DEF) ? superType[NG_DIR_DEF] : void 0;
    if (isComponentDef(definition)) {
      superDef = cmpDef ?? dirDef;
    } else {
      if (cmpDef) {
        throw new RuntimeError(903, ngDevMode && `Directives cannot inherit Components. Directive ${stringifyForError(definition.type)} is attempting to extend component ${stringifyForError(superType)}`);
      }
      superDef = dirDef;
    }
    if (superDef) {
      if (shouldInheritFields) {
        inheritanceChain.push(superDef);
        const writeableDef = definition;
        writeableDef.inputs = maybeUnwrapEmpty(definition.inputs);
        writeableDef.declaredInputs = maybeUnwrapEmpty(definition.declaredInputs);
        writeableDef.outputs = maybeUnwrapEmpty(definition.outputs);
        const superHostBindings = superDef.hostBindings;
        superHostBindings && inheritHostBindings(definition, superHostBindings);
        const superViewQuery = superDef.viewQuery;
        const superContentQueries = superDef.contentQueries;
        superViewQuery && inheritViewQuery(definition, superViewQuery);
        superContentQueries && inheritContentQueries(definition, superContentQueries);
        mergeInputsWithTransforms(definition, superDef);
        fillProperties(definition.outputs, superDef.outputs);
        if (isComponentDef(superDef) && superDef.data.animation) {
          const defData = definition.data;
          defData.animation = (defData.animation || []).concat(superDef.data.animation);
        }
      }
      const features = superDef.features;
      if (features) {
        for (let i = 0; i < features.length; i++) {
          const feature = features[i];
          if (feature && feature.ngInherit) {
            feature(definition);
          }
          if (feature === \u0275\u0275InheritDefinitionFeature) {
            shouldInheritFields = false;
          }
        }
      }
    }
    superType = Object.getPrototypeOf(superType);
  }
  mergeHostAttrsAcrossInheritance(inheritanceChain);
}
function mergeInputsWithTransforms(target, source) {
  for (const key in source.inputs) {
    if (!source.inputs.hasOwnProperty(key)) {
      continue;
    }
    if (target.inputs.hasOwnProperty(key)) {
      continue;
    }
    const value = source.inputs[key];
    if (value !== void 0) {
      target.inputs[key] = value;
      target.declaredInputs[key] = source.declaredInputs[key];
    }
  }
}
function mergeHostAttrsAcrossInheritance(inheritanceChain) {
  let hostVars = 0;
  let hostAttrs = null;
  for (let i = inheritanceChain.length - 1; i >= 0; i--) {
    const def = inheritanceChain[i];
    def.hostVars = hostVars += def.hostVars;
    def.hostAttrs = mergeHostAttrs(def.hostAttrs, hostAttrs = mergeHostAttrs(hostAttrs, def.hostAttrs));
  }
}
function maybeUnwrapEmpty(value) {
  if (value === EMPTY_OBJ) {
    return {};
  } else if (value === EMPTY_ARRAY) {
    return [];
  } else {
    return value;
  }
}
function inheritViewQuery(definition, superViewQuery) {
  const prevViewQuery = definition.viewQuery;
  if (prevViewQuery) {
    definition.viewQuery = (rf, ctx) => {
      superViewQuery(rf, ctx);
      prevViewQuery(rf, ctx);
    };
  } else {
    definition.viewQuery = superViewQuery;
  }
}
function inheritContentQueries(definition, superContentQueries) {
  const prevContentQueries = definition.contentQueries;
  if (prevContentQueries) {
    definition.contentQueries = (rf, ctx, directiveIndex) => {
      superContentQueries(rf, ctx, directiveIndex);
      prevContentQueries(rf, ctx, directiveIndex);
    };
  } else {
    definition.contentQueries = superContentQueries;
  }
}
function inheritHostBindings(definition, superHostBindings) {
  const prevHostBindings = definition.hostBindings;
  if (prevHostBindings) {
    definition.hostBindings = (rf, ctx) => {
      superHostBindings(rf, ctx);
      prevHostBindings(rf, ctx);
    };
  } else {
    definition.hostBindings = superHostBindings;
  }
}
function templateCreate(tNode, declarationLView, declarationTView, index, templateFn, decls, vars, flags) {
  if (declarationTView.firstCreatePass) {
    tNode.mergedAttrs = mergeHostAttrs(tNode.mergedAttrs, tNode.attrs);
    const embeddedTView = tNode.tView = createTView(2, tNode, templateFn, decls, vars, declarationTView.directiveRegistry, declarationTView.pipeRegistry, null, declarationTView.schemas, declarationTView.consts, null);
    if (declarationTView.queries !== null) {
      declarationTView.queries.template(declarationTView, tNode);
      embeddedTView.queries = declarationTView.queries.embeddedTView(tNode);
    }
  }
  if (flags) {
    tNode.flags |= flags;
  }
  setCurrentTNode(tNode, false);
  const comment = _locateOrCreateContainerAnchor(declarationTView, declarationLView, tNode, index);
  if (wasLastNodeCreated()) {
    appendChild(declarationTView, declarationLView, comment, tNode);
  }
  attachPatchData(comment, declarationLView);
  const lContainer = createLContainer(comment, declarationLView, comment, tNode);
  declarationLView[index + HEADER_OFFSET] = lContainer;
  addToEndOfViewTree(declarationLView, lContainer);
  populateDehydratedViewsInLContainer(lContainer, tNode, declarationLView);
}
function declareDirectiveHostTemplate(declarationLView, declarationTView, index, templateFn, decls, vars, tagName, attrs, flags, localRefsIndex, localRefExtractor) {
  const adjustedIndex = index + HEADER_OFFSET;
  let tNode;
  if (declarationTView.firstCreatePass) {
    tNode = getOrCreateTNode(declarationTView, adjustedIndex, 4, tagName || null, attrs || null);
    if (getBindingsEnabled()) {
      resolveDirectives(declarationTView, declarationLView, tNode, getConstant(declarationTView.consts, localRefsIndex), findDirectiveDefMatches);
    }
    registerPostOrderHooks(declarationTView, tNode);
  } else {
    tNode = declarationTView.data[adjustedIndex];
  }
  templateCreate(tNode, declarationLView, declarationTView, index, templateFn, decls, vars, flags);
  if (isDirectiveHost(tNode)) {
    createDirectivesInstances(declarationTView, declarationLView, tNode);
  }
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(declarationLView, tNode, localRefExtractor);
  }
  return tNode;
}
function declareNoDirectiveHostTemplate(declarationLView, declarationTView, index, templateFn, decls, vars, tagName, attrs, flags, localRefsIndex, localRefExtractor) {
  const adjustedIndex = index + HEADER_OFFSET;
  let tNode;
  if (declarationTView.firstCreatePass) {
    tNode = getOrCreateTNode(declarationTView, adjustedIndex, 4, tagName || null, attrs || null);
    if (localRefsIndex != null) {
      const refs = getConstant(declarationTView.consts, localRefsIndex);
      tNode.localNames = [];
      for (let i = 0; i < refs.length; i += 2) {
        tNode.localNames.push(refs[i], -1);
      }
    }
  } else {
    tNode = declarationTView.data[adjustedIndex];
  }
  templateCreate(tNode, declarationLView, declarationTView, index, templateFn, decls, vars, flags);
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(declarationLView, tNode, localRefExtractor);
  }
  return tNode;
}
function \u0275\u0275template(index, templateFn, decls, vars, tagName, attrsIndex, localRefsIndex, localRefExtractor) {
  const lView = getLView();
  const tView = getTView();
  const attrs = getConstant(tView.consts, attrsIndex);
  declareDirectiveHostTemplate(lView, tView, index, templateFn, decls, vars, tagName, attrs, void 0, localRefsIndex, localRefExtractor);
  return \u0275\u0275template;
}
function \u0275\u0275domTemplate(index, templateFn, decls, vars, tagName, attrsIndex, localRefsIndex, localRefExtractor) {
  const lView = getLView();
  const tView = getTView();
  const attrs = getConstant(tView.consts, attrsIndex);
  declareNoDirectiveHostTemplate(lView, tView, index, templateFn, decls, vars, tagName, attrs, void 0, localRefsIndex, localRefExtractor);
  return \u0275\u0275domTemplate;
}
var _locateOrCreateContainerAnchor = createContainerAnchorImpl;
function createContainerAnchorImpl(tView, lView, tNode, index) {
  lastNodeWasCreated(true);
  return lView[RENDERER].createComment(ngDevMode ? "container" : "");
}
var DeferDependenciesLoadingState;
(function(DeferDependenciesLoadingState2) {
  DeferDependenciesLoadingState2[DeferDependenciesLoadingState2["NOT_STARTED"] = 0] = "NOT_STARTED";
  DeferDependenciesLoadingState2[DeferDependenciesLoadingState2["IN_PROGRESS"] = 1] = "IN_PROGRESS";
  DeferDependenciesLoadingState2[DeferDependenciesLoadingState2["COMPLETE"] = 2] = "COMPLETE";
  DeferDependenciesLoadingState2[DeferDependenciesLoadingState2["FAILED"] = 3] = "FAILED";
})(DeferDependenciesLoadingState || (DeferDependenciesLoadingState = {}));
var MINIMUM_SLOT = 0;
var LOADING_AFTER_SLOT = 1;
var DeferBlockState;
(function(DeferBlockState2) {
  DeferBlockState2[DeferBlockState2["Placeholder"] = 0] = "Placeholder";
  DeferBlockState2[DeferBlockState2["Loading"] = 1] = "Loading";
  DeferBlockState2[DeferBlockState2["Complete"] = 2] = "Complete";
  DeferBlockState2[DeferBlockState2["Error"] = 3] = "Error";
})(DeferBlockState || (DeferBlockState = {}));
var DeferBlockInternalState;
(function(DeferBlockInternalState2) {
  DeferBlockInternalState2[DeferBlockInternalState2["Initial"] = -1] = "Initial";
})(DeferBlockInternalState || (DeferBlockInternalState = {}));
var NEXT_DEFER_BLOCK_STATE = 0;
var DEFER_BLOCK_STATE = 1;
var STATE_IS_FROZEN_UNTIL = 2;
var LOADING_AFTER_CLEANUP_FN = 3;
var TRIGGER_CLEANUP_FNS = 4;
var PREFETCH_TRIGGER_CLEANUP_FNS = 5;
var SSR_UNIQUE_ID = 6;
var SSR_BLOCK_STATE = 7;
var ON_COMPLETE_FNS = 8;
var HYDRATE_TRIGGER_CLEANUP_FNS = 9;
var DeferBlockBehavior;
(function(DeferBlockBehavior2) {
  DeferBlockBehavior2[DeferBlockBehavior2["Manual"] = 0] = "Manual";
  DeferBlockBehavior2[DeferBlockBehavior2["Playthrough"] = 1] = "Playthrough";
})(DeferBlockBehavior || (DeferBlockBehavior = {}));
function storeTriggerCleanupFn(type, lDetails, cleanupFn) {
  const key = getCleanupFnKeyByType(type);
  if (lDetails[key] === null) {
    lDetails[key] = [];
  }
  lDetails[key].push(cleanupFn);
}
function invokeTriggerCleanupFns(type, lDetails) {
  const key = getCleanupFnKeyByType(type);
  const cleanupFns = lDetails[key];
  if (cleanupFns !== null) {
    for (const cleanupFn of cleanupFns) {
      cleanupFn();
    }
    lDetails[key] = null;
  }
}
function invokeAllTriggerCleanupFns(lDetails) {
  invokeTriggerCleanupFns(1, lDetails);
  invokeTriggerCleanupFns(0, lDetails);
  invokeTriggerCleanupFns(2, lDetails);
}
function getCleanupFnKeyByType(type) {
  let key = TRIGGER_CLEANUP_FNS;
  if (type === 1) {
    key = PREFETCH_TRIGGER_CLEANUP_FNS;
  } else if (type === 2) {
    key = HYDRATE_TRIGGER_CLEANUP_FNS;
  }
  return key;
}
function getDeferBlockDataIndex(deferBlockIndex) {
  return deferBlockIndex + 1;
}
function getLDeferBlockDetails(lView, tNode) {
  const tView = lView[TVIEW];
  const slotIndex = getDeferBlockDataIndex(tNode.index);
  ngDevMode && assertIndexInDeclRange(tView, slotIndex);
  return lView[slotIndex];
}
function setLDeferBlockDetails(lView, deferBlockIndex, lDetails) {
  const tView = lView[TVIEW];
  const slotIndex = getDeferBlockDataIndex(deferBlockIndex);
  ngDevMode && assertIndexInDeclRange(tView, slotIndex);
  lView[slotIndex] = lDetails;
}
function getTDeferBlockDetails(tView, tNode) {
  const slotIndex = getDeferBlockDataIndex(tNode.index);
  ngDevMode && assertIndexInDeclRange(tView, slotIndex);
  return tView.data[slotIndex];
}
function setTDeferBlockDetails(tView, deferBlockIndex, deferBlockConfig) {
  const slotIndex = getDeferBlockDataIndex(deferBlockIndex);
  ngDevMode && assertIndexInDeclRange(tView, slotIndex);
  tView.data[slotIndex] = deferBlockConfig;
}
function getTemplateIndexForState(newState, hostLView, tNode) {
  const tView = hostLView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  switch (newState) {
    case DeferBlockState.Complete:
      return tDetails.primaryTmplIndex;
    case DeferBlockState.Loading:
      return tDetails.loadingTmplIndex;
    case DeferBlockState.Error:
      return tDetails.errorTmplIndex;
    case DeferBlockState.Placeholder:
      return tDetails.placeholderTmplIndex;
    default:
      ngDevMode && throwError2(`Unexpected defer block state: ${newState}`);
      return null;
  }
}
function getMinimumDurationForState(tDetails, currentState) {
  if (currentState === DeferBlockState.Placeholder) {
    return tDetails.placeholderBlockConfig?.[MINIMUM_SLOT] ?? null;
  } else if (currentState === DeferBlockState.Loading) {
    return tDetails.loadingBlockConfig?.[MINIMUM_SLOT] ?? null;
  }
  return null;
}
function getLoadingBlockAfter(tDetails) {
  return tDetails.loadingBlockConfig?.[LOADING_AFTER_SLOT] ?? null;
}
function addDepsToRegistry(currentDeps, newDeps) {
  if (!currentDeps || currentDeps.length === 0) {
    return newDeps;
  }
  const currentDepSet = new Set(currentDeps);
  for (const dep of newDeps) {
    currentDepSet.add(dep);
  }
  return currentDeps.length === currentDepSet.size ? currentDeps : Array.from(currentDepSet);
}
function getPrimaryBlockTNode(tView, tDetails) {
  const adjustedIndex = tDetails.primaryTmplIndex + HEADER_OFFSET;
  return getTNode(tView, adjustedIndex);
}
function assertDeferredDependenciesLoaded(tDetails) {
  assertEqual(tDetails.loadingState, DeferDependenciesLoadingState.COMPLETE, "Expecting all deferred dependencies to be loaded.");
}
function isTDeferBlockDetails(value) {
  return value !== null && typeof value === "object" && typeof value.primaryTmplIndex === "number";
}
function trackTriggerForDebugging(tView, tNode, textRepresentation) {
  const tDetails = getTDeferBlockDetails(tView, tNode);
  tDetails.debug ??= {};
  tDetails.debug.triggers ??= /* @__PURE__ */ new Set();
  tDetails.debug.triggers.add(textRepresentation);
}
function onViewportWrapper(trigger, callback, injector, wrapperOptions) {
  const ngZone = injector.get(NgZone);
  return onViewport(trigger, () => ngZone.run(callback), (options) => ngZone.runOutsideAngular(() => createIntersectionObserver(options)), wrapperOptions);
}
function getTriggerLView(deferredHostLView, deferredTNode, walkUpTimes) {
  if (walkUpTimes == null) {
    return deferredHostLView;
  }
  if (walkUpTimes >= 0) {
    return walkUpViews(walkUpTimes, deferredHostLView);
  }
  const deferredContainer = deferredHostLView[deferredTNode.index];
  ngDevMode && assertLContainer(deferredContainer);
  const triggerLView = deferredContainer[CONTAINER_HEADER_OFFSET] ?? null;
  if (ngDevMode && triggerLView !== null) {
    const lDetails = getLDeferBlockDetails(deferredHostLView, deferredTNode);
    const renderedState = lDetails[DEFER_BLOCK_STATE];
    assertEqual(renderedState, DeferBlockState.Placeholder, "Expected a placeholder to be rendered in this defer block.");
    assertLView(triggerLView);
  }
  return triggerLView;
}
function getTriggerElement(triggerLView, triggerIndex) {
  const element = getNativeByIndex(HEADER_OFFSET + triggerIndex, triggerLView);
  ngDevMode && assertElement(element);
  return element;
}
function registerDomTrigger(initialLView, tNode, triggerIndex, walkUpTimes, registerFn, callback, type, options) {
  const injector = initialLView[INJECTOR];
  const zone = injector.get(NgZone);
  let poll;
  function pollDomTrigger() {
    if (isDestroyed(initialLView)) {
      poll.destroy();
      return;
    }
    const lDetails = getLDeferBlockDetails(initialLView, tNode);
    const renderedState = lDetails[DEFER_BLOCK_STATE];
    if (renderedState !== DeferBlockInternalState.Initial && renderedState !== DeferBlockState.Placeholder) {
      poll.destroy();
      return;
    }
    const triggerLView = getTriggerLView(initialLView, tNode, walkUpTimes);
    if (!triggerLView) {
      return;
    }
    poll.destroy();
    if (isDestroyed(triggerLView)) {
      return;
    }
    const element = getTriggerElement(triggerLView, triggerIndex);
    const cleanup = registerFn(element, () => {
      zone.run(() => {
        if (initialLView !== triggerLView) {
          removeLViewOnDestroy(triggerLView, cleanup);
        }
        callback();
      });
    }, injector, options);
    if (initialLView !== triggerLView) {
      storeLViewOnDestroy(triggerLView, cleanup);
    }
    storeTriggerCleanupFn(type, lDetails, cleanup);
  }
  poll = afterEveryRender({
    read: pollDomTrigger
  }, {
    injector
  });
}
function onIdle(callback, injector) {
  const scheduler = injector.get(IdleScheduler);
  const cleanupFn = () => scheduler.remove(callback);
  scheduler.add(callback);
  return cleanupFn;
}
var _requestIdleCallback = () => typeof requestIdleCallback !== "undefined" ? requestIdleCallback : setTimeout;
var _cancelIdleCallback = () => typeof requestIdleCallback !== "undefined" ? cancelIdleCallback : clearTimeout;
var IdleScheduler = class _IdleScheduler {
  executingCallbacks = false;
  idleId = null;
  current = /* @__PURE__ */ new Set();
  deferred = /* @__PURE__ */ new Set();
  ngZone = inject2(NgZone);
  requestIdleCallbackFn = _requestIdleCallback().bind(globalThis);
  cancelIdleCallbackFn = _cancelIdleCallback().bind(globalThis);
  add(callback) {
    const target = this.executingCallbacks ? this.deferred : this.current;
    target.add(callback);
    if (this.idleId === null) {
      this.scheduleIdleCallback();
    }
  }
  remove(callback) {
    const {
      current,
      deferred
    } = this;
    current.delete(callback);
    deferred.delete(callback);
    if (current.size === 0 && deferred.size === 0) {
      this.cancelIdleCallback();
    }
  }
  scheduleIdleCallback() {
    const callback = () => {
      this.cancelIdleCallback();
      this.executingCallbacks = true;
      for (const callback2 of this.current) {
        callback2();
      }
      this.current.clear();
      this.executingCallbacks = false;
      if (this.deferred.size > 0) {
        for (const callback2 of this.deferred) {
          this.current.add(callback2);
        }
        this.deferred.clear();
        this.scheduleIdleCallback();
      }
    };
    this.idleId = this.requestIdleCallbackFn(() => this.ngZone.run(callback));
  }
  cancelIdleCallback() {
    if (this.idleId !== null) {
      this.cancelIdleCallbackFn(this.idleId);
      this.idleId = null;
    }
  }
  ngOnDestroy() {
    this.cancelIdleCallback();
    this.current.clear();
    this.deferred.clear();
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _IdleScheduler,
    providedIn: "root",
    factory: () => new _IdleScheduler()
  });
};
function onTimer(delay) {
  return (callback, injector) => scheduleTimerTrigger(delay, callback, injector);
}
function scheduleTimerTrigger(delay, callback, injector) {
  const scheduler = injector.get(TimerScheduler);
  const ngZone = injector.get(NgZone);
  const cleanupFn = () => scheduler.remove(callback);
  scheduler.add(delay, callback, ngZone);
  return cleanupFn;
}
var TimerScheduler = class _TimerScheduler {
  executingCallbacks = false;
  timeoutId = null;
  invokeTimerAt = null;
  current = [];
  deferred = [];
  add(delay, callback, ngZone) {
    const target = this.executingCallbacks ? this.deferred : this.current;
    this.addToQueue(target, Date.now() + delay, callback);
    this.scheduleTimer(ngZone);
  }
  remove(callback) {
    const {
      current,
      deferred
    } = this;
    const callbackIndex = this.removeFromQueue(current, callback);
    if (callbackIndex === -1) {
      this.removeFromQueue(deferred, callback);
    }
    if (current.length === 0 && deferred.length === 0) {
      this.clearTimeout();
    }
  }
  addToQueue(target, invokeAt, callback) {
    let insertAtIndex = target.length;
    for (let i = 0; i < target.length; i += 2) {
      const invokeQueuedCallbackAt = target[i];
      if (invokeQueuedCallbackAt > invokeAt) {
        insertAtIndex = i;
        break;
      }
    }
    arrayInsert2(target, insertAtIndex, invokeAt, callback);
  }
  removeFromQueue(target, callback) {
    let index = -1;
    for (let i = 0; i < target.length; i += 2) {
      const queuedCallback = target[i + 1];
      if (queuedCallback === callback) {
        index = i;
        break;
      }
    }
    if (index > -1) {
      arraySplice(target, index, 2);
    }
    return index;
  }
  scheduleTimer(ngZone) {
    const callback = () => {
      this.clearTimeout();
      this.executingCallbacks = true;
      const current = [...this.current];
      const now = Date.now();
      for (let i = 0; i < current.length; i += 2) {
        const invokeAt = current[i];
        const callback2 = current[i + 1];
        if (invokeAt <= now) {
          callback2();
        } else {
          break;
        }
      }
      let lastCallbackIndex = -1;
      for (let i = 0; i < this.current.length; i += 2) {
        const invokeAt = this.current[i];
        if (invokeAt <= now) {
          lastCallbackIndex = i + 1;
        } else {
          break;
        }
      }
      if (lastCallbackIndex >= 0) {
        arraySplice(this.current, 0, lastCallbackIndex + 1);
      }
      this.executingCallbacks = false;
      if (this.deferred.length > 0) {
        for (let i = 0; i < this.deferred.length; i += 2) {
          const invokeAt = this.deferred[i];
          const callback2 = this.deferred[i + 1];
          this.addToQueue(this.current, invokeAt, callback2);
        }
        this.deferred.length = 0;
      }
      this.scheduleTimer(ngZone);
    };
    const FRAME_DURATION_MS = 16;
    if (this.current.length > 0) {
      const now = Date.now();
      const invokeAt = this.current[0];
      if (this.timeoutId === null || this.invokeTimerAt && this.invokeTimerAt - invokeAt > FRAME_DURATION_MS) {
        this.clearTimeout();
        const timeout = Math.max(invokeAt - now, FRAME_DURATION_MS);
        this.invokeTimerAt = invokeAt;
        this.timeoutId = ngZone.runOutsideAngular(() => {
          return setTimeout(() => ngZone.run(callback), timeout);
        });
      }
    }
  }
  clearTimeout() {
    if (this.timeoutId !== null) {
      clearTimeout(this.timeoutId);
      this.timeoutId = null;
    }
  }
  ngOnDestroy() {
    this.clearTimeout();
    this.current.length = 0;
    this.deferred.length = 0;
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TimerScheduler,
    providedIn: "root",
    factory: () => new _TimerScheduler()
  });
};
var CachedInjectorService = class _CachedInjectorService {
  cachedInjectors = /* @__PURE__ */ new Map();
  getOrCreateInjector(key, parentInjector, providers, debugName) {
    if (!this.cachedInjectors.has(key)) {
      const injector = providers.length > 0 ? createEnvironmentInjector(providers, parentInjector, debugName) : null;
      this.cachedInjectors.set(key, injector);
    }
    return this.cachedInjectors.get(key);
  }
  ngOnDestroy() {
    try {
      for (const injector of this.cachedInjectors.values()) {
        if (injector !== null) {
          injector.destroy();
        }
      }
    } finally {
      this.cachedInjectors.clear();
    }
  }
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _CachedInjectorService,
    providedIn: "environment",
    factory: () => new _CachedInjectorService()
  });
};
var DEFER_BLOCK_DEPENDENCY_INTERCEPTOR = /* @__PURE__ */ new InjectionToken("DEFER_BLOCK_DEPENDENCY_INTERCEPTOR");
var DEFER_BLOCK_CONFIG = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DEFER_BLOCK_CONFIG" : "");
function getOrCreateEnvironmentInjector(parentInjector, tDetails, providers) {
  return parentInjector.get(CachedInjectorService).getOrCreateInjector(tDetails, parentInjector, providers, ngDevMode ? "DeferBlock Injector" : "");
}
function createDeferBlockInjector(parentInjector, tDetails, providers) {
  if (parentInjector instanceof ChainedInjector) {
    const origInjector = parentInjector.injector;
    const parentEnvInjector2 = parentInjector.parentInjector;
    const envInjector = getOrCreateEnvironmentInjector(parentEnvInjector2, tDetails, providers);
    return new ChainedInjector(origInjector, envInjector);
  }
  const parentEnvInjector = parentInjector.get(EnvironmentInjector);
  if (parentEnvInjector !== parentInjector) {
    const envInjector = getOrCreateEnvironmentInjector(parentEnvInjector, tDetails, providers);
    return new ChainedInjector(parentInjector, envInjector);
  }
  return getOrCreateEnvironmentInjector(parentInjector, tDetails, providers);
}
function renderDeferBlockState(newState, tNode, lContainer, skipTimerScheduling = false) {
  const hostLView = lContainer[PARENT];
  const hostTView = hostLView[TVIEW];
  if (isDestroyed(hostLView)) return;
  ngDevMode && assertTNodeForLView(tNode, hostLView);
  const lDetails = getLDeferBlockDetails(hostLView, tNode);
  ngDevMode && assertDefined(lDetails, "Expected a defer block state defined");
  const currentState = lDetails[DEFER_BLOCK_STATE];
  const ssrState = lDetails[SSR_BLOCK_STATE];
  if (ssrState !== null && newState < ssrState) {
    return;
  }
  if (isValidStateChange(currentState, newState) && isValidStateChange(lDetails[NEXT_DEFER_BLOCK_STATE] ?? -1, newState)) {
    const tDetails = getTDeferBlockDetails(hostTView, tNode);
    const needsScheduling = !skipTimerScheduling && true && (getLoadingBlockAfter(tDetails) !== null || getMinimumDurationForState(tDetails, DeferBlockState.Loading) !== null || getMinimumDurationForState(tDetails, DeferBlockState.Placeholder));
    if (ngDevMode && needsScheduling) {
      assertDefined(applyDeferBlockStateWithSchedulingImpl, "Expected scheduling function to be defined");
    }
    const applyStateFn = needsScheduling ? applyDeferBlockStateWithSchedulingImpl : applyDeferBlockState;
    try {
      applyStateFn(newState, lDetails, lContainer, tNode, hostLView);
    } catch (error) {
      handleUncaughtError(hostLView, error);
    }
  }
}
function findMatchingDehydratedViewForDeferBlock(lContainer, lDetails) {
  const dehydratedViewIx = lContainer[DEHYDRATED_VIEWS]?.findIndex((view) => view.data[DEFER_BLOCK_STATE$1] === lDetails[DEFER_BLOCK_STATE]) ?? -1;
  const dehydratedView = dehydratedViewIx > -1 ? lContainer[DEHYDRATED_VIEWS][dehydratedViewIx] : null;
  return {
    dehydratedView,
    dehydratedViewIx
  };
}
function applyDeferBlockState(newState, lDetails, lContainer, tNode, hostLView) {
  profiler(ProfilerEvent.DeferBlockStateStart);
  const stateTmplIndex = getTemplateIndexForState(newState, hostLView, tNode);
  if (stateTmplIndex !== null) {
    lDetails[DEFER_BLOCK_STATE] = newState;
    const hostTView = hostLView[TVIEW];
    const adjustedIndex = stateTmplIndex + HEADER_OFFSET;
    const activeBlockTNode = getTNode(hostTView, adjustedIndex);
    const viewIndex = 0;
    removeLViewFromLContainer(lContainer, viewIndex);
    let injector;
    if (newState === DeferBlockState.Complete) {
      const tDetails = getTDeferBlockDetails(hostTView, tNode);
      const providers = tDetails.providers;
      if (providers && providers.length > 0) {
        injector = createDeferBlockInjector(hostLView[INJECTOR], tDetails, providers);
      }
    }
    const {
      dehydratedView,
      dehydratedViewIx
    } = findMatchingDehydratedViewForDeferBlock(lContainer, lDetails);
    const embeddedLView = createAndRenderEmbeddedLView(hostLView, activeBlockTNode, null, {
      injector,
      dehydratedView
    });
    addLViewToLContainer(lContainer, embeddedLView, viewIndex, shouldAddViewToDom(activeBlockTNode, dehydratedView));
    markViewDirty(embeddedLView, 2);
    if (dehydratedViewIx > -1) {
      lContainer[DEHYDRATED_VIEWS]?.splice(dehydratedViewIx, 1);
    }
    if ((newState === DeferBlockState.Complete || newState === DeferBlockState.Error) && Array.isArray(lDetails[ON_COMPLETE_FNS])) {
      for (const callback of lDetails[ON_COMPLETE_FNS]) {
        callback();
      }
      lDetails[ON_COMPLETE_FNS] = null;
    }
  }
  profiler(ProfilerEvent.DeferBlockStateEnd);
}
function applyDeferBlockStateWithScheduling(newState, lDetails, lContainer, tNode, hostLView) {
  const now = Date.now();
  const hostTView = hostLView[TVIEW];
  const tDetails = getTDeferBlockDetails(hostTView, tNode);
  if (lDetails[STATE_IS_FROZEN_UNTIL] === null || lDetails[STATE_IS_FROZEN_UNTIL] <= now) {
    lDetails[STATE_IS_FROZEN_UNTIL] = null;
    const loadingAfter = getLoadingBlockAfter(tDetails);
    const inLoadingAfterPhase = lDetails[LOADING_AFTER_CLEANUP_FN] !== null;
    if (newState === DeferBlockState.Loading && loadingAfter !== null && !inLoadingAfterPhase) {
      lDetails[NEXT_DEFER_BLOCK_STATE] = newState;
      const cleanupFn = scheduleDeferBlockUpdate(loadingAfter, lDetails, tNode, lContainer, hostLView);
      lDetails[LOADING_AFTER_CLEANUP_FN] = cleanupFn;
    } else {
      if (newState > DeferBlockState.Loading && inLoadingAfterPhase) {
        lDetails[LOADING_AFTER_CLEANUP_FN]();
        lDetails[LOADING_AFTER_CLEANUP_FN] = null;
        lDetails[NEXT_DEFER_BLOCK_STATE] = null;
      }
      applyDeferBlockState(newState, lDetails, lContainer, tNode, hostLView);
      const duration = getMinimumDurationForState(tDetails, newState);
      if (duration !== null) {
        lDetails[STATE_IS_FROZEN_UNTIL] = now + duration;
        scheduleDeferBlockUpdate(duration, lDetails, tNode, lContainer, hostLView);
      }
    }
  } else {
    lDetails[NEXT_DEFER_BLOCK_STATE] = newState;
  }
}
function scheduleDeferBlockUpdate(timeout, lDetails, tNode, lContainer, hostLView) {
  const callback = () => {
    const nextState = lDetails[NEXT_DEFER_BLOCK_STATE];
    lDetails[STATE_IS_FROZEN_UNTIL] = null;
    lDetails[NEXT_DEFER_BLOCK_STATE] = null;
    if (nextState !== null) {
      renderDeferBlockState(nextState, tNode, lContainer);
    }
  };
  return scheduleTimerTrigger(timeout, callback, hostLView[INJECTOR]);
}
function isValidStateChange(currentState, newState) {
  return currentState < newState;
}
function renderPlaceholder(lView, tNode) {
  const lContainer = lView[tNode.index];
  ngDevMode && assertLContainer(lContainer);
  renderDeferBlockState(DeferBlockState.Placeholder, tNode, lContainer);
}
function renderDeferStateAfterResourceLoading(tDetails, tNode, lContainer) {
  ngDevMode && assertDefined(tDetails.loadingPromise, "Expected loading Promise to exist on this defer block");
  tDetails.loadingPromise.then(() => {
    if (tDetails.loadingState === DeferDependenciesLoadingState.COMPLETE) {
      ngDevMode && assertDeferredDependenciesLoaded(tDetails);
      renderDeferBlockState(DeferBlockState.Complete, tNode, lContainer);
    } else if (tDetails.loadingState === DeferDependenciesLoadingState.FAILED) {
      renderDeferBlockState(DeferBlockState.Error, tNode, lContainer);
    }
  });
}
var applyDeferBlockStateWithSchedulingImpl = null;
function \u0275\u0275deferEnableTimerScheduling(tView, tDetails, placeholderConfigIndex, loadingConfigIndex) {
  const tViewConsts = tView.consts;
  if (placeholderConfigIndex != null) {
    tDetails.placeholderBlockConfig = getConstant(tViewConsts, placeholderConfigIndex);
  }
  if (loadingConfigIndex != null) {
    tDetails.loadingBlockConfig = getConstant(tViewConsts, loadingConfigIndex);
  }
  if (applyDeferBlockStateWithSchedulingImpl === null) {
    applyDeferBlockStateWithSchedulingImpl = applyDeferBlockStateWithScheduling;
  }
}
var ASYNC_COMPONENT_METADATA_FN = "__ngAsyncComponentMetadataFn__";
function setClassMetadataAsync(type, dependencyLoaderFn, metadataSetterFn) {
  const componentClass = type;
  componentClass[ASYNC_COMPONENT_METADATA_FN] = () => Promise.all(dependencyLoaderFn()).then((dependencies) => {
    metadataSetterFn(...dependencies);
    componentClass[ASYNC_COMPONENT_METADATA_FN] = null;
    return dependencies;
  });
  return componentClass[ASYNC_COMPONENT_METADATA_FN];
}
function setClassMetadata(type, decorators, ctorParameters, propDecorators) {
  return noSideEffects(() => {
    const clazz = type;
    if (decorators !== null) {
      if (clazz.hasOwnProperty("decorators") && clazz.decorators !== void 0) {
        clazz.decorators.push(...decorators);
      } else {
        clazz.decorators = decorators;
      }
    }
    if (ctorParameters !== null) {
      clazz.ctorParameters = ctorParameters;
    }
    if (propDecorators !== null) {
      if (clazz.hasOwnProperty("propDecorators") && clazz.propDecorators !== void 0) {
        clazz.propDecorators = __spreadValues(__spreadValues({}, clazz.propDecorators), propDecorators);
      } else {
        clazz.propDecorators = propDecorators;
      }
    }
  });
}
var Console = class _Console {
  log(message) {
    console.log(message);
  }
  warn(message) {
    console.warn(message);
  }
  static \u0275fac = function Console_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Console)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Console,
    factory: _Console.\u0275fac,
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Console, [{
    type: Injectable,
    args: [{
      providedIn: "platform"
    }]
  }], null, null);
})();
var DIDebugData = class {
  resolverToTokenToDependencies = /* @__PURE__ */ new WeakMap();
  resolverToProviders = /* @__PURE__ */ new WeakMap();
  resolverToEffects = /* @__PURE__ */ new WeakMap();
  standaloneInjectorToComponent = /* @__PURE__ */ new WeakMap();
  reset() {
    this.resolverToTokenToDependencies = /* @__PURE__ */ new WeakMap();
    this.resolverToProviders = /* @__PURE__ */ new WeakMap();
    this.standaloneInjectorToComponent = /* @__PURE__ */ new WeakMap();
  }
};
var frameworkDIDebugData = new DIDebugData();
function getFrameworkDIDebugData() {
  return frameworkDIDebugData;
}
function setupFrameworkInjectorProfiler() {
  frameworkDIDebugData.reset();
  setInjectorProfiler(injectorProfilerEventHandler);
}
function injectorProfilerEventHandler(injectorProfilerEvent) {
  const {
    context: context2,
    type
  } = injectorProfilerEvent;
  if (type === 0) {
    handleInjectEvent(context2, injectorProfilerEvent.service);
  } else if (type === 1) {
    handleInstanceCreatedByInjectorEvent(context2, injectorProfilerEvent.instance);
  } else if (type === 2) {
    handleProviderConfiguredEvent(context2, injectorProfilerEvent.providerRecord);
  } else if (type === 3) {
    handleEffectCreatedEvent(context2, injectorProfilerEvent.effect);
  } else if (type === 4) {
    handleEffectCreatedEvent(context2, injectorProfilerEvent.effectPhase);
  }
}
function handleEffectCreatedEvent(context2, effect2) {
  const diResolver = getDIResolver(context2.injector);
  if (diResolver === null) {
    throwError2("An EffectCreated event must be run within an injection context.");
  }
  const {
    resolverToEffects
  } = frameworkDIDebugData;
  const cleanupContainer = effect2 instanceof EffectRefImpl ? effect2[SIGNAL] : effect2.sequence;
  let trackedEffects = resolverToEffects.get(diResolver);
  if (!trackedEffects) {
    trackedEffects = [];
    resolverToEffects.set(diResolver, trackedEffects);
  }
  trackedEffects.push(effect2);
  cleanupContainer.onDestroyFns ??= [];
  cleanupContainer.onDestroyFns.push(() => {
    const index = trackedEffects.indexOf(effect2);
    if (index > -1) {
      trackedEffects.splice(index, 1);
    }
  });
}
function handleInjectEvent(context2, data) {
  const diResolver = getDIResolver(context2.injector);
  if (diResolver === null) {
    throwError2("An Inject event must be run within an injection context.");
  }
  const diResolverToInstantiatedToken = frameworkDIDebugData.resolverToTokenToDependencies;
  if (!diResolverToInstantiatedToken.has(diResolver)) {
    diResolverToInstantiatedToken.set(diResolver, /* @__PURE__ */ new WeakMap());
  }
  if (!canBeHeldWeakly(context2.token)) {
    return;
  }
  const instantiatedTokenToDependencies = diResolverToInstantiatedToken.get(diResolver);
  if (!instantiatedTokenToDependencies.has(context2.token)) {
    instantiatedTokenToDependencies.set(context2.token, []);
  }
  const {
    token,
    value,
    flags
  } = data;
  assertDefined(context2.token, "Injector profiler context token is undefined.");
  const dependencies = instantiatedTokenToDependencies.get(context2.token);
  assertDefined(dependencies, "Could not resolve dependencies for token.");
  if (context2.injector instanceof NodeInjector) {
    dependencies.push({
      token,
      value,
      flags,
      injectedIn: getNodeInjectorContext(context2.injector)
    });
  } else {
    dependencies.push({
      token,
      value,
      flags
    });
  }
}
function getNodeInjectorContext(injector) {
  if (!(injector instanceof NodeInjector)) {
    throwError2("getNodeInjectorContext must be called with a NodeInjector");
  }
  const lView = getNodeInjectorLView(injector);
  const tNode = getNodeInjectorTNode(injector);
  if (tNode === null) {
    return;
  }
  assertTNodeForLView(tNode, lView);
  return {
    lView,
    tNode
  };
}
function handleInstanceCreatedByInjectorEvent(context2, data) {
  const {
    value
  } = data;
  if (data.value == null) {
    return;
  }
  if (getDIResolver(context2.injector) === null) {
    throwError2("An InjectorCreatedInstance event must be run within an injection context.");
  }
  let standaloneComponent = void 0;
  if (typeof value === "object") {
    standaloneComponent = value?.constructor;
  }
  if (standaloneComponent == void 0 || !isStandaloneComponent(standaloneComponent)) {
    return;
  }
  const environmentInjector = context2.injector.get(EnvironmentInjector, null, {
    optional: true
  });
  if (environmentInjector === null) {
    return;
  }
  const {
    standaloneInjectorToComponent
  } = frameworkDIDebugData;
  if (standaloneInjectorToComponent.has(environmentInjector)) {
    return;
  }
  standaloneInjectorToComponent.set(environmentInjector, standaloneComponent);
}
function isStandaloneComponent(value) {
  const def = getComponentDef(value);
  return !!def?.standalone;
}
function handleProviderConfiguredEvent(context2, data) {
  const {
    resolverToProviders
  } = frameworkDIDebugData;
  let diResolver;
  if (context2?.injector instanceof NodeInjector) {
    diResolver = getNodeInjectorTNode(context2.injector);
  } else {
    diResolver = context2.injector;
  }
  if (diResolver === null) {
    throwError2("A ProviderConfigured event must be run within an injection context.");
  }
  if (!resolverToProviders.has(diResolver)) {
    resolverToProviders.set(diResolver, []);
  }
  resolverToProviders.get(diResolver).push(data);
}
function getDIResolver(injector) {
  let diResolver = null;
  if (injector === void 0) {
    return diResolver;
  }
  if (injector instanceof NodeInjector) {
    diResolver = getNodeInjectorLView(injector);
  } else {
    diResolver = injector;
  }
  return diResolver;
}
function canBeHeldWeakly(value) {
  return value !== null && (typeof value === "object" || typeof value === "function" || typeof value === "symbol");
}
function isSignal2(value) {
  return typeof value === "function" && value[SIGNAL] !== void 0;
}
function isWritableSignal(value) {
  return isSignal2(value) && typeof value.set === "function";
}
function applyChanges(component) {
  ngDevMode && assertDefined(component, "component");
  markViewDirty(getComponentViewByInstance(component), 3);
  getRootComponents(component).forEach((rootComponent) => detectChanges(rootComponent));
}
function detectChanges(component) {
  const view = getComponentViewByInstance(component);
  view[FLAGS] |= 1024;
  detectChangesInternal(view);
}
function getDeferBlocks$1(lView, deferBlocks) {
  const tView = lView[TVIEW];
  for (let i = HEADER_OFFSET; i < tView.bindingStartIndex; i++) {
    if (isLContainer(lView[i])) {
      const lContainer = lView[i];
      const isLast = i === tView.bindingStartIndex - 1;
      if (!isLast) {
        const tNode = tView.data[i];
        const tDetails = getTDeferBlockDetails(tView, tNode);
        if (isTDeferBlockDetails(tDetails)) {
          deferBlocks.push({
            lContainer,
            lView,
            tNode,
            tDetails
          });
          continue;
        }
      }
      if (isLView(lContainer[HOST])) {
        getDeferBlocks$1(lContainer[HOST], deferBlocks);
      }
      for (let j = CONTAINER_HEADER_OFFSET; j < lContainer.length; j++) {
        getDeferBlocks$1(lContainer[j], deferBlocks);
      }
    } else if (isLView(lView[i])) {
      getDeferBlocks$1(lView[i], deferBlocks);
    }
  }
}
function getDeferBlocks(node) {
  const results = [];
  const lView = getLContext(node)?.lView;
  if (lView) {
    findDeferBlocks(node, lView, results);
  }
  return results;
}
function findDeferBlocks(node, lView, results) {
  const viewInjector = lView[INJECTOR];
  const registry = viewInjector.get(DEHYDRATED_BLOCK_REGISTRY, null, {
    optional: true
  });
  const blocks = [];
  getDeferBlocks$1(lView, blocks);
  const transferState = viewInjector.get(TransferState);
  const deferBlockParents = transferState.get(NGH_DEFER_BLOCKS_KEY, {});
  for (const details of blocks) {
    const native = getNativeByTNode(details.tNode, details.lView);
    const lDetails = getLDeferBlockDetails(details.lView, details.tNode);
    if (!node.contains(native)) {
      continue;
    }
    const tDetails = details.tDetails;
    const renderedLView = getRendererLView(details);
    const rootNodes = [];
    const hydrationState = inferHydrationState(tDetails, lDetails, registry);
    if (renderedLView !== null) {
      collectNativeNodes(renderedLView[TVIEW], renderedLView, renderedLView[TVIEW].firstChild, rootNodes);
    } else if (hydrationState === "dehydrated") {
      const deferId = lDetails[SSR_UNIQUE_ID];
      const deferData = deferBlockParents[deferId];
      const numberOfRootNodes = deferData[NUM_ROOT_NODES];
      let collectedNodeCount = 0;
      const deferBlockCommentNode = details.lContainer[NATIVE];
      let currentNode = deferBlockCommentNode.previousSibling;
      while (collectedNodeCount < numberOfRootNodes && currentNode) {
        rootNodes.unshift(currentNode);
        currentNode = currentNode.previousSibling;
        collectedNodeCount++;
      }
    }
    const data = {
      state: stringifyState(lDetails[DEFER_BLOCK_STATE]),
      incrementalHydrationState: hydrationState,
      hasErrorBlock: tDetails.errorTmplIndex !== null,
      loadingBlock: {
        exists: tDetails.loadingTmplIndex !== null,
        minimumTime: tDetails.loadingBlockConfig?.[MINIMUM_SLOT] ?? null,
        afterTime: tDetails.loadingBlockConfig?.[LOADING_AFTER_SLOT] ?? null
      },
      placeholderBlock: {
        exists: tDetails.placeholderTmplIndex !== null,
        minimumTime: tDetails.placeholderBlockConfig?.[MINIMUM_SLOT] ?? null
      },
      triggers: tDetails.debug?.triggers ? Array.from(tDetails.debug.triggers).sort() : [],
      hostNode: details.lContainer[HOST],
      rootNodes
    };
    results.push(data);
    if (renderedLView !== null) {
      findDeferBlocks(node, renderedLView, results);
    }
  }
}
function stringifyState(state) {
  switch (state) {
    case DeferBlockState.Complete:
      return "complete";
    case DeferBlockState.Loading:
      return "loading";
    case DeferBlockState.Placeholder:
      return "placeholder";
    case DeferBlockState.Error:
      return "error";
    case DeferBlockInternalState.Initial:
      return "initial";
    default:
      throw new Error(`Unrecognized state ${state}`);
  }
}
function inferHydrationState(tDetails, lDetails, registry) {
  if (registry === null || lDetails[SSR_UNIQUE_ID] === null || tDetails.hydrateTriggers === null || tDetails.hydrateTriggers.has(7)) {
    return "not-configured";
  }
  return registry.has(lDetails[SSR_UNIQUE_ID]) ? "dehydrated" : "hydrated";
}
function getRendererLView(details) {
  if (details.lContainer.length <= CONTAINER_HEADER_OFFSET) {
    return null;
  }
  const lView = details.lContainer[CONTAINER_HEADER_OFFSET];
  ngDevMode && assertLView(lView);
  return lView;
}
function getDependenciesFromInjectable(injector, token) {
  const instance = injector.get(token, null, {
    self: true,
    optional: true
  });
  if (instance === null) {
    throw new Error(`Unable to determine instance of ${token} in given injector`);
  }
  const unformattedDependencies = getDependenciesForTokenInInjector(token, injector);
  const resolutionPath = getInjectorResolutionPath(injector);
  const dependencies = unformattedDependencies.map((dep) => {
    const formattedDependency = {
      value: dep.value
    };
    const flags = dep.flags;
    formattedDependency.flags = {
      optional: (8 & flags) === 8,
      host: (1 & flags) === 1,
      self: (2 & flags) === 2,
      skipSelf: (4 & flags) === 4
    };
    for (let i = 0; i < resolutionPath.length; i++) {
      const injectorToCheck = resolutionPath[i];
      if (i === 0 && formattedDependency.flags.skipSelf) {
        continue;
      }
      if (formattedDependency.flags.host && injectorToCheck instanceof EnvironmentInjector) {
        break;
      }
      const instance2 = injectorToCheck.get(dep.token, null, {
        self: true,
        optional: true
      });
      if (instance2 !== null) {
        if (formattedDependency.flags.host) {
          const firstInjector = resolutionPath[0];
          const lookupFromFirstInjector = firstInjector.get(dep.token, null, __spreadProps(__spreadValues({}, formattedDependency.flags), {
            optional: true
          }));
          if (lookupFromFirstInjector !== null) {
            formattedDependency.providedIn = injectorToCheck;
          }
          break;
        }
        formattedDependency.providedIn = injectorToCheck;
        break;
      }
      if (i === 0 && formattedDependency.flags.self) {
        break;
      }
    }
    if (dep.token) formattedDependency.token = dep.token;
    return formattedDependency;
  });
  return {
    instance,
    dependencies
  };
}
function getDependenciesForTokenInInjector(token, injector) {
  const {
    resolverToTokenToDependencies
  } = getFrameworkDIDebugData();
  if (!(injector instanceof NodeInjector)) {
    return resolverToTokenToDependencies.get(injector)?.get?.(token) ?? [];
  }
  const lView = getNodeInjectorLView(injector);
  const tokenDependencyMap = resolverToTokenToDependencies.get(lView);
  const dependencies = tokenDependencyMap?.get(token) ?? [];
  return dependencies.filter((dependency) => {
    const dependencyNode = dependency.injectedIn?.tNode;
    if (dependencyNode === void 0) {
      return false;
    }
    const instanceNode = getNodeInjectorTNode(injector);
    assertTNode(dependencyNode);
    assertTNode(instanceNode);
    return dependencyNode === instanceNode;
  });
}
function getProviderImportsContainer(injector) {
  const {
    standaloneInjectorToComponent
  } = getFrameworkDIDebugData();
  if (standaloneInjectorToComponent.has(injector)) {
    return standaloneInjectorToComponent.get(injector);
  }
  const defTypeRef = injector.get(NgModuleRef$1, null, {
    self: true,
    optional: true
  });
  if (defTypeRef === null) {
    return null;
  }
  if (defTypeRef.instance === null) {
    return null;
  }
  return defTypeRef.instance.constructor;
}
function getNodeInjectorProviders(injector) {
  const diResolver = getNodeInjectorTNode(injector);
  const {
    resolverToProviders
  } = getFrameworkDIDebugData();
  return resolverToProviders.get(diResolver) ?? [];
}
function getProviderImportPaths(providerImportsContainer) {
  const providerToPath = /* @__PURE__ */ new Map();
  const visitedContainers = /* @__PURE__ */ new Set();
  const visitor = walkProviderTreeToDiscoverImportPaths(providerToPath, visitedContainers);
  walkProviderTree(providerImportsContainer, visitor, [], /* @__PURE__ */ new Set());
  return providerToPath;
}
function walkProviderTreeToDiscoverImportPaths(providerToPath, visitedContainers) {
  return (provider, container) => {
    if (!providerToPath.has(provider)) {
      providerToPath.set(provider, [container]);
    }
    if (!visitedContainers.has(container)) {
      for (const prov of providerToPath.keys()) {
        const existingImportPath = providerToPath.get(prov);
        let containerDef = getInjectorDef(container);
        if (!containerDef) {
          const ngModule = container.ngModule;
          containerDef = getInjectorDef(ngModule);
        }
        if (!containerDef) {
          return;
        }
        const lastContainerAddedToPath = existingImportPath[0];
        let isNextStepInPath = false;
        deepForEach(containerDef.imports, (moduleImport) => {
          if (isNextStepInPath) {
            return;
          }
          isNextStepInPath = moduleImport.ngModule === lastContainerAddedToPath || moduleImport === lastContainerAddedToPath;
          if (isNextStepInPath) {
            providerToPath.get(prov)?.unshift(container);
          }
        });
      }
    }
    visitedContainers.add(container);
  };
}
function getEnvironmentInjectorProviders(injector) {
  const providerRecordsWithoutImportPaths = getFrameworkDIDebugData().resolverToProviders.get(injector) ?? [];
  if (isPlatformInjector(injector)) {
    return providerRecordsWithoutImportPaths;
  }
  const providerImportsContainer = getProviderImportsContainer(injector);
  if (providerImportsContainer === null) {
    return providerRecordsWithoutImportPaths;
  }
  const providerToPath = getProviderImportPaths(providerImportsContainer);
  const providerRecords = [];
  for (const providerRecord of providerRecordsWithoutImportPaths) {
    const provider = providerRecord.provider;
    const token = provider.provide;
    if (token === ENVIRONMENT_INITIALIZER || token === INJECTOR_DEF_TYPES) {
      continue;
    }
    let importPath = providerToPath.get(provider) ?? [];
    const def = getComponentDef(providerImportsContainer);
    const isStandaloneComponent2 = !!def?.standalone;
    if (isStandaloneComponent2) {
      importPath = [providerImportsContainer, ...importPath];
    }
    providerRecords.push(__spreadProps(__spreadValues({}, providerRecord), {
      importPath
    }));
  }
  return providerRecords;
}
function isPlatformInjector(injector) {
  return injector instanceof R3Injector && injector.scopes.has("platform");
}
function getInjectorProviders(injector) {
  if (injector instanceof NodeInjector) {
    return getNodeInjectorProviders(injector);
  } else if (injector instanceof EnvironmentInjector) {
    return getEnvironmentInjectorProviders(injector);
  }
  throwError2("getInjectorProviders only supports NodeInjector and EnvironmentInjector");
}
function getInjectorMetadata(injector) {
  if (injector instanceof NodeInjector) {
    const lView = getNodeInjectorLView(injector);
    const tNode = getNodeInjectorTNode(injector);
    assertTNodeForLView(tNode, lView);
    return {
      type: "element",
      source: getNativeByTNode(tNode, lView)
    };
  }
  if (injector instanceof R3Injector) {
    return {
      type: "environment",
      source: injector.source ?? null
    };
  }
  if (injector instanceof NullInjector) {
    return {
      type: "null",
      source: null
    };
  }
  return null;
}
function getInjectorResolutionPath(injector) {
  const resolutionPath = [injector];
  getInjectorResolutionPathHelper(injector, resolutionPath);
  return resolutionPath;
}
function getInjectorResolutionPathHelper(injector, resolutionPath) {
  const parent = getInjectorParent(injector);
  if (parent === null) {
    if (injector instanceof NodeInjector) {
      const firstInjector = resolutionPath[0];
      if (firstInjector instanceof NodeInjector) {
        const moduleInjector = getModuleInjectorOfNodeInjector(firstInjector);
        if (moduleInjector === null) {
          throwError2("NodeInjector must have some connection to the module injector tree");
        }
        resolutionPath.push(moduleInjector);
        getInjectorResolutionPathHelper(moduleInjector, resolutionPath);
      }
      return resolutionPath;
    }
  } else {
    resolutionPath.push(parent);
    getInjectorResolutionPathHelper(parent, resolutionPath);
  }
  return resolutionPath;
}
function getInjectorParent(injector) {
  if (injector instanceof R3Injector) {
    return injector.parent;
  }
  let tNode;
  let lView;
  if (injector instanceof NodeInjector) {
    tNode = getNodeInjectorTNode(injector);
    lView = getNodeInjectorLView(injector);
  } else if (injector instanceof NullInjector) {
    return null;
  } else if (injector instanceof ChainedInjector) {
    return injector.parentInjector;
  } else {
    throwError2("getInjectorParent only support injectors of type R3Injector, NodeInjector, NullInjector");
  }
  const parentLocation = getParentInjectorLocation(tNode, lView);
  if (hasParentInjector(parentLocation)) {
    const parentInjectorIndex = getParentInjectorIndex(parentLocation);
    const parentLView = getParentInjectorView(parentLocation, lView);
    const parentTView = parentLView[TVIEW];
    const parentTNode = parentTView.data[parentInjectorIndex + 8];
    return new NodeInjector(parentTNode, parentLView);
  } else {
    const chainedInjector = lView[INJECTOR];
    const injectorParent = chainedInjector.injector?.parent;
    if (injectorParent instanceof NodeInjector) {
      return injectorParent;
    }
  }
  return null;
}
function getModuleInjectorOfNodeInjector(injector) {
  let lView;
  if (injector instanceof NodeInjector) {
    lView = getNodeInjectorLView(injector);
  } else {
    throwError2("getModuleInjectorOfNodeInjector must be called with a NodeInjector");
  }
  const inj = lView[INJECTOR];
  const moduleInjector = inj instanceof ChainedInjector ? inj.parentInjector : inj.parent;
  if (!moduleInjector) {
    throwError2("NodeInjector must have some connection to the module injector tree");
  }
  return moduleInjector;
}
function isComputedNode(node) {
  return node.kind === "computed";
}
function isTemplateEffectNode(node) {
  return node.kind === "template";
}
function isSignalNode(node) {
  return node.kind === "signal";
}
function getTemplateConsumer(injector) {
  const tNode = getNodeInjectorTNode(injector);
  assertTNode(tNode);
  const lView = getNodeInjectorLView(injector);
  assertLView(lView);
  const templateLView = lView[tNode.index];
  if (isLView(templateLView)) {
    return templateLView[REACTIVE_TEMPLATE_CONSUMER] ?? null;
  }
  return null;
}
var signalDebugMap = /* @__PURE__ */ new WeakMap();
var counter$1 = 0;
function getNodesAndEdgesFromSignalMap(signalMap) {
  const nodes = Array.from(signalMap.keys());
  const debugSignalGraphNodes = [];
  const edges = [];
  for (const [consumer, producers] of signalMap.entries()) {
    const consumerIndex = nodes.indexOf(consumer);
    let id = signalDebugMap.get(consumer);
    if (!id) {
      counter$1++;
      id = counter$1.toString();
      signalDebugMap.set(consumer, id);
    }
    if (isComputedNode(consumer)) {
      debugSignalGraphNodes.push({
        label: consumer.debugName,
        value: consumer.value,
        kind: consumer.kind,
        epoch: consumer.version,
        debuggableFn: consumer.computation,
        id
      });
    } else if (isSignalNode(consumer)) {
      debugSignalGraphNodes.push({
        label: consumer.debugName,
        value: consumer.value,
        kind: consumer.kind,
        epoch: consumer.version,
        id
      });
    } else if (isTemplateEffectNode(consumer)) {
      debugSignalGraphNodes.push({
        label: consumer.debugName ?? consumer.lView?.[HOST]?.tagName?.toLowerCase?.(),
        kind: consumer.kind,
        epoch: consumer.version,
        debuggableFn: consumer.lView?.[CONTEXT]?.constructor,
        id
      });
    } else {
      debugSignalGraphNodes.push({
        label: consumer.debugName,
        kind: consumer.kind,
        epoch: consumer.version,
        id
      });
    }
    for (const producer of producers) {
      edges.push({
        consumer: consumerIndex,
        producer: nodes.indexOf(producer)
      });
    }
  }
  return {
    nodes: debugSignalGraphNodes,
    edges
  };
}
function extractEffectsFromInjector(injector) {
  let diResolver = injector;
  if (injector instanceof NodeInjector) {
    const lView = getNodeInjectorLView(injector);
    diResolver = lView;
  }
  const resolverToEffects = getFrameworkDIDebugData().resolverToEffects;
  const effects = resolverToEffects.get(diResolver) ?? [];
  return effects.map((effect2) => {
    if (effect2 instanceof EffectRefImpl) {
      return effect2[SIGNAL];
    } else {
      return effect2.signal[SIGNAL];
    }
  });
}
function extractSignalNodesAndEdgesFromRoots(nodes, signalDependenciesMap = /* @__PURE__ */ new Map()) {
  for (const node of nodes) {
    if (signalDependenciesMap.has(node)) {
      continue;
    }
    const producerNodes = [];
    for (let link = node.producers; link !== void 0; link = link.nextProducer) {
      const producer = link.producer;
      producerNodes.push(producer);
    }
    signalDependenciesMap.set(node, producerNodes);
    extractSignalNodesAndEdgesFromRoots(producerNodes, signalDependenciesMap);
  }
  return signalDependenciesMap;
}
function getSignalGraph(injector) {
  let templateConsumer = null;
  if (!(injector instanceof NodeInjector) && !(injector instanceof R3Injector)) {
    return throwError2("getSignalGraph must be called with a NodeInjector or R3Injector");
  }
  if (injector instanceof NodeInjector) {
    templateConsumer = getTemplateConsumer(injector);
  }
  const nonTemplateEffectNodes = extractEffectsFromInjector(injector);
  const signalNodes = templateConsumer ? [templateConsumer, ...nonTemplateEffectNodes] : nonTemplateEffectNodes;
  const signalDependenciesMap = extractSignalNodesAndEdgesFromRoots(signalNodes);
  return getNodesAndEdgesFromSignalMap(signalDependenciesMap);
}
var changeDetectionRuns = 0;
var changeDetectionSyncRuns = 0;
var counter = 0;
var eventsStack = [];
function measureStart(startEvent) {
  eventsStack.push([startEvent, counter]);
  console.timeStamp("Event_" + startEvent + "_" + counter++);
}
function measureEnd(startEvent, entryName, color) {
  let top;
  do {
    top = eventsStack.pop();
    assertDefined(top, "Profiling error: could not find start event entry " + startEvent);
  } while (top[0] !== startEvent);
  console.timeStamp(entryName, "Event_" + top[0] + "_" + top[1], void 0, "\u{1F170}\uFE0F Angular", void 0, color);
}
var chromeDevToolsInjectorProfiler = (event) => {
  const eventType = event.type;
  if (eventType === 5) {
    measureStart(100);
  } else if (eventType === 1) {
    const token = event.context.token;
    measureEnd(100, getProviderTokenMeasureName(token), "tertiary-dark");
  }
};
var devToolsProfiler = (event, instance, eventFn) => {
  switch (event) {
    case ProfilerEvent.BootstrapApplicationStart:
    case ProfilerEvent.BootstrapComponentStart:
    case ProfilerEvent.ChangeDetectionStart:
    case ProfilerEvent.ChangeDetectionSyncStart:
    case ProfilerEvent.AfterRenderHooksStart:
    case ProfilerEvent.ComponentStart:
    case ProfilerEvent.DeferBlockStateStart:
    case ProfilerEvent.DynamicComponentStart:
    case ProfilerEvent.TemplateCreateStart:
    case ProfilerEvent.LifecycleHookStart:
    case ProfilerEvent.TemplateUpdateStart:
    case ProfilerEvent.HostBindingsUpdateStart:
    case ProfilerEvent.OutputStart: {
      measureStart(event);
      break;
    }
    case ProfilerEvent.BootstrapApplicationEnd: {
      measureEnd(ProfilerEvent.BootstrapApplicationStart, "Bootstrap application", "primary-dark");
      break;
    }
    case ProfilerEvent.BootstrapComponentEnd: {
      measureEnd(ProfilerEvent.BootstrapComponentStart, "Bootstrap component", "primary-dark");
      break;
    }
    case ProfilerEvent.ChangeDetectionEnd: {
      changeDetectionSyncRuns = 0;
      measureEnd(ProfilerEvent.ChangeDetectionStart, "Change detection " + changeDetectionRuns++, "primary-dark");
      break;
    }
    case ProfilerEvent.ChangeDetectionSyncEnd: {
      measureEnd(ProfilerEvent.ChangeDetectionSyncStart, "Synchronization " + changeDetectionSyncRuns++, "primary");
      break;
    }
    case ProfilerEvent.AfterRenderHooksEnd: {
      measureEnd(ProfilerEvent.AfterRenderHooksStart, "After render hooks", "primary");
      break;
    }
    case ProfilerEvent.ComponentEnd: {
      const typeName = getComponentMeasureName(instance);
      measureEnd(ProfilerEvent.ComponentStart, typeName, "primary-light");
      break;
    }
    case ProfilerEvent.DeferBlockStateEnd: {
      measureEnd(ProfilerEvent.DeferBlockStateStart, "Defer block", "primary-dark");
      break;
    }
    case ProfilerEvent.DynamicComponentEnd: {
      measureEnd(ProfilerEvent.DynamicComponentStart, "Dynamic component creation", "primary-dark");
      break;
    }
    case ProfilerEvent.TemplateUpdateEnd: {
      measureEnd(ProfilerEvent.TemplateUpdateStart, stringifyForError(eventFn) + " (update)", "secondary-dark");
      break;
    }
    case ProfilerEvent.TemplateCreateEnd: {
      measureEnd(ProfilerEvent.TemplateCreateStart, stringifyForError(eventFn) + " (create)", "secondary");
      break;
    }
    case ProfilerEvent.HostBindingsUpdateEnd: {
      measureEnd(ProfilerEvent.HostBindingsUpdateStart, "HostBindings", "secondary-dark");
      break;
    }
    case ProfilerEvent.LifecycleHookEnd: {
      const typeName = getComponentMeasureName(instance);
      measureEnd(ProfilerEvent.LifecycleHookStart, `${typeName}:${stringifyForError(eventFn)}`, "tertiary");
      break;
    }
    case ProfilerEvent.OutputEnd: {
      measureEnd(ProfilerEvent.OutputStart, stringifyForError(eventFn), "tertiary-light");
      break;
    }
    default: {
      throw new Error("Unexpected profiling event type: " + event);
    }
  }
};
function getComponentMeasureName(instance) {
  return instance.constructor.name;
}
function getProviderTokenMeasureName(token) {
  if (isTypeProvider(token)) {
    return token.name;
  } else if (token.provide != null) {
    return getProviderTokenMeasureName(token.provide);
  }
  return token.toString();
}
function enableProfiling() {
  performanceMarkFeature("Chrome DevTools profiling");
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    const removeInjectorProfiler = setInjectorProfiler(chromeDevToolsInjectorProfiler);
    const removeProfiler3 = setProfiler(devToolsProfiler);
    return () => {
      removeInjectorProfiler();
      removeProfiler3();
    };
  }
  return () => {
  };
}
function getTransferState(injector) {
  const doc = injector.get(DOCUMENT);
  const appId = injector.get(APP_ID);
  const transferState = retrieveTransferredState(doc, appId);
  const filteredEntries = {};
  for (const [key, value] of Object.entries(transferState)) {
    if (!isInternalHydrationTransferStateKey(key)) {
      filteredEntries[key] = value;
    }
  }
  return filteredEntries;
}
var GLOBAL_PUBLISH_EXPANDO_KEY = "ng";
var globalUtilsFunctions = {
  "\u0275getDependenciesFromInjectable": getDependenciesFromInjectable,
  "\u0275getInjectorProviders": getInjectorProviders,
  "\u0275getInjectorResolutionPath": getInjectorResolutionPath,
  "\u0275getInjectorMetadata": getInjectorMetadata,
  "\u0275setProfiler": setProfiler,
  "\u0275getSignalGraph": getSignalGraph,
  "\u0275getDeferBlocks": getDeferBlocks,
  "\u0275getTransferState": getTransferState,
  "getDirectiveMetadata": getDirectiveMetadata$1,
  "getComponent": getComponent,
  "getContext": getContext,
  "getListeners": getListeners,
  "getOwningComponent": getOwningComponent,
  "getHostElement": getHostElement,
  "getInjector": getInjector,
  "getRootComponents": getRootComponents,
  "getDirectives": getDirectives,
  "applyChanges": applyChanges,
  "isSignal": isSignal2,
  "enableProfiling": enableProfiling
};
var _published = false;
function publishDefaultGlobalUtils$1() {
  if (!_published) {
    _published = true;
    if (typeof window !== "undefined") {
      setupFrameworkInjectorProfiler();
    }
    for (const [methodName, method] of Object.entries(globalUtilsFunctions)) {
      publishGlobalUtil(methodName, method);
    }
  }
}
function publishGlobalUtil(name, fn) {
  publishUtil(name, fn);
}
function publishExternalGlobalUtil(name, fn) {
  publishUtil(name, fn);
}
function publishUtil(name, fn) {
  if (typeof COMPILED === "undefined" || !COMPILED) {
    const w = _global;
    ngDevMode && assertDefined(fn, "function not defined");
    w[GLOBAL_PUBLISH_EXPANDO_KEY] ??= {};
    w[GLOBAL_PUBLISH_EXPANDO_KEY][name] = fn;
  }
}
var TESTABILITY = new InjectionToken("");
var TESTABILITY_GETTER = new InjectionToken("");
var Testability = class _Testability {
  _ngZone;
  registry;
  _isZoneStable = true;
  _callbacks = [];
  _taskTrackingZone = null;
  _destroyRef;
  constructor(_ngZone, registry, testabilityGetter) {
    this._ngZone = _ngZone;
    this.registry = registry;
    if (isInInjectionContext()) {
      this._destroyRef = inject2(DestroyRef, {
        optional: true
      }) ?? void 0;
    }
    if (!_testabilityGetter) {
      setTestabilityGetter(testabilityGetter);
      testabilityGetter.addToWindow(registry);
    }
    this._watchAngularEvents();
    _ngZone.run(() => {
      this._taskTrackingZone = typeof Zone == "undefined" ? null : Zone.current.get("TaskTrackingZone");
    });
  }
  _watchAngularEvents() {
    const onUnstableSubscription = this._ngZone.onUnstable.subscribe({
      next: () => {
        this._isZoneStable = false;
      }
    });
    const onStableSubscription = this._ngZone.runOutsideAngular(() => this._ngZone.onStable.subscribe({
      next: () => {
        NgZone.assertNotInAngularZone();
        queueMicrotask(() => {
          this._isZoneStable = true;
          this._runCallbacksIfReady();
        });
      }
    }));
    this._destroyRef?.onDestroy(() => {
      onUnstableSubscription.unsubscribe();
      onStableSubscription.unsubscribe();
    });
  }
  isStable() {
    return this._isZoneStable && !this._ngZone.hasPendingMacrotasks;
  }
  _runCallbacksIfReady() {
    if (this.isStable()) {
      queueMicrotask(() => {
        while (this._callbacks.length !== 0) {
          let cb = this._callbacks.pop();
          clearTimeout(cb.timeoutId);
          cb.doneCb();
        }
      });
    } else {
      let pending = this.getPendingTasks();
      this._callbacks = this._callbacks.filter((cb) => {
        if (cb.updateCb && cb.updateCb(pending)) {
          clearTimeout(cb.timeoutId);
          return false;
        }
        return true;
      });
    }
  }
  getPendingTasks() {
    if (!this._taskTrackingZone) {
      return [];
    }
    return this._taskTrackingZone.macroTasks.map((t) => {
      return {
        source: t.source,
        creationLocation: t.creationLocation,
        data: t.data
      };
    });
  }
  addCallback(cb, timeout, updateCb) {
    let timeoutId = -1;
    if (timeout && timeout > 0) {
      timeoutId = setTimeout(() => {
        this._callbacks = this._callbacks.filter((cb2) => cb2.timeoutId !== timeoutId);
        cb();
      }, timeout);
    }
    this._callbacks.push({
      doneCb: cb,
      timeoutId,
      updateCb
    });
  }
  whenStable(doneCb, timeout, updateCb) {
    if (updateCb && !this._taskTrackingZone) {
      throw new Error('Task tracking zone is required when passing an update callback to whenStable(). Is "zone.js/plugins/task-tracking" loaded?');
    }
    this.addCallback(doneCb, timeout, updateCb);
    this._runCallbacksIfReady();
  }
  registerApplication(token) {
    this.registry.registerApplication(token, this);
  }
  unregisterApplication(token) {
    this.registry.unregisterApplication(token);
  }
  findProviders(using, provider, exactMatch) {
    return [];
  }
  static \u0275fac = function Testability_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Testability)(\u0275\u0275inject(NgZone), \u0275\u0275inject(TestabilityRegistry), \u0275\u0275inject(TESTABILITY_GETTER));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Testability,
    factory: _Testability.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Testability, [{
    type: Injectable
  }], () => [{
    type: NgZone
  }, {
    type: TestabilityRegistry
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [TESTABILITY_GETTER]
    }]
  }], null);
})();
var TestabilityRegistry = class _TestabilityRegistry {
  _applications = /* @__PURE__ */ new Map();
  registerApplication(token, testability) {
    this._applications.set(token, testability);
  }
  unregisterApplication(token) {
    this._applications.delete(token);
  }
  unregisterAllApplications() {
    this._applications.clear();
  }
  getTestability(elem) {
    return this._applications.get(elem) || null;
  }
  getAllTestabilities() {
    return Array.from(this._applications.values());
  }
  getAllRootElements() {
    return Array.from(this._applications.keys());
  }
  findTestabilityInTree(elem, findInAncestors = true) {
    return _testabilityGetter?.findTestabilityInTree(this, elem, findInAncestors) ?? null;
  }
  static \u0275fac = function TestabilityRegistry_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TestabilityRegistry)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TestabilityRegistry,
    factory: _TestabilityRegistry.\u0275fac,
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TestabilityRegistry, [{
    type: Injectable,
    args: [{
      providedIn: "platform"
    }]
  }], null, null);
})();
function setTestabilityGetter(getter) {
  _testabilityGetter = getter;
}
var _testabilityGetter;
function isPromise2(obj) {
  return !!obj && typeof obj.then === "function";
}
function isSubscribable(obj) {
  return !!obj && typeof obj.subscribe === "function";
}
var APP_INITIALIZER = new InjectionToken(ngDevMode ? "Application Initializer" : "");
function provideAppInitializer(initializerFn) {
  return makeEnvironmentProviders([{
    provide: APP_INITIALIZER,
    multi: true,
    useValue: initializerFn
  }]);
}
var ApplicationInitStatus = class _ApplicationInitStatus {
  resolve;
  reject;
  initialized = false;
  done = false;
  donePromise = new Promise((res, rej) => {
    this.resolve = res;
    this.reject = rej;
  });
  appInits = inject2(APP_INITIALIZER, {
    optional: true
  }) ?? [];
  injector = inject2(Injector);
  constructor() {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !Array.isArray(this.appInits)) {
      throw new RuntimeError(-209, `Unexpected type of the \`APP_INITIALIZER\` token value (expected an array, but got ${typeof this.appInits}). Please check that the \`APP_INITIALIZER\` token is configured as a \`multi: true\` provider.`);
    }
  }
  runInitializers() {
    if (this.initialized) {
      return;
    }
    const asyncInitPromises = [];
    for (const appInits of this.appInits) {
      const initResult = runInInjectionContext(this.injector, appInits);
      if (isPromise2(initResult)) {
        asyncInitPromises.push(initResult);
      } else if (isSubscribable(initResult)) {
        const observableAsPromise = new Promise((resolve, reject) => {
          initResult.subscribe({
            complete: resolve,
            error: reject
          });
        });
        asyncInitPromises.push(observableAsPromise);
      }
    }
    const complete = () => {
      this.done = true;
      this.resolve();
    };
    Promise.all(asyncInitPromises).then(() => {
      complete();
    }).catch((e) => {
      this.reject(e);
    });
    if (asyncInitPromises.length === 0) {
      complete();
    }
    this.initialized = true;
  }
  static \u0275fac = function ApplicationInitStatus_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ApplicationInitStatus)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ApplicationInitStatus,
    factory: _ApplicationInitStatus.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ApplicationInitStatus, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var APP_BOOTSTRAP_LISTENER = new InjectionToken(ngDevMode ? "appBootstrapListener" : "");
function publishDefaultGlobalUtils() {
  ngDevMode && publishDefaultGlobalUtils$1();
}
function publishSignalConfiguration() {
  setThrowInvalidWriteToSignalError(() => {
    let errorMessage = "";
    if (ngDevMode) {
      const activeConsumer2 = getActiveConsumer();
      errorMessage = activeConsumer2 && isReactiveLViewConsumer(activeConsumer2) ? "Writing to signals is not allowed while Angular renders the template (eg. interpolations)" : "Writing to signals is not allowed in a `computed`";
    }
    throw new RuntimeError(600, errorMessage);
  });
}
function isBoundToModule(cf) {
  return cf.isBoundToModule;
}
var MAXIMUM_REFRESH_RERUNS = 10;
function optionsReducer(dst, objs) {
  if (Array.isArray(objs)) {
    return objs.reduce(optionsReducer, dst);
  }
  return __spreadValues(__spreadValues({}, dst), objs);
}
var ApplicationRef = class _ApplicationRef {
  _runningTick = false;
  _destroyed = false;
  _destroyListeners = [];
  _views = [];
  internalErrorHandler = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
  afterRenderManager = inject2(AfterRenderManager);
  zonelessEnabled = inject2(ZONELESS_ENABLED);
  rootEffectScheduler = inject2(EffectScheduler);
  dirtyFlags = 0;
  tracingSnapshot = null;
  allTestViews = /* @__PURE__ */ new Set();
  autoDetectTestViews = /* @__PURE__ */ new Set();
  includeAllTestViews = false;
  afterTick = new Subject();
  get allViews() {
    return [...(this.includeAllTestViews ? this.allTestViews : this.autoDetectTestViews).keys(), ...this._views];
  }
  get destroyed() {
    return this._destroyed;
  }
  componentTypes = [];
  components = [];
  internalPendingTask = inject2(PendingTasksInternal);
  get isStable() {
    return this.internalPendingTask.hasPendingTasksObservable.pipe(map((pending) => !pending));
  }
  constructor() {
    inject2(TracingService, {
      optional: true
    });
  }
  whenStable() {
    let subscription;
    return new Promise((resolve) => {
      subscription = this.isStable.subscribe({
        next: (stable) => {
          if (stable) {
            resolve();
          }
        }
      });
    }).finally(() => {
      subscription.unsubscribe();
    });
  }
  _injector = inject2(EnvironmentInjector);
  _rendererFactory = null;
  get injector() {
    return this._injector;
  }
  bootstrap(componentOrFactory, rootSelectorOrNode) {
    return this.bootstrapImpl(componentOrFactory, rootSelectorOrNode);
  }
  bootstrapImpl(componentOrFactory, rootSelectorOrNode, injector = Injector.NULL) {
    const ngZone = this._injector.get(NgZone);
    return ngZone.run(() => {
      profiler(ProfilerEvent.BootstrapComponentStart);
      (typeof ngDevMode === "undefined" || ngDevMode) && warnIfDestroyed(this._destroyed);
      const isComponentFactory = componentOrFactory instanceof ComponentFactory$1;
      const initStatus = this._injector.get(ApplicationInitStatus);
      if (!initStatus.done) {
        let errorMessage = "";
        if (typeof ngDevMode === "undefined" || ngDevMode) {
          const standalone = !isComponentFactory && isStandalone(componentOrFactory);
          errorMessage = "Cannot bootstrap as there are still asynchronous initializers running." + (standalone ? "" : " Bootstrap components in the `ngDoBootstrap` method of the root module.");
        }
        throw new RuntimeError(405, errorMessage);
      }
      let componentFactory;
      if (isComponentFactory) {
        componentFactory = componentOrFactory;
      } else {
        const resolver = this._injector.get(ComponentFactoryResolver$1);
        componentFactory = resolver.resolveComponentFactory(componentOrFactory);
      }
      this.componentTypes.push(componentFactory.componentType);
      const ngModule = isBoundToModule(componentFactory) ? void 0 : this._injector.get(NgModuleRef$1);
      const selectorOrNode = rootSelectorOrNode || componentFactory.selector;
      const compRef = componentFactory.create(injector, [], selectorOrNode, ngModule);
      const nativeElement = compRef.location.nativeElement;
      const testability = compRef.injector.get(TESTABILITY, null);
      testability?.registerApplication(nativeElement);
      compRef.onDestroy(() => {
        this.detachView(compRef.hostView);
        remove(this.components, compRef);
        testability?.unregisterApplication(nativeElement);
      });
      this._loadComponent(compRef);
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        const _console = this._injector.get(Console);
        _console.log(`Angular is running in development mode.`);
      }
      profiler(ProfilerEvent.BootstrapComponentEnd, compRef);
      return compRef;
    });
  }
  tick() {
    if (!this.zonelessEnabled) {
      this.dirtyFlags |= 1;
    }
    this._tick();
  }
  _tick() {
    profiler(ProfilerEvent.ChangeDetectionStart);
    if (this.tracingSnapshot !== null) {
      this.tracingSnapshot.run(TracingAction.CHANGE_DETECTION, this.tickImpl);
    } else {
      this.tickImpl();
    }
  }
  tickImpl = () => {
    (typeof ngDevMode === "undefined" || ngDevMode) && warnIfDestroyed(this._destroyed);
    if (this._runningTick) {
      profiler(ProfilerEvent.ChangeDetectionEnd);
      throw new RuntimeError(101, ngDevMode && "ApplicationRef.tick is called recursively");
    }
    const prevConsumer = setActiveConsumer(null);
    try {
      this._runningTick = true;
      this.synchronize();
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        for (let view of this.allViews) {
          view.checkNoChanges();
        }
      }
    } finally {
      this._runningTick = false;
      this.tracingSnapshot?.dispose();
      this.tracingSnapshot = null;
      setActiveConsumer(prevConsumer);
      this.afterTick.next();
      profiler(ProfilerEvent.ChangeDetectionEnd);
    }
  };
  synchronize() {
    if (this._rendererFactory === null && !this._injector.destroyed) {
      this._rendererFactory = this._injector.get(RendererFactory2, null, {
        optional: true
      });
    }
    let runs = 0;
    while (this.dirtyFlags !== 0 && runs++ < MAXIMUM_REFRESH_RERUNS) {
      profiler(ProfilerEvent.ChangeDetectionSyncStart);
      try {
        this.synchronizeOnce();
      } finally {
        profiler(ProfilerEvent.ChangeDetectionSyncEnd);
      }
    }
    if ((typeof ngDevMode === "undefined" || ngDevMode) && runs >= MAXIMUM_REFRESH_RERUNS) {
      throw new RuntimeError(103, ngDevMode && "Infinite change detection while refreshing application views. Ensure views are not calling `markForCheck` on every template execution or that afterRender hooks always mark views for check.");
    }
  }
  synchronizeOnce() {
    if (this.dirtyFlags & 16) {
      this.dirtyFlags &= -17;
      this.rootEffectScheduler.flush();
    }
    let ranDetectChanges = false;
    if (this.dirtyFlags & 7) {
      const useGlobalCheck = Boolean(this.dirtyFlags & 1);
      this.dirtyFlags &= -8;
      this.dirtyFlags |= 8;
      for (let {
        _lView
      } of this.allViews) {
        if (!useGlobalCheck && !requiresRefreshOrTraversal(_lView)) {
          continue;
        }
        const mode = useGlobalCheck && !this.zonelessEnabled ? 0 : 1;
        detectChangesInternal(_lView, mode);
        ranDetectChanges = true;
      }
      this.dirtyFlags &= -5;
      this.syncDirtyFlagsWithViews();
      if (this.dirtyFlags & (7 | 16)) {
        return;
      }
    }
    if (!ranDetectChanges) {
      this._rendererFactory?.begin?.();
      this._rendererFactory?.end?.();
    }
    if (this.dirtyFlags & 8) {
      this.dirtyFlags &= -9;
      this.afterRenderManager.execute();
    }
    this.syncDirtyFlagsWithViews();
  }
  syncDirtyFlagsWithViews() {
    if (this.allViews.some(({
      _lView
    }) => requiresRefreshOrTraversal(_lView))) {
      this.dirtyFlags |= 2;
      return;
    } else {
      this.dirtyFlags &= -8;
    }
  }
  attachView(viewRef) {
    (typeof ngDevMode === "undefined" || ngDevMode) && warnIfDestroyed(this._destroyed);
    const view = viewRef;
    this._views.push(view);
    view.attachToAppRef(this);
  }
  detachView(viewRef) {
    (typeof ngDevMode === "undefined" || ngDevMode) && warnIfDestroyed(this._destroyed);
    const view = viewRef;
    remove(this._views, view);
    view.detachFromAppRef();
  }
  _loadComponent(componentRef) {
    this.attachView(componentRef.hostView);
    try {
      this.tick();
    } catch (e) {
      this.internalErrorHandler(e);
    }
    this.components.push(componentRef);
    const listeners = this._injector.get(APP_BOOTSTRAP_LISTENER, []);
    if (ngDevMode && !Array.isArray(listeners)) {
      throw new RuntimeError(-209, `Unexpected type of the \`APP_BOOTSTRAP_LISTENER\` token value (expected an array, but got ${typeof listeners}). Please check that the \`APP_BOOTSTRAP_LISTENER\` token is configured as a \`multi: true\` provider.`);
    }
    listeners.forEach((listener) => listener(componentRef));
  }
  ngOnDestroy() {
    if (this._destroyed) return;
    try {
      this._destroyListeners.forEach((listener) => listener());
      this._views.slice().forEach((view) => view.destroy());
    } finally {
      this._destroyed = true;
      this._views = [];
      this._destroyListeners = [];
    }
  }
  onDestroy(callback) {
    (typeof ngDevMode === "undefined" || ngDevMode) && warnIfDestroyed(this._destroyed);
    this._destroyListeners.push(callback);
    return () => remove(this._destroyListeners, callback);
  }
  destroy() {
    if (this._destroyed) {
      throw new RuntimeError(406, ngDevMode && "This instance of the `ApplicationRef` has already been destroyed.");
    }
    const injector = this._injector;
    if (injector.destroy && !injector.destroyed) {
      injector.destroy();
    }
  }
  get viewCount() {
    return this._views.length;
  }
  static \u0275fac = function ApplicationRef_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ApplicationRef)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ApplicationRef,
    factory: _ApplicationRef.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ApplicationRef, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function warnIfDestroyed(destroyed) {
  if (destroyed) {
    console.warn(formatRuntimeError(406, "This instance of the `ApplicationRef` has already been destroyed."));
  }
}
function remove(list, el) {
  const index = list.indexOf(el);
  if (index > -1) {
    list.splice(index, 1);
  }
}
function promiseWithResolvers() {
  let resolve;
  let reject;
  const promise = new Promise((res, rej) => {
    resolve = res;
    reject = rej;
  });
  return {
    promise,
    resolve,
    reject
  };
}
function scheduleDelayedTrigger(scheduleFn) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  renderPlaceholder(lView, tNode);
  if (!shouldTriggerDeferBlock(0, lView)) return;
  const injector = lView[INJECTOR];
  const lDetails = getLDeferBlockDetails(lView, tNode);
  const cleanupFn = scheduleFn(() => triggerDeferBlock(0, lView, tNode), injector);
  storeTriggerCleanupFn(0, lDetails, cleanupFn);
}
function scheduleDelayedPrefetching(scheduleFn) {
  if (false) return;
  const lView = getLView();
  const injector = lView[INJECTOR];
  const tNode = getCurrentTNode();
  const tView = lView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  if (tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
    const lDetails = getLDeferBlockDetails(lView, tNode);
    const prefetch = () => triggerPrefetching(tDetails, lView, tNode);
    const cleanupFn = scheduleFn(prefetch, injector);
    storeTriggerCleanupFn(1, lDetails, cleanupFn);
  }
}
function scheduleDelayedHydrating(scheduleFn, lView, tNode) {
  if (false) return;
  const injector = lView[INJECTOR];
  const lDetails = getLDeferBlockDetails(lView, tNode);
  const ssrUniqueId = lDetails[SSR_UNIQUE_ID];
  ngDevMode && assertSsrIdDefined(ssrUniqueId);
  const cleanupFn = scheduleFn(() => triggerHydrationFromBlockName(injector, ssrUniqueId), injector);
  storeTriggerCleanupFn(2, lDetails, cleanupFn);
}
function triggerPrefetching(tDetails, lView, tNode) {
  triggerResourceLoading(tDetails, lView, tNode);
}
function triggerResourceLoading(tDetails, lView, tNode) {
  const injector = lView[INJECTOR];
  const tView = lView[TVIEW];
  if (tDetails.loadingState !== DeferDependenciesLoadingState.NOT_STARTED) {
    return tDetails.loadingPromise ?? Promise.resolve();
  }
  const lDetails = getLDeferBlockDetails(lView, tNode);
  const primaryBlockTNode = getPrimaryBlockTNode(tView, tDetails);
  tDetails.loadingState = DeferDependenciesLoadingState.IN_PROGRESS;
  invokeTriggerCleanupFns(1, lDetails);
  let dependenciesFn = tDetails.dependencyResolverFn;
  if (ngDevMode) {
    const deferDependencyInterceptor = injector.get(DEFER_BLOCK_DEPENDENCY_INTERCEPTOR, null, {
      optional: true
    });
    if (deferDependencyInterceptor) {
      dependenciesFn = deferDependencyInterceptor.intercept(dependenciesFn);
    }
  }
  const removeTask = injector.get(PendingTasks).add();
  if (!dependenciesFn) {
    tDetails.loadingPromise = Promise.resolve().then(() => {
      tDetails.loadingPromise = null;
      tDetails.loadingState = DeferDependenciesLoadingState.COMPLETE;
      removeTask();
    });
    return tDetails.loadingPromise;
  }
  tDetails.loadingPromise = Promise.allSettled(dependenciesFn()).then((results) => {
    let failed = false;
    let failedReason = null;
    const directiveDefs = [];
    const pipeDefs = [];
    for (let i = 0; i < results.length; i++) {
      const result = results[i];
      if (result.status === "fulfilled") {
        const dependency = result.value;
        const directiveDef = getComponentDef(dependency) || getDirectiveDef(dependency);
        if (directiveDef) {
          directiveDefs.push(directiveDef);
        } else {
          const pipeDef = getPipeDef(dependency);
          if (pipeDef) {
            pipeDefs.push(pipeDef);
          }
        }
      } else {
        failed = true;
        failedReason = result.reason instanceof Error ? result.reason : new Error(String(result.reason));
        break;
      }
    }
    if (failed) {
      tDetails.loadingState = DeferDependenciesLoadingState.FAILED;
      if (tDetails.errorTmplIndex === null) {
        const templateLocation = ngDevMode ? getTemplateLocationDetails(lView) : "";
        let errorMsg = "";
        if (ngDevMode) {
          errorMsg = `Loading dependencies for \`@defer\` block failed, but no \`@error\` block was configured${templateLocation}. Consider using the \`@error\` block to render an error state.`;
          const depsFn = tDetails.dependencyResolverFn;
          const errorReason = failedReason?.message;
          if (depsFn) {
            errorMsg += `

Angular tried to invoke the following dependency function (compiler-generated):
\`\`\`
${depsFn.toString()}
\`\`\``;
          }
          if (errorReason) {
            errorMsg += depsFn ? `

but it resulted in the following error:

${errorReason}` : `

The loading resulted in the following error:

${errorReason}`;
          }
        }
        const error = new RuntimeError(-750, errorMsg);
        handleUncaughtError(lView, error);
      }
    } else {
      tDetails.loadingState = DeferDependenciesLoadingState.COMPLETE;
      const primaryBlockTView = primaryBlockTNode.tView;
      if (directiveDefs.length > 0) {
        primaryBlockTView.directiveRegistry = addDepsToRegistry(primaryBlockTView.directiveRegistry, directiveDefs);
        const directiveTypes = directiveDefs.map((def) => def.type);
        const providers = internalImportProvidersFrom(false, ...directiveTypes);
        tDetails.providers = providers;
      }
      if (pipeDefs.length > 0) {
        primaryBlockTView.pipeRegistry = addDepsToRegistry(primaryBlockTView.pipeRegistry, pipeDefs);
      }
    }
  });
  return tDetails.loadingPromise.finally(() => {
    tDetails.loadingPromise = null;
    removeTask();
  });
}
function shouldTriggerDeferBlock(triggerType, lView) {
  if (triggerType === 0 && true && false) {
    return false;
  }
  const injector = lView[INJECTOR];
  const config2 = injector.get(DEFER_BLOCK_CONFIG, null, {
    optional: true
  });
  if (config2?.behavior === DeferBlockBehavior.Manual) {
    return false;
  }
  return true;
}
function triggerDeferBlock(triggerType, lView, tNode) {
  const tView = lView[TVIEW];
  const lContainer = lView[tNode.index];
  ngDevMode && assertLContainer(lContainer);
  if (!shouldTriggerDeferBlock(triggerType, lView)) return;
  const lDetails = getLDeferBlockDetails(lView, tNode);
  const tDetails = getTDeferBlockDetails(tView, tNode);
  invokeAllTriggerCleanupFns(lDetails);
  switch (tDetails.loadingState) {
    case DeferDependenciesLoadingState.NOT_STARTED:
      renderDeferBlockState(DeferBlockState.Loading, tNode, lContainer);
      triggerResourceLoading(tDetails, lView, tNode);
      if (tDetails.loadingState === DeferDependenciesLoadingState.IN_PROGRESS) {
        renderDeferStateAfterResourceLoading(tDetails, tNode, lContainer);
      }
      break;
    case DeferDependenciesLoadingState.IN_PROGRESS:
      renderDeferBlockState(DeferBlockState.Loading, tNode, lContainer);
      renderDeferStateAfterResourceLoading(tDetails, tNode, lContainer);
      break;
    case DeferDependenciesLoadingState.COMPLETE:
      ngDevMode && assertDeferredDependenciesLoaded(tDetails);
      renderDeferBlockState(DeferBlockState.Complete, tNode, lContainer);
      break;
    case DeferDependenciesLoadingState.FAILED:
      renderDeferBlockState(DeferBlockState.Error, tNode, lContainer);
      break;
    default:
      if (ngDevMode) {
        throwError2("Unknown defer block state");
      }
  }
}
async function triggerHydrationFromBlockName(injector, blockName, replayQueuedEventsFn) {
  const dehydratedBlockRegistry = injector.get(DEHYDRATED_BLOCK_REGISTRY);
  const blocksBeingHydrated = dehydratedBlockRegistry.hydrating;
  if (blocksBeingHydrated.has(blockName)) {
    return;
  }
  const {
    parentBlockPromise,
    hydrationQueue
  } = getParentBlockHydrationQueue(blockName, injector);
  if (hydrationQueue.length === 0) return;
  if (parentBlockPromise !== null) {
    hydrationQueue.shift();
  }
  populateHydratingStateForQueue(dehydratedBlockRegistry, hydrationQueue);
  if (parentBlockPromise !== null) {
    await parentBlockPromise;
  }
  const topmostParentBlock = hydrationQueue[0];
  if (dehydratedBlockRegistry.has(topmostParentBlock)) {
    await triggerHydrationForBlockQueue(injector, hydrationQueue, replayQueuedEventsFn);
  } else {
    dehydratedBlockRegistry.awaitParentBlock(topmostParentBlock, async () => await triggerHydrationForBlockQueue(injector, hydrationQueue, replayQueuedEventsFn));
  }
}
async function triggerHydrationForBlockQueue(injector, hydrationQueue, replayQueuedEventsFn) {
  const dehydratedBlockRegistry = injector.get(DEHYDRATED_BLOCK_REGISTRY);
  const blocksBeingHydrated = dehydratedBlockRegistry.hydrating;
  const pendingTasks = injector.get(PendingTasksInternal);
  const taskId = pendingTasks.add();
  for (let blockQueueIdx = 0; blockQueueIdx < hydrationQueue.length; blockQueueIdx++) {
    const dehydratedBlockId = hydrationQueue[blockQueueIdx];
    const dehydratedDeferBlock = dehydratedBlockRegistry.get(dehydratedBlockId);
    if (dehydratedDeferBlock != null) {
      await triggerResourceLoadingForHydration(dehydratedDeferBlock);
      await nextRender(injector);
      if (deferBlockHasErrored(dehydratedDeferBlock)) {
        removeDehydratedViewList(dehydratedDeferBlock);
        cleanupRemainingHydrationQueue(hydrationQueue.slice(blockQueueIdx), dehydratedBlockRegistry);
        break;
      }
      blocksBeingHydrated.get(dehydratedBlockId).resolve();
    } else {
      cleanupParentContainer(blockQueueIdx, hydrationQueue, dehydratedBlockRegistry);
      cleanupRemainingHydrationQueue(hydrationQueue.slice(blockQueueIdx), dehydratedBlockRegistry);
      break;
    }
  }
  const lastBlockName = hydrationQueue[hydrationQueue.length - 1];
  await blocksBeingHydrated.get(lastBlockName)?.promise;
  pendingTasks.remove(taskId);
  if (replayQueuedEventsFn) {
    replayQueuedEventsFn(hydrationQueue);
  }
  cleanupHydratedDeferBlocks(dehydratedBlockRegistry.get(lastBlockName), hydrationQueue, dehydratedBlockRegistry, injector.get(ApplicationRef));
}
function deferBlockHasErrored(deferBlock) {
  return getLDeferBlockDetails(deferBlock.lView, deferBlock.tNode)[DEFER_BLOCK_STATE] === DeferBlockState.Error;
}
function cleanupParentContainer(currentBlockIdx, hydrationQueue, dehydratedBlockRegistry) {
  const parentDeferBlockIdx = currentBlockIdx - 1;
  const parentDeferBlock = parentDeferBlockIdx > -1 ? dehydratedBlockRegistry.get(hydrationQueue[parentDeferBlockIdx]) : null;
  if (parentDeferBlock) {
    cleanupLContainer(parentDeferBlock.lContainer);
  }
}
function cleanupRemainingHydrationQueue(hydrationQueue, dehydratedBlockRegistry) {
  const blocksBeingHydrated = dehydratedBlockRegistry.hydrating;
  for (const dehydratedBlockId in hydrationQueue) {
    blocksBeingHydrated.get(dehydratedBlockId)?.reject();
  }
  dehydratedBlockRegistry.cleanup(hydrationQueue);
}
function populateHydratingStateForQueue(registry, queue) {
  for (let blockId of queue) {
    registry.hydrating.set(blockId, promiseWithResolvers());
  }
}
function nextRender(injector) {
  return new Promise((resolveFn) => afterNextRender(resolveFn, {
    injector
  }));
}
async function triggerResourceLoadingForHydration(dehydratedBlock) {
  const {
    tNode,
    lView
  } = dehydratedBlock;
  const lDetails = getLDeferBlockDetails(lView, tNode);
  return new Promise((resolve) => {
    onDeferBlockCompletion(lDetails, resolve);
    triggerDeferBlock(2, lView, tNode);
  });
}
function onDeferBlockCompletion(lDetails, callback) {
  if (!Array.isArray(lDetails[ON_COMPLETE_FNS])) {
    lDetails[ON_COMPLETE_FNS] = [];
  }
  lDetails[ON_COMPLETE_FNS].push(callback);
}
function shouldAttachTrigger(triggerType, lView, tNode) {
  if (triggerType === 0) {
    return shouldAttachRegularTrigger(lView, tNode);
  } else if (triggerType === 2) {
    return !shouldAttachRegularTrigger(lView, tNode);
  }
  return true;
}
function hasHydrateTriggers(flags) {
  return flags != null && (flags & 1) === 1;
}
function shouldAttachRegularTrigger(lView, tNode) {
  const injector = lView[INJECTOR];
  const tDetails = getTDeferBlockDetails(lView[TVIEW], tNode);
  const incrementalHydrationEnabled = isIncrementalHydrationEnabled(injector);
  const _hasHydrateTriggers = hasHydrateTriggers(tDetails.flags);
  if (false) {
    return !incrementalHydrationEnabled || !_hasHydrateTriggers;
  }
  const lDetails = getLDeferBlockDetails(lView, tNode);
  const wasServerSideRendered = lDetails[SSR_UNIQUE_ID] !== null;
  if (_hasHydrateTriggers && wasServerSideRendered && incrementalHydrationEnabled) {
    return false;
  }
  return true;
}
function getHydrateTriggers(tView, tNode) {
  const tDetails = getTDeferBlockDetails(tView, tNode);
  return tDetails.hydrateTriggers ??= /* @__PURE__ */ new Map();
}
function \u0275\u0275defer(index, primaryTmplIndex, dependencyResolverFn, loadingTmplIndex, placeholderTmplIndex, errorTmplIndex, loadingConfigIndex, placeholderConfigIndex, enableTimerScheduling, flags) {
  const lView = getLView();
  const tView = getTView();
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = declareNoDirectiveHostTemplate(lView, tView, index, null, 0, 0);
  const injector = lView[INJECTOR];
  const incrementalHydrationEnabled = isIncrementalHydrationEnabled(injector);
  if (tView.firstCreatePass) {
    performanceMarkFeature("NgDefer");
    if (ngDevMode) {
      if (false) {
        logHmrWarning(injector);
      }
      if (hasHydrateTriggers(flags) && !incrementalHydrationEnabled) {
        warnIncrementalHydrationNotConfigured();
      }
    }
    const tDetails = {
      primaryTmplIndex,
      loadingTmplIndex: loadingTmplIndex ?? null,
      placeholderTmplIndex: placeholderTmplIndex ?? null,
      errorTmplIndex: errorTmplIndex ?? null,
      placeholderBlockConfig: null,
      loadingBlockConfig: null,
      dependencyResolverFn: dependencyResolverFn ?? null,
      loadingState: DeferDependenciesLoadingState.NOT_STARTED,
      loadingPromise: null,
      providers: null,
      hydrateTriggers: null,
      debug: null,
      flags: flags ?? 0
    };
    enableTimerScheduling?.(tView, tDetails, placeholderConfigIndex, loadingConfigIndex);
    setTDeferBlockDetails(tView, adjustedIndex, tDetails);
  }
  const lContainer = lView[adjustedIndex];
  populateDehydratedViewsInLContainer(lContainer, tNode, lView);
  let ssrBlockState = null;
  let ssrUniqueId = null;
  if (lContainer[DEHYDRATED_VIEWS]?.length > 0) {
    const info = lContainer[DEHYDRATED_VIEWS][0].data;
    ssrUniqueId = info[DEFER_BLOCK_ID] ?? null;
    ssrBlockState = info[DEFER_BLOCK_STATE$1];
  }
  const lDetails = [null, DeferBlockInternalState.Initial, null, null, null, null, ssrUniqueId, ssrBlockState, null, null];
  setLDeferBlockDetails(lView, adjustedIndex, lDetails);
  let registry = null;
  if (ssrUniqueId !== null && incrementalHydrationEnabled) {
    registry = injector.get(DEHYDRATED_BLOCK_REGISTRY);
    registry.add(ssrUniqueId, {
      lView,
      tNode,
      lContainer
    });
  }
  const onLViewDestroy = () => {
    invokeAllTriggerCleanupFns(lDetails);
    if (ssrUniqueId !== null) {
      registry?.cleanup([ssrUniqueId]);
    }
  };
  storeTriggerCleanupFn(0, lDetails, () => removeLViewOnDestroy(lView, onLViewDestroy));
  storeLViewOnDestroy(lView, onLViewDestroy);
}
function \u0275\u0275deferWhen(rawValue) {
  const lView = getLView();
  const tNode = getSelectedTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "when <expression>");
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, rawValue)) {
    const prevConsumer = setActiveConsumer(null);
    try {
      const value = Boolean(rawValue);
      const lDetails = getLDeferBlockDetails(lView, tNode);
      const renderedState = lDetails[DEFER_BLOCK_STATE];
      if (value === false && renderedState === DeferBlockInternalState.Initial) {
        renderPlaceholder(lView, tNode);
      } else if (value === true && (renderedState === DeferBlockInternalState.Initial || renderedState === DeferBlockState.Placeholder)) {
        triggerDeferBlock(0, lView, tNode);
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
}
function \u0275\u0275deferPrefetchWhen(rawValue) {
  const lView = getLView();
  const tNode = getSelectedTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "prefetch when <expression>");
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, rawValue)) {
    const prevConsumer = setActiveConsumer(null);
    try {
      const value = Boolean(rawValue);
      const tView = lView[TVIEW];
      const tDetails = getTDeferBlockDetails(tView, tNode);
      if (value === true && tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
        triggerPrefetching(tDetails, lView, tNode);
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  }
}
function \u0275\u0275deferHydrateWhen(rawValue) {
  const lView = getLView();
  const tNode = getSelectedTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate when <expression>");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const bindingIndex = nextBindingIndex();
  const tView = getTView();
  const hydrateTriggers = getHydrateTriggers(tView, tNode);
  hydrateTriggers.set(6, null);
  if (bindingUpdated(lView, bindingIndex, rawValue)) {
    if (false) {
      triggerDeferBlock(2, lView, tNode);
    } else {
      const injector = lView[INJECTOR];
      const prevConsumer = setActiveConsumer(null);
      try {
        const value = Boolean(rawValue);
        if (value === true) {
          const lDetails = getLDeferBlockDetails(lView, tNode);
          const ssrUniqueId = lDetails[SSR_UNIQUE_ID];
          ngDevMode && assertSsrIdDefined(ssrUniqueId);
          triggerHydrationFromBlockName(injector, ssrUniqueId);
        }
      } finally {
        setActiveConsumer(prevConsumer);
      }
    }
  }
}
function \u0275\u0275deferHydrateNever() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate never");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(7, null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  }
}
function \u0275\u0275deferOnIdle() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "on idle");
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  scheduleDelayedTrigger(onIdle);
}
function \u0275\u0275deferPrefetchOnIdle() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "prefetch on idle");
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  scheduleDelayedPrefetching(onIdle);
}
function \u0275\u0275deferHydrateOnIdle() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate on idle");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(0, null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  } else {
    scheduleDelayedHydrating(onIdle, lView, tNode);
  }
}
function \u0275\u0275deferOnImmediate() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "on immediate");
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  const tDetails = getTDeferBlockDetails(lView[TVIEW], tNode);
  if (tDetails.loadingTmplIndex === null) {
    renderPlaceholder(lView, tNode);
  }
  triggerDeferBlock(0, lView, tNode);
}
function \u0275\u0275deferPrefetchOnImmediate() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "prefetch on immediate");
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  const tView = lView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  if (tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
    triggerResourceLoading(tDetails, lView, tNode);
  }
}
function \u0275\u0275deferHydrateOnImmediate() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate on immediate");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(1, null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  } else {
    const injector = lView[INJECTOR];
    const lDetails = getLDeferBlockDetails(lView, tNode);
    const ssrUniqueId = lDetails[SSR_UNIQUE_ID];
    ngDevMode && assertSsrIdDefined(ssrUniqueId);
    triggerHydrationFromBlockName(injector, ssrUniqueId);
  }
}
function \u0275\u0275deferOnTimer(delay) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `on timer(${delay}ms)`);
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  scheduleDelayedTrigger(onTimer(delay));
}
function \u0275\u0275deferPrefetchOnTimer(delay) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `prefetch on timer(${delay}ms)`);
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  scheduleDelayedPrefetching(onTimer(delay));
}
function \u0275\u0275deferHydrateOnTimer(delay) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `hydrate on timer(${delay}ms)`);
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(5, {
    type: 5,
    delay
  });
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  } else {
    scheduleDelayedHydrating(onTimer(delay), lView, tNode);
  }
}
function \u0275\u0275deferOnHover(triggerIndex, walkUpTimes) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `on hover${walkUpTimes === -1 ? "" : "(<target>)"}`);
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  renderPlaceholder(lView, tNode);
  if (true) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onHover, () => triggerDeferBlock(0, lView, tNode), 0);
  }
}
function \u0275\u0275deferPrefetchOnHover(triggerIndex, walkUpTimes) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `prefetch on hover${walkUpTimes === -1 ? "" : "(<target>)"}`);
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  const tView = lView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  if (tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onHover, () => triggerPrefetching(tDetails, lView, tNode), 1);
  }
}
function \u0275\u0275deferHydrateOnHover() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate on hover");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(4, null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  }
}
function \u0275\u0275deferOnInteraction(triggerIndex, walkUpTimes) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `on interaction${walkUpTimes === -1 ? "" : "(<target>)"}`);
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  renderPlaceholder(lView, tNode);
  if (true) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onInteraction, () => triggerDeferBlock(0, lView, tNode), 0);
  }
}
function \u0275\u0275deferPrefetchOnInteraction(triggerIndex, walkUpTimes) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `prefetch on interaction${walkUpTimes === -1 ? "" : "(<target>)"}`);
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  const tView = lView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  if (tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onInteraction, () => triggerPrefetching(tDetails, lView, tNode), 1);
  }
}
function \u0275\u0275deferHydrateOnInteraction() {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, "hydrate on interaction");
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(3, null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  }
}
function \u0275\u0275deferOnViewport(triggerIndex, walkUpTimes, options) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    const args = [];
    if (walkUpTimes !== void 0 && walkUpTimes !== -1) {
      args.push("<target>");
    }
    if (options) {
      args.push(JSON.stringify(options));
    }
    trackTriggerForDebugging(lView[TVIEW], tNode, `on viewport${args.length === 0 ? "" : `(${args.join(", ")})`}`);
  }
  if (!shouldAttachTrigger(0, lView, tNode)) return;
  renderPlaceholder(lView, tNode);
  if (true) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onViewportWrapper, () => triggerDeferBlock(0, lView, tNode), 0, options);
  }
}
function \u0275\u0275deferPrefetchOnViewport(triggerIndex, walkUpTimes, options) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    const args = [];
    if (walkUpTimes !== void 0 && walkUpTimes !== -1) {
      args.push("<target>");
    }
    if (options) {
      args.push(JSON.stringify(options));
    }
    trackTriggerForDebugging(lView[TVIEW], tNode, `prefetch on viewport${args.length === 0 ? "" : `(${args.join(", ")})`}`);
  }
  if (!shouldAttachTrigger(1, lView, tNode)) return;
  const tView = lView[TVIEW];
  const tDetails = getTDeferBlockDetails(tView, tNode);
  if (tDetails.loadingState === DeferDependenciesLoadingState.NOT_STARTED) {
    registerDomTrigger(lView, tNode, triggerIndex, walkUpTimes, onViewportWrapper, () => triggerPrefetching(tDetails, lView, tNode), 1, options);
  }
}
function \u0275\u0275deferHydrateOnViewport(options) {
  const lView = getLView();
  const tNode = getCurrentTNode();
  if (ngDevMode) {
    trackTriggerForDebugging(lView[TVIEW], tNode, `hydrate on viewport${options ? `(${JSON.stringify(options)})` : ""}`);
  }
  if (!shouldAttachTrigger(2, lView, tNode)) return;
  const hydrateTriggers = getHydrateTriggers(getTView(), tNode);
  hydrateTriggers.set(2, options ? {
    type: 2,
    intersectionObserverOptions: options
  } : null);
  if (false) {
    triggerDeferBlock(2, lView, tNode);
  }
}
function \u0275\u0275ariaProperty(name, value) {
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    const hasSetInput = setAllInputsForProperty(tNode, tView, lView, name, value);
    if (hasSetInput) {
      isComponentHost(tNode) && markDirtyIfOnPush(lView, tNode.index);
      ngDevMode && setNgReflectProperties(lView, tView, tNode, name, value);
    } else {
      ngDevMode && assertTNodeType(tNode, 2);
      const element = getNativeByTNode(tNode, lView);
      setElementAttribute(lView[RENDERER], element, null, tNode.value, name, value, null);
    }
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, name, bindingIndex);
  }
  return \u0275\u0275ariaProperty;
}
function \u0275\u0275attribute(name, value, sanitizer, namespace) {
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    elementAttributeInternal(tNode, lView, name, value, sanitizer, namespace);
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, "attr." + name, bindingIndex);
  }
  return \u0275\u0275attribute;
}
function \u0275\u0275animateEnter(value) {
  performanceMarkFeature("NgAnimateEnter");
  if (!areAnimationSupported) {
    return \u0275\u0275animateEnter;
  }
  ngDevMode && assertAnimationTypes(value, "animate.enter");
  const lView = getLView();
  if (areAnimationsDisabled(lView)) {
    return \u0275\u0275animateEnter;
  }
  const tNode = getCurrentTNode();
  const ngZone = lView[INJECTOR].get(NgZone);
  addAnimationToLView(getLViewEnterAnimations(lView), tNode, () => runEnterAnimation(lView, tNode, value, ngZone));
  initializeAnimationQueueScheduler(lView[INJECTOR]);
  queueEnterAnimations(lView[INJECTOR], getLViewEnterAnimations(lView));
  return \u0275\u0275animateEnter;
}
function runEnterAnimation(lView, tNode, value, ngZone) {
  const nativeElement = getNativeByTNode(tNode, lView);
  ngDevMode && assertElementNodes(nativeElement, "animate.enter");
  const renderer = lView[RENDERER];
  const activeClasses = getClassListFromValue(value);
  const cleanupFns = [];
  let hasCompleted = false;
  const handleEnterAnimationStart = (event) => {
    if (getEventTarget(event) !== nativeElement) return;
    const eventName = event instanceof AnimationEvent ? "animationend" : "transitionend";
    ngZone.runOutsideAngular(() => {
      renderer.listen(nativeElement, eventName, handleEnterAnimationEnd);
    });
  };
  const handleEnterAnimationEnd = (event) => {
    if (getEventTarget(event) !== nativeElement) return;
    if (isLongestAnimation(event, nativeElement)) {
      hasCompleted = true;
    }
    enterAnimationEnd(event, nativeElement, renderer);
  };
  if (activeClasses && activeClasses.length > 0) {
    ngZone.runOutsideAngular(() => {
      cleanupFns.push(renderer.listen(nativeElement, "animationstart", handleEnterAnimationStart));
      cleanupFns.push(renderer.listen(nativeElement, "transitionstart", handleEnterAnimationStart));
    });
    trackEnterClasses(nativeElement, activeClasses, cleanupFns);
    for (const klass of activeClasses) {
      renderer.addClass(nativeElement, klass);
    }
    ngZone.runOutsideAngular(() => {
      requestAnimationFrame(() => {
        if (hasCompleted) return;
        determineLongestAnimation(nativeElement, longestAnimations, areAnimationSupported);
        if (!longestAnimations.has(nativeElement)) {
          for (const klass of activeClasses) {
            renderer.removeClass(nativeElement, klass);
          }
          cleanupEnterClassData(nativeElement);
        }
      });
    });
  }
}
function enterAnimationEnd(event, nativeElement, renderer) {
  const elementData = enterClassMap.get(nativeElement);
  if (getEventTarget(event) !== nativeElement || !elementData) return;
  if (isLongestAnimation(event, nativeElement)) {
    event.stopPropagation();
    for (const klass of elementData.classList) {
      renderer.removeClass(nativeElement, klass);
    }
    cleanupEnterClassData(nativeElement);
  }
}
function \u0275\u0275animateEnterListener(value) {
  performanceMarkFeature("NgAnimateEnter");
  if (!areAnimationSupported) {
    return \u0275\u0275animateEnterListener;
  }
  ngDevMode && assertAnimationTypes(value, "animate.enter");
  const lView = getLView();
  if (areAnimationsDisabled(lView)) {
    return \u0275\u0275animateEnterListener;
  }
  const tNode = getCurrentTNode();
  addAnimationToLView(getLViewEnterAnimations(lView), tNode, () => runEnterAnimationFunction(lView, tNode, value));
  initializeAnimationQueueScheduler(lView[INJECTOR]);
  queueEnterAnimations(lView[INJECTOR], getLViewEnterAnimations(lView));
  return \u0275\u0275animateEnterListener;
}
function runEnterAnimationFunction(lView, tNode, value) {
  const nativeElement = getNativeByTNode(tNode, lView);
  ngDevMode && assertElementNodes(nativeElement, "animate.enter");
  value.call(lView[CONTEXT], {
    target: nativeElement,
    animationComplete: noOpAnimationComplete
  });
}
function \u0275\u0275animateLeave(value) {
  performanceMarkFeature("NgAnimateLeave");
  if (!areAnimationSupported) {
    return \u0275\u0275animateLeave;
  }
  ngDevMode && assertAnimationTypes(value, "animate.leave");
  const lView = getLView();
  const animationsDisabled = areAnimationsDisabled(lView);
  if (animationsDisabled) {
    return \u0275\u0275animateLeave;
  }
  const tNode = getCurrentTNode();
  const ngZone = lView[INJECTOR].get(NgZone);
  addAnimationToLView(getLViewLeaveAnimations(lView), tNode, () => runLeaveAnimations(lView, tNode, value, ngZone));
  initializeAnimationQueueScheduler(lView[INJECTOR]);
  return \u0275\u0275animateLeave;
}
function runLeaveAnimations(lView, tNode, value, ngZone) {
  const {
    promise,
    resolve
  } = promiseWithResolvers();
  const nativeElement = getNativeByTNode(tNode, lView);
  ngDevMode && assertElementNodes(nativeElement, "animate.leave");
  const renderer = lView[RENDERER];
  allLeavingAnimations.add(lView[ID]);
  (getLViewLeaveAnimations(lView).get(tNode.index).resolvers ??= []).push(resolve);
  const activeClasses = getClassListFromValue(value);
  if (activeClasses && activeClasses.length > 0) {
    animateLeaveClassRunner(nativeElement, tNode, lView, activeClasses, renderer, ngZone);
  } else {
    resolve();
  }
  return {
    promise,
    resolve
  };
}
function animateLeaveClassRunner(el, tNode, lView, classList, renderer, ngZone) {
  cancelAnimationsIfRunning(el, renderer);
  const cleanupFns = [];
  const componentResolvers = getLViewLeaveAnimations(lView).get(tNode.index)?.resolvers;
  let fallbackTimeoutId;
  let hasCompleted = false;
  const handleOutAnimationEnd = (event) => {
    const target = getEventTarget(event);
    if (target !== el && event.type !== "animation-fallback") return;
    if (event.type === "animation-fallback" || isLongestAnimation(event, el)) {
      hasCompleted = true;
      if (fallbackTimeoutId) clearTimeout(fallbackTimeoutId);
      if (event.type !== "animation-fallback") event.stopPropagation();
      longestAnimations.delete(el);
      clearLeavingNodes(tNode, el);
      if (Array.isArray(tNode.projection)) {
        for (const item of classList) {
          renderer.removeClass(el, item);
        }
      }
      cleanupAfterLeaveAnimations(componentResolvers, cleanupFns);
      clearLViewNodeAnimationResolvers(lView, tNode);
    }
  };
  ngZone.runOutsideAngular(() => {
    cleanupFns.push(renderer.listen(el, "animationend", handleOutAnimationEnd));
    cleanupFns.push(renderer.listen(el, "transitionend", handleOutAnimationEnd));
  });
  trackLeavingNodes(tNode, el);
  for (const item of classList) {
    renderer.addClass(el, item);
  }
  ngZone.runOutsideAngular(() => {
    requestAnimationFrame(() => {
      if (hasCompleted) return;
      determineLongestAnimation(el, longestAnimations, areAnimationSupported);
      const longest = longestAnimations.get(el);
      if (!longest) {
        clearLeavingNodes(tNode, el);
        cleanupAfterLeaveAnimations(componentResolvers, cleanupFns);
        clearLViewNodeAnimationResolvers(lView, tNode);
      } else {
        fallbackTimeoutId = setTimeout(() => {
          handleOutAnimationEnd(new CustomEvent("animation-fallback"));
        }, longest.duration + 50);
        cleanupFns.push(() => clearTimeout(fallbackTimeoutId));
      }
    });
  });
}
function \u0275\u0275animateLeaveListener(value) {
  performanceMarkFeature("NgAnimateLeave");
  if (!areAnimationSupported) {
    return \u0275\u0275animateLeaveListener;
  }
  ngDevMode && assertAnimationTypes(value, "animate.leave");
  const lView = getLView();
  const tNode = getCurrentTNode();
  allLeavingAnimations.add(lView[ID]);
  const ngZone = lView[INJECTOR].get(NgZone);
  const maxAnimationTimeout = lView[INJECTOR].get(MAX_ANIMATION_TIMEOUT);
  addAnimationToLView(getLViewLeaveAnimations(lView), tNode, () => runLeaveAnimationFunction(lView, tNode, value, ngZone, maxAnimationTimeout));
  initializeAnimationQueueScheduler(lView[INJECTOR]);
  return \u0275\u0275animateLeaveListener;
}
function runLeaveAnimationFunction(lView, tNode, value, ngZone, maxAnimationTimeout) {
  const {
    promise,
    resolve
  } = promiseWithResolvers();
  const nativeElement = getNativeByTNode(tNode, lView);
  ngDevMode && assertElementNodes(nativeElement, "animate.leave");
  const cleanupFns = [];
  const renderer = lView[RENDERER];
  const animationsDisabled = areAnimationsDisabled(lView);
  (getLViewLeaveAnimations(lView).get(tNode.index).resolvers ??= []).push(resolve);
  const resolvers = getLViewLeaveAnimations(lView).get(tNode.index)?.resolvers;
  if (animationsDisabled) {
    leaveAnimationFunctionCleanup(lView, tNode, nativeElement, resolvers, cleanupFns);
  } else {
    const timeoutId = setTimeout(() => leaveAnimationFunctionCleanup(lView, tNode, nativeElement, resolvers, cleanupFns), maxAnimationTimeout);
    const event = {
      target: nativeElement,
      animationComplete: () => {
        leaveAnimationFunctionCleanup(lView, tNode, nativeElement, resolvers, cleanupFns);
        clearTimeout(timeoutId);
      }
    };
    trackLeavingNodes(tNode, nativeElement);
    ngZone.runOutsideAngular(() => {
      cleanupFns.push(renderer.listen(nativeElement, "animationend", () => {
        leaveAnimationFunctionCleanup(lView, tNode, nativeElement, resolvers, cleanupFns);
        clearTimeout(timeoutId);
      }, {
        once: true
      }));
    });
    value.call(lView[CONTEXT], event);
  }
  return {
    promise,
    resolve
  };
}
function \u0275\u0275componentInstance() {
  const instance = getLView()[DECLARATION_COMPONENT_VIEW][CONTEXT];
  ngDevMode && assertDefined(instance, "Expected component instance to be defined");
  return instance;
}
var LiveCollection = class {
  destroy(item) {
  }
  updateValue(index, value) {
  }
  swap(index1, index2) {
    const startIdx = Math.min(index1, index2);
    const endIdx = Math.max(index1, index2);
    const endItem = this.detach(endIdx);
    if (endIdx - startIdx > 1) {
      const startItem = this.detach(startIdx);
      this.attach(startIdx, endItem);
      this.attach(endIdx, startItem);
    } else {
      this.attach(startIdx, endItem);
    }
  }
  move(prevIndex, newIdx) {
    this.attach(newIdx, this.detach(prevIndex));
  }
};
function valuesMatching(liveIdx, liveValue, newIdx, newValue, trackBy) {
  if (liveIdx === newIdx && Object.is(liveValue, newValue)) {
    return 1;
  } else if (Object.is(trackBy(liveIdx, liveValue), trackBy(newIdx, newValue))) {
    return -1;
  }
  return 0;
}
function recordDuplicateKeys(keyToIdx, key, idx) {
  const idxSoFar = keyToIdx.get(key);
  if (idxSoFar !== void 0) {
    idxSoFar.add(idx);
  } else {
    keyToIdx.set(key, /* @__PURE__ */ new Set([idx]));
  }
}
function reconcile(liveCollection, newCollection, trackByFn, reactiveConsumer) {
  let detachedItems = void 0;
  let liveKeysInTheFuture = void 0;
  let liveStartIdx = 0;
  let liveEndIdx = liveCollection.length - 1;
  const duplicateKeys = ngDevMode ? /* @__PURE__ */ new Map() : void 0;
  if (Array.isArray(newCollection)) {
    setActiveConsumer(reactiveConsumer);
    let newEndIdx = newCollection.length - 1;
    setActiveConsumer(null);
    while (liveStartIdx <= liveEndIdx && liveStartIdx <= newEndIdx) {
      const liveStartValue = liveCollection.at(liveStartIdx);
      const newStartValue = newCollection[liveStartIdx];
      if (ngDevMode) {
        recordDuplicateKeys(duplicateKeys, trackByFn(liveStartIdx, newStartValue), liveStartIdx);
      }
      const isStartMatching = valuesMatching(liveStartIdx, liveStartValue, liveStartIdx, newStartValue, trackByFn);
      if (isStartMatching !== 0) {
        if (isStartMatching < 0) {
          liveCollection.updateValue(liveStartIdx, newStartValue);
        }
        liveStartIdx++;
        continue;
      }
      const liveEndValue = liveCollection.at(liveEndIdx);
      const newEndValue = newCollection[newEndIdx];
      if (ngDevMode) {
        recordDuplicateKeys(duplicateKeys, trackByFn(newEndIdx, newEndValue), newEndIdx);
      }
      const isEndMatching = valuesMatching(liveEndIdx, liveEndValue, newEndIdx, newEndValue, trackByFn);
      if (isEndMatching !== 0) {
        if (isEndMatching < 0) {
          liveCollection.updateValue(liveEndIdx, newEndValue);
        }
        liveEndIdx--;
        newEndIdx--;
        continue;
      }
      const liveStartKey = trackByFn(liveStartIdx, liveStartValue);
      const liveEndKey = trackByFn(liveEndIdx, liveEndValue);
      const newStartKey = trackByFn(liveStartIdx, newStartValue);
      if (Object.is(newStartKey, liveEndKey)) {
        const newEndKey = trackByFn(newEndIdx, newEndValue);
        if (Object.is(newEndKey, liveStartKey)) {
          liveCollection.swap(liveStartIdx, liveEndIdx);
          liveCollection.updateValue(liveEndIdx, newEndValue);
          newEndIdx--;
          liveEndIdx--;
        } else {
          liveCollection.move(liveEndIdx, liveStartIdx);
        }
        liveCollection.updateValue(liveStartIdx, newStartValue);
        liveStartIdx++;
        continue;
      }
      detachedItems ??= new UniqueValueMultiKeyMap();
      liveKeysInTheFuture ??= initLiveItemsInTheFuture(liveCollection, liveStartIdx, liveEndIdx, trackByFn);
      if (attachPreviouslyDetached(liveCollection, detachedItems, liveStartIdx, newStartKey)) {
        liveCollection.updateValue(liveStartIdx, newStartValue);
        liveStartIdx++;
        liveEndIdx++;
      } else if (!liveKeysInTheFuture.has(newStartKey)) {
        const newItem = liveCollection.create(liveStartIdx, newCollection[liveStartIdx]);
        liveCollection.attach(liveStartIdx, newItem);
        liveStartIdx++;
        liveEndIdx++;
      } else {
        detachedItems.set(liveStartKey, liveCollection.detach(liveStartIdx));
        liveEndIdx--;
      }
    }
    while (liveStartIdx <= newEndIdx) {
      createOrAttach(liveCollection, detachedItems, trackByFn, liveStartIdx, newCollection[liveStartIdx]);
      liveStartIdx++;
    }
  } else if (newCollection != null) {
    setActiveConsumer(reactiveConsumer);
    const newCollectionIterator = newCollection[Symbol.iterator]();
    setActiveConsumer(null);
    let newIterationResult = newCollectionIterator.next();
    while (!newIterationResult.done && liveStartIdx <= liveEndIdx) {
      const liveValue = liveCollection.at(liveStartIdx);
      const newValue = newIterationResult.value;
      if (ngDevMode) {
        recordDuplicateKeys(duplicateKeys, trackByFn(liveStartIdx, newValue), liveStartIdx);
      }
      const isStartMatching = valuesMatching(liveStartIdx, liveValue, liveStartIdx, newValue, trackByFn);
      if (isStartMatching !== 0) {
        if (isStartMatching < 0) {
          liveCollection.updateValue(liveStartIdx, newValue);
        }
        liveStartIdx++;
        newIterationResult = newCollectionIterator.next();
      } else {
        detachedItems ??= new UniqueValueMultiKeyMap();
        liveKeysInTheFuture ??= initLiveItemsInTheFuture(liveCollection, liveStartIdx, liveEndIdx, trackByFn);
        const newKey = trackByFn(liveStartIdx, newValue);
        if (attachPreviouslyDetached(liveCollection, detachedItems, liveStartIdx, newKey)) {
          liveCollection.updateValue(liveStartIdx, newValue);
          liveStartIdx++;
          liveEndIdx++;
          newIterationResult = newCollectionIterator.next();
        } else if (!liveKeysInTheFuture.has(newKey)) {
          liveCollection.attach(liveStartIdx, liveCollection.create(liveStartIdx, newValue));
          liveStartIdx++;
          liveEndIdx++;
          newIterationResult = newCollectionIterator.next();
        } else {
          const liveKey = trackByFn(liveStartIdx, liveValue);
          detachedItems.set(liveKey, liveCollection.detach(liveStartIdx));
          liveEndIdx--;
        }
      }
    }
    while (!newIterationResult.done) {
      createOrAttach(liveCollection, detachedItems, trackByFn, liveCollection.length, newIterationResult.value);
      newIterationResult = newCollectionIterator.next();
    }
  }
  while (liveStartIdx <= liveEndIdx) {
    liveCollection.destroy(liveCollection.detach(liveEndIdx--));
  }
  detachedItems?.forEach((item) => {
    liveCollection.destroy(item);
  });
  if (ngDevMode) {
    let duplicatedKeysMsg = [];
    for (const [key, idxSet] of duplicateKeys) {
      if (idxSet.size > 1) {
        const idx = [...idxSet].sort((a, b) => a - b);
        for (let i = 1; i < idx.length; i++) {
          duplicatedKeysMsg.push(`key "${stringifyForError(key)}" at index "${idx[i - 1]}" and "${idx[i]}"`);
        }
      }
    }
    if (duplicatedKeysMsg.length > 0) {
      const message = formatRuntimeError(-955, "The provided track expression resulted in duplicated keys for a given collection. Adjust the tracking expression such that it uniquely identifies all the items in the collection. Duplicated keys were: \n" + duplicatedKeysMsg.join(", \n") + ".");
      console.warn(message);
    }
  }
}
function attachPreviouslyDetached(prevCollection, detachedItems, index, key) {
  if (detachedItems !== void 0 && detachedItems.has(key)) {
    prevCollection.attach(index, detachedItems.get(key));
    detachedItems.delete(key);
    return true;
  }
  return false;
}
function createOrAttach(liveCollection, detachedItems, trackByFn, index, value) {
  if (!attachPreviouslyDetached(liveCollection, detachedItems, index, trackByFn(index, value))) {
    const newItem = liveCollection.create(index, value);
    liveCollection.attach(index, newItem);
  } else {
    liveCollection.updateValue(index, value);
  }
}
function initLiveItemsInTheFuture(liveCollection, start, end, trackByFn) {
  const keys = /* @__PURE__ */ new Set();
  for (let i = start; i <= end; i++) {
    keys.add(trackByFn(i, liveCollection.at(i)));
  }
  return keys;
}
var UniqueValueMultiKeyMap = class {
  kvMap = /* @__PURE__ */ new Map();
  _vMap = void 0;
  has(key) {
    return this.kvMap.has(key);
  }
  delete(key) {
    if (!this.has(key)) return false;
    const value = this.kvMap.get(key);
    if (this._vMap !== void 0 && this._vMap.has(value)) {
      this.kvMap.set(key, this._vMap.get(value));
      this._vMap.delete(value);
    } else {
      this.kvMap.delete(key);
    }
    return true;
  }
  get(key) {
    return this.kvMap.get(key);
  }
  set(key, value) {
    if (this.kvMap.has(key)) {
      let prevValue = this.kvMap.get(key);
      if (ngDevMode && prevValue === value) {
        throw new Error(`Detected a duplicated value ${value} for the key ${key}`);
      }
      if (this._vMap === void 0) {
        this._vMap = /* @__PURE__ */ new Map();
      }
      const vMap = this._vMap;
      while (vMap.has(prevValue)) {
        prevValue = vMap.get(prevValue);
      }
      vMap.set(prevValue, value);
    } else {
      this.kvMap.set(key, value);
    }
  }
  forEach(cb) {
    for (let [key, value] of this.kvMap) {
      cb(value, key);
      if (this._vMap !== void 0) {
        const vMap = this._vMap;
        while (vMap.has(value)) {
          value = vMap.get(value);
          cb(value, key);
        }
      }
    }
  }
};
function \u0275\u0275conditionalCreate(index, templateFn, decls, vars, tagName, attrsIndex, localRefsIndex, localRefExtractor) {
  performanceMarkFeature("NgControlFlow");
  const lView = getLView();
  const tView = getTView();
  const attrs = getConstant(tView.consts, attrsIndex);
  declareNoDirectiveHostTemplate(lView, tView, index, templateFn, decls, vars, tagName, attrs, 256, localRefsIndex, localRefExtractor);
  return \u0275\u0275conditionalBranchCreate;
}
function \u0275\u0275conditionalBranchCreate(index, templateFn, decls, vars, tagName, attrsIndex, localRefsIndex, localRefExtractor) {
  performanceMarkFeature("NgControlFlow");
  const lView = getLView();
  const tView = getTView();
  const attrs = getConstant(tView.consts, attrsIndex);
  declareNoDirectiveHostTemplate(lView, tView, index, templateFn, decls, vars, tagName, attrs, 512, localRefsIndex, localRefExtractor);
  return \u0275\u0275conditionalBranchCreate;
}
function \u0275\u0275conditional(matchingTemplateIndex, contextValue) {
  performanceMarkFeature("NgControlFlow");
  const hostLView = getLView();
  const bindingIndex = nextBindingIndex();
  const prevMatchingTemplateIndex = hostLView[bindingIndex] !== NO_CHANGE ? hostLView[bindingIndex] : -1;
  const prevContainer = prevMatchingTemplateIndex !== -1 ? getLContainer(hostLView, HEADER_OFFSET + prevMatchingTemplateIndex) : void 0;
  const viewInContainerIdx = 0;
  if (bindingUpdated(hostLView, bindingIndex, matchingTemplateIndex)) {
    const prevConsumer = setActiveConsumer(null);
    try {
      if (prevContainer !== void 0) {
        removeLViewFromLContainer(prevContainer, viewInContainerIdx);
      }
      if (matchingTemplateIndex !== -1) {
        const nextLContainerIndex = HEADER_OFFSET + matchingTemplateIndex;
        const nextContainer = getLContainer(hostLView, nextLContainerIndex);
        const templateTNode = getExistingTNode(hostLView[TVIEW], nextLContainerIndex);
        const dehydratedView = findAndReconcileMatchingDehydratedViews(nextContainer, templateTNode, hostLView);
        const embeddedLView = createAndRenderEmbeddedLView(hostLView, templateTNode, contextValue, {
          dehydratedView
        });
        addLViewToLContainer(nextContainer, embeddedLView, viewInContainerIdx, shouldAddViewToDom(templateTNode, dehydratedView));
      }
    } finally {
      setActiveConsumer(prevConsumer);
    }
  } else if (prevContainer !== void 0) {
    const lView = getLViewFromLContainer(prevContainer, viewInContainerIdx);
    if (lView !== void 0) {
      lView[CONTEXT] = contextValue;
    }
  }
}
var RepeaterContext = class {
  lContainer;
  $implicit;
  $index;
  constructor(lContainer, $implicit, $index) {
    this.lContainer = lContainer;
    this.$implicit = $implicit;
    this.$index = $index;
  }
  get $count() {
    return this.lContainer.length - CONTAINER_HEADER_OFFSET;
  }
};
function \u0275\u0275repeaterTrackByIndex(index) {
  return index;
}
function \u0275\u0275repeaterTrackByIdentity(_, value) {
  return value;
}
var RepeaterMetadata = class {
  hasEmptyBlock;
  trackByFn;
  liveCollection;
  constructor(hasEmptyBlock, trackByFn, liveCollection) {
    this.hasEmptyBlock = hasEmptyBlock;
    this.trackByFn = trackByFn;
    this.liveCollection = liveCollection;
  }
};
function \u0275\u0275repeaterCreate(index, templateFn, decls, vars, tagName, attrsIndex, trackByFn, trackByUsesComponentInstance, emptyTemplateFn, emptyDecls, emptyVars, emptyTagName, emptyAttrsIndex) {
  performanceMarkFeature("NgControlFlow");
  ngDevMode && assertFunction(trackByFn, `A track expression must be a function, was ${typeof trackByFn} instead.`);
  const lView = getLView();
  const tView = getTView();
  const hasEmptyBlock = emptyTemplateFn !== void 0;
  const hostLView = getLView();
  const boundTrackBy = trackByUsesComponentInstance ? trackByFn.bind(hostLView[DECLARATION_COMPONENT_VIEW][CONTEXT]) : trackByFn;
  const metadata = new RepeaterMetadata(hasEmptyBlock, boundTrackBy);
  hostLView[HEADER_OFFSET + index] = metadata;
  declareNoDirectiveHostTemplate(lView, tView, index + 1, templateFn, decls, vars, tagName, getConstant(tView.consts, attrsIndex), 256);
  if (hasEmptyBlock) {
    ngDevMode && assertDefined(emptyDecls, "Missing number of declarations for the empty repeater block.");
    ngDevMode && assertDefined(emptyVars, "Missing number of bindings for the empty repeater block.");
    declareNoDirectiveHostTemplate(lView, tView, index + 2, emptyTemplateFn, emptyDecls, emptyVars, emptyTagName, getConstant(tView.consts, emptyAttrsIndex), 512);
  }
}
function isViewExpensiveToRecreate(lView) {
  return lView.length - HEADER_OFFSET > 2;
}
var OperationsCounter = class {
  created = 0;
  destroyed = 0;
  reset() {
    this.created = 0;
    this.destroyed = 0;
  }
  recordCreate() {
    this.created++;
  }
  recordDestroy() {
    this.destroyed++;
  }
  wasReCreated(collectionLen) {
    return collectionLen > 0 && this.created === this.destroyed && this.created === collectionLen;
  }
};
var LiveCollectionLContainerImpl = class extends LiveCollection {
  lContainer;
  hostLView;
  templateTNode;
  operationsCounter = ngDevMode ? new OperationsCounter() : void 0;
  needsIndexUpdate = false;
  constructor(lContainer, hostLView, templateTNode) {
    super();
    this.lContainer = lContainer;
    this.hostLView = hostLView;
    this.templateTNode = templateTNode;
  }
  get length() {
    return this.lContainer.length - CONTAINER_HEADER_OFFSET;
  }
  at(index) {
    return this.getLView(index)[CONTEXT].$implicit;
  }
  attach(index, lView) {
    const dehydratedView = lView[HYDRATION];
    this.needsIndexUpdate ||= index !== this.length;
    addLViewToLContainer(this.lContainer, lView, index, shouldAddViewToDom(this.templateTNode, dehydratedView));
    clearDetachAnimationList(this.lContainer, index);
  }
  detach(index) {
    this.needsIndexUpdate ||= index !== this.length - 1;
    maybeInitDetachAnimationList(this.lContainer, index);
    return detachExistingView(this.lContainer, index);
  }
  create(index, value) {
    const dehydratedView = findMatchingDehydratedView(this.lContainer, this.templateTNode.tView.ssrId);
    const embeddedLView = createAndRenderEmbeddedLView(this.hostLView, this.templateTNode, new RepeaterContext(this.lContainer, value, index), {
      dehydratedView
    });
    ngDevMode && this.operationsCounter?.recordCreate();
    return embeddedLView;
  }
  destroy(lView) {
    destroyLView(lView[TVIEW], lView);
    ngDevMode && this.operationsCounter?.recordDestroy();
  }
  updateValue(index, value) {
    this.getLView(index)[CONTEXT].$implicit = value;
  }
  reset() {
    this.needsIndexUpdate = false;
    ngDevMode && this.operationsCounter?.reset();
  }
  updateIndexes() {
    if (this.needsIndexUpdate) {
      for (let i = 0; i < this.length; i++) {
        this.getLView(i)[CONTEXT].$index = i;
      }
    }
  }
  getLView(index) {
    return getExistingLViewFromLContainer(this.lContainer, index);
  }
};
function \u0275\u0275repeater(collection) {
  const prevConsumer = setActiveConsumer(null);
  const metadataSlotIdx = getSelectedIndex();
  try {
    const hostLView = getLView();
    const hostTView = hostLView[TVIEW];
    const metadata = hostLView[metadataSlotIdx];
    const containerIndex = metadataSlotIdx + 1;
    const lContainer = getLContainer(hostLView, containerIndex);
    if (metadata.liveCollection === void 0) {
      const itemTemplateTNode = getExistingTNode(hostTView, containerIndex);
      metadata.liveCollection = new LiveCollectionLContainerImpl(lContainer, hostLView, itemTemplateTNode);
    } else {
      metadata.liveCollection.reset();
    }
    const liveCollection = metadata.liveCollection;
    reconcile(liveCollection, collection, metadata.trackByFn, prevConsumer);
    if (ngDevMode && metadata.trackByFn === \u0275\u0275repeaterTrackByIdentity && liveCollection.operationsCounter?.wasReCreated(liveCollection.length) && isViewExpensiveToRecreate(getExistingLViewFromLContainer(lContainer, 0))) {
      const message = formatRuntimeError(-956, `The configured tracking expression (track by identity) caused re-creation of the entire collection of size ${liveCollection.length}. This is an expensive operation requiring destruction and subsequent creation of DOM nodes, directives, components etc. Please review the "track expression" and make sure that it uniquely identifies items in a collection.`);
      console.warn(message);
    }
    liveCollection.updateIndexes();
    if (metadata.hasEmptyBlock) {
      const bindingIndex = nextBindingIndex();
      const isCollectionEmpty = liveCollection.length === 0;
      if (bindingUpdated(hostLView, bindingIndex, isCollectionEmpty)) {
        const emptyTemplateIndex = metadataSlotIdx + 2;
        const lContainerForEmpty = getLContainer(hostLView, emptyTemplateIndex);
        if (isCollectionEmpty) {
          const emptyTemplateTNode = getExistingTNode(hostTView, emptyTemplateIndex);
          const dehydratedView = findAndReconcileMatchingDehydratedViews(lContainerForEmpty, emptyTemplateTNode, hostLView);
          const embeddedLView = createAndRenderEmbeddedLView(hostLView, emptyTemplateTNode, void 0, {
            dehydratedView
          });
          addLViewToLContainer(lContainerForEmpty, embeddedLView, 0, shouldAddViewToDom(emptyTemplateTNode, dehydratedView));
        } else {
          if (hostTView.firstUpdatePass) {
            removeDehydratedViews(lContainerForEmpty);
          }
          removeLViewFromLContainer(lContainerForEmpty, 0);
        }
      }
    }
  } finally {
    setActiveConsumer(prevConsumer);
  }
}
function getLContainer(lView, index) {
  const lContainer = lView[index];
  ngDevMode && assertLContainer(lContainer);
  return lContainer;
}
function clearDetachAnimationList(lContainer, index) {
  if (lContainer.length <= CONTAINER_HEADER_OFFSET) return;
  const indexInContainer = CONTAINER_HEADER_OFFSET + index;
  const viewToDetach = lContainer[indexInContainer];
  const animations = viewToDetach ? viewToDetach[ANIMATIONS] : void 0;
  if (viewToDetach && animations && animations.detachedLeaveAnimationFns && animations.detachedLeaveAnimationFns.length > 0) {
    const injector = viewToDetach[INJECTOR];
    removeFromAnimationQueue(injector, animations);
    allLeavingAnimations.delete(viewToDetach[ID]);
    animations.detachedLeaveAnimationFns = void 0;
  }
}
function maybeInitDetachAnimationList(lContainer, index) {
  if (lContainer.length <= CONTAINER_HEADER_OFFSET) return;
  const indexInContainer = CONTAINER_HEADER_OFFSET + index;
  const viewToDetach = lContainer[indexInContainer];
  const animations = viewToDetach ? viewToDetach[ANIMATIONS] : void 0;
  if (animations && animations.leave && animations.leave.size > 0) {
    animations.detachedLeaveAnimationFns = [];
  }
}
function detachExistingView(lContainer, index) {
  const existingLView = detachView(lContainer, index);
  ngDevMode && assertLView(existingLView);
  return existingLView;
}
function getExistingLViewFromLContainer(lContainer, index) {
  const existingLView = getLViewFromLContainer(lContainer, index);
  ngDevMode && assertLView(existingLView);
  return existingLView;
}
function getExistingTNode(tView, index) {
  const tNode = getTNode(tView, index);
  ngDevMode && assertTNode(tNode);
  return tNode;
}
function \u0275\u0275property(propName, value, sanitizer) {
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    setPropertyAndInputs(tNode, lView, propName, value, lView[RENDERER], sanitizer);
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, propName, bindingIndex);
  }
  return \u0275\u0275property;
}
function setDirectiveInputsWhichShadowsStyling(tView, tNode, lView, value, isClassBased) {
  setAllInputsForProperty(tNode, tView, lView, isClassBased ? "class" : "style", value);
}
function \u0275\u0275elementStart(index, name, attrsIndex, localRefsIndex) {
  const lView = getLView();
  ngDevMode && assertTNodeCreationIndex(lView, index);
  const tView = lView[TVIEW];
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = tView.firstCreatePass ? directiveHostFirstCreatePass(adjustedIndex, lView, 2, name, findDirectiveDefMatches, getBindingsEnabled(), attrsIndex, localRefsIndex) : tView.data[adjustedIndex];
  if (isComponentHost(tNode)) {
    const tracingService = lView[ENVIRONMENT].tracingService;
    if (tracingService && tracingService.componentCreate) {
      const def = tView.data[tNode.directiveStart + tNode.componentOffset];
      return tracingService.componentCreate(getComponentName(def), () => {
        initializeElement(index, name, lView, tNode, localRefsIndex);
        return \u0275\u0275elementStart;
      });
    }
  }
  initializeElement(index, name, lView, tNode, localRefsIndex);
  return \u0275\u0275elementStart;
}
function initializeElement(index, name, lView, tNode, localRefsIndex) {
  elementLikeStartShared(tNode, lView, index, name, _locateOrCreateElementNode);
  if (isDirectiveHost(tNode)) {
    const tView = lView[TVIEW];
    createDirectivesInstances(tView, lView, tNode);
    executeContentQueries(tView, tNode, lView);
  }
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(lView, tNode);
  }
  if (ngDevMode && lView[TVIEW].firstCreatePass) {
    validateElementIsKnown(lView, tNode);
  }
}
function \u0275\u0275elementEnd() {
  const tView = getTView();
  const initialTNode = getCurrentTNode();
  ngDevMode && assertDefined(initialTNode, "No parent node to close.");
  const currentTNode = elementLikeEndShared(initialTNode);
  ngDevMode && assertTNodeType(currentTNode, 3);
  if (tView.firstCreatePass) {
    directiveHostEndFirstCreatePass(tView, currentTNode);
  }
  if (isSkipHydrationRootTNode(currentTNode)) {
    leaveSkipHydrationBlock();
  }
  decreaseElementDepthCount();
  if (currentTNode.classesWithoutHost != null && hasClassInput(currentTNode)) {
    setDirectiveInputsWhichShadowsStyling(tView, currentTNode, getLView(), currentTNode.classesWithoutHost, true);
  }
  if (currentTNode.stylesWithoutHost != null && hasStyleInput(currentTNode)) {
    setDirectiveInputsWhichShadowsStyling(tView, currentTNode, getLView(), currentTNode.stylesWithoutHost, false);
  }
  return \u0275\u0275elementEnd;
}
function \u0275\u0275element(index, name, attrsIndex, localRefsIndex) {
  \u0275\u0275elementStart(index, name, attrsIndex, localRefsIndex);
  \u0275\u0275elementEnd();
  return \u0275\u0275element;
}
function \u0275\u0275domElementStart(index, name, attrsIndex, localRefsIndex) {
  const lView = getLView();
  ngDevMode && assertTNodeCreationIndex(lView, index);
  const tView = lView[TVIEW];
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = tView.firstCreatePass ? domOnlyFirstCreatePass(adjustedIndex, tView, 2, name, attrsIndex, localRefsIndex) : tView.data[adjustedIndex];
  elementLikeStartShared(tNode, lView, index, name, _locateOrCreateElementNode);
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(lView, tNode);
  }
  if (ngDevMode && lView[TVIEW].firstCreatePass) {
    validateElementIsKnown(lView, tNode);
  }
  return \u0275\u0275domElementStart;
}
function \u0275\u0275domElementEnd() {
  const initialTNode = getCurrentTNode();
  ngDevMode && assertDefined(initialTNode, "No parent node to close.");
  const currentTNode = elementLikeEndShared(initialTNode);
  ngDevMode && assertTNodeType(currentTNode, 3);
  if (isSkipHydrationRootTNode(currentTNode)) {
    leaveSkipHydrationBlock();
  }
  decreaseElementDepthCount();
  return \u0275\u0275domElementEnd;
}
function \u0275\u0275domElement(index, name, attrsIndex, localRefsIndex) {
  \u0275\u0275domElementStart(index, name, attrsIndex, localRefsIndex);
  \u0275\u0275domElementEnd();
  return \u0275\u0275domElement;
}
var _locateOrCreateElementNode = (tView, lView, tNode, name, index) => {
  lastNodeWasCreated(true);
  return createElementNode(lView[RENDERER], name, getNamespace());
};
function \u0275\u0275elementContainerStart(index, attrsIndex, localRefsIndex) {
  const lView = getLView();
  ngDevMode && assertTNodeCreationIndex(lView, index);
  const tView = lView[TVIEW];
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = tView.firstCreatePass ? directiveHostFirstCreatePass(adjustedIndex, lView, 8, "ng-container", findDirectiveDefMatches, getBindingsEnabled(), attrsIndex, localRefsIndex) : tView.data[adjustedIndex];
  elementLikeStartShared(tNode, lView, index, "ng-container", _locateOrCreateElementContainerNode);
  if (isDirectiveHost(tNode)) {
    const tView2 = lView[TVIEW];
    createDirectivesInstances(tView2, lView, tNode);
    executeContentQueries(tView2, tNode, lView);
  }
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(lView, tNode);
  }
  return \u0275\u0275elementContainerStart;
}
function \u0275\u0275elementContainerEnd() {
  const tView = getTView();
  const initialTNode = getCurrentTNode();
  ngDevMode && assertDefined(initialTNode, "No parent node to close.");
  const currentTNode = elementLikeEndShared(initialTNode);
  if (tView.firstCreatePass) {
    directiveHostEndFirstCreatePass(tView, currentTNode);
  }
  ngDevMode && assertTNodeType(currentTNode, 8);
  return \u0275\u0275elementContainerEnd;
}
function \u0275\u0275elementContainer(index, attrsIndex, localRefsIndex) {
  \u0275\u0275elementContainerStart(index, attrsIndex, localRefsIndex);
  \u0275\u0275elementContainerEnd();
  return \u0275\u0275elementContainer;
}
function \u0275\u0275domElementContainerStart(index, attrsIndex, localRefsIndex) {
  const lView = getLView();
  ngDevMode && assertTNodeCreationIndex(lView, index);
  const tView = lView[TVIEW];
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = tView.firstCreatePass ? domOnlyFirstCreatePass(adjustedIndex, tView, 8, "ng-container", attrsIndex, localRefsIndex) : tView.data[adjustedIndex];
  elementLikeStartShared(tNode, lView, index, "ng-container", _locateOrCreateElementContainerNode);
  if (localRefsIndex != null) {
    saveResolvedLocalsInData(lView, tNode);
  }
  return \u0275\u0275domElementContainerStart;
}
function \u0275\u0275domElementContainerEnd() {
  const initialTNode = getCurrentTNode();
  ngDevMode && assertDefined(initialTNode, "No parent node to close.");
  const currentTNode = elementLikeEndShared(initialTNode);
  ngDevMode && assertTNodeType(currentTNode, 8);
  return \u0275\u0275elementContainerEnd;
}
function \u0275\u0275domElementContainer(index, attrsIndex, localRefsIndex) {
  \u0275\u0275domElementContainerStart(index, attrsIndex, localRefsIndex);
  \u0275\u0275domElementContainerEnd();
  return \u0275\u0275domElementContainer;
}
var _locateOrCreateElementContainerNode = (tView, lView, tNode, commentText, index) => {
  lastNodeWasCreated(true);
  return createCommentNode(lView[RENDERER], ngDevMode ? commentText : "");
};
function \u0275\u0275getCurrentView() {
  return getLView();
}
function \u0275\u0275domProperty(propName, value, sanitizer) {
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    setDomProperty(tNode, lView, propName, value, lView[RENDERER], sanitizer);
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, propName, bindingIndex);
  }
  return \u0275\u0275domProperty;
}
function \u0275\u0275syntheticHostProperty(propName, value, sanitizer) {
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    const currentDef = getCurrentDirectiveDef(tView.data);
    const renderer = loadComponentRenderer(currentDef, tNode, lView);
    setDomProperty(tNode, lView, propName, value, renderer, sanitizer);
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, propName, bindingIndex);
  }
  return \u0275\u0275syntheticHostProperty;
}
var u = void 0;
function plural(val) {
  const i = Math.floor(Math.abs(val)), v = val.toString().replace(/^[^.]*\.?/, "").length;
  if (i === 1 && v === 0) return 1;
  return 5;
}
var localeEn = ["en", [["a", "p"], ["AM", "PM"]], [["AM", "PM"]], [["S", "M", "T", "W", "T", "F", "S"], ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"], ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"], ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"]], u, [["J", "F", "M", "A", "M", "J", "J", "A", "S", "O", "N", "D"], ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"], ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]], u, [["B", "A"], ["BC", "AD"], ["Before Christ", "Anno Domini"]], 0, [6, 0], ["M/d/yy", "MMM d, y", "MMMM d, y", "EEEE, MMMM d, y"], ["h:mm\u202Fa", "h:mm:ss\u202Fa", "h:mm:ss\u202Fa z", "h:mm:ss\u202Fa zzzz"], ["{1}, {0}", u, u, u], [".", ",", ";", "%", "+", "-", "E", "\xD7", "\u2030", "\u221E", "NaN", ":"], ["#,##0.###", "#,##0%", "\xA4#,##0.00", "#E0"], "USD", "$", "US Dollar", {}, "ltr", plural];
var LOCALE_DATA = /* @__PURE__ */ Object.create(null);
function findLocaleData(locale) {
  const normalizedLocale = normalizeLocale(locale);
  let match = getLocaleData(normalizedLocale);
  if (match) {
    return match;
  }
  const parentLocale = normalizedLocale.split("-")[0];
  match = getLocaleData(parentLocale);
  if (match) {
    return match;
  }
  if (parentLocale === "en") {
    return localeEn;
  }
  throw new RuntimeError(701, ngDevMode && `Missing locale data for the locale "${locale}".`);
}
function getLocalePluralCase(locale) {
  const data = findLocaleData(locale);
  return data[LocaleDataIndex.PluralCase];
}
function getLocaleData(normalizedLocale) {
  if (!(normalizedLocale in LOCALE_DATA)) {
    LOCALE_DATA[normalizedLocale] = _global.ng && _global.ng.common && _global.ng.common.locales && _global.ng.common.locales[normalizedLocale];
  }
  return LOCALE_DATA[normalizedLocale];
}
var LocaleDataIndex;
(function(LocaleDataIndex2) {
  LocaleDataIndex2[LocaleDataIndex2["LocaleId"] = 0] = "LocaleId";
  LocaleDataIndex2[LocaleDataIndex2["DayPeriodsFormat"] = 1] = "DayPeriodsFormat";
  LocaleDataIndex2[LocaleDataIndex2["DayPeriodsStandalone"] = 2] = "DayPeriodsStandalone";
  LocaleDataIndex2[LocaleDataIndex2["DaysFormat"] = 3] = "DaysFormat";
  LocaleDataIndex2[LocaleDataIndex2["DaysStandalone"] = 4] = "DaysStandalone";
  LocaleDataIndex2[LocaleDataIndex2["MonthsFormat"] = 5] = "MonthsFormat";
  LocaleDataIndex2[LocaleDataIndex2["MonthsStandalone"] = 6] = "MonthsStandalone";
  LocaleDataIndex2[LocaleDataIndex2["Eras"] = 7] = "Eras";
  LocaleDataIndex2[LocaleDataIndex2["FirstDayOfWeek"] = 8] = "FirstDayOfWeek";
  LocaleDataIndex2[LocaleDataIndex2["WeekendRange"] = 9] = "WeekendRange";
  LocaleDataIndex2[LocaleDataIndex2["DateFormat"] = 10] = "DateFormat";
  LocaleDataIndex2[LocaleDataIndex2["TimeFormat"] = 11] = "TimeFormat";
  LocaleDataIndex2[LocaleDataIndex2["DateTimeFormat"] = 12] = "DateTimeFormat";
  LocaleDataIndex2[LocaleDataIndex2["NumberSymbols"] = 13] = "NumberSymbols";
  LocaleDataIndex2[LocaleDataIndex2["NumberFormats"] = 14] = "NumberFormats";
  LocaleDataIndex2[LocaleDataIndex2["CurrencyCode"] = 15] = "CurrencyCode";
  LocaleDataIndex2[LocaleDataIndex2["CurrencySymbol"] = 16] = "CurrencySymbol";
  LocaleDataIndex2[LocaleDataIndex2["CurrencyName"] = 17] = "CurrencyName";
  LocaleDataIndex2[LocaleDataIndex2["Currencies"] = 18] = "Currencies";
  LocaleDataIndex2[LocaleDataIndex2["Directionality"] = 19] = "Directionality";
  LocaleDataIndex2[LocaleDataIndex2["PluralCase"] = 20] = "PluralCase";
  LocaleDataIndex2[LocaleDataIndex2["ExtraData"] = 21] = "ExtraData";
})(LocaleDataIndex || (LocaleDataIndex = {}));
function normalizeLocale(locale) {
  return locale.toLowerCase().replace(/_/g, "-");
}
var pluralMapping = ["zero", "one", "two", "few", "many"];
function getPluralCase(value, locale) {
  const plural2 = getLocalePluralCase(locale)(parseInt(value, 10));
  const result = pluralMapping[plural2];
  return result !== void 0 ? result : "other";
}
var DEFAULT_LOCALE_ID = "en-US";
var USD_CURRENCY_CODE = "USD";
var ELEMENT_MARKER = {
  marker: "element"
};
var ICU_MARKER = {
  marker: "ICU"
};
var I18nCreateOpCode;
(function(I18nCreateOpCode2) {
  I18nCreateOpCode2[I18nCreateOpCode2["SHIFT"] = 2] = "SHIFT";
  I18nCreateOpCode2[I18nCreateOpCode2["APPEND_EAGERLY"] = 1] = "APPEND_EAGERLY";
  I18nCreateOpCode2[I18nCreateOpCode2["COMMENT"] = 2] = "COMMENT";
})(I18nCreateOpCode || (I18nCreateOpCode = {}));
var LOCALE_ID$1 = DEFAULT_LOCALE_ID;
function setLocaleId(localeId) {
  ngDevMode && assertDefined(localeId, `Expected localeId to be defined`);
  if (typeof localeId === "string") {
    LOCALE_ID$1 = localeId.toLowerCase().replace(/_/g, "-");
  }
}
function getLocaleId() {
  return LOCALE_ID$1;
}
var changeMask = 0;
var changeMaskCounter = 0;
function setMaskBit(hasChange) {
  if (hasChange) {
    changeMask = changeMask | 1 << Math.min(changeMaskCounter, 31);
  }
  changeMaskCounter++;
}
function applyI18n(tView, lView, index) {
  try {
    if (changeMaskCounter > 0) {
      ngDevMode && assertDefined(tView, `tView should be defined`);
      const tI18n = tView.data[index];
      const updateOpCodes = Array.isArray(tI18n) ? tI18n : tI18n.update;
      const bindingsStartIndex = getBindingIndex() - changeMaskCounter - 1;
      applyUpdateOpCodes(tView, lView, updateOpCodes, bindingsStartIndex, changeMask);
    }
  } finally {
    changeMask = 0;
    changeMaskCounter = 0;
  }
}
function createNodeWithoutHydration(lView, textOrName, nodeType) {
  const renderer = lView[RENDERER];
  switch (nodeType) {
    case Node.COMMENT_NODE:
      return createCommentNode(renderer, textOrName);
    case Node.TEXT_NODE:
      return createTextNode(renderer, textOrName);
    case Node.ELEMENT_NODE:
      return createElementNode(renderer, textOrName, null);
  }
}
var _locateOrCreateNode = (lView, index, textOrName, nodeType) => {
  lastNodeWasCreated(true);
  return createNodeWithoutHydration(lView, textOrName, nodeType);
};
function applyCreateOpCodes(lView, createOpCodes, parentRNode, insertInFrontOf) {
  const renderer = lView[RENDERER];
  for (let i = 0; i < createOpCodes.length; i++) {
    const opCode = createOpCodes[i++];
    const text = createOpCodes[i];
    const isComment = (opCode & I18nCreateOpCode.COMMENT) === I18nCreateOpCode.COMMENT;
    const appendNow = (opCode & I18nCreateOpCode.APPEND_EAGERLY) === I18nCreateOpCode.APPEND_EAGERLY;
    const index = opCode >>> I18nCreateOpCode.SHIFT;
    let rNode = lView[index];
    let lastNodeWasCreated2 = false;
    if (rNode === null) {
      rNode = lView[index] = _locateOrCreateNode(lView, index, text, isComment ? Node.COMMENT_NODE : Node.TEXT_NODE);
      lastNodeWasCreated2 = wasLastNodeCreated();
    }
    if (appendNow && parentRNode !== null && lastNodeWasCreated2) {
      nativeInsertBefore(renderer, parentRNode, rNode, insertInFrontOf, false);
    }
  }
}
function applyMutableOpCodes(tView, mutableOpCodes, lView, anchorRNode) {
  ngDevMode && assertDomNode(anchorRNode);
  const renderer = lView[RENDERER];
  let rootIdx = null;
  let rootRNode;
  for (let i = 0; i < mutableOpCodes.length; i++) {
    const opCode = mutableOpCodes[i];
    if (typeof opCode == "string") {
      const textNodeIndex = mutableOpCodes[++i];
      if (lView[textNodeIndex] === null) {
        ngDevMode && assertIndexInRange(lView, textNodeIndex);
        lView[textNodeIndex] = _locateOrCreateNode(lView, textNodeIndex, opCode, Node.TEXT_NODE);
      }
    } else if (typeof opCode == "number") {
      switch (opCode & 1) {
        case 0:
          const parentIdx = getParentFromIcuCreateOpCode(opCode);
          if (rootIdx === null) {
            rootIdx = parentIdx;
            rootRNode = renderer.parentNode(anchorRNode);
          }
          let insertInFrontOf;
          let parentRNode;
          if (parentIdx === rootIdx) {
            insertInFrontOf = anchorRNode;
            parentRNode = rootRNode;
          } else {
            insertInFrontOf = null;
            parentRNode = unwrapRNode(lView[parentIdx]);
          }
          if (parentRNode !== null) {
            ngDevMode && assertDomNode(parentRNode);
            const refIdx = getRefFromIcuCreateOpCode(opCode);
            ngDevMode && assertGreaterThan(refIdx, HEADER_OFFSET, "Missing ref");
            const child = lView[refIdx];
            ngDevMode && assertDomNode(child);
            nativeInsertBefore(renderer, parentRNode, child, insertInFrontOf, false);
            const tIcu = getTIcu(tView, refIdx);
            if (tIcu !== null && typeof tIcu === "object") {
              ngDevMode && assertTIcu(tIcu);
              const caseIndex = getCurrentICUCaseIndex(tIcu, lView);
              if (caseIndex !== null) {
                applyMutableOpCodes(tView, tIcu.create[caseIndex], lView, lView[tIcu.anchorIdx]);
              }
            }
          }
          break;
        case 1:
          const elementNodeIndex = opCode >>> 1;
          const attrName = mutableOpCodes[++i];
          const attrValue = mutableOpCodes[++i];
          setElementAttribute(renderer, getNativeByIndex(elementNodeIndex, lView), null, null, attrName, attrValue, null);
          break;
        default:
          if (ngDevMode) {
            throw new RuntimeError(700, `Unable to determine the type of mutate operation for "${opCode}"`);
          }
      }
    } else {
      switch (opCode) {
        case ICU_MARKER:
          const commentValue = mutableOpCodes[++i];
          const commentNodeIndex = mutableOpCodes[++i];
          if (lView[commentNodeIndex] === null) {
            ngDevMode && assertEqual(typeof commentValue, "string", `Expected "${commentValue}" to be a comment node value`);
            ngDevMode && assertIndexInExpandoRange(lView, commentNodeIndex);
            const commentRNode = lView[commentNodeIndex] = _locateOrCreateNode(lView, commentNodeIndex, commentValue, Node.COMMENT_NODE);
            attachPatchData(commentRNode, lView);
          }
          break;
        case ELEMENT_MARKER:
          const tagName = mutableOpCodes[++i];
          const elementNodeIndex = mutableOpCodes[++i];
          if (lView[elementNodeIndex] === null) {
            ngDevMode && assertEqual(typeof tagName, "string", `Expected "${tagName}" to be an element node tag name`);
            ngDevMode && assertIndexInExpandoRange(lView, elementNodeIndex);
            const elementRNode = lView[elementNodeIndex] = _locateOrCreateNode(lView, elementNodeIndex, tagName, Node.ELEMENT_NODE);
            attachPatchData(elementRNode, lView);
          }
          break;
        default:
          ngDevMode && throwError2(`Unable to determine the type of mutate operation for "${opCode}"`);
      }
    }
  }
}
function applyUpdateOpCodes(tView, lView, updateOpCodes, bindingsStartIndex, changeMask2) {
  for (let i = 0; i < updateOpCodes.length; i++) {
    const checkBit = updateOpCodes[i];
    const skipCodes = updateOpCodes[++i];
    if (checkBit & changeMask2) {
      let value = "";
      for (let j = i + 1; j <= i + skipCodes; j++) {
        const opCode = updateOpCodes[j];
        if (typeof opCode == "string") {
          value += opCode;
        } else if (typeof opCode == "number") {
          if (opCode < 0) {
            value += renderStringify(lView[bindingsStartIndex - opCode]);
          } else {
            const nodeIndex = opCode >>> 2;
            switch (opCode & 3) {
              case 1:
                const propName = updateOpCodes[++j];
                const sanitizeFn = updateOpCodes[++j];
                const tNodeOrTagName = tView.data[nodeIndex];
                ngDevMode && assertDefined(tNodeOrTagName, "Experting TNode or string");
                if (typeof tNodeOrTagName === "string") {
                  setElementAttribute(lView[RENDERER], lView[nodeIndex], null, tNodeOrTagName, propName, value, sanitizeFn);
                } else {
                  const prevSelectedIndex = getSelectedIndex();
                  setSelectedIndex(nodeIndex);
                  try {
                    setPropertyAndInputs(tNodeOrTagName, lView, propName, value, lView[RENDERER], sanitizeFn);
                  } finally {
                    setSelectedIndex(prevSelectedIndex);
                  }
                }
                break;
              case 0:
                const rText = lView[nodeIndex];
                rText !== null && updateTextNode(lView[RENDERER], rText, value);
                break;
              case 2:
                applyIcuSwitchCase(tView, getTIcu(tView, nodeIndex), lView, value);
                break;
              case 3:
                applyIcuUpdateCase(tView, getTIcu(tView, nodeIndex), bindingsStartIndex, lView);
                break;
            }
          }
        }
      }
    } else {
      const opCode = updateOpCodes[i + 1];
      if (opCode > 0 && (opCode & 3) === 3) {
        const nodeIndex = opCode >>> 2;
        const tIcu = getTIcu(tView, nodeIndex);
        const currentIndex = lView[tIcu.currentCaseLViewIndex];
        if (currentIndex < 0) {
          applyIcuUpdateCase(tView, tIcu, bindingsStartIndex, lView);
        }
      }
    }
    i += skipCodes;
  }
}
function applyIcuUpdateCase(tView, tIcu, bindingsStartIndex, lView) {
  ngDevMode && assertIndexInRange(lView, tIcu.currentCaseLViewIndex);
  let activeCaseIndex = lView[tIcu.currentCaseLViewIndex];
  if (activeCaseIndex !== null) {
    let mask = changeMask;
    if (activeCaseIndex < 0) {
      activeCaseIndex = lView[tIcu.currentCaseLViewIndex] = ~activeCaseIndex;
      mask = -1;
    }
    applyUpdateOpCodes(tView, lView, tIcu.update[activeCaseIndex], bindingsStartIndex, mask);
  }
}
function applyIcuSwitchCase(tView, tIcu, lView, value) {
  const caseIndex = getCaseIndex(tIcu, value);
  let activeCaseIndex = getCurrentICUCaseIndex(tIcu, lView);
  if (activeCaseIndex !== caseIndex) {
    applyIcuSwitchCaseRemove(tView, tIcu, lView);
    lView[tIcu.currentCaseLViewIndex] = caseIndex === null ? null : ~caseIndex;
    if (caseIndex !== null) {
      const anchorRNode = lView[tIcu.anchorIdx];
      if (anchorRNode) {
        ngDevMode && assertDomNode(anchorRNode);
        applyMutableOpCodes(tView, tIcu.create[caseIndex], lView, anchorRNode);
      }
      claimDehydratedIcuCase(lView, tIcu.anchorIdx, caseIndex);
    }
  }
}
function applyIcuSwitchCaseRemove(tView, tIcu, lView) {
  let activeCaseIndex = getCurrentICUCaseIndex(tIcu, lView);
  if (activeCaseIndex !== null) {
    const removeCodes = tIcu.remove[activeCaseIndex];
    for (let i = 0; i < removeCodes.length; i++) {
      const nodeOrIcuIndex = removeCodes[i];
      if (nodeOrIcuIndex > 0) {
        const rNode = getNativeByIndex(nodeOrIcuIndex, lView);
        rNode !== null && nativeRemoveNode(lView[RENDERER], rNode);
      } else {
        applyIcuSwitchCaseRemove(tView, getTIcu(tView, ~nodeOrIcuIndex), lView);
      }
    }
  }
}
function getCaseIndex(icuExpression, bindingValue) {
  let index = icuExpression.cases.indexOf(bindingValue);
  if (index === -1) {
    switch (icuExpression.type) {
      case 1: {
        const resolvedCase = getPluralCase(bindingValue, getLocaleId());
        index = icuExpression.cases.indexOf(resolvedCase);
        if (index === -1 && resolvedCase !== "other") {
          index = icuExpression.cases.indexOf("other");
        }
        break;
      }
      case 0: {
        index = icuExpression.cases.indexOf("other");
        break;
      }
    }
  }
  return index === -1 ? null : index;
}
function i18nCreateOpCodesToString(opcodes) {
  const createOpCodes = opcodes || (Array.isArray(this) ? this : []);
  let lines = [];
  for (let i = 0; i < createOpCodes.length; i++) {
    const opCode = createOpCodes[i++];
    const text = createOpCodes[i];
    const isComment = (opCode & I18nCreateOpCode.COMMENT) === I18nCreateOpCode.COMMENT;
    const appendNow = (opCode & I18nCreateOpCode.APPEND_EAGERLY) === I18nCreateOpCode.APPEND_EAGERLY;
    const index = opCode >>> I18nCreateOpCode.SHIFT;
    lines.push(`lView[${index}] = document.${isComment ? "createComment" : "createText"}(${JSON.stringify(text)});`);
    if (appendNow) {
      lines.push(`parent.appendChild(lView[${index}]);`);
    }
  }
  return lines;
}
function i18nUpdateOpCodesToString(opcodes) {
  const parser = new OpCodeParser(opcodes || (Array.isArray(this) ? this : []));
  let lines = [];
  function consumeOpCode(value) {
    const ref = value >>> 2;
    const opCode = value & 3;
    switch (opCode) {
      case 0:
        return `(lView[${ref}] as Text).textContent = $$$`;
      case 1:
        const attrName = parser.consumeString();
        const sanitizationFn = parser.consumeFunction();
        const value2 = sanitizationFn ? `(${sanitizationFn})($$$)` : "$$$";
        return `(lView[${ref}] as Element).setAttribute('${attrName}', ${value2})`;
      case 2:
        return `icuSwitchCase(${ref}, $$$)`;
      case 3:
        return `icuUpdateCase(${ref})`;
    }
    throw new Error("unexpected OpCode");
  }
  while (parser.hasMore()) {
    let mask = parser.consumeNumber();
    let size = parser.consumeNumber();
    const end = parser.i + size;
    const statements = [];
    let statement = "";
    while (parser.i < end) {
      let value = parser.consumeNumberOrString();
      if (typeof value === "string") {
        statement += value;
      } else if (value < 0) {
        statement += "${lView[i" + value + "]}";
      } else {
        const opCodeText = consumeOpCode(value);
        statements.push(opCodeText.replace("$$$", "`" + statement + "`") + ";");
        statement = "";
      }
    }
    lines.push(`if (mask & 0b${mask.toString(2)}) { ${statements.join(" ")} }`);
  }
  return lines;
}
function icuCreateOpCodesToString(opcodes) {
  const parser = new OpCodeParser(opcodes || (Array.isArray(this) ? this : []));
  let lines = [];
  function consumeOpCode(opCode) {
    const parent = getParentFromIcuCreateOpCode(opCode);
    const ref = getRefFromIcuCreateOpCode(opCode);
    switch (getInstructionFromIcuCreateOpCode(opCode)) {
      case 0:
        return `(lView[${parent}] as Element).appendChild(lView[${lastRef}])`;
      case 1:
        return `(lView[${ref}] as Element).setAttribute("${parser.consumeString()}", "${parser.consumeString()}")`;
    }
    throw new Error("Unexpected OpCode: " + getInstructionFromIcuCreateOpCode(opCode));
  }
  let lastRef = -1;
  while (parser.hasMore()) {
    let value = parser.consumeNumberStringOrMarker();
    if (value === ICU_MARKER) {
      const text = parser.consumeString();
      lastRef = parser.consumeNumber();
      lines.push(`lView[${lastRef}] = document.createComment("${text}")`);
    } else if (value === ELEMENT_MARKER) {
      const text = parser.consumeString();
      lastRef = parser.consumeNumber();
      lines.push(`lView[${lastRef}] = document.createElement("${text}")`);
    } else if (typeof value === "string") {
      lastRef = parser.consumeNumber();
      lines.push(`lView[${lastRef}] = document.createTextNode("${value}")`);
    } else if (typeof value === "number") {
      const line = consumeOpCode(value);
      line && lines.push(line);
    } else {
      throw new Error("Unexpected value");
    }
  }
  return lines;
}
function i18nRemoveOpCodesToString(opcodes) {
  const removeCodes = opcodes || (Array.isArray(this) ? this : []);
  let lines = [];
  for (let i = 0; i < removeCodes.length; i++) {
    const nodeOrIcuIndex = removeCodes[i];
    if (nodeOrIcuIndex > 0) {
      lines.push(`remove(lView[${nodeOrIcuIndex}])`);
    } else {
      lines.push(`removeNestedICU(${~nodeOrIcuIndex})`);
    }
  }
  return lines;
}
var OpCodeParser = class {
  i = 0;
  codes;
  constructor(codes) {
    this.codes = codes;
  }
  hasMore() {
    return this.i < this.codes.length;
  }
  consumeNumber() {
    let value = this.codes[this.i++];
    assertNumber(value, "expecting number in OpCode");
    return value;
  }
  consumeString() {
    let value = this.codes[this.i++];
    assertString(value, "expecting string in OpCode");
    return value;
  }
  consumeFunction() {
    let value = this.codes[this.i++];
    if (value === null || typeof value === "function") {
      return value;
    }
    throw new Error("expecting function in OpCode");
  }
  consumeNumberOrString() {
    let value = this.codes[this.i++];
    if (typeof value === "string") {
      return value;
    }
    assertNumber(value, "expecting number or string in OpCode");
    return value;
  }
  consumeNumberStringOrMarker() {
    let value = this.codes[this.i++];
    if (typeof value === "string" || typeof value === "number" || value == ICU_MARKER || value == ELEMENT_MARKER) {
      return value;
    }
    assertNumber(value, "expecting number, string, ICU_MARKER or ELEMENT_MARKER in OpCode");
    return value;
  }
};
var BINDING_REGEXP = /�(\d+):?\d*�/gi;
var ICU_REGEXP = /({\s*�\d+:?\d*�\s*,\s*\S{6}\s*,[\s\S]*})/gi;
var NESTED_ICU = /�(\d+)�/;
var ICU_BLOCK_REGEXP = /^\s*(�\d+:?\d*�)\s*,\s*(select|plural)\s*,/;
var MARKER = `\uFFFD`;
var SUBTEMPLATE_REGEXP = /�\/?\*(\d+:\d+)�/gi;
var PH_REGEXP = /�(\/?[#*]\d+):?\d*�/gi;
var NGSP_UNICODE_REGEXP = /\uE500/g;
function replaceNgsp(value) {
  return value.replace(NGSP_UNICODE_REGEXP, " ");
}
function attachDebugGetter(obj, debugGetter) {
  if (ngDevMode) {
    Object.defineProperty(obj, "debug", {
      get: debugGetter,
      enumerable: false
    });
  } else {
    throw new Error("This method should be guarded with `ngDevMode` so that it can be tree shaken in production!");
  }
}
function i18nStartFirstCreatePass(tView, parentTNodeIndex, lView, index, message, subTemplateIndex) {
  const rootTNode = getCurrentParentTNode();
  const createOpCodes = [];
  const updateOpCodes = [];
  const existingTNodeStack = [[]];
  const astStack = [[]];
  if (ngDevMode) {
    attachDebugGetter(createOpCodes, i18nCreateOpCodesToString);
    attachDebugGetter(updateOpCodes, i18nUpdateOpCodesToString);
  }
  message = getTranslationForTemplate(message, subTemplateIndex);
  const msgParts = replaceNgsp(message).split(PH_REGEXP);
  for (let i = 0; i < msgParts.length; i++) {
    let value = msgParts[i];
    if ((i & 1) === 0) {
      const parts = i18nParseTextIntoPartsAndICU(value);
      for (let j = 0; j < parts.length; j++) {
        let part = parts[j];
        if ((j & 1) === 0) {
          const text = part;
          ngDevMode && assertString(text, "Parsed ICU part should be string");
          if (text !== "") {
            i18nStartFirstCreatePassProcessTextNode(astStack[0], tView, rootTNode, existingTNodeStack[0], createOpCodes, updateOpCodes, lView, text);
          }
        } else {
          const icuExpression = part;
          if (typeof icuExpression !== "object") {
            throw new Error(`Unable to parse ICU expression in "${message}" message.`);
          }
          const icuContainerTNode = createTNodeAndAddOpCode(tView, rootTNode, existingTNodeStack[0], lView, createOpCodes, ngDevMode ? `ICU ${index}:${icuExpression.mainBinding}` : "", true);
          const icuNodeIndex = icuContainerTNode.index;
          ngDevMode && assertGreaterThanOrEqual(icuNodeIndex, HEADER_OFFSET, "Index must be in absolute LView offset");
          icuStart(astStack[0], tView, lView, updateOpCodes, parentTNodeIndex, icuExpression, icuNodeIndex);
        }
      }
    } else {
      const isClosing = value.charCodeAt(0) === 47;
      const type = value.charCodeAt(isClosing ? 1 : 0);
      ngDevMode && assertOneOf(type, 42, 35);
      const index2 = HEADER_OFFSET + Number.parseInt(value.substring(isClosing ? 2 : 1));
      if (isClosing) {
        existingTNodeStack.shift();
        astStack.shift();
        setCurrentTNode(getCurrentParentTNode(), false);
      } else {
        const tNode = createTNodePlaceholder(tView, existingTNodeStack[0], index2);
        existingTNodeStack.unshift([]);
        setCurrentTNode(tNode, true);
        const placeholderNode = {
          kind: 2,
          index: index2,
          children: [],
          type: type === 35 ? 0 : 1
        };
        astStack[0].push(placeholderNode);
        astStack.unshift(placeholderNode.children);
      }
    }
  }
  tView.data[index] = {
    create: createOpCodes,
    update: updateOpCodes,
    ast: astStack[0],
    parentTNodeIndex
  };
}
function createTNodeAndAddOpCode(tView, rootTNode, existingTNodes, lView, createOpCodes, text, isICU) {
  const i18nNodeIdx = allocExpando(tView, lView, 1, null);
  let opCode = i18nNodeIdx << I18nCreateOpCode.SHIFT;
  let parentTNode = getCurrentParentTNode();
  if (rootTNode === parentTNode) {
    parentTNode = null;
  }
  if (parentTNode === null) {
    opCode |= I18nCreateOpCode.APPEND_EAGERLY;
  }
  if (isICU) {
    opCode |= I18nCreateOpCode.COMMENT;
    ensureIcuContainerVisitorLoaded(loadIcuContainerVisitor);
  }
  createOpCodes.push(opCode, text === null ? "" : text);
  const tNode = createTNodeAtIndex(tView, i18nNodeIdx, isICU ? 32 : 1, text === null ? ngDevMode ? "{{?}}" : "" : text, null);
  addTNodeAndUpdateInsertBeforeIndex(existingTNodes, tNode);
  const tNodeIdx = tNode.index;
  setCurrentTNode(tNode, false);
  if (parentTNode !== null && rootTNode !== parentTNode) {
    setTNodeInsertBeforeIndex(parentTNode, tNodeIdx);
  }
  return tNode;
}
function i18nStartFirstCreatePassProcessTextNode(ast, tView, rootTNode, existingTNodes, createOpCodes, updateOpCodes, lView, text) {
  const hasBinding = text.match(BINDING_REGEXP);
  const tNode = createTNodeAndAddOpCode(tView, rootTNode, existingTNodes, lView, createOpCodes, hasBinding ? null : text, false);
  const index = tNode.index;
  if (hasBinding) {
    generateBindingUpdateOpCodes(updateOpCodes, text, index, null, 0, null);
  }
  ast.push({
    kind: 0,
    index
  });
}
function i18nAttributesFirstPass(tView, index, values) {
  const previousElement = getCurrentTNode();
  const previousElementIndex = previousElement.index;
  const updateOpCodes = [];
  if (ngDevMode) {
    attachDebugGetter(updateOpCodes, i18nUpdateOpCodesToString);
  }
  if (tView.firstCreatePass && tView.data[index] === null) {
    for (let i = 0; i < values.length; i += 2) {
      const attrName = values[i];
      const message = values[i + 1];
      if (message !== "") {
        if (ICU_REGEXP.test(message)) {
          throw new Error(`ICU expressions are not supported in attributes. Message: "${message}".`);
        }
        const tagName = previousElement.namespace ? `:${previousElement.namespace}:${previousElement.value}` : previousElement.value;
        generateBindingUpdateOpCodes(updateOpCodes, message, previousElementIndex, attrName, countBindings(updateOpCodes), i18nResolveSanitizer(attrName, tagName));
      }
    }
    tView.data[index] = updateOpCodes;
  }
}
function generateBindingUpdateOpCodes(updateOpCodes, str, destinationNode, attrName, bindingStart, sanitizeFn) {
  ngDevMode && assertGreaterThanOrEqual(destinationNode, HEADER_OFFSET, "Index must be in absolute LView offset");
  const maskIndex = updateOpCodes.length;
  const sizeIndex = maskIndex + 1;
  updateOpCodes.push(null, null);
  const startIndex = maskIndex + 2;
  if (ngDevMode) {
    attachDebugGetter(updateOpCodes, i18nUpdateOpCodesToString);
  }
  const textParts = str.split(BINDING_REGEXP);
  let mask = 0;
  for (let j = 0; j < textParts.length; j++) {
    const textValue = textParts[j];
    if (j & 1) {
      const bindingIndex = bindingStart + parseInt(textValue, 10);
      updateOpCodes.push(-1 - bindingIndex);
      mask = mask | toMaskBit(bindingIndex);
    } else if (textValue !== "") {
      updateOpCodes.push(textValue);
    }
  }
  updateOpCodes.push(destinationNode << 2 | (attrName ? 1 : 0));
  if (attrName) {
    updateOpCodes.push(attrName, sanitizeFn);
  }
  updateOpCodes[maskIndex] = mask;
  updateOpCodes[sizeIndex] = updateOpCodes.length - startIndex;
  return mask;
}
function countBindings(opCodes) {
  let count = 0;
  for (let i = 0; i < opCodes.length; i++) {
    const opCode = opCodes[i];
    if (typeof opCode === "number" && opCode < 0) {
      count++;
    }
  }
  return count;
}
function toMaskBit(bindingIndex) {
  return 1 << Math.min(bindingIndex, 31);
}
function removeInnerTemplateTranslation(message) {
  let match;
  let res = "";
  let index = 0;
  let inTemplate = false;
  let tagMatched;
  while ((match = SUBTEMPLATE_REGEXP.exec(message)) !== null) {
    if (!inTemplate) {
      res += message.substring(index, match.index + match[0].length);
      tagMatched = match[1];
      inTemplate = true;
    } else {
      if (match[0] === `${MARKER}/*${tagMatched}${MARKER}`) {
        index = match.index;
        inTemplate = false;
      }
    }
  }
  ngDevMode && assertEqual(inTemplate, false, `Tag mismatch: unable to find the end of the sub-template in the translation "${message}"`);
  res += message.slice(index);
  return res;
}
function getTranslationForTemplate(message, subTemplateIndex) {
  if (isRootTemplateMessage(subTemplateIndex)) {
    return removeInnerTemplateTranslation(message);
  } else {
    const start = message.indexOf(`:${subTemplateIndex}${MARKER}`) + 2 + subTemplateIndex.toString().length;
    const end = message.search(new RegExp(`${MARKER}\\/\\*\\d+:${subTemplateIndex}${MARKER}`));
    return removeInnerTemplateTranslation(message.substring(start, end));
  }
}
function icuStart(ast, tView, lView, updateOpCodes, parentIdx, icuExpression, anchorIdx) {
  ngDevMode && assertDefined(icuExpression, "ICU expression must be defined");
  let bindingMask = 0;
  const tIcu = {
    type: icuExpression.type,
    currentCaseLViewIndex: allocExpando(tView, lView, 1, null),
    anchorIdx,
    cases: [],
    create: [],
    remove: [],
    update: []
  };
  addUpdateIcuSwitch(updateOpCodes, icuExpression, anchorIdx);
  setTIcu(tView, anchorIdx, tIcu);
  const values = icuExpression.values;
  const cases = [];
  for (let i = 0; i < values.length; i++) {
    const valueArr = values[i];
    const nestedIcus = [];
    for (let j = 0; j < valueArr.length; j++) {
      const value = valueArr[j];
      if (typeof value !== "string") {
        const icuIndex = nestedIcus.push(value) - 1;
        valueArr[j] = `<!--\uFFFD${icuIndex}\uFFFD-->`;
      }
    }
    const caseAst = [];
    cases.push(caseAst);
    bindingMask = parseIcuCase(caseAst, tView, tIcu, lView, updateOpCodes, parentIdx, icuExpression.cases[i], valueArr.join(""), nestedIcus) | bindingMask;
  }
  if (bindingMask) {
    addUpdateIcuUpdate(updateOpCodes, bindingMask, anchorIdx);
  }
  ast.push({
    kind: 3,
    index: anchorIdx,
    cases,
    currentCaseLViewIndex: tIcu.currentCaseLViewIndex
  });
}
function parseICUBlock(pattern) {
  const cases = [];
  const values = [];
  let icuType = 1;
  let mainBinding = 0;
  pattern = pattern.replace(ICU_BLOCK_REGEXP, function(str, binding, type) {
    if (type === "select") {
      icuType = 0;
    } else {
      icuType = 1;
    }
    mainBinding = parseInt(binding.slice(1), 10);
    return "";
  });
  const parts = i18nParseTextIntoPartsAndICU(pattern);
  for (let pos = 0; pos < parts.length; ) {
    let key = parts[pos++].trim();
    if (icuType === 1) {
      key = key.replace(/\s*(?:=)?(\w+)\s*/, "$1");
    }
    if (key.length) {
      cases.push(key);
    }
    const blocks = i18nParseTextIntoPartsAndICU(parts[pos++]);
    if (cases.length > values.length) {
      values.push(blocks);
    }
  }
  return {
    type: icuType,
    mainBinding,
    cases,
    values
  };
}
function i18nParseTextIntoPartsAndICU(pattern) {
  if (!pattern) {
    return [];
  }
  let prevPos = 0;
  const braceStack = [];
  const results = [];
  const braces = /[{}]/g;
  braces.lastIndex = 0;
  let match;
  while (match = braces.exec(pattern)) {
    const pos = match.index;
    if (match[0] == "}") {
      braceStack.pop();
      if (braceStack.length == 0) {
        const block = pattern.substring(prevPos, pos);
        if (ICU_BLOCK_REGEXP.test(block)) {
          results.push(parseICUBlock(block));
        } else {
          results.push(block);
        }
        prevPos = pos + 1;
      }
    } else {
      if (braceStack.length == 0) {
        const substring2 = pattern.substring(prevPos, pos);
        results.push(substring2);
        prevPos = pos + 1;
      }
      braceStack.push("{");
    }
  }
  const substring = pattern.substring(prevPos);
  results.push(substring);
  return results;
}
function parseIcuCase(ast, tView, tIcu, lView, updateOpCodes, parentIdx, caseName, unsafeCaseHtml, nestedIcus) {
  const create = [];
  const remove2 = [];
  const update = [];
  if (ngDevMode) {
    attachDebugGetter(create, icuCreateOpCodesToString);
    attachDebugGetter(remove2, i18nRemoveOpCodesToString);
    attachDebugGetter(update, i18nUpdateOpCodesToString);
  }
  tIcu.cases.push(caseName);
  tIcu.create.push(create);
  tIcu.remove.push(remove2);
  tIcu.update.push(update);
  const inertBodyHelper2 = getInertBodyHelper(getDocument());
  const inertBodyElement = inertBodyHelper2.getInertBodyElement(unsafeCaseHtml);
  ngDevMode && assertDefined(inertBodyElement, "Unable to generate inert body element");
  const inertRootNode = getTemplateContent(inertBodyElement) || inertBodyElement;
  if (inertRootNode) {
    return walkIcuTree(ast, tView, tIcu, lView, updateOpCodes, create, remove2, update, inertRootNode, parentIdx, nestedIcus, 0);
  } else {
    return 0;
  }
}
function walkIcuTree(ast, tView, tIcu, lView, sharedUpdateOpCodes, create, remove2, update, parentNode, parentIdx, nestedIcus, depth) {
  let bindingMask = 0;
  let currentNode = parentNode.firstChild;
  while (currentNode) {
    const newIndex = allocExpando(tView, lView, 1, null);
    switch (currentNode.nodeType) {
      case Node.ELEMENT_NODE:
        const element = currentNode;
        const tagName = element.tagName.toLowerCase();
        if (VALID_ELEMENTS.hasOwnProperty(tagName)) {
          addCreateNodeAndAppend(create, ELEMENT_MARKER, tagName, parentIdx, newIndex);
          tView.data[newIndex] = tagName;
          const elAttrs = element.attributes;
          for (let i = 0; i < elAttrs.length; i++) {
            const attr = elAttrs.item(i);
            const lowerAttrName = attr.name.toLowerCase();
            const hasBinding2 = !!attr.value.match(BINDING_REGEXP);
            const elementNS = element.namespaceURI;
            const tagNameWithNamespace = elementNS === "http://www.w3.org/2000/svg" ? `:svg:${tagName}` : elementNS === "http://www.w3.org/1998/Math/MathML" ? `:math:${tagName}` : tagName;
            if (hasBinding2) {
              if (VALID_ATTRS.hasOwnProperty(lowerAttrName)) {
                generateBindingUpdateOpCodes(update, attr.value, newIndex, attr.name, 0, i18nResolveSanitizer(lowerAttrName, tagNameWithNamespace));
              } else {
                ngDevMode && console.warn(`WARNING: ignoring unsafe attribute value ${lowerAttrName} on element ${tagName} (see ${XSS_SECURITY_URL})`);
              }
            } else if (VALID_ATTRS[lowerAttrName]) {
              let val = attr.value;
              const sanitizer = i18nResolveSanitizer(lowerAttrName, tagNameWithNamespace);
              if (sanitizer) {
                if (typeof ngDevMode !== "undefined" && ngDevMode) {
                  console.warn(`WARNING: ignoring unsafe attribute ${lowerAttrName} on element ${tagName} (see ${XSS_SECURITY_URL})`);
                }
                addCreateAttribute(create, newIndex, attr.name, "unsafe:blocked");
              } else {
                addCreateAttribute(create, newIndex, attr.name, val);
              }
            } else {
              if (typeof ngDevMode !== "undefined" && ngDevMode) {
                console.warn(`WARNING: ignoring unknown attribute name ${lowerAttrName} on element ${tagName} (see ${XSS_SECURITY_URL})`);
              }
            }
          }
          const elementNode = {
            kind: 1,
            index: newIndex,
            children: []
          };
          ast.push(elementNode);
          bindingMask = walkIcuTree(elementNode.children, tView, tIcu, lView, sharedUpdateOpCodes, create, remove2, update, currentNode, newIndex, nestedIcus, depth + 1) | bindingMask;
          addRemoveNode(remove2, newIndex, depth);
        }
        break;
      case Node.TEXT_NODE:
        const value = currentNode.textContent || "";
        const hasBinding = value.match(BINDING_REGEXP);
        addCreateNodeAndAppend(create, null, hasBinding ? "" : value, parentIdx, newIndex);
        addRemoveNode(remove2, newIndex, depth);
        if (hasBinding) {
          bindingMask = generateBindingUpdateOpCodes(update, value, newIndex, null, 0, null) | bindingMask;
        }
        ast.push({
          kind: 0,
          index: newIndex
        });
        break;
      case Node.COMMENT_NODE:
        const isNestedIcu = NESTED_ICU.exec(currentNode.textContent || "");
        if (isNestedIcu) {
          const nestedIcuIndex = parseInt(isNestedIcu[1], 10);
          const icuExpression = nestedIcus[nestedIcuIndex];
          addCreateNodeAndAppend(create, ICU_MARKER, ngDevMode ? `nested ICU ${nestedIcuIndex}` : "", parentIdx, newIndex);
          icuStart(ast, tView, lView, sharedUpdateOpCodes, parentIdx, icuExpression, newIndex);
          addRemoveNestedIcu(remove2, newIndex, depth);
        }
        break;
    }
    currentNode = currentNode.nextSibling;
  }
  return bindingMask;
}
function addRemoveNode(remove2, index, depth) {
  if (depth === 0) {
    remove2.push(index);
  }
}
function addRemoveNestedIcu(remove2, index, depth) {
  if (depth === 0) {
    remove2.push(~index);
    remove2.push(index);
  }
}
function addUpdateIcuSwitch(update, icuExpression, index) {
  update.push(toMaskBit(icuExpression.mainBinding), 2, -1 - icuExpression.mainBinding, index << 2 | 2);
}
function addUpdateIcuUpdate(update, bindingMask, index) {
  update.push(bindingMask, 1, index << 2 | 3);
}
function addCreateNodeAndAppend(create, marker, text, appendToParentIdx, createAtIdx) {
  if (marker !== null) {
    create.push(marker);
  }
  create.push(text, createAtIdx, icuCreateOpCode(0, appendToParentIdx, createAtIdx));
}
function addCreateAttribute(create, newIndex, attrName, attrValue) {
  create.push(newIndex << 1 | 1, attrName, attrValue);
}
function splitNsName(elementName, fatal = true) {
  if (elementName[0] != ":") {
    return [null, elementName];
  }
  const colonIndex = elementName.indexOf(":", 1);
  if (colonIndex === -1) {
    if (fatal) {
      throw new Error(`Unsupported format "${elementName}" expecting ":namespace:name"`);
    } else {
      return [null, elementName];
    }
  }
  return [elementName.slice(1, colonIndex), elementName.slice(colonIndex + 1)];
}
function normalizeTagName(tagName) {
  const tagNameLower = tagName.toLowerCase();
  const [ns, name] = splitNsName(tagNameLower, false);
  return ns === SVG_NAMESPACE || ns === MATH_ML_NAMESPACE ? `:${ns}:${name}` : name;
}
function i18nResolveSanitizer(attrName, tagName) {
  const lowerAttrName = attrName.toLowerCase();
  const lowerTagName = tagName ? normalizeTagName(tagName) : "*";
  const schema = SECURITY_SCHEMA();
  const schemaContext = schema[`${lowerTagName}|${lowerAttrName}`] || schema[`*|${lowerAttrName}`] || SecurityContext.NONE;
  switch (schemaContext) {
    case SecurityContext.HTML:
      return \u0275\u0275sanitizeHtml;
    case SecurityContext.STYLE:
      return \u0275\u0275sanitizeStyle;
    case SecurityContext.SCRIPT:
      return \u0275\u0275sanitizeScript;
    case SecurityContext.URL:
      return _sanitizeUrl;
    case SecurityContext.RESOURCE_URL:
      return \u0275\u0275sanitizeResourceUrl;
    case SecurityContext.ATTRIBUTE_NO_BINDING:
      return \u0275\u0275validateAttribute;
    default:
      return null;
  }
}
var ROOT_TEMPLATE_ID = 0;
var PP_MULTI_VALUE_PLACEHOLDERS_REGEXP = /\[(�.+?�?)\]/;
var PP_PLACEHOLDERS_REGEXP = /\[(�.+?�?)\]|(�\/?\*\d+:\d+�)/g;
var PP_ICU_VARS_REGEXP = /({\s*)(VAR_(PLURAL|SELECT)(_\d+)?)(\s*,)/g;
var PP_ICU_PLACEHOLDERS_REGEXP = /{([A-Z0-9_]+)}/g;
var PP_ICUS_REGEXP = /�I18N_EXP_(ICU(_\d+)?)�/g;
var PP_CLOSE_TEMPLATE_REGEXP = /\/\*/;
var PP_TEMPLATE_ID_REGEXP = /\d+\:(\d+)/;
function i18nPostprocess(message, replacements = {}) {
  let result = message;
  if (PP_MULTI_VALUE_PLACEHOLDERS_REGEXP.test(message)) {
    const matches = {};
    const templateIdsStack = [ROOT_TEMPLATE_ID];
    result = result.replace(PP_PLACEHOLDERS_REGEXP, (m, phs, tmpl) => {
      const content = phs || tmpl;
      const placeholders = matches[content] || [];
      if (!placeholders.length) {
        content.split("|").forEach((placeholder2) => {
          const match = placeholder2.match(PP_TEMPLATE_ID_REGEXP);
          const templateId2 = match ? parseInt(match[1], 10) : ROOT_TEMPLATE_ID;
          const isCloseTemplateTag2 = PP_CLOSE_TEMPLATE_REGEXP.test(placeholder2);
          placeholders.push([templateId2, isCloseTemplateTag2, placeholder2]);
        });
        matches[content] = placeholders;
      }
      if (!placeholders.length) {
        throw new Error(`i18n postprocess: unmatched placeholder - ${content}`);
      }
      const currentTemplateId = templateIdsStack[templateIdsStack.length - 1];
      let idx = 0;
      for (let i = 0; i < placeholders.length; i++) {
        if (placeholders[i][0] === currentTemplateId) {
          idx = i;
          break;
        }
      }
      const [templateId, isCloseTemplateTag, placeholder] = placeholders[idx];
      if (isCloseTemplateTag) {
        templateIdsStack.pop();
      } else if (currentTemplateId !== templateId) {
        templateIdsStack.push(templateId);
      }
      placeholders.splice(idx, 1);
      return placeholder;
    });
  }
  if (!Object.keys(replacements).length) {
    return result;
  }
  result = result.replace(PP_ICU_VARS_REGEXP, (match, start, key, _type, _idx, end) => {
    return replacements.hasOwnProperty(key) ? `${start}${replacements[key]}${end}` : match;
  });
  result = result.replace(PP_ICU_PLACEHOLDERS_REGEXP, (match, key) => {
    return replacements.hasOwnProperty(key) ? replacements[key] : match;
  });
  result = result.replace(PP_ICUS_REGEXP, (match, key) => {
    if (replacements.hasOwnProperty(key)) {
      const list = replacements[key];
      if (!list.length) {
        throw new Error(`i18n postprocess: unmatched ICU - ${match} with key: ${key}`);
      }
      return list.shift();
    }
    return match;
  });
  return result;
}
function \u0275\u0275i18nStart(index, messageIndex, subTemplateIndex = -1) {
  const tView = getTView();
  const lView = getLView();
  const adjustedIndex = HEADER_OFFSET + index;
  ngDevMode && assertDefined(tView, `tView should be defined`);
  const message = getConstant(tView.consts, messageIndex);
  const parentTNode = getCurrentParentTNode();
  if (tView.firstCreatePass) {
    i18nStartFirstCreatePass(tView, parentTNode === null ? 0 : parentTNode.index, lView, adjustedIndex, message, subTemplateIndex);
  }
  if (tView.type === 2) {
    const componentLView = lView[DECLARATION_COMPONENT_VIEW];
    componentLView[FLAGS] |= 32;
  } else {
    lView[FLAGS] |= 32;
  }
  const tI18n = tView.data[adjustedIndex];
  const sameViewParentTNode = parentTNode === lView[T_HOST] ? null : parentTNode;
  const parentRNode = getClosestRElement(tView, sameViewParentTNode, lView);
  const insertInFrontOf = parentTNode && parentTNode.type & 8 ? lView[parentTNode.index] : null;
  prepareI18nBlockForHydration(lView, adjustedIndex, parentTNode, subTemplateIndex);
  applyCreateOpCodes(lView, tI18n.create, parentRNode, insertInFrontOf);
  setInI18nBlock(true);
}
function \u0275\u0275i18nEnd() {
  setInI18nBlock(false);
}
function \u0275\u0275i18n(index, messageIndex, subTemplateIndex) {
  \u0275\u0275i18nStart(index, messageIndex, subTemplateIndex);
  \u0275\u0275i18nEnd();
}
function \u0275\u0275i18nAttributes(index, attrsIndex) {
  const tView = getTView();
  ngDevMode && assertDefined(tView, `tView should be defined`);
  const attrs = getConstant(tView.consts, attrsIndex);
  i18nAttributesFirstPass(tView, index + HEADER_OFFSET, attrs);
}
function \u0275\u0275i18nExp(value) {
  const lView = getLView();
  setMaskBit(bindingUpdated(lView, nextBindingIndex(), value));
  return \u0275\u0275i18nExp;
}
function \u0275\u0275i18nApply(index) {
  applyI18n(getTView(), getLView(), index + HEADER_OFFSET);
}
function \u0275\u0275i18nPostprocess(message, replacements = {}) {
  return i18nPostprocess(message, replacements);
}
function \u0275\u0275listener(eventName, listenerFn, eventTargetResolver) {
  const lView = getLView();
  const tView = getTView();
  const tNode = getCurrentTNode();
  listenerInternal(tView, lView, lView[RENDERER], tNode, eventName, listenerFn, eventTargetResolver);
  return \u0275\u0275listener;
}
function \u0275\u0275syntheticHostListener(eventName, listenerFn) {
  const tNode = getCurrentTNode();
  const lView = getLView();
  const tView = getTView();
  const currentDef = getCurrentDirectiveDef(tView.data);
  const renderer = loadComponentRenderer(currentDef, tNode, lView);
  listenerInternal(tView, lView, renderer, tNode, eventName, listenerFn);
  return \u0275\u0275syntheticHostListener;
}
function \u0275\u0275domListener(eventName, listenerFn, eventTargetResolver) {
  const lView = getLView();
  const tView = getTView();
  const tNode = getCurrentTNode();
  if (tNode.type & 3 || eventTargetResolver) {
    listenToDomEvent(tNode, tView, lView, eventTargetResolver, lView[RENDERER], eventName, listenerFn, wrapListener(tNode, lView, listenerFn));
  }
  return \u0275\u0275domListener;
}
function listenerInternal(tView, lView, renderer, tNode, eventName, listenerFn, eventTargetResolver) {
  ngDevMode && assertTNodeType(tNode, 3 | 12);
  let processOutputs = true;
  let wrappedListener = null;
  if (tNode.type & 3 || eventTargetResolver) {
    wrappedListener ??= wrapListener(tNode, lView, listenerFn);
    const hasCoalescedDomEvent = listenToDomEvent(tNode, tView, lView, eventTargetResolver, renderer, eventName, listenerFn, wrappedListener);
    if (hasCoalescedDomEvent) {
      processOutputs = false;
    }
  }
  if (processOutputs) {
    const outputConfig = tNode.outputs?.[eventName];
    const hostDirectiveOutputConfig = tNode.hostDirectiveOutputs?.[eventName];
    if (hostDirectiveOutputConfig && hostDirectiveOutputConfig.length) {
      for (let i = 0; i < hostDirectiveOutputConfig.length; i += 2) {
        const index = hostDirectiveOutputConfig[i];
        const lookupName = hostDirectiveOutputConfig[i + 1];
        wrappedListener ??= wrapListener(tNode, lView, listenerFn);
        listenToOutput(tNode, lView, index, lookupName, eventName, wrappedListener);
      }
    }
    if (outputConfig && outputConfig.length) {
      for (const index of outputConfig) {
        wrappedListener ??= wrapListener(tNode, lView, listenerFn);
        listenToOutput(tNode, lView, index, eventName, eventName, wrappedListener);
      }
    }
  }
}
function \u0275\u0275nextContext(level = 1) {
  return nextContextImpl(level);
}
function matchingProjectionSlotIndex(tNode, projectionSlots) {
  let wildcardNgContentIndex = null;
  const ngProjectAsAttrVal = getProjectAsAttrValue(tNode);
  for (let i = 0; i < projectionSlots.length; i++) {
    const slotValue = projectionSlots[i];
    if (slotValue === "*") {
      wildcardNgContentIndex = i;
      continue;
    }
    if (ngProjectAsAttrVal === null ? isNodeMatchingSelectorList(tNode, slotValue, true) : isSelectorInSelectorList(ngProjectAsAttrVal, slotValue)) {
      return i;
    }
  }
  return wildcardNgContentIndex;
}
function \u0275\u0275projectionDef(projectionSlots) {
  const componentNode = getLView()[DECLARATION_COMPONENT_VIEW][T_HOST];
  if (!componentNode.projection) {
    const numProjectionSlots = projectionSlots ? projectionSlots.length : 1;
    const projectionHeads = componentNode.projection = newArray(numProjectionSlots, null);
    const tails = projectionHeads.slice();
    let componentChild = componentNode.child;
    while (componentChild !== null) {
      if (componentChild.type !== 128) {
        const slotIndex = projectionSlots ? matchingProjectionSlotIndex(componentChild, projectionSlots) : 0;
        if (slotIndex !== null) {
          if (tails[slotIndex]) {
            tails[slotIndex].projectionNext = componentChild;
          } else {
            projectionHeads[slotIndex] = componentChild;
          }
          tails[slotIndex] = componentChild;
        }
      }
      componentChild = componentChild.next;
    }
  }
}
function \u0275\u0275projection(nodeIndex, selectorIndex = 0, attrs, fallbackTemplateFn, fallbackDecls, fallbackVars) {
  const lView = getLView();
  const tView = getTView();
  const fallbackIndex = fallbackTemplateFn ? nodeIndex + 1 : null;
  if (fallbackIndex !== null) {
    declareNoDirectiveHostTemplate(lView, tView, fallbackIndex, fallbackTemplateFn, fallbackDecls, fallbackVars, null, attrs);
  }
  const tProjectionNode = getOrCreateTNode(tView, HEADER_OFFSET + nodeIndex, 16, null, attrs || null);
  if (tProjectionNode.projection === null) {
    tProjectionNode.projection = selectorIndex;
  }
  setCurrentTNodeAsNotParent();
  const hydrationInfo = lView[HYDRATION];
  const isNodeCreationMode = !hydrationInfo || isInSkipHydrationBlock();
  const componentHostNode = lView[DECLARATION_COMPONENT_VIEW][T_HOST];
  const isEmpty = componentHostNode.projection[tProjectionNode.projection] === null;
  if (isEmpty && fallbackIndex !== null) {
    insertFallbackContent(lView, tView, fallbackIndex);
  } else if (isNodeCreationMode && !isDetachedByI18n(tProjectionNode)) {
    applyProjection(tView, lView, tProjectionNode);
  }
}
function insertFallbackContent(lView, tView, fallbackIndex) {
  const adjustedIndex = HEADER_OFFSET + fallbackIndex;
  const fallbackTNode = tView.data[adjustedIndex];
  const fallbackLContainer = lView[adjustedIndex];
  ngDevMode && assertTNode(fallbackTNode);
  ngDevMode && assertLContainer(fallbackLContainer);
  const dehydratedView = findMatchingDehydratedView(fallbackLContainer, fallbackTNode.tView.ssrId);
  const fallbackLView = createAndRenderEmbeddedLView(lView, fallbackTNode, void 0, {
    dehydratedView
  });
  addLViewToLContainer(fallbackLContainer, fallbackLView, 0, shouldAddViewToDom(fallbackTNode, dehydratedView));
}
function \u0275\u0275contentQuery(directiveIndex, predicate, flags, read) {
  createContentQuery(directiveIndex, predicate, flags, read);
  return \u0275\u0275contentQuery;
}
function \u0275\u0275viewQuery(predicate, flags, read) {
  createViewQuery(predicate, flags, read);
  return \u0275\u0275viewQuery;
}
function \u0275\u0275queryRefresh(queryList) {
  const lView = getLView();
  const tView = getTView();
  const queryIndex = getCurrentQueryIndex();
  setCurrentQueryIndex(queryIndex + 1);
  const tQuery = getTQuery(tView, queryIndex);
  if (queryList.dirty && isCreationMode(lView) === ((tQuery.metadata.flags & 2) === 2)) {
    if (tQuery.matches === null) {
      queryList.reset([]);
    } else {
      const result = getQueryResults(lView, queryIndex);
      queryList.reset(result, unwrapElementRef);
      queryList.notifyOnChanges();
    }
    return true;
  }
  return false;
}
function \u0275\u0275loadQuery() {
  return loadQueryInternal(getLView(), getCurrentQueryIndex());
}
function \u0275\u0275contentQuerySignal(directiveIndex, target, predicate, flags, read) {
  bindQueryToSignal(target, createContentQuery(directiveIndex, predicate, flags, read));
  return \u0275\u0275contentQuerySignal;
}
function \u0275\u0275viewQuerySignal(target, predicate, flags, read) {
  bindQueryToSignal(target, createViewQuery(predicate, flags, read));
  return \u0275\u0275viewQuerySignal;
}
function \u0275\u0275queryAdvance(indexOffset = 1) {
  setCurrentQueryIndex(getCurrentQueryIndex() + indexOffset);
}
function \u0275\u0275reference(index) {
  const contextLView = getContextLView();
  return load(contextLView, HEADER_OFFSET + index);
}
function toTStylingRange(prev, next) {
  ngDevMode && assertNumberInRange(prev, 0, 32767);
  ngDevMode && assertNumberInRange(next, 0, 32767);
  return prev << 17 | next << 2;
}
function getTStylingRangePrev(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return tStylingRange >> 17 & 32767;
}
function getTStylingRangePrevDuplicate(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return (tStylingRange & 2) == 2;
}
function setTStylingRangePrev(tStylingRange, previous) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  ngDevMode && assertNumberInRange(previous, 0, 32767);
  return tStylingRange & 131071 | previous << 17;
}
function setTStylingRangePrevDuplicate(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return tStylingRange | 2;
}
function getTStylingRangeNext(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return (tStylingRange & 131068) >> 2;
}
function setTStylingRangeNext(tStylingRange, next) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  ngDevMode && assertNumberInRange(next, 0, 32767);
  return tStylingRange & -131069 | next << 2;
}
function getTStylingRangeNextDuplicate(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return (tStylingRange & 1) === 1;
}
function setTStylingRangeNextDuplicate(tStylingRange) {
  ngDevMode && assertNumber(tStylingRange, "expected number");
  return tStylingRange | 1;
}
function insertTStylingBinding(tData, tNode, tStylingKeyWithStatic, index, isHostBinding, isClassBinding) {
  ngDevMode && assertFirstUpdatePass(getTView());
  let tBindings = isClassBinding ? tNode.classBindings : tNode.styleBindings;
  let tmplHead = getTStylingRangePrev(tBindings);
  let tmplTail = getTStylingRangeNext(tBindings);
  tData[index] = tStylingKeyWithStatic;
  let isKeyDuplicateOfStatic = false;
  let tStylingKey;
  if (Array.isArray(tStylingKeyWithStatic)) {
    const staticKeyValueArray = tStylingKeyWithStatic;
    tStylingKey = staticKeyValueArray[1];
    if (tStylingKey === null || keyValueArrayIndexOf(staticKeyValueArray, tStylingKey) > 0) {
      isKeyDuplicateOfStatic = true;
    }
  } else {
    tStylingKey = tStylingKeyWithStatic;
  }
  if (isHostBinding) {
    const hasTemplateBindings = tmplTail !== 0;
    if (hasTemplateBindings) {
      const previousNode = getTStylingRangePrev(tData[tmplHead + 1]);
      tData[index + 1] = toTStylingRange(previousNode, tmplHead);
      if (previousNode !== 0) {
        tData[previousNode + 1] = setTStylingRangeNext(tData[previousNode + 1], index);
      }
      tData[tmplHead + 1] = setTStylingRangePrev(tData[tmplHead + 1], index);
    } else {
      tData[index + 1] = toTStylingRange(tmplHead, 0);
      if (tmplHead !== 0) {
        tData[tmplHead + 1] = setTStylingRangeNext(tData[tmplHead + 1], index);
      }
      tmplHead = index;
    }
  } else {
    tData[index + 1] = toTStylingRange(tmplTail, 0);
    ngDevMode && assertEqual(tmplHead !== 0 && tmplTail === 0, false, "Adding template bindings after hostBindings is not allowed.");
    if (tmplHead === 0) {
      tmplHead = index;
    } else {
      tData[tmplTail + 1] = setTStylingRangeNext(tData[tmplTail + 1], index);
    }
    tmplTail = index;
  }
  if (isKeyDuplicateOfStatic) {
    tData[index + 1] = setTStylingRangePrevDuplicate(tData[index + 1]);
  }
  markDuplicates(tData, tStylingKey, index, true);
  markDuplicates(tData, tStylingKey, index, false);
  markDuplicateOfResidualStyling(tNode, tStylingKey, tData, index, isClassBinding);
  tBindings = toTStylingRange(tmplHead, tmplTail);
  if (isClassBinding) {
    tNode.classBindings = tBindings;
  } else {
    tNode.styleBindings = tBindings;
  }
}
function markDuplicateOfResidualStyling(tNode, tStylingKey, tData, index, isClassBinding) {
  const residual = isClassBinding ? tNode.residualClasses : tNode.residualStyles;
  if (residual != null && typeof tStylingKey == "string" && keyValueArrayIndexOf(residual, tStylingKey) >= 0) {
    tData[index + 1] = setTStylingRangeNextDuplicate(tData[index + 1]);
  }
}
function markDuplicates(tData, tStylingKey, index, isPrevDir) {
  const tStylingAtIndex = tData[index + 1];
  const isMap = tStylingKey === null;
  let cursor = isPrevDir ? getTStylingRangePrev(tStylingAtIndex) : getTStylingRangeNext(tStylingAtIndex);
  let foundDuplicate = false;
  while (cursor !== 0 && (foundDuplicate === false || isMap)) {
    ngDevMode && assertIndexInRange(tData, cursor);
    const tStylingValueAtCursor = tData[cursor];
    const tStyleRangeAtCursor = tData[cursor + 1];
    if (isStylingMatch(tStylingValueAtCursor, tStylingKey)) {
      foundDuplicate = true;
      tData[cursor + 1] = isPrevDir ? setTStylingRangeNextDuplicate(tStyleRangeAtCursor) : setTStylingRangePrevDuplicate(tStyleRangeAtCursor);
    }
    cursor = isPrevDir ? getTStylingRangePrev(tStyleRangeAtCursor) : getTStylingRangeNext(tStyleRangeAtCursor);
  }
  if (foundDuplicate) {
    tData[index + 1] = isPrevDir ? setTStylingRangePrevDuplicate(tStylingAtIndex) : setTStylingRangeNextDuplicate(tStylingAtIndex);
  }
}
function isStylingMatch(tStylingKeyCursor, tStylingKey) {
  ngDevMode && assertNotEqual(Array.isArray(tStylingKey), true, "Expected that 'tStylingKey' has been unwrapped");
  if (tStylingKeyCursor === null || tStylingKey == null || (Array.isArray(tStylingKeyCursor) ? tStylingKeyCursor[1] : tStylingKeyCursor) === tStylingKey) {
    return true;
  } else if (Array.isArray(tStylingKeyCursor) && typeof tStylingKey === "string") {
    return keyValueArrayIndexOf(tStylingKeyCursor, tStylingKey) >= 0;
  }
  return false;
}
var parserState = {
  textEnd: 0,
  key: 0,
  keyEnd: 0,
  value: 0,
  valueEnd: 0
};
function getLastParsedKey(text) {
  return text.substring(parserState.key, parserState.keyEnd);
}
function getLastParsedValue(text) {
  return text.substring(parserState.value, parserState.valueEnd);
}
function parseClassName(text) {
  resetParserState(text);
  return parseClassNameNext(text, consumeWhitespace(text, 0, parserState.textEnd));
}
function parseClassNameNext(text, index) {
  const end = parserState.textEnd;
  if (end === index) {
    return -1;
  }
  index = parserState.keyEnd = consumeClassToken(text, parserState.key = index, end);
  return consumeWhitespace(text, index, end);
}
function parseStyle(text) {
  resetParserState(text);
  return parseStyleNext(text, consumeWhitespace(text, 0, parserState.textEnd));
}
function parseStyleNext(text, startIndex) {
  const end = parserState.textEnd;
  let index = parserState.key = consumeWhitespace(text, startIndex, end);
  if (end === index) {
    return -1;
  }
  index = parserState.keyEnd = consumeStyleKey(text, index, end);
  index = consumeSeparator(text, index, end, 58);
  index = parserState.value = consumeWhitespace(text, index, end);
  index = parserState.valueEnd = consumeStyleValue(text, index, end);
  return consumeSeparator(text, index, end, 59);
}
function resetParserState(text) {
  parserState.key = 0;
  parserState.keyEnd = 0;
  parserState.value = 0;
  parserState.valueEnd = 0;
  parserState.textEnd = text.length;
}
function consumeWhitespace(text, startIndex, endIndex) {
  while (startIndex < endIndex && text.charCodeAt(startIndex) <= 32) {
    startIndex++;
  }
  return startIndex;
}
function consumeClassToken(text, startIndex, endIndex) {
  while (startIndex < endIndex && text.charCodeAt(startIndex) > 32) {
    startIndex++;
  }
  return startIndex;
}
function consumeStyleKey(text, startIndex, endIndex) {
  let ch;
  while (startIndex < endIndex && ((ch = text.charCodeAt(startIndex)) === 45 || ch === 95 || (ch & -33) >= 65 && (ch & -33) <= 90 || ch >= 48 && ch <= 57)) {
    startIndex++;
  }
  return startIndex;
}
function consumeSeparator(text, startIndex, endIndex, separator) {
  startIndex = consumeWhitespace(text, startIndex, endIndex);
  if (startIndex < endIndex) {
    if (ngDevMode && text.charCodeAt(startIndex) !== separator) {
      malformedStyleError(text, String.fromCharCode(separator), startIndex);
    }
    startIndex++;
  }
  return startIndex;
}
function consumeStyleValue(text, startIndex, endIndex) {
  let ch1 = -1;
  let ch2 = -1;
  let ch3 = -1;
  let i = startIndex;
  let lastChIndex = i;
  while (i < endIndex) {
    const ch = text.charCodeAt(i++);
    if (ch === 59) {
      return lastChIndex;
    } else if (ch === 34 || ch === 39) {
      lastChIndex = i = consumeQuotedText(text, ch, i, endIndex);
    } else if (startIndex === i - 4 && ch3 === 85 && ch2 === 82 && ch1 === 76 && ch === 40) {
      lastChIndex = i = consumeQuotedText(text, 41, i, endIndex);
    } else if (ch > 32) {
      lastChIndex = i;
    }
    ch3 = ch2;
    ch2 = ch1;
    ch1 = ch & -33;
  }
  return lastChIndex;
}
function consumeQuotedText(text, quoteCharCode, startIndex, endIndex) {
  let ch1 = -1;
  let index = startIndex;
  while (index < endIndex) {
    const ch = text.charCodeAt(index++);
    if (ch == quoteCharCode && ch1 !== 92) {
      return index;
    }
    if (ch == 92 && ch1 === 92) {
      ch1 = 0;
    } else {
      ch1 = ch;
    }
  }
  throw ngDevMode ? malformedStyleError(text, String.fromCharCode(quoteCharCode), endIndex) : new Error();
}
function malformedStyleError(text, expecting, index) {
  ngDevMode && assertEqual(typeof text === "string", true, "String expected here");
  throw throwError2(`Malformed style at location ${index} in string '` + text.substring(0, index) + "[>>" + text.substring(index, index + 1) + "<<]" + text.slice(index + 1) + `'. Expecting '${expecting}'.`);
}
function \u0275\u0275styleProp(prop, value, suffix) {
  checkStylingProperty(prop, value, suffix, false);
  return \u0275\u0275styleProp;
}
function \u0275\u0275classProp(className, value) {
  checkStylingProperty(className, value, null, true);
  return \u0275\u0275classProp;
}
function \u0275\u0275styleMap(styles) {
  checkStylingMap(styleKeyValueArraySet, styleStringParser, styles, false);
}
function styleStringParser(keyValueArray, text) {
  for (let i = parseStyle(text); i >= 0; i = parseStyleNext(text, i)) {
    styleKeyValueArraySet(keyValueArray, getLastParsedKey(text), getLastParsedValue(text));
  }
}
function \u0275\u0275classMap(classes) {
  checkStylingMap(classKeyValueArraySet, classStringParser, classes, true);
}
function classStringParser(keyValueArray, text) {
  for (let i = parseClassName(text); i >= 0; i = parseClassNameNext(text, i)) {
    keyValueArraySet(keyValueArray, getLastParsedKey(text), true);
  }
}
function checkStylingProperty(prop, value, suffix, isClassBased) {
  const lView = getLView();
  const tView = getTView();
  const bindingIndex = incrementBindingIndex(2);
  if (tView.firstUpdatePass) {
    stylingFirstUpdatePass(tView, prop, bindingIndex, isClassBased);
  }
  if (value !== NO_CHANGE && bindingUpdated(lView, bindingIndex, value)) {
    const tNode = tView.data[getSelectedIndex()];
    updateStyling(tView, tNode, lView, lView[RENDERER], prop, lView[bindingIndex + 1] = normalizeSuffix(value, suffix), isClassBased, bindingIndex);
  }
}
function checkStylingMap(keyValueArraySet2, stringParser, value, isClassBased) {
  const tView = getTView();
  const bindingIndex = incrementBindingIndex(2);
  if (tView.firstUpdatePass) {
    stylingFirstUpdatePass(tView, null, bindingIndex, isClassBased);
  }
  const lView = getLView();
  if (value !== NO_CHANGE && bindingUpdated(lView, bindingIndex, value)) {
    const tNode = tView.data[getSelectedIndex()];
    if (hasStylingInputShadow(tNode, isClassBased) && !isInHostBindings(tView, bindingIndex)) {
      if (ngDevMode) {
        const tStylingKey = tView.data[bindingIndex];
        assertEqual(Array.isArray(tStylingKey) ? tStylingKey[1] : tStylingKey, false, "Styling linked list shadow input should be marked as 'false'");
      }
      let staticPrefix = isClassBased ? tNode.classesWithoutHost : tNode.stylesWithoutHost;
      ngDevMode && isClassBased === false && staticPrefix !== null && assertEqual(staticPrefix.endsWith(";"), true, "Expecting static portion to end with ';'");
      if (staticPrefix !== null) {
        value = concatStringsWithSpace(staticPrefix, value ? value : "");
      }
      setDirectiveInputsWhichShadowsStyling(tView, tNode, lView, value, isClassBased);
    } else {
      updateStylingMap(tView, tNode, lView, lView[RENDERER], lView[bindingIndex + 1], lView[bindingIndex + 1] = toStylingKeyValueArray(keyValueArraySet2, stringParser, value), isClassBased, bindingIndex);
    }
  }
}
function isInHostBindings(tView, bindingIndex) {
  return bindingIndex >= tView.expandoStartIndex;
}
function stylingFirstUpdatePass(tView, tStylingKey, bindingIndex, isClassBased) {
  ngDevMode && assertFirstUpdatePass(tView);
  const tData = tView.data;
  if (tData[bindingIndex + 1] === null) {
    const tNode = tData[getSelectedIndex()];
    ngDevMode && assertDefined(tNode, "TNode expected");
    const isHostBindings = isInHostBindings(tView, bindingIndex);
    if (hasStylingInputShadow(tNode, isClassBased) && tStylingKey === null && !isHostBindings) {
      tStylingKey = false;
    }
    tStylingKey = wrapInStaticStylingKey(tData, tNode, tStylingKey, isClassBased);
    insertTStylingBinding(tData, tNode, tStylingKey, bindingIndex, isHostBindings, isClassBased);
  }
}
function wrapInStaticStylingKey(tData, tNode, stylingKey, isClassBased) {
  const hostDirectiveDef = getCurrentDirectiveDef(tData);
  let residual = isClassBased ? tNode.residualClasses : tNode.residualStyles;
  if (hostDirectiveDef === null) {
    const isFirstStylingInstructionInTemplate = (isClassBased ? tNode.classBindings : tNode.styleBindings) === 0;
    if (isFirstStylingInstructionInTemplate) {
      stylingKey = collectStylingFromDirectives(null, tData, tNode, stylingKey, isClassBased);
      stylingKey = collectStylingFromTAttrs(stylingKey, tNode.attrs, isClassBased);
      residual = null;
    }
  } else {
    const directiveStylingLast = tNode.directiveStylingLast;
    const isFirstStylingInstructionInHostBinding = directiveStylingLast === -1 || tData[directiveStylingLast] !== hostDirectiveDef;
    if (isFirstStylingInstructionInHostBinding) {
      stylingKey = collectStylingFromDirectives(hostDirectiveDef, tData, tNode, stylingKey, isClassBased);
      if (residual === null) {
        let templateStylingKey = getTemplateHeadTStylingKey(tData, tNode, isClassBased);
        if (templateStylingKey !== void 0 && Array.isArray(templateStylingKey)) {
          templateStylingKey = collectStylingFromDirectives(null, tData, tNode, templateStylingKey[1], isClassBased);
          templateStylingKey = collectStylingFromTAttrs(templateStylingKey, tNode.attrs, isClassBased);
          setTemplateHeadTStylingKey(tData, tNode, isClassBased, templateStylingKey);
        }
      } else {
        residual = collectResidual(tData, tNode, isClassBased);
      }
    }
  }
  if (residual !== void 0) {
    isClassBased ? tNode.residualClasses = residual : tNode.residualStyles = residual;
  }
  return stylingKey;
}
function getTemplateHeadTStylingKey(tData, tNode, isClassBased) {
  const bindings = isClassBased ? tNode.classBindings : tNode.styleBindings;
  if (getTStylingRangeNext(bindings) === 0) {
    return void 0;
  }
  return tData[getTStylingRangePrev(bindings)];
}
function setTemplateHeadTStylingKey(tData, tNode, isClassBased, tStylingKey) {
  const bindings = isClassBased ? tNode.classBindings : tNode.styleBindings;
  ngDevMode && assertNotEqual(getTStylingRangeNext(bindings), 0, "Expecting to have at least one template styling binding.");
  tData[getTStylingRangePrev(bindings)] = tStylingKey;
}
function collectResidual(tData, tNode, isClassBased) {
  let residual = void 0;
  const directiveEnd = tNode.directiveEnd;
  ngDevMode && assertNotEqual(tNode.directiveStylingLast, -1, "By the time this function gets called at least one hostBindings-node styling instruction must have executed.");
  for (let i = 1 + tNode.directiveStylingLast; i < directiveEnd; i++) {
    const attrs = tData[i].hostAttrs;
    residual = collectStylingFromTAttrs(residual, attrs, isClassBased);
  }
  return collectStylingFromTAttrs(residual, tNode.attrs, isClassBased);
}
function collectStylingFromDirectives(hostDirectiveDef, tData, tNode, stylingKey, isClassBased) {
  let currentDirective = null;
  const directiveEnd = tNode.directiveEnd;
  let directiveStylingLast = tNode.directiveStylingLast;
  if (directiveStylingLast === -1) {
    directiveStylingLast = tNode.directiveStart;
  } else {
    directiveStylingLast++;
  }
  while (directiveStylingLast < directiveEnd) {
    currentDirective = tData[directiveStylingLast];
    ngDevMode && assertDefined(currentDirective, "expected to be defined");
    stylingKey = collectStylingFromTAttrs(stylingKey, currentDirective.hostAttrs, isClassBased);
    if (currentDirective === hostDirectiveDef) break;
    directiveStylingLast++;
  }
  if (hostDirectiveDef !== null) {
    tNode.directiveStylingLast = directiveStylingLast;
  }
  return stylingKey;
}
function collectStylingFromTAttrs(stylingKey, attrs, isClassBased) {
  const desiredMarker = isClassBased ? 1 : 2;
  let currentMarker = -1;
  if (attrs !== null) {
    for (let i = 0; i < attrs.length; i++) {
      const item = attrs[i];
      if (typeof item === "number") {
        currentMarker = item;
      } else {
        if (currentMarker === desiredMarker) {
          if (!Array.isArray(stylingKey)) {
            stylingKey = stylingKey === void 0 ? [] : ["", stylingKey];
          }
          keyValueArraySet(stylingKey, item, isClassBased ? true : attrs[++i]);
        }
      }
    }
  }
  return stylingKey === void 0 ? null : stylingKey;
}
function toStylingKeyValueArray(keyValueArraySet2, stringParser, value) {
  if (value == null || value === "") return EMPTY_ARRAY;
  const styleKeyValueArray = [];
  const unwrappedValue = unwrapSafeValue(value);
  if (Array.isArray(unwrappedValue)) {
    for (let i = 0; i < unwrappedValue.length; i++) {
      keyValueArraySet2(styleKeyValueArray, unwrappedValue[i], true);
    }
  } else if (unwrappedValue instanceof Set) {
    for (const current of unwrappedValue) {
      keyValueArraySet2(styleKeyValueArray, current, true);
    }
  } else if (typeof unwrappedValue === "object") {
    for (const key in unwrappedValue) {
      if (unwrappedValue.hasOwnProperty(key)) {
        keyValueArraySet2(styleKeyValueArray, key, unwrappedValue[key]);
      }
    }
  } else if (typeof unwrappedValue === "string") {
    stringParser(styleKeyValueArray, unwrappedValue);
  } else {
    ngDevMode && throwError2("Unsupported styling type: " + typeof unwrappedValue + " (" + unwrappedValue + ")");
  }
  return styleKeyValueArray;
}
function styleKeyValueArraySet(keyValueArray, key, value) {
  keyValueArraySet(keyValueArray, key, unwrapSafeValue(value));
}
function classKeyValueArraySet(keyValueArray, key, value) {
  const stringKey = String(key);
  if (stringKey !== "" && !stringKey.includes(" ")) {
    keyValueArraySet(keyValueArray, stringKey, value);
  }
}
function updateStylingMap(tView, tNode, lView, renderer, oldKeyValueArray, newKeyValueArray, isClassBased, bindingIndex) {
  if (oldKeyValueArray === NO_CHANGE) {
    oldKeyValueArray = EMPTY_ARRAY;
  }
  let oldIndex = 0;
  let newIndex = 0;
  let oldKey = 0 < oldKeyValueArray.length ? oldKeyValueArray[0] : null;
  let newKey = 0 < newKeyValueArray.length ? newKeyValueArray[0] : null;
  while (oldKey !== null || newKey !== null) {
    ngDevMode && assertLessThan(oldIndex, 999, "Are we stuck in infinite loop?");
    ngDevMode && assertLessThan(newIndex, 999, "Are we stuck in infinite loop?");
    const oldValue = oldIndex < oldKeyValueArray.length ? oldKeyValueArray[oldIndex + 1] : void 0;
    const newValue = newIndex < newKeyValueArray.length ? newKeyValueArray[newIndex + 1] : void 0;
    let setKey = null;
    let setValue = void 0;
    if (oldKey === newKey) {
      oldIndex += 2;
      newIndex += 2;
      if (oldValue !== newValue) {
        setKey = newKey;
        setValue = newValue;
      }
    } else if (newKey === null || oldKey !== null && oldKey < newKey) {
      oldIndex += 2;
      setKey = oldKey;
    } else {
      ngDevMode && assertDefined(newKey, "Expecting to have a valid key");
      newIndex += 2;
      setKey = newKey;
      setValue = newValue;
    }
    if (setKey !== null) {
      updateStyling(tView, tNode, lView, renderer, setKey, setValue, isClassBased, bindingIndex);
    }
    oldKey = oldIndex < oldKeyValueArray.length ? oldKeyValueArray[oldIndex] : null;
    newKey = newIndex < newKeyValueArray.length ? newKeyValueArray[newIndex] : null;
  }
}
function updateStyling(tView, tNode, lView, renderer, prop, value, isClassBased, bindingIndex) {
  if (!(tNode.type & 3)) {
    return;
  }
  const tData = tView.data;
  const tRange = tData[bindingIndex + 1];
  const higherPriorityValue = getTStylingRangeNextDuplicate(tRange) ? findStylingValue(tData, tNode, lView, prop, getTStylingRangeNext(tRange), isClassBased) : void 0;
  if (!isStylingValuePresent(higherPriorityValue)) {
    if (!isStylingValuePresent(value)) {
      if (getTStylingRangePrevDuplicate(tRange)) {
        value = findStylingValue(tData, null, lView, prop, bindingIndex, isClassBased);
      }
    }
    const rNode = getNativeByIndex(getSelectedIndex(), lView);
    applyStyling(renderer, isClassBased, rNode, prop, value);
  }
}
function findStylingValue(tData, tNode, lView, prop, index, isClassBased) {
  const isPrevDirection = tNode === null;
  let value = void 0;
  while (index > 0) {
    const rawKey = tData[index];
    const containsStatics = Array.isArray(rawKey);
    const key = containsStatics ? rawKey[1] : rawKey;
    const isStylingMap = key === null;
    let valueAtLViewIndex = lView[index + 1];
    if (valueAtLViewIndex === NO_CHANGE) {
      valueAtLViewIndex = isStylingMap ? EMPTY_ARRAY : void 0;
    }
    let currentValue = isStylingMap ? keyValueArrayGet(valueAtLViewIndex, prop) : key === prop ? valueAtLViewIndex : void 0;
    if (containsStatics && !isStylingValuePresent(currentValue)) {
      currentValue = keyValueArrayGet(rawKey, prop);
    }
    if (isStylingValuePresent(currentValue)) {
      value = currentValue;
      if (isPrevDirection) {
        return value;
      }
    }
    const tRange = tData[index + 1];
    index = isPrevDirection ? getTStylingRangePrev(tRange) : getTStylingRangeNext(tRange);
  }
  if (tNode !== null) {
    let residual = isClassBased ? tNode.residualClasses : tNode.residualStyles;
    if (residual != null) {
      value = keyValueArrayGet(residual, prop);
    }
  }
  return value;
}
function isStylingValuePresent(value) {
  return value !== void 0;
}
function normalizeSuffix(value, suffix) {
  if (value == null || value === "") ;
  else if (typeof suffix === "string") {
    value = value + suffix;
  } else if (typeof value === "object") {
    value = stringify(unwrapSafeValue(value));
  }
  return value;
}
function hasStylingInputShadow(tNode, isClassBased) {
  return (tNode.flags & (isClassBased ? 8 : 16)) !== 0;
}
function \u0275\u0275text(index, value = "") {
  const lView = getLView();
  const tView = getTView();
  const adjustedIndex = index + HEADER_OFFSET;
  ngDevMode && assertTNodeCreationIndex(lView, index);
  const tNode = tView.firstCreatePass ? getOrCreateTNode(tView, adjustedIndex, 1, value, null) : tView.data[adjustedIndex];
  const textNative = _locateOrCreateTextNode(tView, lView, tNode, value);
  lView[adjustedIndex] = textNative;
  if (wasLastNodeCreated()) {
    appendChild(tView, lView, textNative, tNode);
  }
  setCurrentTNode(tNode, false);
}
var _locateOrCreateTextNode = (tView, lView, tNode, value) => {
  lastNodeWasCreated(true);
  return createTextNode(lView[RENDERER], value);
};
function interpolationV(lView, values) {
  ngDevMode && assertLessThan(2, values.length, "should have at least 3 values");
  let isBindingUpdated = false;
  let bindingIndex = getBindingIndex();
  for (let i = 1; i < values.length; i += 2) {
    isBindingUpdated = bindingUpdated(lView, bindingIndex++, values[i]) || isBindingUpdated;
  }
  setBindingIndex(bindingIndex);
  if (!isBindingUpdated) {
    return NO_CHANGE;
  }
  let content = values[0];
  for (let i = 1; i < values.length; i += 2) {
    content += renderStringify(values[i]) + (i + 1 !== values.length ? values[i + 1] : "");
  }
  return content;
}
function interpolation1(lView, prefix, v0, suffix = "") {
  const different = bindingUpdated(lView, nextBindingIndex(), v0);
  return different ? prefix + renderStringify(v0) + suffix : NO_CHANGE;
}
function interpolation2(lView, prefix, v0, i0, v1, suffix = "") {
  const bindingIndex = getBindingIndex();
  const different = bindingUpdated2(lView, bindingIndex, v0, v1);
  incrementBindingIndex(2);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + suffix : NO_CHANGE;
}
function interpolation3(lView, prefix, v0, i0, v1, i1, v2, suffix = "") {
  const bindingIndex = getBindingIndex();
  const different = bindingUpdated3(lView, bindingIndex, v0, v1, v2);
  incrementBindingIndex(3);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + suffix : NO_CHANGE;
}
function interpolation4(lView, prefix, v0, i0, v1, i1, v2, i2, v3, suffix = "") {
  const bindingIndex = getBindingIndex();
  const different = bindingUpdated4(lView, bindingIndex, v0, v1, v2, v3);
  incrementBindingIndex(4);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + i2 + renderStringify(v3) + suffix : NO_CHANGE;
}
function interpolation5(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, suffix = "") {
  const bindingIndex = getBindingIndex();
  let different = bindingUpdated4(lView, bindingIndex, v0, v1, v2, v3);
  different = bindingUpdated(lView, bindingIndex + 4, v4) || different;
  incrementBindingIndex(5);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + i2 + renderStringify(v3) + i3 + renderStringify(v4) + suffix : NO_CHANGE;
}
function interpolation6(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, suffix = "") {
  const bindingIndex = getBindingIndex();
  let different = bindingUpdated4(lView, bindingIndex, v0, v1, v2, v3);
  different = bindingUpdated2(lView, bindingIndex + 4, v4, v5) || different;
  incrementBindingIndex(6);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + i2 + renderStringify(v3) + i3 + renderStringify(v4) + i4 + renderStringify(v5) + suffix : NO_CHANGE;
}
function interpolation7(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, suffix = "") {
  const bindingIndex = getBindingIndex();
  let different = bindingUpdated4(lView, bindingIndex, v0, v1, v2, v3);
  different = bindingUpdated3(lView, bindingIndex + 4, v4, v5, v6) || different;
  incrementBindingIndex(7);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + i2 + renderStringify(v3) + i3 + renderStringify(v4) + i4 + renderStringify(v5) + i5 + renderStringify(v6) + suffix : NO_CHANGE;
}
function interpolation8(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, i6, v7, suffix = "") {
  const bindingIndex = getBindingIndex();
  let different = bindingUpdated4(lView, bindingIndex, v0, v1, v2, v3);
  different = bindingUpdated4(lView, bindingIndex + 4, v4, v5, v6, v7) || different;
  incrementBindingIndex(8);
  return different ? prefix + renderStringify(v0) + i0 + renderStringify(v1) + i1 + renderStringify(v2) + i2 + renderStringify(v3) + i3 + renderStringify(v4) + i4 + renderStringify(v5) + i5 + renderStringify(v6) + i6 + renderStringify(v7) + suffix : NO_CHANGE;
}
function \u0275\u0275textInterpolate(v0) {
  \u0275\u0275textInterpolate1("", v0);
  return \u0275\u0275textInterpolate;
}
function \u0275\u0275textInterpolate1(prefix, v0, suffix) {
  const lView = getLView();
  const interpolated = interpolation1(lView, prefix, v0, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate1;
}
function \u0275\u0275textInterpolate2(prefix, v0, i0, v1, suffix) {
  const lView = getLView();
  const interpolated = interpolation2(lView, prefix, v0, i0, v1, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate2;
}
function \u0275\u0275textInterpolate3(prefix, v0, i0, v1, i1, v2, suffix) {
  const lView = getLView();
  const interpolated = interpolation3(lView, prefix, v0, i0, v1, i1, v2, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate3;
}
function \u0275\u0275textInterpolate4(prefix, v0, i0, v1, i1, v2, i2, v3, suffix) {
  const lView = getLView();
  const interpolated = interpolation4(lView, prefix, v0, i0, v1, i1, v2, i2, v3, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate4;
}
function \u0275\u0275textInterpolate5(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, suffix) {
  const lView = getLView();
  const interpolated = interpolation5(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate5;
}
function \u0275\u0275textInterpolate6(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, suffix) {
  const lView = getLView();
  const interpolated = interpolation6(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate6;
}
function \u0275\u0275textInterpolate7(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, suffix) {
  const lView = getLView();
  const interpolated = interpolation7(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate7;
}
function \u0275\u0275textInterpolate8(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, i6, v7, suffix) {
  const lView = getLView();
  const interpolated = interpolation8(lView, prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, i6, v7, suffix);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolate8;
}
function \u0275\u0275textInterpolateV(values) {
  const lView = getLView();
  const interpolated = interpolationV(lView, values);
  if (interpolated !== NO_CHANGE) {
    textBindingInternal(lView, getSelectedIndex(), interpolated);
  }
  return \u0275\u0275textInterpolateV;
}
function textBindingInternal(lView, index, value) {
  ngDevMode && assertString(value, "Value should be a string");
  ngDevMode && assertNotSame(value, NO_CHANGE, "value should not be NO_CHANGE");
  ngDevMode && assertIndexInRange(lView, index);
  const element = getNativeByIndex(index, lView);
  ngDevMode && assertDefined(element, "native element should exist");
  updateTextNode(lView[RENDERER], element, value);
}
function \u0275\u0275twoWayProperty(propName, value, sanitizer) {
  if (isWritableSignal(value)) {
    value = value();
  }
  const lView = getLView();
  const bindingIndex = nextBindingIndex();
  if (bindingUpdated(lView, bindingIndex, value)) {
    const tView = getTView();
    const tNode = getSelectedTNode();
    setPropertyAndInputs(tNode, lView, propName, value, lView[RENDERER], sanitizer);
    ngDevMode && storePropertyBindingMetadata(tView.data, tNode, propName, bindingIndex);
  }
  return \u0275\u0275twoWayProperty;
}
function \u0275\u0275twoWayBindingSet(target, value) {
  const canWrite = isWritableSignal(target);
  canWrite && target.set(value);
  return canWrite;
}
function \u0275\u0275twoWayListener(eventName, listenerFn) {
  const lView = getLView();
  const tView = getTView();
  const tNode = getCurrentTNode();
  listenerInternal(tView, lView, lView[RENDERER], tNode, eventName, listenerFn);
  return \u0275\u0275twoWayListener;
}
var UNINITIALIZED_LET = {};
function \u0275\u0275declareLet(index) {
  performanceMarkFeature("NgLet");
  const tView = getTView();
  const lView = getLView();
  const adjustedIndex = index + HEADER_OFFSET;
  const tNode = getOrCreateTNode(tView, adjustedIndex, 128, null, null);
  setCurrentTNode(tNode, false);
  store(tView, lView, adjustedIndex, UNINITIALIZED_LET);
  return \u0275\u0275declareLet;
}
function \u0275\u0275storeLet(value) {
  const tView = getTView();
  const lView = getLView();
  const index = getSelectedIndex();
  store(tView, lView, index, value);
  return value;
}
function \u0275\u0275readContextLet(index) {
  const contextLView = getContextLView();
  const value = load(contextLView, HEADER_OFFSET + index);
  if (value === UNINITIALIZED_LET) {
    throw new RuntimeError(314, ngDevMode && "Attempting to access a @let declaration whose value is not available yet");
  }
  return value;
}
function \u0275\u0275attachSourceLocations(templatePath, locations) {
  const tView = getTView();
  const lView = getLView();
  const renderer = lView[RENDERER];
  const attributeName = "data-ng-source-location";
  for (const [index, offset, line, column] of locations) {
    const tNode = getTNode(tView, index + HEADER_OFFSET);
    ngDevMode && assertTNodeType(tNode, 2);
    const node = getNativeByIndex(index + HEADER_OFFSET, lView);
    if (!node.hasAttribute(attributeName)) {
      const attributeValue = `${templatePath}@o:${offset},l:${line},c:${column}`;
      renderer.setAttribute(node, attributeName, attributeValue);
    }
  }
}
function \u0275\u0275interpolate(v0) {
  return bindingUpdated(getLView(), nextBindingIndex(), v0) ? renderStringify(v0) : NO_CHANGE;
}
function \u0275\u0275interpolate1(prefix, v0, suffix = "") {
  return interpolation1(getLView(), prefix, v0, suffix);
}
function \u0275\u0275interpolate2(prefix, v0, i0, v1, suffix = "") {
  return interpolation2(getLView(), prefix, v0, i0, v1, suffix);
}
function \u0275\u0275interpolate3(prefix, v0, i0, v1, i1, v2, suffix = "") {
  return interpolation3(getLView(), prefix, v0, i0, v1, i1, v2, suffix);
}
function \u0275\u0275interpolate4(prefix, v0, i0, v1, i1, v2, i2, v3, suffix = "") {
  return interpolation4(getLView(), prefix, v0, i0, v1, i1, v2, i2, v3, suffix);
}
function \u0275\u0275interpolate5(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, suffix = "") {
  return interpolation5(getLView(), prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, suffix);
}
function \u0275\u0275interpolate6(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, suffix = "") {
  return interpolation6(getLView(), prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, suffix);
}
function \u0275\u0275interpolate7(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, suffix = "") {
  return interpolation7(getLView(), prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, suffix);
}
function \u0275\u0275interpolate8(prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, i6, v7, suffix = "") {
  return interpolation8(getLView(), prefix, v0, i0, v1, i1, v2, i2, v3, i3, v4, i4, v5, i5, v6, i6, v7, suffix);
}
function \u0275\u0275interpolateV(values) {
  return interpolationV(getLView(), values);
}
function \u0275\u0275arrowFunction(slotOffset, factory, context2) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  return lView[bindingIndex] === NO_CHANGE ? updateBinding(lView, bindingIndex, factory(context2, lView)) : getBinding(lView, bindingIndex);
}
function providersResolver(def, providers, isViewProviders) {
  const tView = getTView();
  if (tView.firstCreatePass) {
    resolveProvider(providers, tView.data, tView.blueprint, isComponentDef(def), isViewProviders);
  }
}
function resolveProvider(provider, tInjectables, lInjectablesBlueprint, isComponent2, isViewProvider) {
  provider = resolveForwardRef(provider);
  if (Array.isArray(provider)) {
    for (let i = 0; i < provider.length; i++) {
      resolveProvider(provider[i], tInjectables, lInjectablesBlueprint, isComponent2, isViewProvider);
    }
  } else {
    const tView = getTView();
    const lView = getLView();
    const tNode = getCurrentTNode();
    let token = isTypeProvider(provider) ? provider : resolveForwardRef(provider.provide);
    const providerFactory = providerToFactory(provider);
    if (ngDevMode) {
      const injector = new NodeInjector(tNode, lView);
      runInInjectorProfilerContext(injector, token, () => {
        emitProviderConfiguredEvent(provider, isViewProvider);
      });
    }
    const beginIndex = tNode.providerIndexes & 1048575;
    const endIndex = tNode.directiveStart;
    const cptViewProvidersCount = tNode.providerIndexes >> 20;
    if (isTypeProvider(provider) || !provider.multi) {
      const factory = new NodeInjectorFactory(providerFactory, isViewProvider, \u0275\u0275directiveInject, ngDevMode ? providerName(provider) : null);
      const existingFactoryIndex = indexOf(token, tInjectables, isViewProvider ? beginIndex : beginIndex + cptViewProvidersCount, endIndex);
      if (existingFactoryIndex === -1) {
        diPublicInInjector(getOrCreateNodeInjectorForNode(tNode, lView), tView, token);
        registerDestroyHooksIfSupported(tView, provider, tInjectables.length);
        tInjectables.push(token);
        tNode.directiveStart++;
        tNode.directiveEnd++;
        if (isViewProvider) {
          tNode.providerIndexes += 1048576;
        }
        lInjectablesBlueprint.push(factory);
        lView.push(factory);
      } else {
        lInjectablesBlueprint[existingFactoryIndex] = factory;
        lView[existingFactoryIndex] = factory;
      }
    } else {
      const existingProvidersFactoryIndex = indexOf(token, tInjectables, beginIndex + cptViewProvidersCount, endIndex);
      const existingViewProvidersFactoryIndex = indexOf(token, tInjectables, beginIndex, beginIndex + cptViewProvidersCount);
      const doesProvidersFactoryExist = existingProvidersFactoryIndex >= 0 && lInjectablesBlueprint[existingProvidersFactoryIndex];
      const doesViewProvidersFactoryExist = existingViewProvidersFactoryIndex >= 0 && lInjectablesBlueprint[existingViewProvidersFactoryIndex];
      if (isViewProvider && !doesViewProvidersFactoryExist || !isViewProvider && !doesProvidersFactoryExist) {
        diPublicInInjector(getOrCreateNodeInjectorForNode(tNode, lView), tView, token);
        const factory = multiFactory(isViewProvider ? multiViewProvidersFactoryResolver : multiProvidersFactoryResolver, lInjectablesBlueprint.length, isViewProvider, isComponent2, providerFactory, provider);
        if (!isViewProvider && doesViewProvidersFactoryExist) {
          lInjectablesBlueprint[existingViewProvidersFactoryIndex].providerFactory = factory;
        }
        registerDestroyHooksIfSupported(tView, provider, tInjectables.length, 0);
        tInjectables.push(token);
        tNode.directiveStart++;
        tNode.directiveEnd++;
        if (isViewProvider) {
          tNode.providerIndexes += 1048576;
        }
        lInjectablesBlueprint.push(factory);
        lView.push(factory);
      } else {
        const indexInFactory = multiFactoryAdd(lInjectablesBlueprint[isViewProvider ? existingViewProvidersFactoryIndex : existingProvidersFactoryIndex], providerFactory, !isViewProvider && isComponent2);
        registerDestroyHooksIfSupported(tView, provider, existingProvidersFactoryIndex > -1 ? existingProvidersFactoryIndex : existingViewProvidersFactoryIndex, indexInFactory);
      }
      if (!isViewProvider && isComponent2 && doesViewProvidersFactoryExist) {
        lInjectablesBlueprint[existingViewProvidersFactoryIndex].componentProviders++;
      }
    }
  }
}
function registerDestroyHooksIfSupported(tView, provider, contextIndex, indexInFactory) {
  const providerIsTypeProvider = isTypeProvider(provider);
  const providerIsClassProvider = isClassProvider(provider);
  if (providerIsTypeProvider || providerIsClassProvider) {
    const classToken = providerIsClassProvider ? resolveForwardRef(provider.useClass) : provider;
    const prototype = classToken.prototype;
    const ngOnDestroy = prototype.ngOnDestroy;
    if (ngOnDestroy) {
      const hooks = tView.destroyHooks || (tView.destroyHooks = []);
      if (!providerIsTypeProvider && provider.multi) {
        ngDevMode && assertDefined(indexInFactory, "indexInFactory when registering multi factory destroy hook");
        const existingCallbacksIndex = hooks.indexOf(contextIndex);
        if (existingCallbacksIndex === -1) {
          hooks.push(contextIndex, [indexInFactory, ngOnDestroy]);
        } else {
          hooks[existingCallbacksIndex + 1].push(indexInFactory, ngOnDestroy);
        }
      } else {
        hooks.push(contextIndex, ngOnDestroy);
      }
    }
  }
}
function multiFactoryAdd(multiFactory2, factory, isComponentProvider) {
  if (isComponentProvider) {
    multiFactory2.componentProviders++;
  }
  return multiFactory2.multi.push(factory) - 1;
}
function indexOf(item, arr, begin, end) {
  for (let i = begin; i < end; i++) {
    if (arr[i] === item) return i;
  }
  return -1;
}
function multiProvidersFactoryResolver(_, flags, tData, lData, tNode) {
  return multiResolve(this.multi, []);
}
function multiViewProvidersFactoryResolver(_, _flags, _tData, lView, tNode) {
  const factories = this.multi;
  let result;
  if (this.providerFactory) {
    const componentCount = this.providerFactory.componentProviders;
    const multiProviders = getNodeInjectable(lView, lView[TVIEW], this.providerFactory.index, tNode);
    result = multiProviders.slice(0, componentCount);
    multiResolve(factories, result);
    for (let i = componentCount; i < multiProviders.length; i++) {
      result.push(multiProviders[i]);
    }
  } else {
    result = [];
    multiResolve(factories, result);
  }
  return result;
}
function multiResolve(factories, result) {
  for (let i = 0; i < factories.length; i++) {
    const factory = factories[i];
    result.push(factory());
  }
  return result;
}
function multiFactory(factoryFn, index, isViewProvider, isComponent2, f, provider) {
  const factory = new NodeInjectorFactory(factoryFn, isViewProvider, \u0275\u0275directiveInject, ngDevMode ? providerName(provider) : null);
  factory.multi = [];
  factory.index = index;
  factory.componentProviders = 0;
  multiFactoryAdd(factory, f, isComponent2 && !isViewProvider);
  return factory;
}
function providerName(provider) {
  if (Array.isArray(provider)) {
    return null;
  }
  if (isTypeProvider(provider)) {
    return provider.name;
  } else if (isClassProvider(provider)) {
    if (provider.provide instanceof InjectionToken) {
      return `('${provider.provide.toString()}':${provider.useClass.name})`;
    }
    return provider.useClass.name;
  } else if (provider.provide instanceof InjectionToken) {
    return provider.provide.toString();
  } else if (typeof provider.provide === "string") {
    return provider.provide;
  } else {
    return null;
  }
}
function \u0275\u0275ProvidersFeature(providers, viewProviders) {
  return (definition) => {
    definition.providersResolver = (def, processProvidersFn) => providersResolver(def, processProvidersFn ? processProvidersFn(providers) : providers, false);
    if (viewProviders) {
      definition.viewProvidersResolver = (def, processProvidersFn) => providersResolver(def, processProvidersFn ? processProvidersFn(viewProviders) : viewProviders, true);
    }
  };
}
function \u0275\u0275ExternalStylesFeature(styleUrls) {
  return (definition) => {
    if (styleUrls.length < 1) {
      return;
    }
    definition.getExternalStyles = (encapsulationId) => {
      const urls = styleUrls.map((value) => value + "?ngcomp" + (encapsulationId ? "=" + encodeURIComponent(encapsulationId) : "") + "&e=" + definition.encapsulation);
      return urls;
    };
  };
}
function \u0275\u0275setComponentScope(type, directives, pipes) {
  const def = type.\u0275cmp;
  def.directiveDefs = extractDefListOrFactory(directives, extractDirectiveDef);
  def.pipeDefs = extractDefListOrFactory(pipes, getPipeDef);
}
function \u0275\u0275setNgModuleScope(type, scope) {
  return noSideEffects(() => {
    const ngModuleDef = getNgModuleDefOrThrow(type);
    ngModuleDef.declarations = convertToTypeArray(scope.declarations || EMPTY_ARRAY);
    ngModuleDef.imports = convertToTypeArray(scope.imports || EMPTY_ARRAY);
    ngModuleDef.exports = convertToTypeArray(scope.exports || EMPTY_ARRAY);
    if (scope.bootstrap) {
      ngModuleDef.bootstrap = convertToTypeArray(scope.bootstrap);
    }
    depsTracker.registerNgModule(type, scope);
  });
}
function convertToTypeArray(values) {
  if (typeof values === "function") {
    return values;
  }
  const flattenValues = flatten(values);
  if (flattenValues.some(isForwardRef)) {
    return () => flattenValues.map(resolveForwardRef).map(maybeUnwrapModuleWithProviders);
  } else {
    return flattenValues.map(maybeUnwrapModuleWithProviders);
  }
}
function maybeUnwrapModuleWithProviders(value) {
  return isModuleWithProviders(value) ? value.ngModule : value;
}
function \u0275\u0275pureFunction0(slotOffset, pureFn) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  return lView[bindingIndex] === NO_CHANGE ? updateBinding(lView, bindingIndex, pureFn()) : getBinding(lView, bindingIndex);
}
function \u0275\u0275pureFunction1(slotOffset, pureFn, exp) {
  return pureFunction1Internal(getLView(), getBindingRoot(), slotOffset, pureFn, exp);
}
function \u0275\u0275pureFunction2(slotOffset, pureFn, exp1, exp2) {
  return pureFunction2Internal(getLView(), getBindingRoot(), slotOffset, pureFn, exp1, exp2);
}
function \u0275\u0275pureFunction3(slotOffset, pureFn, exp1, exp2, exp3) {
  return pureFunction3Internal(getLView(), getBindingRoot(), slotOffset, pureFn, exp1, exp2, exp3);
}
function \u0275\u0275pureFunction4(slotOffset, pureFn, exp1, exp2, exp3, exp4, thisArg) {
  return pureFunction4Internal(getLView(), getBindingRoot(), slotOffset, pureFn, exp1, exp2, exp3, exp4);
}
function \u0275\u0275pureFunction5(slotOffset, pureFn, exp1, exp2, exp3, exp4, exp5) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  const different = bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4);
  return bindingUpdated(lView, bindingIndex + 4, exp5) || different ? updateBinding(lView, bindingIndex + 5, pureFn(exp1, exp2, exp3, exp4, exp5)) : getBinding(lView, bindingIndex + 5);
}
function \u0275\u0275pureFunction6(slotOffset, pureFn, exp1, exp2, exp3, exp4, exp5, exp6) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  const different = bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4);
  return bindingUpdated2(lView, bindingIndex + 4, exp5, exp6) || different ? updateBinding(lView, bindingIndex + 6, pureFn(exp1, exp2, exp3, exp4, exp5, exp6)) : getBinding(lView, bindingIndex + 6);
}
function \u0275\u0275pureFunction7(slotOffset, pureFn, exp1, exp2, exp3, exp4, exp5, exp6, exp7) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  let different = bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4);
  return bindingUpdated3(lView, bindingIndex + 4, exp5, exp6, exp7) || different ? updateBinding(lView, bindingIndex + 7, pureFn(exp1, exp2, exp3, exp4, exp5, exp6, exp7)) : getBinding(lView, bindingIndex + 7);
}
function \u0275\u0275pureFunction8(slotOffset, pureFn, exp1, exp2, exp3, exp4, exp5, exp6, exp7, exp8) {
  const bindingIndex = getBindingRoot() + slotOffset;
  const lView = getLView();
  const different = bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4);
  return bindingUpdated4(lView, bindingIndex + 4, exp5, exp6, exp7, exp8) || different ? updateBinding(lView, bindingIndex + 8, pureFn(exp1, exp2, exp3, exp4, exp5, exp6, exp7, exp8)) : getBinding(lView, bindingIndex + 8);
}
function \u0275\u0275pureFunctionV(slotOffset, pureFn, exps) {
  return pureFunctionVInternal(getLView(), getBindingRoot(), slotOffset, pureFn, exps);
}
function getPureFunctionReturnValue(lView, returnValueIndex) {
  ngDevMode && assertIndexInRange(lView, returnValueIndex);
  const lastReturnValue = lView[returnValueIndex];
  return lastReturnValue === NO_CHANGE ? void 0 : lastReturnValue;
}
function pureFunction1Internal(lView, bindingRoot, slotOffset, pureFn, exp, thisArg) {
  const bindingIndex = bindingRoot + slotOffset;
  return bindingUpdated(lView, bindingIndex, exp) ? updateBinding(lView, bindingIndex + 1, thisArg ? pureFn.call(thisArg, exp) : pureFn(exp)) : getPureFunctionReturnValue(lView, bindingIndex + 1);
}
function pureFunction2Internal(lView, bindingRoot, slotOffset, pureFn, exp1, exp2, thisArg) {
  const bindingIndex = bindingRoot + slotOffset;
  return bindingUpdated2(lView, bindingIndex, exp1, exp2) ? updateBinding(lView, bindingIndex + 2, thisArg ? pureFn.call(thisArg, exp1, exp2) : pureFn(exp1, exp2)) : getPureFunctionReturnValue(lView, bindingIndex + 2);
}
function pureFunction3Internal(lView, bindingRoot, slotOffset, pureFn, exp1, exp2, exp3, thisArg) {
  const bindingIndex = bindingRoot + slotOffset;
  return bindingUpdated3(lView, bindingIndex, exp1, exp2, exp3) ? updateBinding(lView, bindingIndex + 3, thisArg ? pureFn.call(thisArg, exp1, exp2, exp3) : pureFn(exp1, exp2, exp3)) : getPureFunctionReturnValue(lView, bindingIndex + 3);
}
function pureFunction4Internal(lView, bindingRoot, slotOffset, pureFn, exp1, exp2, exp3, exp4, thisArg) {
  const bindingIndex = bindingRoot + slotOffset;
  return bindingUpdated4(lView, bindingIndex, exp1, exp2, exp3, exp4) ? updateBinding(lView, bindingIndex + 4, thisArg ? pureFn.call(thisArg, exp1, exp2, exp3, exp4) : pureFn(exp1, exp2, exp3, exp4)) : getPureFunctionReturnValue(lView, bindingIndex + 4);
}
function pureFunctionVInternal(lView, bindingRoot, slotOffset, pureFn, exps, thisArg) {
  let bindingIndex = bindingRoot + slotOffset;
  let different = false;
  for (let i = 0; i < exps.length; i++) {
    bindingUpdated(lView, bindingIndex++, exps[i]) && (different = true);
  }
  return different ? updateBinding(lView, bindingIndex, pureFn.apply(thisArg, exps)) : getPureFunctionReturnValue(lView, bindingIndex);
}
function \u0275\u0275pipe(index, pipeName) {
  const tView = getTView();
  let pipeDef;
  const adjustedIndex = index + HEADER_OFFSET;
  if (tView.firstCreatePass) {
    pipeDef = getPipeDef2(pipeName, tView.pipeRegistry);
    tView.data[adjustedIndex] = pipeDef;
    if (pipeDef.onDestroy) {
      (tView.destroyHooks ??= []).push(adjustedIndex, pipeDef.onDestroy);
    }
  } else {
    pipeDef = tView.data[adjustedIndex];
  }
  const pipeFactory = pipeDef.factory || (pipeDef.factory = getFactoryDef(pipeDef.type, true));
  let previousInjectorProfilerContext;
  if (ngDevMode) {
    previousInjectorProfilerContext = setInjectorProfilerContext({
      injector: new NodeInjector(getCurrentTNode(), getLView()),
      token: pipeDef.type
    });
  }
  const previousInjectImplementation = setInjectImplementation(\u0275\u0275directiveInject);
  try {
    const previousIncludeViewProviders = setIncludeViewProviders(false);
    const pipeInstance = pipeFactory();
    setIncludeViewProviders(previousIncludeViewProviders);
    store(tView, getLView(), adjustedIndex, pipeInstance);
    return pipeInstance;
  } finally {
    setInjectImplementation(previousInjectImplementation);
    ngDevMode && setInjectorProfilerContext(previousInjectorProfilerContext);
  }
}
function getPipeDef2(name, registry) {
  if (registry) {
    if (ngDevMode) {
      const pipes = registry.filter((pipe2) => pipe2.name === name);
      if (pipes.length > 1) {
        console.warn(formatRuntimeError(313, getMultipleMatchingPipesMessage(name)));
      }
    }
    for (let i = registry.length - 1; i >= 0; i--) {
      const pipeDef = registry[i];
      if (name === pipeDef.name) {
        return pipeDef;
      }
    }
  }
  if (ngDevMode) {
    throw new RuntimeError(-302, getPipeNotFoundErrorMessage(name));
  }
  return;
}
function getMultipleMatchingPipesMessage(name) {
  const lView = getLView();
  const declarationLView = lView[DECLARATION_COMPONENT_VIEW];
  const context2 = declarationLView[CONTEXT];
  const hostIsStandalone = isHostComponentStandalone(lView);
  const componentInfoMessage = context2 ? ` in the '${context2.constructor.name}' component` : "";
  const verifyMessage = `check ${hostIsStandalone ? "'@Component.imports' of this component" : "the imports of this module"}`;
  const errorMessage = `Multiple pipes match the name \`${name}\`${componentInfoMessage}. ${verifyMessage}`;
  return errorMessage;
}
function getPipeNotFoundErrorMessage(name) {
  const lView = getLView();
  const declarationLView = lView[DECLARATION_COMPONENT_VIEW];
  const context2 = declarationLView[CONTEXT];
  const hostIsStandalone = isHostComponentStandalone(lView);
  const componentInfoMessage = context2 ? ` in the '${context2.constructor.name}' component` : "";
  const verifyMessage = `Verify that it is ${hostIsStandalone ? "included in the '@Component.imports' of this component" : "declared or imported in this module"}`;
  const errorMessage = `The pipe '${name}' could not be found${componentInfoMessage}. ${verifyMessage}`;
  return errorMessage;
}
function \u0275\u0275pipeBind1(index, offset, v1) {
  const adjustedIndex = index + HEADER_OFFSET;
  const lView = getLView();
  const pipeInstance = load(lView, adjustedIndex);
  return isPure(lView, adjustedIndex) ? pureFunction1Internal(lView, getBindingRoot(), offset, pipeInstance.transform, v1, pipeInstance) : pipeInstance.transform(v1);
}
function \u0275\u0275pipeBind2(index, slotOffset, v1, v2) {
  const adjustedIndex = index + HEADER_OFFSET;
  const lView = getLView();
  const pipeInstance = load(lView, adjustedIndex);
  return isPure(lView, adjustedIndex) ? pureFunction2Internal(lView, getBindingRoot(), slotOffset, pipeInstance.transform, v1, v2, pipeInstance) : pipeInstance.transform(v1, v2);
}
function \u0275\u0275pipeBind3(index, slotOffset, v1, v2, v3) {
  const adjustedIndex = index + HEADER_OFFSET;
  const lView = getLView();
  const pipeInstance = load(lView, adjustedIndex);
  return isPure(lView, adjustedIndex) ? pureFunction3Internal(lView, getBindingRoot(), slotOffset, pipeInstance.transform, v1, v2, v3, pipeInstance) : pipeInstance.transform(v1, v2, v3);
}
function \u0275\u0275pipeBind4(index, slotOffset, v1, v2, v3, v4) {
  const adjustedIndex = index + HEADER_OFFSET;
  const lView = getLView();
  const pipeInstance = load(lView, adjustedIndex);
  return isPure(lView, adjustedIndex) ? pureFunction4Internal(lView, getBindingRoot(), slotOffset, pipeInstance.transform, v1, v2, v3, v4, pipeInstance) : pipeInstance.transform(v1, v2, v3, v4);
}
function \u0275\u0275pipeBindV(index, slotOffset, values) {
  const adjustedIndex = index + HEADER_OFFSET;
  const lView = getLView();
  const pipeInstance = load(lView, adjustedIndex);
  return isPure(lView, adjustedIndex) ? pureFunctionVInternal(lView, getBindingRoot(), slotOffset, pipeInstance.transform, values, pipeInstance) : pipeInstance.transform.apply(pipeInstance, values);
}
function isPure(lView, index) {
  return lView[TVIEW].data[index].pure;
}
function \u0275\u0275templateRefExtractor(tNode, lView) {
  return createTemplateRef(tNode, lView);
}
function \u0275\u0275getComponentDepsFactory(type, rawImports) {
  return () => {
    try {
      return depsTracker.getComponentDependencies(type, rawImports).dependencies;
    } catch (e) {
      console.error(`Computing dependencies in local compilation mode for the component "${type.name}" failed with the exception:`, e);
      throw e;
    }
  };
}
function \u0275setClassDebugInfo(type, debugInfo) {
  const def = getComponentDef(type);
  if (def !== null) {
    def.debugInfo = debugInfo;
  }
}
function \u0275\u0275getReplaceMetadataURL(id, timestamp, base) {
  const url = `./@ng/component?c=${id}&t=${encodeURIComponent(timestamp)}`;
  return new URL(url, base).href;
}
function \u0275\u0275replaceMetadata(type, applyMetadata, namespaces, locals, importMeta = null, id = null) {
  ngDevMode && assertComponentDef(type);
  const currentDef = getComponentDef(type);
  applyMetadata.apply(null, [type, namespaces, ...locals]);
  const {
    newDef,
    oldDef
  } = mergeWithExistingDefinition(currentDef, getComponentDef(type));
  type[NG_COMP_DEF] = newDef;
  if (oldDef.tView) {
    const trackedViews = getTrackedLViews().values();
    for (const root of trackedViews) {
      if (isRootView(root) && root[PARENT] === null) {
        recreateMatchingLViews(importMeta, id, newDef, oldDef, root);
      }
    }
  }
}
function mergeWithExistingDefinition(currentDef, newDef) {
  const clone = __spreadValues({}, currentDef);
  const replacement = Object.assign(currentDef, newDef, {
    directiveDefs: clone.directiveDefs,
    pipeDefs: clone.pipeDefs,
    setInput: clone.setInput,
    type: clone.type
  });
  ngDevMode && assertEqual(replacement, currentDef, "Expected definition to be merged in place");
  return {
    newDef: replacement,
    oldDef: clone
  };
}
function recreateMatchingLViews(importMeta, id, newDef, oldDef, rootLView) {
  ngDevMode && assertDefined(oldDef.tView, "Expected a component definition that has been instantiated at least once");
  const tView = rootLView[TVIEW];
  if (tView === oldDef.tView) {
    ngDevMode && assertComponentDef(oldDef.type);
    recreateLView(importMeta, id, newDef, oldDef, rootLView);
    return;
  }
  for (let i = HEADER_OFFSET; i < tView.bindingStartIndex; i++) {
    const current = rootLView[i];
    if (isLContainer(current)) {
      if (isLView(current[HOST])) {
        recreateMatchingLViews(importMeta, id, newDef, oldDef, current[HOST]);
      }
      for (let j = CONTAINER_HEADER_OFFSET; j < current.length; j++) {
        recreateMatchingLViews(importMeta, id, newDef, oldDef, current[j]);
      }
    } else if (isLView(current)) {
      recreateMatchingLViews(importMeta, id, newDef, oldDef, current);
    }
  }
}
function clearRendererCache(factory, def) {
  factory.componentReplaced?.(def.id);
}
function recreateLView(importMeta, id, newDef, oldDef, lView) {
  const instance = lView[CONTEXT];
  let host = lView[HOST];
  const parentLView = lView[PARENT];
  ngDevMode && assertLView(parentLView);
  const tNode = lView[T_HOST];
  ngDevMode && assertTNodeType(tNode, 2);
  ngDevMode && assertNotEqual(newDef, oldDef, "Expected different component definition");
  const zone = lView[INJECTOR].get(NgZone, null);
  const recreate = () => {
    if (oldDef.encapsulation === ViewEncapsulation.ShadowDom || oldDef.encapsulation === ViewEncapsulation.ExperimentalIsolatedShadowDom) {
      const newHost = host.cloneNode(false);
      host.replaceWith(newHost);
      host = newHost;
    }
    const newTView = getOrCreateComponentTView(newDef);
    const newLView = createLView(parentLView, newTView, instance, getInitialLViewFlagsFromDef(newDef), host, tNode, null, null, null, null, null);
    replaceLViewInTree(parentLView, lView, newLView, tNode.index);
    destroyLView(lView[TVIEW], lView);
    cleanupLView(lView);
    const rendererFactory = lView[ENVIRONMENT].rendererFactory;
    clearRendererCache(rendererFactory, oldDef);
    newLView[RENDERER] = rendererFactory.createRenderer(host, newDef);
    removeViewFromDOM(lView[TVIEW], lView);
    resetProjectionState(tNode);
    renderView(newTView, newLView, instance);
    refreshView(newTView, newLView, newTView.template, instance);
  };
  if (zone === null) {
    executeWithInvalidateFallback(importMeta, id, recreate);
  } else {
    zone.run(() => executeWithInvalidateFallback(importMeta, id, recreate));
  }
}
function executeWithInvalidateFallback(importMeta, id, callback) {
  try {
    callback();
  } catch (e) {
    const error = e;
    if (id !== null && error.message) {
      const toLog = error.message + (error.stack ? "\n" + error.stack : "");
      importMeta?.hot?.send?.("angular:invalidate", {
        id,
        message: toLog,
        error: true
      });
    }
    throw e;
  }
}
function replaceLViewInTree(parentLView, oldLView, newLView, index) {
  for (let i = HEADER_OFFSET; i < parentLView[TVIEW].bindingStartIndex; i++) {
    const current = parentLView[i];
    if ((isLView(current) || isLContainer(current)) && current[NEXT] === oldLView) {
      current[NEXT] = newLView;
      break;
    }
  }
  if (parentLView[CHILD_HEAD] === oldLView) {
    parentLView[CHILD_HEAD] = newLView;
  }
  if (parentLView[CHILD_TAIL] === oldLView) {
    parentLView[CHILD_TAIL] = newLView;
  }
  newLView[NEXT] = oldLView[NEXT];
  oldLView[NEXT] = null;
  parentLView[index] = newLView;
}
function resetProjectionState(tNode) {
  if (tNode.projection !== null) {
    for (const current of tNode.projection) {
      if (isTNodeShape(current)) {
        current.projectionNext = null;
        current.flags &= -3;
      }
    }
    tNode.projection = null;
  }
}
var angularCoreEnv = /* @__PURE__ */ (() => ({
  "\u0275\u0275animateEnter": \u0275\u0275animateEnter,
  "\u0275\u0275animateEnterListener": \u0275\u0275animateEnterListener,
  "\u0275\u0275animateLeave": \u0275\u0275animateLeave,
  "\u0275\u0275animateLeaveListener": \u0275\u0275animateLeaveListener,
  "\u0275\u0275attribute": \u0275\u0275attribute,
  "\u0275\u0275defineComponent": \u0275\u0275defineComponent,
  "\u0275\u0275defineDirective": \u0275\u0275defineDirective,
  "\u0275\u0275defineInjectable": \u0275\u0275defineInjectable,
  "\u0275\u0275defineInjector": \u0275\u0275defineInjector,
  "\u0275\u0275defineNgModule": \u0275\u0275defineNgModule,
  "\u0275\u0275definePipe": \u0275\u0275definePipe,
  "\u0275\u0275directiveInject": \u0275\u0275directiveInject,
  "\u0275\u0275getInheritedFactory": \u0275\u0275getInheritedFactory,
  "\u0275\u0275inject": \u0275\u0275inject,
  "\u0275\u0275injectAttribute": \u0275\u0275injectAttribute,
  "\u0275\u0275invalidFactory": \u0275\u0275invalidFactory,
  "\u0275\u0275invalidFactoryDep": \u0275\u0275invalidFactoryDep,
  "\u0275\u0275templateRefExtractor": \u0275\u0275templateRefExtractor,
  "\u0275\u0275resetView": \u0275\u0275resetView,
  "\u0275\u0275HostDirectivesFeature": \u0275\u0275HostDirectivesFeature,
  "\u0275\u0275NgOnChangesFeature": \u0275\u0275NgOnChangesFeature,
  "\u0275\u0275ControlFeature": \u0275\u0275ControlFeature,
  "\u0275\u0275ProvidersFeature": \u0275\u0275ProvidersFeature,
  "\u0275\u0275InheritDefinitionFeature": \u0275\u0275InheritDefinitionFeature,
  "\u0275\u0275ExternalStylesFeature": \u0275\u0275ExternalStylesFeature,
  "\u0275\u0275nextContext": \u0275\u0275nextContext,
  "\u0275\u0275namespaceHTML": \u0275\u0275namespaceHTML,
  "\u0275\u0275namespaceMathML": \u0275\u0275namespaceMathML,
  "\u0275\u0275namespaceSVG": \u0275\u0275namespaceSVG,
  "\u0275\u0275enableBindings": \u0275\u0275enableBindings,
  "\u0275\u0275disableBindings": \u0275\u0275disableBindings,
  "\u0275\u0275elementStart": \u0275\u0275elementStart,
  "\u0275\u0275elementEnd": \u0275\u0275elementEnd,
  "\u0275\u0275element": \u0275\u0275element,
  "\u0275\u0275elementContainerStart": \u0275\u0275elementContainerStart,
  "\u0275\u0275elementContainerEnd": \u0275\u0275elementContainerEnd,
  "\u0275\u0275domElement": \u0275\u0275domElement,
  "\u0275\u0275domElementStart": \u0275\u0275domElementStart,
  "\u0275\u0275domElementEnd": \u0275\u0275domElementEnd,
  "\u0275\u0275domElementContainer": \u0275\u0275domElementContainer,
  "\u0275\u0275domElementContainerStart": \u0275\u0275domElementContainerStart,
  "\u0275\u0275domElementContainerEnd": \u0275\u0275domElementContainerEnd,
  "\u0275\u0275domTemplate": \u0275\u0275domTemplate,
  "\u0275\u0275domListener": \u0275\u0275domListener,
  "\u0275\u0275elementContainer": \u0275\u0275elementContainer,
  "\u0275\u0275pureFunction0": \u0275\u0275pureFunction0,
  "\u0275\u0275pureFunction1": \u0275\u0275pureFunction1,
  "\u0275\u0275pureFunction2": \u0275\u0275pureFunction2,
  "\u0275\u0275pureFunction3": \u0275\u0275pureFunction3,
  "\u0275\u0275pureFunction4": \u0275\u0275pureFunction4,
  "\u0275\u0275pureFunction5": \u0275\u0275pureFunction5,
  "\u0275\u0275pureFunction6": \u0275\u0275pureFunction6,
  "\u0275\u0275pureFunction7": \u0275\u0275pureFunction7,
  "\u0275\u0275pureFunction8": \u0275\u0275pureFunction8,
  "\u0275\u0275pureFunctionV": \u0275\u0275pureFunctionV,
  "\u0275\u0275getCurrentView": \u0275\u0275getCurrentView,
  "\u0275\u0275restoreView": \u0275\u0275restoreView,
  "\u0275\u0275listener": \u0275\u0275listener,
  "\u0275\u0275projection": \u0275\u0275projection,
  "\u0275\u0275syntheticHostProperty": \u0275\u0275syntheticHostProperty,
  "\u0275\u0275syntheticHostListener": \u0275\u0275syntheticHostListener,
  "\u0275\u0275pipeBind1": \u0275\u0275pipeBind1,
  "\u0275\u0275pipeBind2": \u0275\u0275pipeBind2,
  "\u0275\u0275pipeBind3": \u0275\u0275pipeBind3,
  "\u0275\u0275pipeBind4": \u0275\u0275pipeBind4,
  "\u0275\u0275pipeBindV": \u0275\u0275pipeBindV,
  "\u0275\u0275projectionDef": \u0275\u0275projectionDef,
  "\u0275\u0275domProperty": \u0275\u0275domProperty,
  "\u0275\u0275ariaProperty": \u0275\u0275ariaProperty,
  "\u0275\u0275property": \u0275\u0275property,
  "\u0275\u0275control": \u0275\u0275control,
  "\u0275\u0275controlCreate": \u0275\u0275controlCreate,
  "\u0275\u0275pipe": \u0275\u0275pipe,
  "\u0275\u0275queryRefresh": \u0275\u0275queryRefresh,
  "\u0275\u0275queryAdvance": \u0275\u0275queryAdvance,
  "\u0275\u0275viewQuery": \u0275\u0275viewQuery,
  "\u0275\u0275viewQuerySignal": \u0275\u0275viewQuerySignal,
  "\u0275\u0275loadQuery": \u0275\u0275loadQuery,
  "\u0275\u0275contentQuery": \u0275\u0275contentQuery,
  "\u0275\u0275contentQuerySignal": \u0275\u0275contentQuerySignal,
  "\u0275\u0275reference": \u0275\u0275reference,
  "\u0275\u0275classMap": \u0275\u0275classMap,
  "\u0275\u0275styleMap": \u0275\u0275styleMap,
  "\u0275\u0275styleProp": \u0275\u0275styleProp,
  "\u0275\u0275classProp": \u0275\u0275classProp,
  "\u0275\u0275advance": \u0275\u0275advance,
  "\u0275\u0275template": \u0275\u0275template,
  "\u0275\u0275conditional": \u0275\u0275conditional,
  "\u0275\u0275conditionalCreate": \u0275\u0275conditionalCreate,
  "\u0275\u0275conditionalBranchCreate": \u0275\u0275conditionalBranchCreate,
  "\u0275\u0275defer": \u0275\u0275defer,
  "\u0275\u0275deferWhen": \u0275\u0275deferWhen,
  "\u0275\u0275deferOnIdle": \u0275\u0275deferOnIdle,
  "\u0275\u0275deferOnImmediate": \u0275\u0275deferOnImmediate,
  "\u0275\u0275deferOnTimer": \u0275\u0275deferOnTimer,
  "\u0275\u0275deferOnHover": \u0275\u0275deferOnHover,
  "\u0275\u0275deferOnInteraction": \u0275\u0275deferOnInteraction,
  "\u0275\u0275deferOnViewport": \u0275\u0275deferOnViewport,
  "\u0275\u0275deferPrefetchWhen": \u0275\u0275deferPrefetchWhen,
  "\u0275\u0275deferPrefetchOnIdle": \u0275\u0275deferPrefetchOnIdle,
  "\u0275\u0275deferPrefetchOnImmediate": \u0275\u0275deferPrefetchOnImmediate,
  "\u0275\u0275deferPrefetchOnTimer": \u0275\u0275deferPrefetchOnTimer,
  "\u0275\u0275deferPrefetchOnHover": \u0275\u0275deferPrefetchOnHover,
  "\u0275\u0275deferPrefetchOnInteraction": \u0275\u0275deferPrefetchOnInteraction,
  "\u0275\u0275deferPrefetchOnViewport": \u0275\u0275deferPrefetchOnViewport,
  "\u0275\u0275deferHydrateWhen": \u0275\u0275deferHydrateWhen,
  "\u0275\u0275deferHydrateNever": \u0275\u0275deferHydrateNever,
  "\u0275\u0275deferHydrateOnIdle": \u0275\u0275deferHydrateOnIdle,
  "\u0275\u0275deferHydrateOnImmediate": \u0275\u0275deferHydrateOnImmediate,
  "\u0275\u0275deferHydrateOnTimer": \u0275\u0275deferHydrateOnTimer,
  "\u0275\u0275deferHydrateOnHover": \u0275\u0275deferHydrateOnHover,
  "\u0275\u0275deferHydrateOnInteraction": \u0275\u0275deferHydrateOnInteraction,
  "\u0275\u0275deferHydrateOnViewport": \u0275\u0275deferHydrateOnViewport,
  "\u0275\u0275deferEnableTimerScheduling": \u0275\u0275deferEnableTimerScheduling,
  "\u0275\u0275repeater": \u0275\u0275repeater,
  "\u0275\u0275repeaterCreate": \u0275\u0275repeaterCreate,
  "\u0275\u0275repeaterTrackByIndex": \u0275\u0275repeaterTrackByIndex,
  "\u0275\u0275repeaterTrackByIdentity": \u0275\u0275repeaterTrackByIdentity,
  "\u0275\u0275componentInstance": \u0275\u0275componentInstance,
  "\u0275\u0275text": \u0275\u0275text,
  "\u0275\u0275textInterpolate": \u0275\u0275textInterpolate,
  "\u0275\u0275textInterpolate1": \u0275\u0275textInterpolate1,
  "\u0275\u0275textInterpolate2": \u0275\u0275textInterpolate2,
  "\u0275\u0275textInterpolate3": \u0275\u0275textInterpolate3,
  "\u0275\u0275textInterpolate4": \u0275\u0275textInterpolate4,
  "\u0275\u0275textInterpolate5": \u0275\u0275textInterpolate5,
  "\u0275\u0275textInterpolate6": \u0275\u0275textInterpolate6,
  "\u0275\u0275textInterpolate7": \u0275\u0275textInterpolate7,
  "\u0275\u0275textInterpolate8": \u0275\u0275textInterpolate8,
  "\u0275\u0275textInterpolateV": \u0275\u0275textInterpolateV,
  "\u0275\u0275i18n": \u0275\u0275i18n,
  "\u0275\u0275i18nAttributes": \u0275\u0275i18nAttributes,
  "\u0275\u0275i18nExp": \u0275\u0275i18nExp,
  "\u0275\u0275i18nStart": \u0275\u0275i18nStart,
  "\u0275\u0275i18nEnd": \u0275\u0275i18nEnd,
  "\u0275\u0275i18nApply": \u0275\u0275i18nApply,
  "\u0275\u0275i18nPostprocess": \u0275\u0275i18nPostprocess,
  "\u0275\u0275resolveWindow": \u0275\u0275resolveWindow,
  "\u0275\u0275resolveDocument": \u0275\u0275resolveDocument,
  "\u0275\u0275resolveBody": \u0275\u0275resolveBody,
  "\u0275\u0275setComponentScope": \u0275\u0275setComponentScope,
  "\u0275\u0275setNgModuleScope": \u0275\u0275setNgModuleScope,
  "\u0275\u0275registerNgModuleType": registerNgModuleType,
  "\u0275\u0275getComponentDepsFactory": \u0275\u0275getComponentDepsFactory,
  "\u0275setClassDebugInfo": \u0275setClassDebugInfo,
  "\u0275\u0275declareLet": \u0275\u0275declareLet,
  "\u0275\u0275storeLet": \u0275\u0275storeLet,
  "\u0275\u0275arrowFunction": \u0275\u0275arrowFunction,
  "\u0275\u0275readContextLet": \u0275\u0275readContextLet,
  "\u0275\u0275attachSourceLocations": \u0275\u0275attachSourceLocations,
  "\u0275\u0275interpolate": \u0275\u0275interpolate,
  "\u0275\u0275interpolate1": \u0275\u0275interpolate1,
  "\u0275\u0275interpolate2": \u0275\u0275interpolate2,
  "\u0275\u0275interpolate3": \u0275\u0275interpolate3,
  "\u0275\u0275interpolate4": \u0275\u0275interpolate4,
  "\u0275\u0275interpolate5": \u0275\u0275interpolate5,
  "\u0275\u0275interpolate6": \u0275\u0275interpolate6,
  "\u0275\u0275interpolate7": \u0275\u0275interpolate7,
  "\u0275\u0275interpolate8": \u0275\u0275interpolate8,
  "\u0275\u0275interpolateV": \u0275\u0275interpolateV,
  "\u0275\u0275sanitizeHtml": \u0275\u0275sanitizeHtml,
  "\u0275\u0275sanitizeStyle": \u0275\u0275sanitizeStyle,
  "\u0275\u0275sanitizeResourceUrl": \u0275\u0275sanitizeResourceUrl,
  "\u0275\u0275sanitizeScript": \u0275\u0275sanitizeScript,
  "\u0275\u0275validateAttribute": \u0275\u0275validateAttribute,
  "\u0275\u0275sanitizeUrl": \u0275\u0275sanitizeUrl,
  "\u0275\u0275sanitizeUrlOrResourceUrl": \u0275\u0275sanitizeUrlOrResourceUrl,
  "\u0275\u0275trustConstantHtml": \u0275\u0275trustConstantHtml,
  "\u0275\u0275trustConstantResourceUrl": \u0275\u0275trustConstantResourceUrl,
  "forwardRef": forwardRef,
  "resolveForwardRef": resolveForwardRef,
  "\u0275\u0275twoWayProperty": \u0275\u0275twoWayProperty,
  "\u0275\u0275twoWayBindingSet": \u0275\u0275twoWayBindingSet,
  "\u0275\u0275twoWayListener": \u0275\u0275twoWayListener,
  "\u0275\u0275replaceMetadata": \u0275\u0275replaceMetadata,
  "\u0275\u0275getReplaceMetadataURL": \u0275\u0275getReplaceMetadataURL
}))();
var jitOptions = null;
function setJitOptions(options) {
  if (jitOptions !== null) {
    if (options.defaultEncapsulation !== jitOptions.defaultEncapsulation) {
      ngDevMode && console.error("Provided value for `defaultEncapsulation` can not be changed once it has been set.");
      return;
    }
    if (options.preserveWhitespaces !== jitOptions.preserveWhitespaces) {
      ngDevMode && console.error("Provided value for `preserveWhitespaces` can not be changed once it has been set.");
      return;
    }
  }
  jitOptions = options;
}
function getJitOptions() {
  return jitOptions;
}
var moduleQueue = [];
function enqueueModuleForDelayedScoping(moduleType, ngModule) {
  moduleQueue.push({
    moduleType,
    ngModule
  });
}
var flushingModuleQueue = false;
function flushModuleScopingQueueAsMuchAsPossible() {
  if (!flushingModuleQueue) {
    flushingModuleQueue = true;
    try {
      for (let i = moduleQueue.length - 1; i >= 0; i--) {
        const {
          moduleType,
          ngModule
        } = moduleQueue[i];
        if (ngModule.declarations && ngModule.declarations.every(isResolvedDeclaration)) {
          moduleQueue.splice(i, 1);
          setScopeOnDeclaredComponents(moduleType, ngModule);
        }
      }
    } finally {
      flushingModuleQueue = false;
    }
  }
}
function isResolvedDeclaration(declaration) {
  if (Array.isArray(declaration)) {
    return declaration.every(isResolvedDeclaration);
  }
  return !!resolveForwardRef(declaration);
}
function compileNgModule(moduleType, ngModule = {}) {
  compileNgModuleDefs(moduleType, ngModule);
  if (ngModule.id !== void 0) {
    registerNgModuleType(moduleType, ngModule.id);
  }
  enqueueModuleForDelayedScoping(moduleType, ngModule);
}
function compileNgModuleDefs(moduleType, ngModule, allowDuplicateDeclarationsInRoot = false) {
  ngDevMode && assertDefined(moduleType, "Required value moduleType");
  ngDevMode && assertDefined(ngModule, "Required value ngModule");
  const declarations = flatten(ngModule.declarations || EMPTY_ARRAY);
  let ngModuleDef = null;
  Object.defineProperty(moduleType, NG_MOD_DEF, {
    configurable: true,
    get: () => {
      if (ngModuleDef === null) {
        if (ngDevMode && ngModule.imports && ngModule.imports.indexOf(moduleType) > -1) {
          throw new Error(`'${stringifyForError(moduleType)}' module can't import itself`);
        }
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "NgModule",
          type: moduleType
        });
        ngModuleDef = compiler.compileNgModule(angularCoreEnv, `ng:///${moduleType.name}/\u0275mod.js`, {
          type: moduleType,
          bootstrap: flatten(ngModule.bootstrap || EMPTY_ARRAY).map(resolveForwardRef),
          declarations: declarations.map(resolveForwardRef),
          imports: flatten(ngModule.imports || EMPTY_ARRAY).map(resolveForwardRef).map(expandModuleWithProviders),
          exports: flatten(ngModule.exports || EMPTY_ARRAY).map(resolveForwardRef).map(expandModuleWithProviders),
          schemas: ngModule.schemas ? flatten(ngModule.schemas) : null,
          id: ngModule.id || null
        });
        if (!ngModuleDef.schemas) {
          ngModuleDef.schemas = [];
        }
      }
      return ngModuleDef;
    }
  });
  let ngFactoryDef = null;
  Object.defineProperty(moduleType, NG_FACTORY_DEF, {
    get: () => {
      if (ngFactoryDef === null) {
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "NgModule",
          type: moduleType
        });
        ngFactoryDef = compiler.compileFactory(angularCoreEnv, `ng:///${moduleType.name}/\u0275fac.js`, {
          name: moduleType.name,
          type: moduleType,
          deps: reflectDependencies(moduleType),
          target: compiler.FactoryTarget.NgModule,
          typeArgumentCount: 0
        });
      }
      return ngFactoryDef;
    },
    configurable: !!ngDevMode
  });
  let ngInjectorDef = null;
  Object.defineProperty(moduleType, NG_INJ_DEF, {
    get: () => {
      if (ngInjectorDef === null) {
        ngDevMode && verifySemanticsOfNgModuleDef(moduleType, allowDuplicateDeclarationsInRoot);
        const meta = {
          name: moduleType.name,
          type: moduleType,
          providers: ngModule.providers || EMPTY_ARRAY,
          imports: [(ngModule.imports || EMPTY_ARRAY).map(resolveForwardRef), (ngModule.exports || EMPTY_ARRAY).map(resolveForwardRef)]
        };
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "NgModule",
          type: moduleType
        });
        ngInjectorDef = compiler.compileInjector(angularCoreEnv, `ng:///${moduleType.name}/\u0275inj.js`, meta);
      }
      return ngInjectorDef;
    },
    configurable: !!ngDevMode
  });
}
function generateStandaloneInDeclarationsError(type, location2) {
  const prefix = `Unexpected "${stringifyForError(type)}" found in the "declarations" array of the`;
  const suffix = `"${stringifyForError(type)}" is marked as standalone and can't be declared in any NgModule - did you intend to import it instead (by adding it to the "imports" array)?`;
  return `${prefix} ${location2}, ${suffix}`;
}
function verifySemanticsOfNgModuleDef(moduleType, allowDuplicateDeclarationsInRoot, importingModule) {
  if (verifiedNgModule.get(moduleType)) return;
  if (isStandalone(moduleType)) return;
  verifiedNgModule.set(moduleType, true);
  moduleType = resolveForwardRef(moduleType);
  let ngModuleDef;
  if (importingModule) {
    ngModuleDef = getNgModuleDef(moduleType);
    if (!ngModuleDef) {
      throw new Error(`Unexpected value '${moduleType.name}' imported by the module '${importingModule.name}'. Please add an @NgModule annotation.`);
    }
  } else {
    ngModuleDef = getNgModuleDefOrThrow(moduleType);
  }
  const errors = [];
  const declarations = maybeUnwrapFn(ngModuleDef.declarations);
  const imports = maybeUnwrapFn(ngModuleDef.imports);
  flatten(imports).map(unwrapModuleWithProvidersImports).forEach((modOrStandaloneCmpt) => {
    verifySemanticsOfNgModuleImport(modOrStandaloneCmpt, moduleType);
    verifySemanticsOfNgModuleDef(modOrStandaloneCmpt, false, moduleType);
  });
  const exports$1 = maybeUnwrapFn(ngModuleDef.exports);
  declarations.forEach(verifyDeclarationsHaveDefinitions);
  declarations.forEach(verifyDirectivesHaveSelector);
  declarations.forEach((declarationType) => verifyNotStandalone(declarationType, moduleType));
  const combinedDeclarations = [...declarations.map(resolveForwardRef), ...flatten(imports.map(computeCombinedExports)).map(resolveForwardRef)];
  exports$1.forEach(verifyExportsAreDeclaredOrReExported);
  declarations.forEach((decl) => verifyDeclarationIsUnique(decl, allowDuplicateDeclarationsInRoot));
  const ngModule = getAnnotation(moduleType, "NgModule");
  if (ngModule) {
    ngModule.imports && flatten(ngModule.imports).map(unwrapModuleWithProvidersImports).forEach((mod) => {
      verifySemanticsOfNgModuleImport(mod, moduleType);
      verifySemanticsOfNgModuleDef(mod, false, moduleType);
    });
    ngModule.bootstrap && deepForEach(ngModule.bootstrap, verifyCorrectBootstrapType);
    ngModule.bootstrap && deepForEach(ngModule.bootstrap, verifyComponentIsPartOfNgModule);
  }
  if (errors.length) {
    throw new Error(errors.join("\n"));
  }
  function verifyDeclarationsHaveDefinitions(type) {
    type = resolveForwardRef(type);
    const def = getComponentDef(type) || getDirectiveDef(type) || getPipeDef(type);
    if (!def) {
      errors.push(`Unexpected value '${stringifyForError(type)}' declared by the module '${stringifyForError(moduleType)}'. Please add a @Pipe/@Directive/@Component annotation.`);
    }
  }
  function verifyDirectivesHaveSelector(type) {
    type = resolveForwardRef(type);
    const def = getDirectiveDef(type);
    if (!getComponentDef(type) && def && def.selectors.length == 0) {
      errors.push(`Directive ${stringifyForError(type)} has no selector, please add it!`);
    }
  }
  function verifyNotStandalone(type, moduleType2) {
    type = resolveForwardRef(type);
    const def = getComponentDef(type) || getDirectiveDef(type) || getPipeDef(type);
    if (def?.standalone) {
      const location2 = `"${stringifyForError(moduleType2)}" NgModule`;
      errors.push(generateStandaloneInDeclarationsError(type, location2));
    }
  }
  function verifyExportsAreDeclaredOrReExported(type) {
    type = resolveForwardRef(type);
    const kind = getComponentDef(type) && "component" || getDirectiveDef(type) && "directive" || getPipeDef(type) && "pipe";
    if (kind) {
      if (combinedDeclarations.lastIndexOf(type) === -1) {
        errors.push(`Can't export ${kind} ${stringifyForError(type)} from ${stringifyForError(moduleType)} as it was neither declared nor imported!`);
      }
    }
  }
  function verifyDeclarationIsUnique(type, suppressErrors) {
    type = resolveForwardRef(type);
    const existingModule = ownerNgModule.get(type);
    if (existingModule && existingModule !== moduleType) {
      if (!suppressErrors) {
        const modules2 = [existingModule, moduleType].map(stringifyForError).sort();
        errors.push(`Type ${stringifyForError(type)} is part of the declarations of 2 modules: ${modules2[0]} and ${modules2[1]}! Please consider moving ${stringifyForError(type)} to a higher module that imports ${modules2[0]} and ${modules2[1]}. You can also create a new NgModule that exports and includes ${stringifyForError(type)} then import that NgModule in ${modules2[0]} and ${modules2[1]}.`);
      }
    } else {
      ownerNgModule.set(type, moduleType);
    }
  }
  function verifyComponentIsPartOfNgModule(type) {
    type = resolveForwardRef(type);
    const existingModule = ownerNgModule.get(type);
    if (!existingModule && !isStandalone(type)) {
      errors.push(`Component ${stringifyForError(type)} is not part of any NgModule or the module has not been imported into your module.`);
    }
  }
  function verifyCorrectBootstrapType(type) {
    type = resolveForwardRef(type);
    if (!getComponentDef(type)) {
      errors.push(`${stringifyForError(type)} cannot be used as an entry component.`);
    }
    if (isStandalone(type)) {
      errors.push(`The \`${stringifyForError(type)}\` class is a standalone component, which can not be used in the \`@NgModule.bootstrap\` array. Use the \`bootstrapApplication\` function for bootstrap instead.`);
    }
  }
  function verifySemanticsOfNgModuleImport(type, importingModule2) {
    type = resolveForwardRef(type);
    const directiveDef = getComponentDef(type) || getDirectiveDef(type);
    if (directiveDef !== null && !directiveDef.standalone) {
      throw new Error(`Unexpected directive '${type.name}' imported by the module '${importingModule2.name}'. Please add an @NgModule annotation.`);
    }
    const pipeDef = getPipeDef(type);
    if (pipeDef !== null && !pipeDef.standalone) {
      throw new Error(`Unexpected pipe '${type.name}' imported by the module '${importingModule2.name}'. Please add an @NgModule annotation.`);
    }
  }
}
function unwrapModuleWithProvidersImports(typeOrWithProviders) {
  typeOrWithProviders = resolveForwardRef(typeOrWithProviders);
  return typeOrWithProviders.ngModule || typeOrWithProviders;
}
function getAnnotation(type, name) {
  let annotation = null;
  collect(type.__annotations__);
  collect(type.decorators);
  return annotation;
  function collect(annotations) {
    if (annotations) {
      annotations.forEach(readAnnotation);
    }
  }
  function readAnnotation(decorator) {
    if (!annotation) {
      const proto = Object.getPrototypeOf(decorator);
      if (proto.ngMetadataName == name) {
        annotation = decorator;
      } else if (decorator.type) {
        const proto2 = Object.getPrototypeOf(decorator.type);
        if (proto2.ngMetadataName == name) {
          annotation = decorator.args[0];
        }
      }
    }
  }
}
var ownerNgModule = /* @__PURE__ */ new WeakMap();
var verifiedNgModule = /* @__PURE__ */ new WeakMap();
function computeCombinedExports(type) {
  type = resolveForwardRef(type);
  const ngModuleDef = getNgModuleDef(type);
  if (ngModuleDef === null) {
    return [type];
  }
  return flatten(maybeUnwrapFn(ngModuleDef.exports).map((type2) => {
    const ngModuleDef2 = getNgModuleDef(type2);
    if (ngModuleDef2) {
      verifySemanticsOfNgModuleDef(type2, false);
      return computeCombinedExports(type2);
    } else {
      return type2;
    }
  }));
}
function setScopeOnDeclaredComponents(moduleType, ngModule) {
  const declarations = flatten(ngModule.declarations || EMPTY_ARRAY);
  const transitiveScopes = transitiveScopesFor(moduleType);
  declarations.forEach((declaration) => {
    declaration = resolveForwardRef(declaration);
    if (declaration.hasOwnProperty(NG_COMP_DEF)) {
      const component = declaration;
      const componentDef = getComponentDef(component);
      patchComponentDefWithScope(componentDef, transitiveScopes);
    } else if (!declaration.hasOwnProperty(NG_DIR_DEF) && !declaration.hasOwnProperty(NG_PIPE_DEF)) {
      declaration.ngSelectorScope = moduleType;
    }
  });
}
function patchComponentDefWithScope(componentDef, transitiveScopes) {
  componentDef.directiveDefs = () => Array.from(transitiveScopes.compilation.directives).map((dir) => dir.hasOwnProperty(NG_COMP_DEF) ? getComponentDef(dir) : getDirectiveDef(dir)).filter((def) => !!def);
  componentDef.pipeDefs = () => Array.from(transitiveScopes.compilation.pipes).map((pipe2) => getPipeDef(pipe2));
  componentDef.schemas = transitiveScopes.schemas;
  componentDef.tView = null;
}
function transitiveScopesFor(type) {
  if (isNgModule(type)) {
    const scope = depsTracker.getNgModuleScope(type);
    const def = getNgModuleDefOrThrow(type);
    return __spreadValues({
      schemas: def.schemas || null
    }, scope);
  } else if (isStandalone(type)) {
    const directiveDef = getComponentDef(type) || getDirectiveDef(type);
    if (directiveDef !== null) {
      return {
        schemas: null,
        compilation: {
          directives: /* @__PURE__ */ new Set(),
          pipes: /* @__PURE__ */ new Set()
        },
        exported: {
          directives: /* @__PURE__ */ new Set([type]),
          pipes: /* @__PURE__ */ new Set()
        }
      };
    }
    const pipeDef = getPipeDef(type);
    if (pipeDef !== null) {
      return {
        schemas: null,
        compilation: {
          directives: /* @__PURE__ */ new Set(),
          pipes: /* @__PURE__ */ new Set()
        },
        exported: {
          directives: /* @__PURE__ */ new Set(),
          pipes: /* @__PURE__ */ new Set([type])
        }
      };
    }
  }
  throw new Error(`${type.name} does not have a module def (\u0275mod property)`);
}
function expandModuleWithProviders(value) {
  if (isModuleWithProviders(value)) {
    return value.ngModule;
  }
  return value;
}
var compilationDepth = 0;
function compileComponent(type, metadata) {
  (typeof ngDevMode === "undefined" || ngDevMode) && initNgDevMode();
  let ngComponentDef = null;
  maybeQueueResolutionOfComponentResources(type, metadata);
  addDirectiveFactoryDef(type, metadata);
  Object.defineProperty(type, NG_COMP_DEF, {
    get: () => {
      if (ngComponentDef === null) {
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "component",
          type
        });
        if (componentNeedsResolution(metadata)) {
          const error = [`Component '${type.name}' is not resolved:`];
          if (metadata.templateUrl) {
            error.push(` - templateUrl: ${metadata.templateUrl}`);
          }
          if (metadata.styleUrls && metadata.styleUrls.length) {
            error.push(` - styleUrls: ${JSON.stringify(metadata.styleUrls)}`);
          }
          if (metadata.styleUrl) {
            error.push(` - styleUrl: ${metadata.styleUrl}`);
          }
          error.push(`Did you run and wait for 'resolveComponentResources()'?`);
          throw new Error(error.join("\n"));
        }
        const options = getJitOptions();
        let preserveWhitespaces = metadata.preserveWhitespaces;
        if (preserveWhitespaces === void 0) {
          if (options !== null && options.preserveWhitespaces !== void 0) {
            preserveWhitespaces = options.preserveWhitespaces;
          } else {
            preserveWhitespaces = false;
          }
        }
        let encapsulation = metadata.encapsulation;
        if (encapsulation === void 0) {
          if (options !== null && options.defaultEncapsulation !== void 0) {
            encapsulation = options.defaultEncapsulation;
          } else {
            encapsulation = ViewEncapsulation.Emulated;
          }
        }
        const templateUrl = metadata.templateUrl || `ng:///${type.name}/template.html`;
        const baseMeta = directiveMetadata(type, metadata);
        const meta = __spreadProps(__spreadValues({}, baseMeta), {
          typeSourceSpan: compiler.createParseSourceSpan("Component", type.name, templateUrl),
          template: metadata.template || "",
          preserveWhitespaces,
          styles: typeof metadata.styles === "string" ? [metadata.styles] : metadata.styles || EMPTY_ARRAY,
          animations: metadata.animations,
          declarations: [],
          changeDetection: metadata.changeDetection,
          encapsulation,
          viewProviders: metadata.viewProviders || null,
          hasDirectiveDependencies: !baseMeta.isStandalone || metadata.imports != null && metadata.imports.length > 0
        });
        compilationDepth++;
        try {
          if (meta.usesInheritance) {
            addDirectiveDefToUndecoratedParents(type);
          }
          ngComponentDef = compiler.compileComponent(angularCoreEnv, templateUrl, meta);
          if (meta.isStandalone) {
            const imports = flatten(metadata.imports || EMPTY_ARRAY);
            const {
              directiveDefs,
              pipeDefs
            } = getStandaloneDefFunctions(type, imports);
            ngComponentDef.directiveDefs = directiveDefs;
            ngComponentDef.pipeDefs = pipeDefs;
            ngComponentDef.dependencies = () => imports.map(resolveForwardRef);
          }
        } finally {
          compilationDepth--;
        }
        if (compilationDepth === 0) {
          flushModuleScopingQueueAsMuchAsPossible();
        }
        if (hasSelectorScope(type)) {
          const scopes = transitiveScopesFor(type.ngSelectorScope);
          patchComponentDefWithScope(ngComponentDef, scopes);
        }
        if (metadata.schemas) {
          if (meta.isStandalone) {
            ngComponentDef.schemas = metadata.schemas;
          } else {
            throw new Error(`The 'schemas' was specified for the ${stringifyForError(type)} but is only valid on a component that is standalone.`);
          }
        } else if (meta.isStandalone) {
          ngComponentDef.schemas = [];
        }
      }
      return ngComponentDef;
    },
    set: (def) => {
      ngComponentDef = def;
    },
    configurable: !!ngDevMode
  });
}
function getStandaloneDefFunctions(type, imports) {
  const directiveDefs = () => {
    if (ngDevMode) {
      for (const rawDep of imports) {
        verifyStandaloneImport(rawDep, type);
      }
    }
    if (!isComponent(type)) {
      return [];
    }
    const scope = depsTracker.getStandaloneComponentScope(type, imports);
    return [...scope.compilation.directives].map((p) => getComponentDef(p) || getDirectiveDef(p)).filter((d) => d !== null);
  };
  const pipeDefs = () => {
    if (ngDevMode) {
      for (const rawDep of imports) {
        verifyStandaloneImport(rawDep, type);
      }
    }
    if (!isComponent(type)) {
      return [];
    }
    const scope = depsTracker.getStandaloneComponentScope(type, imports);
    return [...scope.compilation.pipes].map((p) => getPipeDef(p)).filter((d) => d !== null);
  };
  return {
    directiveDefs,
    pipeDefs
  };
}
function hasSelectorScope(component) {
  return component.ngSelectorScope !== void 0;
}
function compileDirective(type, directive) {
  let ngDirectiveDef = null;
  addDirectiveFactoryDef(type, directive || {});
  Object.defineProperty(type, NG_DIR_DEF, {
    get: () => {
      if (ngDirectiveDef === null) {
        const meta = getDirectiveMetadata(type, directive || {});
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "directive",
          type
        });
        ngDirectiveDef = compiler.compileDirective(angularCoreEnv, meta.sourceMapUrl, meta.metadata);
      }
      return ngDirectiveDef;
    },
    configurable: !!ngDevMode
  });
}
function getDirectiveMetadata(type, metadata) {
  const name = type && type.name;
  const sourceMapUrl = `ng:///${name}/\u0275dir.js`;
  const compiler = getCompilerFacade({
    usage: 0,
    kind: "directive",
    type
  });
  const facade = directiveMetadata(type, metadata);
  facade.typeSourceSpan = compiler.createParseSourceSpan("Directive", name, sourceMapUrl);
  if (facade.usesInheritance) {
    addDirectiveDefToUndecoratedParents(type);
  }
  return {
    metadata: facade,
    sourceMapUrl
  };
}
function addDirectiveFactoryDef(type, metadata) {
  let ngFactoryDef = null;
  Object.defineProperty(type, NG_FACTORY_DEF, {
    get: () => {
      if (ngFactoryDef === null) {
        const meta = getDirectiveMetadata(type, metadata);
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "directive",
          type
        });
        ngFactoryDef = compiler.compileFactory(angularCoreEnv, `ng:///${type.name}/\u0275fac.js`, {
          name: meta.metadata.name,
          type: meta.metadata.type,
          typeArgumentCount: 0,
          deps: reflectDependencies(type),
          target: compiler.FactoryTarget.Directive
        });
      }
      return ngFactoryDef;
    },
    configurable: !!ngDevMode
  });
}
function extendsDirectlyFromObject(type) {
  return Object.getPrototypeOf(type.prototype) === Object.prototype;
}
function directiveMetadata(type, metadata) {
  const reflect = getReflect();
  const propMetadata = reflect.ownPropMetadata(type);
  return {
    name: type.name,
    type,
    selector: metadata.selector !== void 0 ? metadata.selector : null,
    host: metadata.host || EMPTY_OBJ,
    propMetadata,
    inputs: metadata.inputs || EMPTY_ARRAY,
    outputs: metadata.outputs || EMPTY_ARRAY,
    queries: extractQueriesMetadata(type, propMetadata, isContentQuery),
    lifecycle: {
      usesOnChanges: reflect.hasLifecycleHook(type, "ngOnChanges")
    },
    controlCreate: null,
    typeSourceSpan: null,
    usesInheritance: !extendsDirectlyFromObject(type),
    exportAs: extractExportAs(metadata.exportAs),
    providers: metadata.providers || null,
    viewQueries: extractQueriesMetadata(type, propMetadata, isViewQuery),
    isStandalone: metadata.standalone === void 0 ? true : !!metadata.standalone,
    isSignal: !!metadata.signals,
    hostDirectives: metadata.hostDirectives?.map((directive) => typeof directive === "function" ? {
      directive
    } : directive) || null
  };
}
function addDirectiveDefToUndecoratedParents(type) {
  const objPrototype = Object.prototype;
  let parent = Object.getPrototypeOf(type.prototype).constructor;
  while (parent && parent !== objPrototype) {
    if (!getDirectiveDef(parent) && !getComponentDef(parent) && shouldAddAbstractDirective(parent)) {
      compileDirective(parent, null);
    }
    parent = Object.getPrototypeOf(parent);
  }
}
function convertToR3QueryPredicate(selector) {
  return typeof selector === "string" ? splitByComma(selector) : resolveForwardRef(selector);
}
function convertToR3QueryMetadata(propertyName, ann) {
  return {
    propertyName,
    predicate: convertToR3QueryPredicate(ann.selector),
    descendants: ann.descendants,
    first: ann.first,
    read: ann.read ? ann.read : null,
    static: !!ann.static,
    emitDistinctChangesOnly: !!ann.emitDistinctChangesOnly,
    isSignal: !!ann.isSignal
  };
}
function extractQueriesMetadata(type, propMetadata, isQueryAnn) {
  const signalQueriesMeta = [];
  const decoratorQueriesMeta = [];
  for (const field in propMetadata) {
    if (propMetadata.hasOwnProperty(field)) {
      const annotations = propMetadata[field];
      annotations.forEach((ann) => {
        if (isQueryAnn(ann)) {
          if (!ann.selector) {
            throw new Error(`Can't construct a query for the property "${field}" of "${stringifyForError(type)}" since the query selector wasn't defined.`);
          }
          if (annotations.some(isInputAnnotation)) {
            throw new Error(`Cannot combine @Input decorators with query decorators`);
          }
          const queryMeta = convertToR3QueryMetadata(field, ann);
          if (queryMeta.isSignal) {
            signalQueriesMeta.push(queryMeta);
          } else {
            decoratorQueriesMeta.push(queryMeta);
          }
        }
      });
    }
  }
  return [...signalQueriesMeta, ...decoratorQueriesMeta];
}
function extractExportAs(exportAs) {
  return exportAs === void 0 ? null : splitByComma(exportAs);
}
function isContentQuery(value) {
  const name = value.ngMetadataName;
  return name === "ContentChild" || name === "ContentChildren";
}
function isViewQuery(value) {
  const name = value.ngMetadataName;
  return name === "ViewChild" || name === "ViewChildren";
}
function isInputAnnotation(value) {
  return value.ngMetadataName === "Input";
}
function splitByComma(value) {
  return value.split(",").map((piece) => piece.trim());
}
var LIFECYCLE_HOOKS = ["ngOnChanges", "ngOnInit", "ngOnDestroy", "ngDoCheck", "ngAfterViewInit", "ngAfterViewChecked", "ngAfterContentInit", "ngAfterContentChecked"];
function shouldAddAbstractDirective(type) {
  const reflect = getReflect();
  if (LIFECYCLE_HOOKS.some((hookName) => reflect.hasLifecycleHook(type, hookName))) {
    return true;
  }
  const propMetadata = reflect.propMetadata(type);
  for (const field in propMetadata) {
    const annotations = propMetadata[field];
    for (let i = 0; i < annotations.length; i++) {
      const current = annotations[i];
      const metadataName = current.ngMetadataName;
      if (isInputAnnotation(current) || isContentQuery(current) || isViewQuery(current) || metadataName === "Output" || metadataName === "HostBinding" || metadataName === "HostListener") {
        return true;
      }
    }
  }
  return false;
}
function compilePipe(type, meta) {
  let ngPipeDef = null;
  let ngFactoryDef = null;
  Object.defineProperty(type, NG_FACTORY_DEF, {
    get: () => {
      if (ngFactoryDef === null) {
        const metadata = getPipeMetadata(type, meta);
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "pipe",
          type: metadata.type
        });
        ngFactoryDef = compiler.compileFactory(angularCoreEnv, `ng:///${metadata.name}/\u0275fac.js`, {
          name: metadata.name,
          type: metadata.type,
          typeArgumentCount: 0,
          deps: reflectDependencies(type),
          target: compiler.FactoryTarget.Pipe
        });
      }
      return ngFactoryDef;
    },
    configurable: !!ngDevMode
  });
  Object.defineProperty(type, NG_PIPE_DEF, {
    get: () => {
      if (ngPipeDef === null) {
        const metadata = getPipeMetadata(type, meta);
        const compiler = getCompilerFacade({
          usage: 0,
          kind: "pipe",
          type: metadata.type
        });
        ngPipeDef = compiler.compilePipe(angularCoreEnv, `ng:///${metadata.name}/\u0275pipe.js`, metadata);
      }
      return ngPipeDef;
    },
    configurable: !!ngDevMode
  });
}
function getPipeMetadata(type, meta) {
  return {
    type,
    name: type.name,
    pipeName: meta.name,
    pure: meta.pure !== void 0 ? meta.pure : true,
    isStandalone: meta.standalone === void 0 ? true : !!meta.standalone
  };
}
var Directive = makeDecorator("Directive", (dir = {}) => dir, void 0, void 0, (type, meta) => compileDirective(type, meta));
var Component = makeDecorator("Component", (c = {}) => __spreadValues({
  changeDetection: ChangeDetectionStrategy.Eager
}, c), Directive, void 0, (type, meta) => compileComponent(type, meta));
var Pipe = makeDecorator("Pipe", (p) => __spreadValues({
  pure: true
}, p), void 0, void 0, (type, meta) => compilePipe(type, meta));
var Input = makePropDecorator("Input", (arg) => {
  if (!arg) {
    return {};
  }
  return typeof arg === "string" ? {
    alias: arg
  } : arg;
});
var Output = makePropDecorator("Output", (alias) => ({
  alias
}));
var HostBinding = makePropDecorator("HostBinding", (hostPropertyName) => ({
  hostPropertyName
}));
var HostListener = makePropDecorator("HostListener", (eventName, args) => ({
  eventName,
  args
}));
var NgModule = makeDecorator("NgModule", (ngModule) => ngModule, void 0, void 0, (type, meta) => compileNgModule(type, meta));
var ModuleWithComponentFactories = class {
  ngModuleFactory;
  componentFactories;
  constructor(ngModuleFactory, componentFactories) {
    this.ngModuleFactory = ngModuleFactory;
    this.componentFactories = componentFactories;
  }
};
var Compiler = class _Compiler {
  compileModuleSync(moduleType) {
    return new NgModuleFactory2(moduleType);
  }
  compileModuleAsync(moduleType) {
    return Promise.resolve(this.compileModuleSync(moduleType));
  }
  compileModuleAndAllComponentsSync(moduleType) {
    const ngModuleFactory = this.compileModuleSync(moduleType);
    const moduleDef = getNgModuleDef(moduleType);
    const componentFactories = maybeUnwrapFn(moduleDef.declarations).reduce((factories, declaration) => {
      const componentDef = getComponentDef(declaration);
      componentDef && factories.push(new ComponentFactory2(componentDef));
      return factories;
    }, []);
    return new ModuleWithComponentFactories(ngModuleFactory, componentFactories);
  }
  compileModuleAndAllComponentsAsync(moduleType) {
    return Promise.resolve(this.compileModuleAndAllComponentsSync(moduleType));
  }
  clearCache() {
  }
  clearCacheFor(type) {
  }
  getModuleId(moduleType) {
    return void 0;
  }
  static \u0275fac = function Compiler_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Compiler)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Compiler,
    factory: _Compiler.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Compiler, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var COMPILER_OPTIONS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "compilerOptions" : "");
var CONSECUTIVE_MICROTASK_NOTIFICATION_LIMIT = 100;
var consecutiveMicrotaskNotifications = 0;
var stackFromLastFewNotifications = [];
function trackMicrotaskNotificationForDebugging() {
  consecutiveMicrotaskNotifications++;
  if (CONSECUTIVE_MICROTASK_NOTIFICATION_LIMIT - consecutiveMicrotaskNotifications < 5) {
    const stack = new Error().stack;
    if (stack) {
      stackFromLastFewNotifications.push(stack);
    }
  }
  if (consecutiveMicrotaskNotifications === CONSECUTIVE_MICROTASK_NOTIFICATION_LIMIT) {
    throw new RuntimeError(103, "Angular could not stabilize because there were endless change notifications within the browser event loop. The stack from the last several notifications: \n" + stackFromLastFewNotifications.join("\n"));
  }
}
var ChangeDetectionSchedulerImpl = class _ChangeDetectionSchedulerImpl {
  applicationErrorHandler = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
  appRef = inject2(ApplicationRef);
  taskService = inject2(PendingTasksInternal);
  ngZone = inject2(NgZone);
  zonelessEnabled = inject2(ZONELESS_ENABLED);
  tracing = inject2(TracingService, {
    optional: true
  });
  zoneIsDefined = typeof Zone !== "undefined" && !!Zone.root.run;
  schedulerTickApplyArgs = [{
    data: {
      "__scheduler_tick__": true
    }
  }];
  subscriptions = new Subscription();
  angularZoneId = this.zoneIsDefined ? this.ngZone._inner?.get(angularZoneInstanceIdProperty) : null;
  scheduleInRootZone = !this.zonelessEnabled && this.zoneIsDefined && (inject2(SCHEDULE_IN_ROOT_ZONE, {
    optional: true
  }) ?? false);
  cancelScheduledCallback = null;
  useMicrotaskScheduler = false;
  runningTick = false;
  pendingRenderTaskId = null;
  constructor() {
    this.subscriptions.add(this.appRef.afterTick.subscribe(() => {
      const task = this.taskService.add();
      if (!this.runningTick) {
        this.cleanup();
        if (!this.zonelessEnabled || this.appRef.includeAllTestViews) {
          this.taskService.remove(task);
          return;
        }
      }
      this.switchToMicrotaskScheduler();
      this.taskService.remove(task);
    }));
    this.subscriptions.add(this.ngZone.onUnstable.subscribe(() => {
      if (!this.runningTick) {
        this.cleanup();
      }
    }));
  }
  switchToMicrotaskScheduler() {
    this.ngZone.runOutsideAngular(() => {
      const task = this.taskService.add();
      this.useMicrotaskScheduler = true;
      queueMicrotask(() => {
        this.useMicrotaskScheduler = false;
        this.taskService.remove(task);
      });
    });
  }
  notify(source) {
    if (!this.zonelessEnabled && source === 5) {
      return;
    }
    switch (source) {
      case 0: {
        this.appRef.dirtyFlags |= 2;
        break;
      }
      case 3:
      case 2:
      case 4:
      case 5:
      case 1: {
        this.appRef.dirtyFlags |= 4;
        break;
      }
      case 6: {
        this.appRef.dirtyFlags |= 2;
        break;
      }
      case 12: {
        this.appRef.dirtyFlags |= 16;
        break;
      }
      case 13: {
        this.appRef.dirtyFlags |= 2;
        break;
      }
      case 11: {
        break;
      }
      case 9:
      case 8:
      case 7:
      case 10:
      default: {
        this.appRef.dirtyFlags |= 8;
      }
    }
    this.appRef.tracingSnapshot = this.tracing?.snapshot(this.appRef.tracingSnapshot) ?? null;
    if (!this.shouldScheduleTick()) {
      return;
    }
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (this.useMicrotaskScheduler) {
        trackMicrotaskNotificationForDebugging();
      } else {
        consecutiveMicrotaskNotifications = 0;
        stackFromLastFewNotifications.length = 0;
      }
    }
    const scheduleCallback = this.useMicrotaskScheduler ? scheduleCallbackWithMicrotask : scheduleCallbackWithRafRace;
    this.pendingRenderTaskId = this.taskService.add();
    if (this.scheduleInRootZone) {
      this.cancelScheduledCallback = Zone.root.run(() => scheduleCallback(() => this.tick()));
    } else {
      this.cancelScheduledCallback = this.ngZone.runOutsideAngular(() => scheduleCallback(() => this.tick()));
    }
  }
  shouldScheduleTick() {
    if (this.appRef.destroyed) {
      return false;
    }
    if (this.pendingRenderTaskId !== null || this.runningTick || this.appRef._runningTick) {
      return false;
    }
    if (!this.zonelessEnabled && this.zoneIsDefined && Zone.current.get(angularZoneInstanceIdProperty + this.angularZoneId)) {
      return false;
    }
    return true;
  }
  tick() {
    if (this.runningTick || this.appRef.destroyed) {
      return;
    }
    if (this.appRef.dirtyFlags === 0) {
      this.cleanup();
      return;
    }
    if (!this.zonelessEnabled && this.appRef.dirtyFlags & 7) {
      this.appRef.dirtyFlags |= 1;
    }
    const task = this.taskService.add();
    try {
      this.ngZone.run(() => {
        this.runningTick = true;
        this.appRef._tick();
      }, void 0, this.schedulerTickApplyArgs);
    } catch (e) {
      this.applicationErrorHandler(e);
    } finally {
      this.taskService.remove(task);
      this.cleanup();
    }
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
    this.cleanup();
  }
  cleanup() {
    this.runningTick = false;
    this.cancelScheduledCallback?.();
    this.cancelScheduledCallback = null;
    if (this.pendingRenderTaskId !== null) {
      const taskId = this.pendingRenderTaskId;
      this.pendingRenderTaskId = null;
      this.taskService.remove(taskId);
    }
  }
  static \u0275fac = function ChangeDetectionSchedulerImpl_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ChangeDetectionSchedulerImpl)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ChangeDetectionSchedulerImpl,
    factory: _ChangeDetectionSchedulerImpl.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ChangeDetectionSchedulerImpl, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function provideZonelessChangeDetectionInternal() {
  return [{
    provide: ChangeDetectionScheduler,
    useExisting: ChangeDetectionSchedulerImpl
  }, {
    provide: NgZone,
    useClass: NoopNgZone
  }, {
    provide: ZONELESS_ENABLED,
    useValue: true
  }];
}
function getGlobalLocale() {
  if (false) {
    return goog.LOCALE;
  } else {
    return typeof $localize !== "undefined" && $localize.locale || DEFAULT_LOCALE_ID;
  }
}
var LOCALE_ID = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "LocaleId" : "", {
  factory: () => inject2(LOCALE_ID, {
    optional: true,
    skipSelf: true
  }) || getGlobalLocale()
});
var DEFAULT_CURRENCY_CODE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DefaultCurrencyCode" : "", {
  factory: () => USD_CURRENCY_CODE
});
var TRANSLATIONS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "Translations" : "");
var TRANSLATIONS_FORMAT = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "TranslationsFormat" : "");
var MissingTranslationStrategy;
(function(MissingTranslationStrategy2) {
  MissingTranslationStrategy2[MissingTranslationStrategy2["Error"] = 0] = "Error";
  MissingTranslationStrategy2[MissingTranslationStrategy2["Warning"] = 1] = "Warning";
  MissingTranslationStrategy2[MissingTranslationStrategy2["Ignore"] = 2] = "Ignore";
})(MissingTranslationStrategy || (MissingTranslationStrategy = {}));

// node_modules/@angular/core/fesm2022/_resource-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var OutputEmitterRef = class {
  destroyed = false;
  listeners = null;
  errorHandler = inject2(ErrorHandler, {
    optional: true
  });
  destroyRef = inject2(DestroyRef);
  constructor() {
    this.destroyRef.onDestroy(() => {
      this.destroyed = true;
      this.listeners = null;
    });
  }
  subscribe(callback) {
    if (this.destroyed) {
      throw new RuntimeError(953, ngDevMode && "Unexpected subscription to destroyed `OutputRef`. The owning directive/component is destroyed.");
    }
    (this.listeners ??= []).push(callback);
    return {
      unsubscribe: () => {
        const idx = this.listeners?.indexOf(callback);
        if (idx !== void 0 && idx !== -1) {
          this.listeners?.splice(idx, 1);
        }
      }
    };
  }
  emit(value) {
    if (this.destroyed) {
      console.warn(formatRuntimeError(953, ngDevMode && "Unexpected emit for destroyed `OutputRef`. The owning directive/component is destroyed."));
      return;
    }
    if (this.listeners === null) {
      return;
    }
    const previousConsumer = setActiveConsumer(null);
    try {
      for (const listenerFn of this.listeners) {
        try {
          listenerFn(value);
        } catch (err) {
          this.errorHandler?.handleError(err);
        }
      }
    } finally {
      setActiveConsumer(previousConsumer);
    }
  }
};
function untracked2(nonReactiveReadsFn) {
  return untracked(nonReactiveReadsFn);
}
function computed(computation, options) {
  const getter = createComputed(computation, options?.equal);
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    const debugName = options?.debugName;
    getter[SIGNAL].debugName = debugName;
    getter.toString = () => `[Computed${debugName ? " (" + debugName + ")" : ""}: ${getter()}]`;
  }
  return getter;
}
var identityFn = (v) => v;
function linkedSignal(optionsOrComputation, options) {
  if (typeof optionsOrComputation === "function") {
    const getter = createLinkedSignal(optionsOrComputation, identityFn, options?.equal);
    return upgradeLinkedSignalGetter(getter, options?.debugName);
  } else {
    const getter = createLinkedSignal(optionsOrComputation.source, optionsOrComputation.computation, optionsOrComputation.equal);
    return upgradeLinkedSignalGetter(getter, optionsOrComputation.debugName);
  }
}
function upgradeLinkedSignalGetter(getter, debugName) {
  if (typeof ngDevMode !== "undefined" && ngDevMode) {
    getter[SIGNAL].debugName = debugName;
    getter.toString = () => `[LinkedSignal${debugName ? " (" + debugName + ")" : ""}: ${getter()}]`;
  }
  const node = getter[SIGNAL];
  const upgradedGetter = getter;
  upgradedGetter.set = (newValue) => linkedSignalSetFn(node, newValue);
  upgradedGetter.update = (updateFn) => linkedSignalUpdateFn(node, updateFn);
  upgradedGetter.asReadonly = signalAsReadonlyFn.bind(getter);
  return upgradedGetter;
}

// node_modules/@angular/core/fesm2022/core.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var REQUIRED_UNSET_VALUE = /* @__PURE__ */ Symbol("InputSignalNode#UNSET");
var INPUT_SIGNAL_NODE = /* @__PURE__ */ (() => {
  return __spreadProps(__spreadValues({}, SIGNAL_NODE), {
    transformFn: void 0,
    applyValueToInputSignal(node, value) {
      signalSetFn(node, value);
    }
  });
})();
function createInputSignal(initialValue, options) {
  const node = Object.create(INPUT_SIGNAL_NODE);
  node.value = initialValue;
  node.transformFn = options?.transform;
  function inputValueFn() {
    producerAccessed(node);
    if (node.value === REQUIRED_UNSET_VALUE) {
      let message = null;
      if (ngDevMode) {
        const name = options?.debugName ?? options?.alias;
        message = `Input${name ? ` "${name}"` : ""} is required but no value is available yet.`;
      }
      throw new RuntimeError(-950, message);
    }
    return node.value;
  }
  inputValueFn[SIGNAL] = node;
  if (ngDevMode) {
    inputValueFn.toString = () => `[Input Signal: ${inputValueFn()}]`;
    node.debugName = options?.debugName;
  }
  return inputValueFn;
}
var FactoryTarget;
(function(FactoryTarget2) {
  FactoryTarget2[FactoryTarget2["Directive"] = 0] = "Directive";
  FactoryTarget2[FactoryTarget2["Component"] = 1] = "Component";
  FactoryTarget2[FactoryTarget2["Injectable"] = 2] = "Injectable";
  FactoryTarget2[FactoryTarget2["Pipe"] = 3] = "Pipe";
  FactoryTarget2[FactoryTarget2["NgModule"] = 4] = "NgModule";
})(FactoryTarget || (FactoryTarget = {}));
var R3TemplateDependencyKind;
(function(R3TemplateDependencyKind2) {
  R3TemplateDependencyKind2[R3TemplateDependencyKind2["Directive"] = 0] = "Directive";
  R3TemplateDependencyKind2[R3TemplateDependencyKind2["Pipe"] = 1] = "Pipe";
  R3TemplateDependencyKind2[R3TemplateDependencyKind2["NgModule"] = 2] = "NgModule";
})(R3TemplateDependencyKind || (R3TemplateDependencyKind = {}));
var ViewEncapsulation2;
(function(ViewEncapsulation3) {
  ViewEncapsulation3[ViewEncapsulation3["Emulated"] = 0] = "Emulated";
  ViewEncapsulation3[ViewEncapsulation3["None"] = 2] = "None";
  ViewEncapsulation3[ViewEncapsulation3["ShadowDom"] = 3] = "ShadowDom";
  ViewEncapsulation3[ViewEncapsulation3["ExperimentalIsolatedShadowDom"] = 4] = "ExperimentalIsolatedShadowDom";
})(ViewEncapsulation2 || (ViewEncapsulation2 = {}));
var Framework;
(function(Framework2) {
  Framework2["Angular"] = "angular";
  Framework2["ACX"] = "acx";
  Framework2["Wiz"] = "wiz";
})(Framework || (Framework = {}));
var HostAttributeToken = class {
  attributeName;
  constructor(attributeName) {
    this.attributeName = attributeName;
  }
  __NG_ELEMENT_ID__ = () => \u0275\u0275injectAttribute(this.attributeName);
  toString() {
    return `HostAttributeToken ${this.attributeName}`;
  }
};
function inputFunction(initialValue, opts) {
  ngDevMode && assertInInjectionContext(input);
  return createInputSignal(initialValue, opts);
}
function inputRequiredFunction(opts) {
  ngDevMode && assertInInjectionContext(input);
  return createInputSignal(REQUIRED_UNSET_VALUE, opts);
}
var input = (() => {
  inputFunction.required = inputRequiredFunction;
  return inputFunction;
})();
function viewChildFn(locator, opts) {
  ngDevMode && assertInInjectionContext(viewChild);
  return createSingleResultOptionalQuerySignalFn(opts);
}
function viewChildRequiredFn(locator, opts) {
  ngDevMode && assertInInjectionContext(viewChild);
  return createSingleResultRequiredQuerySignalFn(opts);
}
var viewChild = (() => {
  viewChildFn.required = viewChildRequiredFn;
  return viewChildFn;
})();
function contentChildFn(locator, opts) {
  ngDevMode && assertInInjectionContext(contentChild);
  return createSingleResultOptionalQuerySignalFn(opts);
}
function contentChildRequiredFn(locator, opts) {
  ngDevMode && assertInInjectionContext(contentChildren);
  return createSingleResultRequiredQuerySignalFn(opts);
}
var contentChild = (() => {
  contentChildFn.required = contentChildRequiredFn;
  return contentChildFn;
})();
function contentChildren(locator, opts) {
  return createMultiResultQuerySignalFn(opts);
}
function createModelSignal(initialValue, opts) {
  const node = Object.create(INPUT_SIGNAL_NODE);
  const emitterRef = new OutputEmitterRef();
  node.value = initialValue;
  function getter() {
    producerAccessed(node);
    assertModelSet(node.value);
    return node.value;
  }
  getter[SIGNAL] = node;
  getter.asReadonly = signalAsReadonlyFn.bind(getter);
  getter.set = (newValue) => {
    if (!node.equal(node.value, newValue)) {
      signalSetFn(node, newValue);
      emitterRef.emit(newValue);
    }
  };
  getter.update = (updateFn) => {
    assertModelSet(node.value);
    getter.set(updateFn(node.value));
  };
  getter.subscribe = emitterRef.subscribe.bind(emitterRef);
  getter.destroyRef = emitterRef.destroyRef;
  if (ngDevMode) {
    getter.toString = () => `[Model Signal: ${getter()}]`;
    node.debugName = opts?.debugName;
  }
  return getter;
}
function assertModelSet(value) {
  if (value === REQUIRED_UNSET_VALUE) {
    throw new RuntimeError(952, ngDevMode && "Model is required but no value is available yet.");
  }
}
function modelFunction(initialValue, opts) {
  ngDevMode && assertInInjectionContext(model);
  return createModelSignal(initialValue, opts);
}
function modelRequiredFunction(opts) {
  ngDevMode && assertInInjectionContext(model);
  return createModelSignal(REQUIRED_UNSET_VALUE, opts);
}
var model = (() => {
  modelFunction.required = modelRequiredFunction;
  return modelFunction;
})();
var emitDistinctChangesOnlyDefaultValue = true;
var Query = class {
};
var ContentChildren = makePropDecorator("ContentChildren", (selector, opts = {}) => __spreadValues({
  selector,
  first: false,
  isViewQuery: false,
  descendants: false,
  emitDistinctChangesOnly: emitDistinctChangesOnlyDefaultValue
}, opts), Query);
var ContentChild = makePropDecorator("ContentChild", (selector, opts = {}) => __spreadValues({
  selector,
  first: true,
  isViewQuery: false,
  descendants: true
}, opts), Query);
var ViewChildren = makePropDecorator("ViewChildren", (selector, opts = {}) => __spreadValues({
  selector,
  first: false,
  isViewQuery: true,
  descendants: true,
  emitDistinctChangesOnly: emitDistinctChangesOnlyDefaultValue
}, opts), Query);
var ViewChild = makePropDecorator("ViewChild", (selector, opts) => __spreadValues({
  selector,
  first: true,
  isViewQuery: true,
  descendants: true
}, opts), Query);
function compileNgModuleFactory(injector, options, moduleType) {
  ngDevMode && assertNgModuleType(moduleType);
  const moduleFactory = new NgModuleFactory2(moduleType);
  if (true) {
    return Promise.resolve(moduleFactory);
  }
  const compilerOptions = injector.get(COMPILER_OPTIONS, []).concat(options);
  setJitOptions({
    defaultEncapsulation: _lastDefined(compilerOptions.map((opts) => opts.defaultEncapsulation)),
    preserveWhitespaces: _lastDefined(compilerOptions.map((opts) => opts.preserveWhitespaces))
  });
  if (isComponentResourceResolutionQueueEmpty()) {
    return Promise.resolve(moduleFactory);
  }
  const compilerProviders = compilerOptions.flatMap((option) => option.providers ?? []);
  if (compilerProviders.length === 0) {
    return Promise.resolve(moduleFactory);
  }
  const compiler = getCompilerFacade({
    usage: 0,
    kind: "NgModule",
    type: moduleType
  });
  const compilerInjector = Injector.create({
    providers: compilerProviders
  });
  const resourceLoader = compilerInjector.get(compiler.ResourceLoader);
  return resolveComponentResources((url) => Promise.resolve(resourceLoader.get(url))).then(() => moduleFactory);
}
function _lastDefined(args) {
  for (let i = args.length - 1; i >= 0; i--) {
    if (args[i] !== void 0) {
      return args[i];
    }
  }
  return void 0;
}
var NgZoneChangeDetectionScheduler = class _NgZoneChangeDetectionScheduler {
  zone = inject2(NgZone);
  changeDetectionScheduler = inject2(ChangeDetectionScheduler);
  applicationRef = inject2(ApplicationRef);
  applicationErrorHandler = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
  _onMicrotaskEmptySubscription;
  initialize() {
    if (this._onMicrotaskEmptySubscription) {
      return;
    }
    this._onMicrotaskEmptySubscription = this.zone.onMicrotaskEmpty.subscribe({
      next: () => {
        if (this.changeDetectionScheduler.runningTick) {
          return;
        }
        this.zone.run(() => {
          try {
            this.applicationRef.dirtyFlags |= 1;
            this.applicationRef._tick();
          } catch (e) {
            this.applicationErrorHandler(e);
          }
        });
      }
    });
  }
  ngOnDestroy() {
    this._onMicrotaskEmptySubscription?.unsubscribe();
  }
  static \u0275fac = function NgZoneChangeDetectionScheduler_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgZoneChangeDetectionScheduler)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NgZoneChangeDetectionScheduler,
    factory: _NgZoneChangeDetectionScheduler.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgZoneChangeDetectionScheduler, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PROVIDED_NG_ZONE = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "provideZoneChangeDetection token" : "", {
  factory: () => false
});
var ZoneStablePendingTask = class _ZoneStablePendingTask {
  subscription = new Subscription();
  initialized = false;
  zone = inject2(NgZone);
  pendingTasks = inject2(PendingTasksInternal);
  initialize() {
    if (this.initialized) {
      return;
    }
    this.initialized = true;
    let task = null;
    if (!this.zone.isStable && !this.zone.hasPendingMacrotasks && !this.zone.hasPendingMicrotasks) {
      task = this.pendingTasks.add();
    }
    this.zone.runOutsideAngular(() => {
      this.subscription.add(this.zone.onStable.subscribe(() => {
        NgZone.assertNotInAngularZone();
        queueMicrotask(() => {
          if (task !== null && !this.zone.hasPendingMacrotasks && !this.zone.hasPendingMicrotasks) {
            this.pendingTasks.remove(task);
            task = null;
          }
        });
      }));
    });
    this.subscription.add(this.zone.onUnstable.subscribe(() => {
      NgZone.assertInAngularZone();
      task ??= this.pendingTasks.add();
    }));
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  static \u0275fac = function ZoneStablePendingTask_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ZoneStablePendingTask)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ZoneStablePendingTask,
    factory: _ZoneStablePendingTask.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ZoneStablePendingTask, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var SCAN_DELAY = 200;
var OVERSIZED_IMAGE_TOLERANCE = 1200;
var ImagePerformanceWarning = class _ImagePerformanceWarning {
  window = null;
  observer = null;
  options = inject2(IMAGE_CONFIG);
  lcpImageUrl;
  start() {
    if (typeof PerformanceObserver === "undefined" || this.options?.disableImageSizeWarning && this.options?.disableImageLazyLoadWarning) {
      return;
    }
    this.observer = this.initPerformanceObserver();
    const doc = getDocument();
    const win = doc.defaultView;
    if (win) {
      this.window = win;
      const waitToScan = () => {
        setTimeout(this.scanImages.bind(this), SCAN_DELAY);
      };
      const setup = () => {
        if (doc.readyState === "complete") {
          waitToScan();
        } else {
          this.window?.addEventListener("load", waitToScan, {
            once: true
          });
        }
      };
      if (typeof Zone !== "undefined") {
        Zone.root.run(() => setup());
      } else {
        setup();
      }
    }
  }
  ngOnDestroy() {
    this.observer?.disconnect();
  }
  initPerformanceObserver() {
    if (typeof PerformanceObserver === "undefined") {
      return null;
    }
    const observer = new PerformanceObserver((entryList) => {
      const entries = entryList.getEntries();
      if (entries.length === 0) return;
      const lcpElement = entries[entries.length - 1];
      const imgSrc = lcpElement.element?.src ?? "";
      if (imgSrc.startsWith("data:") || imgSrc.startsWith("blob:")) return;
      this.lcpImageUrl = imgSrc;
    });
    observer.observe({
      type: "largest-contentful-paint",
      buffered: true
    });
    return observer;
  }
  scanImages() {
    const images = getDocument().querySelectorAll("img");
    let lcpElementFound, lcpElementLoadedCorrectly = false;
    for (let index = 0; index < images.length; index++) {
      const image = images[index];
      if (!image) {
        continue;
      }
      if (!this.options?.disableImageSizeWarning) {
        if (!image.getAttribute("ng-img") && this.isOversized(image)) {
          logOversizedImageWarning(image.src);
        }
      }
      if (!this.options?.disableImageLazyLoadWarning && this.lcpImageUrl) {
        if (image.src === this.lcpImageUrl) {
          lcpElementFound = true;
          if (image.loading !== "lazy" || image.getAttribute("ng-img")) {
            lcpElementLoadedCorrectly = true;
          }
        }
      }
    }
    if (lcpElementFound && !lcpElementLoadedCorrectly && this.lcpImageUrl && !this.options?.disableImageLazyLoadWarning) {
      logLazyLCPWarning(this.lcpImageUrl);
    }
  }
  isOversized(image) {
    if (!this.window) {
      return false;
    }
    const nonOversizedImageExtentions = [".svg"];
    const imageSource = (image.src || "").toLowerCase();
    if (nonOversizedImageExtentions.some((extension) => imageSource.endsWith(extension))) {
      return false;
    }
    const computedStyle = this.window.getComputedStyle(image);
    let renderedWidth = parseFloat(computedStyle.getPropertyValue("width"));
    let renderedHeight = parseFloat(computedStyle.getPropertyValue("height"));
    const boxSizing = computedStyle.getPropertyValue("box-sizing");
    const objectFit = computedStyle.getPropertyValue("object-fit");
    if (objectFit === `cover`) {
      return false;
    }
    if (boxSizing === "border-box") {
      const paddingTop = computedStyle.getPropertyValue("padding-top");
      const paddingRight = computedStyle.getPropertyValue("padding-right");
      const paddingBottom = computedStyle.getPropertyValue("padding-bottom");
      const paddingLeft = computedStyle.getPropertyValue("padding-left");
      renderedWidth -= parseFloat(paddingRight) + parseFloat(paddingLeft);
      renderedHeight -= parseFloat(paddingTop) + parseFloat(paddingBottom);
    }
    const intrinsicWidth = image.naturalWidth;
    const intrinsicHeight = image.naturalHeight;
    const recommendedWidth = this.window.devicePixelRatio * renderedWidth;
    const recommendedHeight = this.window.devicePixelRatio * renderedHeight;
    const oversizedWidth = intrinsicWidth - recommendedWidth >= OVERSIZED_IMAGE_TOLERANCE;
    const oversizedHeight = intrinsicHeight - recommendedHeight >= OVERSIZED_IMAGE_TOLERANCE;
    return oversizedWidth || oversizedHeight;
  }
  static \u0275fac = function ImagePerformanceWarning_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ImagePerformanceWarning)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ImagePerformanceWarning,
    factory: _ImagePerformanceWarning.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ImagePerformanceWarning, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
function logLazyLCPWarning(src) {
  console.warn(formatRuntimeError(-913, `An image with src ${src} is the Largest Contentful Paint (LCP) element but was given a "loading" value of "lazy", which can negatively impact application loading performance. This warning can be addressed by changing the loading value of the LCP image to "eager", or by using the NgOptimizedImage directive's prioritization utilities. For more information about addressing or disabling this warning, see ${ERROR_DETAILS_PAGE_BASE_URL}/NG0913`));
}
function logOversizedImageWarning(src) {
  console.warn(formatRuntimeError(-913, `An image with src ${src} has intrinsic file dimensions much larger than its rendered size. This can negatively impact application loading performance. For more information about addressing or disabling this warning, see ${ERROR_DETAILS_PAGE_BASE_URL}/NG0913`));
}
var PLATFORM_DESTROY_LISTENERS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "PlatformDestroyListeners" : "");
var ENABLE_ROOT_COMPONENT_BOOTSTRAP = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "ENABLE_ROOT_COMPONENT_BOOTSTRAP" : "");
function isApplicationBootstrapConfig(config2) {
  return !config2.moduleRef;
}
function bootstrap(config2) {
  const envInjector = isApplicationBootstrapConfig(config2) ? config2.r3Injector : config2.moduleRef.injector;
  const ngZone = envInjector.get(NgZone);
  return ngZone.run(() => {
    if (isApplicationBootstrapConfig(config2)) {
      config2.r3Injector.resolveInjectorInitializers();
    } else {
      config2.moduleRef.resolveInjectorInitializers();
    }
    const exceptionHandler = envInjector.get(INTERNAL_APPLICATION_ERROR_HANDLER);
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (envInjector.get(PROVIDED_ZONELESS) && envInjector.get(PROVIDED_NG_ZONE)) {
        console.warn(formatRuntimeError(408, "Both provideZoneChangeDetection and provideZonelessChangeDetection are provided. This is likely a mistake. Update the application providers to use only one of the two."));
      }
    }
    let onErrorSubscription;
    ngZone.runOutsideAngular(() => {
      onErrorSubscription = ngZone.onError.subscribe({
        next: exceptionHandler
      });
    });
    if (isApplicationBootstrapConfig(config2)) {
      const destroyListener = () => envInjector.destroy();
      const onPlatformDestroyListeners = config2.platformInjector.get(PLATFORM_DESTROY_LISTENERS);
      onPlatformDestroyListeners.add(destroyListener);
      envInjector.onDestroy(() => {
        onErrorSubscription.unsubscribe();
        onPlatformDestroyListeners.delete(destroyListener);
      });
    } else {
      const destroyListener = () => config2.moduleRef.destroy();
      const onPlatformDestroyListeners = config2.platformInjector.get(PLATFORM_DESTROY_LISTENERS);
      onPlatformDestroyListeners.add(destroyListener);
      config2.moduleRef.onDestroy(() => {
        remove(config2.allPlatformModules, config2.moduleRef);
        onErrorSubscription.unsubscribe();
        onPlatformDestroyListeners.delete(destroyListener);
      });
    }
    return _callAndReportToErrorHandler(exceptionHandler, ngZone, () => {
      const pendingTasks = envInjector.get(PendingTasksInternal);
      const taskId = pendingTasks.add();
      const initStatus = envInjector.get(ApplicationInitStatus);
      initStatus.runInitializers();
      return initStatus.donePromise.then(() => {
        const localeId = envInjector.get(LOCALE_ID, DEFAULT_LOCALE_ID);
        setLocaleId(localeId || DEFAULT_LOCALE_ID);
        const enableRootComponentbootstrap = envInjector.get(ENABLE_ROOT_COMPONENT_BOOTSTRAP, true);
        if (!enableRootComponentbootstrap) {
          if (isApplicationBootstrapConfig(config2)) {
            return envInjector.get(ApplicationRef);
          }
          config2.allPlatformModules.push(config2.moduleRef);
          return config2.moduleRef;
        }
        if (typeof ngDevMode === "undefined" || ngDevMode) {
          const imagePerformanceService = envInjector.get(ImagePerformanceWarning);
          imagePerformanceService.start();
        }
        if (isApplicationBootstrapConfig(config2)) {
          const appRef = envInjector.get(ApplicationRef);
          if (config2.rootComponent !== void 0) {
            appRef.bootstrap(config2.rootComponent);
          }
          return appRef;
        } else {
          moduleBootstrapImpl?.(config2.moduleRef, config2.allPlatformModules);
          return config2.moduleRef;
        }
      }).finally(() => void pendingTasks.remove(taskId));
    });
  });
}
var moduleBootstrapImpl;
function setModuleBootstrapImpl() {
  moduleBootstrapImpl = _moduleDoBootstrap;
}
function _moduleDoBootstrap(moduleRef, allPlatformModules) {
  const appRef = moduleRef.injector.get(ApplicationRef);
  if (moduleRef._bootstrapComponents.length > 0) {
    moduleRef._bootstrapComponents.forEach((f) => appRef.bootstrap(f));
  } else if (moduleRef.instance.ngDoBootstrap) {
    moduleRef.instance.ngDoBootstrap(appRef);
  } else {
    throw new RuntimeError(-403, ngDevMode && `The module ${stringify(moduleRef.instance.constructor)} was bootstrapped, but it does not declare "@NgModule.bootstrap" components nor a "ngDoBootstrap" method. Please define one of these.`);
  }
  allPlatformModules.push(moduleRef);
}
function _callAndReportToErrorHandler(errorHandler2, ngZone, callback) {
  try {
    const result = callback();
    if (isPromise2(result)) {
      return result.catch((e) => {
        ngZone.runOutsideAngular(() => errorHandler2(e));
        throw e;
      });
    }
    return result;
  } catch (e) {
    ngZone.runOutsideAngular(() => errorHandler2(e));
    throw e;
  }
}
var PlatformRef = class _PlatformRef {
  _injector;
  _modules = [];
  _destroyListeners = [];
  _destroyed = false;
  constructor(_injector) {
    this._injector = _injector;
  }
  bootstrapModuleFactory(moduleFactory, options) {
    const allAppProviders = [provideZonelessChangeDetectionInternal(), ...options?.applicationProviders ?? [], errorHandlerEnvironmentInitializer, ...ngDevMode ? [validAppIdInitializer] : []];
    const moduleRef = createNgModuleRefWithProviders(moduleFactory.moduleType, this.injector, allAppProviders);
    setModuleBootstrapImpl();
    return bootstrap({
      moduleRef,
      allPlatformModules: this._modules,
      platformInjector: this.injector
    });
  }
  bootstrapModule(moduleType, compilerOptions = []) {
    const options = optionsReducer({}, compilerOptions);
    setModuleBootstrapImpl();
    return compileNgModuleFactory(this.injector, options, moduleType).then((moduleFactory) => this.bootstrapModuleFactory(moduleFactory, options));
  }
  onDestroy(callback) {
    this._destroyListeners.push(callback);
  }
  get injector() {
    return this._injector;
  }
  destroy() {
    if (this._destroyed) {
      throw new RuntimeError(404, ngDevMode && "The platform has already been destroyed!");
    }
    this._modules.slice().forEach((module) => module.destroy());
    this._destroyListeners.forEach((listener) => listener());
    const destroyListeners = this._injector.get(PLATFORM_DESTROY_LISTENERS, null);
    if (destroyListeners) {
      destroyListeners.forEach((listener) => listener());
      destroyListeners.clear();
    }
    this._destroyed = true;
  }
  get destroyed() {
    return this._destroyed;
  }
  static \u0275fac = function PlatformRef_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PlatformRef)(\u0275\u0275inject(Injector));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PlatformRef,
    factory: _PlatformRef.\u0275fac,
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PlatformRef, [{
    type: Injectable,
    args: [{
      providedIn: "platform"
    }]
  }], () => [{
    type: Injector
  }], null);
})();
var _platformInjector = null;
function createPlatform(injector) {
  if (getPlatform()) {
    throw new RuntimeError(400, ngDevMode && "There can be only one platform. Destroy the previous one to create a new one.");
  }
  publishDefaultGlobalUtils();
  publishSignalConfiguration();
  _platformInjector = true ? injector : null;
  const platform = injector.get(PlatformRef);
  runPlatformInitializers(injector);
  return platform;
}
function createPlatformFactory(parentPlatformFactory, name, providers = []) {
  const desc = `Platform: ${name}`;
  const marker = new InjectionToken(desc);
  return (extraProviders = []) => {
    let platform = getPlatform();
    if (!platform) {
      const platformProviders = [...providers, ...extraProviders, {
        provide: marker,
        useValue: true
      }];
      platform = parentPlatformFactory?.(platformProviders) ?? createPlatform(createPlatformInjector(platformProviders, desc));
    }
    return false ? platform : assertPlatform(marker);
  };
}
function createPlatformInjector(providers = [], name) {
  return Injector.create({
    name,
    providers: [{
      provide: INJECTOR_SCOPE,
      useValue: "platform"
    }, {
      provide: PLATFORM_DESTROY_LISTENERS,
      useValue: /* @__PURE__ */ new Set([() => _platformInjector = null])
    }, ...providers]
  });
}
function assertPlatform(requiredToken) {
  const platform = getPlatform();
  if (!platform) {
    throw new RuntimeError(-401, ngDevMode && "No platform exists!");
  }
  if ((typeof ngDevMode === "undefined" || ngDevMode) && !platform.injector.get(requiredToken, null)) {
    throw new RuntimeError(400, "A platform with a different configuration has been created. Please destroy it first.");
  }
  return platform;
}
function getPlatform() {
  if (false) {
    return null;
  }
  return _platformInjector?.get(PlatformRef) ?? null;
}
function createOrReusePlatformInjector(providers = []) {
  if (_platformInjector) return _platformInjector;
  publishDefaultGlobalUtils();
  const injector = createPlatformInjector(providers);
  if (true) {
    _platformInjector = injector;
  }
  publishSignalConfiguration();
  runPlatformInitializers(injector);
  return injector;
}
function runPlatformInitializers(injector) {
  const inits = injector.get(PLATFORM_INITIALIZER, null);
  runInInjectionContext(injector, () => {
    inits?.forEach((init) => init());
  });
}
var APPLICATION_IS_STABLE_TIMEOUT = 1e4;
var STABILITY_WARNING_THRESHOLD = APPLICATION_IS_STABLE_TIMEOUT - 1e3;
var ChangeDetectorRef = class {
  static __NG_ELEMENT_ID__ = injectChangeDetectorRef;
};
function injectChangeDetectorRef(flags) {
  return createViewRef(getCurrentTNode(), getLView(), (flags & 16) === 16);
}
function createViewRef(tNode, lView, isPipe2) {
  if (isComponentHost(tNode) && !isPipe2) {
    const componentView = getComponentLViewByIndex(tNode.index, lView);
    return new ViewRef(componentView, componentView);
  } else if (tNode.type & (3 | 12 | 32 | 128)) {
    const hostComponentView = lView[DECLARATION_COMPONENT_VIEW];
    return new ViewRef(hostComponentView, lView);
  }
  return null;
}
var DefaultIterableDifferFactory = class {
  supports(obj) {
    return isListLikeIterable(obj);
  }
  create(trackByFn) {
    return new DefaultIterableDiffer(trackByFn);
  }
};
var trackByIdentity = (index, item) => item;
var DefaultIterableDiffer = class {
  length = 0;
  collection;
  _linkedRecords = null;
  _unlinkedRecords = null;
  _previousItHead = null;
  _itHead = null;
  _itTail = null;
  _additionsHead = null;
  _additionsTail = null;
  _movesHead = null;
  _movesTail = null;
  _removalsHead = null;
  _removalsTail = null;
  _identityChangesHead = null;
  _identityChangesTail = null;
  _trackByFn;
  constructor(trackByFn) {
    this._trackByFn = trackByFn || trackByIdentity;
  }
  forEachItem(fn) {
    let record;
    for (record = this._itHead; record !== null; record = record._next) {
      fn(record);
    }
  }
  forEachOperation(fn) {
    let nextIt = this._itHead;
    let nextRemove = this._removalsHead;
    let addRemoveOffset = 0;
    let moveOffsets = null;
    while (nextIt || nextRemove) {
      const record = !nextRemove || nextIt && nextIt.currentIndex < getPreviousIndex(nextRemove, addRemoveOffset, moveOffsets) ? nextIt : nextRemove;
      const adjPreviousIndex = getPreviousIndex(record, addRemoveOffset, moveOffsets);
      const currentIndex = record.currentIndex;
      if (record === nextRemove) {
        addRemoveOffset--;
        nextRemove = nextRemove._nextRemoved;
      } else {
        nextIt = nextIt._next;
        if (record.previousIndex == null) {
          addRemoveOffset++;
        } else {
          if (!moveOffsets) moveOffsets = [];
          const localMovePreviousIndex = adjPreviousIndex - addRemoveOffset;
          const localCurrentIndex = currentIndex - addRemoveOffset;
          if (localMovePreviousIndex != localCurrentIndex) {
            for (let i = 0; i < localMovePreviousIndex; i++) {
              const offset = i < moveOffsets.length ? moveOffsets[i] : moveOffsets[i] = 0;
              const index = offset + i;
              if (localCurrentIndex <= index && index < localMovePreviousIndex) {
                moveOffsets[i] = offset + 1;
              }
            }
            const previousIndex = record.previousIndex;
            moveOffsets[previousIndex] = localCurrentIndex - localMovePreviousIndex;
          }
        }
      }
      if (adjPreviousIndex !== currentIndex) {
        fn(record, adjPreviousIndex, currentIndex);
      }
    }
  }
  forEachPreviousItem(fn) {
    let record;
    for (record = this._previousItHead; record !== null; record = record._nextPrevious) {
      fn(record);
    }
  }
  forEachAddedItem(fn) {
    let record;
    for (record = this._additionsHead; record !== null; record = record._nextAdded) {
      fn(record);
    }
  }
  forEachMovedItem(fn) {
    let record;
    for (record = this._movesHead; record !== null; record = record._nextMoved) {
      fn(record);
    }
  }
  forEachRemovedItem(fn) {
    let record;
    for (record = this._removalsHead; record !== null; record = record._nextRemoved) {
      fn(record);
    }
  }
  forEachIdentityChange(fn) {
    let record;
    for (record = this._identityChangesHead; record !== null; record = record._nextIdentityChange) {
      fn(record);
    }
  }
  diff(collection) {
    if (collection == null) collection = [];
    if (!isListLikeIterable(collection)) {
      throw new RuntimeError(900, ngDevMode && `Error trying to diff '${stringify(collection)}'. Only arrays and iterables are allowed`);
    }
    if (this.check(collection)) {
      return this;
    } else {
      return null;
    }
  }
  onDestroy() {
  }
  check(collection) {
    this._reset();
    let record = this._itHead;
    let mayBeDirty = false;
    let index;
    let item;
    let itemTrackBy;
    if (Array.isArray(collection)) {
      this.length = collection.length;
      for (let index2 = 0; index2 < this.length; index2++) {
        item = collection[index2];
        itemTrackBy = this._trackByFn(index2, item);
        if (record === null || !Object.is(record.trackById, itemTrackBy)) {
          record = this._mismatch(record, item, itemTrackBy, index2);
          mayBeDirty = true;
        } else {
          if (mayBeDirty) {
            record = this._verifyReinsertion(record, item, itemTrackBy, index2);
          }
          if (!Object.is(record.item, item)) this._addIdentityChange(record, item);
        }
        record = record._next;
      }
    } else {
      index = 0;
      iterateListLike(collection, (item2) => {
        itemTrackBy = this._trackByFn(index, item2);
        if (record === null || !Object.is(record.trackById, itemTrackBy)) {
          record = this._mismatch(record, item2, itemTrackBy, index);
          mayBeDirty = true;
        } else {
          if (mayBeDirty) {
            record = this._verifyReinsertion(record, item2, itemTrackBy, index);
          }
          if (!Object.is(record.item, item2)) this._addIdentityChange(record, item2);
        }
        record = record._next;
        index++;
      });
      this.length = index;
    }
    this._truncate(record);
    this.collection = collection;
    return this.isDirty;
  }
  get isDirty() {
    return this._additionsHead !== null || this._movesHead !== null || this._removalsHead !== null || this._identityChangesHead !== null;
  }
  _reset() {
    if (this.isDirty) {
      let record;
      for (record = this._previousItHead = this._itHead; record !== null; record = record._next) {
        record._nextPrevious = record._next;
      }
      for (record = this._additionsHead; record !== null; record = record._nextAdded) {
        record.previousIndex = record.currentIndex;
      }
      this._additionsHead = this._additionsTail = null;
      for (record = this._movesHead; record !== null; record = record._nextMoved) {
        record.previousIndex = record.currentIndex;
      }
      this._movesHead = this._movesTail = null;
      this._removalsHead = this._removalsTail = null;
      this._identityChangesHead = this._identityChangesTail = null;
    }
  }
  _mismatch(record, item, itemTrackBy, index) {
    let previousRecord;
    if (record === null) {
      previousRecord = this._itTail;
    } else {
      previousRecord = record._prev;
      this._remove(record);
    }
    record = this._unlinkedRecords === null ? null : this._unlinkedRecords.get(itemTrackBy, null);
    if (record !== null) {
      if (!Object.is(record.item, item)) this._addIdentityChange(record, item);
      this._reinsertAfter(record, previousRecord, index);
    } else {
      record = this._linkedRecords === null ? null : this._linkedRecords.get(itemTrackBy, index);
      if (record !== null) {
        if (!Object.is(record.item, item)) this._addIdentityChange(record, item);
        this._moveAfter(record, previousRecord, index);
      } else {
        record = this._addAfter(new IterableChangeRecord_(item, itemTrackBy), previousRecord, index);
      }
    }
    return record;
  }
  _verifyReinsertion(record, item, itemTrackBy, index) {
    let reinsertRecord = this._unlinkedRecords === null ? null : this._unlinkedRecords.get(itemTrackBy, null);
    if (reinsertRecord !== null) {
      record = this._reinsertAfter(reinsertRecord, record._prev, index);
    } else if (record.currentIndex != index) {
      record.currentIndex = index;
      this._addToMoves(record, index);
    }
    return record;
  }
  _truncate(record) {
    while (record !== null) {
      const nextRecord = record._next;
      this._addToRemovals(this._unlink(record));
      record = nextRecord;
    }
    if (this._unlinkedRecords !== null) {
      this._unlinkedRecords.clear();
    }
    if (this._additionsTail !== null) {
      this._additionsTail._nextAdded = null;
    }
    if (this._movesTail !== null) {
      this._movesTail._nextMoved = null;
    }
    if (this._itTail !== null) {
      this._itTail._next = null;
    }
    if (this._removalsTail !== null) {
      this._removalsTail._nextRemoved = null;
    }
    if (this._identityChangesTail !== null) {
      this._identityChangesTail._nextIdentityChange = null;
    }
  }
  _reinsertAfter(record, prevRecord, index) {
    if (this._unlinkedRecords !== null) {
      this._unlinkedRecords.remove(record);
    }
    const prev = record._prevRemoved;
    const next = record._nextRemoved;
    if (prev === null) {
      this._removalsHead = next;
    } else {
      prev._nextRemoved = next;
    }
    if (next === null) {
      this._removalsTail = prev;
    } else {
      next._prevRemoved = prev;
    }
    this._insertAfter(record, prevRecord, index);
    this._addToMoves(record, index);
    return record;
  }
  _moveAfter(record, prevRecord, index) {
    this._unlink(record);
    this._insertAfter(record, prevRecord, index);
    this._addToMoves(record, index);
    return record;
  }
  _addAfter(record, prevRecord, index) {
    this._insertAfter(record, prevRecord, index);
    if (this._additionsTail === null) {
      this._additionsTail = this._additionsHead = record;
    } else {
      this._additionsTail = this._additionsTail._nextAdded = record;
    }
    return record;
  }
  _insertAfter(record, prevRecord, index) {
    const next = prevRecord === null ? this._itHead : prevRecord._next;
    record._next = next;
    record._prev = prevRecord;
    if (next === null) {
      this._itTail = record;
    } else {
      next._prev = record;
    }
    if (prevRecord === null) {
      this._itHead = record;
    } else {
      prevRecord._next = record;
    }
    if (this._linkedRecords === null) {
      this._linkedRecords = new _DuplicateMap();
    }
    this._linkedRecords.put(record);
    record.currentIndex = index;
    return record;
  }
  _remove(record) {
    return this._addToRemovals(this._unlink(record));
  }
  _unlink(record) {
    if (this._linkedRecords !== null) {
      this._linkedRecords.remove(record);
    }
    const prev = record._prev;
    const next = record._next;
    if (prev === null) {
      this._itHead = next;
    } else {
      prev._next = next;
    }
    if (next === null) {
      this._itTail = prev;
    } else {
      next._prev = prev;
    }
    return record;
  }
  _addToMoves(record, toIndex) {
    if (record.previousIndex === toIndex) {
      return record;
    }
    if (this._movesTail === null) {
      this._movesTail = this._movesHead = record;
    } else {
      this._movesTail = this._movesTail._nextMoved = record;
    }
    return record;
  }
  _addToRemovals(record) {
    if (this._unlinkedRecords === null) {
      this._unlinkedRecords = new _DuplicateMap();
    }
    this._unlinkedRecords.put(record);
    record.currentIndex = null;
    record._nextRemoved = null;
    if (this._removalsTail === null) {
      this._removalsTail = this._removalsHead = record;
      record._prevRemoved = null;
    } else {
      record._prevRemoved = this._removalsTail;
      this._removalsTail = this._removalsTail._nextRemoved = record;
    }
    return record;
  }
  _addIdentityChange(record, item) {
    record.item = item;
    if (this._identityChangesTail === null) {
      this._identityChangesTail = this._identityChangesHead = record;
    } else {
      this._identityChangesTail = this._identityChangesTail._nextIdentityChange = record;
    }
    return record;
  }
};
var IterableChangeRecord_ = class {
  item;
  trackById;
  currentIndex = null;
  previousIndex = null;
  _nextPrevious = null;
  _prev = null;
  _next = null;
  _prevDup = null;
  _nextDup = null;
  _prevRemoved = null;
  _nextRemoved = null;
  _nextAdded = null;
  _nextMoved = null;
  _nextIdentityChange = null;
  constructor(item, trackById) {
    this.item = item;
    this.trackById = trackById;
  }
};
var _DuplicateItemRecordList = class {
  _head = null;
  _tail = null;
  add(record) {
    if (this._head === null) {
      this._head = this._tail = record;
      record._nextDup = null;
      record._prevDup = null;
    } else {
      this._tail._nextDup = record;
      record._prevDup = this._tail;
      record._nextDup = null;
      this._tail = record;
    }
  }
  get(trackById, atOrAfterIndex) {
    let record;
    for (record = this._head; record !== null; record = record._nextDup) {
      if ((atOrAfterIndex === null || atOrAfterIndex <= record.currentIndex) && Object.is(record.trackById, trackById)) {
        return record;
      }
    }
    return null;
  }
  remove(record) {
    const prev = record._prevDup;
    const next = record._nextDup;
    if (prev === null) {
      this._head = next;
    } else {
      prev._nextDup = next;
    }
    if (next === null) {
      this._tail = prev;
    } else {
      next._prevDup = prev;
    }
    return this._head === null;
  }
};
var _DuplicateMap = class {
  map = /* @__PURE__ */ new Map();
  put(record) {
    const key = record.trackById;
    let duplicates = this.map.get(key);
    if (!duplicates) {
      duplicates = new _DuplicateItemRecordList();
      this.map.set(key, duplicates);
    }
    duplicates.add(record);
  }
  get(trackById, atOrAfterIndex) {
    const key = trackById;
    const recordList = this.map.get(key);
    return recordList ? recordList.get(trackById, atOrAfterIndex) : null;
  }
  remove(record) {
    const key = record.trackById;
    const recordList = this.map.get(key);
    if (recordList.remove(record)) {
      this.map.delete(key);
    }
    return record;
  }
  get isEmpty() {
    return this.map.size === 0;
  }
  clear() {
    this.map.clear();
  }
};
function getPreviousIndex(item, addRemoveOffset, moveOffsets) {
  const previousIndex = item.previousIndex;
  if (previousIndex === null) return previousIndex;
  let moveOffset = 0;
  if (moveOffsets && previousIndex < moveOffsets.length) {
    moveOffset = moveOffsets[previousIndex];
  }
  return previousIndex + addRemoveOffset + moveOffset;
}
var DefaultKeyValueDifferFactory = class {
  supports(obj) {
    return obj instanceof Map || isJsObject(obj);
  }
  create() {
    return new DefaultKeyValueDiffer();
  }
};
var DefaultKeyValueDiffer = class {
  _records = /* @__PURE__ */ new Map();
  _mapHead = null;
  _appendAfter = null;
  _previousMapHead = null;
  _changesHead = null;
  _changesTail = null;
  _additionsHead = null;
  _additionsTail = null;
  _removalsHead = null;
  get isDirty() {
    return this._additionsHead !== null || this._changesHead !== null || this._removalsHead !== null;
  }
  forEachItem(fn) {
    let record;
    for (record = this._mapHead; record !== null; record = record._next) {
      fn(record);
    }
  }
  forEachPreviousItem(fn) {
    let record;
    for (record = this._previousMapHead; record !== null; record = record._nextPrevious) {
      fn(record);
    }
  }
  forEachChangedItem(fn) {
    let record;
    for (record = this._changesHead; record !== null; record = record._nextChanged) {
      fn(record);
    }
  }
  forEachAddedItem(fn) {
    let record;
    for (record = this._additionsHead; record !== null; record = record._nextAdded) {
      fn(record);
    }
  }
  forEachRemovedItem(fn) {
    let record;
    for (record = this._removalsHead; record !== null; record = record._nextRemoved) {
      fn(record);
    }
  }
  diff(map2) {
    if (!map2) {
      map2 = /* @__PURE__ */ new Map();
    } else if (!(map2 instanceof Map || isJsObject(map2))) {
      throw new RuntimeError(900, ngDevMode && `Error trying to diff '${stringify(map2)}'. Only maps and objects are allowed`);
    }
    return this.check(map2) ? this : null;
  }
  check(map2) {
    this._reset();
    let insertBefore = this._mapHead;
    this._appendAfter = null;
    this._forEach(map2, (value, key) => {
      if (insertBefore && insertBefore.key === key) {
        this._maybeAddToChanges(insertBefore, value);
        this._appendAfter = insertBefore;
        insertBefore = insertBefore._next;
      } else {
        const record = this._getOrCreateRecordForKey(key, value);
        insertBefore = this._insertBeforeOrAppend(insertBefore, record);
      }
    });
    if (insertBefore) {
      if (insertBefore._prev) {
        insertBefore._prev._next = null;
      }
      this._removalsHead = insertBefore;
      for (let record = insertBefore; record !== null; record = record._nextRemoved) {
        if (record === this._mapHead) {
          this._mapHead = null;
        }
        this._records.delete(record.key);
        record._nextRemoved = record._next;
        record.previousValue = record.currentValue;
        record.currentValue = null;
        record._prev = null;
        record._next = null;
      }
    }
    if (this._changesTail) this._changesTail._nextChanged = null;
    if (this._additionsTail) this._additionsTail._nextAdded = null;
    return this.isDirty;
  }
  _insertBeforeOrAppend(before, record) {
    if (before) {
      const prev = before._prev;
      record._next = before;
      record._prev = prev;
      before._prev = record;
      if (prev) {
        prev._next = record;
      }
      if (before === this._mapHead) {
        this._mapHead = record;
      }
      this._appendAfter = before;
      return before;
    }
    if (this._appendAfter) {
      this._appendAfter._next = record;
      record._prev = this._appendAfter;
    } else {
      this._mapHead = record;
    }
    this._appendAfter = record;
    return null;
  }
  _getOrCreateRecordForKey(key, value) {
    if (this._records.has(key)) {
      const record2 = this._records.get(key);
      this._maybeAddToChanges(record2, value);
      const prev = record2._prev;
      const next = record2._next;
      if (prev) {
        prev._next = next;
      }
      if (next) {
        next._prev = prev;
      }
      record2._next = null;
      record2._prev = null;
      return record2;
    }
    const record = new KeyValueChangeRecord_(key);
    this._records.set(key, record);
    record.currentValue = value;
    this._addToAdditions(record);
    return record;
  }
  _reset() {
    if (this.isDirty) {
      let record;
      this._previousMapHead = this._mapHead;
      for (record = this._previousMapHead; record !== null; record = record._next) {
        record._nextPrevious = record._next;
      }
      for (record = this._changesHead; record !== null; record = record._nextChanged) {
        record.previousValue = record.currentValue;
      }
      for (record = this._additionsHead; record != null; record = record._nextAdded) {
        record.previousValue = record.currentValue;
      }
      this._changesHead = this._changesTail = null;
      this._additionsHead = this._additionsTail = null;
      this._removalsHead = null;
    }
  }
  _maybeAddToChanges(record, newValue) {
    if (!Object.is(newValue, record.currentValue)) {
      record.previousValue = record.currentValue;
      record.currentValue = newValue;
      this._addToChanges(record);
    }
  }
  _addToAdditions(record) {
    if (this._additionsHead === null) {
      this._additionsHead = this._additionsTail = record;
    } else {
      this._additionsTail._nextAdded = record;
      this._additionsTail = record;
    }
  }
  _addToChanges(record) {
    if (this._changesHead === null) {
      this._changesHead = this._changesTail = record;
    } else {
      this._changesTail._nextChanged = record;
      this._changesTail = record;
    }
  }
  _forEach(obj, fn) {
    if (obj instanceof Map) {
      obj.forEach(fn);
    } else {
      Object.keys(obj).forEach((k) => fn(obj[k], k));
    }
  }
};
var KeyValueChangeRecord_ = class {
  key;
  previousValue = null;
  currentValue = null;
  _nextPrevious = null;
  _next = null;
  _prev = null;
  _nextAdded = null;
  _nextRemoved = null;
  _nextChanged = null;
  constructor(key) {
    this.key = key;
  }
};
function defaultIterableDiffersFactory() {
  return new IterableDiffers([new DefaultIterableDifferFactory()]);
}
var IterableDiffers = class _IterableDiffers {
  factories;
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _IterableDiffers,
    providedIn: "root",
    factory: defaultIterableDiffersFactory
  });
  constructor(factories) {
    this.factories = factories;
  }
  static create(factories, parent) {
    if (parent != null) {
      const copied = parent.factories.slice();
      factories = factories.concat(copied);
    }
    return new _IterableDiffers(factories);
  }
  static extend(factories) {
    return {
      provide: _IterableDiffers,
      useFactory: () => {
        const parent = inject2(_IterableDiffers, {
          optional: true,
          skipSelf: true
        });
        return _IterableDiffers.create(factories, parent || defaultIterableDiffersFactory());
      }
    };
  }
  find(iterable) {
    const factory = this.factories.find((f) => f.supports(iterable));
    if (factory != null) {
      return factory;
    } else {
      throw new RuntimeError(901, ngDevMode && `Cannot find a differ supporting object '${iterable}' of type '${getTypeNameForDebugging(iterable)}'`);
    }
  }
};
function getTypeNameForDebugging(type) {
  return type["name"] || typeof type;
}
function defaultKeyValueDiffersFactory() {
  return new KeyValueDiffers([new DefaultKeyValueDifferFactory()]);
}
var KeyValueDiffers = class _KeyValueDiffers {
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _KeyValueDiffers,
    providedIn: "root",
    factory: defaultKeyValueDiffersFactory
  });
  factories;
  constructor(factories) {
    this.factories = factories;
  }
  static create(factories, parent) {
    if (parent) {
      const copied = parent.factories.slice();
      factories = factories.concat(copied);
    }
    return new _KeyValueDiffers(factories);
  }
  static extend(factories) {
    return {
      provide: _KeyValueDiffers,
      useFactory: () => {
        const parent = inject2(_KeyValueDiffers, {
          optional: true,
          skipSelf: true
        });
        return _KeyValueDiffers.create(factories, parent || defaultKeyValueDiffersFactory());
      }
    };
  }
  find(kv) {
    const factory = this.factories.find((f) => f.supports(kv));
    if (factory) {
      return factory;
    }
    throw new RuntimeError(901, ngDevMode && `Cannot find a differ supporting object '${kv}'`);
  }
};
var keyValDiff = [new DefaultKeyValueDifferFactory()];
var iterableDiff = [new DefaultIterableDifferFactory()];
var defaultIterableDiffers = new IterableDiffers(iterableDiff);
var defaultKeyValueDiffers = new KeyValueDiffers(keyValDiff);
var platformCore = createPlatformFactory(null, "core", []);
var ApplicationModule = class _ApplicationModule {
  constructor(appRef) {
  }
  static \u0275fac = function ApplicationModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ApplicationModule)(\u0275\u0275inject(ApplicationRef));
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _ApplicationModule
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ApplicationModule, [{
    type: NgModule
  }], () => [{
    type: ApplicationRef
  }], null);
})();
function internalCreateApplication(config2) {
  const {
    rootComponent,
    appProviders,
    platformProviders,
    platformRef
  } = config2;
  profiler(ProfilerEvent.BootstrapApplicationStart);
  if (false) {
    throw new RuntimeError(-401, ngDevMode && "Missing Platform: This may be due to using `bootstrapApplication` on the server without passing a `BootstrapContext`. Please make sure that `bootstrapApplication` is called with a `context` argument.");
  }
  try {
    const platformInjector = platformRef?.injector ?? createOrReusePlatformInjector(platformProviders);
    if ((typeof ngDevMode === "undefined" || ngDevMode) && rootComponent !== void 0) {
      assertStandaloneComponentType(rootComponent);
    }
    const allAppProviders = [provideZonelessChangeDetectionInternal(), errorHandlerEnvironmentInitializer, ...ngDevMode ? [validAppIdInitializer] : [], ...appProviders || []];
    const adapter = new EnvironmentNgModuleRefAdapter({
      providers: allAppProviders,
      parent: platformInjector,
      debugName: typeof ngDevMode === "undefined" || ngDevMode ? "Environment Injector" : "",
      runEnvironmentInitializers: false
    });
    return bootstrap({
      r3Injector: adapter.injector,
      platformInjector,
      rootComponent
    });
  } catch (e) {
    return Promise.reject(e);
  } finally {
    profiler(ProfilerEvent.BootstrapApplicationEnd);
  }
}
function booleanAttribute(value) {
  return typeof value === "boolean" ? value : value != null && value !== "false";
}
function numberAttribute(value, fallbackValue = NaN) {
  const isNumberValue = !isNaN(parseFloat(value)) && !isNaN(Number(value));
  return isNumberValue ? Number(value) : fallbackValue;
}
function reflectComponentType(component) {
  const componentDef = getComponentDef(component);
  if (!componentDef) return null;
  const factory = new ComponentFactory2(componentDef);
  return {
    get selector() {
      return factory.selector;
    },
    get type() {
      return factory.componentType;
    },
    get inputs() {
      return factory.inputs;
    },
    get outputs() {
      return factory.outputs;
    },
    get ngContentSelectors() {
      return factory.ngContentSelectors;
    },
    get isStandalone() {
      return componentDef.standalone;
    },
    get isSignal() {
      return componentDef.signals;
    }
  };
}
var REQUEST = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "REQUEST" : "", {
  providedIn: "platform",
  factory: () => null
});
var RESPONSE_INIT = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "RESPONSE_INIT" : "", {
  providedIn: "platform",
  factory: () => null
});
var REQUEST_CONTEXT = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "REQUEST_CONTEXT" : "", {
  providedIn: "platform",
  factory: () => null
});

// node_modules/@angular/common/fesm2022/_platform_location-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var _DOM = null;
function getDOM() {
  return _DOM;
}
function setRootDomAdapter(adapter) {
  _DOM ??= adapter;
}
var DomAdapter = class {
};
var PlatformLocation = class _PlatformLocation {
  historyGo(relativePosition) {
    throw new Error(ngDevMode ? "Not implemented" : "");
  }
  static \u0275fac = function PlatformLocation_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PlatformLocation)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PlatformLocation,
    factory: () => (() => inject2(BrowserPlatformLocation))(),
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PlatformLocation, [{
    type: Injectable,
    args: [{
      providedIn: "platform",
      useFactory: () => inject2(BrowserPlatformLocation)
    }]
  }], null, null);
})();
var LOCATION_INITIALIZED = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "Location Initialized" : "");
var BrowserPlatformLocation = class _BrowserPlatformLocation extends PlatformLocation {
  _location;
  _history;
  _doc = inject2(DOCUMENT);
  constructor() {
    super();
    this._location = window.location;
    this._history = window.history;
  }
  getBaseHrefFromDOM() {
    return getDOM().getBaseHref(this._doc);
  }
  onPopState(fn) {
    const window2 = getDOM().getGlobalEventTarget(this._doc, "window");
    window2.addEventListener("popstate", fn, false);
    return () => window2.removeEventListener("popstate", fn);
  }
  onHashChange(fn) {
    const window2 = getDOM().getGlobalEventTarget(this._doc, "window");
    window2.addEventListener("hashchange", fn, false);
    return () => window2.removeEventListener("hashchange", fn);
  }
  get href() {
    return this._location.href;
  }
  get protocol() {
    return this._location.protocol;
  }
  get hostname() {
    return this._location.hostname;
  }
  get port() {
    return this._location.port;
  }
  get pathname() {
    return this._location.pathname;
  }
  get search() {
    return this._location.search;
  }
  get hash() {
    return this._location.hash;
  }
  set pathname(newPath) {
    this._location.pathname = newPath;
  }
  pushState(state, title, url) {
    this._history.pushState(state, title, url);
  }
  replaceState(state, title, url) {
    this._history.replaceState(state, title, url);
  }
  forward() {
    this._history.forward();
  }
  back() {
    this._history.back();
  }
  historyGo(relativePosition = 0) {
    this._history.go(relativePosition);
  }
  getState() {
    return this._history.state;
  }
  static \u0275fac = function BrowserPlatformLocation_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BrowserPlatformLocation)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _BrowserPlatformLocation,
    factory: () => (() => new _BrowserPlatformLocation())(),
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BrowserPlatformLocation, [{
    type: Injectable,
    args: [{
      providedIn: "platform",
      useFactory: () => new BrowserPlatformLocation()
    }]
  }], () => [], null);
})();

// node_modules/@angular/common/fesm2022/_location-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
function joinWithSlash(start, end) {
  if (!start) return end;
  if (!end) return start;
  if (start.endsWith("/")) {
    return end.startsWith("/") ? start + end.slice(1) : start + end;
  }
  return end.startsWith("/") ? start + end : `${start}/${end}`;
}
function stripTrailingSlash(url) {
  const pathEndIdx = url.search(/#|\?|$/);
  return url[pathEndIdx - 1] === "/" ? url.slice(0, pathEndIdx - 1) + url.slice(pathEndIdx) : url;
}
function normalizeQueryParams(params) {
  return params && params[0] !== "?" ? `?${params}` : params;
}
var LocationStrategy = class _LocationStrategy {
  historyGo(relativePosition) {
    throw new Error(ngDevMode ? "Not implemented" : "");
  }
  static \u0275fac = function LocationStrategy_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _LocationStrategy)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _LocationStrategy,
    factory: () => (() => inject2(PathLocationStrategy))(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LocationStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: () => inject2(PathLocationStrategy)
    }]
  }], null, null);
})();
var APP_BASE_HREF = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "appBaseHref" : "");
var PathLocationStrategy = class _PathLocationStrategy extends LocationStrategy {
  _platformLocation;
  _baseHref;
  _removeListenerFns = [];
  constructor(_platformLocation, href) {
    super();
    this._platformLocation = _platformLocation;
    this._baseHref = href ?? this._platformLocation.getBaseHrefFromDOM() ?? inject2(DOCUMENT).location?.origin ?? "";
  }
  ngOnDestroy() {
    while (this._removeListenerFns.length) {
      this._removeListenerFns.pop()();
    }
  }
  onPopState(fn) {
    this._removeListenerFns.push(this._platformLocation.onPopState(fn), this._platformLocation.onHashChange(fn));
  }
  getBaseHref() {
    return this._baseHref;
  }
  prepareExternalUrl(internal) {
    return joinWithSlash(this._baseHref, internal);
  }
  path(includeHash = false) {
    const pathname = this._platformLocation.pathname + normalizeQueryParams(this._platformLocation.search);
    const hash = this._platformLocation.hash;
    return hash && includeHash ? `${pathname}${hash}` : pathname;
  }
  pushState(state, title, url, queryParams) {
    const externalUrl = this.prepareExternalUrl(url + normalizeQueryParams(queryParams));
    this._platformLocation.pushState(state, title, externalUrl);
  }
  replaceState(state, title, url, queryParams) {
    const externalUrl = this.prepareExternalUrl(url + normalizeQueryParams(queryParams));
    this._platformLocation.replaceState(state, title, externalUrl);
  }
  forward() {
    this._platformLocation.forward();
  }
  back() {
    this._platformLocation.back();
  }
  getState() {
    return this._platformLocation.getState();
  }
  historyGo(relativePosition = 0) {
    this._platformLocation.historyGo?.(relativePosition);
  }
  static \u0275fac = function PathLocationStrategy_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PathLocationStrategy)(\u0275\u0275inject(PlatformLocation), \u0275\u0275inject(APP_BASE_HREF, 8));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PathLocationStrategy,
    factory: _PathLocationStrategy.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PathLocationStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PlatformLocation
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [APP_BASE_HREF]
    }]
  }], null);
})();
var NoTrailingSlashPathLocationStrategy = class _NoTrailingSlashPathLocationStrategy extends PathLocationStrategy {
  prepareExternalUrl(internal) {
    const path = extractUrlPath(internal);
    if (path.endsWith("/") && path.length > 1) {
      internal = path.slice(0, -1) + internal.slice(path.length);
    }
    return super.prepareExternalUrl(internal);
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275NoTrailingSlashPathLocationStrategy_BaseFactory;
    return function NoTrailingSlashPathLocationStrategy_Factory(__ngFactoryType__) {
      return (\u0275NoTrailingSlashPathLocationStrategy_BaseFactory || (\u0275NoTrailingSlashPathLocationStrategy_BaseFactory = \u0275\u0275getInheritedFactory(_NoTrailingSlashPathLocationStrategy)))(__ngFactoryType__ || _NoTrailingSlashPathLocationStrategy);
    };
  })();
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NoTrailingSlashPathLocationStrategy,
    factory: _NoTrailingSlashPathLocationStrategy.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NoTrailingSlashPathLocationStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var TrailingSlashPathLocationStrategy = class _TrailingSlashPathLocationStrategy extends PathLocationStrategy {
  prepareExternalUrl(internal) {
    const path = extractUrlPath(internal);
    if (!path.endsWith("/")) {
      internal = path + "/" + internal.slice(path.length);
    }
    return super.prepareExternalUrl(internal);
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275TrailingSlashPathLocationStrategy_BaseFactory;
    return function TrailingSlashPathLocationStrategy_Factory(__ngFactoryType__) {
      return (\u0275TrailingSlashPathLocationStrategy_BaseFactory || (\u0275TrailingSlashPathLocationStrategy_BaseFactory = \u0275\u0275getInheritedFactory(_TrailingSlashPathLocationStrategy)))(__ngFactoryType__ || _TrailingSlashPathLocationStrategy);
    };
  })();
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _TrailingSlashPathLocationStrategy,
    factory: _TrailingSlashPathLocationStrategy.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TrailingSlashPathLocationStrategy, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
function extractUrlPath(url) {
  const questionMarkOrHashIndex = url.search(/[?#]/);
  const pathEnd = questionMarkOrHashIndex > -1 ? questionMarkOrHashIndex : url.length;
  return url.slice(0, pathEnd);
}
var Location = class _Location {
  _subject = new Subject();
  _basePath;
  _locationStrategy;
  _urlChangeListeners = [];
  _urlChangeSubscription = null;
  constructor(locationStrategy) {
    this._locationStrategy = locationStrategy;
    const baseHref = this._locationStrategy.getBaseHref();
    this._basePath = _stripOrigin(stripTrailingSlash(_stripIndexHtml(baseHref)));
    this._locationStrategy.onPopState((ev) => {
      this._subject.next({
        "url": this.path(true),
        "pop": true,
        "state": ev.state,
        "type": ev.type
      });
    });
  }
  ngOnDestroy() {
    this._urlChangeSubscription?.unsubscribe();
    this._urlChangeListeners = [];
  }
  path(includeHash = false) {
    return this.normalize(this._locationStrategy.path(includeHash));
  }
  getState() {
    return this._locationStrategy.getState();
  }
  isCurrentPathEqualTo(path, query = "") {
    return this.path() == this.normalize(path + normalizeQueryParams(query));
  }
  normalize(url) {
    return _Location.stripTrailingSlash(_stripBasePath(this._basePath, _stripIndexHtml(url)));
  }
  prepareExternalUrl(url) {
    if (url && url[0] !== "/") {
      url = "/" + url;
    }
    return this._locationStrategy.prepareExternalUrl(url);
  }
  go(path, query = "", state = null) {
    this._locationStrategy.pushState(state, "", path, query);
    this._notifyUrlChangeListeners(this.prepareExternalUrl(path + normalizeQueryParams(query)), state);
  }
  replaceState(path, query = "", state = null) {
    this._locationStrategy.replaceState(state, "", path, query);
    this._notifyUrlChangeListeners(this.prepareExternalUrl(path + normalizeQueryParams(query)), state);
  }
  forward() {
    this._locationStrategy.forward();
  }
  back() {
    this._locationStrategy.back();
  }
  historyGo(relativePosition = 0) {
    this._locationStrategy.historyGo?.(relativePosition);
  }
  onUrlChange(fn) {
    this._urlChangeListeners.push(fn);
    this._urlChangeSubscription ??= this.subscribe((v) => {
      this._notifyUrlChangeListeners(v.url, v.state);
    });
    return () => {
      const fnIndex = this._urlChangeListeners.indexOf(fn);
      this._urlChangeListeners.splice(fnIndex, 1);
      if (this._urlChangeListeners.length === 0) {
        this._urlChangeSubscription?.unsubscribe();
        this._urlChangeSubscription = null;
      }
    };
  }
  _notifyUrlChangeListeners(url = "", state) {
    this._urlChangeListeners.forEach((fn) => fn(url, state));
  }
  subscribe(onNext, onThrow, onReturn) {
    return this._subject.subscribe({
      next: onNext,
      error: onThrow ?? void 0,
      complete: onReturn ?? void 0
    });
  }
  static normalizeQueryParams = normalizeQueryParams;
  static joinWithSlash = joinWithSlash;
  static stripTrailingSlash = stripTrailingSlash;
  static \u0275fac = function Location_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Location)(\u0275\u0275inject(LocationStrategy));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Location,
    factory: () => createLocation(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Location, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: createLocation
    }]
  }], () => [{
    type: LocationStrategy
  }], null);
})();
function createLocation() {
  return new Location(\u0275\u0275inject(LocationStrategy));
}
function _stripBasePath(basePath, url) {
  if (!basePath || !url.startsWith(basePath)) {
    return url;
  }
  const strippedUrl = url.substring(basePath.length);
  if (strippedUrl === "" || ["/", ";", "?", "#"].includes(strippedUrl[0])) {
    return strippedUrl;
  }
  return url;
}
function _stripIndexHtml(url) {
  return url.replace(/\/index\.html$/, "");
}
function _stripOrigin(baseHref) {
  const isAbsoluteUrl2 = new RegExp("^(https?:)?//").test(baseHref);
  if (isAbsoluteUrl2) {
    const [, pathname] = baseHref.split(/\/\/[^\/]+/);
    return pathname;
  }
  return baseHref;
}

// node_modules/@angular/common/fesm2022/_common_module-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var HashLocationStrategy = class _HashLocationStrategy extends LocationStrategy {
  _platformLocation;
  _baseHref = "";
  _removeListenerFns = [];
  constructor(_platformLocation, _baseHref) {
    super();
    this._platformLocation = _platformLocation;
    if (_baseHref != null) {
      this._baseHref = _baseHref;
    }
  }
  ngOnDestroy() {
    while (this._removeListenerFns.length) {
      this._removeListenerFns.pop()();
    }
  }
  onPopState(fn) {
    this._removeListenerFns.push(this._platformLocation.onPopState(fn), this._platformLocation.onHashChange(fn));
  }
  getBaseHref() {
    return this._baseHref;
  }
  path(includeHash = false) {
    const path = this._platformLocation.hash ?? "#";
    return path.length > 0 ? path.substring(1) : path;
  }
  prepareExternalUrl(internal) {
    const url = joinWithSlash(this._baseHref, internal);
    return url.length > 0 ? "#" + url : url;
  }
  pushState(state, title, path, queryParams) {
    const url = this.prepareExternalUrl(path + normalizeQueryParams(queryParams)) || this._platformLocation.pathname;
    this._platformLocation.pushState(state, title, url);
  }
  replaceState(state, title, path, queryParams) {
    const url = this.prepareExternalUrl(path + normalizeQueryParams(queryParams)) || this._platformLocation.pathname;
    this._platformLocation.replaceState(state, title, url);
  }
  forward() {
    this._platformLocation.forward();
  }
  back() {
    this._platformLocation.back();
  }
  getState() {
    return this._platformLocation.getState();
  }
  historyGo(relativePosition = 0) {
    this._platformLocation.historyGo?.(relativePosition);
  }
  static \u0275fac = function HashLocationStrategy_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _HashLocationStrategy)(\u0275\u0275inject(PlatformLocation), \u0275\u0275inject(APP_BASE_HREF, 8));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _HashLocationStrategy,
    factory: _HashLocationStrategy.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HashLocationStrategy, [{
    type: Injectable
  }], () => [{
    type: PlatformLocation
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [APP_BASE_HREF]
    }]
  }], null);
})();
var CURRENCIES_EN = {
  "ADP": [void 0, void 0, 0],
  "AFN": [void 0, "\u060B", 0],
  "ALL": [void 0, void 0, 0],
  "AMD": [void 0, "\u058F", 2],
  "AOA": [void 0, "Kz"],
  "ARS": [void 0, "$"],
  "AUD": ["A$", "$"],
  "AZN": [void 0, "\u20BC"],
  "BAM": [void 0, "KM"],
  "BBD": [void 0, "$"],
  "BDT": [void 0, "\u09F3"],
  "BHD": [void 0, void 0, 3],
  "BIF": [void 0, void 0, 0],
  "BMD": [void 0, "$"],
  "BND": [void 0, "$"],
  "BOB": [void 0, "Bs"],
  "BRL": ["R$"],
  "BSD": [void 0, "$"],
  "BWP": [void 0, "P"],
  "BYN": [void 0, void 0, 2],
  "BYR": [void 0, void 0, 0],
  "BZD": [void 0, "$"],
  "CAD": ["CA$", "$", 2],
  "CHF": [void 0, void 0, 2],
  "CLF": [void 0, void 0, 4],
  "CLP": [void 0, "$", 0],
  "CNY": ["CN\xA5", "\xA5"],
  "COP": [void 0, "$", 2],
  "CRC": [void 0, "\u20A1", 2],
  "CUC": [void 0, "$"],
  "CUP": [void 0, "$"],
  "CZK": [void 0, "K\u010D", 2],
  "DJF": [void 0, void 0, 0],
  "DKK": [void 0, "kr", 2],
  "DOP": [void 0, "$"],
  "EGP": [void 0, "E\xA3"],
  "ESP": [void 0, "\u20A7", 0],
  "EUR": ["\u20AC"],
  "FJD": [void 0, "$"],
  "FKP": [void 0, "\xA3"],
  "GBP": ["\xA3"],
  "GEL": [void 0, "\u20BE"],
  "GHS": [void 0, "GH\u20B5"],
  "GIP": [void 0, "\xA3"],
  "GNF": [void 0, "FG", 0],
  "GTQ": [void 0, "Q"],
  "GYD": [void 0, "$", 2],
  "HKD": ["HK$", "$"],
  "HNL": [void 0, "L"],
  "HRK": [void 0, "kn"],
  "HUF": [void 0, "Ft", 2],
  "IDR": [void 0, "Rp", 2],
  "ILS": ["\u20AA"],
  "INR": ["\u20B9"],
  "IQD": [void 0, void 0, 0],
  "IRR": [void 0, void 0, 0],
  "ISK": [void 0, "kr", 0],
  "ITL": [void 0, void 0, 0],
  "JMD": [void 0, "$"],
  "JOD": [void 0, void 0, 3],
  "JPY": ["\xA5", void 0, 0],
  "KGS": [void 0, "\u20C0"],
  "KHR": [void 0, "\u17DB"],
  "KMF": [void 0, "CF", 0],
  "KPW": [void 0, "\u20A9", 0],
  "KRW": ["\u20A9", void 0, 0],
  "KWD": [void 0, void 0, 3],
  "KYD": [void 0, "$"],
  "KZT": [void 0, "\u20B8"],
  "LAK": [void 0, "\u20AD", 0],
  "LBP": [void 0, "L\xA3", 0],
  "LKR": [void 0, "Rs"],
  "LRD": [void 0, "$"],
  "LTL": [void 0, "Lt"],
  "LUF": [void 0, void 0, 0],
  "LVL": [void 0, "Ls"],
  "LYD": [void 0, void 0, 3],
  "MGA": [void 0, "Ar", 0],
  "MGF": [void 0, void 0, 0],
  "MMK": [void 0, "K", 0],
  "MNT": [void 0, "\u20AE", 2],
  "MRO": [void 0, void 0, 0],
  "MUR": [void 0, "Rs", 2],
  "MXN": ["MX$", "$"],
  "MYR": [void 0, "RM"],
  "NAD": [void 0, "$"],
  "NGN": [void 0, "\u20A6"],
  "NIO": [void 0, "C$"],
  "NOK": [void 0, "kr", 2],
  "NPR": [void 0, "Rs"],
  "NZD": ["NZ$", "$"],
  "OMR": [void 0, void 0, 3],
  "PHP": ["\u20B1"],
  "PKR": [void 0, "Rs", 2],
  "PLN": [void 0, "z\u0142"],
  "PYG": [void 0, "\u20B2", 0],
  "RON": [void 0, "lei"],
  "RSD": [void 0, void 0, 0],
  "RUB": [void 0, "\u20BD"],
  "RWF": [void 0, "RF", 0],
  "SBD": [void 0, "$"],
  "SEK": [void 0, "kr", 2],
  "SGD": [void 0, "$"],
  "SHP": [void 0, "\xA3"],
  "SLE": [void 0, void 0, 2],
  "SLL": [void 0, void 0, 0],
  "SOS": [void 0, void 0, 0],
  "SRD": [void 0, "$"],
  "SSP": [void 0, "\xA3"],
  "STD": [void 0, void 0, 0],
  "STN": [void 0, "Db"],
  "SYP": [void 0, "\xA3", 0],
  "THB": [void 0, "\u0E3F"],
  "TMM": [void 0, void 0, 0],
  "TND": [void 0, void 0, 3],
  "TOP": [void 0, "T$"],
  "TRL": [void 0, void 0, 0],
  "TRY": [void 0, "\u20BA"],
  "TTD": [void 0, "$"],
  "TWD": ["NT$", "$", 2],
  "TZS": [void 0, void 0, 2],
  "UAH": [void 0, "\u20B4"],
  "UGX": [void 0, void 0, 0],
  "USD": ["$"],
  "UYI": [void 0, void 0, 0],
  "UYU": [void 0, "$"],
  "UYW": [void 0, void 0, 4],
  "UZS": [void 0, void 0, 2],
  "VEF": [void 0, "Bs", 2],
  "VND": ["\u20AB", void 0, 0],
  "VUV": [void 0, void 0, 0],
  "XAF": ["FCFA", void 0, 0],
  "XCD": ["EC$", "$"],
  "XCG": ["Cg."],
  "XOF": ["F\u202FCFA", void 0, 0],
  "XPF": ["CFPF", void 0, 0],
  "XXX": ["\xA4"],
  "YER": [void 0, void 0, 0],
  "ZAR": [void 0, "R"],
  "ZMK": [void 0, void 0, 0],
  "ZMW": [void 0, "ZK"],
  "ZWD": [void 0, void 0, 0]
};
var NumberFormatStyle;
(function(NumberFormatStyle2) {
  NumberFormatStyle2[NumberFormatStyle2["Decimal"] = 0] = "Decimal";
  NumberFormatStyle2[NumberFormatStyle2["Percent"] = 1] = "Percent";
  NumberFormatStyle2[NumberFormatStyle2["Currency"] = 2] = "Currency";
  NumberFormatStyle2[NumberFormatStyle2["Scientific"] = 3] = "Scientific";
})(NumberFormatStyle || (NumberFormatStyle = {}));
var Plural;
(function(Plural2) {
  Plural2[Plural2["Zero"] = 0] = "Zero";
  Plural2[Plural2["One"] = 1] = "One";
  Plural2[Plural2["Two"] = 2] = "Two";
  Plural2[Plural2["Few"] = 3] = "Few";
  Plural2[Plural2["Many"] = 4] = "Many";
  Plural2[Plural2["Other"] = 5] = "Other";
})(Plural || (Plural = {}));
var FormStyle;
(function(FormStyle2) {
  FormStyle2[FormStyle2["Format"] = 0] = "Format";
  FormStyle2[FormStyle2["Standalone"] = 1] = "Standalone";
})(FormStyle || (FormStyle = {}));
var TranslationWidth;
(function(TranslationWidth2) {
  TranslationWidth2[TranslationWidth2["Narrow"] = 0] = "Narrow";
  TranslationWidth2[TranslationWidth2["Abbreviated"] = 1] = "Abbreviated";
  TranslationWidth2[TranslationWidth2["Wide"] = 2] = "Wide";
  TranslationWidth2[TranslationWidth2["Short"] = 3] = "Short";
})(TranslationWidth || (TranslationWidth = {}));
var FormatWidth;
(function(FormatWidth2) {
  FormatWidth2[FormatWidth2["Short"] = 0] = "Short";
  FormatWidth2[FormatWidth2["Medium"] = 1] = "Medium";
  FormatWidth2[FormatWidth2["Long"] = 2] = "Long";
  FormatWidth2[FormatWidth2["Full"] = 3] = "Full";
})(FormatWidth || (FormatWidth = {}));
var NumberSymbol = {
  Decimal: 0,
  Group: 1,
  List: 2,
  PercentSign: 3,
  PlusSign: 4,
  MinusSign: 5,
  Exponential: 6,
  SuperscriptingExponent: 7,
  PerMille: 8,
  Infinity: 9,
  NaN: 10,
  TimeSeparator: 11,
  CurrencyDecimal: 12,
  CurrencyGroup: 13
};
var WeekDay;
(function(WeekDay2) {
  WeekDay2[WeekDay2["Sunday"] = 0] = "Sunday";
  WeekDay2[WeekDay2["Monday"] = 1] = "Monday";
  WeekDay2[WeekDay2["Tuesday"] = 2] = "Tuesday";
  WeekDay2[WeekDay2["Wednesday"] = 3] = "Wednesday";
  WeekDay2[WeekDay2["Thursday"] = 4] = "Thursday";
  WeekDay2[WeekDay2["Friday"] = 5] = "Friday";
  WeekDay2[WeekDay2["Saturday"] = 6] = "Saturday";
})(WeekDay || (WeekDay = {}));
function getLocaleId2(locale) {
  return findLocaleData(locale)[LocaleDataIndex.LocaleId];
}
function getLocaleDayPeriods(locale, formStyle, width) {
  const data = findLocaleData(locale);
  const amPmData = [data[LocaleDataIndex.DayPeriodsFormat], data[LocaleDataIndex.DayPeriodsStandalone]];
  const amPm = getLastDefinedValue(amPmData, formStyle);
  return getLastDefinedValue(amPm, width);
}
function getLocaleDayNames(locale, formStyle, width) {
  const data = findLocaleData(locale);
  const daysData = [data[LocaleDataIndex.DaysFormat], data[LocaleDataIndex.DaysStandalone]];
  const days = getLastDefinedValue(daysData, formStyle);
  return getLastDefinedValue(days, width);
}
function getLocaleMonthNames(locale, formStyle, width) {
  const data = findLocaleData(locale);
  const monthsData = [data[LocaleDataIndex.MonthsFormat], data[LocaleDataIndex.MonthsStandalone]];
  const months = getLastDefinedValue(monthsData, formStyle);
  return getLastDefinedValue(months, width);
}
function getLocaleEraNames(locale, width) {
  const data = findLocaleData(locale);
  const erasData = data[LocaleDataIndex.Eras];
  return getLastDefinedValue(erasData, width);
}
function getLocaleDateFormat(locale, width) {
  const data = findLocaleData(locale);
  return getLastDefinedValue(data[LocaleDataIndex.DateFormat], width);
}
function getLocaleTimeFormat(locale, width) {
  const data = findLocaleData(locale);
  return getLastDefinedValue(data[LocaleDataIndex.TimeFormat], width);
}
function getLocaleDateTimeFormat(locale, width) {
  const data = findLocaleData(locale);
  const dateTimeFormatData = data[LocaleDataIndex.DateTimeFormat];
  return getLastDefinedValue(dateTimeFormatData, width);
}
function getLocaleNumberSymbol(locale, symbol) {
  const data = findLocaleData(locale);
  const res = data[LocaleDataIndex.NumberSymbols][symbol];
  if (typeof res === "undefined") {
    if (symbol === NumberSymbol.CurrencyDecimal) {
      return data[LocaleDataIndex.NumberSymbols][NumberSymbol.Decimal];
    } else if (symbol === NumberSymbol.CurrencyGroup) {
      return data[LocaleDataIndex.NumberSymbols][NumberSymbol.Group];
    }
  }
  return res;
}
function getLocaleNumberFormat(locale, type) {
  const data = findLocaleData(locale);
  return data[LocaleDataIndex.NumberFormats][type];
}
function getLocaleCurrencies(locale) {
  const data = findLocaleData(locale);
  return data[LocaleDataIndex.Currencies];
}
var getLocalePluralCase2 = getLocalePluralCase;
function checkFullData(data) {
  if (!data[LocaleDataIndex.ExtraData]) {
    throw new RuntimeError(2303, ngDevMode && `Missing extra locale data for the locale "${data[LocaleDataIndex.LocaleId]}". Use "registerLocaleData" to load new data. See the "I18n guide" on angular.io to know more.`);
  }
}
function getLocaleExtraDayPeriodRules(locale) {
  const data = findLocaleData(locale);
  checkFullData(data);
  const rules = data[LocaleDataIndex.ExtraData][2] || [];
  return rules.map((rule) => {
    if (typeof rule === "string") {
      return extractTime(rule);
    }
    return [extractTime(rule[0]), extractTime(rule[1])];
  });
}
function getLocaleExtraDayPeriods(locale, formStyle, width) {
  const data = findLocaleData(locale);
  checkFullData(data);
  const dayPeriodsData = [data[LocaleDataIndex.ExtraData][0], data[LocaleDataIndex.ExtraData][1]];
  const dayPeriods = getLastDefinedValue(dayPeriodsData, formStyle) || [];
  return getLastDefinedValue(dayPeriods, width) || [];
}
function getLastDefinedValue(data, index) {
  for (let i = index; i > -1; i--) {
    if (typeof data[i] !== "undefined") {
      return data[i];
    }
  }
  throw new RuntimeError(2304, ngDevMode && "Locale data API: locale data undefined");
}
function extractTime(time) {
  const [h, m] = time.split(":");
  return {
    hours: +h,
    minutes: +m
  };
}
function getCurrencySymbol(code, format, locale = "en") {
  const currency = getLocaleCurrencies(locale)[code] || CURRENCIES_EN[code] || [];
  const symbolNarrow = currency[1];
  if (format === "narrow" && typeof symbolNarrow === "string") {
    return symbolNarrow;
  }
  return currency[0] || code;
}
var DEFAULT_NB_OF_CURRENCY_DIGITS = 2;
function getNumberOfCurrencyDigits(code) {
  let digits;
  const currency = CURRENCIES_EN[code];
  if (currency) {
    digits = currency[2];
  }
  return typeof digits === "number" ? digits : DEFAULT_NB_OF_CURRENCY_DIGITS;
}
var ISO8601_DATE_REGEX = /^(\d{4,})-?(\d\d)-?(\d\d)(?:T(\d\d)(?::?(\d\d)(?::?(\d\d)(?:\.(\d+))?)?)?(Z|([+-])(\d\d):?(\d\d))?)?$/;
var NAMED_FORMATS = {};
var DATE_FORMATS_SPLIT = /((?:[^BEGHLMOSWYZabcdhmswyz']+)|(?:'(?:[^']|'')*')|(?:G{1,5}|y{1,4}|Y{1,4}|M{1,5}|L{1,5}|w{1,2}|W{1}|d{1,2}|E{1,6}|c{1,6}|a{1,5}|b{1,5}|B{1,5}|h{1,2}|H{1,2}|m{1,2}|s{1,2}|S{1,3}|z{1,4}|Z{1,5}|O{1,4}))([\s\S]*)/;
var MAX_DATE_FORMAT_LENGTH = 256;
function formatDate(value, format, locale, timezone) {
  let date = toDate(value);
  assertValidDateFormatLength(format);
  const namedFormat = getNamedFormat(locale, format);
  format = namedFormat || format;
  let parts = [];
  let match;
  while (format) {
    match = DATE_FORMATS_SPLIT.exec(format);
    if (match) {
      parts = parts.concat(match.slice(1));
      const part = parts.pop();
      if (!part) {
        break;
      }
      format = part;
    } else {
      parts.push(format);
      break;
    }
  }
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    assertValidDateFormat(parts);
  }
  let dateTimezoneOffset = date.getTimezoneOffset();
  if (timezone) {
    dateTimezoneOffset = timezoneToOffset(timezone, dateTimezoneOffset);
    date = convertTimezoneToLocal(date, timezone);
  }
  let text = "";
  parts.forEach((value2) => {
    const dateFormatter = getDateFormatter(value2);
    text += dateFormatter ? dateFormatter(date, locale, dateTimezoneOffset) : value2 === "''" ? "'" : value2.replace(/(^'|'$)/g, "").replace(/''/g, "'");
  });
  return text;
}
function assertValidDateFormatLength(format) {
  if (format.length > MAX_DATE_FORMAT_LENGTH) {
    throw new RuntimeError(2300, ngDevMode && `Date format is too long. Exceeded maximum length of ${MAX_DATE_FORMAT_LENGTH} characters.`);
  }
}
function assertValidDateFormat(parts) {
  if (parts.some((part) => /^Y+$/.test(part)) && !parts.some((part) => /^w+$/.test(part))) {
    const message = `Suspicious use of week-based year "Y" in date pattern "${parts.join("")}". Did you mean to use calendar year "y" instead?`;
    if (parts.length === 1) {
      console.error(formatRuntimeError(2300, message));
    } else {
      throw new RuntimeError(2300, message);
    }
  }
}
function createDate(year, month, date) {
  const newDate = /* @__PURE__ */ new Date(0);
  newDate.setFullYear(year, month, date);
  newDate.setHours(0, 0, 0);
  return newDate;
}
function getNamedFormat(locale, format) {
  const localeId = getLocaleId2(locale);
  NAMED_FORMATS[localeId] ??= {};
  if (NAMED_FORMATS[localeId][format]) {
    return NAMED_FORMATS[localeId][format];
  }
  let formatValue2 = "";
  switch (format) {
    case "shortDate":
      formatValue2 = getLocaleDateFormat(locale, FormatWidth.Short);
      break;
    case "mediumDate":
      formatValue2 = getLocaleDateFormat(locale, FormatWidth.Medium);
      break;
    case "longDate":
      formatValue2 = getLocaleDateFormat(locale, FormatWidth.Long);
      break;
    case "fullDate":
      formatValue2 = getLocaleDateFormat(locale, FormatWidth.Full);
      break;
    case "shortTime":
      formatValue2 = getLocaleTimeFormat(locale, FormatWidth.Short);
      break;
    case "mediumTime":
      formatValue2 = getLocaleTimeFormat(locale, FormatWidth.Medium);
      break;
    case "longTime":
      formatValue2 = getLocaleTimeFormat(locale, FormatWidth.Long);
      break;
    case "fullTime":
      formatValue2 = getLocaleTimeFormat(locale, FormatWidth.Full);
      break;
    case "short":
      const shortTime = getNamedFormat(locale, "shortTime");
      const shortDate = getNamedFormat(locale, "shortDate");
      formatValue2 = formatDateTime(getLocaleDateTimeFormat(locale, FormatWidth.Short), [shortTime, shortDate]);
      break;
    case "medium":
      const mediumTime = getNamedFormat(locale, "mediumTime");
      const mediumDate = getNamedFormat(locale, "mediumDate");
      formatValue2 = formatDateTime(getLocaleDateTimeFormat(locale, FormatWidth.Medium), [mediumTime, mediumDate]);
      break;
    case "long":
      const longTime = getNamedFormat(locale, "longTime");
      const longDate = getNamedFormat(locale, "longDate");
      formatValue2 = formatDateTime(getLocaleDateTimeFormat(locale, FormatWidth.Long), [longTime, longDate]);
      break;
    case "full":
      const fullTime = getNamedFormat(locale, "fullTime");
      const fullDate = getNamedFormat(locale, "fullDate");
      formatValue2 = formatDateTime(getLocaleDateTimeFormat(locale, FormatWidth.Full), [fullTime, fullDate]);
      break;
  }
  if (formatValue2) {
    NAMED_FORMATS[localeId][format] = formatValue2;
  }
  return formatValue2;
}
function formatDateTime(str, opt_values) {
  if (opt_values) {
    str = str.replace(/\{([^}]+)}/g, function(match, key) {
      return opt_values != null && key in opt_values ? opt_values[key] : match;
    });
  }
  return str;
}
function padNumber(num, digits, minusSign = "-", trim, negWrap) {
  let neg = "";
  if (num < 0 || negWrap && num <= 0) {
    if (negWrap) {
      num = -num + 1;
    } else {
      num = -num;
      neg = minusSign;
    }
  }
  let strNum = String(num);
  while (strNum.length < digits) {
    strNum = "0" + strNum;
  }
  if (trim) {
    strNum = strNum.slice(strNum.length - digits);
  }
  return neg + strNum;
}
function formatFractionalSeconds(milliseconds, digits) {
  const strMs = padNumber(milliseconds, 3);
  return strMs.substring(0, digits);
}
function dateGetter(name, size, offset = 0, trim = false, negWrap = false) {
  return function(date, locale) {
    let part = getDatePart(name, date);
    if (offset > 0 || part > -offset) {
      part += offset;
    }
    if (name === 3) {
      if (part === 0 && offset === -12) {
        part = 12;
      }
    } else if (name === 6) {
      return formatFractionalSeconds(part, size);
    }
    const localeMinus = getLocaleNumberSymbol(locale, NumberSymbol.MinusSign);
    return padNumber(part, size, localeMinus, trim, negWrap);
  };
}
function getDatePart(part, date) {
  switch (part) {
    case 0:
      return date.getFullYear();
    case 1:
      return date.getMonth();
    case 2:
      return date.getDate();
    case 3:
      return date.getHours();
    case 4:
      return date.getMinutes();
    case 5:
      return date.getSeconds();
    case 6:
      return date.getMilliseconds();
    case 7:
      return date.getDay();
    default:
      throw new RuntimeError(2301, ngDevMode && `Unknown DateType value "${part}".`);
  }
}
function dateStrGetter(name, width, form = FormStyle.Format, extended = false) {
  return function(date, locale) {
    return getDateTranslation(date, locale, name, width, form, extended);
  };
}
function getDateTranslation(date, locale, name, width, form, extended) {
  switch (name) {
    case 2:
      return getLocaleMonthNames(locale, form, width)[date.getMonth()];
    case 1:
      return getLocaleDayNames(locale, form, width)[date.getDay()];
    case 0:
      const currentHours = date.getHours();
      const currentMinutes = date.getMinutes();
      if (extended) {
        const rules = getLocaleExtraDayPeriodRules(locale);
        const dayPeriods = getLocaleExtraDayPeriods(locale, form, width);
        const index = rules.findIndex((rule) => {
          if (Array.isArray(rule)) {
            const [from2, to] = rule;
            const afterFrom = currentHours >= from2.hours && currentMinutes >= from2.minutes;
            const beforeTo = currentHours < to.hours || currentHours === to.hours && currentMinutes < to.minutes;
            if (from2.hours < to.hours) {
              if (afterFrom && beforeTo) {
                return true;
              }
            } else if (afterFrom || beforeTo) {
              return true;
            }
          } else {
            if (rule.hours === currentHours && rule.minutes === currentMinutes) {
              return true;
            }
          }
          return false;
        });
        if (index !== -1) {
          return dayPeriods[index];
        }
      }
      return getLocaleDayPeriods(locale, form, width)[currentHours < 12 ? 0 : 1];
    case 3:
      return getLocaleEraNames(locale, width)[date.getFullYear() <= 0 ? 0 : 1];
    default:
      const unexpected = name;
      throw new RuntimeError(2302, ngDevMode && `unexpected translation type ${unexpected}`);
  }
}
function timeZoneGetter(width) {
  return function(date, locale, offset) {
    const zone = -1 * offset;
    const minusSign = getLocaleNumberSymbol(locale, NumberSymbol.MinusSign);
    const hours = zone > 0 ? Math.floor(zone / 60) : Math.ceil(zone / 60);
    switch (width) {
      case 0:
        return (zone >= 0 ? "+" : "") + padNumber(hours, 2, minusSign) + padNumber(Math.abs(zone % 60), 2, minusSign);
      case 1:
        return "GMT" + (zone >= 0 ? "+" : "") + padNumber(hours, 1, minusSign);
      case 2:
        return "GMT" + (zone >= 0 ? "+" : "") + padNumber(hours, 2, minusSign) + ":" + padNumber(Math.abs(zone % 60), 2, minusSign);
      case 3:
        if (offset === 0) {
          return "Z";
        } else {
          return (zone >= 0 ? "+" : "") + padNumber(hours, 2, minusSign) + ":" + padNumber(Math.abs(zone % 60), 2, minusSign);
        }
      default:
        throw new RuntimeError(2310, ngDevMode && `Unknown zone width "${width}"`);
    }
  };
}
var JANUARY = 0;
var THURSDAY = 4;
function getFirstThursdayOfYear(year) {
  const firstDayOfYear = createDate(year, JANUARY, 1).getDay();
  return createDate(year, 0, 1 + (firstDayOfYear <= THURSDAY ? THURSDAY : THURSDAY + 7) - firstDayOfYear);
}
function getThursdayThisIsoWeek(datetime) {
  const currentDay = datetime.getDay();
  const deltaToThursday = currentDay === 0 ? -3 : THURSDAY - currentDay;
  return createDate(datetime.getFullYear(), datetime.getMonth(), datetime.getDate() + deltaToThursday);
}
function weekGetter(size, monthBased = false) {
  return function(date, locale) {
    let result;
    if (monthBased) {
      const nbDaysBefore1stDayOfMonth = new Date(date.getFullYear(), date.getMonth(), 1).getDay() - 1;
      const today = date.getDate();
      result = 1 + Math.floor((today + nbDaysBefore1stDayOfMonth) / 7);
    } else {
      const thisThurs = getThursdayThisIsoWeek(date);
      const firstThurs = getFirstThursdayOfYear(thisThurs.getFullYear());
      const diff = thisThurs.getTime() - firstThurs.getTime();
      result = 1 + Math.round(diff / 6048e5);
    }
    return padNumber(result, size, getLocaleNumberSymbol(locale, NumberSymbol.MinusSign));
  };
}
function weekNumberingYearGetter(size, trim = false) {
  return function(date, locale) {
    const thisThurs = getThursdayThisIsoWeek(date);
    const weekNumberingYear = thisThurs.getFullYear();
    return padNumber(weekNumberingYear, size, getLocaleNumberSymbol(locale, NumberSymbol.MinusSign), trim);
  };
}
var DATE_FORMATS = {};
function getDateFormatter(format) {
  if (DATE_FORMATS[format]) {
    return DATE_FORMATS[format];
  }
  let formatter2;
  switch (format) {
    case "G":
    case "GG":
    case "GGG":
      formatter2 = dateStrGetter(3, TranslationWidth.Abbreviated);
      break;
    case "GGGG":
      formatter2 = dateStrGetter(3, TranslationWidth.Wide);
      break;
    case "GGGGG":
      formatter2 = dateStrGetter(3, TranslationWidth.Narrow);
      break;
    case "y":
      formatter2 = dateGetter(0, 1, 0, false, true);
      break;
    case "yy":
      formatter2 = dateGetter(0, 2, 0, true, true);
      break;
    case "yyy":
      formatter2 = dateGetter(0, 3, 0, false, true);
      break;
    case "yyyy":
      formatter2 = dateGetter(0, 4, 0, false, true);
      break;
    case "Y":
      formatter2 = weekNumberingYearGetter(1);
      break;
    case "YY":
      formatter2 = weekNumberingYearGetter(2, true);
      break;
    case "YYY":
      formatter2 = weekNumberingYearGetter(3);
      break;
    case "YYYY":
      formatter2 = weekNumberingYearGetter(4);
      break;
    case "M":
    case "L":
      formatter2 = dateGetter(1, 1, 1);
      break;
    case "MM":
    case "LL":
      formatter2 = dateGetter(1, 2, 1);
      break;
    case "MMM":
      formatter2 = dateStrGetter(2, TranslationWidth.Abbreviated);
      break;
    case "MMMM":
      formatter2 = dateStrGetter(2, TranslationWidth.Wide);
      break;
    case "MMMMM":
      formatter2 = dateStrGetter(2, TranslationWidth.Narrow);
      break;
    case "LLL":
      formatter2 = dateStrGetter(2, TranslationWidth.Abbreviated, FormStyle.Standalone);
      break;
    case "LLLL":
      formatter2 = dateStrGetter(2, TranslationWidth.Wide, FormStyle.Standalone);
      break;
    case "LLLLL":
      formatter2 = dateStrGetter(2, TranslationWidth.Narrow, FormStyle.Standalone);
      break;
    case "w":
      formatter2 = weekGetter(1);
      break;
    case "ww":
      formatter2 = weekGetter(2);
      break;
    case "W":
      formatter2 = weekGetter(1, true);
      break;
    case "d":
      formatter2 = dateGetter(2, 1);
      break;
    case "dd":
      formatter2 = dateGetter(2, 2);
      break;
    case "c":
    case "cc":
      formatter2 = dateGetter(7, 1);
      break;
    case "ccc":
      formatter2 = dateStrGetter(1, TranslationWidth.Abbreviated, FormStyle.Standalone);
      break;
    case "cccc":
      formatter2 = dateStrGetter(1, TranslationWidth.Wide, FormStyle.Standalone);
      break;
    case "ccccc":
      formatter2 = dateStrGetter(1, TranslationWidth.Narrow, FormStyle.Standalone);
      break;
    case "cccccc":
      formatter2 = dateStrGetter(1, TranslationWidth.Short, FormStyle.Standalone);
      break;
    case "E":
    case "EE":
    case "EEE":
      formatter2 = dateStrGetter(1, TranslationWidth.Abbreviated);
      break;
    case "EEEE":
      formatter2 = dateStrGetter(1, TranslationWidth.Wide);
      break;
    case "EEEEE":
      formatter2 = dateStrGetter(1, TranslationWidth.Narrow);
      break;
    case "EEEEEE":
      formatter2 = dateStrGetter(1, TranslationWidth.Short);
      break;
    case "a":
    case "aa":
    case "aaa":
      formatter2 = dateStrGetter(0, TranslationWidth.Abbreviated);
      break;
    case "aaaa":
      formatter2 = dateStrGetter(0, TranslationWidth.Wide);
      break;
    case "aaaaa":
      formatter2 = dateStrGetter(0, TranslationWidth.Narrow);
      break;
    case "b":
    case "bb":
    case "bbb":
      formatter2 = dateStrGetter(0, TranslationWidth.Abbreviated, FormStyle.Standalone, true);
      break;
    case "bbbb":
      formatter2 = dateStrGetter(0, TranslationWidth.Wide, FormStyle.Standalone, true);
      break;
    case "bbbbb":
      formatter2 = dateStrGetter(0, TranslationWidth.Narrow, FormStyle.Standalone, true);
      break;
    case "B":
    case "BB":
    case "BBB":
      formatter2 = dateStrGetter(0, TranslationWidth.Abbreviated, FormStyle.Format, true);
      break;
    case "BBBB":
      formatter2 = dateStrGetter(0, TranslationWidth.Wide, FormStyle.Format, true);
      break;
    case "BBBBB":
      formatter2 = dateStrGetter(0, TranslationWidth.Narrow, FormStyle.Format, true);
      break;
    case "h":
      formatter2 = dateGetter(3, 1, -12);
      break;
    case "hh":
      formatter2 = dateGetter(3, 2, -12);
      break;
    case "H":
      formatter2 = dateGetter(3, 1);
      break;
    case "HH":
      formatter2 = dateGetter(3, 2);
      break;
    case "m":
      formatter2 = dateGetter(4, 1);
      break;
    case "mm":
      formatter2 = dateGetter(4, 2);
      break;
    case "s":
      formatter2 = dateGetter(5, 1);
      break;
    case "ss":
      formatter2 = dateGetter(5, 2);
      break;
    case "S":
      formatter2 = dateGetter(6, 1);
      break;
    case "SS":
      formatter2 = dateGetter(6, 2);
      break;
    case "SSS":
      formatter2 = dateGetter(6, 3);
      break;
    case "Z":
    case "ZZ":
    case "ZZZ":
      formatter2 = timeZoneGetter(0);
      break;
    case "ZZZZZ":
      formatter2 = timeZoneGetter(3);
      break;
    case "O":
    case "OO":
    case "OOO":
    case "z":
    case "zz":
    case "zzz":
      formatter2 = timeZoneGetter(1);
      break;
    case "OOOO":
    case "ZZZZ":
    case "zzzz":
      formatter2 = timeZoneGetter(2);
      break;
    default:
      return null;
  }
  DATE_FORMATS[format] = formatter2;
  return formatter2;
}
function timezoneToOffset(timezone, fallback) {
  timezone = timezone.replace(/:/g, "");
  const requestedTimezoneOffset = Date.parse("Jan 01, 1970 00:00:00 " + timezone) / 6e4;
  return isNaN(requestedTimezoneOffset) ? fallback : requestedTimezoneOffset;
}
function addDateMinutes(date, minutes) {
  date = new Date(date.getTime());
  date.setMinutes(date.getMinutes() + minutes);
  return date;
}
function convertTimezoneToLocal(date, timezone, reverse) {
  const reverseValue = -1;
  const dateTimezoneOffset = date.getTimezoneOffset();
  const timezoneOffset = timezoneToOffset(timezone, dateTimezoneOffset);
  return addDateMinutes(date, reverseValue * (timezoneOffset - dateTimezoneOffset));
}
function toDate(value) {
  if (isDate(value)) {
    return value;
  }
  if (typeof value === "number" && !isNaN(value)) {
    return new Date(value);
  }
  if (typeof value === "string") {
    value = value.trim();
    if (/^(\d{4}(-\d{1,2}(-\d{1,2})?)?)$/.test(value)) {
      const [y, m = 1, d = 1] = value.split("-").map((val) => +val);
      return createDate(y, m - 1, d);
    }
    const parsedNb = parseFloat(value);
    if (!isNaN(value - parsedNb)) {
      return new Date(parsedNb);
    }
    let match;
    if (match = value.match(ISO8601_DATE_REGEX)) {
      return isoStringToDate(match);
    }
  }
  const date = new Date(value);
  if (!isDate(date)) {
    throw new RuntimeError(2311, ngDevMode && `Unable to convert "${value}" into a date`);
  }
  return date;
}
function isoStringToDate(match) {
  const date = /* @__PURE__ */ new Date(0);
  let tzHour = 0;
  let tzMin = 0;
  const dateSetter = match[8] ? date.setUTCFullYear : date.setFullYear;
  const timeSetter = match[8] ? date.setUTCHours : date.setHours;
  if (match[9]) {
    tzHour = Number(match[9] + match[10]);
    tzMin = Number(match[9] + match[11]);
  }
  dateSetter.call(date, Number(match[1]), Number(match[2]) - 1, Number(match[3]));
  const h = Number(match[4] || 0) - tzHour;
  const m = Number(match[5] || 0) - tzMin;
  const s = Number(match[6] || 0);
  const ms = Math.floor(parseFloat("0." + (match[7] || 0)) * 1e3);
  timeSetter.call(date, h, m, s, ms);
  return date;
}
function isDate(value) {
  return value instanceof Date && !isNaN(value.valueOf());
}
var NUMBER_FORMAT_REGEXP = /^(\d+)?\.((\d+)(-(\d+))?)?$/;
var MAX_DIGITS = 22;
var DECIMAL_SEP = ".";
var ZERO_CHAR = "0";
var PATTERN_SEP = ";";
var GROUP_SEP = ",";
var DIGIT_CHAR = "#";
var CURRENCY_CHAR = "\xA4";
var PERCENT_CHAR = "%";
function formatNumberToLocaleString(value, pattern, locale, groupSymbol, decimalSymbol, digitsInfo, isPercent = false) {
  let formattedText = "";
  let isZero = false;
  if (!isFinite(value)) {
    formattedText = getLocaleNumberSymbol(locale, NumberSymbol.Infinity);
  } else {
    let parsedNumber = parseNumber(value);
    if (isPercent) {
      parsedNumber = toPercent(parsedNumber);
    }
    let minInt = pattern.minInt;
    let minFraction = pattern.minFrac;
    let maxFraction = pattern.maxFrac;
    if (digitsInfo) {
      const parts = digitsInfo.match(NUMBER_FORMAT_REGEXP);
      if (parts === null) {
        throw new RuntimeError(2306, ngDevMode && `${digitsInfo} is not a valid digit info`);
      }
      const minIntPart = parts[1];
      const minFractionPart = parts[3];
      const maxFractionPart = parts[5];
      if (minIntPart != null) {
        minInt = parseIntAutoRadix(minIntPart);
      }
      if (minFractionPart != null) {
        minFraction = parseIntAutoRadix(minFractionPart);
      }
      if (maxFractionPart != null) {
        maxFraction = parseIntAutoRadix(maxFractionPart);
      } else if (minFractionPart != null && minFraction > maxFraction) {
        maxFraction = minFraction;
      }
      const MAX_ALLOWED_DIGITS = 100;
      if (minInt > MAX_ALLOWED_DIGITS || minFraction > MAX_ALLOWED_DIGITS || maxFraction > MAX_ALLOWED_DIGITS) {
        throw new RuntimeError(2306, ngDevMode && `${digitsInfo} is not a valid digit info. Exceeded maximum limits of ${MAX_ALLOWED_DIGITS} digits.`);
      }
    }
    roundNumber(parsedNumber, minFraction, maxFraction);
    let digits = parsedNumber.digits;
    let integerLen = parsedNumber.integerLen;
    const exponent = parsedNumber.exponent;
    let decimals = [];
    isZero = digits.every((d) => !d);
    for (; integerLen < minInt; integerLen++) {
      digits.unshift(0);
    }
    for (; integerLen < 0; integerLen++) {
      digits.unshift(0);
    }
    if (integerLen > 0) {
      decimals = digits.splice(integerLen, digits.length);
    } else {
      decimals = digits;
      digits = [0];
    }
    const groups = [];
    if (digits.length >= pattern.lgSize) {
      groups.unshift(digits.splice(-pattern.lgSize, digits.length).join(""));
    }
    while (digits.length > pattern.gSize) {
      groups.unshift(digits.splice(-pattern.gSize, digits.length).join(""));
    }
    if (digits.length) {
      groups.unshift(digits.join(""));
    }
    formattedText = groups.join(getLocaleNumberSymbol(locale, groupSymbol));
    if (decimals.length) {
      formattedText += getLocaleNumberSymbol(locale, decimalSymbol) + decimals.join("");
    }
    if (exponent) {
      formattedText += getLocaleNumberSymbol(locale, NumberSymbol.Exponential) + "+" + exponent;
    }
  }
  if (value < 0 && !isZero) {
    formattedText = pattern.negPre + formattedText + pattern.negSuf;
  } else {
    formattedText = pattern.posPre + formattedText + pattern.posSuf;
  }
  return formattedText;
}
function formatCurrency(value, locale, currency, currencyCode, digitsInfo) {
  const format = getLocaleNumberFormat(locale, NumberFormatStyle.Currency);
  const pattern = parseNumberFormat(format, getLocaleNumberSymbol(locale, NumberSymbol.MinusSign));
  pattern.minFrac = getNumberOfCurrencyDigits(currencyCode);
  pattern.maxFrac = pattern.minFrac;
  const res = formatNumberToLocaleString(value, pattern, locale, NumberSymbol.CurrencyGroup, NumberSymbol.CurrencyDecimal, digitsInfo);
  return res.replace(CURRENCY_CHAR, currency).replace(CURRENCY_CHAR, "").trim();
}
function formatPercent(value, locale, digitsInfo) {
  const format = getLocaleNumberFormat(locale, NumberFormatStyle.Percent);
  const pattern = parseNumberFormat(format, getLocaleNumberSymbol(locale, NumberSymbol.MinusSign));
  const res = formatNumberToLocaleString(value, pattern, locale, NumberSymbol.Group, NumberSymbol.Decimal, digitsInfo, true);
  return res.replace(new RegExp(PERCENT_CHAR, "g"), getLocaleNumberSymbol(locale, NumberSymbol.PercentSign));
}
function formatNumber(value, locale, digitsInfo) {
  const format = getLocaleNumberFormat(locale, NumberFormatStyle.Decimal);
  const pattern = parseNumberFormat(format, getLocaleNumberSymbol(locale, NumberSymbol.MinusSign));
  return formatNumberToLocaleString(value, pattern, locale, NumberSymbol.Group, NumberSymbol.Decimal, digitsInfo);
}
function parseNumberFormat(format, minusSign = "-") {
  const p = {
    minInt: 1,
    minFrac: 0,
    maxFrac: 0,
    posPre: "",
    posSuf: "",
    negPre: "",
    negSuf: "",
    gSize: 0,
    lgSize: 0
  };
  const patternParts = format.split(PATTERN_SEP);
  const positive = patternParts[0];
  const negative = patternParts[1];
  const positiveParts = positive.indexOf(DECIMAL_SEP) !== -1 ? positive.split(DECIMAL_SEP) : [positive.substring(0, positive.lastIndexOf(ZERO_CHAR) + 1), positive.substring(positive.lastIndexOf(ZERO_CHAR) + 1)], integer = positiveParts[0], fraction = positiveParts[1] || "";
  p.posPre = integer.substring(0, integer.indexOf(DIGIT_CHAR));
  for (let i = 0; i < fraction.length; i++) {
    const ch = fraction.charAt(i);
    if (ch === ZERO_CHAR) {
      p.minFrac = p.maxFrac = i + 1;
    } else if (ch === DIGIT_CHAR) {
      p.maxFrac = i + 1;
    } else {
      p.posSuf += ch;
    }
  }
  const groups = integer.split(GROUP_SEP);
  p.gSize = groups[1] ? groups[1].length : 0;
  p.lgSize = groups[2] || groups[1] ? (groups[2] || groups[1]).length : 0;
  if (negative) {
    const trunkLen = positive.length - p.posPre.length - p.posSuf.length, pos = negative.indexOf(DIGIT_CHAR);
    p.negPre = negative.substring(0, pos).replace(/'/g, "");
    p.negSuf = negative.slice(pos + trunkLen).replace(/'/g, "");
  } else {
    p.negPre = minusSign + p.posPre;
    p.negSuf = p.posSuf;
  }
  return p;
}
function toPercent(parsedNumber) {
  if (parsedNumber.digits[0] === 0) {
    return parsedNumber;
  }
  const fractionLen = parsedNumber.digits.length - parsedNumber.integerLen;
  if (parsedNumber.exponent) {
    parsedNumber.exponent += 2;
  } else {
    if (fractionLen === 0) {
      parsedNumber.digits.push(0, 0);
    } else if (fractionLen === 1) {
      parsedNumber.digits.push(0);
    }
    parsedNumber.integerLen += 2;
  }
  return parsedNumber;
}
function parseNumber(num) {
  let numStr = Math.abs(num) + "";
  let exponent = 0, digits, integerLen;
  let i, j, zeros;
  if ((integerLen = numStr.indexOf(DECIMAL_SEP)) > -1) {
    numStr = numStr.replace(DECIMAL_SEP, "");
  }
  if ((i = numStr.search(/e/i)) > 0) {
    if (integerLen < 0) integerLen = i;
    integerLen += +numStr.slice(i + 1);
    numStr = numStr.substring(0, i);
  } else if (integerLen < 0) {
    integerLen = numStr.length;
  }
  for (i = 0; numStr.charAt(i) === ZERO_CHAR; i++) {
  }
  if (i === (zeros = numStr.length)) {
    digits = [0];
    integerLen = 1;
  } else {
    zeros--;
    while (numStr.charAt(zeros) === ZERO_CHAR) zeros--;
    integerLen -= i;
    digits = [];
    for (j = 0; i <= zeros; i++, j++) {
      digits[j] = Number(numStr.charAt(i));
    }
  }
  if (integerLen > MAX_DIGITS) {
    digits = digits.splice(0, MAX_DIGITS - 1);
    exponent = integerLen - 1;
    integerLen = 1;
  }
  return {
    digits,
    exponent,
    integerLen
  };
}
function roundNumber(parsedNumber, minFrac, maxFrac) {
  if (minFrac > maxFrac) {
    throw new RuntimeError(2307, ngDevMode && `The minimum number of digits after fraction (${minFrac}) is higher than the maximum (${maxFrac}).`);
  }
  let digits = parsedNumber.digits;
  let fractionLen = digits.length - parsedNumber.integerLen;
  const fractionSize = Math.min(Math.max(minFrac, fractionLen), maxFrac);
  let roundAt = fractionSize + parsedNumber.integerLen;
  let digit = digits[roundAt];
  if (roundAt > 0) {
    digits.splice(Math.max(parsedNumber.integerLen, roundAt));
    for (let j = roundAt; j < digits.length; j++) {
      digits[j] = 0;
    }
  } else {
    fractionLen = Math.max(0, fractionLen);
    parsedNumber.integerLen = 1;
    digits.length = Math.max(1, roundAt = fractionSize + 1);
    digits[0] = 0;
    for (let i = 1; i < roundAt; i++) digits[i] = 0;
  }
  if (digit >= 5) {
    if (roundAt - 1 < 0) {
      for (let k = 0; k > roundAt; k--) {
        digits.unshift(0);
        parsedNumber.integerLen++;
      }
      digits.unshift(1);
      parsedNumber.integerLen++;
    } else {
      digits[roundAt - 1]++;
    }
  }
  for (; fractionLen < Math.max(0, fractionSize); fractionLen++) digits.push(0);
  let dropTrailingZeros = fractionSize !== 0;
  const minLen = minFrac + parsedNumber.integerLen;
  const carry = digits.reduceRight(function(carry2, d, i, digits2) {
    d = d + carry2;
    digits2[i] = d < 10 ? d : d - 10;
    if (dropTrailingZeros) {
      if (digits2[i] === 0 && i >= minLen) {
        digits2.pop();
      } else {
        dropTrailingZeros = false;
      }
    }
    return d >= 10 ? 1 : 0;
  }, 0);
  if (carry) {
    digits.unshift(carry);
    parsedNumber.integerLen++;
  }
}
function parseIntAutoRadix(text) {
  const result = parseInt(text);
  if (isNaN(result)) {
    throw new RuntimeError(2305, ngDevMode && "Invalid integer literal when parsing " + text);
  }
  return result;
}
var NgLocalization = class _NgLocalization {
  static \u0275fac = function NgLocalization_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgLocalization)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NgLocalization,
    factory: () => (() => new NgLocaleLocalization(inject2(LOCALE_ID)))(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgLocalization, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: () => new NgLocaleLocalization(inject2(LOCALE_ID))
    }]
  }], null, null);
})();
function getPluralCategory(value, cases, ngLocalization, locale) {
  let key = `=${value}`;
  if (cases.indexOf(key) > -1) {
    return key;
  }
  key = ngLocalization.getPluralCategory(value, locale);
  if (cases.indexOf(key) > -1) {
    return key;
  }
  if (cases.indexOf("other") > -1) {
    return "other";
  }
  throw new RuntimeError(2308, ngDevMode && `No plural message found for value "${value}"`);
}
var NgLocaleLocalization = class _NgLocaleLocalization extends NgLocalization {
  locale;
  constructor(locale) {
    super();
    this.locale = locale;
  }
  getPluralCategory(value, locale) {
    const plural2 = getLocalePluralCase2(locale || this.locale)(value);
    switch (plural2) {
      case Plural.Zero:
        return "zero";
      case Plural.One:
        return "one";
      case Plural.Two:
        return "two";
      case Plural.Few:
        return "few";
      case Plural.Many:
        return "many";
      default:
        return "other";
    }
  }
  static \u0275fac = function NgLocaleLocalization_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgLocaleLocalization)(\u0275\u0275inject(LOCALE_ID));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NgLocaleLocalization,
    factory: _NgLocaleLocalization.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgLocaleLocalization, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LOCALE_ID]
    }]
  }], null);
})();
var WS_REGEXP = /\s+/;
var EMPTY_ARRAY2 = [];
var NgClass = class _NgClass {
  _ngEl;
  _renderer;
  initialClasses = EMPTY_ARRAY2;
  rawClass;
  stateMap = /* @__PURE__ */ new Map();
  constructor(_ngEl, _renderer) {
    this._ngEl = _ngEl;
    this._renderer = _renderer;
  }
  set klass(value) {
    this.initialClasses = value != null ? value.trim().split(WS_REGEXP) : EMPTY_ARRAY2;
  }
  set ngClass(value) {
    this.rawClass = typeof value === "string" ? value.trim().split(WS_REGEXP) : value;
  }
  ngDoCheck() {
    for (const klass of this.initialClasses) {
      this._updateState(klass, true);
    }
    const rawClass = this.rawClass;
    if (Array.isArray(rawClass) || rawClass instanceof Set) {
      for (const klass of rawClass) {
        this._updateState(klass, true);
      }
    } else if (rawClass != null) {
      for (const klass of Object.keys(rawClass)) {
        this._updateState(klass, Boolean(rawClass[klass]));
      }
    }
    this._applyStateDiff();
  }
  _updateState(klass, nextEnabled) {
    const state = this.stateMap.get(klass);
    if (state !== void 0) {
      if (state.enabled !== nextEnabled) {
        state.changed = true;
        state.enabled = nextEnabled;
      }
      state.touched = true;
    } else {
      this.stateMap.set(klass, {
        enabled: nextEnabled,
        changed: true,
        touched: true
      });
    }
  }
  _applyStateDiff() {
    for (const stateEntry of this.stateMap) {
      const klass = stateEntry[0];
      const state = stateEntry[1];
      if (state.changed) {
        this._toggleClass(klass, state.enabled);
        state.changed = false;
      } else if (!state.touched) {
        if (state.enabled) {
          this._toggleClass(klass, false);
        }
        this.stateMap.delete(klass);
      }
      state.touched = false;
    }
  }
  _toggleClass(klass, enabled) {
    if (ngDevMode) {
      if (typeof klass !== "string") {
        throw new Error(`NgClass can only toggle CSS classes expressed as strings, got ${stringify(klass)}`);
      }
    }
    klass = klass.trim();
    if (klass.length > 0) {
      klass.split(WS_REGEXP).forEach((klass2) => {
        if (enabled) {
          this._renderer.addClass(this._ngEl.nativeElement, klass2);
        } else {
          this._renderer.removeClass(this._ngEl.nativeElement, klass2);
        }
      });
    }
  }
  static \u0275fac = function NgClass_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgClass)(\u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(Renderer2));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgClass,
    selectors: [["", "ngClass", ""]],
    inputs: {
      klass: [0, "class", "klass"],
      ngClass: "ngClass"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgClass, [{
    type: Directive,
    args: [{
      selector: "[ngClass]"
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }], {
    klass: [{
      type: Input,
      args: ["class"]
    }],
    ngClass: [{
      type: Input,
      args: ["ngClass"]
    }]
  });
})();
var NgComponentOutlet = class _NgComponentOutlet {
  _viewContainerRef;
  ngComponentOutlet = null;
  ngComponentOutletInputs;
  ngComponentOutletInjector;
  ngComponentOutletEnvironmentInjector;
  ngComponentOutletContent;
  ngComponentOutletNgModule;
  _componentRef;
  _moduleRef;
  _inputsUsed = /* @__PURE__ */ new Map();
  get componentInstance() {
    return this._componentRef?.instance ?? null;
  }
  constructor(_viewContainerRef) {
    this._viewContainerRef = _viewContainerRef;
  }
  _needToReCreateNgModuleInstance(changes) {
    return changes["ngComponentOutletNgModule"] !== void 0;
  }
  _needToReCreateComponentInstance(changes) {
    return changes["ngComponentOutlet"] !== void 0 || changes["ngComponentOutletContent"] !== void 0 || changes["ngComponentOutletInjector"] !== void 0 || changes["ngComponentOutletEnvironmentInjector"] !== void 0 || this._needToReCreateNgModuleInstance(changes);
  }
  ngOnChanges(changes) {
    if (this._needToReCreateComponentInstance(changes)) {
      this._viewContainerRef.clear();
      this._inputsUsed.clear();
      this._componentRef = void 0;
      if (this.ngComponentOutlet) {
        const injector = this.ngComponentOutletInjector || this._viewContainerRef.parentInjector;
        if (this._needToReCreateNgModuleInstance(changes)) {
          this._moduleRef?.destroy();
          if (this.ngComponentOutletNgModule) {
            this._moduleRef = createNgModule(this.ngComponentOutletNgModule, getParentInjector(injector));
          } else {
            this._moduleRef = void 0;
          }
        }
        this._componentRef = this._viewContainerRef.createComponent(this.ngComponentOutlet, {
          injector,
          ngModuleRef: this._moduleRef,
          projectableNodes: this.ngComponentOutletContent,
          environmentInjector: this.ngComponentOutletEnvironmentInjector
        });
      }
    }
  }
  ngDoCheck() {
    if (this._componentRef) {
      if (this.ngComponentOutletInputs) {
        for (const inputName of Object.keys(this.ngComponentOutletInputs)) {
          this._inputsUsed.set(inputName, true);
        }
      }
      this._applyInputStateDiff(this._componentRef);
    }
  }
  ngOnDestroy() {
    this._moduleRef?.destroy();
  }
  _applyInputStateDiff(componentRef) {
    for (const [inputName, touched] of this._inputsUsed) {
      if (!touched) {
        componentRef.setInput(inputName, void 0);
        this._inputsUsed.delete(inputName);
      } else {
        componentRef.setInput(inputName, this.ngComponentOutletInputs[inputName]);
        this._inputsUsed.set(inputName, false);
      }
    }
  }
  static \u0275fac = function NgComponentOutlet_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgComponentOutlet)(\u0275\u0275directiveInject(ViewContainerRef));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgComponentOutlet,
    selectors: [["", "ngComponentOutlet", ""]],
    inputs: {
      ngComponentOutlet: "ngComponentOutlet",
      ngComponentOutletInputs: "ngComponentOutletInputs",
      ngComponentOutletInjector: "ngComponentOutletInjector",
      ngComponentOutletEnvironmentInjector: "ngComponentOutletEnvironmentInjector",
      ngComponentOutletContent: "ngComponentOutletContent",
      ngComponentOutletNgModule: "ngComponentOutletNgModule"
    },
    exportAs: ["ngComponentOutlet"],
    features: [\u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgComponentOutlet, [{
    type: Directive,
    args: [{
      selector: "[ngComponentOutlet]",
      exportAs: "ngComponentOutlet"
    }]
  }], () => [{
    type: ViewContainerRef
  }], {
    ngComponentOutlet: [{
      type: Input
    }],
    ngComponentOutletInputs: [{
      type: Input
    }],
    ngComponentOutletInjector: [{
      type: Input
    }],
    ngComponentOutletEnvironmentInjector: [{
      type: Input
    }],
    ngComponentOutletContent: [{
      type: Input
    }],
    ngComponentOutletNgModule: [{
      type: Input
    }]
  });
})();
function getParentInjector(injector) {
  const parentNgModule = injector.get(NgModuleRef$1);
  return parentNgModule.injector;
}
var NgForOfContext = class {
  $implicit;
  ngForOf;
  index;
  count;
  constructor($implicit, ngForOf, index, count) {
    this.$implicit = $implicit;
    this.ngForOf = ngForOf;
    this.index = index;
    this.count = count;
  }
  get first() {
    return this.index === 0;
  }
  get last() {
    return this.index === this.count - 1;
  }
  get even() {
    return this.index % 2 === 0;
  }
  get odd() {
    return !this.even;
  }
};
var NgForOf = class _NgForOf {
  _viewContainer;
  _template;
  _differs;
  set ngForOf(ngForOf) {
    this._ngForOf = ngForOf;
    this._ngForOfDirty = true;
  }
  set ngForTrackBy(fn) {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && fn != null && typeof fn !== "function") {
      console.warn(`trackBy must be a function, but received ${JSON.stringify(fn)}. See https://angular.dev/api/common/NgForOf#change-propagation for more information.`);
    }
    this._trackByFn = fn;
  }
  get ngForTrackBy() {
    return this._trackByFn;
  }
  _ngForOf = null;
  _ngForOfDirty = true;
  _differ = null;
  _trackByFn;
  constructor(_viewContainer, _template, _differs) {
    this._viewContainer = _viewContainer;
    this._template = _template;
    this._differs = _differs;
  }
  set ngForTemplate(value) {
    if (value) {
      this._template = value;
    }
  }
  ngDoCheck() {
    if (this._ngForOfDirty) {
      this._ngForOfDirty = false;
      const value = this._ngForOf;
      if (!this._differ && value) {
        if (typeof ngDevMode === "undefined" || ngDevMode) {
          try {
            this._differ = this._differs.find(value).create(this.ngForTrackBy);
          } catch {
            let errorMessage = `Cannot find a differ supporting object '${value}' of type '${getTypeName(value)}'. NgFor only supports binding to Iterables, such as Arrays.`;
            if (typeof value === "object") {
              errorMessage += " Did you mean to use the keyvalue pipe?";
            }
            throw new RuntimeError(-2200, errorMessage);
          }
        } else {
          this._differ = this._differs.find(value).create(this.ngForTrackBy);
        }
      }
    }
    if (this._differ) {
      const changes = this._differ.diff(this._ngForOf);
      if (changes) this._applyChanges(changes);
    }
  }
  _applyChanges(changes) {
    const viewContainer = this._viewContainer;
    changes.forEachOperation((item, adjustedPreviousIndex, currentIndex) => {
      if (item.previousIndex == null) {
        viewContainer.createEmbeddedView(this._template, new NgForOfContext(item.item, this._ngForOf, -1, -1), currentIndex === null ? void 0 : currentIndex);
      } else if (currentIndex == null) {
        viewContainer.remove(adjustedPreviousIndex === null ? void 0 : adjustedPreviousIndex);
      } else if (adjustedPreviousIndex !== null) {
        const view = viewContainer.get(adjustedPreviousIndex);
        viewContainer.move(view, currentIndex);
        applyViewChange(view, item);
      }
    });
    for (let i = 0, ilen = viewContainer.length; i < ilen; i++) {
      const viewRef = viewContainer.get(i);
      const context2 = viewRef.context;
      context2.index = i;
      context2.count = ilen;
      context2.ngForOf = this._ngForOf;
    }
    changes.forEachIdentityChange((record) => {
      const viewRef = viewContainer.get(record.currentIndex);
      applyViewChange(viewRef, record);
    });
  }
  static ngTemplateContextGuard(dir, ctx) {
    return true;
  }
  static \u0275fac = function NgForOf_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgForOf)(\u0275\u0275directiveInject(ViewContainerRef), \u0275\u0275directiveInject(TemplateRef), \u0275\u0275directiveInject(IterableDiffers));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgForOf,
    selectors: [["", "ngFor", "", "ngForOf", ""]],
    inputs: {
      ngForOf: "ngForOf",
      ngForTrackBy: "ngForTrackBy",
      ngForTemplate: "ngForTemplate"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgForOf, [{
    type: Directive,
    args: [{
      selector: "[ngFor][ngForOf]"
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: TemplateRef
  }, {
    type: IterableDiffers
  }], {
    ngForOf: [{
      type: Input
    }],
    ngForTrackBy: [{
      type: Input
    }],
    ngForTemplate: [{
      type: Input
    }]
  });
})();
function applyViewChange(view, record) {
  view.context.$implicit = record.item;
}
function getTypeName(type) {
  return type["name"] || typeof type;
}
var NgIf = class _NgIf {
  _viewContainer;
  _context = new NgIfContext();
  _thenTemplateRef = null;
  _elseTemplateRef = null;
  _thenViewRef = null;
  _elseViewRef = null;
  constructor(_viewContainer, templateRef) {
    this._viewContainer = _viewContainer;
    this._thenTemplateRef = templateRef;
  }
  set ngIf(condition) {
    this._context.$implicit = this._context.ngIf = condition;
    this._updateView();
  }
  set ngIfThen(templateRef) {
    assertTemplate(templateRef, (typeof ngDevMode === "undefined" || ngDevMode) && "ngIfThen");
    this._thenTemplateRef = templateRef;
    this._thenViewRef = null;
    this._updateView();
  }
  set ngIfElse(templateRef) {
    assertTemplate(templateRef, (typeof ngDevMode === "undefined" || ngDevMode) && "ngIfElse");
    this._elseTemplateRef = templateRef;
    this._elseViewRef = null;
    this._updateView();
  }
  _updateView() {
    if (this._context.$implicit) {
      if (!this._thenViewRef) {
        this._viewContainer.clear();
        this._elseViewRef = null;
        if (this._thenTemplateRef) {
          this._thenViewRef = this._viewContainer.createEmbeddedView(this._thenTemplateRef, this._context);
        }
      }
    } else {
      if (!this._elseViewRef) {
        this._viewContainer.clear();
        this._thenViewRef = null;
        if (this._elseTemplateRef) {
          this._elseViewRef = this._viewContainer.createEmbeddedView(this._elseTemplateRef, this._context);
        }
      }
    }
  }
  static ngIfUseIfTypeGuard;
  static ngTemplateGuard_ngIf;
  static ngTemplateContextGuard(dir, ctx) {
    return true;
  }
  static \u0275fac = function NgIf_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgIf)(\u0275\u0275directiveInject(ViewContainerRef), \u0275\u0275directiveInject(TemplateRef));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgIf,
    selectors: [["", "ngIf", ""]],
    inputs: {
      ngIf: "ngIf",
      ngIfThen: "ngIfThen",
      ngIfElse: "ngIfElse"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgIf, [{
    type: Directive,
    args: [{
      selector: "[ngIf]"
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: TemplateRef
  }], {
    ngIf: [{
      type: Input
    }],
    ngIfThen: [{
      type: Input
    }],
    ngIfElse: [{
      type: Input
    }]
  });
})();
var NgIfContext = class {
  $implicit = null;
  ngIf = null;
};
function assertTemplate(templateRef, property) {
  if (templateRef && !templateRef.createEmbeddedView) {
    throw new RuntimeError(2020, (typeof ngDevMode === "undefined" || ngDevMode) && `${property} must be a TemplateRef, but received '${stringify(templateRef)}'.`);
  }
}
var SwitchView = class {
  _viewContainerRef;
  _templateRef;
  _created = false;
  constructor(_viewContainerRef, _templateRef) {
    this._viewContainerRef = _viewContainerRef;
    this._templateRef = _templateRef;
  }
  create() {
    this._created = true;
    this._viewContainerRef.createEmbeddedView(this._templateRef);
  }
  destroy() {
    this._created = false;
    this._viewContainerRef.clear();
  }
  enforceState(created) {
    if (created && !this._created) {
      this.create();
    } else if (!created && this._created) {
      this.destroy();
    }
  }
};
var NgSwitch = class _NgSwitch {
  _defaultViews = [];
  _defaultUsed = false;
  _caseCount = 0;
  _lastCaseCheckIndex = 0;
  _lastCasesMatched = false;
  _ngSwitch;
  set ngSwitch(newValue) {
    this._ngSwitch = newValue;
    if (this._caseCount === 0) {
      this._updateDefaultCases(true);
    }
  }
  _addCase() {
    return this._caseCount++;
  }
  _addDefault(view) {
    this._defaultViews.push(view);
  }
  _matchCase(value) {
    const matched = value === this._ngSwitch;
    this._lastCasesMatched ||= matched;
    this._lastCaseCheckIndex++;
    if (this._lastCaseCheckIndex === this._caseCount) {
      this._updateDefaultCases(!this._lastCasesMatched);
      this._lastCaseCheckIndex = 0;
      this._lastCasesMatched = false;
    }
    return matched;
  }
  _updateDefaultCases(useDefault) {
    if (this._defaultViews.length > 0 && useDefault !== this._defaultUsed) {
      this._defaultUsed = useDefault;
      for (const defaultView of this._defaultViews) {
        defaultView.enforceState(useDefault);
      }
    }
  }
  static \u0275fac = function NgSwitch_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgSwitch)();
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgSwitch,
    selectors: [["", "ngSwitch", ""]],
    inputs: {
      ngSwitch: "ngSwitch"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSwitch, [{
    type: Directive,
    args: [{
      selector: "[ngSwitch]"
    }]
  }], null, {
    ngSwitch: [{
      type: Input
    }]
  });
})();
var NgSwitchCase = class _NgSwitchCase {
  ngSwitch;
  _view;
  ngSwitchCase;
  constructor(viewContainer, templateRef, ngSwitch) {
    this.ngSwitch = ngSwitch;
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !ngSwitch) {
      throwNgSwitchProviderNotFoundError("ngSwitchCase", "NgSwitchCase");
    }
    ngSwitch._addCase();
    this._view = new SwitchView(viewContainer, templateRef);
  }
  ngDoCheck() {
    this._view.enforceState(this.ngSwitch._matchCase(this.ngSwitchCase));
  }
  static \u0275fac = function NgSwitchCase_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgSwitchCase)(\u0275\u0275directiveInject(ViewContainerRef), \u0275\u0275directiveInject(TemplateRef), \u0275\u0275directiveInject(NgSwitch, 9));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgSwitchCase,
    selectors: [["", "ngSwitchCase", ""]],
    inputs: {
      ngSwitchCase: "ngSwitchCase"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSwitchCase, [{
    type: Directive,
    args: [{
      selector: "[ngSwitchCase]"
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: TemplateRef
  }, {
    type: NgSwitch,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], {
    ngSwitchCase: [{
      type: Input
    }]
  });
})();
var NgSwitchDefault = class _NgSwitchDefault {
  constructor(viewContainer, templateRef, ngSwitch) {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !ngSwitch) {
      throwNgSwitchProviderNotFoundError("ngSwitchDefault", "NgSwitchDefault");
    }
    ngSwitch._addDefault(new SwitchView(viewContainer, templateRef));
  }
  static \u0275fac = function NgSwitchDefault_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgSwitchDefault)(\u0275\u0275directiveInject(ViewContainerRef), \u0275\u0275directiveInject(TemplateRef), \u0275\u0275directiveInject(NgSwitch, 9));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgSwitchDefault,
    selectors: [["", "ngSwitchDefault", ""]]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSwitchDefault, [{
    type: Directive,
    args: [{
      selector: "[ngSwitchDefault]"
    }]
  }], () => [{
    type: ViewContainerRef
  }, {
    type: TemplateRef
  }, {
    type: NgSwitch,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], null);
})();
function throwNgSwitchProviderNotFoundError(attrName, directiveName) {
  throw new RuntimeError(2e3, `An element with the "${attrName}" attribute (matching the "${directiveName}" directive) must be located inside an element with the "ngSwitch" attribute (matching "NgSwitch" directive)`);
}
var NgPlural = class _NgPlural {
  _localization;
  _activeView;
  _caseViews = {};
  constructor(_localization) {
    this._localization = _localization;
  }
  set ngPlural(value) {
    this._updateView(value);
  }
  addCase(value, switchView) {
    this._caseViews[value] = switchView;
  }
  _updateView(switchValue) {
    this._clearViews();
    const cases = Object.keys(this._caseViews);
    const key = getPluralCategory(switchValue, cases, this._localization);
    this._activateView(this._caseViews[key]);
  }
  _clearViews() {
    if (this._activeView) this._activeView.destroy();
  }
  _activateView(view) {
    if (view) {
      this._activeView = view;
      this._activeView.create();
    }
  }
  static \u0275fac = function NgPlural_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgPlural)(\u0275\u0275directiveInject(NgLocalization));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgPlural,
    selectors: [["", "ngPlural", ""]],
    inputs: {
      ngPlural: "ngPlural"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgPlural, [{
    type: Directive,
    args: [{
      selector: "[ngPlural]"
    }]
  }], () => [{
    type: NgLocalization
  }], {
    ngPlural: [{
      type: Input
    }]
  });
})();
var NgPluralCase = class _NgPluralCase {
  value;
  constructor(value, template, viewContainer, ngPlural) {
    this.value = value;
    const isANumber = !isNaN(Number(value));
    ngPlural.addCase(isANumber ? `=${value}` : value, new SwitchView(viewContainer, template));
  }
  static \u0275fac = function NgPluralCase_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgPluralCase)(\u0275\u0275injectAttribute("ngPluralCase"), \u0275\u0275directiveInject(TemplateRef), \u0275\u0275directiveInject(ViewContainerRef), \u0275\u0275directiveInject(NgPlural, 1));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgPluralCase,
    selectors: [["", "ngPluralCase", ""]]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgPluralCase, [{
    type: Directive,
    args: [{
      selector: "[ngPluralCase]"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Attribute,
      args: ["ngPluralCase"]
    }]
  }, {
    type: TemplateRef
  }, {
    type: ViewContainerRef
  }, {
    type: NgPlural,
    decorators: [{
      type: Host
    }]
  }], null);
})();
var NgStyle = class _NgStyle {
  _ngEl;
  _differs;
  _renderer;
  _ngStyle = null;
  _differ = null;
  constructor(_ngEl, _differs, _renderer) {
    this._ngEl = _ngEl;
    this._differs = _differs;
    this._renderer = _renderer;
  }
  set ngStyle(values) {
    this._ngStyle = values;
    if (!this._differ && values) {
      this._differ = this._differs.find(values).create();
    }
  }
  ngDoCheck() {
    if (this._differ) {
      const changes = this._differ.diff(this._ngStyle);
      if (changes) {
        this._applyChanges(changes);
      }
    }
  }
  _setStyle(nameAndUnit, value) {
    const [name, unit] = nameAndUnit.split(".");
    const flags = name.indexOf("-") === -1 ? void 0 : RendererStyleFlags2.DashCase;
    if (value != null) {
      this._renderer.setStyle(this._ngEl.nativeElement, name, unit ? `${value}${unit}` : value, flags);
    } else {
      this._renderer.removeStyle(this._ngEl.nativeElement, name, flags);
    }
  }
  _applyChanges(changes) {
    changes.forEachRemovedItem((record) => this._setStyle(record.key, null));
    changes.forEachAddedItem((record) => this._setStyle(record.key, record.currentValue));
    changes.forEachChangedItem((record) => this._setStyle(record.key, record.currentValue));
  }
  static \u0275fac = function NgStyle_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgStyle)(\u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(KeyValueDiffers), \u0275\u0275directiveInject(Renderer2));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgStyle,
    selectors: [["", "ngStyle", ""]],
    inputs: {
      ngStyle: "ngStyle"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgStyle, [{
    type: Directive,
    args: [{
      selector: "[ngStyle]"
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: KeyValueDiffers
  }, {
    type: Renderer2
  }], {
    ngStyle: [{
      type: Input,
      args: ["ngStyle"]
    }]
  });
})();
var NgTemplateOutlet = class _NgTemplateOutlet {
  _viewContainerRef;
  _viewRef = null;
  ngTemplateOutletContext = null;
  ngTemplateOutlet = null;
  ngTemplateOutletInjector = null;
  injector = inject2(Injector);
  constructor(_viewContainerRef) {
    this._viewContainerRef = _viewContainerRef;
  }
  ngOnChanges(changes) {
    if (this._shouldRecreateView(changes)) {
      const viewContainerRef = this._viewContainerRef;
      if (this._viewRef) {
        viewContainerRef.remove(viewContainerRef.indexOf(this._viewRef));
      }
      if (!this.ngTemplateOutlet) {
        this._viewRef = null;
        return;
      }
      const viewContext = this._createContextForwardProxy();
      this._viewRef = viewContainerRef.createEmbeddedView(this.ngTemplateOutlet, viewContext, {
        injector: this._getInjector()
      });
    }
  }
  _getInjector() {
    if (this.ngTemplateOutletInjector === "outlet") {
      return this.injector;
    }
    return this.ngTemplateOutletInjector ?? void 0;
  }
  _shouldRecreateView(changes) {
    return !!changes["ngTemplateOutlet"] || !!changes["ngTemplateOutletInjector"];
  }
  _createContextForwardProxy() {
    return new Proxy({}, {
      set: (_target, prop, newValue) => {
        if (!this.ngTemplateOutletContext) {
          return false;
        }
        return Reflect.set(this.ngTemplateOutletContext, prop, newValue);
      },
      get: (_target, prop, receiver) => {
        if (!this.ngTemplateOutletContext) {
          return void 0;
        }
        return Reflect.get(this.ngTemplateOutletContext, prop, receiver);
      }
    });
  }
  static \u0275fac = function NgTemplateOutlet_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgTemplateOutlet)(\u0275\u0275directiveInject(ViewContainerRef));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgTemplateOutlet,
    selectors: [["", "ngTemplateOutlet", ""]],
    inputs: {
      ngTemplateOutletContext: "ngTemplateOutletContext",
      ngTemplateOutlet: "ngTemplateOutlet",
      ngTemplateOutletInjector: "ngTemplateOutletInjector"
    },
    features: [\u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgTemplateOutlet, [{
    type: Directive,
    args: [{
      selector: "[ngTemplateOutlet]"
    }]
  }], () => [{
    type: ViewContainerRef
  }], {
    ngTemplateOutletContext: [{
      type: Input
    }],
    ngTemplateOutlet: [{
      type: Input
    }],
    ngTemplateOutletInjector: [{
      type: Input
    }]
  });
})();
var COMMON_DIRECTIVES = [NgClass, NgComponentOutlet, NgForOf, NgIf, NgTemplateOutlet, NgStyle, NgSwitch, NgSwitchCase, NgSwitchDefault, NgPlural, NgPluralCase];
function invalidPipeArgumentError(type, value) {
  return new RuntimeError(2100, ngDevMode && `InvalidPipeArgument: '${value}' for pipe '${stringify(type)}'`);
}
function warnIfSignal(pipeName, value) {
  if (isSignal2(value)) {
    console.warn(`The ${pipeName} does not unwrap signals. Received a signal with value:`, value());
  }
}
var SubscribableStrategy = class {
  createSubscription(async, updateLatestValue, onError) {
    return untracked2(() => async.subscribe({
      next: updateLatestValue,
      error: onError
    }));
  }
  dispose(subscription) {
    untracked2(() => subscription.unsubscribe());
  }
};
var PromiseStrategy = class {
  createSubscription(async, updateLatestValue, onError) {
    async.then((v) => updateLatestValue?.(v), (e) => onError?.(e));
    return {
      unsubscribe: () => {
        updateLatestValue = null;
        onError = null;
      }
    };
  }
  dispose(subscription) {
    subscription.unsubscribe();
  }
};
var _promiseStrategy = new PromiseStrategy();
var _subscribableStrategy = new SubscribableStrategy();
var AsyncPipe = class _AsyncPipe {
  _ref;
  _latestValue = null;
  markForCheckOnValueUpdate = true;
  _subscription = null;
  _obj = null;
  _strategy = null;
  applicationErrorHandler = inject2(INTERNAL_APPLICATION_ERROR_HANDLER);
  constructor(ref) {
    this._ref = ref;
  }
  ngOnDestroy() {
    if (this._subscription) {
      this._dispose();
    }
    this._ref = null;
  }
  transform(obj) {
    if (!this._obj) {
      if (obj) {
        try {
          this.markForCheckOnValueUpdate = false;
          this._subscribe(obj);
        } finally {
          this.markForCheckOnValueUpdate = true;
        }
      }
      return this._latestValue;
    }
    if (obj !== this._obj) {
      this._dispose();
      return this.transform(obj);
    }
    return this._latestValue;
  }
  _subscribe(obj) {
    this._obj = obj;
    this._strategy = this._selectStrategy(obj);
    this._subscription = this._strategy.createSubscription(obj, (value) => this._updateLatestValue(obj, value), (e) => this.applicationErrorHandler(e));
  }
  _selectStrategy(obj) {
    if (isPromise2(obj)) {
      return _promiseStrategy;
    }
    if (isSubscribable(obj)) {
      return _subscribableStrategy;
    }
    throw invalidPipeArgumentError(_AsyncPipe, obj);
  }
  _dispose() {
    this._strategy.dispose(this._subscription);
    this._latestValue = null;
    this._subscription = null;
    this._obj = null;
  }
  _updateLatestValue(async, value) {
    if (async === this._obj) {
      this._latestValue = value;
      if (this.markForCheckOnValueUpdate) {
        this._ref?.markForCheck();
      }
    }
  }
  static \u0275fac = function AsyncPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AsyncPipe)(\u0275\u0275directiveInject(ChangeDetectorRef, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "async",
    type: _AsyncPipe,
    pure: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AsyncPipe, [{
    type: Pipe,
    args: [{
      name: "async",
      pure: false
    }]
  }], () => [{
    type: ChangeDetectorRef
  }], null);
})();
var LowerCasePipe = class _LowerCasePipe {
  transform(value) {
    if (value == null) return null;
    assertPipeArgument(_LowerCasePipe, value);
    return value.toLowerCase();
  }
  static \u0275fac = function LowerCasePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _LowerCasePipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "lowercase",
    type: _LowerCasePipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LowerCasePipe, [{
    type: Pipe,
    args: [{
      name: "lowercase"
    }]
  }], null, null);
})();
var unicodeWordMatch = /(?:[0-9A-Za-z\xAA\xB5\xBA\xC0-\xD6\xD8-\xF6\xF8-\u02C1\u02C6-\u02D1\u02E0-\u02E4\u02EC\u02EE\u0370-\u0374\u0376\u0377\u037A-\u037D\u037F\u0386\u0388-\u038A\u038C\u038E-\u03A1\u03A3-\u03F5\u03F7-\u0481\u048A-\u052F\u0531-\u0556\u0559\u0560-\u0588\u05D0-\u05EA\u05EF-\u05F2\u0620-\u064A\u066E\u066F\u0671-\u06D3\u06D5\u06E5\u06E6\u06EE\u06EF\u06FA-\u06FC\u06FF\u0710\u0712-\u072F\u074D-\u07A5\u07B1\u07CA-\u07EA\u07F4\u07F5\u07FA\u0800-\u0815\u081A\u0824\u0828\u0840-\u0858\u0860-\u086A\u0870-\u0887\u0889-\u088E\u08A0-\u08C9\u0904-\u0939\u093D\u0950\u0958-\u0961\u0971-\u0980\u0985-\u098C\u098F\u0990\u0993-\u09A8\u09AA-\u09B0\u09B2\u09B6-\u09B9\u09BD\u09CE\u09DC\u09DD\u09DF-\u09E1\u09F0\u09F1\u09FC\u0A05-\u0A0A\u0A0F\u0A10\u0A13-\u0A28\u0A2A-\u0A30\u0A32\u0A33\u0A35\u0A36\u0A38\u0A39\u0A59-\u0A5C\u0A5E\u0A72-\u0A74\u0A85-\u0A8D\u0A8F-\u0A91\u0A93-\u0AA8\u0AAA-\u0AB0\u0AB2\u0AB3\u0AB5-\u0AB9\u0ABD\u0AD0\u0AE0\u0AE1\u0AF9\u0B05-\u0B0C\u0B0F\u0B10\u0B13-\u0B28\u0B2A-\u0B30\u0B32\u0B33\u0B35-\u0B39\u0B3D\u0B5C\u0B5D\u0B5F-\u0B61\u0B71\u0B83\u0B85-\u0B8A\u0B8E-\u0B90\u0B92-\u0B95\u0B99\u0B9A\u0B9C\u0B9E\u0B9F\u0BA3\u0BA4\u0BA8-\u0BAA\u0BAE-\u0BB9\u0BD0\u0C05-\u0C0C\u0C0E-\u0C10\u0C12-\u0C28\u0C2A-\u0C39\u0C3D\u0C58-\u0C5A\u0C5D\u0C60\u0C61\u0C80\u0C85-\u0C8C\u0C8E-\u0C90\u0C92-\u0CA8\u0CAA-\u0CB3\u0CB5-\u0CB9\u0CBD\u0CDD\u0CDE\u0CE0\u0CE1\u0CF1\u0CF2\u0D04-\u0D0C\u0D0E-\u0D10\u0D12-\u0D3A\u0D3D\u0D4E\u0D54-\u0D56\u0D5F-\u0D61\u0D7A-\u0D7F\u0D85-\u0D96\u0D9A-\u0DB1\u0DB3-\u0DBB\u0DBD\u0DC0-\u0DC6\u0E01-\u0E30\u0E32\u0E33\u0E40-\u0E46\u0E81\u0E82\u0E84\u0E86-\u0E8A\u0E8C-\u0EA3\u0EA5\u0EA7-\u0EB0\u0EB2\u0EB3\u0EBD\u0EC0-\u0EC4\u0EC6\u0EDC-\u0EDF\u0F00\u0F40-\u0F47\u0F49-\u0F6C\u0F88-\u0F8C\u1000-\u102A\u103F\u1050-\u1055\u105A-\u105D\u1061\u1065\u1066\u106E-\u1070\u1075-\u1081\u108E\u10A0-\u10C5\u10C7\u10CD\u10D0-\u10FA\u10FC-\u1248\u124A-\u124D\u1250-\u1256\u1258\u125A-\u125D\u1260-\u1288\u128A-\u128D\u1290-\u12B0\u12B2-\u12B5\u12B8-\u12BE\u12C0\u12C2-\u12C5\u12C8-\u12D6\u12D8-\u1310\u1312-\u1315\u1318-\u135A\u1380-\u138F\u13A0-\u13F5\u13F8-\u13FD\u1401-\u166C\u166F-\u167F\u1681-\u169A\u16A0-\u16EA\u16F1-\u16F8\u1700-\u1711\u171F-\u1731\u1740-\u1751\u1760-\u176C\u176E-\u1770\u1780-\u17B3\u17D7\u17DC\u1820-\u1878\u1880-\u1884\u1887-\u18A8\u18AA\u18B0-\u18F5\u1900-\u191E\u1950-\u196D\u1970-\u1974\u1980-\u19AB\u19B0-\u19C9\u1A00-\u1A16\u1A20-\u1A54\u1AA7\u1B05-\u1B33\u1B45-\u1B4C\u1B83-\u1BA0\u1BAE\u1BAF\u1BBA-\u1BE5\u1C00-\u1C23\u1C4D-\u1C4F\u1C5A-\u1C7D\u1C80-\u1C88\u1C90-\u1CBA\u1CBD-\u1CBF\u1CE9-\u1CEC\u1CEE-\u1CF3\u1CF5\u1CF6\u1CFA\u1D00-\u1DBF\u1E00-\u1F15\u1F18-\u1F1D\u1F20-\u1F45\u1F48-\u1F4D\u1F50-\u1F57\u1F59\u1F5B\u1F5D\u1F5F-\u1F7D\u1F80-\u1FB4\u1FB6-\u1FBC\u1FBE\u1FC2-\u1FC4\u1FC6-\u1FCC\u1FD0-\u1FD3\u1FD6-\u1FDB\u1FE0-\u1FEC\u1FF2-\u1FF4\u1FF6-\u1FFC\u2071\u207F\u2090-\u209C\u2102\u2107\u210A-\u2113\u2115\u2119-\u211D\u2124\u2126\u2128\u212A-\u212D\u212F-\u2139\u213C-\u213F\u2145-\u2149\u214E\u2183\u2184\u2C00-\u2CE4\u2CEB-\u2CEE\u2CF2\u2CF3\u2D00-\u2D25\u2D27\u2D2D\u2D30-\u2D67\u2D6F\u2D80-\u2D96\u2DA0-\u2DA6\u2DA8-\u2DAE\u2DB0-\u2DB6\u2DB8-\u2DBE\u2DC0-\u2DC6\u2DC8-\u2DCE\u2DD0-\u2DD6\u2DD8-\u2DDE\u2E2F\u3005\u3006\u3031-\u3035\u303B\u303C\u3041-\u3096\u309D-\u309F\u30A1-\u30FA\u30FC-\u30FF\u3105-\u312F\u3131-\u318E\u31A0-\u31BF\u31F0-\u31FF\u3400-\u4DBF\u4E00-\uA48C\uA4D0-\uA4FD\uA500-\uA60C\uA610-\uA61F\uA62A\uA62B\uA640-\uA66E\uA67F-\uA69D\uA6A0-\uA6E5\uA717-\uA71F\uA722-\uA788\uA78B-\uA7CA\uA7D0\uA7D1\uA7D3\uA7D5-\uA7D9\uA7F2-\uA801\uA803-\uA805\uA807-\uA80A\uA80C-\uA822\uA840-\uA873\uA882-\uA8B3\uA8F2-\uA8F7\uA8FB\uA8FD\uA8FE\uA90A-\uA925\uA930-\uA946\uA960-\uA97C\uA984-\uA9B2\uA9CF\uA9E0-\uA9E4\uA9E6-\uA9EF\uA9FA-\uA9FE\uAA00-\uAA28\uAA40-\uAA42\uAA44-\uAA4B\uAA60-\uAA76\uAA7A\uAA7E-\uAAAF\uAAB1\uAAB5\uAAB6\uAAB9-\uAABD\uAAC0\uAAC2\uAADB-\uAADD\uAAE0-\uAAEA\uAAF2-\uAAF4\uAB01-\uAB06\uAB09-\uAB0E\uAB11-\uAB16\uAB20-\uAB26\uAB28-\uAB2E\uAB30-\uAB5A\uAB5C-\uAB69\uAB70-\uABE2\uAC00-\uD7A3\uD7B0-\uD7C6\uD7CB-\uD7FB\uF900-\uFA6D\uFA70-\uFAD9\uFB00-\uFB06\uFB13-\uFB17\uFB1D\uFB1F-\uFB28\uFB2A-\uFB36\uFB38-\uFB3C\uFB3E\uFB40\uFB41\uFB43\uFB44\uFB46-\uFBB1\uFBD3-\uFD3D\uFD50-\uFD8F\uFD92-\uFDC7\uFDF0-\uFDFB\uFE70-\uFE74\uFE76-\uFEFC\uFF21-\uFF3A\uFF41-\uFF5A\uFF66-\uFFBE\uFFC2-\uFFC7\uFFCA-\uFFCF\uFFD2-\uFFD7\uFFDA-\uFFDC]|\uD800[\uDC00-\uDC0B\uDC0D-\uDC26\uDC28-\uDC3A\uDC3C\uDC3D\uDC3F-\uDC4D\uDC50-\uDC5D\uDC80-\uDCFA\uDE80-\uDE9C\uDEA0-\uDED0\uDF00-\uDF1F\uDF2D-\uDF40\uDF42-\uDF49\uDF50-\uDF75\uDF80-\uDF9D\uDFA0-\uDFC3\uDFC8-\uDFCF]|\uD801[\uDC00-\uDC9D\uDCB0-\uDCD3\uDCD8-\uDCFB\uDD00-\uDD27\uDD30-\uDD63\uDD70-\uDD7A\uDD7C-\uDD8A\uDD8C-\uDD92\uDD94\uDD95\uDD97-\uDDA1\uDDA3-\uDDB1\uDDB3-\uDDB9\uDDBB\uDDBC\uDE00-\uDF36\uDF40-\uDF55\uDF60-\uDF67\uDF80-\uDF85\uDF87-\uDFB0\uDFB2-\uDFBA]|\uD802[\uDC00-\uDC05\uDC08\uDC0A-\uDC35\uDC37\uDC38\uDC3C\uDC3F-\uDC55\uDC60-\uDC76\uDC80-\uDC9E\uDCE0-\uDCF2\uDCF4\uDCF5\uDD00-\uDD15\uDD20-\uDD39\uDD80-\uDDB7\uDDBE\uDDBF\uDE00\uDE10-\uDE13\uDE15-\uDE17\uDE19-\uDE35\uDE60-\uDE7C\uDE80-\uDE9C\uDEC0-\uDEC7\uDEC9-\uDEE4\uDF00-\uDF35\uDF40-\uDF55\uDF60-\uDF72\uDF80-\uDF91]|\uD803[\uDC00-\uDC48\uDC80-\uDCB2\uDCC0-\uDCF2\uDD00-\uDD23\uDE80-\uDEA9\uDEB0\uDEB1\uDF00-\uDF1C\uDF27\uDF30-\uDF45\uDF70-\uDF81\uDFB0-\uDFC4\uDFE0-\uDFF6]|\uD804[\uDC03-\uDC37\uDC71\uDC72\uDC75\uDC83-\uDCAF\uDCD0-\uDCE8\uDD03-\uDD26\uDD44\uDD47\uDD50-\uDD72\uDD76\uDD83-\uDDB2\uDDC1-\uDDC4\uDDDA\uDDDC\uDE00-\uDE11\uDE13-\uDE2B\uDE80-\uDE86\uDE88\uDE8A-\uDE8D\uDE8F-\uDE9D\uDE9F-\uDEA8\uDEB0-\uDEDE\uDF05-\uDF0C\uDF0F\uDF10\uDF13-\uDF28\uDF2A-\uDF30\uDF32\uDF33\uDF35-\uDF39\uDF3D\uDF50\uDF5D-\uDF61]|\uD805[\uDC00-\uDC34\uDC47-\uDC4A\uDC5F-\uDC61\uDC80-\uDCAF\uDCC4\uDCC5\uDCC7\uDD80-\uDDAE\uDDD8-\uDDDB\uDE00-\uDE2F\uDE44\uDE80-\uDEAA\uDEB8\uDF00-\uDF1A\uDF40-\uDF46]|\uD806[\uDC00-\uDC2B\uDCA0-\uDCDF\uDCFF-\uDD06\uDD09\uDD0C-\uDD13\uDD15\uDD16\uDD18-\uDD2F\uDD3F\uDD41\uDDA0-\uDDA7\uDDAA-\uDDD0\uDDE1\uDDE3\uDE00\uDE0B-\uDE32\uDE3A\uDE50\uDE5C-\uDE89\uDE9D\uDEB0-\uDEF8]|\uD807[\uDC00-\uDC08\uDC0A-\uDC2E\uDC40\uDC72-\uDC8F\uDD00-\uDD06\uDD08\uDD09\uDD0B-\uDD30\uDD46\uDD60-\uDD65\uDD67\uDD68\uDD6A-\uDD89\uDD98\uDEE0-\uDEF2\uDFB0]|\uD808[\uDC00-\uDF99]|\uD809[\uDC80-\uDD43]|\uD80B[\uDF90-\uDFF0]|[\uD80C\uD81C-\uD820\uD822\uD840-\uD868\uD86A-\uD86C\uD86F-\uD872\uD874-\uD879\uD880-\uD883][\uDC00-\uDFFF]|\uD80D[\uDC00-\uDC2E]|\uD811[\uDC00-\uDE46]|\uD81A[\uDC00-\uDE38\uDE40-\uDE5E\uDE70-\uDEBE\uDED0-\uDEED\uDF00-\uDF2F\uDF40-\uDF43\uDF63-\uDF77\uDF7D-\uDF8F]|\uD81B[\uDE40-\uDE7F\uDF00-\uDF4A\uDF50\uDF93-\uDF9F\uDFE0\uDFE1\uDFE3]|\uD821[\uDC00-\uDFF7]|\uD823[\uDC00-\uDCD5\uDD00-\uDD08]|\uD82B[\uDFF0-\uDFF3\uDFF5-\uDFFB\uDFFD\uDFFE]|\uD82C[\uDC00-\uDD22\uDD50-\uDD52\uDD64-\uDD67\uDD70-\uDEFB]|\uD82F[\uDC00-\uDC6A\uDC70-\uDC7C\uDC80-\uDC88\uDC90-\uDC99]|\uD835[\uDC00-\uDC54\uDC56-\uDC9C\uDC9E\uDC9F\uDCA2\uDCA5\uDCA6\uDCA9-\uDCAC\uDCAE-\uDCB9\uDCBB\uDCBD-\uDCC3\uDCC5-\uDD05\uDD07-\uDD0A\uDD0D-\uDD14\uDD16-\uDD1C\uDD1E-\uDD39\uDD3B-\uDD3E\uDD40-\uDD44\uDD46\uDD4A-\uDD50\uDD52-\uDEA5\uDEA8-\uDEC0\uDEC2-\uDEDA\uDEDC-\uDEFA\uDEFC-\uDF14\uDF16-\uDF34\uDF36-\uDF4E\uDF50-\uDF6E\uDF70-\uDF88\uDF8A-\uDFA8\uDFAA-\uDFC2\uDFC4-\uDFCB]|\uD837[\uDF00-\uDF1E]|\uD838[\uDD00-\uDD2C\uDD37-\uDD3D\uDD4E\uDE90-\uDEAD\uDEC0-\uDEEB]|\uD839[\uDFE0-\uDFE6\uDFE8-\uDFEB\uDFED\uDFEE\uDFF0-\uDFFE]|\uD83A[\uDC00-\uDCC4\uDD00-\uDD43\uDD4B]|\uD83B[\uDE00-\uDE03\uDE05-\uDE1F\uDE21\uDE22\uDE24\uDE27\uDE29-\uDE32\uDE34-\uDE37\uDE39\uDE3B\uDE42\uDE47\uDE49\uDE4B\uDE4D-\uDE4F\uDE51\uDE52\uDE54\uDE57\uDE59\uDE5B\uDE5D\uDE5F\uDE61\uDE62\uDE64\uDE67-\uDE6A\uDE6C-\uDE72\uDE74-\uDE77\uDE79-\uDE7C\uDE7E\uDE80-\uDE89\uDE8B-\uDE9B\uDEA1-\uDEA3\uDEA5-\uDEA9\uDEAB-\uDEBB]|\uD869[\uDC00-\uDEDF\uDF00-\uDFFF]|\uD86D[\uDC00-\uDF38\uDF40-\uDFFF]|\uD86E[\uDC00-\uDC1D\uDC20-\uDFFF]|\uD873[\uDC00-\uDEA1\uDEB0-\uDFFF]|\uD87A[\uDC00-\uDFE0]|\uD87E[\uDC00-\uDE1D]|\uD884[\uDC00-\uDF4A])\S*/g;
var TitleCasePipe = class _TitleCasePipe {
  transform(value) {
    if (value == null) return null;
    assertPipeArgument(_TitleCasePipe, value);
    return value.replace(unicodeWordMatch, (txt) => txt[0].toUpperCase() + txt.slice(1).toLowerCase());
  }
  static \u0275fac = function TitleCasePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TitleCasePipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "titlecase",
    type: _TitleCasePipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TitleCasePipe, [{
    type: Pipe,
    args: [{
      name: "titlecase"
    }]
  }], null, null);
})();
var UpperCasePipe = class _UpperCasePipe {
  transform(value) {
    if (value == null) return null;
    assertPipeArgument(_UpperCasePipe, value);
    return value.toUpperCase();
  }
  static \u0275fac = function UpperCasePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _UpperCasePipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "uppercase",
    type: _UpperCasePipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UpperCasePipe, [{
    type: Pipe,
    args: [{
      name: "uppercase"
    }]
  }], null, null);
})();
function assertPipeArgument(pipe2, value) {
  if (typeof value !== "string") {
    throw invalidPipeArgumentError(pipe2, value);
  }
}
var DEFAULT_DATE_FORMAT = "mediumDate";
var DATE_PIPE_DEFAULT_TIMEZONE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DATE_PIPE_DEFAULT_TIMEZONE" : "");
var DATE_PIPE_DEFAULT_OPTIONS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "DATE_PIPE_DEFAULT_OPTIONS" : "");
var DatePipe = class _DatePipe {
  locale;
  defaultTimezone;
  defaultOptions;
  constructor(locale, defaultTimezone, defaultOptions) {
    this.locale = locale;
    this.defaultTimezone = defaultTimezone;
    this.defaultOptions = defaultOptions;
  }
  transform(value, format, timezone, locale) {
    if (value == null || value === "" || value !== value) return null;
    try {
      const _format = format ?? this.defaultOptions?.dateFormat ?? DEFAULT_DATE_FORMAT;
      const _timezone = timezone ?? this.defaultOptions?.timezone ?? this.defaultTimezone ?? void 0;
      return formatDate(value, _format, locale || this.locale, _timezone);
    } catch (error) {
      throw invalidPipeArgumentError(_DatePipe, error.message);
    }
  }
  static \u0275fac = function DatePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DatePipe)(\u0275\u0275directiveInject(LOCALE_ID, 16), \u0275\u0275directiveInject(DATE_PIPE_DEFAULT_TIMEZONE, 24), \u0275\u0275directiveInject(DATE_PIPE_DEFAULT_OPTIONS, 24));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "date",
    type: _DatePipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DatePipe, [{
    type: Pipe,
    args: [{
      name: "date"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LOCALE_ID]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DATE_PIPE_DEFAULT_TIMEZONE]
    }, {
      type: Optional
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DATE_PIPE_DEFAULT_OPTIONS]
    }, {
      type: Optional
    }]
  }], null);
})();
var _INTERPOLATION_REGEXP = /#/g;
var I18nPluralPipe = class _I18nPluralPipe {
  _localization;
  constructor(_localization) {
    this._localization = _localization;
  }
  transform(value, pluralMap, locale) {
    if (value == null) return "";
    if (typeof pluralMap !== "object" || pluralMap === null) {
      throw invalidPipeArgumentError(_I18nPluralPipe, pluralMap);
    }
    const key = getPluralCategory(value, Object.keys(pluralMap), this._localization, locale);
    return pluralMap[key].replace(_INTERPOLATION_REGEXP, value.toString());
  }
  static \u0275fac = function I18nPluralPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _I18nPluralPipe)(\u0275\u0275directiveInject(NgLocalization, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "i18nPlural",
    type: _I18nPluralPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(I18nPluralPipe, [{
    type: Pipe,
    args: [{
      name: "i18nPlural"
    }]
  }], () => [{
    type: NgLocalization
  }], null);
})();
var I18nSelectPipe = class _I18nSelectPipe {
  transform(value, mapping) {
    if (value == null) return "";
    if (typeof mapping !== "object" || typeof value !== "string") {
      throw invalidPipeArgumentError(_I18nSelectPipe, mapping);
    }
    if (mapping.hasOwnProperty(value)) {
      return mapping[value];
    }
    if (mapping.hasOwnProperty("other")) {
      return mapping["other"];
    }
    return "";
  }
  static \u0275fac = function I18nSelectPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _I18nSelectPipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "i18nSelect",
    type: _I18nSelectPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(I18nSelectPipe, [{
    type: Pipe,
    args: [{
      name: "i18nSelect"
    }]
  }], null, null);
})();
var JsonPipe = class _JsonPipe {
  transform(value) {
    ngDevMode && warnIfSignal("JsonPipe", value);
    return JSON.stringify(value, null, 2);
  }
  static \u0275fac = function JsonPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _JsonPipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "json",
    type: _JsonPipe,
    pure: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(JsonPipe, [{
    type: Pipe,
    args: [{
      name: "json",
      pure: false
    }]
  }], null, null);
})();
function makeKeyValuePair(key, value) {
  return {
    key,
    value
  };
}
var KeyValuePipe = class _KeyValuePipe {
  differs;
  constructor(differs) {
    this.differs = differs;
  }
  differ;
  keyValues = [];
  compareFn = defaultComparator;
  transform(input2, compareFn = defaultComparator) {
    ngDevMode && warnIfSignal("KeyValuePipe", input2);
    if (!input2 || !(input2 instanceof Map) && typeof input2 !== "object") {
      return null;
    }
    this.differ ??= this.differs.find(input2).create();
    const differChanges = this.differ.diff(input2);
    const compareFnChanged = compareFn !== this.compareFn;
    if (differChanges) {
      this.keyValues = [];
      differChanges.forEachItem((r) => {
        this.keyValues.push(makeKeyValuePair(r.key, r.currentValue));
      });
    }
    if (differChanges || compareFnChanged) {
      if (compareFn) {
        this.keyValues.sort(compareFn);
      }
      this.compareFn = compareFn;
    }
    return this.keyValues;
  }
  static \u0275fac = function KeyValuePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _KeyValuePipe)(\u0275\u0275directiveInject(KeyValueDiffers, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "keyvalue",
    type: _KeyValuePipe,
    pure: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(KeyValuePipe, [{
    type: Pipe,
    args: [{
      name: "keyvalue",
      pure: false
    }]
  }], () => [{
    type: KeyValueDiffers
  }], null);
})();
function defaultComparator(keyValueA, keyValueB) {
  const a = keyValueA.key;
  const b = keyValueB.key;
  if (a === b) return 0;
  if (a == null) return 1;
  if (b == null) return -1;
  if (typeof a == "string" && typeof b == "string") {
    return a < b ? -1 : 1;
  }
  if (typeof a == "number" && typeof b == "number") {
    return a - b;
  }
  if (typeof a == "boolean" && typeof b == "boolean") {
    return a < b ? -1 : 1;
  }
  const aString = String(a);
  const bString = String(b);
  return aString == bString ? 0 : aString < bString ? -1 : 1;
}
var DecimalPipe = class _DecimalPipe {
  _locale;
  constructor(_locale) {
    this._locale = _locale;
  }
  transform(value, digitsInfo, locale) {
    if (!isValue(value)) return null;
    locale ||= this._locale;
    try {
      const num = strToNumber(value);
      return formatNumber(num, locale, digitsInfo);
    } catch (error) {
      throw invalidPipeArgumentError(_DecimalPipe, error.message);
    }
  }
  static \u0275fac = function DecimalPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DecimalPipe)(\u0275\u0275directiveInject(LOCALE_ID, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "number",
    type: _DecimalPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DecimalPipe, [{
    type: Pipe,
    args: [{
      name: "number"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LOCALE_ID]
    }]
  }], null);
})();
var PercentPipe = class _PercentPipe {
  _locale;
  constructor(_locale) {
    this._locale = _locale;
  }
  transform(value, digitsInfo, locale) {
    if (!isValue(value)) return null;
    locale ||= this._locale;
    try {
      const num = strToNumber(value);
      return formatPercent(num, locale, digitsInfo);
    } catch (error) {
      throw invalidPipeArgumentError(_PercentPipe, error.message);
    }
  }
  static \u0275fac = function PercentPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PercentPipe)(\u0275\u0275directiveInject(LOCALE_ID, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "percent",
    type: _PercentPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PercentPipe, [{
    type: Pipe,
    args: [{
      name: "percent"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LOCALE_ID]
    }]
  }], null);
})();
var CurrencyPipe = class _CurrencyPipe {
  _locale;
  _defaultCurrencyCode;
  constructor(_locale, _defaultCurrencyCode = "USD") {
    this._locale = _locale;
    this._defaultCurrencyCode = _defaultCurrencyCode;
  }
  transform(value, currencyCode = this._defaultCurrencyCode, display = "symbol", digitsInfo, locale) {
    if (!isValue(value)) return null;
    locale ||= this._locale;
    if (typeof display === "boolean") {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        console.warn(`Warning: the currency pipe has been changed in Angular v5. The symbolDisplay option (third parameter) is now a string instead of a boolean. The accepted values are "code", "symbol" or "symbol-narrow".`);
      }
      display = display ? "symbol" : "code";
    }
    let currency = currencyCode || this._defaultCurrencyCode;
    if (display !== "code") {
      if (display === "symbol" || display === "symbol-narrow") {
        currency = getCurrencySymbol(currency, display === "symbol" ? "wide" : "narrow", locale);
      } else {
        currency = display;
      }
    }
    try {
      const num = strToNumber(value);
      return formatCurrency(num, locale, currency, currencyCode, digitsInfo);
    } catch (error) {
      throw invalidPipeArgumentError(_CurrencyPipe, error.message);
    }
  }
  static \u0275fac = function CurrencyPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CurrencyPipe)(\u0275\u0275directiveInject(LOCALE_ID, 16), \u0275\u0275directiveInject(DEFAULT_CURRENCY_CODE, 16));
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "currency",
    type: _CurrencyPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CurrencyPipe, [{
    type: Pipe,
    args: [{
      name: "currency"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [LOCALE_ID]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DEFAULT_CURRENCY_CODE]
    }]
  }], null);
})();
function isValue(value) {
  return !(value == null || value === "" || value !== value);
}
function strToNumber(value) {
  if (typeof value === "string" && !isNaN(Number(value) - parseFloat(value))) {
    return Number(value);
  }
  if (typeof value !== "number") {
    throw new RuntimeError(2309, ngDevMode && `${value} is not a number`);
  }
  return value;
}
var SlicePipe = class _SlicePipe {
  transform(value, start, end) {
    if (value == null) return null;
    const supports = typeof value === "string" || Array.isArray(value);
    if (!supports) {
      throw invalidPipeArgumentError(_SlicePipe, value);
    }
    return value.slice(start, end);
  }
  static \u0275fac = function SlicePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SlicePipe)();
  };
  static \u0275pipe = /* @__PURE__ */ \u0275\u0275definePipe({
    name: "slice",
    type: _SlicePipe,
    pure: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SlicePipe, [{
    type: Pipe,
    args: [{
      name: "slice",
      pure: false
    }]
  }], null, null);
})();
var COMMON_PIPES = [AsyncPipe, UpperCasePipe, LowerCasePipe, JsonPipe, SlicePipe, DecimalPipe, PercentPipe, TitleCasePipe, CurrencyPipe, DatePipe, I18nPluralPipe, I18nSelectPipe, KeyValuePipe];
var CommonModule = class _CommonModule {
  static \u0275fac = function CommonModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CommonModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _CommonModule,
    imports: [NgClass, NgComponentOutlet, NgForOf, NgIf, NgTemplateOutlet, NgStyle, NgSwitch, NgSwitchCase, NgSwitchDefault, NgPlural, NgPluralCase, AsyncPipe, UpperCasePipe, LowerCasePipe, JsonPipe, SlicePipe, DecimalPipe, PercentPipe, TitleCasePipe, CurrencyPipe, DatePipe, I18nPluralPipe, I18nSelectPipe, KeyValuePipe],
    exports: [NgClass, NgComponentOutlet, NgForOf, NgIf, NgTemplateOutlet, NgStyle, NgSwitch, NgSwitchCase, NgSwitchDefault, NgPlural, NgPluralCase, AsyncPipe, UpperCasePipe, LowerCasePipe, JsonPipe, SlicePipe, DecimalPipe, PercentPipe, TitleCasePipe, CurrencyPipe, DatePipe, I18nPluralPipe, I18nSelectPipe, KeyValuePipe]
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CommonModule, [{
    type: NgModule,
    args: [{
      imports: [COMMON_DIRECTIVES, COMMON_PIPES],
      exports: [COMMON_DIRECTIVES, COMMON_PIPES]
    }]
  }], null, null);
})();

// node_modules/@angular/common/fesm2022/_platform_navigation-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var PRECOMMIT_HANDLER_SUPPORTED = new InjectionToken("", {
  factory: () => {
    return typeof window !== "undefined" && typeof window.NavigationPrecommitController !== "undefined";
  }
});
var PlatformNavigation = class _PlatformNavigation {
  static \u0275fac = function PlatformNavigation_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PlatformNavigation)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PlatformNavigation,
    factory: () => (() => window.navigation)(),
    providedIn: "platform"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PlatformNavigation, [{
    type: Injectable,
    args: [{
      providedIn: "platform",
      useFactory: () => window.navigation
    }]
  }], null, null);
})();

// node_modules/@angular/common/fesm2022/_xhr-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
function parseCookieValue(cookieStr, name) {
  name = encodeURIComponent(name);
  for (const cookie of cookieStr.split(";")) {
    const eqIndex = cookie.indexOf("=");
    const [cookieName, cookieValue] = eqIndex == -1 ? [cookie, ""] : [cookie.slice(0, eqIndex), cookie.slice(eqIndex + 1)];
    if (cookieName.trim() === name) {
      return decodeURIComponent(cookieValue);
    }
  }
  return null;
}
var XhrFactory = class {
};

// node_modules/@angular/common/fesm2022/common.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var NavigationAdapterForLocation = class _NavigationAdapterForLocation extends Location {
  navigation = inject2(PlatformNavigation);
  destroyRef = inject2(DestroyRef);
  constructor() {
    super(inject2(LocationStrategy));
    this.registerNavigationListeners();
  }
  registerNavigationListeners() {
    const currentEntryChangeListener = () => {
      this._notifyUrlChangeListeners(this.path(true), this.getState());
    };
    this.navigation.addEventListener("currententrychange", currentEntryChangeListener);
    this.destroyRef.onDestroy(() => {
      this.navigation.removeEventListener("currententrychange", currentEntryChangeListener);
    });
  }
  getState() {
    return this.navigation.currentEntry?.getState();
  }
  replaceState(path, query = "", state = null) {
    const url = this.prepareExternalUrl(path + normalizeQueryParams(query));
    this.navigation.navigate(url, {
      state,
      history: "replace"
    });
  }
  go(path, query = "", state = null) {
    const url = this.prepareExternalUrl(path + normalizeQueryParams(query));
    this.navigation.navigate(url, {
      state,
      history: "push"
    });
  }
  back() {
    this.navigation.back();
  }
  forward() {
    this.navigation.forward();
  }
  onUrlChange(fn) {
    this._urlChangeListeners.push(fn);
    return () => {
      const fnIndex = this._urlChangeListeners.indexOf(fn);
      this._urlChangeListeners.splice(fnIndex, 1);
    };
  }
  static \u0275fac = function NavigationAdapterForLocation_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NavigationAdapterForLocation)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NavigationAdapterForLocation,
    factory: _NavigationAdapterForLocation.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NavigationAdapterForLocation, [{
    type: Injectable
  }], () => [], null);
})();
var PLATFORM_BROWSER_ID = "browser";
var ViewportScroller = class _ViewportScroller {
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _ViewportScroller,
    providedIn: "root",
    factory: () => false ? new NullViewportScroller() : new BrowserViewportScroller(inject2(DOCUMENT), window)
  });
};
var BrowserViewportScroller = class {
  document;
  window;
  offset = () => [0, 0];
  constructor(document2, window2) {
    this.document = document2;
    this.window = window2;
  }
  setOffset(offset) {
    if (Array.isArray(offset)) {
      this.offset = () => offset;
    } else {
      this.offset = offset;
    }
  }
  getScrollPosition() {
    return [this.window.scrollX, this.window.scrollY];
  }
  scrollToPosition(position, options) {
    this.window.scrollTo(__spreadProps(__spreadValues({}, options), {
      left: position[0],
      top: position[1]
    }));
  }
  scrollToAnchor(target, options) {
    const elSelected = findAnchorFromDocument(this.document, target);
    if (elSelected) {
      this.scrollToElement(elSelected, options);
      elSelected.focus({
        preventScroll: true
      });
    }
  }
  setHistoryScrollRestoration(scrollRestoration) {
    try {
      this.window.history.scrollRestoration = scrollRestoration;
    } catch {
      console.warn(formatRuntimeError(2400, ngDevMode && "Failed to set `window.history.scrollRestoration`. This may occur when:\n\u2022 The script is running inside a sandboxed iframe\n\u2022 The window is partially navigated or inactive\n\u2022 The script is executed in an untrusted or special context (e.g., test runners, browser extensions, or content previews)\nScroll position may not be preserved across navigation."));
    }
  }
  scrollToElement(el, options) {
    const rect = el.getBoundingClientRect();
    const left = rect.left + this.window.pageXOffset;
    const top = rect.top + this.window.pageYOffset;
    const offset = this.offset();
    this.window.scrollTo(__spreadProps(__spreadValues({}, options), {
      left: left - offset[0],
      top: top - offset[1]
    }));
  }
};
function findAnchorFromDocument(document2, target) {
  const documentResult = document2.getElementById(target) || document2.getElementsByName(target)[0];
  if (documentResult) {
    return documentResult;
  }
  if (typeof document2.createTreeWalker === "function" && document2.body && typeof document2.body.attachShadow === "function") {
    const treeWalker = document2.createTreeWalker(document2.body, NodeFilter.SHOW_ELEMENT);
    let currentNode = treeWalker.currentNode;
    while (currentNode) {
      const shadowRoot = currentNode.shadowRoot;
      if (shadowRoot) {
        const result = shadowRoot.getElementById(target) || shadowRoot.querySelector(`[name="${target}"]`);
        if (result) {
          return result;
        }
      }
      currentNode = treeWalker.nextNode();
    }
  }
  return null;
}
var PLACEHOLDER_QUALITY = "20";
function getUrl(src, win) {
  return isAbsoluteUrl(src) ? new URL(src) : new URL(src, win.location.href);
}
function isAbsoluteUrl(src) {
  return /^https?:\/\//.test(src);
}
function extractHostname(url) {
  return isAbsoluteUrl(url) ? new URL(url).hostname : url;
}
function isValidPath(path) {
  const isString = typeof path === "string";
  if (!isString || path.trim() === "") {
    return false;
  }
  try {
    const url = new URL(path);
    return true;
  } catch {
    return false;
  }
}
function normalizePath(path) {
  return path.endsWith("/") ? path.slice(0, -1) : path;
}
function normalizeSrc(src) {
  return src.startsWith("/") ? src.slice(1) : src;
}
var noopImageLoader = (config2) => config2.src;
var IMAGE_LOADER = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "ImageLoader" : "", {
  factory: () => noopImageLoader
});
function createImageLoader(buildUrlFn, exampleUrls) {
  return function provideImageLoader(path) {
    if (!isValidPath(path)) {
      throwInvalidPathError(path, exampleUrls || []);
    }
    path = normalizePath(path);
    const loaderFn = (config2) => {
      if (isAbsoluteUrl(config2.src)) {
        throwUnexpectedAbsoluteUrlError(path, config2.src);
      }
      return buildUrlFn(path, __spreadProps(__spreadValues({}, config2), {
        src: normalizeSrc(config2.src)
      }));
    };
    const providers = [{
      provide: IMAGE_LOADER,
      useValue: loaderFn
    }];
    return providers;
  };
}
function throwInvalidPathError(path, exampleUrls) {
  throw new RuntimeError(2959, ngDevMode && `Image loader has detected an invalid path (\`${path}\`). To fix this, supply a path using one of the following formats: ${exampleUrls.join(" or ")}`);
}
function throwUnexpectedAbsoluteUrlError(path, url) {
  throw new RuntimeError(2959, ngDevMode && `Image loader has detected a \`<img>\` tag with an invalid \`ngSrc\` attribute: ${url}. This image loader expects \`ngSrc\` to be a relative URL - however the provided value is an absolute URL. To fix this, provide \`ngSrc\` as a path relative to the base URL configured for this loader (\`${path}\`).`);
}
function normalizeLoaderTransform(transform, separator) {
  if (typeof transform === "string") {
    return transform;
  }
  return Object.entries(transform).map(([key, value]) => `${key}${separator}${value}`).join(",");
}
var provideCloudflareLoader = createImageLoader(createCloudflareUrl, ngDevMode ? ["https://<ZONE>/cdn-cgi/image/<OPTIONS>/<SOURCE-IMAGE>"] : void 0);
function createCloudflareUrl(path, config2) {
  let params = `format=auto`;
  if (config2.width) {
    params += `,width=${config2.width}`;
  }
  if (config2.height) {
    params += `,height=${config2.height}`;
  }
  if (config2.isPlaceholder) {
    params += `,quality=${PLACEHOLDER_QUALITY}`;
  }
  if (config2.loaderParams?.["transform"]) {
    const transformStr = normalizeLoaderTransform(config2.loaderParams["transform"], "=");
    params += `,${transformStr}`;
  }
  return `${path}/cdn-cgi/image/${params}/${config2.src}`;
}
var cloudinaryLoaderInfo = {
  name: "Cloudinary",
  testUrl: isCloudinaryUrl
};
var CLOUDINARY_LOADER_REGEX = /https?\:\/\/[^\/]+\.cloudinary\.com\/.+/;
function isCloudinaryUrl(url) {
  return CLOUDINARY_LOADER_REGEX.test(url);
}
var provideCloudinaryLoader = createImageLoader(createCloudinaryUrl, ngDevMode ? ["https://res.cloudinary.com/mysite", "https://mysite.cloudinary.com", "https://subdomain.mysite.com"] : void 0);
function createCloudinaryUrl(path, config2) {
  const quality = config2.isPlaceholder ? "q_auto:low" : "q_auto";
  let params = `f_auto,${quality}`;
  if (config2.width) {
    params += `,w_${config2.width}`;
  }
  if (config2.height) {
    params += `,h_${config2.height}`;
  }
  if (config2.loaderParams?.["rounded"]) {
    params += `,r_max`;
  }
  if (config2.loaderParams?.["transform"]) {
    const transformStr = normalizeLoaderTransform(config2.loaderParams["transform"], "_");
    params += `,${transformStr}`;
  }
  return `${path}/image/upload/${params}/${config2.src}`;
}
var imageKitLoaderInfo = {
  name: "ImageKit",
  testUrl: isImageKitUrl
};
var IMAGE_KIT_LOADER_REGEX = /https?\:\/\/[^\/]+\.imagekit\.io\/.+/;
function isImageKitUrl(url) {
  return IMAGE_KIT_LOADER_REGEX.test(url);
}
var provideImageKitLoader = createImageLoader(createImagekitUrl, ngDevMode ? ["https://ik.imagekit.io/mysite", "https://subdomain.mysite.com"] : void 0);
function createImagekitUrl(path, config2) {
  const {
    src,
    width
  } = config2;
  const params = [];
  if (width) {
    params.push(`w-${width}`);
  }
  if (config2.height) {
    params.push(`h-${config2.height}`);
  }
  if (config2.isPlaceholder) {
    params.push(`q-${PLACEHOLDER_QUALITY}`);
  }
  if (config2.loaderParams?.["transform"]) {
    const transformStr = normalizeLoaderTransform(config2.loaderParams["transform"], "-");
    params.push(transformStr);
  }
  const urlSegments = params.length ? [path, `tr:${params.join(",")}`, src] : [path, src];
  const url = new URL(urlSegments.join("/"));
  return url.href;
}
var imgixLoaderInfo = {
  name: "Imgix",
  testUrl: isImgixUrl
};
var IMGIX_LOADER_REGEX = /https?\:\/\/[^\/]+\.imgix\.net\/.+/;
function isImgixUrl(url) {
  return IMGIX_LOADER_REGEX.test(url);
}
var provideImgixLoader = createImageLoader(createImgixUrl, ngDevMode ? ["https://somepath.imgix.net/"] : void 0);
function createImgixUrl(path, config2) {
  const params = [];
  params.push("auto=format");
  if (config2.width) {
    params.push(`w=${config2.width}`);
  }
  if (config2.height) {
    params.push(`h=${config2.height}`);
  }
  if (config2.isPlaceholder) {
    params.push(`q=${PLACEHOLDER_QUALITY}`);
  }
  if (config2.loaderParams?.["transform"]) {
    const transform = normalizeLoaderTransform(config2.loaderParams["transform"], "=").split(",");
    params.push(...transform);
  }
  const url = new URL(`${path}/${config2.src}`);
  url.search = params.join("&");
  return url.href;
}
var netlifyLoaderInfo = {
  name: "Netlify",
  testUrl: isNetlifyUrl
};
var NETLIFY_LOADER_REGEX = /https?\:\/\/[^\/]+\.netlify\.app\/.+/;
function isNetlifyUrl(url) {
  return NETLIFY_LOADER_REGEX.test(url);
}
function imgDirectiveDetails(ngSrc, includeNgSrc = true) {
  const ngSrcInfo = includeNgSrc ? `(activated on an <img> element with the \`ngSrc="${ngSrc}"\`) ` : "";
  return `The NgOptimizedImage directive ${ngSrcInfo}has detected that`;
}
function assertDevMode(checkName) {
  if (!ngDevMode) {
    throw new RuntimeError(2958, `Unexpected invocation of the ${checkName} in the prod mode. Please make sure that the prod mode is enabled for production builds.`);
  }
}
var LCPImageObserver = class _LCPImageObserver {
  images = /* @__PURE__ */ new Map();
  window = inject2(DOCUMENT).defaultView;
  observer = null;
  constructor() {
    assertDevMode("LCP checker");
    if (typeof PerformanceObserver !== "undefined") {
      this.observer = this.initPerformanceObserver();
    }
  }
  initPerformanceObserver() {
    const observer = new PerformanceObserver((entryList) => {
      const entries = entryList.getEntries();
      if (entries.length === 0) return;
      const lcpElement = entries[entries.length - 1];
      const imgSrc = lcpElement.element?.src ?? "";
      if (imgSrc.startsWith("data:") || imgSrc.startsWith("blob:")) return;
      const img = this.images.get(imgSrc);
      if (!img) return;
      if (!img.priority && !img.alreadyWarnedPriority) {
        img.alreadyWarnedPriority = true;
        logMissingPriorityError(imgSrc);
      }
      if (img.modified && !img.alreadyWarnedModified) {
        img.alreadyWarnedModified = true;
        logModifiedWarning(imgSrc);
      }
    });
    observer.observe({
      type: "largest-contentful-paint",
      buffered: true
    });
    return observer;
  }
  registerImage(rewrittenSrc, isPriority) {
    if (!this.observer) return;
    const url = getUrl(rewrittenSrc, this.window).href;
    const existingState = this.images.get(url);
    if (existingState) {
      existingState.priority = existingState.priority || isPriority;
      existingState.count++;
    } else {
      const newObservedImageState = {
        priority: isPriority,
        modified: false,
        alreadyWarnedModified: false,
        alreadyWarnedPriority: false,
        count: 1
      };
      this.images.set(url, newObservedImageState);
    }
  }
  unregisterImage(rewrittenSrc) {
    if (!this.observer) return;
    const url = getUrl(rewrittenSrc, this.window).href;
    const existingState = this.images.get(url);
    if (existingState) {
      existingState.count--;
      if (existingState.count <= 0) {
        this.images.delete(url);
      }
    }
  }
  updateImage(originalSrc, newSrc) {
    if (!this.observer) return;
    const originalUrl = getUrl(originalSrc, this.window).href;
    const newUrl = getUrl(newSrc, this.window).href;
    if (originalUrl === newUrl) return;
    const originalState = this.images.get(originalUrl);
    if (!originalState) return;
    originalState.count--;
    if (originalState.count <= 0) {
      this.images.delete(originalUrl);
    }
    const newState = this.images.get(newUrl);
    if (newState) {
      newState.priority = newState.priority || originalState.priority;
      newState.modified = true;
      newState.alreadyWarnedPriority = newState.alreadyWarnedPriority || originalState.alreadyWarnedPriority;
      newState.alreadyWarnedModified = newState.alreadyWarnedModified || originalState.alreadyWarnedModified;
      newState.count++;
    } else {
      this.images.set(newUrl, {
        priority: originalState.priority,
        modified: true,
        alreadyWarnedModified: originalState.alreadyWarnedModified,
        alreadyWarnedPriority: originalState.alreadyWarnedPriority,
        count: 1
      });
    }
  }
  ngOnDestroy() {
    if (!this.observer) return;
    this.observer.disconnect();
    this.images.clear();
  }
  static \u0275fac = function LCPImageObserver_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _LCPImageObserver)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _LCPImageObserver,
    factory: _LCPImageObserver.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(LCPImageObserver, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function logMissingPriorityError(ngSrc) {
  const directiveDetails = imgDirectiveDetails(ngSrc);
  console.error(formatRuntimeError(2955, `${directiveDetails} this image is the Largest Contentful Paint (LCP) element but was not marked "priority". This image should be marked "priority" in order to prioritize its loading. To fix this, add the "priority" attribute.`));
}
function logModifiedWarning(ngSrc) {
  const directiveDetails = imgDirectiveDetails(ngSrc);
  console.warn(formatRuntimeError(2964, `${directiveDetails} this image is the Largest Contentful Paint (LCP) element and has had its "ngSrc" attribute modified. This can cause slower loading performance. It is recommended not to modify the "ngSrc" property on any image which could be the LCP element.`));
}
var INTERNAL_PRECONNECT_CHECK_BLOCKLIST = /* @__PURE__ */ new Set(["localhost", "127.0.0.1", "0.0.0.0", "[::1]"]);
var PRECONNECT_CHECK_BLOCKLIST = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "PRECONNECT_CHECK_BLOCKLIST" : "");
var PreconnectLinkChecker = class _PreconnectLinkChecker {
  document = inject2(DOCUMENT);
  preconnectLinks = null;
  alreadySeen = /* @__PURE__ */ new Set();
  window = this.document.defaultView;
  blocklist = new Set(INTERNAL_PRECONNECT_CHECK_BLOCKLIST);
  constructor() {
    assertDevMode("preconnect link checker");
    const blocklist = inject2(PRECONNECT_CHECK_BLOCKLIST, {
      optional: true
    });
    if (blocklist) {
      this.populateBlocklist(blocklist);
    }
  }
  populateBlocklist(origins) {
    if (Array.isArray(origins)) {
      deepForEach2(origins, (origin) => {
        this.blocklist.add(extractHostname(origin));
      });
    } else {
      this.blocklist.add(extractHostname(origins));
    }
  }
  assertPreconnect(rewrittenSrc, originalNgSrc) {
    if (false) return;
    const imgUrl = getUrl(rewrittenSrc, this.window);
    if (this.blocklist.has(imgUrl.hostname) || this.alreadySeen.has(imgUrl.origin)) return;
    this.alreadySeen.add(imgUrl.origin);
    this.preconnectLinks ??= this.queryPreconnectLinks();
    if (!this.preconnectLinks.has(imgUrl.origin)) {
      console.warn(formatRuntimeError(2956, `${imgDirectiveDetails(originalNgSrc)} there is no preconnect tag present for this image. Preconnecting to the origin(s) that serve priority images ensures that these images are delivered as soon as possible. To fix this, please add the following element into the <head> of the document:
  <link rel="preconnect" href="${imgUrl.origin}">`));
    }
  }
  queryPreconnectLinks() {
    const preconnectUrls = /* @__PURE__ */ new Set();
    const links = this.document.querySelectorAll("link[rel=preconnect]");
    for (const link of links) {
      const url = getUrl(link.href, this.window);
      preconnectUrls.add(url.origin);
    }
    return preconnectUrls;
  }
  ngOnDestroy() {
    this.preconnectLinks?.clear();
    this.alreadySeen.clear();
  }
  static \u0275fac = function PreconnectLinkChecker_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PreconnectLinkChecker)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PreconnectLinkChecker,
    factory: _PreconnectLinkChecker.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PreconnectLinkChecker, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function deepForEach2(input2, fn) {
  for (let value of input2) {
    Array.isArray(value) ? deepForEach2(value, fn) : fn(value);
  }
}
var DEFAULT_PRELOADED_IMAGES_LIMIT = 5;
var PRELOADED_IMAGES = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "NG_OPTIMIZED_PRELOADED_IMAGES" : "", {
  factory: () => /* @__PURE__ */ new Set()
});
var PreloadLinkCreator = class _PreloadLinkCreator {
  preloadedImages = inject2(PRELOADED_IMAGES);
  document = inject2(DOCUMENT);
  errorShown = false;
  createPreloadLinkTag(renderer, src, srcset, sizes) {
    if (ngDevMode && !this.errorShown && this.preloadedImages.size >= DEFAULT_PRELOADED_IMAGES_LIMIT) {
      this.errorShown = true;
      console.warn(formatRuntimeError(2961, `The \`NgOptimizedImage\` directive has detected that more than ${DEFAULT_PRELOADED_IMAGES_LIMIT} images were marked as priority. This might negatively affect an overall performance of the page. To fix this, remove the "priority" attribute from images with less priority.`));
    }
    if (this.preloadedImages.has(src)) {
      return;
    }
    this.preloadedImages.add(src);
    const preload = renderer.createElement("link");
    renderer.setAttribute(preload, "as", "image");
    renderer.setAttribute(preload, "href", src);
    renderer.setAttribute(preload, "rel", "preload");
    renderer.setAttribute(preload, "fetchpriority", "high");
    if (sizes) {
      renderer.setAttribute(preload, "imageSizes", sizes);
    }
    if (srcset) {
      renderer.setAttribute(preload, "imageSrcset", srcset);
    }
    renderer.appendChild(this.document.head, preload);
  }
  static \u0275fac = function PreloadLinkCreator_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PreloadLinkCreator)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _PreloadLinkCreator,
    factory: _PreloadLinkCreator.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PreloadLinkCreator, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var BASE64_IMG_MAX_LENGTH_IN_ERROR = 50;
var VALID_WIDTH_DESCRIPTOR_SRCSET = /^((\s*\d+w\s*(,|$)){1,})$/;
var VALID_DENSITY_DESCRIPTOR_SRCSET = /^((\s*\d+(\.\d+)?x\s*(,|$)){1,})$/;
var ABSOLUTE_SRCSET_DENSITY_CAP = 3;
var RECOMMENDED_SRCSET_DENSITY_CAP = 2;
var DENSITY_SRCSET_MULTIPLIERS = [1, 2];
var VIEWPORT_BREAKPOINT_CUTOFF = 640;
var ASPECT_RATIO_TOLERANCE = 0.1;
var OVERSIZED_IMAGE_TOLERANCE2 = 1e3;
var FIXED_SRCSET_WIDTH_LIMIT = 1920;
var FIXED_SRCSET_HEIGHT_LIMIT = 1080;
var PLACEHOLDER_DIMENSION_LIMIT = 1e3;
var DATA_URL_WARN_LIMIT = 4e3;
var DATA_URL_ERROR_LIMIT = 1e4;
var BUILT_IN_LOADERS = [imgixLoaderInfo, imageKitLoaderInfo, cloudinaryLoaderInfo, netlifyLoaderInfo];
var PRIORITY_COUNT_THRESHOLD = 10;
var IMGS_WITH_PRIORITY_ATTR_COUNT = 0;
var NgOptimizedImage = class _NgOptimizedImage {
  imageLoader = inject2(IMAGE_LOADER);
  config = processConfig(inject2(IMAGE_CONFIG));
  renderer = inject2(Renderer2);
  imgElement = inject2(ElementRef).nativeElement;
  injector = inject2(Injector);
  destroyRef = inject2(DestroyRef);
  lcpObserver;
  _renderedSrc = null;
  ngSrc;
  ngSrcset;
  sizes;
  width;
  height;
  decoding;
  loading;
  priority = false;
  loaderParams;
  disableOptimizedSrcset = false;
  fill = false;
  placeholder;
  placeholderConfig;
  src;
  srcset;
  constructor() {
    if (ngDevMode) {
      this.lcpObserver = this.injector.get(LCPImageObserver);
      this.destroyRef.onDestroy(() => {
        if (!this.priority && this._renderedSrc !== null) {
          this.lcpObserver.unregisterImage(this._renderedSrc);
        }
      });
    }
    this.destroyRef.onDestroy(() => {
      this.renderer.removeAttribute(this.imgElement, "loading");
    });
  }
  ngOnInit() {
    performanceMarkFeature("NgOptimizedImage");
    if (ngDevMode) {
      const ngZone = this.injector.get(NgZone);
      assertNonEmptyInput(this, "ngSrc", this.ngSrc);
      assertValidNgSrcset(this, this.ngSrcset);
      assertNoConflictingSrc(this);
      if (this.ngSrcset) {
        assertNoConflictingSrcset(this);
      }
      assertNotBase64Image(this);
      assertNotBlobUrl(this);
      if (this.fill) {
        assertEmptyWidthAndHeight(this);
        ngZone.runOutsideAngular(() => assertNonZeroRenderedHeight(this, this.imgElement, this.renderer, this.destroyRef));
      } else {
        assertNonEmptyWidthAndHeight(this);
        if (this.height !== void 0) {
          assertGreaterThanZero(this, this.height, "height");
        }
        if (this.width !== void 0) {
          assertGreaterThanZero(this, this.width, "width");
        }
        ngZone.runOutsideAngular(() => assertNoImageDistortion(this, this.imgElement, this.renderer, this.destroyRef));
      }
      assertValidLoadingInput(this);
      assertValidDecodingInput(this);
      if (!this.ngSrcset) {
        assertNoComplexSizes(this);
      }
      assertValidPlaceholder(this, this.imageLoader);
      assertNotMissingBuiltInLoader(this.ngSrc, this.imageLoader);
      assertNoNgSrcsetWithoutLoader(this, this.imageLoader);
      assertNoLoaderParamsWithoutLoader(this, this.imageLoader);
      ngZone.runOutsideAngular(() => {
        this.lcpObserver.registerImage(this.getRewrittenSrc(), this.priority);
      });
      if (this.priority) {
        const checker = this.injector.get(PreconnectLinkChecker);
        checker.assertPreconnect(this.getRewrittenSrc(), this.ngSrc);
        if (true) {
          const applicationRef = this.injector.get(ApplicationRef);
          assetPriorityCountBelowThreshold(applicationRef);
        }
      }
    }
    if (this.placeholder) {
      this.removePlaceholderOnLoad(this.imgElement);
    }
    this.setHostAttributes();
  }
  setHostAttributes() {
    if (this.fill) {
      this.sizes ||= "100vw";
    } else {
      this.setHostAttribute("width", this.width.toString());
      this.setHostAttribute("height", this.height.toString());
    }
    this.setHostAttribute("loading", this.getLoadingBehavior());
    this.setHostAttribute("fetchpriority", this.getFetchPriority());
    this.setHostAttribute("decoding", this.getDecoding());
    this.setHostAttribute("ng-img", "true");
    const rewrittenSrcset = this.updateSrcAndSrcset();
    if (this.sizes) {
      if (this.getLoadingBehavior() === "lazy") {
        this.setHostAttribute("sizes", "auto, " + this.sizes);
      } else {
        this.setHostAttribute("sizes", this.sizes);
      }
    } else {
      if (this.ngSrcset && VALID_WIDTH_DESCRIPTOR_SRCSET.test(this.ngSrcset) && this.getLoadingBehavior() === "lazy") {
        this.setHostAttribute("sizes", "auto, 100vw");
      }
    }
    if (false) {
      const preloadLinkCreator = this.injector.get(PreloadLinkCreator);
      preloadLinkCreator.createPreloadLinkTag(this.renderer, this.getRewrittenSrc(), rewrittenSrcset, this.sizes);
    }
  }
  ngOnChanges(changes) {
    if (ngDevMode) {
      assertNoPostInitInputChange(this, changes, ["ngSrcset", "width", "height", "priority", "fill", "loading", "sizes", "loaderParams", "disableOptimizedSrcset"]);
    }
    if (changes["ngSrc"] && !changes["ngSrc"].isFirstChange()) {
      const oldSrc = this._renderedSrc;
      this.updateSrcAndSrcset(true);
      if (ngDevMode) {
        const newSrc = this._renderedSrc;
        if (oldSrc && newSrc && oldSrc !== newSrc) {
          const ngZone = this.injector.get(NgZone);
          ngZone.runOutsideAngular(() => {
            this.lcpObserver.updateImage(oldSrc, newSrc);
          });
        }
      }
    }
    if (ngDevMode && changes["placeholder"]?.currentValue && true && true) {
      assertPlaceholderDimensions(this, this.imgElement);
    }
  }
  getAspectRatio() {
    if (this.width && this.height && this.height !== 0) {
      return this.width / this.height;
    }
    return null;
  }
  callImageLoader(configWithoutCustomParams) {
    let augmentedConfig = configWithoutCustomParams;
    if (this.loaderParams) {
      augmentedConfig.loaderParams = this.loaderParams;
    }
    const ratio = this.getAspectRatio();
    if (ratio !== null && augmentedConfig.width) {
      augmentedConfig.height = Math.round(augmentedConfig.width / ratio);
    }
    return this.imageLoader(augmentedConfig);
  }
  getLoadingBehavior() {
    if (!this.priority && this.loading !== void 0) {
      return this.loading;
    }
    return this.priority ? "eager" : "lazy";
  }
  getFetchPriority() {
    return this.priority ? "high" : "auto";
  }
  getDecoding() {
    if (this.priority) {
      return "sync";
    }
    return this.decoding ?? "auto";
  }
  getRewrittenSrc() {
    if (!this._renderedSrc) {
      const imgConfig = {
        src: this.ngSrc
      };
      this._renderedSrc = this.callImageLoader(imgConfig);
    }
    return this._renderedSrc;
  }
  getRewrittenSrcset() {
    const widthSrcSet = VALID_WIDTH_DESCRIPTOR_SRCSET.test(this.ngSrcset);
    const finalSrcs = this.ngSrcset.split(",").filter((src) => src !== "").map((srcStr) => {
      srcStr = srcStr.trim();
      const width = widthSrcSet ? parseFloat(srcStr) : parseFloat(srcStr) * this.width;
      return `${this.callImageLoader({
        src: this.ngSrc,
        width
      })} ${srcStr}`;
    });
    return finalSrcs.join(", ");
  }
  getAutomaticSrcset() {
    if (this.sizes) {
      return this.getResponsiveSrcset();
    } else {
      return this.getFixedSrcset();
    }
  }
  getResponsiveSrcset() {
    const {
      breakpoints
    } = this.config;
    let filteredBreakpoints = breakpoints;
    if (this.sizes?.trim() === "100vw") {
      filteredBreakpoints = breakpoints.filter((bp) => bp >= VIEWPORT_BREAKPOINT_CUTOFF);
    }
    const finalSrcs = filteredBreakpoints.map((bp) => `${this.callImageLoader({
      src: this.ngSrc,
      width: bp
    })} ${bp}w`);
    return finalSrcs.join(", ");
  }
  updateSrcAndSrcset(forceSrcRecalc = false) {
    if (forceSrcRecalc) {
      this._renderedSrc = null;
    }
    const rewrittenSrc = this.getRewrittenSrc();
    this.setHostAttribute("src", rewrittenSrc);
    let rewrittenSrcset = void 0;
    if (this.ngSrcset) {
      rewrittenSrcset = this.getRewrittenSrcset();
    } else if (this.shouldGenerateAutomaticSrcset()) {
      rewrittenSrcset = this.getAutomaticSrcset();
    }
    if (rewrittenSrcset) {
      this.setHostAttribute("srcset", rewrittenSrcset);
    }
    return rewrittenSrcset;
  }
  getFixedSrcset() {
    const finalSrcs = DENSITY_SRCSET_MULTIPLIERS.map((multiplier) => `${this.callImageLoader({
      src: this.ngSrc,
      width: this.width * multiplier
    })} ${multiplier}x`);
    return finalSrcs.join(", ");
  }
  shouldGenerateAutomaticSrcset() {
    let oversizedImage = false;
    if (!this.sizes) {
      oversizedImage = this.width > FIXED_SRCSET_WIDTH_LIMIT || this.height > FIXED_SRCSET_HEIGHT_LIMIT;
    }
    return !this.disableOptimizedSrcset && !this.srcset && this.imageLoader !== noopImageLoader && !oversizedImage;
  }
  generatePlaceholder(placeholderInput) {
    const {
      placeholderResolution
    } = this.config;
    if (placeholderInput === true) {
      return `url("${escapeCssUrl(this.callImageLoader({
        src: this.ngSrc,
        width: placeholderResolution,
        isPlaceholder: true
      }))}")`;
    } else if (typeof placeholderInput === "string") {
      return `url("${escapeCssUrl(placeholderInput)}")`;
    }
    return null;
  }
  shouldBlurPlaceholder(placeholderConfig) {
    if (!placeholderConfig || !placeholderConfig.hasOwnProperty("blur")) {
      return true;
    }
    return Boolean(placeholderConfig.blur);
  }
  removePlaceholderOnLoad(img) {
    const callback = () => {
      const changeDetectorRef = this.injector.get(ChangeDetectorRef);
      removeLoadListenerFn();
      removeErrorListenerFn();
      this.placeholder = false;
      changeDetectorRef.markForCheck();
    };
    const removeLoadListenerFn = this.renderer.listen(img, "load", callback);
    const removeErrorListenerFn = this.renderer.listen(img, "error", callback);
    this.destroyRef.onDestroy(() => {
      removeLoadListenerFn();
      removeErrorListenerFn();
    });
    callOnLoadIfImageIsLoaded(img, callback);
  }
  setHostAttribute(name, value) {
    this.renderer.setAttribute(this.imgElement, name, value);
  }
  static \u0275fac = function NgOptimizedImage_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgOptimizedImage)();
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgOptimizedImage,
    selectors: [["img", "ngSrc", ""]],
    hostVars: 18,
    hostBindings: function NgOptimizedImage_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275styleProp("position", ctx.fill ? "absolute" : null)("width", ctx.fill ? "100%" : null)("height", ctx.fill ? "100%" : null)("inset", ctx.fill ? "0" : null)("background-size", ctx.placeholder ? "cover" : null)("background-position", ctx.placeholder ? "50% 50%" : null)("background-repeat", ctx.placeholder ? "no-repeat" : null)("background-image", ctx.placeholder ? ctx.generatePlaceholder(ctx.placeholder) : null)("filter", ctx.placeholder && ctx.shouldBlurPlaceholder(ctx.placeholderConfig) ? "blur(15px)" : null);
      }
    },
    inputs: {
      ngSrc: [2, "ngSrc", "ngSrc", unwrapSafeUrl],
      ngSrcset: "ngSrcset",
      sizes: "sizes",
      width: [2, "width", "width", numberAttribute],
      height: [2, "height", "height", numberAttribute],
      decoding: "decoding",
      loading: "loading",
      priority: [2, "priority", "priority", booleanAttribute],
      loaderParams: "loaderParams",
      disableOptimizedSrcset: [2, "disableOptimizedSrcset", "disableOptimizedSrcset", booleanAttribute],
      fill: [2, "fill", "fill", booleanAttribute],
      placeholder: [2, "placeholder", "placeholder", booleanOrUrlAttribute],
      placeholderConfig: "placeholderConfig",
      src: "src",
      srcset: "srcset"
    },
    features: [\u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgOptimizedImage, [{
    type: Directive,
    args: [{
      selector: "img[ngSrc]",
      host: {
        "[style.position]": 'fill ? "absolute" : null',
        "[style.width]": 'fill ? "100%" : null',
        "[style.height]": 'fill ? "100%" : null',
        "[style.inset]": 'fill ? "0" : null',
        "[style.background-size]": 'placeholder ? "cover" : null',
        "[style.background-position]": 'placeholder ? "50% 50%" : null',
        "[style.background-repeat]": 'placeholder ? "no-repeat" : null',
        "[style.background-image]": "placeholder ? generatePlaceholder(placeholder) : null",
        "[style.filter]": 'placeholder && shouldBlurPlaceholder(placeholderConfig) ? "blur(15px)" : null'
      }
    }]
  }], () => [], {
    ngSrc: [{
      type: Input,
      args: [{
        required: true,
        transform: unwrapSafeUrl
      }]
    }],
    ngSrcset: [{
      type: Input
    }],
    sizes: [{
      type: Input
    }],
    width: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    height: [{
      type: Input,
      args: [{
        transform: numberAttribute
      }]
    }],
    decoding: [{
      type: Input
    }],
    loading: [{
      type: Input
    }],
    priority: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    loaderParams: [{
      type: Input
    }],
    disableOptimizedSrcset: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    fill: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    placeholder: [{
      type: Input,
      args: [{
        transform: booleanOrUrlAttribute
      }]
    }],
    placeholderConfig: [{
      type: Input
    }],
    src: [{
      type: Input
    }],
    srcset: [{
      type: Input
    }]
  });
})();
function processConfig(config2) {
  let sortedBreakpoints = {};
  if (config2.breakpoints) {
    sortedBreakpoints.breakpoints = config2.breakpoints.sort((a, b) => a - b);
  }
  return Object.assign({}, IMAGE_CONFIG_DEFAULTS, config2, sortedBreakpoints);
}
function assertNoConflictingSrc(dir) {
  if (dir.src) {
    throw new RuntimeError(2950, `${imgDirectiveDetails(dir.ngSrc)} both \`src\` and \`ngSrc\` have been set. Supplying both of these attributes breaks lazy loading. The NgOptimizedImage directive sets \`src\` itself based on the value of \`ngSrc\`. To fix this, please remove the \`src\` attribute.`);
  }
}
function assertNoConflictingSrcset(dir) {
  if (dir.srcset) {
    throw new RuntimeError(2951, `${imgDirectiveDetails(dir.ngSrc)} both \`srcset\` and \`ngSrcset\` have been set. Supplying both of these attributes breaks lazy loading. The NgOptimizedImage directive sets \`srcset\` itself based on the value of \`ngSrcset\`. To fix this, please remove the \`srcset\` attribute.`);
  }
}
function assertNotBase64Image(dir) {
  let ngSrc = dir.ngSrc.trim();
  if (ngSrc.startsWith("data:")) {
    if (ngSrc.length > BASE64_IMG_MAX_LENGTH_IN_ERROR) {
      ngSrc = ngSrc.substring(0, BASE64_IMG_MAX_LENGTH_IN_ERROR) + "...";
    }
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc, false)} \`ngSrc\` is a Base64-encoded string (${ngSrc}). NgOptimizedImage does not support Base64-encoded strings. To fix this, disable the NgOptimizedImage directive for this element by removing \`ngSrc\` and using a standard \`src\` attribute instead.`);
  }
}
function assertNoComplexSizes(dir) {
  let sizes = dir.sizes;
  if (sizes?.match(/((\)|,)\s|^)\d+px/)) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc, false)} \`sizes\` was set to a string including pixel values. For automatic \`srcset\` generation, \`sizes\` must only include responsive values, such as \`sizes="50vw"\` or \`sizes="(min-width: 768px) 50vw, 100vw"\`. To fix this, modify the \`sizes\` attribute, or provide your own \`ngSrcset\` value directly.`);
  }
}
function assertValidPlaceholder(dir, imageLoader) {
  assertNoPlaceholderConfigWithoutPlaceholder(dir);
  assertNoRelativePlaceholderWithoutLoader(dir, imageLoader);
  assertNoOversizedDataUrl(dir);
}
function assertNoPlaceholderConfigWithoutPlaceholder(dir) {
  if (dir.placeholderConfig && !dir.placeholder) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc, false)} \`placeholderConfig\` options were provided for an image that does not use the \`placeholder\` attribute, and will have no effect.`);
  }
}
function assertNoRelativePlaceholderWithoutLoader(dir, imageLoader) {
  if (dir.placeholder === true && imageLoader === noopImageLoader) {
    throw new RuntimeError(2963, `${imgDirectiveDetails(dir.ngSrc)} the \`placeholder\` attribute is set to true but no image loader is configured (i.e. the default one is being used), which would result in the same image being used for the primary image and its placeholder. To fix this, provide a loader or remove the \`placeholder\` attribute from the image.`);
  }
}
function assertNoOversizedDataUrl(dir) {
  if (dir.placeholder && typeof dir.placeholder === "string" && dir.placeholder.startsWith("data:")) {
    if (dir.placeholder.length > DATA_URL_ERROR_LIMIT) {
      throw new RuntimeError(2965, `${imgDirectiveDetails(dir.ngSrc)} the \`placeholder\` attribute is set to a data URL which is longer than ${DATA_URL_ERROR_LIMIT} characters. This is strongly discouraged, as large inline placeholders directly increase the bundle size of Angular and hurt page load performance. To fix this, generate a smaller data URL placeholder.`);
    }
    if (dir.placeholder.length > DATA_URL_WARN_LIMIT) {
      console.warn(formatRuntimeError(2965, `${imgDirectiveDetails(dir.ngSrc)} the \`placeholder\` attribute is set to a data URL which is longer than ${DATA_URL_WARN_LIMIT} characters. This is discouraged, as large inline placeholders directly increase the bundle size of Angular and hurt page load performance. For better loading performance, generate a smaller data URL placeholder.`));
    }
  }
}
function assertNotBlobUrl(dir) {
  const ngSrc = dir.ngSrc.trim();
  if (ngSrc.startsWith("blob:")) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} \`ngSrc\` was set to a blob URL (${ngSrc}). Blob URLs are not supported by the NgOptimizedImage directive. To fix this, disable the NgOptimizedImage directive for this element by removing \`ngSrc\` and using a regular \`src\` attribute instead.`);
  }
}
function assertNonEmptyInput(dir, name, value) {
  const isString = typeof value === "string";
  const isEmptyString = isString && value.trim() === "";
  if (!isString || isEmptyString) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} \`${name}\` has an invalid value (\`${value}\`). To fix this, change the value to a non-empty string.`);
  }
}
function assertValidNgSrcset(dir, value) {
  if (value == null) return;
  assertNonEmptyInput(dir, "ngSrcset", value);
  const stringVal = value;
  const isValidWidthDescriptor = VALID_WIDTH_DESCRIPTOR_SRCSET.test(stringVal);
  const isValidDensityDescriptor = VALID_DENSITY_DESCRIPTOR_SRCSET.test(stringVal);
  if (isValidDensityDescriptor) {
    assertUnderDensityCap(dir, stringVal);
  }
  const isValidSrcset = isValidWidthDescriptor || isValidDensityDescriptor;
  if (!isValidSrcset) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} \`ngSrcset\` has an invalid value (\`${value}\`). To fix this, supply \`ngSrcset\` using a comma-separated list of one or more width descriptors (e.g. "100w, 200w") or density descriptors (e.g. "1x, 2x").`);
  }
}
function assertUnderDensityCap(dir, value) {
  const underDensityCap = value.split(",").every((num) => num === "" || parseFloat(num) <= ABSOLUTE_SRCSET_DENSITY_CAP);
  if (!underDensityCap) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the \`ngSrcset\` contains an unsupported image density:\`${value}\`. NgOptimizedImage generally recommends a max image density of ${RECOMMENDED_SRCSET_DENSITY_CAP}x but supports image densities up to ${ABSOLUTE_SRCSET_DENSITY_CAP}x. The human eye cannot distinguish between image densities greater than ${RECOMMENDED_SRCSET_DENSITY_CAP}x - which makes them unnecessary for most use cases. Images that will be pinch-zoomed are typically the primary use case for ${ABSOLUTE_SRCSET_DENSITY_CAP}x images. Please remove the high density descriptor and try again.`);
  }
}
function postInitInputChangeError(dir, inputName) {
  let reason;
  if (inputName === "width" || inputName === "height") {
    reason = `Changing \`${inputName}\` may result in different attribute value applied to the underlying image element and cause layout shifts on a page.`;
  } else {
    reason = `Changing the \`${inputName}\` would have no effect on the underlying image element, because the resource loading has already occurred.`;
  }
  return new RuntimeError(2953, `${imgDirectiveDetails(dir.ngSrc)} \`${inputName}\` was updated after initialization. The NgOptimizedImage directive will not react to this input change. ${reason} To fix this, either switch \`${inputName}\` to a static value or wrap the image element in an @if that is gated on the necessary value.`);
}
function assertNoPostInitInputChange(dir, changes, inputs) {
  inputs.forEach((input2) => {
    const isUpdated = changes.hasOwnProperty(input2);
    if (isUpdated && !changes[input2].isFirstChange()) {
      if (input2 === "ngSrc") {
        dir = {
          ngSrc: changes[input2].previousValue
        };
      }
      throw postInitInputChangeError(dir, input2);
    }
  });
}
function assertGreaterThanZero(dir, inputValue, inputName) {
  const validNumber = typeof inputValue === "number" && inputValue > 0;
  const validString = typeof inputValue === "string" && /^\d+$/.test(inputValue.trim()) && parseInt(inputValue) > 0;
  if (!validNumber && !validString) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} \`${inputName}\` has an invalid value. To fix this, provide \`${inputName}\` as a number greater than 0.`);
  }
}
function assertNoImageDistortion(dir, img, renderer, destroyRef) {
  const callback = () => {
    removeLoadListenerFn();
    removeErrorListenerFn();
    const computedStyle = window.getComputedStyle(img);
    let renderedWidth = parseFloat(computedStyle.getPropertyValue("width"));
    let renderedHeight = parseFloat(computedStyle.getPropertyValue("height"));
    const boxSizing = computedStyle.getPropertyValue("box-sizing");
    if (boxSizing === "border-box") {
      const paddingTop = computedStyle.getPropertyValue("padding-top");
      const paddingRight = computedStyle.getPropertyValue("padding-right");
      const paddingBottom = computedStyle.getPropertyValue("padding-bottom");
      const paddingLeft = computedStyle.getPropertyValue("padding-left");
      renderedWidth -= parseFloat(paddingRight) + parseFloat(paddingLeft);
      renderedHeight -= parseFloat(paddingTop) + parseFloat(paddingBottom);
    }
    const renderedAspectRatio = renderedWidth / renderedHeight;
    const nonZeroRenderedDimensions = renderedWidth !== 0 && renderedHeight !== 0;
    const intrinsicWidth = img.naturalWidth;
    const intrinsicHeight = img.naturalHeight;
    const intrinsicAspectRatio = intrinsicWidth / intrinsicHeight;
    const suppliedWidth = dir.width;
    const suppliedHeight = dir.height;
    const suppliedAspectRatio = suppliedWidth / suppliedHeight;
    const inaccurateDimensions = Math.abs(suppliedAspectRatio - intrinsicAspectRatio) > ASPECT_RATIO_TOLERANCE;
    const stylingDistortion = nonZeroRenderedDimensions && Math.abs(intrinsicAspectRatio - renderedAspectRatio) > ASPECT_RATIO_TOLERANCE;
    if (inaccurateDimensions) {
      console.warn(formatRuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the aspect ratio of the image does not match the aspect ratio indicated by the width and height attributes. 
Intrinsic image size: ${intrinsicWidth}w x ${intrinsicHeight}h (aspect-ratio: ${round(intrinsicAspectRatio)}). 
Supplied width and height attributes: ${suppliedWidth}w x ${suppliedHeight}h (aspect-ratio: ${round(suppliedAspectRatio)}). 
To fix this, update the width and height attributes.`));
    } else if (stylingDistortion) {
      console.warn(formatRuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the aspect ratio of the rendered image does not match the image's intrinsic aspect ratio. 
Intrinsic image size: ${intrinsicWidth}w x ${intrinsicHeight}h (aspect-ratio: ${round(intrinsicAspectRatio)}). 
Rendered image size: ${renderedWidth}w x ${renderedHeight}h (aspect-ratio: ${round(renderedAspectRatio)}). 
This issue can occur if "width" and "height" attributes are added to an image without updating the corresponding image styling. To fix this, adjust image styling. In most cases, adding "height: auto" or "width: auto" to the image styling will fix this issue.`));
    } else if (!dir.ngSrcset && nonZeroRenderedDimensions) {
      const recommendedWidth = RECOMMENDED_SRCSET_DENSITY_CAP * renderedWidth;
      const recommendedHeight = RECOMMENDED_SRCSET_DENSITY_CAP * renderedHeight;
      const oversizedWidth = intrinsicWidth - recommendedWidth >= OVERSIZED_IMAGE_TOLERANCE2;
      const oversizedHeight = intrinsicHeight - recommendedHeight >= OVERSIZED_IMAGE_TOLERANCE2;
      if (oversizedWidth || oversizedHeight) {
        console.warn(formatRuntimeError(2960, `${imgDirectiveDetails(dir.ngSrc)} the intrinsic image is significantly larger than necessary. 
Rendered image size: ${renderedWidth}w x ${renderedHeight}h. 
Intrinsic image size: ${intrinsicWidth}w x ${intrinsicHeight}h. 
Recommended intrinsic image size: ${recommendedWidth}w x ${recommendedHeight}h. 
Note: Recommended intrinsic image size is calculated assuming a maximum DPR of ${RECOMMENDED_SRCSET_DENSITY_CAP}. To improve loading time, resize the image or consider using the "ngSrcset" and "sizes" attributes.`));
      }
    }
  };
  const removeLoadListenerFn = renderer.listen(img, "load", callback);
  const removeErrorListenerFn = renderer.listen(img, "error", () => {
    removeLoadListenerFn();
    removeErrorListenerFn();
  });
  destroyRef.onDestroy(() => {
    removeLoadListenerFn();
    removeErrorListenerFn();
  });
  callOnLoadIfImageIsLoaded(img, callback);
}
function assertNonEmptyWidthAndHeight(dir) {
  let missingAttributes = [];
  if (dir.width === void 0) missingAttributes.push("width");
  if (dir.height === void 0) missingAttributes.push("height");
  if (missingAttributes.length > 0) {
    throw new RuntimeError(2954, `${imgDirectiveDetails(dir.ngSrc)} these required attributes are missing: ${missingAttributes.map((attr) => `"${attr}"`).join(", ")}. Including "width" and "height" attributes will prevent image-related layout shifts. To fix this, include "width" and "height" attributes on the image tag or turn on "fill" mode with the \`fill\` attribute.`);
  }
}
function assertEmptyWidthAndHeight(dir) {
  if (dir.width || dir.height) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the attributes \`height\` and/or \`width\` are present along with the \`fill\` attribute. Because \`fill\` mode causes an image to fill its containing element, the size attributes have no effect and should be removed.`);
  }
}
function assertNonZeroRenderedHeight(dir, img, renderer, destroyRef) {
  const callback = () => {
    removeLoadListenerFn();
    removeErrorListenerFn();
    const renderedHeight = img.clientHeight;
    if (dir.fill && renderedHeight === 0) {
      console.warn(formatRuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the height of the fill-mode image is zero. This is likely because the containing element does not have the CSS 'position' property set to one of the following: "relative", "fixed", or "absolute". To fix this problem, make sure the container element has the CSS 'position' property defined and the height of the element is not zero.`));
    }
  };
  const removeLoadListenerFn = renderer.listen(img, "load", callback);
  const removeErrorListenerFn = renderer.listen(img, "error", () => {
    removeLoadListenerFn();
    removeErrorListenerFn();
  });
  destroyRef.onDestroy(() => {
    removeLoadListenerFn();
    removeErrorListenerFn();
  });
  callOnLoadIfImageIsLoaded(img, callback);
}
function assertValidLoadingInput(dir) {
  if (dir.loading && dir.priority) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the \`loading\` attribute was used on an image that was marked "priority". Setting \`loading\` on priority images is not allowed because these images will always be eagerly loaded. To fix this, remove the \u201Cloading\u201D attribute from the priority image.`);
  }
  const validInputs = ["auto", "eager", "lazy"];
  if (typeof dir.loading === "string" && !validInputs.includes(dir.loading)) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the \`loading\` attribute has an invalid value (\`${dir.loading}\`). To fix this, provide a valid value ("lazy", "eager", or "auto").`);
  }
}
function assertValidDecodingInput(dir) {
  const validInputs = ["sync", "async", "auto"];
  if (typeof dir.decoding === "string" && !validInputs.includes(dir.decoding)) {
    throw new RuntimeError(2952, `${imgDirectiveDetails(dir.ngSrc)} the \`decoding\` attribute has an invalid value (\`${dir.decoding}\`). To fix this, provide a valid value ("sync", "async", or "auto").`);
  }
}
function assertNotMissingBuiltInLoader(ngSrc, imageLoader) {
  if (imageLoader === noopImageLoader) {
    let builtInLoaderName = "";
    for (const loader of BUILT_IN_LOADERS) {
      if (loader.testUrl(ngSrc)) {
        builtInLoaderName = loader.name;
        break;
      }
    }
    if (builtInLoaderName) {
      console.warn(formatRuntimeError(2962, `NgOptimizedImage: It looks like your images may be hosted on the ${builtInLoaderName} CDN, but your app is not using Angular's built-in loader for that CDN. We recommend switching to use the built-in by calling \`provide${builtInLoaderName}Loader()\` in your \`providers\` and passing it your instance's base URL. If you don't want to use the built-in loader, define a custom loader function using IMAGE_LOADER to silence this warning.`));
    }
  }
}
function assertNoNgSrcsetWithoutLoader(dir, imageLoader) {
  if (dir.ngSrcset && imageLoader === noopImageLoader) {
    console.warn(formatRuntimeError(2963, `${imgDirectiveDetails(dir.ngSrc)} the \`ngSrcset\` attribute is present but no image loader is configured (i.e. the default one is being used), which would result in the same image being used for all configured sizes. To fix this, provide a loader or remove the \`ngSrcset\` attribute from the image.`));
  }
}
function assertNoLoaderParamsWithoutLoader(dir, imageLoader) {
  if (dir.loaderParams && imageLoader === noopImageLoader) {
    console.warn(formatRuntimeError(2963, `${imgDirectiveDetails(dir.ngSrc)} the \`loaderParams\` attribute is present but no image loader is configured (i.e. the default one is being used), which means that the loaderParams data will not be consumed and will not affect the URL. To fix this, provide a custom loader or remove the \`loaderParams\` attribute from the image.`));
  }
}
async function assetPriorityCountBelowThreshold(appRef) {
  if (IMGS_WITH_PRIORITY_ATTR_COUNT === 0) {
    IMGS_WITH_PRIORITY_ATTR_COUNT++;
    await appRef.whenStable();
    if (IMGS_WITH_PRIORITY_ATTR_COUNT > PRIORITY_COUNT_THRESHOLD) {
      console.warn(formatRuntimeError(2966, `NgOptimizedImage: The "priority" attribute is set to true more than ${PRIORITY_COUNT_THRESHOLD} times (${IMGS_WITH_PRIORITY_ATTR_COUNT} times). Marking too many images as "high" priority can hurt your application's LCP (https://web.dev/lcp). "Priority" should only be set on the image expected to be the page's LCP element.`));
    }
  } else {
    IMGS_WITH_PRIORITY_ATTR_COUNT++;
  }
}
function assertPlaceholderDimensions(dir, imgElement) {
  const computedStyle = window.getComputedStyle(imgElement);
  let renderedWidth = parseFloat(computedStyle.getPropertyValue("width"));
  let renderedHeight = parseFloat(computedStyle.getPropertyValue("height"));
  if (renderedWidth > PLACEHOLDER_DIMENSION_LIMIT || renderedHeight > PLACEHOLDER_DIMENSION_LIMIT) {
    console.warn(formatRuntimeError(2967, `${imgDirectiveDetails(dir.ngSrc)} it uses a placeholder image, but at least one of the dimensions attribute (height or width) exceeds the limit of ${PLACEHOLDER_DIMENSION_LIMIT}px. To fix this, use a smaller image as a placeholder.`));
  }
}
function callOnLoadIfImageIsLoaded(img, callback) {
  if (img.complete && img.naturalWidth) {
    callback();
  }
}
function round(input2) {
  return Number.isInteger(input2) ? input2 : input2.toFixed(2);
}
function unwrapSafeUrl(value) {
  if (typeof value === "string") {
    return value;
  }
  return unwrapSafeValue(value);
}
function escapeCssUrl(input2) {
  return input2.replace(/\\/g, "\\\\").replace(/"/g, '\\"');
}
function booleanOrUrlAttribute(value) {
  if (typeof value === "string" && value !== "true" && value !== "false" && value !== "") {
    return value;
  }
  return booleanAttribute(value);
}

// node_modules/@angular/platform-browser/fesm2022/_dom_renderer-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var EventManagerPlugin = class {
  _doc;
  constructor(_doc) {
    this._doc = _doc;
  }
  manager;
};
var DomEventsPlugin = class _DomEventsPlugin extends EventManagerPlugin {
  constructor(doc) {
    super(doc);
  }
  supports(eventName) {
    return true;
  }
  addEventListener(element, eventName, handler, options) {
    element.addEventListener(eventName, handler, options);
    return () => this.removeEventListener(element, eventName, handler, options);
  }
  removeEventListener(target, eventName, callback, options) {
    return target.removeEventListener(eventName, callback, options);
  }
  static \u0275fac = function DomEventsPlugin_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DomEventsPlugin)(\u0275\u0275inject(DOCUMENT));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _DomEventsPlugin,
    factory: _DomEventsPlugin.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomEventsPlugin, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var EVENT_MANAGER_PLUGINS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "EventManagerPlugins" : "");
var EventManager = class _EventManager {
  _zone;
  _plugins;
  _eventNameToPlugin = /* @__PURE__ */ new Map();
  constructor(plugins, _zone) {
    this._zone = _zone;
    plugins.forEach((plugin) => {
      plugin.manager = this;
    });
    const otherPlugins = plugins.filter((p) => !(p instanceof DomEventsPlugin));
    this._plugins = otherPlugins.slice().reverse();
    const domEventPlugin = plugins.find((p) => p instanceof DomEventsPlugin);
    if (domEventPlugin) {
      this._plugins.push(domEventPlugin);
    }
  }
  addEventListener(element, eventName, handler, options) {
    const plugin = this._findPluginFor(eventName);
    return plugin.addEventListener(element, eventName, handler, options);
  }
  getZone() {
    return this._zone;
  }
  _findPluginFor(eventName) {
    let plugin = this._eventNameToPlugin.get(eventName);
    if (plugin) {
      return plugin;
    }
    const plugins = this._plugins;
    plugin = plugins.find((plugin2) => plugin2.supports(eventName));
    if (!plugin) {
      throw new RuntimeError(5101, (typeof ngDevMode === "undefined" || ngDevMode) && `No event manager plugin found for event ${eventName}`);
    }
    this._eventNameToPlugin.set(eventName, plugin);
    return plugin;
  }
  static \u0275fac = function EventManager_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _EventManager)(\u0275\u0275inject(EVENT_MANAGER_PLUGINS), \u0275\u0275inject(NgZone));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _EventManager,
    factory: _EventManager.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EventManager, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [EVENT_MANAGER_PLUGINS]
    }]
  }, {
    type: NgZone
  }], null);
})();
var APP_ID_ATTRIBUTE_NAME = "ng-app-id";
function removeElements(elements) {
  for (const element of elements) {
    element.remove();
  }
}
function createStyleElement(style, doc) {
  const styleElement = doc.createElement("style");
  styleElement.textContent = style;
  return styleElement;
}
function addServerStyles(doc, appId, inline, external) {
  const elements = doc.head?.querySelectorAll(`style[${APP_ID_ATTRIBUTE_NAME}="${appId}"],link[${APP_ID_ATTRIBUTE_NAME}="${appId}"]`);
  if (elements) {
    for (const styleElement of elements) {
      styleElement.removeAttribute(APP_ID_ATTRIBUTE_NAME);
      if (styleElement instanceof HTMLLinkElement) {
        external.set(styleElement.href.slice(styleElement.href.lastIndexOf("/") + 1), {
          usage: 0,
          elements: [styleElement]
        });
      } else if (styleElement.textContent) {
        inline.set(styleElement.textContent, {
          usage: 0,
          elements: [styleElement]
        });
      }
    }
  }
}
function createLinkElement(url, doc) {
  const linkElement = doc.createElement("link");
  linkElement.setAttribute("rel", "stylesheet");
  linkElement.setAttribute("href", url);
  return linkElement;
}
var SharedStylesHost = class _SharedStylesHost {
  doc;
  appId;
  nonce;
  inline = /* @__PURE__ */ new Map();
  external = /* @__PURE__ */ new Map();
  hosts = /* @__PURE__ */ new Set();
  constructor(doc, appId, nonce, platformId = {}) {
    this.doc = doc;
    this.appId = appId;
    this.nonce = nonce;
    addServerStyles(doc, appId, this.inline, this.external);
    this.hosts.add(doc.head);
  }
  addStyles(styles, urls) {
    for (const value of styles) {
      this.addUsage(value, this.inline, createStyleElement);
    }
    urls?.forEach((value) => this.addUsage(value, this.external, createLinkElement));
  }
  removeStyles(styles, urls) {
    for (const value of styles) {
      this.removeUsage(value, this.inline);
    }
    urls?.forEach((value) => this.removeUsage(value, this.external));
  }
  addUsage(value, usages, creator) {
    const record = usages.get(value);
    if (record) {
      if ((typeof ngDevMode === "undefined" || ngDevMode) && record.usage === 0) {
        record.elements.forEach((element) => element.setAttribute("ng-style-reused", ""));
      }
      record.usage++;
    } else {
      usages.set(value, {
        usage: 1,
        elements: [...this.hosts].map((host) => this.addElement(host, creator(value, this.doc)))
      });
    }
  }
  removeUsage(value, usages) {
    const record = usages.get(value);
    if (record) {
      record.usage--;
      if (record.usage <= 0) {
        removeElements(record.elements);
        usages.delete(value);
      }
    }
  }
  ngOnDestroy() {
    for (const [, {
      elements
    }] of [...this.inline, ...this.external]) {
      removeElements(elements);
    }
    this.hosts.clear();
  }
  addHost(hostNode) {
    this.hosts.add(hostNode);
    for (const [style, {
      elements
    }] of this.inline) {
      elements.push(this.addElement(hostNode, createStyleElement(style, this.doc)));
    }
    for (const [url, {
      elements
    }] of this.external) {
      elements.push(this.addElement(hostNode, createLinkElement(url, this.doc)));
    }
  }
  removeHost(hostNode) {
    this.hosts.delete(hostNode);
  }
  addElement(host, element) {
    if (this.nonce) {
      element.setAttribute("nonce", this.nonce);
    }
    if (false) {
      element.setAttribute(APP_ID_ATTRIBUTE_NAME, this.appId);
    }
    return host.appendChild(element);
  }
  static \u0275fac = function SharedStylesHost_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _SharedStylesHost)(\u0275\u0275inject(DOCUMENT), \u0275\u0275inject(APP_ID), \u0275\u0275inject(CSP_NONCE, 8), \u0275\u0275inject(PLATFORM_ID));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _SharedStylesHost,
    factory: _SharedStylesHost.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SharedStylesHost, [{
    type: Injectable
  }], () => [{
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [APP_ID]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [CSP_NONCE]
    }, {
      type: Optional
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [PLATFORM_ID]
    }]
  }], null);
})();
var NAMESPACE_URIS = {
  "svg": "http://www.w3.org/2000/svg",
  "xhtml": "http://www.w3.org/1999/xhtml",
  "xlink": "http://www.w3.org/1999/xlink",
  "xml": "http://www.w3.org/XML/1998/namespace",
  "xmlns": "http://www.w3.org/2000/xmlns/",
  "math": "http://www.w3.org/1998/Math/MathML"
};
var COMPONENT_REGEX = /%COMP%/g;
var SOURCEMAP_URL_REGEXP = /\/\*#\s*sourceMappingURL=(.+?)\s*\*\//;
var PROTOCOL_REGEXP = /^https?:/;
var COMPONENT_VARIABLE = "%COMP%";
var HOST_ATTR = `_nghost-${COMPONENT_VARIABLE}`;
var CONTENT_ATTR = `_ngcontent-${COMPONENT_VARIABLE}`;
var REMOVE_STYLES_ON_COMPONENT_DESTROY_DEFAULT = true;
var REMOVE_STYLES_ON_COMPONENT_DESTROY = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "RemoveStylesOnCompDestroy" : "", {
  factory: () => REMOVE_STYLES_ON_COMPONENT_DESTROY_DEFAULT
});
function shimContentAttribute(componentShortId) {
  return CONTENT_ATTR.replace(COMPONENT_REGEX, componentShortId);
}
function shimHostAttribute(componentShortId) {
  return HOST_ATTR.replace(COMPONENT_REGEX, componentShortId);
}
function shimStylesContent(compId, styles) {
  return styles.map((s) => s.replace(COMPONENT_REGEX, compId));
}
function addBaseHrefToCssSourceMap(baseHref, styles) {
  if (!baseHref) {
    return styles;
  }
  const absoluteBaseHrefUrl = new URL(baseHref, "http://localhost");
  return styles.map((cssContent) => {
    if (!cssContent.includes("sourceMappingURL=")) {
      return cssContent;
    }
    return cssContent.replace(SOURCEMAP_URL_REGEXP, (_, sourceMapUrl) => {
      if (sourceMapUrl[0] === "/" || sourceMapUrl.startsWith("data:") || PROTOCOL_REGEXP.test(sourceMapUrl)) {
        return `/*# sourceMappingURL=${sourceMapUrl} */`;
      }
      const {
        pathname: resolvedSourceMapUrl
      } = new URL(sourceMapUrl, absoluteBaseHrefUrl);
      return `/*# sourceMappingURL=${resolvedSourceMapUrl} */`;
    });
  });
}
var DomRendererFactory2 = class _DomRendererFactory2 {
  eventManager;
  sharedStylesHost;
  appId;
  removeStylesOnCompDestroy;
  doc;
  ngZone;
  nonce;
  tracingService;
  rendererByCompId = /* @__PURE__ */ new Map();
  defaultRenderer;
  constructor(eventManager, sharedStylesHost, appId, removeStylesOnCompDestroy, doc, ngZone, nonce = null, tracingService = null) {
    this.eventManager = eventManager;
    this.sharedStylesHost = sharedStylesHost;
    this.appId = appId;
    this.removeStylesOnCompDestroy = removeStylesOnCompDestroy;
    this.doc = doc;
    this.ngZone = ngZone;
    this.nonce = nonce;
    this.tracingService = tracingService;
    this.defaultRenderer = new DefaultDomRenderer2(eventManager, doc, ngZone, this.tracingService);
  }
  createRenderer(element, type) {
    if (!element || !type) {
      return this.defaultRenderer;
    }
    if (false) {
      type = __spreadProps(__spreadValues({}, type), {
        encapsulation: ViewEncapsulation.Emulated
      });
    }
    const renderer = this.getOrCreateRenderer(element, type);
    if (renderer instanceof EmulatedEncapsulationDomRenderer2) {
      renderer.applyToHost(element);
    } else if (renderer instanceof NoneEncapsulationDomRenderer) {
      renderer.applyStyles();
    }
    return renderer;
  }
  getOrCreateRenderer(element, type) {
    const rendererByCompId = this.rendererByCompId;
    let renderer = rendererByCompId.get(type.id);
    if (!renderer) {
      const doc = this.doc;
      const ngZone = this.ngZone;
      const eventManager = this.eventManager;
      const sharedStylesHost = this.sharedStylesHost;
      const removeStylesOnCompDestroy = this.removeStylesOnCompDestroy;
      const tracingService = this.tracingService;
      switch (type.encapsulation) {
        case ViewEncapsulation.Emulated:
          renderer = new EmulatedEncapsulationDomRenderer2(eventManager, sharedStylesHost, type, this.appId, removeStylesOnCompDestroy, doc, ngZone, tracingService);
          break;
        case ViewEncapsulation.ShadowDom:
          return new ShadowDomRenderer(eventManager, element, type, doc, ngZone, this.nonce, tracingService, sharedStylesHost);
        case ViewEncapsulation.ExperimentalIsolatedShadowDom:
          return new ShadowDomRenderer(eventManager, element, type, doc, ngZone, this.nonce, tracingService);
        default:
          renderer = new NoneEncapsulationDomRenderer(eventManager, sharedStylesHost, type, removeStylesOnCompDestroy, doc, ngZone, tracingService);
          break;
      }
      rendererByCompId.set(type.id, renderer);
    }
    return renderer;
  }
  ngOnDestroy() {
    this.rendererByCompId.clear();
  }
  componentReplaced(componentId) {
    this.rendererByCompId.delete(componentId);
  }
  static \u0275fac = function DomRendererFactory2_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DomRendererFactory2)(\u0275\u0275inject(EventManager), \u0275\u0275inject(SharedStylesHost), \u0275\u0275inject(APP_ID), \u0275\u0275inject(REMOVE_STYLES_ON_COMPONENT_DESTROY), \u0275\u0275inject(DOCUMENT), \u0275\u0275inject(NgZone), \u0275\u0275inject(CSP_NONCE), \u0275\u0275inject(TracingService, 8));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _DomRendererFactory2,
    factory: _DomRendererFactory2.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomRendererFactory2, [{
    type: Injectable
  }], () => [{
    type: EventManager
  }, {
    type: SharedStylesHost
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [APP_ID]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [REMOVE_STYLES_ON_COMPONENT_DESTROY]
    }]
  }, {
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: NgZone
  }, {
    type: void 0,
    decorators: [{
      type: Inject,
      args: [CSP_NONCE]
    }]
  }, {
    type: TracingService,
    decorators: [{
      type: Inject,
      args: [TracingService]
    }, {
      type: Optional
    }]
  }], null);
})();
var DefaultDomRenderer2 = class {
  eventManager;
  doc;
  ngZone;
  tracingService;
  data = /* @__PURE__ */ Object.create(null);
  throwOnSyntheticProps = true;
  constructor(eventManager, doc, ngZone, tracingService) {
    this.eventManager = eventManager;
    this.doc = doc;
    this.ngZone = ngZone;
    this.tracingService = tracingService;
  }
  destroy() {
  }
  destroyNode = null;
  createElement(name, namespace) {
    if (namespace) {
      return this.doc.createElementNS(NAMESPACE_URIS[namespace] || namespace, name);
    }
    return this.doc.createElement(name);
  }
  createComment(value) {
    return this.doc.createComment(value);
  }
  createText(value) {
    return this.doc.createTextNode(value);
  }
  appendChild(parent, newChild) {
    const targetParent = isTemplateNode(parent) ? parent.content : parent;
    targetParent.appendChild(newChild);
  }
  insertBefore(parent, newChild, refChild) {
    if (parent) {
      const targetParent = isTemplateNode(parent) ? parent.content : parent;
      targetParent.insertBefore(newChild, refChild);
    }
  }
  removeChild(_parent, oldChild) {
    oldChild.remove();
  }
  selectRootElement(selectorOrNode, preserveContent) {
    let el = typeof selectorOrNode === "string" ? this.doc.querySelector(selectorOrNode) : selectorOrNode;
    if (!el) {
      throw new RuntimeError(-5104, (typeof ngDevMode === "undefined" || ngDevMode) && `The selector "${selectorOrNode}" did not match any elements`);
    }
    if (!preserveContent) {
      el.textContent = "";
    }
    return el;
  }
  parentNode(node) {
    return node.parentNode;
  }
  nextSibling(node) {
    return node.nextSibling;
  }
  setAttribute(el, name, value, namespace) {
    if (namespace) {
      name = namespace + ":" + name;
      const namespaceUri = NAMESPACE_URIS[namespace];
      if (namespaceUri) {
        el.setAttributeNS(namespaceUri, name, value);
      } else {
        el.setAttribute(name, value);
      }
    } else {
      el.setAttribute(name, value);
    }
  }
  removeAttribute(el, name, namespace) {
    if (namespace) {
      const namespaceUri = NAMESPACE_URIS[namespace];
      if (namespaceUri) {
        el.removeAttributeNS(namespaceUri, name);
      } else {
        el.removeAttribute(`${namespace}:${name}`);
      }
    } else {
      el.removeAttribute(name);
    }
  }
  addClass(el, name) {
    el.classList.add(name);
  }
  removeClass(el, name) {
    el.classList.remove(name);
  }
  setStyle(el, style, value, flags) {
    if (flags & (RendererStyleFlags2.DashCase | RendererStyleFlags2.Important)) {
      el.style.setProperty(style, value, flags & RendererStyleFlags2.Important ? "important" : "");
    } else {
      el.style[style] = value;
    }
  }
  removeStyle(el, style, flags) {
    if (flags & RendererStyleFlags2.DashCase) {
      el.style.removeProperty(style);
    } else {
      el.style[style] = "";
    }
  }
  setProperty(el, name, value) {
    if (el == null) {
      return;
    }
    (typeof ngDevMode === "undefined" || ngDevMode) && this.throwOnSyntheticProps && checkNoSyntheticProp(name, "property");
    el[name] = value;
  }
  setValue(node, value) {
    node.nodeValue = value;
  }
  listen(target, event, callback, options) {
    (typeof ngDevMode === "undefined" || ngDevMode) && this.throwOnSyntheticProps && checkNoSyntheticProp(event, "listener");
    if (typeof target === "string") {
      target = getDOM().getGlobalEventTarget(this.doc, target);
      if (!target) {
        throw new RuntimeError(5102, (typeof ngDevMode === "undefined" || ngDevMode) && `Unsupported event target ${target} for event ${event}`);
      }
    }
    let wrappedCallback = this.decoratePreventDefault(callback);
    if (this.tracingService?.wrapEventListener) {
      wrappedCallback = this.tracingService.wrapEventListener(target, event, wrappedCallback);
    }
    return this.eventManager.addEventListener(target, event, wrappedCallback, options);
  }
  decoratePreventDefault(eventHandler) {
    return (event) => {
      if (event === "__ngUnwrap__") {
        return eventHandler;
      }
      const allowDefaultBehavior = false ? this.ngZone.runGuarded(() => eventHandler(event)) : eventHandler(event);
      if (allowDefaultBehavior === false) {
        event.preventDefault();
      }
      return void 0;
    };
  }
};
var AT_CHARCODE = (() => "@".charCodeAt(0))();
function checkNoSyntheticProp(name, nameKind) {
  if (name.charCodeAt(0) === AT_CHARCODE) {
    throw new RuntimeError(5105, `Unexpected synthetic ${nameKind} ${name} found. Please make sure that:
  - Make sure \`provideAnimationsAsync()\`, \`provideAnimations()\` or \`provideNoopAnimations()\` call was added to a list of providers used to bootstrap an application.
  - There is a corresponding animation configuration named \`${name}\` defined in the \`animations\` field of the \`@Component\` decorator (see https://angular.dev/api/core/Component#animations).`);
  }
}
function isTemplateNode(node) {
  return node.tagName === "TEMPLATE" && node.content !== void 0;
}
var ShadowDomRenderer = class extends DefaultDomRenderer2 {
  hostEl;
  sharedStylesHost;
  shadowRoot;
  constructor(eventManager, hostEl, component, doc, ngZone, nonce, tracingService, sharedStylesHost) {
    super(eventManager, doc, ngZone, tracingService);
    this.hostEl = hostEl;
    this.sharedStylesHost = sharedStylesHost;
    this.shadowRoot = hostEl.attachShadow({
      mode: "open"
    });
    if (this.sharedStylesHost) {
      this.sharedStylesHost.addHost(this.shadowRoot);
    }
    let styles = component.styles;
    if (ngDevMode) {
      const baseHref = getDOM().getBaseHref(doc) ?? "";
      styles = addBaseHrefToCssSourceMap(baseHref, styles);
    }
    styles = shimStylesContent(component.id, styles);
    for (const style of styles) {
      const styleEl = document.createElement("style");
      if (nonce) {
        styleEl.setAttribute("nonce", nonce);
      }
      styleEl.textContent = style;
      this.shadowRoot.appendChild(styleEl);
    }
    const styleUrls = component.getExternalStyles?.();
    if (styleUrls) {
      for (const styleUrl of styleUrls) {
        const linkEl = createLinkElement(styleUrl, doc);
        if (nonce) {
          linkEl.setAttribute("nonce", nonce);
        }
        this.shadowRoot.appendChild(linkEl);
      }
    }
  }
  nodeOrShadowRoot(node) {
    return node === this.hostEl ? this.shadowRoot : node;
  }
  appendChild(parent, newChild) {
    return super.appendChild(this.nodeOrShadowRoot(parent), newChild);
  }
  insertBefore(parent, newChild, refChild) {
    return super.insertBefore(this.nodeOrShadowRoot(parent), newChild, refChild);
  }
  removeChild(_parent, oldChild) {
    return super.removeChild(null, oldChild);
  }
  parentNode(node) {
    return this.nodeOrShadowRoot(super.parentNode(this.nodeOrShadowRoot(node)));
  }
  destroy() {
    if (this.sharedStylesHost) {
      this.sharedStylesHost.removeHost(this.shadowRoot);
    }
  }
};
var NoneEncapsulationDomRenderer = class extends DefaultDomRenderer2 {
  sharedStylesHost;
  removeStylesOnCompDestroy;
  styles;
  styleUrls;
  constructor(eventManager, sharedStylesHost, component, removeStylesOnCompDestroy, doc, ngZone, tracingService, compId) {
    super(eventManager, doc, ngZone, tracingService);
    this.sharedStylesHost = sharedStylesHost;
    this.removeStylesOnCompDestroy = removeStylesOnCompDestroy;
    let styles = component.styles;
    if (ngDevMode) {
      const baseHref = getDOM().getBaseHref(doc) ?? "";
      styles = addBaseHrefToCssSourceMap(baseHref, styles);
    }
    this.styles = compId ? shimStylesContent(compId, styles) : styles;
    this.styleUrls = component.getExternalStyles?.(compId);
  }
  applyStyles() {
    this.sharedStylesHost.addStyles(this.styles, this.styleUrls);
  }
  destroy() {
    if (!this.removeStylesOnCompDestroy) {
      return;
    }
    if (allLeavingAnimations.size === 0) {
      this.sharedStylesHost.removeStyles(this.styles, this.styleUrls);
    }
  }
};
var EmulatedEncapsulationDomRenderer2 = class extends NoneEncapsulationDomRenderer {
  contentAttr;
  hostAttr;
  constructor(eventManager, sharedStylesHost, component, appId, removeStylesOnCompDestroy, doc, ngZone, tracingService) {
    const compId = appId + "-" + component.id;
    super(eventManager, sharedStylesHost, component, removeStylesOnCompDestroy, doc, ngZone, tracingService, compId);
    this.contentAttr = shimContentAttribute(compId);
    this.hostAttr = shimHostAttribute(compId);
  }
  applyToHost(element) {
    this.applyStyles();
    this.setAttribute(element, this.hostAttr, "");
  }
  createElement(parent, name) {
    const el = super.createElement(parent, name);
    super.setAttribute(el, this.contentAttr, "");
    return el;
  }
};

// node_modules/@angular/platform-browser/fesm2022/_browser-chunk.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var BrowserDomAdapter = class _BrowserDomAdapter extends DomAdapter {
  supportsDOMEvents = true;
  static makeCurrent() {
    setRootDomAdapter(new _BrowserDomAdapter());
  }
  onAndCancel(el, evt, listener, options) {
    el.addEventListener(evt, listener, options);
    return () => {
      el.removeEventListener(evt, listener, options);
    };
  }
  dispatchEvent(el, evt) {
    el.dispatchEvent(evt);
  }
  remove(node) {
    node.remove();
  }
  createElement(tagName, doc) {
    doc = doc || this.getDefaultDocument();
    return doc.createElement(tagName);
  }
  createHtmlDocument() {
    return document.implementation.createHTMLDocument("fakeTitle");
  }
  getDefaultDocument() {
    return document;
  }
  isElementNode(node) {
    return node.nodeType === Node.ELEMENT_NODE;
  }
  isShadowRoot(node) {
    return node instanceof DocumentFragment;
  }
  getGlobalEventTarget(doc, target) {
    if (target === "window") {
      return window;
    }
    if (target === "document") {
      return doc;
    }
    if (target === "body") {
      return doc.body;
    }
    return null;
  }
  getBaseHref(doc) {
    const href = getBaseElementHref();
    return href == null ? null : relativePath(href);
  }
  resetBaseElement() {
    baseElement = null;
  }
  getUserAgent() {
    return window.navigator.userAgent;
  }
  getCookie(name) {
    return parseCookieValue(document.cookie, name);
  }
};
var baseElement = null;
function getBaseElementHref() {
  baseElement = baseElement || document.head.querySelector("base");
  return baseElement ? baseElement.getAttribute("href") : null;
}
function relativePath(url) {
  return new URL(url, document.baseURI).pathname;
}
var BrowserGetTestability = class {
  addToWindow(registry) {
    _global["getAngularTestability"] = (elem, findInAncestors = true) => {
      const testability = registry.findTestabilityInTree(elem, findInAncestors);
      if (testability == null) {
        throw new RuntimeError(5103, (typeof ngDevMode === "undefined" || ngDevMode) && "Could not find testability for element.");
      }
      return testability;
    };
    _global["getAllAngularTestabilities"] = () => registry.getAllTestabilities();
    _global["getAllAngularRootElements"] = () => registry.getAllRootElements();
    const whenAllStable = (callback) => {
      const testabilities = _global["getAllAngularTestabilities"]();
      let count = testabilities.length;
      const decrement = function() {
        count--;
        if (count == 0) {
          callback();
        }
      };
      testabilities.forEach((testability) => {
        testability.whenStable(decrement);
      });
    };
    if (!_global["frameworkStabilizers"]) {
      _global["frameworkStabilizers"] = [];
    }
    _global["frameworkStabilizers"].push(whenAllStable);
  }
  findTestabilityInTree(registry, elem, findInAncestors) {
    if (elem == null) {
      return null;
    }
    const t = registry.getTestability(elem);
    if (t != null) {
      return t;
    } else if (!findInAncestors) {
      return null;
    }
    if (getDOM().isShadowRoot(elem)) {
      return this.findTestabilityInTree(registry, elem.host, true);
    }
    return this.findTestabilityInTree(registry, elem.parentElement, true);
  }
};
var BrowserXhr = class _BrowserXhr {
  build() {
    return new XMLHttpRequest();
  }
  static \u0275fac = function BrowserXhr_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BrowserXhr)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _BrowserXhr,
    factory: _BrowserXhr.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BrowserXhr, [{
    type: Injectable
  }], null, null);
})();
var MODIFIER_KEYS = ["alt", "control", "meta", "shift"];
var _keyMap = {
  "\b": "Backspace",
  "	": "Tab",
  "\x7F": "Delete",
  "\x1B": "Escape",
  "Del": "Delete",
  "Esc": "Escape",
  "Left": "ArrowLeft",
  "Right": "ArrowRight",
  "Up": "ArrowUp",
  "Down": "ArrowDown",
  "Menu": "ContextMenu",
  "Scroll": "ScrollLock",
  "Win": "OS"
};
var MODIFIER_KEY_GETTERS = {
  "alt": (event) => event.altKey,
  "control": (event) => event.ctrlKey,
  "meta": (event) => event.metaKey,
  "shift": (event) => event.shiftKey
};
var KeyEventsPlugin = class _KeyEventsPlugin extends EventManagerPlugin {
  constructor(doc) {
    super(doc);
  }
  supports(eventName) {
    return _KeyEventsPlugin.parseEventName(eventName) != null;
  }
  addEventListener(element, eventName, handler, options) {
    const parsedEvent = _KeyEventsPlugin.parseEventName(eventName);
    const outsideHandler = _KeyEventsPlugin.eventCallback(parsedEvent["fullKey"], handler, this.manager.getZone());
    return this.manager.getZone().runOutsideAngular(() => {
      return getDOM().onAndCancel(element, parsedEvent["domEventName"], outsideHandler, options);
    });
  }
  static parseEventName(eventName) {
    const parts = eventName.toLowerCase().split(".");
    const domEventName = parts.shift();
    if (parts.length === 0 || !(domEventName === "keydown" || domEventName === "keyup")) {
      return null;
    }
    const key = _KeyEventsPlugin._normalizeKey(parts.pop());
    let fullKey = "";
    let codeIX = parts.indexOf("code");
    if (codeIX > -1) {
      parts.splice(codeIX, 1);
      fullKey = "code.";
    }
    MODIFIER_KEYS.forEach((modifierName) => {
      const index = parts.indexOf(modifierName);
      if (index > -1) {
        parts.splice(index, 1);
        fullKey += modifierName + ".";
      }
    });
    fullKey += key;
    if (parts.length != 0 || key.length === 0) {
      return null;
    }
    const result = {};
    result["domEventName"] = domEventName;
    result["fullKey"] = fullKey;
    return result;
  }
  static matchEventFullKeyCode(event, fullKeyCode) {
    let keycode = _keyMap[event.key] || event.key;
    let key = "";
    if (fullKeyCode.indexOf("code.") > -1) {
      keycode = event.code;
      key = "code.";
    }
    if (keycode == null || !keycode) return false;
    keycode = keycode.toLowerCase();
    if (keycode === " ") {
      keycode = "space";
    } else if (keycode === ".") {
      keycode = "dot";
    }
    MODIFIER_KEYS.forEach((modifierName) => {
      if (modifierName !== keycode) {
        const modifierGetter = MODIFIER_KEY_GETTERS[modifierName];
        if (modifierGetter(event)) {
          key += modifierName + ".";
        }
      }
    });
    key += keycode;
    return key === fullKeyCode;
  }
  static eventCallback(fullKey, handler, zone) {
    return (event) => {
      if (_KeyEventsPlugin.matchEventFullKeyCode(event, fullKey)) {
        zone.runGuarded(() => handler(event));
      }
    };
  }
  static _normalizeKey(keyName) {
    return keyName === "esc" ? "escape" : keyName;
  }
  static \u0275fac = function KeyEventsPlugin_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _KeyEventsPlugin)(\u0275\u0275inject(DOCUMENT));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _KeyEventsPlugin,
    factory: _KeyEventsPlugin.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(KeyEventsPlugin, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
async function bootstrapApplication(rootComponent, options, context2) {
  const config2 = __spreadValues({
    rootComponent
  }, createProvidersConfig(options, context2));
  if (false) {
    await resolveJitResources();
  }
  return internalCreateApplication(config2);
}
function createProvidersConfig(options, context2) {
  return {
    platformRef: context2?.platformRef,
    appProviders: [...BROWSER_MODULE_PROVIDERS, ...options?.providers ?? []],
    platformProviders: INTERNAL_BROWSER_PLATFORM_PROVIDERS
  };
}
function initDomAdapter() {
  BrowserDomAdapter.makeCurrent();
}
function errorHandler() {
  return new ErrorHandler();
}
function _document() {
  setDocument(document);
  return document;
}
var INTERNAL_BROWSER_PLATFORM_PROVIDERS = [{
  provide: PLATFORM_ID,
  useValue: PLATFORM_BROWSER_ID
}, {
  provide: PLATFORM_INITIALIZER,
  useValue: initDomAdapter,
  multi: true
}, {
  provide: DOCUMENT,
  useFactory: _document
}];
var platformBrowser = createPlatformFactory(platformCore, "browser", INTERNAL_BROWSER_PLATFORM_PROVIDERS);
var BROWSER_MODULE_PROVIDERS_MARKER = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "BrowserModule Providers Marker" : "");
var TESTABILITY_PROVIDERS = [{
  provide: TESTABILITY_GETTER,
  useClass: BrowserGetTestability
}, {
  provide: TESTABILITY,
  useClass: Testability
}, {
  provide: Testability,
  useClass: Testability
}];
var BROWSER_MODULE_PROVIDERS = [{
  provide: INJECTOR_SCOPE,
  useValue: "root"
}, {
  provide: ErrorHandler,
  useFactory: errorHandler
}, {
  provide: EVENT_MANAGER_PLUGINS,
  useClass: DomEventsPlugin,
  multi: true
}, {
  provide: EVENT_MANAGER_PLUGINS,
  useClass: KeyEventsPlugin,
  multi: true
}, DomRendererFactory2, SharedStylesHost, EventManager, {
  provide: RendererFactory2,
  useExisting: DomRendererFactory2
}, {
  provide: XhrFactory,
  useClass: BrowserXhr
}, typeof ngDevMode === "undefined" || ngDevMode ? {
  provide: BROWSER_MODULE_PROVIDERS_MARKER,
  useValue: true
} : []];
var BrowserModule = class _BrowserModule {
  constructor() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      const providersAlreadyPresent = inject2(BROWSER_MODULE_PROVIDERS_MARKER, {
        optional: true,
        skipSelf: true
      });
      if (providersAlreadyPresent) {
        throw new RuntimeError(5100, `Providers from the \`BrowserModule\` have already been loaded. If you need access to common directives such as NgIf and NgFor, import the \`CommonModule\` instead.`);
      }
    }
  }
  static \u0275fac = function BrowserModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BrowserModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _BrowserModule,
    exports: [CommonModule, ApplicationModule]
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
    providers: [...BROWSER_MODULE_PROVIDERS, ...TESTABILITY_PROVIDERS],
    imports: [CommonModule, ApplicationModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BrowserModule, [{
    type: NgModule,
    args: [{
      providers: [...BROWSER_MODULE_PROVIDERS, ...TESTABILITY_PROVIDERS],
      exports: [CommonModule, ApplicationModule]
    }]
  }], () => [], null);
})();

// node_modules/@angular/platform-browser/fesm2022/platform-browser.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var Meta = class _Meta {
  _doc;
  _dom;
  constructor(_doc) {
    this._doc = _doc;
    this._dom = getDOM();
  }
  addTag(tag, forceCreation = false) {
    if (!tag) return null;
    return this._getOrCreateElement(tag, forceCreation);
  }
  addTags(tags, forceCreation = false) {
    if (!tags) return [];
    return tags.reduce((result, tag) => {
      if (tag) {
        result.push(this._getOrCreateElement(tag, forceCreation));
      }
      return result;
    }, []);
  }
  getTag(attrSelector) {
    if (!attrSelector) return null;
    const meta = this._doc.querySelector(`meta[${attrSelector}]`);
    return meta?.nodeName.toLowerCase() === "meta" ? meta : null;
  }
  getTags(attrSelector) {
    if (!attrSelector) return [];
    const list = this._doc.querySelectorAll(`meta[${attrSelector}]`);
    return list ? [].slice.call(list).filter((elem) => elem.nodeName.toLowerCase() === "meta") : [];
  }
  updateTag(tag, selector) {
    if (!tag) return null;
    selector = selector || this._parseSelector(tag);
    const meta = this.getTag(selector);
    if (meta) {
      return this._setMetaElementAttributes(tag, meta);
    }
    return this._getOrCreateElement(tag, true);
  }
  removeTag(attrSelector) {
    this.removeTagElement(this.getTag(attrSelector));
  }
  removeTagElement(meta) {
    if (meta) {
      this._dom.remove(meta);
    }
  }
  _getOrCreateElement(meta, forceCreation = false) {
    if (!forceCreation) {
      const selector = this._parseSelector(meta);
      const elem = this.getTags(selector).filter((elem2) => this._containsAttributes(meta, elem2))[0];
      if (elem !== void 0) return elem;
    }
    const element = this._dom.createElement("meta");
    this._setMetaElementAttributes(meta, element);
    const head = this._doc.getElementsByTagName("head")[0];
    head.appendChild(element);
    return element;
  }
  _setMetaElementAttributes(tag, el) {
    Object.keys(tag).forEach((prop) => el.setAttribute(this._getMetaKeyMap(prop), tag[prop]));
    return el;
  }
  _parseSelector(tag) {
    const attr = tag.name ? "name" : "property";
    return `${attr}=${this._escapeSelectorValue(String(tag[attr]))}`;
  }
  _escapeSelectorValue(value) {
    return `"${value.replace(/\\/g, "\\\\").replace(/"/g, '\\"')}"`;
  }
  _containsAttributes(tag, elem) {
    return Object.keys(tag).every((key) => elem.getAttribute(this._getMetaKeyMap(key)) === tag[key]);
  }
  _getMetaKeyMap(prop) {
    return META_KEYS_MAP[prop] || prop;
  }
  static \u0275fac = function Meta_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Meta)(\u0275\u0275inject(DOCUMENT));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Meta,
    factory: _Meta.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Meta, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var META_KEYS_MAP = {
  httpEquiv: "http-equiv"
};
var Title = class _Title {
  _doc;
  constructor(_doc) {
    this._doc = _doc;
  }
  getTitle() {
    return this._doc.title;
  }
  setTitle(newTitle) {
    this._doc.title = newTitle || "";
  }
  static \u0275fac = function Title_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Title)(\u0275\u0275inject(DOCUMENT));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _Title,
    factory: _Title.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Title, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var EVENT_NAMES = {
  "pan": true,
  "panstart": true,
  "panmove": true,
  "panend": true,
  "pancancel": true,
  "panleft": true,
  "panright": true,
  "panup": true,
  "pandown": true,
  "pinch": true,
  "pinchstart": true,
  "pinchmove": true,
  "pinchend": true,
  "pinchcancel": true,
  "pinchin": true,
  "pinchout": true,
  "press": true,
  "pressup": true,
  "rotate": true,
  "rotatestart": true,
  "rotatemove": true,
  "rotateend": true,
  "rotatecancel": true,
  "swipe": true,
  "swipeleft": true,
  "swiperight": true,
  "swipeup": true,
  "swipedown": true,
  "tap": true,
  "doubletap": true
};
var HAMMER_GESTURE_CONFIG = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "HammerGestureConfig" : "");
var HAMMER_LOADER = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "HammerLoader" : "");
var HammerGestureConfig = class _HammerGestureConfig {
  events = [];
  overrides = {};
  options;
  buildHammer(element) {
    const mc = new Hammer(element, this.options);
    mc.get("pinch").set({
      enable: true
    });
    mc.get("rotate").set({
      enable: true
    });
    for (const eventName in this.overrides) {
      mc.get(eventName).set(this.overrides[eventName]);
    }
    return mc;
  }
  static \u0275fac = function HammerGestureConfig_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _HammerGestureConfig)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _HammerGestureConfig,
    factory: _HammerGestureConfig.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HammerGestureConfig, [{
    type: Injectable
  }], null, null);
})();
var HammerGesturesPlugin = class _HammerGesturesPlugin extends EventManagerPlugin {
  _config;
  _injector;
  loader;
  _loaderPromise = null;
  constructor(doc, _config, _injector, loader) {
    super(doc);
    this._config = _config;
    this._injector = _injector;
    this.loader = loader;
  }
  supports(eventName) {
    if (!EVENT_NAMES.hasOwnProperty(eventName.toLowerCase()) && !this.isCustomEvent(eventName)) {
      return false;
    }
    if (!window.Hammer && !this.loader) {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        const _console = this._injector.get(Console);
        _console.warn(`The "${eventName}" event cannot be bound because Hammer.JS is not loaded and no custom loader has been specified.`);
      }
      return false;
    }
    return true;
  }
  addEventListener(element, eventName, handler) {
    const zone = this.manager.getZone();
    eventName = eventName.toLowerCase();
    if (!window.Hammer && this.loader) {
      this._loaderPromise = this._loaderPromise || zone.runOutsideAngular(() => this.loader());
      let cancelRegistration = false;
      let deregister = () => {
        cancelRegistration = true;
      };
      zone.runOutsideAngular(() => this._loaderPromise.then(() => {
        if (!window.Hammer) {
          if (typeof ngDevMode === "undefined" || ngDevMode) {
            const _console = this._injector.get(Console);
            _console.warn(`The custom HAMMER_LOADER completed, but Hammer.JS is not present.`);
          }
          deregister = () => {
          };
          return;
        }
        if (!cancelRegistration) {
          deregister = this.addEventListener(element, eventName, handler);
        }
      }).catch(() => {
        if (typeof ngDevMode === "undefined" || ngDevMode) {
          const _console = this._injector.get(Console);
          _console.warn(`The "${eventName}" event cannot be bound because the custom Hammer.JS loader failed.`);
        }
        deregister = () => {
        };
      }));
      return () => {
        deregister();
      };
    }
    return zone.runOutsideAngular(() => {
      const mc = this._config.buildHammer(element);
      const callback = function(eventObj) {
        zone.runGuarded(function() {
          handler(eventObj);
        });
      };
      mc.on(eventName, callback);
      return () => {
        mc.off(eventName, callback);
        if (typeof mc.destroy === "function") {
          mc.destroy();
        }
      };
    });
  }
  isCustomEvent(eventName) {
    return this._config.events.indexOf(eventName) > -1;
  }
  static \u0275fac = function HammerGesturesPlugin_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _HammerGesturesPlugin)(\u0275\u0275inject(DOCUMENT), \u0275\u0275inject(HAMMER_GESTURE_CONFIG), \u0275\u0275inject(Injector), \u0275\u0275inject(HAMMER_LOADER, 8));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _HammerGesturesPlugin,
    factory: _HammerGesturesPlugin.\u0275fac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HammerGesturesPlugin, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: HammerGestureConfig,
    decorators: [{
      type: Inject,
      args: [HAMMER_GESTURE_CONFIG]
    }]
  }, {
    type: Injector
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [HAMMER_LOADER]
    }]
  }], null);
})();
var HammerModule = class _HammerModule {
  static \u0275fac = function HammerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _HammerModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _HammerModule
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
    providers: [{
      provide: EVENT_MANAGER_PLUGINS,
      useClass: HammerGesturesPlugin,
      multi: true,
      deps: [DOCUMENT, HAMMER_GESTURE_CONFIG, Injector, [new Optional(), HAMMER_LOADER]]
    }, {
      provide: HAMMER_GESTURE_CONFIG,
      useClass: HammerGestureConfig
    }]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(HammerModule, [{
    type: NgModule,
    args: [{
      providers: [{
        provide: EVENT_MANAGER_PLUGINS,
        useClass: HammerGesturesPlugin,
        multi: true,
        deps: [DOCUMENT, HAMMER_GESTURE_CONFIG, Injector, [new Optional(), HAMMER_LOADER]]
      }, {
        provide: HAMMER_GESTURE_CONFIG,
        useClass: HammerGestureConfig
      }]
    }]
  }], null, null);
})();
var DomSanitizer = class _DomSanitizer {
  static \u0275fac = function DomSanitizer_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DomSanitizer)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _DomSanitizer,
    factory: function DomSanitizer_Factory(__ngFactoryType__) {
      let __ngConditionalFactory__ = null;
      if (__ngFactoryType__) {
        __ngConditionalFactory__ = new (__ngFactoryType__ || _DomSanitizer)();
      } else {
        __ngConditionalFactory__ = \u0275\u0275inject(DomSanitizerImpl);
      }
      return __ngConditionalFactory__;
    },
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomSanitizer, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useExisting: forwardRef(() => DomSanitizerImpl)
    }]
  }], null, null);
})();
var DomSanitizerImpl = class _DomSanitizerImpl extends DomSanitizer {
  _doc;
  constructor(_doc) {
    super();
    this._doc = _doc;
  }
  sanitize(ctx, value) {
    if (value == null) return null;
    switch (ctx) {
      case SecurityContext.NONE:
        return value;
      case SecurityContext.HTML:
        if (allowSanitizationBypassAndThrow(value, "HTML")) {
          return unwrapSafeValue(value);
        }
        return _sanitizeHtml(this._doc, String(value)).toString();
      case SecurityContext.STYLE:
        if (allowSanitizationBypassAndThrow(value, "Style")) {
          return unwrapSafeValue(value);
        }
        return value;
      case SecurityContext.SCRIPT:
        if (allowSanitizationBypassAndThrow(value, "Script")) {
          return unwrapSafeValue(value);
        }
        throw new RuntimeError(5200, (typeof ngDevMode === "undefined" || ngDevMode) && "unsafe value used in a script context");
      case SecurityContext.URL:
        if (allowSanitizationBypassAndThrow(value, "URL")) {
          return unwrapSafeValue(value);
        }
        return _sanitizeUrl(String(value));
      case SecurityContext.RESOURCE_URL:
        if (allowSanitizationBypassAndThrow(value, "ResourceURL")) {
          return unwrapSafeValue(value);
        }
        throw new RuntimeError(5201, (typeof ngDevMode === "undefined" || ngDevMode) && `unsafe value used in a resource URL context (see ${XSS_SECURITY_URL})`);
      default:
        throw new RuntimeError(5202, (typeof ngDevMode === "undefined" || ngDevMode) && `Unexpected SecurityContext ${ctx} (see ${XSS_SECURITY_URL})`);
    }
  }
  bypassSecurityTrustHtml(value) {
    return bypassSanitizationTrustHtml(value);
  }
  bypassSecurityTrustStyle(value) {
    return bypassSanitizationTrustStyle(value);
  }
  bypassSecurityTrustScript(value) {
    return bypassSanitizationTrustScript(value);
  }
  bypassSecurityTrustUrl(value) {
    return bypassSanitizationTrustUrl(value);
  }
  bypassSecurityTrustResourceUrl(value) {
    return bypassSanitizationTrustResourceUrl(value);
  }
  static \u0275fac = function DomSanitizerImpl_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DomSanitizerImpl)(\u0275\u0275inject(DOCUMENT));
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _DomSanitizerImpl,
    factory: _DomSanitizerImpl.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DomSanitizerImpl, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var HydrationFeatureKind;
(function(HydrationFeatureKind2) {
  HydrationFeatureKind2[HydrationFeatureKind2["NoHttpTransferCache"] = 0] = "NoHttpTransferCache";
  HydrationFeatureKind2[HydrationFeatureKind2["HttpTransferCacheOptions"] = 1] = "HttpTransferCacheOptions";
  HydrationFeatureKind2[HydrationFeatureKind2["I18nSupport"] = 2] = "I18nSupport";
  HydrationFeatureKind2[HydrationFeatureKind2["EventReplay"] = 3] = "EventReplay";
  HydrationFeatureKind2[HydrationFeatureKind2["IncrementalHydration"] = 4] = "IncrementalHydration";
})(HydrationFeatureKind || (HydrationFeatureKind = {}));

// src/app/core/document/document-model.ts
var EMPTY_ARRAY_FIELDS = [
  "roles",
  "stages",
  "stageFlowRefs",
  "processes",
  "entities",
  "businessComponents",
  "businessConstructs",
  "taskDefinitions",
  "serviceGroups",
  "services",
  "terms",
  "dataDictionaries",
  "rules"
];
function asArray(value) {
  return Array.isArray(value) ? value : [];
}
function identityOf(item) {
  return String(item?.uid || item?.id || "").trim();
}
function findProcessByIdentity(document2, processId) {
  const target = String(processId || "").trim();
  if (!target)
    return void 0;
  return document2.processes.find((process2) => process2.uid === target || process2.id === target);
}
function findStageByIdentity(document2, stageId) {
  const target = String(stageId || "").trim();
  if (!target)
    return void 0;
  return document2.stages.find((stage) => stage.uid === target || stage.id === target);
}
function normalizeDocument(raw) {
  const document2 = __spreadValues({}, raw || {});
  document2.meta = __spreadProps(__spreadValues({}, document2.meta || {}), {
    domain: String(document2.meta?.domain || document2.meta?.title || "\u672A\u547D\u540D\u6A21\u578B").trim(),
    title: document2.meta?.title,
    author: document2.meta?.author,
    date: document2.meta?.date,
    space: document2.meta?.space,
    tags: document2.meta?.tags,
    version: document2.meta?.version,
    owner: document2.meta?.owner
  });
  for (const field of EMPTY_ARRAY_FIELDS) {
    if (!Array.isArray(document2[field])) {
      document2[field] = [];
    }
  }
  document2.roles = document2.roles.map((role, index) => __spreadProps(__spreadValues({}, role), {
    uid: identityOf(role) || `role-${index + 1}`,
    name: String(role.name || `\u89D2\u8272${index + 1}`).trim()
  }));
  document2.stages = document2.stages.map((stage, index) => __spreadProps(__spreadValues({}, stage), {
    uid: identityOf(stage) || `stage-${index + 1}`,
    name: String(stage.name || `\u9636\u6BB5${index + 1}`).trim()
  }));
  document2.processes = document2.processes.map((process2, index) => __spreadProps(__spreadValues({}, process2), {
    uid: identityOf(process2) || `process-${index + 1}`,
    name: String(process2.name || `\u6D41\u7A0B${index + 1}`).trim(),
    nodes: Array.isArray(process2.nodes) ? process2.nodes : []
  }));
  document2.entities = document2.entities.map((entity, index) => __spreadProps(__spreadValues({}, entity), {
    uid: identityOf(entity) || `entity-${index + 1}`,
    name: String(entity.name || `\u5B9E\u4F53${index + 1}`).trim(),
    fields: Array.isArray(entity.fields) ? entity.fields : []
  }));
  document2.businessComponents = document2.businessComponents.map((component, index) => normalizeComponent(component, index));
  document2.businessConstructs = document2.businessConstructs.map((construct, index) => normalizeConstruct(construct, index));
  document2.taskDefinitions = document2.taskDefinitions.map((taskDefinition, index) => normalizeTaskDefinition(taskDefinition, index));
  migrateLegacyNodeTaskOrchestration(document2);
  rebuildServiceNodeRefs(document2);
  document2.serviceGroups = document2.serviceGroups.map((group, index) => normalizeServiceGroup(group, index));
  document2.services = document2.services.map((service, index) => normalizeService(document2, service, index));
  document2.terms = document2.terms.map((term, index) => __spreadProps(__spreadValues({}, term), {
    uid: String(term.uid || "").trim() || `term-${index + 1}`,
    name: String(term.name || `\u672F\u8BED${index + 1}`).trim()
  }));
  document2.dataDictionaries = document2.dataDictionaries.map((dictionary, index) => {
    const dictionaryUid = identityOf(dictionary) || `dictionary-${index + 1}`;
    return __spreadProps(__spreadValues({}, dictionary), {
      uid: dictionaryUid,
      code: String(dictionary.code || "").trim(),
      name: String(dictionary.name || `\u6570\u636E\u5B57\u5178${index + 1}`).trim(),
      desc: String(dictionary.desc || "").trim(),
      entries: Array.isArray(dictionary.entries) ? dictionary.entries.map((entry, entryIndex) => __spreadProps(__spreadValues({}, entry), {
        uid: identityOf(entry) || `${dictionaryUid}-entry-${entryIndex + 1}`,
        code: String(entry.code || "").trim(),
        name: String(entry.name || `\u5B57\u5178\u9879${entryIndex + 1}`).trim(),
        desc: String(entry.desc || "").trim()
      })) : []
    });
  });
  document2.rules = document2.rules.map((rule, index) => __spreadProps(__spreadValues({}, rule), {
    uid: String(rule.uid || "").trim() || `rule-${index + 1}`,
    name: String(rule.name || `\u89C4\u5219${index + 1}`).trim()
  }));
  document2.stageFlowRefs = normalizeStageFlowRefs(document2);
  return document2;
}
function normalizeServiceGroup(group, index) {
  return __spreadProps(__spreadValues({}, group), {
    uid: identityOf(group) || `service-group-${index + 1}`,
    name: String(group.name || `\u670D\u52A1${index + 1}`).trim(),
    desc: String(group.desc || "").trim()
  });
}
function migrateLegacyNodeTaskOrchestration(document2) {
  const serviceGroups = document2.serviceGroups;
  const services = document2.services;
  const serviceByNode = /* @__PURE__ */ new Map();
  for (const service of services) {
    for (const nodeRef of service.nodeRefs || []) {
      if (nodeRef)
        serviceByNode.set(String(nodeRef), service);
    }
  }
  for (const process2 of document2.processes) {
    const processUid = identityOf(process2);
    const processName = String(process2.name || processUid || "\u6D41\u7A0B").trim();
    let groupUid = `service-group-${safeIdentitySegment(processUid || processName)}`;
    for (const node of process2.nodes || []) {
      const nodeUid = identityOf(node);
      const taskDefinitionUids = legacyNodeTaskDefinitionUids(node);
      if (!nodeUid || !taskDefinitionUids.length)
        continue;
      node.serviceUids ||= [];
      if (serviceByNode.has(nodeUid)) {
        const existing = serviceByNode.get(nodeUid);
        if (!node.serviceUids.includes(existing.uid))
          node.serviceUids.push(existing.uid);
        continue;
      }
      if (!serviceGroups.some((group) => identityOf(group) === groupUid)) {
        serviceGroups.push({
          uid: groupUid,
          name: `${processName}\u5E94\u7528\u670D\u52A1`,
          desc: "\u7531\u6D41\u7A0B\u8282\u70B9\u4EFB\u52A1\u7F16\u6392\u8FC1\u79FB\u751F\u6210"
        });
      }
      const serviceUid = uniqueServiceUid(services, `service-${safeIdentitySegment(nodeUid)}`);
      const nodeName = String(node.name || nodeUid).trim();
      const service = {
        uid: serviceUid,
        name: `${nodeName}\u5E94\u7528\u63A5\u53E3`,
        serviceGroupUid: groupUid,
        method: "POST",
        path: "",
        desc: "\u7531\u6D41\u7A0B\u8282\u70B9\u4EFB\u52A1\u7F16\u6392\u8FC1\u79FB\u751F\u6210",
        taskDefinitionUids,
        nodeRefs: [nodeUid],
        requestParams: [],
        responseParams: [],
        orchestration: {
          variables: [],
          steps: taskDefinitionUids.map((taskDefinitionUid, index) => legacyTaskStep(document2, serviceUid, taskDefinitionUid, index)),
          returnMapping: []
        }
      };
      services.push(service);
      serviceByNode.set(nodeUid, service);
      if (!node.serviceUids.includes(serviceUid))
        node.serviceUids.push(serviceUid);
    }
  }
}
function legacyNodeTaskDefinitionUids(node) {
  const nodeLike = node;
  return uniqueStrings([
    ...node.taskDefinitionUids || [],
    ...(nodeLike.tasks || []).map((task) => typeof task === "string" ? task : task.taskDefinitionUid || task.taskDefUid || task.uid || task.id || ""),
    ...(nodeLike.taskDefinitions || []).map((task) => typeof task === "string" ? task : task.uid || task.id || ""),
    ...(nodeLike.orchestrationTasks || []).map((task) => typeof task === "string" ? task : task.taskDefinitionUid || task.taskDefinitionId || task.taskDefUid || task.uid || task.id || "")
  ]);
}
function safeIdentitySegment(value) {
  return String(value || "").trim().replace(/[^a-zA-Z0-9_-]+/g, "-").replace(/^-+|-+$/g, "") || "item";
}
function uniqueServiceUid(services, baseUid) {
  const used = new Set(services.map((service) => identityOf(service)));
  if (!used.has(baseUid))
    return baseUid;
  let index = 2;
  while (used.has(`${baseUid}-${index}`))
    index += 1;
  return `${baseUid}-${index}`;
}
function normalizeComponent(component, index) {
  const legacyEntityUids = component.entityIds || component.entity_ids || [];
  const legacyTaskUids = component.taskDefinitionIds || component.task_definition_ids || [];
  return __spreadProps(__spreadValues({}, component), {
    uid: identityOf(component) || `component-${index + 1}`,
    name: String(component.name || `\u4E1A\u52A1\u7EC4\u4EF6${index + 1}`).trim(),
    kind: component.kind === "common" || component.kind === "generic" ? component.kind : "core",
    entityUids: uniqueStrings(component.entityUids || legacyEntityUids),
    taskDefinitionUids: uniqueStrings(component.taskDefinitionUids || legacyTaskUids),
    stageUids: uniqueStrings(component.stageUids || [])
  });
}
function normalizeConstruct(construct, index) {
  return __spreadProps(__spreadValues({}, construct), {
    uid: identityOf(construct) || `construct-${index + 1}`,
    name: String(construct.name || `\u4E1A\u52A1\u6784\u4EF6${index + 1}`).trim(),
    businessComponentUid: String(construct.businessComponentUid || "").trim()
  });
}
function normalizeTaskDefinition(taskDefinition, index) {
  return __spreadProps(__spreadValues({}, taskDefinition), {
    uid: identityOf(taskDefinition) || `task-definition-${index + 1}`,
    name: String(taskDefinition.name || `\u4EFB\u52A1\u5B9A\u4E49${index + 1}`).trim(),
    parameters: normalizeTaskParameters(taskDefinition.parameters)
  });
}
function normalizeTaskParameters(parameters) {
  if (Array.isArray(parameters)) {
    return { inputs: normalizeParameters(parameters), outputs: [] };
  }
  const bag = parameters && typeof parameters === "object" ? parameters : {};
  return {
    inputs: normalizeParameters(asArray(bag.inputs)),
    outputs: normalizeParameters(asArray(bag.outputs))
  };
}
function normalizeParameters(parameters) {
  return asArray(parameters).filter((parameter) => Boolean(parameter && typeof parameter === "object")).map((parameter) => {
    const children = normalizeParameters(asArray(parameter.children));
    const code = String(parameter.code ?? parameter.description ?? "").trim();
    const note = parameter.note ?? parameter.example ?? parameter.desc ?? "";
    return __spreadValues(__spreadValues(__spreadProps(__spreadValues(__spreadProps(__spreadValues({}, parameter), {
      name: String(parameter.name || "").trim(),
      type: String(parameter.type || "String").trim(),
      required: Boolean(parameter.required)
    }), code ? { code, description: code } : {}), {
      note
    }), parameter.example !== void 0 ? { example: String(parameter.example || "") } : {}), children.length ? { children } : {});
  }).filter((parameter) => parameter.name);
}
function normalizeServiceParameters(parameters) {
  return asArray(parameters).filter((parameter) => Boolean(parameter && typeof parameter === "object")).map((parameter) => {
    const children = normalizeServiceParameters(asArray(parameter.children));
    return __spreadValues(__spreadProps(__spreadValues({}, parameter), {
      name: String(parameter.name || "").trim(),
      type: String(parameter.type || "String").trim(),
      required: Boolean(parameter.required),
      note: parameter.note ?? parameter.desc ?? ""
    }), children.length ? { children } : {});
  }).filter((parameter) => parameter.name);
}
function rebuildServiceNodeRefs(document2) {
  const servicesById = /* @__PURE__ */ new Map();
  for (const service of document2.services) {
    if (service.uid)
      servicesById.set(service.uid, service);
  }
  if (!servicesById.size)
    return;
  for (const process2 of document2.processes) {
    for (const node of process2.nodes || []) {
      const nodeUid = (node.uid || node.id || "").trim();
      if (!nodeUid)
        continue;
      const sectionServiceUids = /* @__PURE__ */ new Set();
      for (const form of node.forms || []) {
        for (const section of Array.isArray(form.sections) ? form.sections : []) {
          for (const id of [
            ...Array.isArray(section["serviceUids"]) ? section["serviceUids"] : [],
            ...Array.isArray(section["serviceIds"]) ? section["serviceIds"] : [],
            section["serviceUid"],
            section["serviceId"]
          ]) {
            const tid = String(id || "").trim();
            if (tid)
              sectionServiceUids.add(tid);
          }
        }
        if (form.serviceUid)
          sectionServiceUids.add(String(form.serviceUid).trim());
        if (form.serviceId)
          sectionServiceUids.add(String(form.serviceId).trim());
      }
      for (const suid of sectionServiceUids) {
        const service = servicesById.get(suid);
        if (service) {
          service.nodeRefs = service.nodeRefs || [];
          if (!service.nodeRefs.includes(nodeUid))
            service.nodeRefs.push(nodeUid);
        }
      }
    }
  }
}
function normalizeService(document2, service, index) {
  const uid = identityOf(service) || `service-${index + 1}`;
  const legacyInputs = asArray(service.inputs);
  const legacyOutputs = asArray(service.outputs);
  const taskDefinitionUids = uniqueStrings(asArray(service.taskDefinitionUids).concat(legacyStepTaskUids(service)));
  const orchestration = normalizeServiceOrchestration(document2, uid, service);
  const normalizedTaskDefinitionUids = taskDefinitionUids.length ? taskDefinitionUids : uniqueStrings(orchestration.steps.map((step) => step.taskDefinitionUid));
  const normalized = __spreadProps(__spreadValues({}, service), {
    uid,
    name: String(service.name || `\u5E94\u7528\u670D\u52A1${index + 1}`).trim(),
    method: String(service.method || "POST").trim() || "POST",
    serviceGroupUid: String(service.serviceGroupUid || "").trim(),
    path: String(service.path || "").trim(),
    desc: String(service.desc || "").trim(),
    taskDefinitionUids: normalizedTaskDefinitionUids,
    nodeRefs: uniqueStrings(asArray(service.nodeRefs)),
    requestParams: normalizeServiceParameters(asArray(service.requestParams).length ? asArray(service.requestParams) : legacyInputs),
    responseParams: normalizeServiceParameters(asArray(service.responseParams).length ? asArray(service.responseParams) : legacyOutputs),
    orchestration
  });
  for (const field of ["actor", "kind", "responseKind", "rawRequest", "rawResponse"]) {
    const value = String(service[field] || "").trim();
    if (value)
      normalized[field] = value;
  }
  return normalized;
}
function normalizeServiceOrchestration(document2, serviceUid, service) {
  const existing = service.orchestration && typeof service.orchestration === "object" ? service.orchestration : void 0;
  const legacySteps = legacyStepTaskUids(service).map((taskDefinitionUid, index) => legacyTaskStep(document2, serviceUid, taskDefinitionUid, index));
  const existingSteps = asArray(existing?.steps).filter((step) => Boolean(step && typeof step === "object"));
  const steps = existingSteps.length ? existingSteps.map((step, index) => normalizeOrchestrationStep(document2, serviceUid, step, index)) : legacySteps;
  return {
    variables: asArray(existing?.variables),
    steps,
    returnMapping: asArray(existing?.returnMapping)
  };
}
function normalizeOrchestrationStep(document2, serviceUid, step, index) {
  const taskDefinitionUid = String(step.taskDefinitionUid || "").trim();
  const task = document2.taskDefinitions.find((candidate) => candidate.uid === taskDefinitionUid || candidate.id === taskDefinitionUid);
  const normalized = {
    uid: String(step.uid || "").trim() || `step-${serviceUid}-${index + 1}-${taskDefinitionUid || "task"}`,
    name: String(step.name || task?.name || `\u6B65\u9AA4${index + 1}`).trim(),
    stepAlias: String(step.stepAlias || `step${index + 1}`).trim(),
    taskDefinitionUid,
    inputMapping: asArray(step.inputMapping),
    outputMapping: asArray(step.outputMapping)
  };
  const parentUid = String(step.parentUid || "").trim();
  if (parentUid)
    normalized.parentUid = parentUid;
  if (step.slot === "then" || step.slot === "else" || step.slot === "body")
    normalized.slot = step.slot;
  if (Number.isFinite(Number(step.order)))
    normalized.order = Number(step.order);
  return normalized;
}
function legacyStepTaskUids(service) {
  const legacySteps = asArray(service.steps);
  return uniqueStrings([
    ...asArray(service.taskDefinitionUids),
    ...legacySteps.map((step) => step && typeof step === "object" ? step.taskDefinitionUid || step.taskDefUid || "" : "")
  ]);
}
function legacyTaskStep(document2, serviceUid, taskDefinitionUid, index) {
  const task = document2.taskDefinitions.find((candidate) => candidate.uid === taskDefinitionUid || candidate.id === taskDefinitionUid);
  return {
    uid: `step-${serviceUid}-${index + 1}-${taskDefinitionUid}`,
    name: task?.name || `\u6B65\u9AA4${index + 1}`,
    stepAlias: `step${index + 1}`,
    taskDefinitionUid,
    inputMapping: [],
    outputMapping: [],
    order: index + 1
  };
}
function normalizeStageFlowRefs(document2) {
  const refs = [];
  const seen = /* @__PURE__ */ new Set();
  const pushRef = (stageIdentity, processIdentity, order, uid = "", pos) => {
    const stage = findStageByIdentity(document2, stageIdentity);
    const process2 = findProcessByIdentity(document2, processIdentity);
    if (!stage || !process2)
      return;
    const key = `${stage.uid}::${process2.uid}`;
    if (seen.has(key))
      return;
    seen.add(key);
    refs.push({
      uid: uid || `stage-flow-ref-${stage.uid}-${process2.uid}`,
      stageUid: stage.uid,
      processUid: process2.uid,
      order: Math.max(1, Math.round(Number(order || refs.length + 1))),
      pos: pos || { x: 0, y: 0 }
    });
  };
  document2.stageFlowRefs.forEach((ref, index) => {
    const refLike = ref;
    pushRef(refLike.stageUid || refLike.stageId || "", refLike.processUid || refLike.processId || "", refLike.order || index + 1, refLike.uid, refLike.pos);
  });
  document2.processes.forEach((process2) => {
    const stageIdentity = process2.stageUid || process2.stageId || "";
    if (stageIdentity)
      pushRef(stageIdentity, process2.uid, refs.length + 1);
  });
  return refs.sort((left, right) => left.order - right.order);
}
function getStageProcesses(document2, stageId) {
  const stage = findStageByIdentity(document2, stageId);
  if (!stage)
    return [];
  const processByUid = new Map(document2.processes.map((process2) => [process2.uid, process2]));
  return document2.stageFlowRefs.filter((ref) => ref.stageUid === stage.uid).sort((left, right) => left.order - right.order).map((ref) => processByUid.get(ref.processUid)).filter((process2) => Boolean(process2));
}
function getComponentSupportedStages(document2, component) {
  const directStageUids = new Set(component.stageUids || []);
  const taskUids = new Set(component.taskDefinitionUids || []);
  const entityUids = new Set(component.entityUids || []);
  const processUids = /* @__PURE__ */ new Set();
  for (const process2 of document2.processes) {
    if (process2.nodes.some((node) => nodeReferencesComponent(node, taskUids, entityUids))) {
      processUids.add(process2.uid);
    }
  }
  for (const ref of document2.stageFlowRefs) {
    if (processUids.has(ref.processUid)) {
      directStageUids.add(ref.stageUid);
    }
  }
  return document2.stages.filter((stage) => directStageUids.has(stage.uid));
}
function nodeReferencesComponent(node, taskUids, entityUids) {
  if (taskUids.has(node.uid) || node.id && taskUids.has(node.id))
    return true;
  return [...node.entity_ops || [], ...node.forms || []].some((item) => {
    const entityRef = String(item.entity_uid || item.entity_id || "").trim();
    return entityUids.has(entityRef);
  });
}
function uniqueStrings(values) {
  const result = [];
  const seen = /* @__PURE__ */ new Set();
  for (const value of values || []) {
    const text = String(value || "").trim();
    if (!text || seen.has(text))
      continue;
    seen.add(text);
    result.push(text);
  }
  return result;
}

// src/app/core/document/sample-document.ts
var SAMPLE_DOCUMENT = {
  meta: {
    domain: "\u4E1A\u52A1\u8BED\u8A00\u5EFA\u6A21",
    version: "v3",
    owner: "\u4E1A\u52A1\u8D1F\u8D23\u4EBA"
  },
  roles: [
    { uid: "role-owner", id: "R1", name: "\u603B\u8D1F\u8D23\u4EBA", group: "\u6CBB\u7406\u89D2\u8272" },
    { uid: "role-pm", id: "R2", name: "\u4EA7\u54C1\u7ECF\u7406", group: "\u4E1A\u52A1\u89D2\u8272" },
    { uid: "role-tech", id: "R3", name: "\u6280\u672F\u7ECF\u7406", group: "\u6280\u672F\u89D2\u8272" },
    { uid: "role-fe", id: "R4", name: "\u524D\u7AEF\u7814\u53D1", group: "\u6280\u672F\u89D2\u8272" },
    { uid: "role-be", id: "R5", name: "\u540E\u7AEF\u7814\u53D1", group: "\u6280\u672F\u89D2\u8272" }
  ],
  stages: [
    { uid: "stage-entry", id: "S1", name: "\u5165\u5E93\u767B\u8BB0", subDomain: "\u4ED3\u5355\u7BA1\u7406" },
    { uid: "stage-supervision", id: "S2", name: "\u5728\u5E93\u76D1\u7BA1", subDomain: "\u98CE\u9669\u76D1\u7BA1" },
    { uid: "stage-delivery", id: "S3", name: "\u51FA\u5E93\u4EA4\u5272", subDomain: "\u4EA4\u5272\u529E\u7406" }
  ],
  stageFlowRefs: [
    { uid: "ref-entry", stageUid: "stage-entry", processUid: "process-entry", order: 1 },
    { uid: "ref-supervision", stageUid: "stage-supervision", processUid: "process-supervision", order: 1 },
    { uid: "ref-delivery", stageUid: "stage-delivery", processUid: "process-delivery", order: 1 }
  ],
  processes: [
    {
      uid: "process-entry",
      id: "P1",
      name: "\u4ED3\u5355\u5165\u5E93\u767B\u8BB0",
      subDomain: "\u4ED3\u5355\u7BA1\u7406",
      nodes: [
        { uid: "node-entry-apply", id: "T1", name: "\u63D0\u4EA4\u5165\u5E93\u7533\u8BF7", role_uids: ["role-pm"], entity_ops: [{ entity_uid: "entity-receipt" }] },
        { uid: "node-entry-review", id: "T2", name: "\u5BA1\u6838\u5165\u5E93\u8D44\u6599", role_uids: ["role-be"], entity_ops: [{ entity_uid: "entity-receipt" }] }
      ]
    },
    {
      uid: "process-supervision",
      id: "P2",
      name: "\u5728\u5E93\u98CE\u9669\u76D1\u7BA1",
      subDomain: "\u98CE\u9669\u76D1\u7BA1",
      nodes: [
        { uid: "node-risk-check", id: "T3", name: "\u68C0\u67E5\u5E93\u5B58\u72B6\u6001", role_uids: ["role-tech"], entity_ops: [{ entity_uid: "entity-inventory" }] }
      ]
    },
    {
      uid: "process-delivery",
      id: "P3",
      name: "\u51FA\u5E93\u4EA4\u5272\u529E\u7406",
      subDomain: "\u4EA4\u5272\u529E\u7406",
      nodes: [
        { uid: "node-delivery-confirm", id: "T4", name: "\u786E\u8BA4\u4EA4\u5272\u6307\u4EE4", role_uids: ["role-fe"], entity_ops: [{ entity_uid: "entity-delivery" }] }
      ]
    }
  ],
  entities: [
    { uid: "entity-receipt", id: "E1", name: "\u4ED3\u5355", fields: [{ name: "\u72B6\u6001", type: "enum", state_values: "\u8349\u7A3F/\u5DF2\u5BA1\u6838/\u5DF2\u5165\u5E93" }] },
    { uid: "entity-inventory", id: "E2", name: "\u5E93\u5B58\u8BB0\u5F55", fields: [{ name: "\u76D1\u7BA1\u72B6\u6001", type: "enum", state_values: "\u6B63\u5E38/\u9884\u8B66/\u51BB\u7ED3" }] },
    { uid: "entity-delivery", id: "E3", name: "\u4EA4\u5272\u6307\u4EE4", fields: [{ name: "\u529E\u7406\u72B6\u6001", type: "enum", state_values: "\u5F85\u786E\u8BA4/\u5DF2\u786E\u8BA4/\u5DF2\u5B8C\u6210" }] }
  ],
  businessComponents: [
    { uid: "component-receipt", id: "C1", name: "\u4ED3\u5355\u7BA1\u7406\u7EC4\u4EF6", kind: "core", entityUids: ["entity-receipt"], taskDefinitionUids: ["task-entry"] },
    { uid: "component-risk", id: "C2", name: "\u98CE\u9669\u76D1\u7BA1\u7EC4\u4EF6", kind: "core", entityUids: ["entity-inventory"], taskDefinitionUids: ["task-risk"] },
    { uid: "component-platform", id: "C3", name: "\u7528\u6237\u4E0E\u6743\u9650\u7EC4\u4EF6", kind: "common", entityUids: [], taskDefinitionUids: [] }
  ],
  businessConstructs: [],
  taskDefinitions: [
    { uid: "task-entry", id: "TD1", name: "\u5165\u5E93\u767B\u8BB0\u4EFB\u52A1", target: "receipt.register" },
    { uid: "task-risk", id: "TD2", name: "\u98CE\u9669\u68C0\u67E5\u4EFB\u52A1", target: "risk.check" }
  ],
  serviceGroups: [],
  services: [],
  terms: [
    { uid: "term-receipt", name: "\u4ED3\u5355", desc: "\u7528\u4E8E\u8868\u8FBE\u8D27\u7269\u5165\u5E93\u3001\u76D1\u7BA1\u548C\u4EA4\u5272\u72B6\u6001\u7684\u6838\u5FC3\u4E1A\u52A1\u51ED\u8BC1\u3002" }
  ],
  dataDictionaries: [
    {
      uid: "dictionary-status",
      code: "status",
      name: "\u72B6\u6001",
      desc: "\u901A\u7528\u72B6\u6001\u7801\u8868",
      entries: [
        { uid: "dictionary-status-enabled", code: "enabled", name: "\u542F\u7528" },
        { uid: "dictionary-status-disabled", code: "disabled", name: "\u505C\u7528" }
      ]
    }
  ],
  rules: [
    { uid: "rule-entry", name: "\u5165\u5E93\u8D44\u6599\u5B8C\u6574\u6027\u89C4\u5219", desc: "\u63D0\u4EA4\u5165\u5E93\u7533\u8BF7\u524D\u5FC5\u987B\u5177\u5907\u5BA2\u6237\u3001\u4ED3\u5E93\u3001\u54C1\u79CD\u548C\u6570\u91CF\u4FE1\u606F\u3002" }
  ]
};

// src/app/core/runtime/angular-runtime.ts
var ANGULAR_NAV_HISTORY_LIMIT = 30;
var ANGULAR_UNDO_HISTORY_LIMIT = 50;
var ANGULAR_UTILITY_WORKBENCH_IDS = /* @__PURE__ */ new Set(["manual", "feedback"]);
var ANGULAR_NAVIGATION_UI_KEY_PATTERN = /^(process|component|application|orchestration|entity|taskDefinition)[A-Za-z0-9]*(Tab|View|Id|Uid)$/;
var ANGULAR_NAVIGATION_UI_KEYS = /* @__PURE__ */ new Set([
  "procId",
  "taskId",
  "entityId",
  "procView",
  "stageId",
  "processWorkbenchView",
  "processWorkbenchTab",
  "panoramaWorkbenchTab",
  "componentTab",
  "componentWorkbenchTab",
  "componentWorkbenchReturnTab",
  "componentWorkbenchConstructId",
  "taskDefinitionId",
  "applicationWorkbenchTab",
  "applicationServiceGroupUid",
  "applicationServiceUid",
  "applicationServiceId",
  "applicationOrchestrationServiceGroupUid",
  "applicationOrchestrationServiceUid",
  "applicationOrchestrationStepUid"
]);
var documentHistory = {
  undo: [],
  redo: [],
  baseline: null,
  baselineNav: null
};
var runtimeState = {
  doc: normalizeDocument(SAMPLE_DOCUMENT),
  ui: {
    mainTab: "panoramaWorkbench",
    tab: "panorama",
    processWorkbenchView: "valueDomain",
    componentTab: "businessComponents",
    sbCollapse: {},
    businessDomainFilter: "all"
  },
  currentFile: "",
  modified: false,
  readOnly: false,
  runtime: {
    supportsCollab: true,
    agentUrl: "http://127.0.0.1:8088",
    // 版本由后端读取根目录 version 文件后注入；加载前保持空值，避免显示过期版本。
    appVersion: ""
  },
  collab: {
    seq: 0,
    acceptedSeq: 0,
    syncing: false,
    lastError: "",
    connected: false,
    users: [],
    pendingSnapshot: false,
    hasRemoteUpdate: false,
    serverDocumentHash: "",
    lastSyncedAt: "",
    lastActivity: null,
    historyVersionContext: void 0
  },
  recovery: {}
};
function getAngularRuntimeState() {
  return runtimeState;
}
function replaceRuntimeDocument(document2, fileName = "") {
  runtimeState.doc = normalizeDocument(document2 || {});
  runtimeState.currentFile = fileName;
  runtimeState.modified = false;
  resetAngularRuntimeUndoBaseline();
  emitRuntimeRefresh();
}
function markAngularRuntimeModified() {
  recordAngularRuntimeUndoBoundary();
  runtimeState.modified = true;
  if (runtimeState.currentFile && !runtimeState.readOnly) {
    runtimeState.collab.pendingSnapshot = true;
    window.dispatchEvent(new CustomEvent("blm-runtime-local-change"));
  }
  emitRuntimeRefresh();
}
function canUndoAngularRuntimeDocument() {
  return documentHistory.undo.length > 0;
}
function canRedoAngularRuntimeDocument() {
  return documentHistory.redo.length > 0;
}
function undoAngularRuntimeDocument() {
  const previous = documentHistory.undo.pop();
  if (!previous)
    return false;
  documentHistory.redo.push(createAngularDocumentHistoryEntry(runtimeState.doc));
  runtimeState.doc = normalizeDocument(previous[0]);
  restoreAngularNavigationSnapshot(previous[1]);
  documentHistory.baseline = cloneRuntimeDocument(runtimeState.doc);
  documentHistory.baselineNav = captureAngularNavigationSnapshot();
  markRuntimeDocumentChangedByHistory();
  return true;
}
function redoAngularRuntimeDocument() {
  const next = documentHistory.redo.pop();
  if (!next)
    return false;
  documentHistory.undo.push(createAngularDocumentHistoryEntry(runtimeState.doc));
  trimAngularRuntimeHistory(documentHistory.undo);
  runtimeState.doc = normalizeDocument(next[0]);
  restoreAngularNavigationSnapshot(next[1]);
  documentHistory.baseline = cloneRuntimeDocument(runtimeState.doc);
  documentHistory.baselineNav = captureAngularNavigationSnapshot();
  markRuntimeDocumentChangedByHistory();
  return true;
}
function resetAngularRuntimeUndoBaseline() {
  documentHistory.baseline = cloneRuntimeDocument(runtimeState.doc);
  documentHistory.baselineNav = captureAngularNavigationSnapshot();
}
function recordAngularRuntimeUndoBoundary() {
  if (!documentHistory.baseline)
    resetAngularRuntimeUndoBaseline();
  if (areRuntimeDocumentsEqual(documentHistory.baseline, runtimeState.doc))
    return;
  documentHistory.undo.push(createAngularDocumentHistoryEntry(documentHistory.baseline, documentHistory.baselineNav || captureAngularNavigationSnapshot()));
  trimAngularRuntimeHistory(documentHistory.undo);
  documentHistory.redo = [];
  resetAngularRuntimeUndoBaseline();
}
function markRuntimeDocumentChangedByHistory() {
  runtimeState.modified = true;
  if (runtimeState.currentFile && !runtimeState.readOnly) {
    runtimeState.collab.pendingSnapshot = true;
    window.dispatchEvent(new CustomEvent("blm-runtime-local-change"));
  }
  emitRuntimeRefresh();
}
function trimAngularRuntimeHistory(history) {
  if (history.length > ANGULAR_UNDO_HISTORY_LIMIT) {
    history.splice(0, history.length - ANGULAR_UNDO_HISTORY_LIMIT);
  }
}
function createAngularDocumentHistoryEntry(document2, navigation = captureAngularNavigationSnapshot()) {
  return [cloneRuntimeDocument(document2), navigation];
}
function cloneRuntimeDocument(document2) {
  return JSON.parse(JSON.stringify(document2 || {}));
}
function areRuntimeDocumentsEqual(left, right) {
  return JSON.stringify(left || {}) === JSON.stringify(right || {});
}
function switchAngularMainTab(tabId) {
  const nextTab = normalizeMainWorkbenchId(tabId);
  if (runtimeState.ui["mainTab"] === nextTab)
    return;
  recordAngularNavigationSnapshot();
  runtimeState.ui["mainTab"] = nextTab;
  emitRuntimeRefresh();
}
function normalizeMainWorkbenchId(tabId) {
  const aliases = {
    businessArch: "panoramaWorkbench",
    bizDomain: "processWorkbench",
    bizComponent: "constructWorkbench",
    appArch: "orchestrationWorkbench",
    domain: "panoramaWorkbench",
    data: "constructWorkbench",
    panorama: "panoramaWorkbench",
    process: "processWorkbench",
    component: "constructWorkbench",
    application: "applicationWorkbench",
    appService: "applicationWorkbench",
    orchestration: "orchestrationWorkbench",
    knowledge: "knowledge",
    role: "role",
    manual: "manual",
    feedback: "feedback"
  };
  const normalized = aliases[String(tabId || "")] || String(tabId || "panoramaWorkbench");
  return ["panoramaWorkbench", "processWorkbench", "constructWorkbench", "applicationWorkbench", "orchestrationWorkbench", "entity", "knowledge", "role", "preview", "manual", "feedback"].includes(normalized) ? normalized : "panoramaWorkbench";
}
function isAngularUtilityWorkbench(tabId) {
  return ANGULAR_UTILITY_WORKBENCH_IDS.has(normalizeMainWorkbenchId(tabId));
}
function navigateAngularWorkbench(tab, options = {}) {
  const before = captureAngularNavigationSnapshot();
  if (tab === "process") {
    runtimeState.ui["mainTab"] = "processWorkbench";
    runtimeState.ui["tab"] = "process";
    if (options["procId"])
      runtimeState.ui["procId"] = String(options["procId"]);
    if (options["taskId"]) {
      runtimeState.ui["taskId"] = String(options["taskId"]);
      runtimeState.ui["procView"] = "node";
      runtimeState.ui["processWorkbenchView"] = "node";
    } else {
      runtimeState.ui["taskId"] = null;
      runtimeState.ui["procView"] = "flow";
      runtimeState.ui["processWorkbenchView"] = "flow";
    }
  } else if (tab === "data") {
    runtimeState.ui["mainTab"] = "constructWorkbench";
    if (options["entityId"])
      runtimeState.ui["entityId"] = String(options["entityId"]);
  }
  if (!areAngularNavigationSnapshotsEqual(before, captureAngularNavigationSnapshot())) {
    pushAngularNavigationSnapshot(before);
  }
  emitRuntimeRefresh();
}
function recordAngularNavigationBoundary() {
  recordAngularNavigationSnapshot();
}
function emitRuntimeRefresh() {
  window.dispatchEvent(new CustomEvent("blm-angular-runtime-refresh"));
  window.dispatchEvent(new CustomEvent("blm-shell-tabbar-refresh"));
  window.dispatchEvent(new CustomEvent("blm-sidebar-directory-refresh"));
  window.dispatchEvent(new CustomEvent("blm-workbench-refresh"));
}
function confirmRuntimeAction(message, options = {}) {
  let handled = false;
  return new Promise((resolve) => {
    window.dispatchEvent(new CustomEvent("blm-runtime-confirm", {
      detail: {
        message,
        options,
        resolve,
        markHandled: () => {
          handled = true;
        }
      }
    }));
    if (!handled)
      resolve(false);
  });
}
function canGoBackAngularNavigation() {
  return hasAngularInternalBackNavigation() || angularNavigationHistory().length > 0;
}
function getAngularBackNavigationTitle() {
  if (hasAngularInternalBackNavigation())
    return "\u8FD4\u56DE\u5230\u4E1A\u52A1\u7EC4\u4EF6";
  const previous = angularNavigationHistory()[angularNavigationHistory().length - 1];
  if (!previous)
    return "\u5F53\u524D\u6CA1\u6709\u53EF\u8FD4\u56DE\u7684\u4F4D\u7F6E";
  const labels = {
    panoramaWorkbench: "\u8FD4\u56DE\u5230\u5168\u666F\u5DE5\u4F5C\u53F0",
    processWorkbench: previous.ui?.["taskId"] ? "\u8FD4\u56DE\u5230\u6D41\u7A0B\u8282\u70B9" : "\u8FD4\u56DE\u5230\u6D41\u7A0B\u5DE5\u4F5C\u53F0",
    constructWorkbench: previous.ui?.["entityId"] ? "\u8FD4\u56DE\u5230\u5B9E\u4F53" : "\u8FD4\u56DE\u5230\u6784\u4EF6\u5DE5\u4F5C\u53F0",
    applicationWorkbench: "\u8FD4\u56DE\u5230\u5E94\u7528\u5DE5\u4F5C\u53F0",
    orchestrationWorkbench: "\u8FD4\u56DE\u5230\u7F16\u6392\u5DE5\u4F5C\u53F0",
    preview: "\u8FD4\u56DE\u5230\u9884\u89C8"
  };
  return labels[previous.mainTab] || "\u8FD4\u56DE\u5230\u4E0A\u4E00\u4F4D\u7F6E";
}
function goBackAngularNavigation() {
  const internalTarget = consumeAngularInternalBackNavigation();
  if (internalTarget)
    return internalTarget;
  const history = angularNavigationHistory();
  let previous = history.pop();
  const current = captureAngularNavigationSnapshot();
  while (previous && areAngularNavigationSnapshotsEqual(previous, current)) {
    previous = history.pop();
  }
  if (!previous)
    return null;
  restoreAngularNavigationSnapshot(previous);
  emitRuntimeRefresh();
  return runtimeState.ui["mainTab"];
}
function goBackAngularUtilityWorkbench(fallbackTab = "panoramaWorkbench") {
  const fallback = isAngularUtilityWorkbench(fallbackTab) ? "panoramaWorkbench" : normalizeMainWorkbenchId(fallbackTab);
  let target = goBackAngularNavigation();
  while (target && isAngularUtilityWorkbench(target) && angularNavigationHistory().length > 0) {
    target = goBackAngularNavigation();
  }
  if (target && !isAngularUtilityWorkbench(target)) {
    runtimeState.ui["utilityReturnMainTab"] = "";
    return target;
  }
  runtimeState.ui["mainTab"] = fallback;
  runtimeState.ui["utilityReturnMainTab"] = "";
  emitRuntimeRefresh();
  return runtimeState.ui["mainTab"];
}
function hasAngularInternalBackNavigation() {
  return runtimeState.ui["mainTab"] === "constructWorkbench" && runtimeState.ui["componentWorkbenchTab"] === "entity" && !!runtimeState.ui["componentWorkbenchReturnTab"];
}
function consumeAngularInternalBackNavigation() {
  if (!hasAngularInternalBackNavigation())
    return null;
  runtimeState.ui["componentWorkbenchTab"] = String(runtimeState.ui["componentWorkbenchReturnTab"] || "businessComponent");
  runtimeState.ui["componentWorkbenchReturnTab"] = "";
  emitRuntimeRefresh();
  return runtimeState.ui["mainTab"];
}
function recordAngularNavigationSnapshot() {
  pushAngularNavigationSnapshot(captureAngularNavigationSnapshot());
}
function captureAngularNavigationSnapshot() {
  return {
    mainTab: normalizeMainWorkbenchId(runtimeState.ui["mainTab"]),
    ui: captureAngularNavigationUi()
  };
}
function captureAngularNavigationUi() {
  const captured = {};
  for (const key of Object.keys(runtimeState.ui).sort()) {
    if (!isAngularNavigationUiKey(key))
      continue;
    const value = runtimeState.ui[key];
    if (value == null || ["string", "number", "boolean"].includes(typeof value)) {
      captured[key] = value == null ? null : value;
    }
  }
  return captured;
}
function restoreAngularNavigationSnapshot(snapshot) {
  runtimeState.ui["mainTab"] = normalizeMainWorkbenchId(snapshot.mainTab);
  for (const key of Object.keys(runtimeState.ui)) {
    if (isAngularNavigationUiKey(key))
      delete runtimeState.ui[key];
  }
  if (snapshot.ui) {
    for (const [key, value] of Object.entries(snapshot.ui)) {
      if (isAngularNavigationUiKey(key))
        runtimeState.ui[key] = value;
    }
    return;
  }
}
function isAngularNavigationUiKey(key) {
  if (key === "mainTab" || key === "navHistory")
    return false;
  return ANGULAR_NAVIGATION_UI_KEYS.has(key) || ANGULAR_NAVIGATION_UI_KEY_PATTERN.test(key);
}
function pushAngularNavigationSnapshot(snapshot) {
  const history = angularNavigationHistory();
  const last2 = history[history.length - 1];
  if (last2 && areAngularNavigationSnapshotsEqual(last2, snapshot))
    return;
  history.push(__spreadValues({}, snapshot));
  if (history.length > ANGULAR_NAV_HISTORY_LIMIT) {
    history.splice(0, history.length - ANGULAR_NAV_HISTORY_LIMIT);
  }
}
function angularNavigationHistory() {
  const ui = runtimeState.ui;
  if (!Array.isArray(ui["navHistory"]))
    ui["navHistory"] = [];
  return ui["navHistory"];
}
function areAngularNavigationSnapshotsEqual(left, right) {
  return String(left.mainTab || "") === String(right.mainTab || "") && JSON.stringify(left.ui || {}) === JSON.stringify(right.ui || {});
}

// src/app/core/api/api.service.ts
var ApiService = class _ApiService {
  // 模块意图：集中承接浏览器到后端的 HTTP 调用，替代旧 api.js 的散落全局函数。
  // 关键流程：主线只覆盖文件列表、新建、打开、保存和协作快照；工具类接口后续按垂直功能补齐。
  // 边界细节：接口路径保持与后端一致，不改变数据模型，也不改变服务端合并逻辑。
  async runtime() {
    return this.getJson("/api/runtime");
  }
  async fileSummaries() {
    return this.getJson("/api/files/meta", []);
  }
  async trash() {
    return this.getJson("/api/trash", []);
  }
  async restoreTrash(entryId) {
    return this.postJson("/api/trash/restore", { entry_id: entryId });
  }
  async deleteTrash(entryIds) {
    return this.postJson("/api/trash/delete", { entry_ids: entryIds });
  }
  async clearTrash() {
    return this.postJson("/api/trash/clear", {});
  }
  async files() {
    return this.getJson("/api/files", []);
  }
  async create(name) {
    return this.postJson("/api/new", { name });
  }
  async copyDocument(sourceName, targetName) {
    return this.postJson("/api/copy", {
      source_name: sourceName,
      target_name: targetName
    });
  }
  async deleteDocument(name) {
    return this.postJson(`/api/delete/${encodeURIComponent(name)}`, {});
  }
  async createVersion(name, document2, message = "") {
    return this.postJson("/api/version/create", { name, document: document2, message });
  }
  async load(name) {
    return this.getJson(`/api/load/${encodeURIComponent(name)}`);
  }
  async save(name, document2, options = {}) {
    return this.postJson(`/api/save/${encodeURIComponent(name)}`, {
      document: document2,
      base_revision: options["baseRevision"],
      base_document: options["baseDocument"],
      rebase: options["rebase"] !== false,
      save_message: options["saveMessage"] || ""
    });
  }
  async collabSnapshot(name, document2, options = {}) {
    return this.postJson("/api/collab/snapshot", {
      name,
      document: document2,
      baseSeq: options["baseSeq"] || 0,
      baseDocumentHash: options["baseDocumentHash"] || "",
      documentHash: options["documentHash"] || "",
      recoveryMode: Boolean(options["recoveryMode"]),
      user: options["user"] || {}
    });
  }
  async history(name, options = {}) {
    const query = this.paginationQuery(options);
    const result = await this.getJson(`/api/history/${encodeURIComponent(name)}${query}`, []);
    return this.normalizePagedRows(result);
  }
  async versions(name, options = {}) {
    const query = this.paginationQuery(options);
    const result = await this.getJson(`/api/versions/${encodeURIComponent(name)}${query}`, []);
    return this.normalizePagedRows(result);
  }
  async loadVersion(name, versionId) {
    return this.postJson("/api/version/load", { name, version_id: versionId });
  }
  async loadHistory(name, snapshotId) {
    return this.postJson("/api/history/load", { name, snapshot_id: snapshotId });
  }
  async restoreHistory(name, snapshotId) {
    return this.postJson("/api/history/restore", { name, snapshot_id: snapshotId });
  }
  async collabSubmits(name, options = {}) {
    const result = await this.postJson("/api/collab/submits/list", __spreadValues({ name }, options));
    return this.normalizePagedRows(result, "submits");
  }
  async loadCollabSubmit(name, submitId) {
    return this.postJson("/api/collab/submits/load", { name, submitId });
  }
  async analyzeMerge(payload) {
    return this.postJson("/api/merge/analyze", payload);
  }
  async applyMerge(payload) {
    return this.postJson("/api/merge/apply", payload);
  }
  async validateDocument(document2) {
    return this.postJson("/api/document/validate", { document: document2 });
  }
  async exportBundle(name) {
    return fetch(`/api/export-bundle/${encodeURIComponent(name)}`);
  }
  async startDocxExport(name) {
    return this.postJson("/api/export-docx/start", { name });
  }
  async exportJob(jobId) {
    return this.getJson(`/api/export-jobs/${encodeURIComponent(jobId)}`);
  }
  async exportRenderDocument(jobId) {
    return this.getJson(`/api/export-jobs/${encodeURIComponent(jobId)}/render-document`);
  }
  async downloadExportJob(jobId) {
    return fetch(`/api/export-jobs/${encodeURIComponent(jobId)}/download`);
  }
  async docs() {
    return this.getJson("/api/docs", []);
  }
  async doc(docId) {
    return this.getJson(`/api/docs/${encodeURIComponent(docId)}`);
  }
  async feedback() {
    return this.getJson("/api/feedback", { items: [] });
  }
  async saveFeedback(payload) {
    return this.postJson("/api/feedback", payload);
  }
  async uploadFeedbackAttachment(payload) {
    return this.postJson("/api/feedback/attachment", payload);
  }
  feedbackAttachmentUrl(uid, attachmentUid) {
    return `/api/feedback/attachment/${encodeURIComponent(uid)}/${encodeURIComponent(attachmentUid)}`;
  }
  async createAgentHandoff(payload) {
    return this.postJson("/api/agent/handoff", payload);
  }
  async getJson(url, fallback) {
    const response = await fetch(url, { cache: "no-store" });
    if (!response.ok) {
      if (fallback !== void 0)
        return fallback;
      throw new Error(`GET ${url} failed: ${response.status}`);
    }
    return response.json();
  }
  async postJson(url, payload) {
    const response = await fetch(url, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload || {})
    });
    const result = await response.json().catch(() => ({}));
    if (!response.ok || result?.error) {
      throw new Error(result?.error || `POST ${url} failed: ${response.status}`);
    }
    return result;
  }
  paginationQuery(options) {
    const params = new URLSearchParams();
    if (options.limit !== void 0)
      params.set("limit", String(options.limit));
    if (options.offset !== void 0)
      params.set("offset", String(options.offset));
    const query = params.toString();
    return query ? `?${query}` : "";
  }
  normalizePagedRows(result, key = "items") {
    const rows = Array.isArray(result) ? result : Array.isArray(result?.[key]) ? result[key] : Array.isArray(result?.items) ? result.items : [];
    return {
      items: rows,
      hasMore: Boolean(result?.hasMore),
      nextOffset: Number(result?.nextOffset ?? rows.length) || rows.length
    };
  }
  static \u0275fac = function ApiService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ApiService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ApiService, factory: _ApiService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ApiService, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/core/export/graph-export-registry.ts
function asArray2(value) {
  return Array.isArray(value) ? value : [];
}
function identityOf2(item, fallback) {
  return String(item?.uid || item?.id || fallback).trim();
}
function displayName(item, fallback) {
  return String(item?.name || item?.title || fallback).trim();
}
function graphSelector(graphId) {
  return `[data-export-graph-id="${graphId.replace(/"/g, '\\"')}"]`;
}
function exportGraphId(kind, value = "") {
  return value ? `${kind}:${value}` : kind;
}
function listExportGraphs(document2) {
  const graphs = [];
  const stages = asArray2(document2?.stages).filter((stage) => !stage?.virtual);
  const processes = asArray2(document2?.processes);
  const entities = asArray2(document2?.entities);
  if (stages.length) {
    const id = exportGraphId("stage-panorama");
    graphs.push({
      id,
      kind: "stage-panorama",
      title: "\u5168\u666F\u89C6\u56FE",
      selector: graphSelector(id),
      params: {}
    });
    stages.forEach((stage, index) => {
      const stageId = identityOf2(stage, `stage-${index + 1}`);
      const id2 = exportGraphId("stage-flow", stageId);
      graphs.push({
        id: id2,
        kind: "stage-flow",
        title: `\u9636\u6BB5\u89C6\u56FE - ${displayName(stage, `\u9636\u6BB5 ${index + 1}`)}`,
        selector: graphSelector(id2),
        params: { stageId }
      });
    });
  }
  processes.forEach((process2, index) => {
    const processId = identityOf2(process2, `process-${index + 1}`);
    const id = exportGraphId("process-flow", processId);
    graphs.push({
      id,
      kind: "process-flow",
      title: `\u6D41\u7A0B\u56FE - ${displayName(process2, `\u6D41\u7A0B ${index + 1}`)}`,
      selector: graphSelector(id),
      params: { processId }
    });
  });
  if (entities.length) {
    const id = exportGraphId("entity-relation");
    graphs.push({
      id,
      kind: "entity-relation",
      title: "\u5B9E\u4F53\u5173\u7CFB\u56FE",
      selector: graphSelector(id),
      params: {}
    });
    entities.forEach((entity, index) => {
      const entityId2 = identityOf2(entity, `entity-${index + 1}`);
      const id2 = exportGraphId("entity-state", entityId2);
      graphs.push({
        id: id2,
        kind: "entity-state",
        title: `\u5B9E\u4F53\u72B6\u6001\u56FE - ${displayName(entity, `\u5B9E\u4F53 ${index + 1}`)}`,
        selector: graphSelector(id2),
        params: { entityId: entityId2 }
      });
    });
  }
  return graphs;
}

// node_modules/@angular/forms/fesm2022/forms.mjs
/**
 * @license Angular v21.2.17
 * (c) 2010-2026 Google LLC. https://angular.dev/
 * License: MIT
 */
var BaseControlValueAccessor = class _BaseControlValueAccessor {
  _renderer;
  _elementRef;
  onChange = (_) => {
  };
  onTouched = () => {
  };
  constructor(_renderer, _elementRef) {
    this._renderer = _renderer;
    this._elementRef = _elementRef;
  }
  setProperty(key, value) {
    this._renderer.setProperty(this._elementRef.nativeElement, key, value);
  }
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  registerOnChange(fn) {
    this.onChange = fn;
  }
  setDisabledState(isDisabled) {
    this.setProperty("disabled", isDisabled);
  }
  static \u0275fac = function BaseControlValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BaseControlValueAccessor)(\u0275\u0275directiveInject(Renderer2), \u0275\u0275directiveInject(ElementRef));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _BaseControlValueAccessor
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseControlValueAccessor, [{
    type: Directive
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }], null);
})();
var BuiltInControlValueAccessor = class _BuiltInControlValueAccessor extends BaseControlValueAccessor {
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275BuiltInControlValueAccessor_BaseFactory;
    return function BuiltInControlValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275BuiltInControlValueAccessor_BaseFactory || (\u0275BuiltInControlValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_BuiltInControlValueAccessor)))(__ngFactoryType__ || _BuiltInControlValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _BuiltInControlValueAccessor,
    features: [\u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BuiltInControlValueAccessor, [{
    type: Directive
  }], null, null);
})();
var NG_VALUE_ACCESSOR = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "NgValueAccessor" : "");
var CHECKBOX_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => CheckboxControlValueAccessor),
  multi: true
};
var CheckboxControlValueAccessor = class _CheckboxControlValueAccessor extends BuiltInControlValueAccessor {
  writeValue(value) {
    this.setProperty("checked", value);
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275CheckboxControlValueAccessor_BaseFactory;
    return function CheckboxControlValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275CheckboxControlValueAccessor_BaseFactory || (\u0275CheckboxControlValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_CheckboxControlValueAccessor)))(__ngFactoryType__ || _CheckboxControlValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _CheckboxControlValueAccessor,
    selectors: [["input", "type", "checkbox", "formControlName", ""], ["input", "type", "checkbox", "formControl", ""], ["input", "type", "checkbox", "ngModel", ""]],
    hostBindings: function CheckboxControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("change", function CheckboxControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.checked);
        })("blur", function CheckboxControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([CHECKBOX_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CheckboxControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=checkbox][formControlName],input[type=checkbox][formControl],input[type=checkbox][ngModel]",
      host: {
        "(change)": "onChange($any($event.target).checked)",
        "(blur)": "onTouched()"
      },
      providers: [CHECKBOX_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var DEFAULT_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => DefaultValueAccessor),
  multi: true
};
function _isAndroid() {
  const userAgent = getDOM() ? getDOM().getUserAgent() : "";
  return /android (\d+)/.test(userAgent.toLowerCase());
}
var COMPOSITION_BUFFER_MODE = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "CompositionEventMode" : "");
var DefaultValueAccessor = class _DefaultValueAccessor extends BaseControlValueAccessor {
  _compositionMode;
  _composing = false;
  constructor(renderer, elementRef, _compositionMode) {
    super(renderer, elementRef);
    this._compositionMode = _compositionMode;
    if (this._compositionMode == null) {
      this._compositionMode = !_isAndroid();
    }
  }
  writeValue(value) {
    const normalizedValue = value == null ? "" : value;
    this.setProperty("value", normalizedValue);
  }
  _handleInput(value) {
    if (!this._compositionMode || this._compositionMode && !this._composing) {
      this.onChange(value);
    }
  }
  _compositionStart() {
    this._composing = true;
  }
  _compositionEnd(value) {
    this._composing = false;
    this._compositionMode && this.onChange(value);
  }
  static \u0275fac = function DefaultValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DefaultValueAccessor)(\u0275\u0275directiveInject(Renderer2), \u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(COMPOSITION_BUFFER_MODE, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _DefaultValueAccessor,
    selectors: [["input", "formControlName", "", 3, "type", "checkbox"], ["textarea", "formControlName", ""], ["input", "formControl", "", 3, "type", "checkbox"], ["textarea", "formControl", ""], ["input", "ngModel", "", 3, "type", "checkbox"], ["textarea", "ngModel", ""], ["", "ngDefaultControl", ""]],
    hostBindings: function DefaultValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("input", function DefaultValueAccessor_input_HostBindingHandler($event) {
          return ctx._handleInput($event.target.value);
        })("blur", function DefaultValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        })("compositionstart", function DefaultValueAccessor_compositionstart_HostBindingHandler() {
          return ctx._compositionStart();
        })("compositionend", function DefaultValueAccessor_compositionend_HostBindingHandler($event) {
          return ctx._compositionEnd($event.target.value);
        });
      }
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([DEFAULT_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DefaultValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input:not([type=checkbox])[formControlName],textarea[formControlName],input:not([type=checkbox])[formControl],textarea[formControl],input:not([type=checkbox])[ngModel],textarea[ngModel],[ngDefaultControl]",
      host: {
        "(input)": "_handleInput($any($event.target).value)",
        "(blur)": "onTouched()",
        "(compositionstart)": "_compositionStart()",
        "(compositionend)": "_compositionEnd($any($event.target).value)"
      },
      providers: [DEFAULT_VALUE_ACCESSOR],
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [COMPOSITION_BUFFER_MODE]
    }]
  }], null);
})();
function isEmptyInputValue(value) {
  return value == null || lengthOrSize(value) === 0;
}
function lengthOrSize(value) {
  if (value == null) {
    return null;
  } else if (Array.isArray(value) || typeof value === "string") {
    return value.length;
  } else if (value instanceof Set) {
    return value.size;
  }
  return null;
}
var NG_VALIDATORS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "NgValidators" : "");
var NG_ASYNC_VALIDATORS = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "NgAsyncValidators" : "");
var EMAIL_REGEXP = /^(?=.{1,254}$)(?=.{1,64}@)[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-zA-Z0-9!#$%&'*+/=?^_`{|}~-]+)*@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
function minValidator(min) {
  return (control) => {
    if (control.value == null || min == null) {
      return null;
    }
    const value = parseFloat(control.value);
    return !isNaN(value) && value < min ? {
      "min": {
        "min": min,
        "actual": control.value
      }
    } : null;
  };
}
function maxValidator(max) {
  return (control) => {
    if (control.value == null || max == null) {
      return null;
    }
    const value = parseFloat(control.value);
    return !isNaN(value) && value > max ? {
      "max": {
        "max": max,
        "actual": control.value
      }
    } : null;
  };
}
function requiredValidator(control) {
  return isEmptyInputValue(control.value) ? {
    "required": true
  } : null;
}
function requiredTrueValidator(control) {
  return control.value === true ? null : {
    "required": true
  };
}
function emailValidator(control) {
  if (isEmptyInputValue(control.value)) {
    return null;
  }
  return EMAIL_REGEXP.test(control.value) ? null : {
    "email": true
  };
}
function minLengthValidator(minLength) {
  return (control) => {
    const length = control.value?.length ?? lengthOrSize(control.value);
    if (length === null || length === 0) {
      return null;
    }
    return length < minLength ? {
      "minlength": {
        "requiredLength": minLength,
        "actualLength": length
      }
    } : null;
  };
}
function maxLengthValidator(maxLength) {
  return (control) => {
    const length = control.value?.length ?? lengthOrSize(control.value);
    if (length !== null && length > maxLength) {
      return {
        "maxlength": {
          "requiredLength": maxLength,
          "actualLength": length
        }
      };
    }
    return null;
  };
}
function patternValidator(pattern) {
  if (!pattern) return nullValidator;
  let regex;
  let regexStr;
  if (typeof pattern === "string") {
    regexStr = "";
    if (pattern.charAt(0) !== "^") regexStr += "^";
    regexStr += pattern;
    if (pattern.charAt(pattern.length - 1) !== "$") regexStr += "$";
    regex = new RegExp(regexStr);
  } else {
    regexStr = pattern.toString();
    regex = pattern;
  }
  return (control) => {
    if (isEmptyInputValue(control.value)) {
      return null;
    }
    const value = control.value;
    return regex.test(value) ? null : {
      "pattern": {
        "requiredPattern": regexStr,
        "actualValue": value
      }
    };
  };
}
function nullValidator(control) {
  return null;
}
function isPresent(o) {
  return o != null;
}
function toObservable(value) {
  const obs = isPromise2(value) ? from(value) : value;
  if ((typeof ngDevMode === "undefined" || ngDevMode) && !isSubscribable(obs)) {
    let errorMessage = `Expected async validator to return Promise or Observable.`;
    if (typeof value === "object") {
      errorMessage += " Are you using a synchronous validator where an async validator is expected?";
    }
    throw new RuntimeError(-1101, errorMessage);
  }
  return obs;
}
function mergeErrors(arrayOfErrors) {
  let res = {};
  arrayOfErrors.forEach((errors) => {
    res = errors != null ? __spreadValues(__spreadValues({}, res), errors) : res;
  });
  return Object.keys(res).length === 0 ? null : res;
}
function executeValidators(control, validators) {
  return validators.map((validator) => validator(control));
}
function isValidatorFn(validator) {
  return !validator.validate;
}
function normalizeValidators(validators) {
  return validators.map((validator) => {
    return isValidatorFn(validator) ? validator : (c) => validator.validate(c);
  });
}
function compose(validators) {
  if (!validators) return null;
  const presentValidators = validators.filter(isPresent);
  if (presentValidators.length == 0) return null;
  return function(control) {
    return mergeErrors(executeValidators(control, presentValidators));
  };
}
function composeValidators(validators) {
  return validators != null ? compose(normalizeValidators(validators)) : null;
}
function composeAsync(validators) {
  if (!validators) return null;
  const presentValidators = validators.filter(isPresent);
  if (presentValidators.length == 0) return null;
  return function(control) {
    const observables = executeValidators(control, presentValidators).map(toObservable);
    return forkJoin(observables).pipe(map(mergeErrors));
  };
}
function composeAsyncValidators(validators) {
  return validators != null ? composeAsync(normalizeValidators(validators)) : null;
}
function mergeValidators(controlValidators, dirValidator) {
  if (controlValidators === null) return [dirValidator];
  return Array.isArray(controlValidators) ? [...controlValidators, dirValidator] : [controlValidators, dirValidator];
}
function getControlValidators(control) {
  return control._rawValidators;
}
function getControlAsyncValidators(control) {
  return control._rawAsyncValidators;
}
function makeValidatorsArray(validators) {
  if (!validators) return [];
  return Array.isArray(validators) ? validators : [validators];
}
function hasValidator(validators, validator) {
  return Array.isArray(validators) ? validators.includes(validator) : validators === validator;
}
function addValidators(validators, currentValidators) {
  const current = makeValidatorsArray(currentValidators);
  const validatorsToAdd = makeValidatorsArray(validators);
  validatorsToAdd.forEach((v) => {
    if (!hasValidator(current, v)) {
      current.push(v);
    }
  });
  return current;
}
function removeValidators(validators, currentValidators) {
  return makeValidatorsArray(currentValidators).filter((v) => !hasValidator(validators, v));
}
var AbstractControlDirective = class {
  get value() {
    return this.control ? this.control.value : null;
  }
  get valid() {
    return this.control ? this.control.valid : null;
  }
  get invalid() {
    return this.control ? this.control.invalid : null;
  }
  get pending() {
    return this.control ? this.control.pending : null;
  }
  get disabled() {
    return this.control ? this.control.disabled : null;
  }
  get enabled() {
    return this.control ? this.control.enabled : null;
  }
  get errors() {
    return this.control ? this.control.errors : null;
  }
  get pristine() {
    return this.control ? this.control.pristine : null;
  }
  get dirty() {
    return this.control ? this.control.dirty : null;
  }
  get touched() {
    return this.control ? this.control.touched : null;
  }
  get status() {
    return this.control ? this.control.status : null;
  }
  get untouched() {
    return this.control ? this.control.untouched : null;
  }
  get statusChanges() {
    return this.control ? this.control.statusChanges : null;
  }
  get valueChanges() {
    return this.control ? this.control.valueChanges : null;
  }
  get path() {
    return null;
  }
  _composedValidatorFn;
  _composedAsyncValidatorFn;
  _rawValidators = [];
  _rawAsyncValidators = [];
  _setValidators(validators) {
    this._rawValidators = validators || [];
    this._composedValidatorFn = composeValidators(this._rawValidators);
  }
  _setAsyncValidators(validators) {
    this._rawAsyncValidators = validators || [];
    this._composedAsyncValidatorFn = composeAsyncValidators(this._rawAsyncValidators);
  }
  get validator() {
    return this._composedValidatorFn || null;
  }
  get asyncValidator() {
    return this._composedAsyncValidatorFn || null;
  }
  _onDestroyCallbacks = [];
  _registerOnDestroy(fn) {
    this._onDestroyCallbacks.push(fn);
  }
  _invokeOnDestroyCallbacks() {
    this._onDestroyCallbacks.forEach((fn) => fn());
    this._onDestroyCallbacks = [];
  }
  reset(value = void 0) {
    this.control?.reset(value);
  }
  hasError(errorCode, path) {
    return this.control ? this.control.hasError(errorCode, path) : false;
  }
  getError(errorCode, path) {
    return this.control ? this.control.getError(errorCode, path) : null;
  }
};
var ControlContainer = class extends AbstractControlDirective {
  name;
  get formDirective() {
    return null;
  }
  get path() {
    return null;
  }
};
var NgControl = class extends AbstractControlDirective {
  _parent = null;
  name = null;
  valueAccessor = null;
};
var AbstractControlStatus = class {
  _cd;
  constructor(cd) {
    this._cd = cd;
  }
  get isTouched() {
    this._cd?.control?._touched?.();
    return !!this._cd?.control?.touched;
  }
  get isUntouched() {
    return !!this._cd?.control?.untouched;
  }
  get isPristine() {
    this._cd?.control?._pristine?.();
    return !!this._cd?.control?.pristine;
  }
  get isDirty() {
    return !!this._cd?.control?.dirty;
  }
  get isValid() {
    this._cd?.control?._status?.();
    return !!this._cd?.control?.valid;
  }
  get isInvalid() {
    return !!this._cd?.control?.invalid;
  }
  get isPending() {
    return !!this._cd?.control?.pending;
  }
  get isSubmitted() {
    this._cd?._submitted?.();
    return !!this._cd?.submitted;
  }
};
var ngControlStatusHost = {
  "[class.ng-untouched]": "isUntouched",
  "[class.ng-touched]": "isTouched",
  "[class.ng-pristine]": "isPristine",
  "[class.ng-dirty]": "isDirty",
  "[class.ng-valid]": "isValid",
  "[class.ng-invalid]": "isInvalid",
  "[class.ng-pending]": "isPending"
};
var NgControlStatus = class _NgControlStatus extends AbstractControlStatus {
  constructor(cd) {
    super(cd);
  }
  static \u0275fac = function NgControlStatus_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgControlStatus)(\u0275\u0275directiveInject(NgControl, 2));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgControlStatus,
    selectors: [["", "formControlName", ""], ["", "ngModel", ""], ["", "formControl", ""]],
    hostVars: 14,
    hostBindings: function NgControlStatus_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275classProp("ng-untouched", ctx.isUntouched)("ng-touched", ctx.isTouched)("ng-pristine", ctx.isPristine)("ng-dirty", ctx.isDirty)("ng-valid", ctx.isValid)("ng-invalid", ctx.isInvalid)("ng-pending", ctx.isPending);
      }
    },
    standalone: false,
    features: [\u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgControlStatus, [{
    type: Directive,
    args: [{
      selector: "[formControlName],[ngModel],[formControl]",
      host: ngControlStatusHost,
      standalone: false
    }]
  }], () => [{
    type: NgControl,
    decorators: [{
      type: Self
    }]
  }], null);
})();
var NgControlStatusGroup = class _NgControlStatusGroup extends AbstractControlStatus {
  constructor(cd) {
    super(cd);
  }
  static \u0275fac = function NgControlStatusGroup_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgControlStatusGroup)(\u0275\u0275directiveInject(ControlContainer, 10));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgControlStatusGroup,
    selectors: [["", "formGroupName", ""], ["", "formArrayName", ""], ["", "ngModelGroup", ""], ["", "formGroup", ""], ["", "formArray", ""], ["form", 3, "ngNoForm", ""], ["", "ngForm", ""]],
    hostVars: 16,
    hostBindings: function NgControlStatusGroup_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275classProp("ng-untouched", ctx.isUntouched)("ng-touched", ctx.isTouched)("ng-pristine", ctx.isPristine)("ng-dirty", ctx.isDirty)("ng-valid", ctx.isValid)("ng-invalid", ctx.isInvalid)("ng-pending", ctx.isPending)("ng-submitted", ctx.isSubmitted);
      }
    },
    standalone: false,
    features: [\u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgControlStatusGroup, [{
    type: Directive,
    args: [{
      selector: "[formGroupName],[formArrayName],[ngModelGroup],[formGroup],[formArray],form:not([ngNoForm]),[ngForm]",
      host: __spreadProps(__spreadValues({}, ngControlStatusHost), {
        "[class.ng-submitted]": "isSubmitted"
      }),
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }]
  }], null);
})();
var formControlNameExample = `
  <div [formGroup]="myGroup">
    <input formControlName="firstName">
  </div>

  In your class:

  this.myGroup = new FormGroup({
      firstName: new FormControl()
  });`;
var formGroupNameExample = `
  <div [formGroup]="myGroup">
      <div formGroupName="person">
        <input formControlName="firstName">
      </div>
  </div>

  In your class:

  this.myGroup = new FormGroup({
      person: new FormGroup({ firstName: new FormControl() })
  });`;
var formArrayNameExample = `
  <div [formGroup]="myGroup">
    <div formArrayName="cities">
      <div *ngFor="let city of cityArray.controls; index as i">
        <input [formControlName]="i">
      </div>
    </div>
  </div>

  In your class:

  this.cityArray = new FormArray([new FormControl('SF')]);
  this.myGroup = new FormGroup({
    cities: this.cityArray
  });`;
var ngModelGroupExample = `
  <form>
      <div ngModelGroup="person">
        <input [(ngModel)]="person.name" name="firstName">
      </div>
  </form>`;
var ngModelWithFormGroupExample = `
  <div [formGroup]="myGroup">
      <input formControlName="firstName">
      <input [(ngModel)]="showMoreControls" [ngModelOptions]="{standalone: true}">
  </div>
`;
var VERSION2 = /* @__PURE__ */ new Version("21.2.17");
function controlParentException(nameOrIndex) {
  return new RuntimeError(1050, `formControlName must be used with a parent formGroup or formArray directive. You'll want to add a formGroup/formArray
      directive and pass it an existing FormGroup/FormArray instance (you can create one in your class).

      ${describeFormControl(nameOrIndex)}

    Example:

    ${formControlNameExample}`);
}
function describeFormControl(nameOrIndex) {
  if (nameOrIndex == null || nameOrIndex === "") {
    return "";
  }
  const valueType = typeof nameOrIndex === "string" ? "name" : "index";
  return `Affected Form Control ${valueType}: "${nameOrIndex}"`;
}
function ngModelGroupException() {
  return new RuntimeError(1051, `formControlName cannot be used with an ngModelGroup parent. It is only compatible with parents
      that also have a "form" prefix: formGroupName, formArrayName, or formGroup.

      Option 1:  Update the parent to be formGroupName (reactive form strategy)

      ${formGroupNameExample}

      Option 2: Use ngModel instead of formControlName (template-driven strategy)

      ${ngModelGroupExample}`);
}
function missingFormException() {
  return new RuntimeError(1052, `formGroup expects a FormGroup instance. Please pass one in.

      Example:

      ${formControlNameExample}`);
}
function groupParentException() {
  return new RuntimeError(1053, `formGroupName must be used with a parent formGroup directive.  You'll want to add a formGroup
    directive and pass it an existing FormGroup instance (you can create one in your class).

    Example:

    ${formGroupNameExample}`);
}
function arrayParentException() {
  return new RuntimeError(1054, `formArrayName must be used with a parent formGroup directive.  You'll want to add a formGroup
      directive and pass it an existing FormGroup instance (you can create one in your class).

      Example:

      ${formArrayNameExample}`);
}
var disabledAttrWarning = `
  It looks like you're using the disabled attribute with a reactive form directive. If you set disabled to true
  when you set up this control in your component class, the disabled attribute will actually be set in the DOM for
  you. We recommend using this approach to avoid 'changed after checked' errors.

  Example:
  // Specify the \`disabled\` property at control creation time:
  form = new FormGroup({
    first: new FormControl({value: 'Nancy', disabled: true}, Validators.required),
    last: new FormControl('Drew', Validators.required)
  });

  // Controls can also be enabled/disabled after creation:
  form.get('first')?.enable();
  form.get('last')?.disable();
`;
var asyncValidatorsDroppedWithOptsWarning = `
  It looks like you're constructing using a FormControl with both an options argument and an
  async validators argument. Mixing these arguments will cause your async validators to be dropped.
  You should either put all your validators in the options object, or in separate validators
  arguments. For example:

  // Using validators arguments
  fc = new FormControl(42, Validators.required, myAsyncValidator);

  // Using AbstractControlOptions
  fc = new FormControl(42, {validators: Validators.required, asyncValidators: myAV});

  // Do NOT mix them: async validators will be dropped!
  fc = new FormControl(42, {validators: Validators.required}, /* Oops! */ myAsyncValidator);
`;
function ngModelWarning(directiveName) {
  const versionSubDomain = VERSION2.major !== "0" ? `v${VERSION2.major}.` : "";
  return `
  It looks like you're using ngModel on the same form field as ${directiveName}.
  Support for using the ngModel input property and ngModelChange event with
  reactive form directives has been deprecated in Angular v6 and will be removed
  in a future version of Angular.

  For more information on this, see our API docs here:
  https://${versionSubDomain}angular.dev/api/forms/${directiveName === "formControl" ? "FormControlDirective" : "FormControlName"}
  `;
}
function describeKey(isFormGroup, key) {
  return isFormGroup ? `with name: '${key}'` : `at index: ${key}`;
}
function noControlsError(isFormGroup) {
  return `
    There are no form controls registered with this ${isFormGroup ? "group" : "array"} yet. If you're using ngModel,
    you may want to check next tick (e.g. use setTimeout).
  `;
}
function missingControlError(isFormGroup, key) {
  return `Cannot find form control ${describeKey(isFormGroup, key)}`;
}
function missingControlValueError(isFormGroup, key) {
  return `Must supply a value for form control ${describeKey(isFormGroup, key)}`;
}
var VALID = "VALID";
var INVALID = "INVALID";
var PENDING = "PENDING";
var DISABLED = "DISABLED";
var ControlEvent = class {
};
var ValueChangeEvent = class extends ControlEvent {
  value;
  source;
  constructor(value, source) {
    super();
    this.value = value;
    this.source = source;
  }
};
var PristineChangeEvent = class extends ControlEvent {
  pristine;
  source;
  constructor(pristine, source) {
    super();
    this.pristine = pristine;
    this.source = source;
  }
};
var TouchedChangeEvent = class extends ControlEvent {
  touched;
  source;
  constructor(touched, source) {
    super();
    this.touched = touched;
    this.source = source;
  }
};
var StatusChangeEvent = class extends ControlEvent {
  status;
  source;
  constructor(status, source) {
    super();
    this.status = status;
    this.source = source;
  }
};
var FormSubmittedEvent = class extends ControlEvent {
  source;
  constructor(source) {
    super();
    this.source = source;
  }
};
var FormResetEvent = class extends ControlEvent {
  source;
  constructor(source) {
    super();
    this.source = source;
  }
};
function pickValidators(validatorOrOpts) {
  return (isOptionsObj(validatorOrOpts) ? validatorOrOpts.validators : validatorOrOpts) || null;
}
function coerceToValidator(validator) {
  return Array.isArray(validator) ? composeValidators(validator) : validator || null;
}
function pickAsyncValidators(asyncValidator, validatorOrOpts) {
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    if (isOptionsObj(validatorOrOpts) && asyncValidator) {
      console.warn(asyncValidatorsDroppedWithOptsWarning);
    }
  }
  return (isOptionsObj(validatorOrOpts) ? validatorOrOpts.asyncValidators : asyncValidator) || null;
}
function coerceToAsyncValidator(asyncValidator) {
  return Array.isArray(asyncValidator) ? composeAsyncValidators(asyncValidator) : asyncValidator || null;
}
function isOptionsObj(validatorOrOpts) {
  return validatorOrOpts != null && !Array.isArray(validatorOrOpts) && typeof validatorOrOpts === "object";
}
function assertControlPresent(parent, isGroup, key) {
  const controls = parent.controls;
  const collection = isGroup ? Object.keys(controls) : controls;
  if (!collection.length) {
    throw new RuntimeError(1e3, typeof ngDevMode === "undefined" || ngDevMode ? noControlsError(isGroup) : "");
  }
  if (!controls[key]) {
    throw new RuntimeError(1001, typeof ngDevMode === "undefined" || ngDevMode ? missingControlError(isGroup, key) : "");
  }
}
function assertAllValuesPresent(control, isGroup, value) {
  control._forEachChild((_, key) => {
    if (value[key] === void 0) {
      throw new RuntimeError(-1002, typeof ngDevMode === "undefined" || ngDevMode ? missingControlValueError(isGroup, key) : "");
    }
  });
}
var AbstractControl = class {
  _pendingDirty = false;
  _hasOwnPendingAsyncValidator = null;
  _pendingTouched = false;
  _onCollectionChange = () => {
  };
  _updateOn;
  _parent = null;
  _asyncValidationSubscription;
  _composedValidatorFn;
  _composedAsyncValidatorFn;
  _rawValidators;
  _rawAsyncValidators;
  value;
  constructor(validators, asyncValidators) {
    this._assignValidators(validators);
    this._assignAsyncValidators(asyncValidators);
  }
  get validator() {
    return this._composedValidatorFn;
  }
  set validator(validatorFn) {
    this._rawValidators = this._composedValidatorFn = validatorFn;
  }
  get asyncValidator() {
    return this._composedAsyncValidatorFn;
  }
  set asyncValidator(asyncValidatorFn) {
    this._rawAsyncValidators = this._composedAsyncValidatorFn = asyncValidatorFn;
  }
  get parent() {
    return this._parent;
  }
  get status() {
    return untracked2(this.statusReactive);
  }
  set status(v) {
    untracked2(() => this.statusReactive.set(v));
  }
  _status = computed(() => this.statusReactive(), ...ngDevMode ? [{
    debugName: "_status"
  }] : []);
  statusReactive = signal(void 0, ...ngDevMode ? [{
    debugName: "statusReactive"
  }] : []);
  get valid() {
    return this.status === VALID;
  }
  get invalid() {
    return this.status === INVALID;
  }
  get pending() {
    return this.status === PENDING;
  }
  get disabled() {
    return this.status === DISABLED;
  }
  get enabled() {
    return this.status !== DISABLED;
  }
  errors;
  get pristine() {
    return untracked2(this.pristineReactive);
  }
  set pristine(v) {
    untracked2(() => this.pristineReactive.set(v));
  }
  _pristine = computed(() => this.pristineReactive(), ...ngDevMode ? [{
    debugName: "_pristine"
  }] : []);
  pristineReactive = signal(true, ...ngDevMode ? [{
    debugName: "pristineReactive"
  }] : []);
  get dirty() {
    return !this.pristine;
  }
  get touched() {
    return untracked2(this.touchedReactive);
  }
  set touched(v) {
    untracked2(() => this.touchedReactive.set(v));
  }
  _touched = computed(() => this.touchedReactive(), ...ngDevMode ? [{
    debugName: "_touched"
  }] : []);
  touchedReactive = signal(false, ...ngDevMode ? [{
    debugName: "touchedReactive"
  }] : []);
  get untouched() {
    return !this.touched;
  }
  _events = new Subject();
  events = this._events.asObservable();
  valueChanges;
  statusChanges;
  get updateOn() {
    return this._updateOn ? this._updateOn : this.parent ? this.parent.updateOn : "change";
  }
  setValidators(validators) {
    this._assignValidators(validators);
  }
  setAsyncValidators(validators) {
    this._assignAsyncValidators(validators);
  }
  addValidators(validators) {
    this.setValidators(addValidators(validators, this._rawValidators));
  }
  addAsyncValidators(validators) {
    this.setAsyncValidators(addValidators(validators, this._rawAsyncValidators));
  }
  removeValidators(validators) {
    this.setValidators(removeValidators(validators, this._rawValidators));
  }
  removeAsyncValidators(validators) {
    this.setAsyncValidators(removeValidators(validators, this._rawAsyncValidators));
  }
  hasValidator(validator) {
    return hasValidator(this._rawValidators, validator);
  }
  hasAsyncValidator(validator) {
    return hasValidator(this._rawAsyncValidators, validator);
  }
  clearValidators() {
    this.validator = null;
  }
  clearAsyncValidators() {
    this.asyncValidator = null;
  }
  markAsTouched(opts = {}) {
    const changed = this.touched === false;
    this.touched = true;
    const sourceControl = opts.sourceControl ?? this;
    if (!opts.onlySelf) {
      this._parent?.markAsTouched(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new TouchedChangeEvent(true, sourceControl));
    }
  }
  markAllAsDirty(opts = {}) {
    this.markAsDirty({
      onlySelf: true,
      emitEvent: opts.emitEvent,
      sourceControl: this
    });
    this._forEachChild((control) => control.markAllAsDirty(opts));
  }
  markAllAsTouched(opts = {}) {
    this.markAsTouched({
      onlySelf: true,
      emitEvent: opts.emitEvent,
      sourceControl: this
    });
    this._forEachChild((control) => control.markAllAsTouched(opts));
  }
  markAsUntouched(opts = {}) {
    const changed = this.touched === true;
    this.touched = false;
    this._pendingTouched = false;
    const sourceControl = opts.sourceControl ?? this;
    this._forEachChild((control) => {
      control.markAsUntouched({
        onlySelf: true,
        emitEvent: opts.emitEvent,
        sourceControl
      });
    });
    if (!opts.onlySelf) {
      this._parent?._updateTouched(opts, sourceControl);
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new TouchedChangeEvent(false, sourceControl));
    }
  }
  markAsDirty(opts = {}) {
    const changed = this.pristine === true;
    this.pristine = false;
    const sourceControl = opts.sourceControl ?? this;
    if (!opts.onlySelf) {
      this._parent?.markAsDirty(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new PristineChangeEvent(false, sourceControl));
    }
  }
  markAsPristine(opts = {}) {
    const changed = this.pristine === false;
    this.pristine = true;
    this._pendingDirty = false;
    const sourceControl = opts.sourceControl ?? this;
    this._forEachChild((control) => {
      control.markAsPristine({
        onlySelf: true,
        emitEvent: opts.emitEvent
      });
    });
    if (!opts.onlySelf) {
      this._parent?._updatePristine(opts, sourceControl);
    }
    if (changed && opts.emitEvent !== false) {
      this._events.next(new PristineChangeEvent(true, sourceControl));
    }
  }
  markAsPending(opts = {}) {
    this.status = PENDING;
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.statusChanges.emit(this.status);
    }
    if (!opts.onlySelf) {
      this._parent?.markAsPending(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
  }
  disable(opts = {}) {
    const skipPristineCheck = this._parentMarkedDirty(opts.onlySelf);
    this.status = DISABLED;
    this.errors = null;
    this._forEachChild((control) => {
      control.disable(__spreadProps(__spreadValues({}, opts), {
        onlySelf: true
      }));
    });
    this._updateValue();
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new ValueChangeEvent(this.value, sourceControl));
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.valueChanges.emit(this.value);
      this.statusChanges.emit(this.status);
    }
    this._updateAncestors(__spreadProps(__spreadValues({}, opts), {
      skipPristineCheck
    }), this);
    this._onDisabledChange.forEach((changeFn) => changeFn(true));
  }
  enable(opts = {}) {
    const skipPristineCheck = this._parentMarkedDirty(opts.onlySelf);
    this.status = VALID;
    this._forEachChild((control) => {
      control.enable(__spreadProps(__spreadValues({}, opts), {
        onlySelf: true
      }));
    });
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: opts.emitEvent
    });
    this._updateAncestors(__spreadProps(__spreadValues({}, opts), {
      skipPristineCheck
    }), this);
    this._onDisabledChange.forEach((changeFn) => changeFn(false));
  }
  _updateAncestors(opts, sourceControl) {
    if (!opts.onlySelf) {
      this._parent?.updateValueAndValidity(opts);
      if (!opts.skipPristineCheck) {
        this._parent?._updatePristine({}, sourceControl);
      }
      this._parent?._updateTouched({}, sourceControl);
    }
  }
  setParent(parent) {
    this._parent = parent;
  }
  getRawValue() {
    return this.value;
  }
  updateValueAndValidity(opts = {}) {
    this._setInitialStatus();
    this._updateValue();
    if (this.enabled) {
      const shouldHaveEmitted = this._cancelExistingSubscription();
      this.errors = this._runValidator();
      this.status = this._calculateStatus();
      if (this.status === VALID || this.status === PENDING) {
        this._runAsyncValidator(shouldHaveEmitted, opts.emitEvent);
      }
    }
    const sourceControl = opts.sourceControl ?? this;
    if (opts.emitEvent !== false) {
      this._events.next(new ValueChangeEvent(this.value, sourceControl));
      this._events.next(new StatusChangeEvent(this.status, sourceControl));
      this.valueChanges.emit(this.value);
      this.statusChanges.emit(this.status);
    }
    if (!opts.onlySelf) {
      this._parent?.updateValueAndValidity(__spreadProps(__spreadValues({}, opts), {
        sourceControl
      }));
    }
  }
  _updateTreeValidity(opts = {
    emitEvent: true
  }) {
    this._forEachChild((ctrl) => ctrl._updateTreeValidity(opts));
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: opts.emitEvent
    });
  }
  _setInitialStatus() {
    this.status = this._allControlsDisabled() ? DISABLED : VALID;
  }
  _runValidator() {
    return this.validator ? this.validator(this) : null;
  }
  _runAsyncValidator(shouldHaveEmitted, emitEvent) {
    if (this.asyncValidator) {
      this.status = PENDING;
      this._hasOwnPendingAsyncValidator = {
        emitEvent: emitEvent !== false,
        shouldHaveEmitted: shouldHaveEmitted !== false
      };
      const obs = toObservable(this.asyncValidator(this));
      this._asyncValidationSubscription = obs.subscribe((errors) => {
        this._hasOwnPendingAsyncValidator = null;
        this.setErrors(errors, {
          emitEvent,
          shouldHaveEmitted
        });
      });
    }
  }
  _cancelExistingSubscription() {
    if (this._asyncValidationSubscription) {
      this._asyncValidationSubscription.unsubscribe();
      const shouldHaveEmitted = (this._hasOwnPendingAsyncValidator?.emitEvent || this._hasOwnPendingAsyncValidator?.shouldHaveEmitted) ?? false;
      this._hasOwnPendingAsyncValidator = null;
      return shouldHaveEmitted;
    }
    return false;
  }
  setErrors(errors, opts = {}) {
    this.errors = errors;
    this._updateControlsErrors(opts.emitEvent !== false, this, opts.shouldHaveEmitted);
  }
  get(path) {
    let currPath = path;
    if (currPath == null) return null;
    if (!Array.isArray(currPath)) currPath = currPath.split(".");
    if (currPath.length === 0) return null;
    return currPath.reduce((control, name) => control && control._find(name), this);
  }
  getError(errorCode, path) {
    const control = path ? this.get(path) : this;
    return control?.errors ? control.errors[errorCode] : null;
  }
  hasError(errorCode, path) {
    return !!this.getError(errorCode, path);
  }
  get root() {
    let x = this;
    while (x._parent) {
      x = x._parent;
    }
    return x;
  }
  _updateControlsErrors(emitEvent, changedControl, shouldHaveEmitted) {
    this.status = this._calculateStatus();
    if (emitEvent) {
      this.statusChanges.emit(this.status);
    }
    if (emitEvent || shouldHaveEmitted) {
      this._events.next(new StatusChangeEvent(this.status, changedControl));
    }
    if (this._parent) {
      this._parent._updateControlsErrors(emitEvent, changedControl, shouldHaveEmitted);
    }
  }
  _initObservables() {
    this.valueChanges = new EventEmitter();
    this.statusChanges = new EventEmitter();
  }
  _calculateStatus() {
    if (this._allControlsDisabled()) return DISABLED;
    if (this.errors) return INVALID;
    if (this._hasOwnPendingAsyncValidator || this._anyControlsHaveStatus(PENDING)) return PENDING;
    if (this._anyControlsHaveStatus(INVALID)) return INVALID;
    return VALID;
  }
  _anyControlsHaveStatus(status) {
    return this._anyControls((control) => control.status === status);
  }
  _anyControlsDirty() {
    return this._anyControls((control) => control.dirty);
  }
  _anyControlsTouched() {
    return this._anyControls((control) => control.touched);
  }
  _updatePristine(opts, changedControl) {
    const newPristine = !this._anyControlsDirty();
    const changed = this.pristine !== newPristine;
    this.pristine = newPristine;
    if (!opts.onlySelf) {
      this._parent?._updatePristine(opts, changedControl);
    }
    if (changed) {
      this._events.next(new PristineChangeEvent(this.pristine, changedControl));
    }
  }
  _updateTouched(opts = {}, changedControl) {
    this.touched = this._anyControlsTouched();
    this._events.next(new TouchedChangeEvent(this.touched, changedControl));
    if (!opts.onlySelf) {
      this._parent?._updateTouched(opts, changedControl);
    }
  }
  _onDisabledChange = [];
  _registerOnCollectionChange(fn) {
    this._onCollectionChange = fn;
  }
  _setUpdateStrategy(opts) {
    if (isOptionsObj(opts) && opts.updateOn != null) {
      this._updateOn = opts.updateOn;
    }
  }
  _parentMarkedDirty(onlySelf) {
    return !onlySelf && !!this._parent?.dirty && !this._parent._anyControlsDirty();
  }
  _find(name) {
    return null;
  }
  _assignValidators(validators) {
    this._rawValidators = Array.isArray(validators) ? validators.slice() : validators;
    this._composedValidatorFn = coerceToValidator(this._rawValidators);
  }
  _assignAsyncValidators(validators) {
    this._rawAsyncValidators = Array.isArray(validators) ? validators.slice() : validators;
    this._composedAsyncValidatorFn = coerceToAsyncValidator(this._rawAsyncValidators);
  }
};
var FormGroup = class extends AbstractControl {
  constructor(controls, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    (typeof ngDevMode === "undefined" || ngDevMode) && validateFormGroupControls(controls);
    this.controls = controls;
    this._initObservables();
    this._setUpdateStrategy(validatorOrOpts);
    this._setUpControls();
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: !!this.asyncValidator
    });
  }
  controls;
  registerControl(name, control) {
    if (this.controls[name]) return this.controls[name];
    this.controls[name] = control;
    control.setParent(this);
    control._registerOnCollectionChange(this._onCollectionChange);
    return control;
  }
  addControl(name, control, options = {}) {
    this.registerControl(name, control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  removeControl(name, options = {}) {
    if (this.controls[name]) this.controls[name]._registerOnCollectionChange(() => {
    });
    delete this.controls[name];
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  setControl(name, control, options = {}) {
    if (this.controls[name]) this.controls[name]._registerOnCollectionChange(() => {
    });
    delete this.controls[name];
    if (control) this.registerControl(name, control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  contains(controlName) {
    return this.controls.hasOwnProperty(controlName) && this.controls[controlName].enabled;
  }
  setValue(value, options = {}) {
    assertAllValuesPresent(this, true, value);
    Object.keys(value).forEach((name) => {
      assertControlPresent(this, true, name);
      this.controls[name].setValue(value[name], {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this.updateValueAndValidity(options);
  }
  patchValue(value, options = {}) {
    if (value == null) return;
    Object.keys(value).forEach((name) => {
      const control = this.controls[name];
      if (control) {
        control.patchValue(value[name], {
          onlySelf: true,
          emitEvent: options.emitEvent
        });
      }
    });
    this.updateValueAndValidity(options);
  }
  reset(value = {}, options = {}) {
    this._forEachChild((control, name) => {
      control.reset(value ? value[name] : null, __spreadProps(__spreadValues({}, options), {
        onlySelf: true
      }));
    });
    this._updatePristine(options, this);
    this._updateTouched(options, this);
    this.updateValueAndValidity(options);
    if (options?.emitEvent !== false) {
      this._events.next(new FormResetEvent(this));
    }
  }
  getRawValue() {
    return this._reduceChildren({}, (acc, control, name) => {
      acc[name] = control.getRawValue();
      return acc;
    });
  }
  _syncPendingControls() {
    let subtreeUpdated = this._reduceChildren(false, (updated, child) => {
      return child._syncPendingControls() ? true : updated;
    });
    if (subtreeUpdated) this.updateValueAndValidity({
      onlySelf: true
    });
    return subtreeUpdated;
  }
  _forEachChild(cb) {
    Object.keys(this.controls).forEach((key) => {
      const control = this.controls[key];
      control && cb(control, key);
    });
  }
  _setUpControls() {
    this._forEachChild((control) => {
      control.setParent(this);
      control._registerOnCollectionChange(this._onCollectionChange);
    });
  }
  _updateValue() {
    this.value = this._reduceValue();
  }
  _anyControls(condition) {
    for (const [controlName, control] of Object.entries(this.controls)) {
      if (this.contains(controlName) && condition(control)) {
        return true;
      }
    }
    return false;
  }
  _reduceValue() {
    let acc = {};
    return this._reduceChildren(acc, (acc2, control, name) => {
      if (control.enabled || this.disabled) {
        acc2[name] = control.value;
      }
      return acc2;
    });
  }
  _reduceChildren(initValue, fn) {
    let res = initValue;
    this._forEachChild((control, name) => {
      res = fn(res, control, name);
    });
    return res;
  }
  _allControlsDisabled() {
    for (const controlName of Object.keys(this.controls)) {
      if (this.controls[controlName].enabled) {
        return false;
      }
    }
    return Object.keys(this.controls).length > 0 || this.disabled;
  }
  _find(name) {
    return this.controls.hasOwnProperty(name) ? this.controls[name] : null;
  }
};
function validateFormGroupControls(controls) {
  const invalidKeys = Object.keys(controls).filter((key) => key.includes("."));
  if (invalidKeys.length > 0) {
    console.warn(`FormGroup keys cannot include \`.\`, please replace the keys for: ${invalidKeys.join(",")}.`);
  }
}
var FormRecord = class extends FormGroup {
};
var CALL_SET_DISABLED_STATE = new InjectionToken(typeof ngDevMode === "undefined" || ngDevMode ? "CallSetDisabledState" : "", {
  factory: () => setDisabledStateDefault
});
var setDisabledStateDefault = "always";
function controlPath(name, parent) {
  return [...parent.path, name];
}
function setUpControl(control, dir, callSetDisabledState = setDisabledStateDefault) {
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    if (!control) _throwError(dir, "Cannot find control with");
    if (!dir.valueAccessor) _throwMissingValueAccessorError(dir);
  }
  setUpValidators(control, dir);
  dir.valueAccessor.writeValue(control.value);
  if (control.disabled || callSetDisabledState === "always") {
    dir.valueAccessor.setDisabledState?.(control.disabled);
  }
  setUpViewChangePipeline(control, dir);
  setUpModelChangePipeline(control, dir);
  setUpBlurPipeline(control, dir);
  setUpDisabledChangeHandler(control, dir);
}
function cleanUpControl(control, dir, validateControlPresenceOnChange = true) {
  const noop3 = () => {
    if (validateControlPresenceOnChange && (typeof ngDevMode === "undefined" || ngDevMode)) {
      _noControlError(dir);
    }
  };
  dir?.valueAccessor?.registerOnChange(noop3);
  dir?.valueAccessor?.registerOnTouched(noop3);
  cleanUpValidators(control, dir);
  if (control) {
    dir._invokeOnDestroyCallbacks();
    control._registerOnCollectionChange(() => {
    });
  }
}
function registerOnValidatorChange(validators, onChange) {
  validators.forEach((validator) => {
    if (validator.registerOnValidatorChange) validator.registerOnValidatorChange(onChange);
  });
}
function setUpDisabledChangeHandler(control, dir) {
  if (dir.valueAccessor.setDisabledState) {
    const onDisabledChange = (isDisabled) => {
      dir.valueAccessor.setDisabledState(isDisabled);
    };
    control.registerOnDisabledChange(onDisabledChange);
    dir._registerOnDestroy(() => {
      control._unregisterOnDisabledChange(onDisabledChange);
    });
  }
}
function setUpValidators(control, dir) {
  const validators = getControlValidators(control);
  if (dir.validator !== null) {
    control.setValidators(mergeValidators(validators, dir.validator));
  } else if (typeof validators === "function") {
    control.setValidators([validators]);
  }
  const asyncValidators = getControlAsyncValidators(control);
  if (dir.asyncValidator !== null) {
    control.setAsyncValidators(mergeValidators(asyncValidators, dir.asyncValidator));
  } else if (typeof asyncValidators === "function") {
    control.setAsyncValidators([asyncValidators]);
  }
  const onValidatorChange = () => control.updateValueAndValidity();
  registerOnValidatorChange(dir._rawValidators, onValidatorChange);
  registerOnValidatorChange(dir._rawAsyncValidators, onValidatorChange);
}
function cleanUpValidators(control, dir) {
  let isControlUpdated = false;
  if (control !== null) {
    if (dir.validator !== null) {
      const validators = getControlValidators(control);
      if (Array.isArray(validators) && validators.length > 0) {
        const updatedValidators = validators.filter((validator) => validator !== dir.validator);
        if (updatedValidators.length !== validators.length) {
          isControlUpdated = true;
          control.setValidators(updatedValidators);
        }
      }
    }
    if (dir.asyncValidator !== null) {
      const asyncValidators = getControlAsyncValidators(control);
      if (Array.isArray(asyncValidators) && asyncValidators.length > 0) {
        const updatedAsyncValidators = asyncValidators.filter((asyncValidator) => asyncValidator !== dir.asyncValidator);
        if (updatedAsyncValidators.length !== asyncValidators.length) {
          isControlUpdated = true;
          control.setAsyncValidators(updatedAsyncValidators);
        }
      }
    }
  }
  const noop3 = () => {
  };
  registerOnValidatorChange(dir._rawValidators, noop3);
  registerOnValidatorChange(dir._rawAsyncValidators, noop3);
  return isControlUpdated;
}
function setUpViewChangePipeline(control, dir) {
  dir.valueAccessor.registerOnChange((newValue) => {
    control._pendingValue = newValue;
    control._pendingChange = true;
    control._pendingDirty = true;
    if (control.updateOn === "change") updateControl(control, dir);
  });
}
function setUpBlurPipeline(control, dir) {
  dir.valueAccessor.registerOnTouched(() => {
    control._pendingTouched = true;
    if (control.updateOn === "blur" && control._pendingChange) updateControl(control, dir);
    if (control.updateOn !== "submit") control.markAsTouched();
  });
}
function updateControl(control, dir) {
  if (control._pendingDirty) control.markAsDirty();
  control.setValue(control._pendingValue, {
    emitModelToViewChange: false
  });
  dir.viewToModelUpdate(control._pendingValue);
  control._pendingChange = false;
}
function setUpModelChangePipeline(control, dir) {
  const onChange = (newValue, emitModelEvent) => {
    dir.valueAccessor.writeValue(newValue);
    if (emitModelEvent) dir.viewToModelUpdate(newValue);
  };
  control.registerOnChange(onChange);
  dir._registerOnDestroy(() => {
    control._unregisterOnChange(onChange);
  });
}
function setUpFormContainer(control, dir) {
  if (control == null && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "Cannot find control with");
  setUpValidators(control, dir);
}
function cleanUpFormContainer(control, dir) {
  return cleanUpValidators(control, dir);
}
function _noControlError(dir) {
  return _throwError(dir, "There is no FormControl instance attached to form control element with");
}
function _throwError(dir, message) {
  const messageEnd = _describeControlLocation(dir);
  throw new Error(`${message} ${messageEnd}`);
}
function _describeControlLocation(dir) {
  const path = dir.path;
  if (path && path.length > 1) return `path: '${path.join(" -> ")}'`;
  if (path?.[0]) return `name: '${path}'`;
  return "unspecified name attribute";
}
function _throwMissingValueAccessorError(dir) {
  const loc = _describeControlLocation(dir);
  throw new RuntimeError(-1203, `No value accessor for form control ${loc}.`);
}
function _throwInvalidValueAccessorError(dir) {
  const loc = _describeControlLocation(dir);
  throw new RuntimeError(1200, `Value accessor was not provided as an array for form control with ${loc}. Check that the \`NG_VALUE_ACCESSOR\` token is configured as a \`multi: true\` provider.`);
}
function isPropertyUpdated(changes, viewModel) {
  if (!changes.hasOwnProperty("model")) return false;
  const change = changes["model"];
  if (change.isFirstChange()) return true;
  return !Object.is(viewModel, change.currentValue);
}
function isBuiltInAccessor(valueAccessor) {
  return Object.getPrototypeOf(valueAccessor.constructor) === BuiltInControlValueAccessor;
}
function syncPendingControls(form, directives) {
  form._syncPendingControls();
  directives.forEach((dir) => {
    const control = dir.control;
    if (control.updateOn === "submit" && control._pendingChange) {
      dir.viewToModelUpdate(control._pendingValue);
      control._pendingChange = false;
    }
  });
}
function selectValueAccessor(dir, valueAccessors) {
  if (!valueAccessors) return null;
  if (!Array.isArray(valueAccessors) && (typeof ngDevMode === "undefined" || ngDevMode)) _throwInvalidValueAccessorError(dir);
  let defaultAccessor = void 0;
  let builtinAccessor = void 0;
  let customAccessor = void 0;
  valueAccessors.forEach((v) => {
    if (v.constructor === DefaultValueAccessor) {
      defaultAccessor = v;
    } else if (isBuiltInAccessor(v)) {
      if (builtinAccessor && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "More than one built-in value accessor matches form control with");
      builtinAccessor = v;
    } else {
      if (customAccessor && (typeof ngDevMode === "undefined" || ngDevMode)) _throwError(dir, "More than one custom value accessor matches form control with");
      customAccessor = v;
    }
  });
  if (customAccessor) return customAccessor;
  if (builtinAccessor) return builtinAccessor;
  if (defaultAccessor) return defaultAccessor;
  if (typeof ngDevMode === "undefined" || ngDevMode) {
    _throwError(dir, "No valid value accessor for form control with");
  }
  return null;
}
function removeListItem$1(list, el) {
  const index = list.indexOf(el);
  if (index > -1) list.splice(index, 1);
}
function _ngModelWarning(name, type, instance, warningConfig) {
  if (warningConfig === "never") return;
  if ((warningConfig === null || warningConfig === "once") && !type._ngModelWarningSentOnce || warningConfig === "always" && !instance._ngModelWarningSent) {
    console.warn(ngModelWarning(name));
    type._ngModelWarningSentOnce = true;
    instance._ngModelWarningSent = true;
  }
}
var formDirectiveProvider$2 = {
  provide: ControlContainer,
  useExisting: forwardRef(() => NgForm)
};
var resolvedPromise$1 = (() => Promise.resolve())();
var NgForm = class _NgForm extends ControlContainer {
  callSetDisabledState;
  get submitted() {
    return untracked2(this.submittedReactive);
  }
  _submitted = computed(() => this.submittedReactive(), ...ngDevMode ? [{
    debugName: "_submitted"
  }] : []);
  submittedReactive = signal(false, ...ngDevMode ? [{
    debugName: "submittedReactive"
  }] : []);
  _directives = /* @__PURE__ */ new Set();
  form;
  ngSubmit = new EventEmitter();
  options;
  constructor(validators, asyncValidators, callSetDisabledState) {
    super();
    this.callSetDisabledState = callSetDisabledState;
    this.form = new FormGroup({}, composeValidators(validators), composeAsyncValidators(asyncValidators));
  }
  ngAfterViewInit() {
    this._setUpdateStrategy();
  }
  get formDirective() {
    return this;
  }
  get control() {
    return this.form;
  }
  get path() {
    return [];
  }
  get controls() {
    return this.form.controls;
  }
  addControl(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      dir.control = container.registerControl(dir.name, dir.control);
      setUpControl(dir.control, dir, this.callSetDisabledState);
      dir.control.updateValueAndValidity({
        emitEvent: false
      });
      this._directives.add(dir);
    });
  }
  getControl(dir) {
    return this.form.get(dir.path);
  }
  removeControl(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      container?.removeControl(dir.name);
      this._directives.delete(dir);
    });
  }
  addFormGroup(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      const group = new FormGroup({});
      setUpFormContainer(group, dir);
      container.registerControl(dir.name, group);
      group.updateValueAndValidity({
        emitEvent: false
      });
    });
  }
  removeFormGroup(dir) {
    resolvedPromise$1.then(() => {
      const container = this._findContainer(dir.path);
      container?.removeControl?.(dir.name);
    });
  }
  getFormGroup(dir) {
    return this.form.get(dir.path);
  }
  updateModel(dir, value) {
    resolvedPromise$1.then(() => {
      const ctrl = this.form.get(dir.path);
      ctrl.setValue(value);
    });
  }
  setValue(value) {
    this.control.setValue(value);
  }
  onSubmit($event) {
    this.submittedReactive.set(true);
    syncPendingControls(this.form, this._directives);
    this.ngSubmit.emit($event);
    this.form._events.next(new FormSubmittedEvent(this.control));
    return $event?.target?.method === "dialog";
  }
  onReset() {
    this.resetForm();
  }
  resetForm(value = void 0) {
    this.form.reset(value);
    this.submittedReactive.set(false);
  }
  _setUpdateStrategy() {
    if (this.options && this.options.updateOn != null) {
      this.form._updateOn = this.options.updateOn;
    }
  }
  _findContainer(path) {
    path.pop();
    return path.length ? this.form.get(path) : this.form;
  }
  static \u0275fac = function NgForm_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgForm)(\u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10), \u0275\u0275directiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgForm,
    selectors: [["form", 3, "ngNoForm", "", 3, "formGroup", "", 3, "formArray", ""], ["ng-form"], ["", "ngForm", ""]],
    hostBindings: function NgForm_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("submit", function NgForm_submit_HostBindingHandler($event) {
          return ctx.onSubmit($event);
        })("reset", function NgForm_reset_HostBindingHandler() {
          return ctx.onReset();
        });
      }
    },
    inputs: {
      options: [0, "ngFormOptions", "options"]
    },
    outputs: {
      ngSubmit: "ngSubmit"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formDirectiveProvider$2]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgForm, [{
    type: Directive,
    args: [{
      selector: "form:not([ngNoForm]):not([formGroup]):not([formArray]),ng-form,[ngForm]",
      providers: [formDirectiveProvider$2],
      host: {
        "(submit)": "onSubmit($event)",
        "(reset)": "onReset()"
      },
      outputs: ["ngSubmit"],
      exportAs: "ngForm",
      standalone: false
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    options: [{
      type: Input,
      args: ["ngFormOptions"]
    }]
  });
})();
function removeListItem(list, el) {
  const index = list.indexOf(el);
  if (index > -1) list.splice(index, 1);
}
function isFormControlState(formState) {
  return typeof formState === "object" && formState !== null && Object.keys(formState).length === 2 && "value" in formState && "disabled" in formState;
}
var FormControl = class FormControl2 extends AbstractControl {
  defaultValue = null;
  _onChange = [];
  _pendingValue;
  _pendingChange = false;
  constructor(formState = null, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    this._applyFormState(formState);
    this._setUpdateStrategy(validatorOrOpts);
    this._initObservables();
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: !!this.asyncValidator
    });
    if (isOptionsObj(validatorOrOpts) && (validatorOrOpts.nonNullable || validatorOrOpts.initialValueIsDefault)) {
      if (isFormControlState(formState)) {
        this.defaultValue = formState.value;
      } else {
        this.defaultValue = formState;
      }
    }
  }
  setValue(value, options = {}) {
    this.value = this._pendingValue = value;
    if (this._onChange.length && options.emitModelToViewChange !== false) {
      this._onChange.forEach((changeFn) => changeFn(this.value, options.emitViewToModelChange !== false));
    }
    this.updateValueAndValidity(options);
  }
  patchValue(value, options = {}) {
    this.setValue(value, options);
  }
  reset(formState = this.defaultValue, options = {}) {
    this._applyFormState(formState);
    this.markAsPristine(options);
    this.markAsUntouched(options);
    this.setValue(this.value, options);
    if (options.overwriteDefaultValue) {
      this.defaultValue = this.value;
    }
    this._pendingChange = false;
    if (options?.emitEvent !== false) {
      this._events.next(new FormResetEvent(this));
    }
  }
  _updateValue() {
  }
  _anyControls(condition) {
    return false;
  }
  _allControlsDisabled() {
    return this.disabled;
  }
  registerOnChange(fn) {
    this._onChange.push(fn);
  }
  _unregisterOnChange(fn) {
    removeListItem(this._onChange, fn);
  }
  registerOnDisabledChange(fn) {
    this._onDisabledChange.push(fn);
  }
  _unregisterOnDisabledChange(fn) {
    removeListItem(this._onDisabledChange, fn);
  }
  _forEachChild(cb) {
  }
  _syncPendingControls() {
    if (this.updateOn === "submit") {
      if (this._pendingDirty) this.markAsDirty();
      if (this._pendingTouched) this.markAsTouched();
      if (this._pendingChange) {
        this.setValue(this._pendingValue, {
          onlySelf: true,
          emitModelToViewChange: false
        });
        return true;
      }
    }
    return false;
  }
  _applyFormState(formState) {
    if (isFormControlState(formState)) {
      this.value = this._pendingValue = formState.value;
      formState.disabled ? this.disable({
        onlySelf: true,
        emitEvent: false
      }) : this.enable({
        onlySelf: true,
        emitEvent: false
      });
    } else {
      this.value = this._pendingValue = formState;
    }
  }
};
var isFormControl = (control) => control instanceof FormControl;
var AbstractFormGroupDirective = class _AbstractFormGroupDirective extends ControlContainer {
  _parent;
  ngOnInit() {
    this._checkParentType();
    this.formDirective.addFormGroup(this);
  }
  ngOnDestroy() {
    this.formDirective?.removeFormGroup(this);
  }
  get control() {
    return this.formDirective.getFormGroup(this);
  }
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  _checkParentType() {
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275AbstractFormGroupDirective_BaseFactory;
    return function AbstractFormGroupDirective_Factory(__ngFactoryType__) {
      return (\u0275AbstractFormGroupDirective_BaseFactory || (\u0275AbstractFormGroupDirective_BaseFactory = \u0275\u0275getInheritedFactory(_AbstractFormGroupDirective)))(__ngFactoryType__ || _AbstractFormGroupDirective);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _AbstractFormGroupDirective,
    standalone: false,
    features: [\u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AbstractFormGroupDirective, [{
    type: Directive,
    args: [{
      standalone: false
    }]
  }], null, null);
})();
function modelParentException() {
  return new RuntimeError(1350, `
    ngModel cannot be used to register form controls with a parent formGroup directive.  Try using
    formGroup's partner directive "formControlName" instead.  Example:

    ${formControlNameExample}

    Or, if you'd like to avoid registering this form control, indicate that it's standalone in ngModelOptions:

    Example:

    ${ngModelWithFormGroupExample}`);
}
function formGroupNameException() {
  return new RuntimeError(1351, `
    ngModel cannot be used to register form controls with a parent formGroupName or formArrayName directive.

    Option 1: Use formControlName instead of ngModel (reactive strategy):

    ${formGroupNameExample}

    Option 2:  Update ngModel's parent be ngModelGroup (template-driven strategy):

    ${ngModelGroupExample}`);
}
function missingNameException() {
  return new RuntimeError(1352, `If ngModel is used within a form tag, either the name attribute must be set or the form
    control must be defined as 'standalone' in ngModelOptions.

    Example 1: <input [(ngModel)]="person.firstName" name="first">
    Example 2: <input [(ngModel)]="person.firstName" [ngModelOptions]="{standalone: true}">`);
}
function modelGroupParentException() {
  return new RuntimeError(1353, `
    ngModelGroup cannot be used with a parent formGroup directive.

    Option 1: Use formGroupName instead of ngModelGroup (reactive strategy):

    ${formGroupNameExample}

    Option 2:  Use a regular form tag instead of the formGroup directive (template-driven strategy):

    ${ngModelGroupExample}`);
}
var modelGroupProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => NgModelGroup)
};
var NgModelGroup = class _NgModelGroup extends AbstractFormGroupDirective {
  name = "";
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  _checkParentType() {
    if (!(this._parent instanceof _NgModelGroup) && !(this._parent instanceof NgForm) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw modelGroupParentException();
    }
  }
  static \u0275fac = function NgModelGroup_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgModelGroup)(\u0275\u0275directiveInject(ControlContainer, 5), \u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgModelGroup,
    selectors: [["", "ngModelGroup", ""]],
    inputs: {
      name: [0, "ngModelGroup", "name"]
    },
    exportAs: ["ngModelGroup"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([modelGroupProvider]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgModelGroup, [{
    type: Directive,
    args: [{
      selector: "[ngModelGroup]",
      providers: [modelGroupProvider],
      exportAs: "ngModelGroup",
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["ngModelGroup"]
    }]
  });
})();
var formControlBinding$1 = {
  provide: NgControl,
  useExisting: forwardRef(() => NgModel)
};
var resolvedPromise = (() => Promise.resolve())();
var NgModel = class _NgModel extends NgControl {
  _changeDetectorRef;
  callSetDisabledState;
  control = new FormControl();
  static ngAcceptInputType_isDisabled;
  _registered = false;
  viewModel;
  name = "";
  isDisabled;
  model;
  options;
  update = new EventEmitter();
  constructor(parent, validators, asyncValidators, valueAccessors, _changeDetectorRef, callSetDisabledState) {
    super();
    this._changeDetectorRef = _changeDetectorRef;
    this.callSetDisabledState = callSetDisabledState;
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  ngOnChanges(changes) {
    this._checkForErrors();
    if (!this._registered || "name" in changes) {
      if (this._registered) {
        this._checkName();
        if (this.formDirective) {
          const oldName = changes["name"].previousValue;
          this.formDirective.removeControl({
            name: oldName,
            path: this._getPath(oldName)
          });
        }
      }
      this._setUpControl();
    }
    if ("isDisabled" in changes) {
      this._updateDisabled(changes);
    }
    if (isPropertyUpdated(changes, this.viewModel)) {
      this._updateValue(this.model);
      this.viewModel = this.model;
    }
  }
  ngOnDestroy() {
    this.formDirective?.removeControl(this);
  }
  get path() {
    return this._getPath(this.name);
  }
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  _setUpControl() {
    this._setUpdateStrategy();
    this._isStandalone() ? this._setUpStandalone() : this.formDirective.addControl(this);
    this._registered = true;
  }
  _setUpdateStrategy() {
    if (this.options && this.options.updateOn != null) {
      this.control._updateOn = this.options.updateOn;
    }
  }
  _isStandalone() {
    return !this._parent || !!(this.options && this.options.standalone);
  }
  _setUpStandalone() {
    setUpControl(this.control, this, this.callSetDisabledState);
    this.control.updateValueAndValidity({
      emitEvent: false
    });
  }
  _checkForErrors() {
    if ((typeof ngDevMode === "undefined" || ngDevMode) && !this._isStandalone()) {
      checkParentType$1(this._parent);
    }
    this._checkName();
  }
  _checkName() {
    if (this.options && this.options.name) this.name = this.options.name;
    if (!this._isStandalone() && !this.name && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw missingNameException();
    }
  }
  _updateValue(value) {
    resolvedPromise.then(() => {
      this.control.setValue(value, {
        emitViewToModelChange: false
      });
      this._changeDetectorRef?.markForCheck();
    });
  }
  _updateDisabled(changes) {
    const disabledValue = changes["isDisabled"].currentValue;
    const isDisabled = disabledValue !== 0 && booleanAttribute(disabledValue);
    resolvedPromise.then(() => {
      if (isDisabled && !this.control.disabled) {
        this.control.disable();
      } else if (!isDisabled && this.control.disabled) {
        this.control.enable();
      }
      this._changeDetectorRef?.markForCheck();
    });
  }
  _getPath(controlName) {
    return this._parent ? controlPath(controlName, this._parent) : [controlName];
  }
  static \u0275fac = function NgModel_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgModel)(\u0275\u0275directiveInject(ControlContainer, 9), \u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10), \u0275\u0275directiveInject(NG_VALUE_ACCESSOR, 10), \u0275\u0275directiveInject(ChangeDetectorRef, 8), \u0275\u0275directiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgModel,
    selectors: [["", "ngModel", "", 3, "formControlName", "", 3, "formControl", ""]],
    inputs: {
      name: "name",
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"],
      options: [0, "ngModelOptions", "options"]
    },
    outputs: {
      update: "ngModelChange"
    },
    exportAs: ["ngModel"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formControlBinding$1]), \u0275\u0275InheritDefinitionFeature, \u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgModel, [{
    type: Directive,
    args: [{
      selector: "[ngModel]:not([formControlName]):not([formControl])",
      providers: [formControlBinding$1],
      exportAs: "ngModel",
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: ChangeDetectorRef,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [ChangeDetectorRef]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    name: [{
      type: Input
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    options: [{
      type: Input,
      args: ["ngModelOptions"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
function checkParentType$1(parent) {
  if (!(parent instanceof NgModelGroup) && parent instanceof AbstractFormGroupDirective) {
    throw formGroupNameException();
  } else if (!(parent instanceof NgModelGroup) && !(parent instanceof NgForm)) {
    throw modelParentException();
  }
}
var \u0275NgNoValidate = class _\u0275NgNoValidate {
  static \u0275fac = function \u0275NgNoValidate_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _\u0275NgNoValidate)();
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _\u0275NgNoValidate,
    selectors: [["form", 3, "ngNoForm", "", 3, "ngNativeValidate", ""]],
    hostAttrs: ["novalidate", ""],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(\u0275NgNoValidate, [{
    type: Directive,
    args: [{
      selector: "form:not([ngNoForm]):not([ngNativeValidate])",
      host: {
        "novalidate": ""
      },
      standalone: false
    }]
  }], null, null);
})();
var NUMBER_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => NumberValueAccessor),
  multi: true
};
var NumberValueAccessor = class _NumberValueAccessor extends BuiltInControlValueAccessor {
  writeValue(value) {
    const normalizedValue = value == null ? "" : value;
    this.setProperty("value", normalizedValue);
  }
  registerOnChange(fn) {
    this.onChange = (value) => {
      fn(value == "" ? null : parseFloat(value));
    };
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275NumberValueAccessor_BaseFactory;
    return function NumberValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275NumberValueAccessor_BaseFactory || (\u0275NumberValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_NumberValueAccessor)))(__ngFactoryType__ || _NumberValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NumberValueAccessor,
    selectors: [["input", "type", "number", "formControlName", ""], ["input", "type", "number", "formControl", ""], ["input", "type", "number", "ngModel", ""]],
    hostBindings: function NumberValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("input", function NumberValueAccessor_input_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function NumberValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([NUMBER_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NumberValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=number][formControlName],input[type=number][formControl],input[type=number][ngModel]",
      host: {
        "(input)": "onChange($any($event.target).value)",
        "(blur)": "onTouched()"
      },
      providers: [NUMBER_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var RADIO_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => RadioControlValueAccessor),
  multi: true
};
function throwNameError() {
  throw new RuntimeError(1202, `
      If you define both a name and a formControlName attribute on your radio button, their values
      must match. Ex: <input type="radio" formControlName="food" name="food">
    `);
}
var RadioControlRegistry = class _RadioControlRegistry {
  _accessors = [];
  add(control, accessor) {
    this._accessors.push([control, accessor]);
  }
  remove(accessor) {
    for (let i = this._accessors.length - 1; i >= 0; --i) {
      if (this._accessors[i][1] === accessor) {
        this._accessors.splice(i, 1);
        return;
      }
    }
  }
  select(accessor) {
    this._accessors.forEach((c) => {
      if (this._isSameGroup(c, accessor) && c[1] !== accessor) {
        c[1].fireUncheck(accessor.value);
      }
    });
  }
  _isSameGroup(controlPair, accessor) {
    if (!controlPair[0].control) return false;
    return controlPair[0]._parent === accessor._control._parent && controlPair[1].name === accessor.name;
  }
  static \u0275fac = function RadioControlRegistry_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RadioControlRegistry)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _RadioControlRegistry,
    factory: _RadioControlRegistry.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RadioControlRegistry, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var RadioControlValueAccessor = class _RadioControlValueAccessor extends BuiltInControlValueAccessor {
  _registry;
  _injector;
  _state;
  _control;
  _fn;
  setDisabledStateFired = false;
  onChange = () => {
  };
  name;
  formControlName;
  value;
  callSetDisabledState = inject2(CALL_SET_DISABLED_STATE, {
    optional: true
  }) ?? setDisabledStateDefault;
  constructor(renderer, elementRef, _registry, _injector) {
    super(renderer, elementRef);
    this._registry = _registry;
    this._injector = _injector;
  }
  ngOnInit() {
    this._control = this._injector.get(NgControl);
    this._checkName();
    this._registry.add(this._control, this);
  }
  ngOnDestroy() {
    this._registry.remove(this);
  }
  writeValue(value) {
    this._state = value === this.value;
    this.setProperty("checked", this._state);
  }
  registerOnChange(fn) {
    this._fn = fn;
    this.onChange = () => {
      fn(this.value);
      this._registry.select(this);
    };
  }
  setDisabledState(isDisabled) {
    if (this.setDisabledStateFired || isDisabled || this.callSetDisabledState === "whenDisabledForLegacyCode") {
      this.setProperty("disabled", isDisabled);
    }
    this.setDisabledStateFired = true;
  }
  fireUncheck(value) {
    this.writeValue(value);
  }
  _checkName() {
    if (this.name && this.formControlName && this.name !== this.formControlName && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throwNameError();
    }
    if (!this.name && this.formControlName) this.name = this.formControlName;
  }
  static \u0275fac = function RadioControlValueAccessor_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _RadioControlValueAccessor)(\u0275\u0275directiveInject(Renderer2), \u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(RadioControlRegistry), \u0275\u0275directiveInject(Injector));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _RadioControlValueAccessor,
    selectors: [["input", "type", "radio", "formControlName", ""], ["input", "type", "radio", "formControl", ""], ["input", "type", "radio", "ngModel", ""]],
    hostBindings: function RadioControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("change", function RadioControlValueAccessor_change_HostBindingHandler() {
          return ctx.onChange();
        })("blur", function RadioControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      name: "name",
      formControlName: "formControlName",
      value: "value"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([RADIO_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RadioControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=radio][formControlName],input[type=radio][formControl],input[type=radio][ngModel]",
      host: {
        "(change)": "onChange()",
        "(blur)": "onTouched()"
      },
      providers: [RADIO_VALUE_ACCESSOR],
      standalone: false
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: ElementRef
  }, {
    type: RadioControlRegistry
  }, {
    type: Injector
  }], {
    name: [{
      type: Input
    }],
    formControlName: [{
      type: Input
    }],
    value: [{
      type: Input
    }]
  });
})();
var RANGE_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => RangeValueAccessor),
  multi: true
};
var RangeValueAccessor = class _RangeValueAccessor extends BuiltInControlValueAccessor {
  writeValue(value) {
    this.setProperty("value", parseFloat(value));
  }
  registerOnChange(fn) {
    this.onChange = (value) => {
      fn(value == "" ? null : parseFloat(value));
    };
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275RangeValueAccessor_BaseFactory;
    return function RangeValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275RangeValueAccessor_BaseFactory || (\u0275RangeValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_RangeValueAccessor)))(__ngFactoryType__ || _RangeValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _RangeValueAccessor,
    selectors: [["input", "type", "range", "formControlName", ""], ["input", "type", "range", "formControl", ""], ["input", "type", "range", "ngModel", ""]],
    hostBindings: function RangeValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("change", function RangeValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("input", function RangeValueAccessor_input_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function RangeValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([RANGE_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RangeValueAccessor, [{
    type: Directive,
    args: [{
      selector: "input[type=range][formControlName],input[type=range][formControl],input[type=range][ngModel]",
      host: {
        "(change)": "onChange($any($event.target).value)",
        "(input)": "onChange($any($event.target).value)",
        "(blur)": "onTouched()"
      },
      providers: [RANGE_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, null);
})();
var FormArray = class extends AbstractControl {
  constructor(controls, validatorOrOpts, asyncValidator) {
    super(pickValidators(validatorOrOpts), pickAsyncValidators(asyncValidator, validatorOrOpts));
    this.controls = controls;
    this._initObservables();
    this._setUpdateStrategy(validatorOrOpts);
    this._setUpControls();
    this.updateValueAndValidity({
      onlySelf: true,
      emitEvent: !!this.asyncValidator
    });
  }
  controls;
  at(index) {
    return this.controls[this._adjustIndex(index)];
  }
  push(control, options = {}) {
    if (Array.isArray(control)) {
      control.forEach((ctrl) => {
        this.controls.push(ctrl);
        this._registerControl(ctrl);
      });
    } else {
      this.controls.push(control);
      this._registerControl(control);
    }
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  insert(index, control, options = {}) {
    this.controls.splice(index, 0, control);
    this._registerControl(control);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  removeAt(index, options = {}) {
    let adjustedIndex = this._adjustIndex(index);
    if (adjustedIndex < 0) adjustedIndex = 0;
    if (this.controls[adjustedIndex]) this.controls[adjustedIndex]._registerOnCollectionChange(() => {
    });
    this.controls.splice(adjustedIndex, 1);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  setControl(index, control, options = {}) {
    let adjustedIndex = this._adjustIndex(index);
    if (adjustedIndex < 0) adjustedIndex = 0;
    if (this.controls[adjustedIndex]) this.controls[adjustedIndex]._registerOnCollectionChange(() => {
    });
    this.controls.splice(adjustedIndex, 1);
    if (control) {
      this.controls.splice(adjustedIndex, 0, control);
      this._registerControl(control);
    }
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
    this._onCollectionChange();
  }
  get length() {
    return this.controls.length;
  }
  setValue(value, options = {}) {
    assertAllValuesPresent(this, false, value);
    value.forEach((newValue, index) => {
      assertControlPresent(this, false, index);
      this.at(index).setValue(newValue, {
        onlySelf: true,
        emitEvent: options.emitEvent
      });
    });
    this.updateValueAndValidity(options);
  }
  patchValue(value, options = {}) {
    if (value == null) return;
    value.forEach((newValue, index) => {
      if (this.at(index)) {
        this.at(index).patchValue(newValue, {
          onlySelf: true,
          emitEvent: options.emitEvent
        });
      }
    });
    this.updateValueAndValidity(options);
  }
  reset(value = [], options = {}) {
    this._forEachChild((control, index) => {
      control.reset(value[index], __spreadProps(__spreadValues({}, options), {
        onlySelf: true
      }));
    });
    this._updatePristine(options, this);
    this._updateTouched(options, this);
    this.updateValueAndValidity(options);
    if (options?.emitEvent !== false) {
      this._events.next(new FormResetEvent(this));
    }
  }
  getRawValue() {
    return this.controls.map((control) => control.getRawValue());
  }
  clear(options = {}) {
    if (this.controls.length < 1) return;
    this._forEachChild((control) => control._registerOnCollectionChange(() => {
    }));
    this.controls.splice(0);
    this.updateValueAndValidity({
      emitEvent: options.emitEvent
    });
  }
  _adjustIndex(index) {
    return index < 0 ? index + this.length : index;
  }
  _syncPendingControls() {
    let subtreeUpdated = this.controls.reduce((updated, child) => {
      return child._syncPendingControls() ? true : updated;
    }, false);
    if (subtreeUpdated) this.updateValueAndValidity({
      onlySelf: true
    });
    return subtreeUpdated;
  }
  _forEachChild(cb) {
    this.controls.forEach((control, index) => {
      cb(control, index);
    });
  }
  _updateValue() {
    this.value = this.controls.filter((control) => control.enabled || this.disabled).map((control) => control.value);
  }
  _anyControls(condition) {
    return this.controls.some((control) => control.enabled && condition(control));
  }
  _setUpControls() {
    this._forEachChild((control) => this._registerControl(control));
  }
  _allControlsDisabled() {
    for (const control of this.controls) {
      if (control.enabled) return false;
    }
    return this.controls.length > 0 || this.disabled;
  }
  _registerControl(control) {
    control.setParent(this);
    control._registerOnCollectionChange(this._onCollectionChange);
  }
  _find(name) {
    return this.at(name) ?? null;
  }
};
var AbstractFormDirective = class _AbstractFormDirective extends ControlContainer {
  callSetDisabledState;
  get submitted() {
    return untracked2(this._submittedReactive);
  }
  set submitted(value) {
    this._submittedReactive.set(value);
  }
  _submitted = computed(() => this._submittedReactive(), ...ngDevMode ? [{
    debugName: "_submitted"
  }] : []);
  _submittedReactive = signal(false, ...ngDevMode ? [{
    debugName: "_submittedReactive"
  }] : []);
  _oldForm;
  _onCollectionChange = () => this._updateDomValue();
  directives = [];
  constructor(validators, asyncValidators, callSetDisabledState) {
    super();
    this.callSetDisabledState = callSetDisabledState;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  ngOnChanges(changes) {
    this.onChanges(changes);
  }
  ngOnDestroy() {
    this.onDestroy();
  }
  onChanges(changes) {
    this._checkFormPresent();
    if (changes.hasOwnProperty("form")) {
      this._updateValidators();
      this._updateDomValue();
      this._updateRegistrations();
      this._oldForm = this.form;
    }
  }
  onDestroy() {
    if (this.form) {
      cleanUpValidators(this.form, this);
      if (this.form._onCollectionChange === this._onCollectionChange) {
        this.form._registerOnCollectionChange(() => {
        });
      }
    }
  }
  get formDirective() {
    return this;
  }
  get path() {
    return [];
  }
  addControl(dir) {
    const ctrl = this.form.get(dir.path);
    setUpControl(ctrl, dir, this.callSetDisabledState);
    ctrl.updateValueAndValidity({
      emitEvent: false
    });
    this.directives.push(dir);
    return ctrl;
  }
  getControl(dir) {
    return this.form.get(dir.path);
  }
  removeControl(dir) {
    cleanUpControl(dir.control || null, dir, false);
    removeListItem$1(this.directives, dir);
  }
  addFormGroup(dir) {
    this._setUpFormContainer(dir);
  }
  removeFormGroup(dir) {
    this._cleanUpFormContainer(dir);
  }
  getFormGroup(dir) {
    return this.form.get(dir.path);
  }
  getFormArray(dir) {
    return this.form.get(dir.path);
  }
  addFormArray(dir) {
    this._setUpFormContainer(dir);
  }
  removeFormArray(dir) {
    this._cleanUpFormContainer(dir);
  }
  updateModel(dir, value) {
    const ctrl = this.form.get(dir.path);
    ctrl.setValue(value);
  }
  onReset() {
    this.resetForm();
  }
  resetForm(value = void 0, options = {}) {
    this.form.reset(value, options);
    this._submittedReactive.set(false);
  }
  onSubmit($event) {
    this.submitted = true;
    syncPendingControls(this.form, this.directives);
    this.ngSubmit.emit($event);
    this.form._events.next(new FormSubmittedEvent(this.control));
    return $event?.target?.method === "dialog";
  }
  _updateDomValue() {
    this.directives.forEach((dir) => {
      const oldCtrl = dir.control;
      const newCtrl = this.form.get(dir.path);
      if (oldCtrl !== newCtrl) {
        cleanUpControl(oldCtrl || null, dir);
        if (isFormControl(newCtrl)) {
          setUpControl(newCtrl, dir, this.callSetDisabledState);
          dir.control = newCtrl;
        }
      }
    });
    this.form._updateTreeValidity({
      emitEvent: false
    });
  }
  _setUpFormContainer(dir) {
    const ctrl = this.form.get(dir.path);
    setUpFormContainer(ctrl, dir);
    ctrl.updateValueAndValidity({
      emitEvent: false
    });
  }
  _cleanUpFormContainer(dir) {
    const ctrl = this.form?.get(dir.path);
    if (ctrl) {
      const isControlUpdated = cleanUpFormContainer(ctrl, dir);
      if (isControlUpdated) {
        ctrl.updateValueAndValidity({
          emitEvent: false
        });
      }
    }
  }
  _updateRegistrations() {
    this.form._registerOnCollectionChange(this._onCollectionChange);
    this._oldForm?._registerOnCollectionChange(() => {
    });
  }
  _updateValidators() {
    setUpValidators(this.form, this);
    if (this._oldForm) {
      cleanUpValidators(this._oldForm, this);
    }
  }
  _checkFormPresent() {
    if (!this.form && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw missingFormException();
    }
  }
  static \u0275fac = function AbstractFormDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AbstractFormDirective)(\u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10), \u0275\u0275directiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _AbstractFormDirective,
    features: [\u0275\u0275InheritDefinitionFeature, \u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AbstractFormDirective, [{
    type: Directive
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], null);
})();
var formDirectiveProvider$1 = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormArrayDirective)
};
var FormArrayDirective = class _FormArrayDirective extends AbstractFormDirective {
  form = null;
  ngSubmit = new EventEmitter();
  get control() {
    return this.form;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275FormArrayDirective_BaseFactory;
    return function FormArrayDirective_Factory(__ngFactoryType__) {
      return (\u0275FormArrayDirective_BaseFactory || (\u0275FormArrayDirective_BaseFactory = \u0275\u0275getInheritedFactory(_FormArrayDirective)))(__ngFactoryType__ || _FormArrayDirective);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormArrayDirective,
    selectors: [["", "formArray", ""]],
    hostBindings: function FormArrayDirective_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("submit", function FormArrayDirective_submit_HostBindingHandler($event) {
          return ctx.onSubmit($event);
        })("reset", function FormArrayDirective_reset_HostBindingHandler() {
          return ctx.onReset();
        });
      }
    },
    inputs: {
      form: [0, "formArray", "form"]
    },
    outputs: {
      ngSubmit: "ngSubmit"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formDirectiveProvider$1]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormArrayDirective, [{
    type: Directive,
    args: [{
      selector: "[formArray]",
      providers: [formDirectiveProvider$1],
      host: {
        "(submit)": "onSubmit($event)",
        "(reset)": "onReset()"
      },
      exportAs: "ngForm",
      standalone: false
    }]
  }], null, {
    form: [{
      type: Input,
      args: ["formArray"]
    }],
    ngSubmit: [{
      type: Output
    }]
  });
})();
var NG_MODEL_WITH_FORM_CONTROL_WARNING = new InjectionToken(typeof ngDevMode !== "undefined" && ngDevMode ? "NgModelWithFormControlWarning" : "");
var formControlBinding = {
  provide: NgControl,
  useExisting: forwardRef(() => FormControlDirective)
};
var FormControlDirective = class _FormControlDirective extends NgControl {
  _ngModelWarningConfig;
  callSetDisabledState;
  viewModel;
  form;
  set isDisabled(isDisabled) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      console.warn(disabledAttrWarning);
    }
  }
  model;
  update = new EventEmitter();
  static _ngModelWarningSentOnce = false;
  _ngModelWarningSent = false;
  constructor(validators, asyncValidators, valueAccessors, _ngModelWarningConfig, callSetDisabledState) {
    super();
    this._ngModelWarningConfig = _ngModelWarningConfig;
    this.callSetDisabledState = callSetDisabledState;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  ngOnChanges(changes) {
    if (this._isControlChanged(changes)) {
      const previousForm = changes["form"].previousValue;
      if (previousForm) {
        cleanUpControl(previousForm, this, false);
      }
      setUpControl(this.form, this, this.callSetDisabledState);
      this.form.updateValueAndValidity({
        emitEvent: false
      });
    }
    if (isPropertyUpdated(changes, this.viewModel)) {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        _ngModelWarning("formControl", _FormControlDirective, this, this._ngModelWarningConfig);
      }
      this.form.setValue(this.model);
      this.viewModel = this.model;
    }
  }
  ngOnDestroy() {
    if (this.form) {
      cleanUpControl(this.form, this, false);
    }
  }
  get path() {
    return [];
  }
  get control() {
    return this.form;
  }
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  _isControlChanged(changes) {
    return changes.hasOwnProperty("form");
  }
  static \u0275fac = function FormControlDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormControlDirective)(\u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10), \u0275\u0275directiveInject(NG_VALUE_ACCESSOR, 10), \u0275\u0275directiveInject(NG_MODEL_WITH_FORM_CONTROL_WARNING, 8), \u0275\u0275directiveInject(CALL_SET_DISABLED_STATE, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormControlDirective,
    selectors: [["", "formControl", ""]],
    inputs: {
      form: [0, "formControl", "form"],
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"]
    },
    outputs: {
      update: "ngModelChange"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formControlBinding]), \u0275\u0275InheritDefinitionFeature, \u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormControlDirective, [{
    type: Directive,
    args: [{
      selector: "[formControl]",
      providers: [formControlBinding],
      exportAs: "ngForm",
      standalone: false
    }]
  }], () => [{
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [NG_MODEL_WITH_FORM_CONTROL_WARNING]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [CALL_SET_DISABLED_STATE]
    }]
  }], {
    form: [{
      type: Input,
      args: ["formControl"]
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
var formGroupNameProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormGroupName)
};
var FormGroupName = class _FormGroupName extends AbstractFormGroupDirective {
  name = null;
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  _checkParentType() {
    if (hasInvalidParent(this._parent) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw groupParentException();
    }
  }
  static \u0275fac = function FormGroupName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormGroupName)(\u0275\u0275directiveInject(ControlContainer, 13), \u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormGroupName,
    selectors: [["", "formGroupName", ""]],
    inputs: {
      name: [0, "formGroupName", "name"]
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formGroupNameProvider]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormGroupName, [{
    type: Directive,
    args: [{
      selector: "[formGroupName]",
      providers: [formGroupNameProvider],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formGroupName"]
    }]
  });
})();
var formArrayNameProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormArrayName)
};
var FormArrayName = class _FormArrayName extends ControlContainer {
  _parent;
  name = null;
  constructor(parent, validators, asyncValidators) {
    super();
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
  }
  ngOnInit() {
    if (hasInvalidParent(this._parent) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw arrayParentException();
    }
    this.formDirective.addFormArray(this);
  }
  ngOnDestroy() {
    this.formDirective?.removeFormArray(this);
  }
  get control() {
    return this.formDirective.getFormArray(this);
  }
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  static \u0275fac = function FormArrayName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormArrayName)(\u0275\u0275directiveInject(ControlContainer, 13), \u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormArrayName,
    selectors: [["", "formArrayName", ""]],
    inputs: {
      name: [0, "formArrayName", "name"]
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formArrayNameProvider]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormArrayName, [{
    type: Directive,
    args: [{
      selector: "[formArrayName]",
      providers: [formArrayNameProvider],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formArrayName"]
    }]
  });
})();
function hasInvalidParent(parent) {
  return !(parent instanceof FormGroupName) && !(parent instanceof AbstractFormDirective) && !(parent instanceof FormArrayName);
}
var controlNameBinding = {
  provide: NgControl,
  useExisting: forwardRef(() => FormControlName)
};
var FormControlName = class _FormControlName extends NgControl {
  _ngModelWarningConfig;
  _added = false;
  viewModel;
  control;
  name = null;
  set isDisabled(isDisabled) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      console.warn(disabledAttrWarning);
    }
  }
  model;
  update = new EventEmitter();
  static _ngModelWarningSentOnce = false;
  _ngModelWarningSent = false;
  constructor(parent, validators, asyncValidators, valueAccessors, _ngModelWarningConfig) {
    super();
    this._ngModelWarningConfig = _ngModelWarningConfig;
    this._parent = parent;
    this._setValidators(validators);
    this._setAsyncValidators(asyncValidators);
    this.valueAccessor = selectValueAccessor(this, valueAccessors);
  }
  ngOnChanges(changes) {
    if (!this._added) this._setUpControl();
    if (isPropertyUpdated(changes, this.viewModel)) {
      if (typeof ngDevMode === "undefined" || ngDevMode) {
        _ngModelWarning("formControlName", _FormControlName, this, this._ngModelWarningConfig);
      }
      this.viewModel = this.model;
      this.formDirective.updateModel(this, this.model);
    }
  }
  ngOnDestroy() {
    this.formDirective?.removeControl(this);
  }
  viewToModelUpdate(newValue) {
    this.viewModel = newValue;
    this.update.emit(newValue);
  }
  get path() {
    return controlPath(this.name == null ? this.name : this.name.toString(), this._parent);
  }
  get formDirective() {
    return this._parent ? this._parent.formDirective : null;
  }
  _setUpControl() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      checkParentType(this._parent, this.name);
    }
    this.control = this.formDirective.addControl(this);
    this._added = true;
  }
  static \u0275fac = function FormControlName_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormControlName)(\u0275\u0275directiveInject(ControlContainer, 13), \u0275\u0275directiveInject(NG_VALIDATORS, 10), \u0275\u0275directiveInject(NG_ASYNC_VALIDATORS, 10), \u0275\u0275directiveInject(NG_VALUE_ACCESSOR, 10), \u0275\u0275directiveInject(NG_MODEL_WITH_FORM_CONTROL_WARNING, 8));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormControlName,
    selectors: [["", "formControlName", ""]],
    inputs: {
      name: [0, "formControlName", "name"],
      isDisabled: [0, "disabled", "isDisabled"],
      model: [0, "ngModel", "model"]
    },
    outputs: {
      update: "ngModelChange"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([controlNameBinding]), \u0275\u0275InheritDefinitionFeature, \u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormControlName, [{
    type: Directive,
    args: [{
      selector: "[formControlName]",
      providers: [controlNameBinding],
      standalone: false
    }]
  }], () => [{
    type: ControlContainer,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }, {
      type: SkipSelf
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_ASYNC_VALIDATORS]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Self
    }, {
      type: Inject,
      args: [NG_VALUE_ACCESSOR]
    }]
  }, {
    type: void 0,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [NG_MODEL_WITH_FORM_CONTROL_WARNING]
    }]
  }], {
    name: [{
      type: Input,
      args: ["formControlName"]
    }],
    isDisabled: [{
      type: Input,
      args: ["disabled"]
    }],
    model: [{
      type: Input,
      args: ["ngModel"]
    }],
    update: [{
      type: Output,
      args: ["ngModelChange"]
    }]
  });
})();
function checkParentType(parent, name) {
  if (!(parent instanceof FormGroupName) && parent instanceof AbstractFormGroupDirective) {
    throw ngModelGroupException();
  } else if (!(parent instanceof FormGroupName) && !(parent instanceof AbstractFormDirective) && !(parent instanceof FormArrayName)) {
    throw controlParentException(name);
  }
}
var formDirectiveProvider = {
  provide: ControlContainer,
  useExisting: forwardRef(() => FormGroupDirective)
};
var FormGroupDirective = class _FormGroupDirective extends AbstractFormDirective {
  form = null;
  ngSubmit = new EventEmitter();
  get control() {
    return this.form;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275FormGroupDirective_BaseFactory;
    return function FormGroupDirective_Factory(__ngFactoryType__) {
      return (\u0275FormGroupDirective_BaseFactory || (\u0275FormGroupDirective_BaseFactory = \u0275\u0275getInheritedFactory(_FormGroupDirective)))(__ngFactoryType__ || _FormGroupDirective);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _FormGroupDirective,
    selectors: [["", "formGroup", ""]],
    hostBindings: function FormGroupDirective_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("submit", function FormGroupDirective_submit_HostBindingHandler($event) {
          return ctx.onSubmit($event);
        })("reset", function FormGroupDirective_reset_HostBindingHandler() {
          return ctx.onReset();
        });
      }
    },
    inputs: {
      form: [0, "formGroup", "form"]
    },
    outputs: {
      ngSubmit: "ngSubmit"
    },
    exportAs: ["ngForm"],
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([formDirectiveProvider]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormGroupDirective, [{
    type: Directive,
    args: [{
      selector: "[formGroup]",
      providers: [formDirectiveProvider],
      host: {
        "(submit)": "onSubmit($event)",
        "(reset)": "onReset()"
      },
      exportAs: "ngForm",
      standalone: false
    }]
  }], null, {
    form: [{
      type: Input,
      args: ["formGroup"]
    }],
    ngSubmit: [{
      type: Output
    }]
  });
})();
var SELECT_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => SelectControlValueAccessor),
  multi: true
};
function _buildValueString$1(id, value) {
  if (id == null) return `${value}`;
  if (value && typeof value === "object") value = "Object";
  return `${id}: ${value}`.slice(0, 50);
}
function _extractId$1(valueString) {
  return valueString.split(":")[0];
}
var SelectControlValueAccessor = class _SelectControlValueAccessor extends BuiltInControlValueAccessor {
  value;
  _optionMap = /* @__PURE__ */ new Map();
  _idCounter = 0;
  set compareWith(fn) {
    if (typeof fn !== "function" && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw new RuntimeError(1201, `compareWith must be a function, but received ${JSON.stringify(fn)}`);
    }
    this._compareWith = fn;
  }
  _compareWith = Object.is;
  appRefInjector = inject2(ApplicationRef).injector;
  destroyRef = inject2(DestroyRef);
  cdr = inject2(ChangeDetectorRef);
  _queuedWrite = false;
  _writeValueAfterRender() {
    if (this._queuedWrite || this.appRefInjector.destroyed) {
      return;
    }
    this._queuedWrite = true;
    afterNextRender({
      write: () => {
        if (this.destroyRef.destroyed) {
          return;
        }
        this._queuedWrite = false;
        this.writeValue(this.value);
      }
    }, {
      injector: this.appRefInjector
    });
  }
  writeValue(value) {
    this.cdr.markForCheck();
    this.value = value;
    const id = this._getOptionId(value);
    const valueString = _buildValueString$1(id, value);
    this.setProperty("value", valueString);
  }
  registerOnChange(fn) {
    this.onChange = (valueString) => {
      this.value = this._getOptionValue(valueString);
      fn(this.value);
    };
  }
  _registerOption() {
    return (this._idCounter++).toString();
  }
  _getOptionId(value) {
    for (const id of this._optionMap.keys()) {
      if (this._compareWith(this._optionMap.get(id), value)) return id;
    }
    return null;
  }
  _getOptionValue(valueString) {
    const id = _extractId$1(valueString);
    return this._optionMap.has(id) ? this._optionMap.get(id) : valueString;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275SelectControlValueAccessor_BaseFactory;
    return function SelectControlValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275SelectControlValueAccessor_BaseFactory || (\u0275SelectControlValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_SelectControlValueAccessor)))(__ngFactoryType__ || _SelectControlValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _SelectControlValueAccessor,
    selectors: [["select", "formControlName", "", 3, "multiple", ""], ["select", "formControl", "", 3, "multiple", ""], ["select", "ngModel", "", 3, "multiple", ""]],
    hostBindings: function SelectControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("change", function SelectControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target.value);
        })("blur", function SelectControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      compareWith: "compareWith"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([SELECT_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SelectControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "select:not([multiple])[formControlName],select:not([multiple])[formControl],select:not([multiple])[ngModel]",
      host: {
        "(change)": "onChange($any($event.target).value)",
        "(blur)": "onTouched()"
      },
      providers: [SELECT_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, {
    compareWith: [{
      type: Input
    }]
  });
})();
var NgSelectOption = class _NgSelectOption {
  _element;
  _renderer;
  _select;
  id;
  constructor(_element, _renderer, _select) {
    this._element = _element;
    this._renderer = _renderer;
    this._select = _select;
    if (this._select) this.id = this._select._registerOption();
  }
  set ngValue(value) {
    if (this._select == null) return;
    this._select._optionMap.set(this.id, value);
    this._setElementValue(_buildValueString$1(this.id, value));
    this._select._writeValueAfterRender();
  }
  set value(value) {
    this._setElementValue(value);
    this._select?._writeValueAfterRender();
  }
  _setElementValue(value) {
    this._renderer.setProperty(this._element.nativeElement, "value", value);
  }
  ngOnDestroy() {
    this._select?._optionMap.delete(this.id);
    this._select?._writeValueAfterRender();
  }
  static \u0275fac = function NgSelectOption_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NgSelectOption)(\u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(Renderer2), \u0275\u0275directiveInject(SelectControlValueAccessor, 9));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _NgSelectOption,
    selectors: [["option"]],
    inputs: {
      ngValue: "ngValue",
      value: "value"
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NgSelectOption, [{
    type: Directive,
    args: [{
      selector: "option",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }, {
    type: SelectControlValueAccessor,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], {
    ngValue: [{
      type: Input,
      args: ["ngValue"]
    }],
    value: [{
      type: Input,
      args: ["value"]
    }]
  });
})();
var SELECT_MULTIPLE_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => SelectMultipleControlValueAccessor),
  multi: true
};
function _buildValueString(id, value) {
  if (id == null) return `${value}`;
  if (typeof value === "string") value = `'${value}'`;
  if (value && typeof value === "object") value = "Object";
  return `${id}: ${value}`.slice(0, 50);
}
function _extractId(valueString) {
  return valueString.split(":")[0];
}
var SelectMultipleControlValueAccessor = class _SelectMultipleControlValueAccessor extends BuiltInControlValueAccessor {
  value;
  _optionMap = /* @__PURE__ */ new Map();
  _idCounter = 0;
  set compareWith(fn) {
    if (typeof fn !== "function" && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw new RuntimeError(1201, `compareWith must be a function, but received ${JSON.stringify(fn)}`);
    }
    this._compareWith = fn;
  }
  _compareWith = Object.is;
  writeValue(value) {
    this.value = value;
    let optionSelectedStateSetter;
    if (Array.isArray(value)) {
      const ids = value.map((v) => this._getOptionId(v));
      optionSelectedStateSetter = (opt, id) => {
        opt._setSelected(ids.indexOf(id) > -1);
      };
    } else {
      optionSelectedStateSetter = (opt) => {
        opt._setSelected(false);
      };
    }
    this._optionMap.forEach(optionSelectedStateSetter);
  }
  registerOnChange(fn) {
    this.onChange = (element) => {
      const selected = [];
      const selectedOptions = element.selectedOptions;
      if (selectedOptions !== void 0) {
        const options = selectedOptions;
        for (let i = 0; i < options.length; i++) {
          const opt = options[i];
          const val = this._getOptionValue(opt.value);
          selected.push(val);
        }
      } else {
        const options = element.options;
        for (let i = 0; i < options.length; i++) {
          const opt = options[i];
          if (opt.selected) {
            const val = this._getOptionValue(opt.value);
            selected.push(val);
          }
        }
      }
      this.value = selected;
      fn(selected);
    };
  }
  _registerOption(value) {
    const id = (this._idCounter++).toString();
    this._optionMap.set(id, value);
    return id;
  }
  _getOptionId(value) {
    for (const id of this._optionMap.keys()) {
      if (this._compareWith(this._optionMap.get(id)._value, value)) return id;
    }
    return null;
  }
  _getOptionValue(valueString) {
    const id = _extractId(valueString);
    return this._optionMap.has(id) ? this._optionMap.get(id)._value : valueString;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275SelectMultipleControlValueAccessor_BaseFactory;
    return function SelectMultipleControlValueAccessor_Factory(__ngFactoryType__) {
      return (\u0275SelectMultipleControlValueAccessor_BaseFactory || (\u0275SelectMultipleControlValueAccessor_BaseFactory = \u0275\u0275getInheritedFactory(_SelectMultipleControlValueAccessor)))(__ngFactoryType__ || _SelectMultipleControlValueAccessor);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _SelectMultipleControlValueAccessor,
    selectors: [["select", "multiple", "", "formControlName", ""], ["select", "multiple", "", "formControl", ""], ["select", "multiple", "", "ngModel", ""]],
    hostBindings: function SelectMultipleControlValueAccessor_HostBindings(rf, ctx) {
      if (rf & 1) {
        \u0275\u0275listener("change", function SelectMultipleControlValueAccessor_change_HostBindingHandler($event) {
          return ctx.onChange($event.target);
        })("blur", function SelectMultipleControlValueAccessor_blur_HostBindingHandler() {
          return ctx.onTouched();
        });
      }
    },
    inputs: {
      compareWith: "compareWith"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([SELECT_MULTIPLE_VALUE_ACCESSOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(SelectMultipleControlValueAccessor, [{
    type: Directive,
    args: [{
      selector: "select[multiple][formControlName],select[multiple][formControl],select[multiple][ngModel]",
      host: {
        "(change)": "onChange($event.target)",
        "(blur)": "onTouched()"
      },
      providers: [SELECT_MULTIPLE_VALUE_ACCESSOR],
      standalone: false
    }]
  }], null, {
    compareWith: [{
      type: Input
    }]
  });
})();
var \u0275NgSelectMultipleOption = class _\u0275NgSelectMultipleOption {
  _element;
  _renderer;
  _select;
  id;
  _value;
  constructor(_element, _renderer, _select) {
    this._element = _element;
    this._renderer = _renderer;
    this._select = _select;
    if (this._select) {
      this.id = this._select._registerOption(this);
    }
  }
  set ngValue(value) {
    if (this._select == null) return;
    this._value = value;
    this._setElementValue(_buildValueString(this.id, value));
    this._select.writeValue(this._select.value);
  }
  set value(value) {
    if (this._select) {
      this._value = value;
      this._setElementValue(_buildValueString(this.id, value));
      this._select.writeValue(this._select.value);
    } else {
      this._setElementValue(value);
    }
  }
  _setElementValue(value) {
    this._renderer.setProperty(this._element.nativeElement, "value", value);
  }
  _setSelected(selected) {
    this._renderer.setProperty(this._element.nativeElement, "selected", selected);
  }
  ngOnDestroy() {
    if (this._select) {
      this._select._optionMap.delete(this.id);
      this._select.writeValue(this._select.value);
    }
  }
  static \u0275fac = function \u0275NgSelectMultipleOption_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _\u0275NgSelectMultipleOption)(\u0275\u0275directiveInject(ElementRef), \u0275\u0275directiveInject(Renderer2), \u0275\u0275directiveInject(SelectMultipleControlValueAccessor, 9));
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _\u0275NgSelectMultipleOption,
    selectors: [["option"]],
    inputs: {
      ngValue: "ngValue",
      value: "value"
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(\u0275NgSelectMultipleOption, [{
    type: Directive,
    args: [{
      selector: "option",
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }, {
    type: SelectMultipleControlValueAccessor,
    decorators: [{
      type: Optional
    }, {
      type: Host
    }]
  }], {
    ngValue: [{
      type: Input,
      args: ["ngValue"]
    }],
    value: [{
      type: Input,
      args: ["value"]
    }]
  });
})();
function toInteger(value) {
  return typeof value === "number" ? value : parseInt(value, 10);
}
function toFloat(value) {
  return typeof value === "number" ? value : parseFloat(value);
}
var AbstractValidatorDirective = class _AbstractValidatorDirective {
  _validator = nullValidator;
  _onChange;
  _enabled;
  ngOnChanges(changes) {
    if (this.inputName in changes) {
      const input2 = this.normalizeInput(changes[this.inputName].currentValue);
      this._enabled = this.enabled(input2);
      this._validator = this._enabled ? this.createValidator(input2) : nullValidator;
      this._onChange?.();
    }
  }
  validate(control) {
    return this._validator(control);
  }
  registerOnValidatorChange(fn) {
    this._onChange = fn;
  }
  enabled(input2) {
    return input2 != null;
  }
  static \u0275fac = function AbstractValidatorDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AbstractValidatorDirective)();
  };
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _AbstractValidatorDirective,
    features: [\u0275\u0275NgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AbstractValidatorDirective, [{
    type: Directive
  }], null, null);
})();
var MAX_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MaxValidator),
  multi: true
};
var MaxValidator = class _MaxValidator extends AbstractValidatorDirective {
  max;
  inputName = "max";
  normalizeInput = (input2) => toFloat(input2);
  createValidator = (max) => maxValidator(max);
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275MaxValidator_BaseFactory;
    return function MaxValidator_Factory(__ngFactoryType__) {
      return (\u0275MaxValidator_BaseFactory || (\u0275MaxValidator_BaseFactory = \u0275\u0275getInheritedFactory(_MaxValidator)))(__ngFactoryType__ || _MaxValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _MaxValidator,
    selectors: [["input", "type", "number", "max", "", "formControlName", ""], ["input", "type", "number", "max", "", "formControl", ""], ["input", "type", "number", "max", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MaxValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("max", ctx._enabled ? ctx.max : null);
      }
    },
    inputs: {
      max: "max"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([MAX_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MaxValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=number][max][formControlName],input[type=number][max][formControl],input[type=number][max][ngModel]",
      providers: [MAX_VALIDATOR],
      host: {
        "[attr.max]": "_enabled ? max : null"
      },
      standalone: false
    }]
  }], null, {
    max: [{
      type: Input
    }]
  });
})();
var MIN_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MinValidator),
  multi: true
};
var MinValidator = class _MinValidator extends AbstractValidatorDirective {
  min;
  inputName = "min";
  normalizeInput = (input2) => toFloat(input2);
  createValidator = (min) => minValidator(min);
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275MinValidator_BaseFactory;
    return function MinValidator_Factory(__ngFactoryType__) {
      return (\u0275MinValidator_BaseFactory || (\u0275MinValidator_BaseFactory = \u0275\u0275getInheritedFactory(_MinValidator)))(__ngFactoryType__ || _MinValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _MinValidator,
    selectors: [["input", "type", "number", "min", "", "formControlName", ""], ["input", "type", "number", "min", "", "formControl", ""], ["input", "type", "number", "min", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MinValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("min", ctx._enabled ? ctx.min : null);
      }
    },
    inputs: {
      min: "min"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([MIN_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MinValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=number][min][formControlName],input[type=number][min][formControl],input[type=number][min][ngModel]",
      providers: [MIN_VALIDATOR],
      host: {
        "[attr.min]": "_enabled ? min : null"
      },
      standalone: false
    }]
  }], null, {
    min: [{
      type: Input
    }]
  });
})();
var REQUIRED_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => RequiredValidator),
  multi: true
};
var CHECKBOX_REQUIRED_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => CheckboxRequiredValidator),
  multi: true
};
var RequiredValidator = class _RequiredValidator extends AbstractValidatorDirective {
  required;
  inputName = "required";
  normalizeInput = booleanAttribute;
  createValidator = (input2) => requiredValidator;
  enabled(input2) {
    return input2;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275RequiredValidator_BaseFactory;
    return function RequiredValidator_Factory(__ngFactoryType__) {
      return (\u0275RequiredValidator_BaseFactory || (\u0275RequiredValidator_BaseFactory = \u0275\u0275getInheritedFactory(_RequiredValidator)))(__ngFactoryType__ || _RequiredValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _RequiredValidator,
    selectors: [["", "required", "", "formControlName", "", 3, "type", "checkbox"], ["", "required", "", "formControl", "", 3, "type", "checkbox"], ["", "required", "", "ngModel", "", 3, "type", "checkbox"]],
    hostVars: 1,
    hostBindings: function RequiredValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("required", ctx._enabled ? "" : null);
      }
    },
    inputs: {
      required: "required"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([REQUIRED_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(RequiredValidator, [{
    type: Directive,
    args: [{
      selector: ":not([type=checkbox])[required][formControlName],:not([type=checkbox])[required][formControl],:not([type=checkbox])[required][ngModel]",
      providers: [REQUIRED_VALIDATOR],
      host: {
        "[attr.required]": '_enabled ? "" : null'
      },
      standalone: false
    }]
  }], null, {
    required: [{
      type: Input
    }]
  });
})();
var CheckboxRequiredValidator = class _CheckboxRequiredValidator extends RequiredValidator {
  createValidator = (input2) => requiredTrueValidator;
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275CheckboxRequiredValidator_BaseFactory;
    return function CheckboxRequiredValidator_Factory(__ngFactoryType__) {
      return (\u0275CheckboxRequiredValidator_BaseFactory || (\u0275CheckboxRequiredValidator_BaseFactory = \u0275\u0275getInheritedFactory(_CheckboxRequiredValidator)))(__ngFactoryType__ || _CheckboxRequiredValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _CheckboxRequiredValidator,
    selectors: [["input", "type", "checkbox", "required", "", "formControlName", ""], ["input", "type", "checkbox", "required", "", "formControl", ""], ["input", "type", "checkbox", "required", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function CheckboxRequiredValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("required", ctx._enabled ? "" : null);
      }
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([CHECKBOX_REQUIRED_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CheckboxRequiredValidator, [{
    type: Directive,
    args: [{
      selector: "input[type=checkbox][required][formControlName],input[type=checkbox][required][formControl],input[type=checkbox][required][ngModel]",
      providers: [CHECKBOX_REQUIRED_VALIDATOR],
      host: {
        "[attr.required]": '_enabled ? "" : null'
      },
      standalone: false
    }]
  }], null, null);
})();
var EMAIL_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => EmailValidator),
  multi: true
};
var EmailValidator = class _EmailValidator extends AbstractValidatorDirective {
  email;
  inputName = "email";
  normalizeInput = booleanAttribute;
  createValidator = (input2) => emailValidator;
  enabled(input2) {
    return input2;
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275EmailValidator_BaseFactory;
    return function EmailValidator_Factory(__ngFactoryType__) {
      return (\u0275EmailValidator_BaseFactory || (\u0275EmailValidator_BaseFactory = \u0275\u0275getInheritedFactory(_EmailValidator)))(__ngFactoryType__ || _EmailValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _EmailValidator,
    selectors: [["", "email", "", "formControlName", ""], ["", "email", "", "formControl", ""], ["", "email", "", "ngModel", ""]],
    inputs: {
      email: "email"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([EMAIL_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EmailValidator, [{
    type: Directive,
    args: [{
      selector: "[email][formControlName],[email][formControl],[email][ngModel]",
      providers: [EMAIL_VALIDATOR],
      standalone: false
    }]
  }], null, {
    email: [{
      type: Input
    }]
  });
})();
var MIN_LENGTH_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MinLengthValidator),
  multi: true
};
var MinLengthValidator = class _MinLengthValidator extends AbstractValidatorDirective {
  minlength;
  inputName = "minlength";
  normalizeInput = (input2) => toInteger(input2);
  createValidator = (minlength) => minLengthValidator(minlength);
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275MinLengthValidator_BaseFactory;
    return function MinLengthValidator_Factory(__ngFactoryType__) {
      return (\u0275MinLengthValidator_BaseFactory || (\u0275MinLengthValidator_BaseFactory = \u0275\u0275getInheritedFactory(_MinLengthValidator)))(__ngFactoryType__ || _MinLengthValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _MinLengthValidator,
    selectors: [["", "minlength", "", "formControlName", ""], ["", "minlength", "", "formControl", ""], ["", "minlength", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MinLengthValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("minlength", ctx._enabled ? ctx.minlength : null);
      }
    },
    inputs: {
      minlength: "minlength"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([MIN_LENGTH_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MinLengthValidator, [{
    type: Directive,
    args: [{
      selector: "[minlength][formControlName],[minlength][formControl],[minlength][ngModel]",
      providers: [MIN_LENGTH_VALIDATOR],
      host: {
        "[attr.minlength]": "_enabled ? minlength : null"
      },
      standalone: false
    }]
  }], null, {
    minlength: [{
      type: Input
    }]
  });
})();
var MAX_LENGTH_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => MaxLengthValidator),
  multi: true
};
var MaxLengthValidator = class _MaxLengthValidator extends AbstractValidatorDirective {
  maxlength;
  inputName = "maxlength";
  normalizeInput = (input2) => toInteger(input2);
  createValidator = (maxlength) => maxLengthValidator(maxlength);
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275MaxLengthValidator_BaseFactory;
    return function MaxLengthValidator_Factory(__ngFactoryType__) {
      return (\u0275MaxLengthValidator_BaseFactory || (\u0275MaxLengthValidator_BaseFactory = \u0275\u0275getInheritedFactory(_MaxLengthValidator)))(__ngFactoryType__ || _MaxLengthValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _MaxLengthValidator,
    selectors: [["", "maxlength", "", "formControlName", ""], ["", "maxlength", "", "formControl", ""], ["", "maxlength", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function MaxLengthValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("maxlength", ctx._enabled ? ctx.maxlength : null);
      }
    },
    inputs: {
      maxlength: "maxlength"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([MAX_LENGTH_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(MaxLengthValidator, [{
    type: Directive,
    args: [{
      selector: "[maxlength][formControlName],[maxlength][formControl],[maxlength][ngModel]",
      providers: [MAX_LENGTH_VALIDATOR],
      host: {
        "[attr.maxlength]": "_enabled ? maxlength : null"
      },
      standalone: false
    }]
  }], null, {
    maxlength: [{
      type: Input
    }]
  });
})();
var PATTERN_VALIDATOR = {
  provide: NG_VALIDATORS,
  useExisting: forwardRef(() => PatternValidator),
  multi: true
};
var PatternValidator = class _PatternValidator extends AbstractValidatorDirective {
  pattern;
  inputName = "pattern";
  normalizeInput = (input2) => input2;
  createValidator = (input2) => patternValidator(input2);
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275PatternValidator_BaseFactory;
    return function PatternValidator_Factory(__ngFactoryType__) {
      return (\u0275PatternValidator_BaseFactory || (\u0275PatternValidator_BaseFactory = \u0275\u0275getInheritedFactory(_PatternValidator)))(__ngFactoryType__ || _PatternValidator);
    };
  })();
  static \u0275dir = /* @__PURE__ */ \u0275\u0275defineDirective({
    type: _PatternValidator,
    selectors: [["", "pattern", "", "formControlName", ""], ["", "pattern", "", "formControl", ""], ["", "pattern", "", "ngModel", ""]],
    hostVars: 1,
    hostBindings: function PatternValidator_HostBindings(rf, ctx) {
      if (rf & 2) {
        \u0275\u0275attribute("pattern", ctx._enabled ? ctx.pattern : null);
      }
    },
    inputs: {
      pattern: "pattern"
    },
    standalone: false,
    features: [\u0275\u0275ProvidersFeature([PATTERN_VALIDATOR]), \u0275\u0275InheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PatternValidator, [{
    type: Directive,
    args: [{
      selector: "[pattern][formControlName],[pattern][formControl],[pattern][ngModel]",
      providers: [PATTERN_VALIDATOR],
      host: {
        "[attr.pattern]": "_enabled ? pattern : null"
      },
      standalone: false
    }]
  }], null, {
    pattern: [{
      type: Input
    }]
  });
})();
var SHARED_FORM_DIRECTIVES = [\u0275NgNoValidate, NgSelectOption, \u0275NgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator];
var TEMPLATE_DRIVEN_DIRECTIVES = [NgModel, NgModelGroup, NgForm];
var REACTIVE_DRIVEN_DIRECTIVES = [FormControlDirective, FormGroupDirective, FormArrayDirective, FormControlName, FormGroupName, FormArrayName];
var \u0275InternalFormsSharedModule = class _\u0275InternalFormsSharedModule {
  static \u0275fac = function \u0275InternalFormsSharedModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _\u0275InternalFormsSharedModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _\u0275InternalFormsSharedModule,
    declarations: [\u0275NgNoValidate, NgSelectOption, \u0275NgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator],
    exports: [\u0275NgNoValidate, NgSelectOption, \u0275NgSelectMultipleOption, DefaultValueAccessor, NumberValueAccessor, RangeValueAccessor, CheckboxControlValueAccessor, SelectControlValueAccessor, SelectMultipleControlValueAccessor, RadioControlValueAccessor, NgControlStatus, NgControlStatusGroup, RequiredValidator, MinLengthValidator, MaxLengthValidator, PatternValidator, CheckboxRequiredValidator, EmailValidator, MinValidator, MaxValidator]
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(\u0275InternalFormsSharedModule, [{
    type: NgModule,
    args: [{
      declarations: SHARED_FORM_DIRECTIVES,
      exports: SHARED_FORM_DIRECTIVES
    }]
  }], null, null);
})();
function isAbstractControlOptions(options) {
  return !!options && (options.asyncValidators !== void 0 || options.validators !== void 0 || options.updateOn !== void 0);
}
var FormBuilder = class _FormBuilder {
  useNonNullable = false;
  get nonNullable() {
    const nnfb = new _FormBuilder();
    nnfb.useNonNullable = true;
    return nnfb;
  }
  group(controls, options = null) {
    const reducedControls = this._reduceControls(controls);
    let newOptions = {};
    if (isAbstractControlOptions(options)) {
      newOptions = options;
    } else if (options !== null) {
      newOptions.validators = options.validator;
      newOptions.asyncValidators = options.asyncValidator;
    }
    return new FormGroup(reducedControls, newOptions);
  }
  record(controls, options = null) {
    const reducedControls = this._reduceControls(controls);
    return new FormRecord(reducedControls, options);
  }
  control(formState, validatorOrOpts, asyncValidator) {
    let newOptions = {};
    if (!this.useNonNullable) {
      return new FormControl(formState, validatorOrOpts, asyncValidator);
    }
    if (isAbstractControlOptions(validatorOrOpts)) {
      newOptions = validatorOrOpts;
    } else {
      newOptions.validators = validatorOrOpts;
      newOptions.asyncValidators = asyncValidator;
    }
    return new FormControl(formState, __spreadProps(__spreadValues({}, newOptions), {
      nonNullable: true
    }));
  }
  array(controls, validatorOrOpts, asyncValidator) {
    const createdControls = controls.map((c) => this._createControl(c));
    return new FormArray(createdControls, validatorOrOpts, asyncValidator);
  }
  _reduceControls(controls) {
    const createdControls = {};
    Object.keys(controls).forEach((controlName) => {
      createdControls[controlName] = this._createControl(controls[controlName]);
    });
    return createdControls;
  }
  _createControl(controls) {
    if (controls instanceof FormControl) {
      return controls;
    } else if (controls instanceof AbstractControl) {
      return controls;
    } else if (Array.isArray(controls)) {
      const value = controls[0];
      const validator = controls.length > 1 ? controls[1] : null;
      const asyncValidator = controls.length > 2 ? controls[2] : null;
      return this.control(value, validator, asyncValidator);
    } else {
      return this.control(controls);
    }
  }
  static \u0275fac = function FormBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormBuilder)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _FormBuilder,
    factory: _FormBuilder.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var NonNullableFormBuilder = class _NonNullableFormBuilder {
  static \u0275fac = function NonNullableFormBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _NonNullableFormBuilder)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _NonNullableFormBuilder,
    factory: () => (() => inject2(FormBuilder).nonNullable)(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(NonNullableFormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: () => inject2(FormBuilder).nonNullable
    }]
  }], null, null);
})();
var UntypedFormBuilder = class _UntypedFormBuilder extends FormBuilder {
  group(controlsConfig, options = null) {
    return super.group(controlsConfig, options);
  }
  control(formState, validatorOrOpts, asyncValidator) {
    return super.control(formState, validatorOrOpts, asyncValidator);
  }
  array(controlsConfig, validatorOrOpts, asyncValidator) {
    return super.array(controlsConfig, validatorOrOpts, asyncValidator);
  }
  static \u0275fac = /* @__PURE__ */ (() => {
    let \u0275UntypedFormBuilder_BaseFactory;
    return function UntypedFormBuilder_Factory(__ngFactoryType__) {
      return (\u0275UntypedFormBuilder_BaseFactory || (\u0275UntypedFormBuilder_BaseFactory = \u0275\u0275getInheritedFactory(_UntypedFormBuilder)))(__ngFactoryType__ || _UntypedFormBuilder);
    };
  })();
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({
    token: _UntypedFormBuilder,
    factory: _UntypedFormBuilder.\u0275fac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(UntypedFormBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var FormsModule = class _FormsModule {
  static withConfig(opts) {
    return {
      ngModule: _FormsModule,
      providers: [{
        provide: CALL_SET_DISABLED_STATE,
        useValue: opts.callSetDisabledState ?? setDisabledStateDefault
      }]
    };
  }
  static \u0275fac = function FormsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FormsModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _FormsModule,
    declarations: [NgModel, NgModelGroup, NgForm],
    exports: [\u0275InternalFormsSharedModule, NgModel, NgModelGroup, NgForm]
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
    imports: [\u0275InternalFormsSharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FormsModule, [{
    type: NgModule,
    args: [{
      declarations: TEMPLATE_DRIVEN_DIRECTIVES,
      exports: [\u0275InternalFormsSharedModule, TEMPLATE_DRIVEN_DIRECTIVES]
    }]
  }], null, null);
})();
var ReactiveFormsModule = class _ReactiveFormsModule {
  static withConfig(opts) {
    return {
      ngModule: _ReactiveFormsModule,
      providers: [{
        provide: NG_MODEL_WITH_FORM_CONTROL_WARNING,
        useValue: opts.warnOnNgModelWithFormControl ?? "always"
      }, {
        provide: CALL_SET_DISABLED_STATE,
        useValue: opts.callSetDisabledState ?? setDisabledStateDefault
      }]
    };
  }
  static \u0275fac = function ReactiveFormsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ReactiveFormsModule)();
  };
  static \u0275mod = /* @__PURE__ */ \u0275\u0275defineNgModule({
    type: _ReactiveFormsModule,
    declarations: [FormControlDirective, FormGroupDirective, FormArrayDirective, FormControlName, FormGroupName, FormArrayName],
    exports: [\u0275InternalFormsSharedModule, FormControlDirective, FormGroupDirective, FormArrayDirective, FormControlName, FormGroupName, FormArrayName]
  });
  static \u0275inj = /* @__PURE__ */ \u0275\u0275defineInjector({
    imports: [\u0275InternalFormsSharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ReactiveFormsModule, [{
    type: NgModule,
    args: [{
      declarations: [REACTIVE_DRIVEN_DIRECTIVES],
      exports: [\u0275InternalFormsSharedModule, REACTIVE_DRIVEN_DIRECTIVES]
    }]
  }], null, null);
})();

// src/app/workbenches/component/entity-design/entity-design-legacy-adapter.ts
function createEntityDesignLegacyAdapter(runtime = getAngularRuntimeState()) {
  const legacyWindow = runtime;
  const document2 = () => {
    const direct = runtime;
    if (direct.doc)
      return direct.doc;
    legacyWindow.S ||= {};
    legacyWindow.S.doc ||= {};
    return legacyWindow.S.doc;
  };
  return {
    entities() {
      const doc = document2();
      doc.entities ||= [];
      return doc.entities;
    },
    relations() {
      const doc = document2();
      doc.relations ||= [];
      return doc.relations;
    },
    constructs() {
      const doc = document2();
      return doc.businessConstructs || doc.constructs || [];
    },
    components() {
      const doc = document2();
      return doc.businessComponents || [];
    },
    markChanged() {
      if (legacyWindow.markModified)
        legacyWindow.markModified();
      else
        markAngularRuntimeModified();
      legacyWindow.renderSidebar?.();
    },
    nextId(prefix, items) {
      const used = new Set(items.map((item) => {
        const record = item;
        return String(record.uid || record.id || "");
      }));
      let index = items.length + 1;
      let id = `${prefix}${index}`;
      while (used.has(id)) {
        index += 1;
        id = `${prefix}${index}`;
      }
      return id;
    }
  };
}

// src/app/workbenches/component/entity-design/entity-design-relation-endpoint.ts
function entityId(entity) {
  return String(entity?.uid || entity?.id || "").trim();
}
function matchEndpointByIdentity(entities, raw) {
  const target = String(raw || "").trim();
  if (!target)
    return "";
  const entity = entities.find((item) => entityId(item) === target || item.uid === target || item.id === target);
  return entity ? entityId(entity) : "";
}
function matchEndpointByName(entities, raw) {
  const target = String(raw || "").trim();
  if (!target)
    return "";
  const entity = entities.find((item) => {
    const labels = [item.name, item.title, item.label].map((value) => String(value || "").trim()).filter(Boolean);
    return labels.includes(target);
  });
  return entity ? entityId(entity) : "";
}
function resolveRelationEndpoint(entities, relation, side) {
  const candidates = side === "from" ? [
    relation.sourceEntityUid,
    relation.sourceEntityId,
    relation.fromEntityUid,
    relation.fromEntityId,
    relation.source_entity_uid,
    relation.source_entity_id,
    relation.from_entity_uid,
    relation.from_entity_id,
    relation.sourceUid,
    relation.sourceId,
    relation.fromUid,
    relation.fromId,
    relation.source,
    relation.from
  ] : [
    relation.targetEntityUid,
    relation.targetEntityId,
    relation.toEntityUid,
    relation.toEntityId,
    relation.target_entity_uid,
    relation.target_entity_id,
    relation.to_entity_uid,
    relation.to_entity_id,
    relation.targetUid,
    relation.targetId,
    relation.toUid,
    relation.toId,
    relation.target,
    relation.to
  ];
  const rawValues = candidates.map((candidate) => String(candidate || "").trim()).filter(Boolean);
  const identityMatched = rawValues.map((value) => matchEndpointByIdentity(entities, value)).find(Boolean);
  if (identityMatched)
    return identityMatched;
  const nameMatched = rawValues.map((value) => matchEndpointByName(entities, value)).find(Boolean);
  if (nameMatched)
    return nameMatched;
  return "";
}

// src/app/workbenches/component/entity-design/entity-design-workbench.component.ts
var _c0 = () => [];
var _forTrack0 = ($index, $item) => $item.key;
var _forTrack1 = ($index, $item) => $item.from.id + "-" + $item.to.id + "-" + $index;
var _forTrack2 = ($index, $item) => $item.from.id + "-" + $item.to.id + "-label-" + $index;
var _forTrack3 = ($index, $item) => $item.id;
var _forTrack4 = ($index, $item) => $item.name;
var _forTrack5 = ($index, $item) => $item.name || $index;
function _forTrack6($index, $item) {
  return this.entityId($item);
}
var _forTrack7 = ($index, $item) => $item.path;
var _forTrack8 = ($index, $item) => $item.label + "-" + $index;
var _forTrack9 = ($index, $item) => $item.label + "-hitbox-" + $index;
function _forTrack10($index, $item) {
  return this.constructId($item);
}
var _forTrack11 = ($index, $item) => $item.uid || $item.id || $index;
var _forTrack12 = ($index, $item) => $item.processName + "-" + $item.taskName + "-" + $index;
function _forTrack13($index, $item) {
  return this.fieldId($item, $index);
}
var _forTrack14 = ($index, $item) => $item.value;
var _forTrack15 = ($index, $item) => $item.scope + "-" + $item.index + "-" + $item.owner;
var _forTrack16 = ($index, $item) => $item.transition.uid || $item.transition.id || $item.index;
function EntityDesignWorkbenchComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 5)(1, "strong");
    \u0275\u0275text(2, "\u62D6\u62FD\u72B6\u6001\u8282\u70B9");
    \u0275\u0275elementEnd();
    \u0275\u0275text(3, "\u8C03\u6574\u4F4D\u7F6E \xB7 ");
    \u0275\u0275elementStart(4, "strong");
    \u0275\u0275text(5, "\u62D6\u62FD\u8D77\u6B62\u5706\u70B9");
    \u0275\u0275elementEnd();
    \u0275\u0275text(6, "\u8C03\u6574\u5F00\u59CB/\u7ED3\u675F\u6807\u8BB0 \xB7 ");
    \u0275\u0275elementStart(7, "strong");
    \u0275\u0275text(8, "\u70B9\u51FB\u8FDE\u7EBF");
    \u0275\u0275elementEnd();
    \u0275\u0275text(9, "\u9009\u4E2D \xB7 ");
    \u0275\u0275elementStart(10, "strong");
    \u0275\u0275text(11, "\u62D6\u62FD\u8FDE\u7EBF\u6BB5");
    \u0275\u0275elementEnd();
    \u0275\u0275text(12, "\u8C03\u6574\u8DEF\u5F84 \xB7 ");
    \u0275\u0275elementStart(13, "strong");
    \u0275\u0275text(14, "\u62D6\u62FD\u6587\u5B57");
    \u0275\u0275elementEnd();
    \u0275\u0275text(15, "\u8C03\u6574\u8BF4\u660E \xB7 ");
    \u0275\u0275elementStart(16, "strong");
    \u0275\u0275text(17, "Ctrl+\u6EDA\u8F6E");
    \u0275\u0275elementEnd();
    \u0275\u0275text(18, "\u7F29\u653E ");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 6)(1, "strong");
    \u0275\u0275text(2, "\u62D6\u62FD");
    \u0275\u0275elementEnd();
    \u0275\u0275text(3, "\u79FB\u52A8\u8282\u70B9 \xB7 \u70B9\u51FB\u5B9E\u4F53\u5207\u6362\u7F16\u8F91\u5BF9\u8C61 \xB7 ");
    \u0275\u0275elementStart(4, "strong");
    \u0275\u0275text(5, "\u53CC\u51FB\u5B9E\u4F53");
    \u0275\u0275elementEnd();
    \u0275\u0275text(6, "\u8FDB\u5165\u7F16\u8F91 \xB7 ");
    \u0275\u0275elementStart(7, "strong");
    \u0275\u0275text(8, "Ctrl+A");
    \u0275\u0275elementEnd();
    \u0275\u0275text(9, "\u5168\u9009\u5B9E\u4F53 \xB7 ");
    \u0275\u0275elementStart(10, "strong");
    \u0275\u0275text(11, "Ctrl+\u70B9\u51FB");
    \u0275\u0275elementEnd();
    \u0275\u0275text(12, "\u591A\u9009 \xB7 ");
    \u0275\u0275elementStart(13, "strong");
    \u0275\u0275text(14, "Shift+\u5DE6\u952E\u62D6\u62FD");
    \u0275\u0275elementEnd();
    \u0275\u0275text(15, "\u6846\u9009 \xB7 ");
    \u0275\u0275elementStart(16, "strong");
    \u0275\u0275text(17, "\u70B9\u51FB\u6784\u4EF6/\u7EC4\u4EF6");
    \u0275\u0275elementEnd();
    \u0275\u0275text(18, "\u9009\u62E9\u6240\u5C5E\u5168\u90E8\u5B9E\u4F53 ");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 17);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_9_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.openSelectedEntityDrawer());
    });
    \u0275\u0275text(1, "\u7F16\u8F91\u5C5E\u6027");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "button", 18);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_9_Template_button_click_2_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addEntity());
    });
    \u0275\u0275text(3, "\uFF0B \u65B0\u5EFA\u5B9E\u4F53");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "button", 19);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_9_Template_button_click_4_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.resetLayout());
    });
    \u0275\u0275text(5, "\u91CD\u7F6E\u5E03\u5C40");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("disabled", !ctx_r1.selectedEntityId());
  }
}
function EntityDesignWorkbenchComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 20);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_10_Template_button_click_0_listener() {
      const stateEntity_r4 = \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addTransition(stateEntity_r4));
    });
    \u0275\u0275text(1, "\uFF0B \u6DFB\u52A0\u6D41\u8F6C");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "button", 21);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_10_Template_button_click_2_listener() {
      const stateEntity_r4 = \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.resetAllStateTransitionRoutes(stateEntity_r4));
    });
    \u0275\u0275text(3, "\u91CD\u7F6E\u8FDE\u7EBF");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275property("disabled", !ctx_r1.selectedStateField());
  }
}
function EntityDesignWorkbenchComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 7)(1, "button", 22);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_11_Template_button_click_1_listener() {
      \u0275\u0275restoreView(_r5);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.nudgeStateZoom(-0.1));
    });
    \u0275\u0275text(2, "\uFF0D");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "button", 23);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_11_Template_button_click_3_listener() {
      \u0275\u0275restoreView(_r5);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.resetStateZoom());
    });
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "button", 24);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_11_Template_button_click_5_listener() {
      \u0275\u0275restoreView(_r5);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.nudgeStateZoom(0.1));
    });
    \u0275\u0275text(6, "\uFF0B");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(ctx_r1.stateZoomLabel());
  }
}
function EntityDesignWorkbenchComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 25);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_12_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.stateEditorOpen.set(false));
    });
    \u0275\u0275text(1, "\u5173\u95ED\u7F16\u8F91");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 26);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_13_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r7);
      const ctx_r1 = \u0275\u0275nextContext();
      ctx_r1.stateEditorOpen.set(true);
      return \u0275\u0275resetView(ctx_r1.ensureActiveStateField(ctx_r1.selectedEntity()));
    });
    \u0275\u0275text(1, "\u6253\u5F00\u7F16\u8F91");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "button", 27);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_13_Template_button_click_2_listener() {
      \u0275\u0275restoreView(_r7);
      const ctx_r1 = \u0275\u0275nextContext();
      ctx_r1.stateEditorOpen.set(true);
      return \u0275\u0275resetView(ctx_r1.ensureActiveStateField(ctx_r1.selectedEntity()));
    });
    \u0275\u0275text(3, "\u6253\u5F00\u7F16\u8F91");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 28);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_14_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r8);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.editorOpen.set(false));
    });
    \u0275\u0275text(1, "\u5173\u95ED\u7F16\u8F91");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 29);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_15_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r9);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.editorOpen.set(true));
    });
    \u0275\u0275text(1, "\u6253\u5F00\u7F16\u8F91");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 39);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_For_3_Template_div_mousedown_0_listener($event) {
      const frame_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startFrameDrag(frame_r12, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(1, "div", 40);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_For_3_Template_div_mousedown_1_listener($event) {
      const frame_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startFrameDrag(frame_r12, $event));
    });
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const frame_r12 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("ngStyle", ctx_r1.frameStyle(frame_r12))("title", "\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 " + ((frame_r12.entityIds == null ? null : frame_r12.entityIds.length) || 0) + " \u4E2A\u5B9E\u4F53");
    \u0275\u0275advance();
    \u0275\u0275styleProp("left", frame_r12.left + 18, "px")("top", frame_r12.top + 10, "px");
    \u0275\u0275property("title", "\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 " + ((frame_r12.entityIds == null ? null : frame_r12.entityIds.length) || 0) + " \u4E2A\u5B9E\u4F53");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(frame_r12.label);
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 41);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_For_5_Template_div_mousedown_0_listener($event) {
      const frame_r14 = \u0275\u0275restoreView(_r13).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startFrameDrag(frame_r14, $event));
    });
    \u0275\u0275elementStart(1, "div", 42);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_For_5_Template_div_mousedown_1_listener($event) {
      const frame_r14 = \u0275\u0275restoreView(_r13).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startFrameDrag(frame_r14, $event));
    });
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const frame_r14 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("ngStyle", ctx_r1.frameStyle(frame_r14))("title", "\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 " + ((frame_r14.entityIds == null ? null : frame_r14.entityIds.length) || 0) + " \u4E2A\u5B9E\u4F53");
    \u0275\u0275advance();
    \u0275\u0275property("title", "\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 " + ((frame_r14.entityIds == null ? null : frame_r14.entityIds.length) || 0) + " \u4E2A\u5B9E\u4F53");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(frame_r14.label);
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_For_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = \u0275\u0275getCurrentView();
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(0, "path", 43);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_18_For_11_Template_path_click_0_listener($event) {
      const line_r16 = \u0275\u0275restoreView(_r15).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.selectRelationLine(line_r16, $event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const line_r16 = ctx.$implicit;
    \u0275\u0275classProp("is-focus", line_r16.focus)("is-muted", line_r16.muted)("ef-muted", line_r16.muted)("ef-focus", line_r16.focus)("is-dashed", line_r16.dashed);
    \u0275\u0275attribute("d", line_r16.path)("stroke", line_r16.color)("stroke-width", line_r16.width)("stroke-opacity", line_r16.opacity);
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_For_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 36);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const line_r17 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("ngStyle", ctx_r1.relationLabelStyle(line_r17));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(line_r17.label);
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_For_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 44);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_For_15_Template_button_mousedown_0_listener($event) {
      const node_r19 = \u0275\u0275restoreView(_r18).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startEntityDrag(node_r19.entity, $event));
    })("click", function EntityDesignWorkbenchComponent_Conditional_18_For_15_Template_button_click_0_listener($event) {
      const node_r19 = \u0275\u0275restoreView(_r18).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      $event.stopPropagation();
      return \u0275\u0275resetView(ctx_r1.selectEntity(node_r19.entity, $event));
    })("dblclick", function EntityDesignWorkbenchComponent_Conditional_18_For_15_Template_button_dblclick_0_listener($event) {
      const node_r19 = \u0275\u0275restoreView(_r18).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openEntityDrawer(node_r19.entity, $event));
    });
    \u0275\u0275elementStart(1, "strong", 45);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const node_r19 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275classProp("is-selected", ctx_r1.isSelected(node_r19.entity))("ef-selected", ctx_r1.isSelected(node_r19.entity))("is-focus", ctx_r1.selectedEntityId() === node_r19.id)("ef-focus", ctx_r1.selectedEntityId() === node_r19.id)("ef-muted", ctx_r1.selectedEntityId() && ctx_r1.selectedEntityId() !== node_r19.id && !ctx_r1.isSelected(node_r19.entity));
    \u0275\u0275property("ngStyle", ctx_r1.nodeStyle(node_r19));
    \u0275\u0275attribute("data-id", node_r19.id);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(node_r19.entity.name || "\u672A\u547D\u540D\u5B9E\u4F53");
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "div", 38);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("ngStyle", ctx_r1.selectionBoxStyle());
  }
}
function EntityDesignWorkbenchComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 13)(1, "div", 30);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_18_Template_div_mousedown_1_listener($event) {
      \u0275\u0275restoreView(_r10);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.startSelectionBox($event));
    })("click", function EntityDesignWorkbenchComponent_Conditional_18_Template_div_click_1_listener() {
      \u0275\u0275restoreView(_r10);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.clearSelection());
    });
    \u0275\u0275repeaterCreate(2, EntityDesignWorkbenchComponent_Conditional_18_For_3_Template, 3, 8, null, null, _forTrack0);
    \u0275\u0275repeaterCreate(4, EntityDesignWorkbenchComponent_Conditional_18_For_5_Template, 3, 4, "div", 31, _forTrack0);
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(6, "svg", 32)(7, "defs")(8, "marker", 33);
    \u0275\u0275element(9, "path", 34);
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(10, EntityDesignWorkbenchComponent_Conditional_18_For_11_Template, 1, 14, ":svg:path", 35, _forTrack1);
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(12, EntityDesignWorkbenchComponent_Conditional_18_For_13_Template, 2, 2, "span", 36, _forTrack2);
    \u0275\u0275repeaterCreate(14, EntityDesignWorkbenchComponent_Conditional_18_For_15_Template, 3, 13, "button", 37, _forTrack3);
    \u0275\u0275conditionalCreate(16, EntityDesignWorkbenchComponent_Conditional_18_Conditional_16_Template, 1, 1, "div", 38);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("ngStyle", ctx_r1.relationCanvasSize());
    \u0275\u0275advance();
    \u0275\u0275classProp("is-editing", ctx_r1.canEditRelation());
    \u0275\u0275property("ngStyle", ctx_r1.boardSize());
    \u0275\u0275attribute("data-export-graph-id", ctx_r1.exportGraphId || null);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.componentFrames());
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.groupFrames());
    \u0275\u0275advance(2);
    \u0275\u0275attribute("width", ctx_r1.boardWidth())("height", ctx_r1.boardHeight());
    \u0275\u0275advance(4);
    \u0275\u0275repeater(ctx_r1.relationLines());
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.relationLines());
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.nodes());
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.selectionBox() ? 16 : -1);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_2_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r21 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 64);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_2_For_5_Template_button_click_0_listener() {
      const item_r22 = \u0275\u0275restoreView(_r21).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.selectEntity(item_r22));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const item_r22 = ctx.$implicit;
    const entity_r23 = \u0275\u0275nextContext(2);
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275classProp("active", ctx_r1.entityId(item_r22) === ctx_r1.entityId(entity_r23));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", item_r22.name || "\u672A\u547D\u540D\u5B9E\u4F53", " ");
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 48)(1, "div", 61);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 62);
    \u0275\u0275repeaterCreate(4, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_2_For_5_Template, 2, 3, "button", 63, _forTrack6, true);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const group_r24 = ctx.$implicit;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(group_r24.name);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(group_r24.items);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r25 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 65);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_15_Template_button_click_0_listener() {
      const panel_r26 = \u0275\u0275restoreView(_r25).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setStateField(panel_r26.name));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const panel_r26 = ctx.$implicit;
    const $index_r27 = ctx.$index;
    \u0275\u0275classProp("active", panel_r26.active);
    \u0275\u0275attribute("data-testid", "entity-state-overview-field-" + $index_r27);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", panel_r26.name || "\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5", " ");
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 72);
    \u0275\u0275text(1, "\u7F16\u8F91\u4E2D");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 85);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r30 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275classProp("kind-initial", node_r30.kind === "initial")("kind-terminal", node_r30.kind === "terminal");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate2("", node_r30.name, " \xB7 ", ctx_r1.stateNodeKindLabel(node_r30.kind));
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_23_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275namespaceSVG();
    \u0275\u0275element(0, "path", 82);
  }
  if (rf & 2) {
    const link_r31 = ctx.$implicit;
    \u0275\u0275attribute("d", link_r31.path)("marker-end", link_r31.terminal ? null : "url(#entity-state-arrow)");
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_25_Template(rf, ctx) {
  if (rf & 1) {
    const _r32 = \u0275\u0275getCurrentView();
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(0, "path", 86);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_25_Template_path_click_0_listener($event) {
      const transition_r33 = \u0275\u0275restoreView(_r32).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.selectTransition(transition_r33.index, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(1, "g", 87);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_25_Template_g_mousedown_1_listener($event) {
      const transition_r33 = \u0275\u0275restoreView(_r32).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startStateLabelDrag(transition_r33, $event));
    });
    \u0275\u0275element(2, "rect", 88);
    \u0275\u0275elementStart(3, "text", 89);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const transition_r33 = ctx.$implicit;
    \u0275\u0275classProp("selected", transition_r33.selected);
    \u0275\u0275attribute("d", transition_r33.path)("data-state-action", transition_r33.label);
    \u0275\u0275advance();
    \u0275\u0275attribute("transform", "rotate(" + transition_r33.labelAngle + " " + transition_r33.labelX + " " + transition_r33.labelY + ")");
    \u0275\u0275advance();
    \u0275\u0275attribute("x", transition_r33.labelX - transition_r33.labelWidth / 2)("y", transition_r33.labelY - transition_r33.labelHeight / 2)("width", transition_r33.labelWidth)("height", transition_r33.labelHeight);
    \u0275\u0275advance();
    \u0275\u0275attribute("x", transition_r33.labelX)("y", transition_r33.labelY);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(transition_r33.label);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r35 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 93);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_For_1_Template_div_mousedown_0_listener($event) {
      const $index_r36 = \u0275\u0275restoreView(_r35).$index;
      const transition_r37 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.startStateRouteDrag(transition_r37, $index_r36, $event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const $index_r36 = ctx.$index;
    const transition_r37 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(5);
    \u0275\u0275property("ngStyle", ctx_r1.stateRouteHitboxStyle(transition_r37, $index_r36));
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r34 = \u0275\u0275getCurrentView();
    \u0275\u0275repeaterCreate(0, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_For_1_Template, 1, 1, "div", 90, \u0275\u0275repeaterTrackByIndex);
    \u0275\u0275elementStart(2, "button", 91);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_Template_button_mousedown_2_listener($event) {
      const transition_r37 = \u0275\u0275restoreView(_r34).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.selectTransition(transition_r37.index, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "button", 92);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_Template_button_mousedown_3_listener($event) {
      const transition_r37 = \u0275\u0275restoreView(_r34).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.selectTransition(transition_r37.index, $event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const transition_r37 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(5);
    \u0275\u0275repeater(transition_r37.points.slice(0, -1));
    \u0275\u0275advance(2);
    \u0275\u0275property("ngStyle", ctx_r1.stateEndpointHandleStyle(transition_r37, "from"));
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.stateEndpointHandleStyle(transition_r37, "to"));
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_For_1_Template, 4, 2, null, null, _forTrack9);
  }
  if (rf & 2) {
    const panel_r29 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275repeater(panel_r29.board.transitions);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r39 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 98);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_0_Template_span_mousedown_0_listener($event) {
      \u0275\u0275restoreView(_r39);
      const node_r40 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startStateMarkerDrag(node_r40, $event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r40 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("ngStyle", ctx_r1.stateMarkerStyle(node_r40.marker));
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r41 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 99);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_4_Template_span_mousedown_0_listener($event) {
      \u0275\u0275restoreView(_r41);
      const node_r40 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startStateMarkerDrag(node_r40, $event));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r40 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("ngStyle", ctx_r1.stateMarkerStyle(node_r40.marker));
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Template(rf, ctx) {
  if (rf & 1) {
    const _r38 = \u0275\u0275getCurrentView();
    \u0275\u0275conditionalCreate(0, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_0_Template, 1, 1, "span", 94);
    \u0275\u0275elementStart(1, "div", 95);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Template_div_mousedown_1_listener($event) {
      const node_r40 = \u0275\u0275restoreView(_r38).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startStateNodeDrag(node_r40, $event));
    });
    \u0275\u0275elementStart(2, "span", 96);
    \u0275\u0275text(3);
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(4, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Conditional_4_Template, 1, 1, "span", 97);
  }
  if (rf & 2) {
    const node_r40 = ctx.$implicit;
    const \u0275$index_272_r42 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275conditional((node_r40.marker == null ? null : node_r40.marker.kind) === "initial" ? 0 : -1);
    \u0275\u0275advance();
    \u0275\u0275classProp("kind-initial", node_r40.kind === "initial")("kind-terminal", node_r40.kind === "terminal");
    \u0275\u0275property("ngStyle", ctx_r1.stateNodeStyle(node_r40));
    \u0275\u0275attribute("data-testid", "entity-state-node-" + \u0275$index_272_r42);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(node_r40.name);
    \u0275\u0275advance();
    \u0275\u0275conditional((node_r40.marker == null ? null : node_r40.marker.kind) === "terminal" ? 4 : -1);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_30_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 84);
    \u0275\u0275text(1, "\u6682\u65E0\u72B6\u6001\u503C\u3002");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r28 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "section", 66);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Template_section_click_0_listener() {
      const panel_r29 = \u0275\u0275restoreView(_r28).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setStateField(panel_r29.name));
    });
    \u0275\u0275elementStart(1, "div", 67)(2, "div")(3, "div", 68);
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "div", 69);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(7, "div", 70)(8, "span", 71);
    \u0275\u0275text(9);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(10, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_10_Template, 2, 0, "span", 72);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(11, "div", 73);
    \u0275\u0275repeaterCreate(12, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_13_Template, 2, 6, "span", 74, _forTrack4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "div", 75)(15, "div", 76)(16, "div", 77)(17, "div", 78);
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(18, "svg", 79)(19, "defs")(20, "marker", 80);
    \u0275\u0275element(21, "path", 81);
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(22, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_23_Template, 1, 2, ":svg:path", 82, _forTrack7);
    \u0275\u0275repeaterCreate(24, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_25_Template, 5, 12, null, null, _forTrack8);
    \u0275\u0275elementEnd();
    \u0275\u0275namespaceHTML();
    \u0275\u0275elementStart(26, "div", 83);
    \u0275\u0275conditionalCreate(27, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_27_Template, 2, 0);
    \u0275\u0275repeaterCreate(28, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_For_29_Template, 5, 9, null, null, _forTrack4);
    \u0275\u0275conditionalCreate(30, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Conditional_30_Template, 2, 0, "div", 84);
    \u0275\u0275elementEnd()()()()()();
  }
  if (rf & 2) {
    const panel_r29 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275classProp("active", panel_r29.active)("role-primary", panel_r29.role === "primary")("role-secondary", panel_r29.role === "secondary");
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(panel_r29.name || "\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("", panel_r29.values.length, " \u4E2A\u72B6\u6001\u503C");
    \u0275\u0275advance(2);
    \u0275\u0275classProp("role-primary", panel_r29.role === "primary")("role-secondary", panel_r29.role === "secondary");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.stateRoleLabel(panel_r29.role));
    \u0275\u0275advance();
    \u0275\u0275conditional(panel_r29.active ? 10 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(panel_r29.board.nodes);
    \u0275\u0275advance(2);
    \u0275\u0275classProp("readonly", !ctx_r1.canEditState() || !panel_r29.active);
    \u0275\u0275attribute("data-export-graph-id", panel_r29.active ? ctx_r1.exportGraphId || null : null);
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.stateZoomStyle(panel_r29.board));
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.stateZoomTargetStyle(panel_r29.board));
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.stateBoardStyle(panel_r29.board));
    \u0275\u0275advance();
    \u0275\u0275attribute("width", panel_r29.board.width)("height", panel_r29.board.height)("viewBox", "0 0 " + panel_r29.board.width + " " + panel_r29.board.height);
    \u0275\u0275advance(4);
    \u0275\u0275repeater(panel_r29.board.markerLinks);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(panel_r29.board.transitions);
    \u0275\u0275advance(2);
    \u0275\u0275property("ngStyle", ctx_r1.stateBoardStyle(panel_r29.board));
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.canEditState() ? 27 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(panel_r29.board.nodes);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(!panel_r29.board.nodes.length ? 30 : -1);
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_ForEmpty_19_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 60);
    \u0275\u0275text(1, " \u5F53\u524D\u5B9E\u4F53\u8FD8\u672A\u5B9A\u4E49\u72B6\u6001\u5B57\u6BB5\uFF0C\u8BF7\u5148\u5728\u5B57\u6BB5\u8868\u4E2D\u6807\u8BB0\u4E3B\u72B6\u6001\u6216\u5B50\u72B6\u6001\uFF0C\u518D\u5728\u5B57\u6BB5\u89C4\u5219\u4E2D\u586B\u5199\u72B6\u6001\u503C\u3002 ");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r20 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "aside", 47);
    \u0275\u0275repeaterCreate(1, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_2_Template, 6, 1, "div", 48, _forTrack4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 49)(4, "div", 50);
    \u0275\u0275listener("wheel", function EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_Template_div_wheel_4_listener($event) {
      \u0275\u0275restoreView(_r20);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onStateDiagramWheel($event));
    });
    \u0275\u0275elementStart(5, "div", 51)(6, "div", 52)(7, "div", 53)(8, "div")(9, "div", 54);
    \u0275\u0275text(10);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(11, "div", 55);
    \u0275\u0275text(12);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(13, "div", 56);
    \u0275\u0275repeaterCreate(14, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_15_Template, 2, 4, "button", 57, _forTrack5);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(16, "div", 58);
    \u0275\u0275repeaterCreate(17, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_For_18_Template, 31, 26, "section", 59, _forTrack5, false, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_ForEmpty_19_Template, 2, 0, "div", 60);
    \u0275\u0275elementEnd()()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.stateEntityGroups());
    \u0275\u0275advance(3);
    \u0275\u0275property("ngStyle", ctx_r1.stateMainShellStyle());
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate(ctx.name || "\u672A\u547D\u540D\u5B9E\u4F53");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate1("\u72B6\u6001\u603B\u89C8 \xB7 \u5F53\u524D\u7F16\u8F91\u5B57\u6BB5\uFF1A", ctx_r1.activeStateFieldLabel());
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.statePanels());
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.statePanels());
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 46);
    \u0275\u0275text(1, "\u6682\u65E0\u5B9E\u4F53\uFF0C\u70B9\u51FB\u201C\u65B0\u5EFA\u5B9E\u4F53\u201D\u5F00\u59CB\u8BBE\u8BA1\u3002");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14);
    \u0275\u0275conditionalCreate(1, EntityDesignWorkbenchComponent_Conditional_19_Conditional_1_Template, 20, 4)(2, EntityDesignWorkbenchComponent_Conditional_19_Conditional_2_Template, 2, 0, "div", 46);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    let tmp_1_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275conditional((tmp_1_0 = ctx_r1.selectedEntity()) ? 1 : 2, tmp_1_0);
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_For_26_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const construct_r45 = ctx.$implicit;
    const entity_r44 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("value", ctx_r1.constructId(construct_r45))("selected", ctx_r1.entityConstructValue(entity_r44) === ctx_r1.constructId(construct_r45));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.constructOptionLabel(construct_r45));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_31_For_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 128);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ref_r46 = ctx.$implicit;
    \u0275\u0275property("title", ref_r46.processName);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ref_r46.taskName);
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_31_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "section", 117)(1, "h4");
    \u0275\u0275text(2, "\u88AB\u4EE5\u4E0B\u6D41\u7A0B\u8282\u70B9\u5F15\u7528");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "div", 127);
    \u0275\u0275repeaterCreate(4, EntityDesignWorkbenchComponent_Conditional_20_Conditional_31_For_5_Template, 2, 2, "span", 128, _forTrack12);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const entity_r44 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(4);
    \u0275\u0275repeater(ctx_r1.entityReferences(entity_r44));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_For_6_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const type_r49 = ctx.$implicit;
    const field_r48 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275property("value", type_r49.value)("selected", ctx_r1.fieldTypeValue(field_r48) === type_r49.value);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(type_r49.label);
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template(rf, ctx) {
  if (rf & 1) {
    const _r47 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "tr", 131)(1, "td", 132)(2, "input", 133);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_input_input_2_listener($event) {
      const field_r48 = \u0275\u0275restoreView(_r47).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setField(field_r48, "name", $event.target.value));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(3, "td", 134)(4, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_select_change_4_listener($event) {
      const field_r48 = \u0275\u0275restoreView(_r47).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setField(field_r48, "type", $event.target.value));
    });
    \u0275\u0275repeaterCreate(5, EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_For_6_Template, 2, 3, "option", 114, _forTrack14);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(7, "td", 136)(8, "input", 137);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_input_change_8_listener($event) {
      const field_r48 = \u0275\u0275restoreView(_r47).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setFieldBoolean(field_r48, "is_key", $event.target.checked));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(9, "td", 138)(10, "span", 139)(11, "select", 140);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_select_change_11_listener($event) {
      const field_r48 = \u0275\u0275restoreView(_r47).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setFieldStatusRole(field_r48, $event.target.value));
    });
    \u0275\u0275elementStart(12, "option", 113);
    \u0275\u0275text(13, "\u5426");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "option", 141);
    \u0275\u0275text(15, "\u4E3B");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(16, "option", 142);
    \u0275\u0275text(17, "\u5B50");
    \u0275\u0275elementEnd()()()();
    \u0275\u0275elementStart(18, "td", 143)(19, "textarea", 144);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_textarea_input_19_listener($event) {
      const field_r48 = \u0275\u0275restoreView(_r47).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.setField(field_r48, "note", $event.target.value));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(20, "td", 145)(21, "div", 146)(22, "button", 147);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_button_click_22_listener() {
      \u0275\u0275restoreView(_r47);
      const entity_r44 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addField(entity_r44));
    });
    \u0275\u0275text(23, "+");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(24, "button", 148);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_button_click_24_listener() {
      const \u0275$index_398_r50 = \u0275\u0275restoreView(_r47).$index;
      const entity_r44 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveField(entity_r44, \u0275$index_398_r50, -1));
    });
    \u0275\u0275text(25, "\u2191");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(26, "button", 149);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_button_click_26_listener() {
      const \u0275$index_398_r50 = \u0275\u0275restoreView(_r47).$index;
      const entity_r44 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveField(entity_r44, \u0275$index_398_r50, 1));
    });
    \u0275\u0275text(27, "\u2193");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(28, "button", 150);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template_button_click_28_listener() {
      const \u0275$index_398_r50 = \u0275\u0275restoreView(_r47).$index;
      const entity_r44 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removeField(entity_r44, \u0275$index_398_r50));
    });
    \u0275\u0275text(29, "\u2715");
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const field_r48 = ctx.$implicit;
    const \u0275$index_398_r50 = ctx.$index;
    const entity_r44 = \u0275\u0275nextContext(2);
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275property("value", field_r48.name || "");
    \u0275\u0275attribute("data-testid", "entity-field-name-" + \u0275$index_398_r50);
    \u0275\u0275advance(2);
    \u0275\u0275property("value", ctx_r1.fieldTypeValue(field_r48));
    \u0275\u0275attribute("data-testid", "entity-field-type-" + \u0275$index_398_r50)("data-selected-type", ctx_r1.fieldTypeValue(field_r48));
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.fieldTypes);
    \u0275\u0275advance(3);
    \u0275\u0275property("checked", field_r48.is_key);
    \u0275\u0275advance(2);
    \u0275\u0275classProp("field-status-role-wrap-none", !field_r48.status_role)("field-status-role-wrap-primary", field_r48.status_role === "primary")("field-status-role-wrap-secondary", field_r48.status_role === "secondary");
    \u0275\u0275advance();
    \u0275\u0275classProp("field-status-role-select-none", !field_r48.status_role)("field-status-role-select-primary", field_r48.status_role === "primary")("field-status-role-select-secondary", field_r48.status_role === "secondary");
    \u0275\u0275property("value", field_r48.status_role || "");
    \u0275\u0275attribute("data-testid", "entity-status-role-" + \u0275$index_398_r50);
    \u0275\u0275advance(8);
    \u0275\u0275styleProp("height", ctx_r1.fieldRuleHeight(field_r48), "px");
    \u0275\u0275property("value", field_r48.note || field_r48.state_values || "");
    \u0275\u0275advance(3);
    \u0275\u0275attribute("data-testid", "entity-field-add-after-" + \u0275$index_398_r50);
    \u0275\u0275advance(2);
    \u0275\u0275property("disabled", \u0275$index_398_r50 === 0);
    \u0275\u0275attribute("data-testid", "entity-field-move-up-" + \u0275$index_398_r50);
    \u0275\u0275advance(2);
    \u0275\u0275property("disabled", \u0275$index_398_r50 === (entity_r44.fields || \u0275\u0275pureFunction0(29, _c0)).length - 1);
    \u0275\u0275attribute("data-testid", "entity-field-move-down-" + \u0275$index_398_r50);
    \u0275\u0275advance(2);
    \u0275\u0275attribute("data-testid", "entity-field-delete-" + \u0275$index_398_r50);
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "table", 120)(1, "thead")(2, "tr")(3, "th");
    \u0275\u0275text(4, "\u5B57\u6BB5\u540D");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "th");
    \u0275\u0275text(6, "\u7C7B\u578B");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(7, "th", 129);
    \u0275\u0275text(8, "\u4E3B\u952E");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "th", 130);
    \u0275\u0275text(10, "\u72B6\u6001");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(11, "th");
    \u0275\u0275text(12, "\u5B57\u6BB5\u89C4\u5219");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(13, "th");
    \u0275\u0275text(14, "\u64CD\u4F5C");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(15, "tbody");
    \u0275\u0275repeaterCreate(16, EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_For_17_Template, 30, 30, "tr", 131, _forTrack13, true);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const entity_r44 = \u0275\u0275nextContext();
    \u0275\u0275advance(16);
    \u0275\u0275repeater(entity_r44.fields || \u0275\u0275pureFunction0(0, _c0));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_38_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "p", 121);
    \u0275\u0275text(1, "\u6682\u65E0\u5B57\u6BB5");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_48_For_26_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "tr")(1, "td");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "td");
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "td");
    \u0275\u0275text(6);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(7, "td")(8, "code");
    \u0275\u0275text(9);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(10, "td");
    \u0275\u0275text(11);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(12, "td");
    \u0275\u0275text(13);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "td");
    \u0275\u0275text(15);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(16, "td");
    \u0275\u0275text(17);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "td");
    \u0275\u0275text(19);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const row_r51 = ctx.$implicit;
    \u0275\u0275classProp("visible-row", row_r51.visible);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.scope);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.index);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.owner || "-");
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate(row_r51.raw);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.resolvedFrom || "-");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.resolvedTo || "-");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.fromSelectValue || "-");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.toSelectValue || "-");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(row_r51.visible ? "\u662F" : "\u5426");
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Conditional_48_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 125)(1, "div", 151);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "table")(4, "thead")(5, "tr")(6, "th");
    \u0275\u0275text(7, "\u6765\u6E90");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "th");
    \u0275\u0275text(9, "\u5E8F\u53F7");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(10, "th");
    \u0275\u0275text(11, "\u5F52\u5C5E");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(12, "th");
    \u0275\u0275text(13, "\u539F\u59CB\u503C");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(14, "th");
    \u0275\u0275text(15, "\u89E3\u6790 from");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(16, "th");
    \u0275\u0275text(17, "\u89E3\u6790 to");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "th");
    \u0275\u0275text(19, "select from");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(20, "th");
    \u0275\u0275text(21, "select to");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(22, "th");
    \u0275\u0275text(23, "\u547D\u4E2D\u5F53\u524D");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(24, "tbody");
    \u0275\u0275repeaterCreate(25, EntityDesignWorkbenchComponent_Conditional_20_Conditional_48_For_26_Template, 20, 11, "tr", 152, _forTrack15);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const entity_r44 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate2("\u5173\u7CFB\u8C03\u8BD5\uFF1A", entity_r44.name || ctx_r1.entityId(entity_r44), " / ", ctx_r1.entityId(entity_r44));
    \u0275\u0275advance(23);
    \u0275\u0275repeater(ctx_r1.relationDebugRows(entity_r44));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_For_50_For_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const source_r54 = ctx.$implicit;
    const relation_r53 = \u0275\u0275nextContext().$implicit;
    const entity_r44 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("value", ctx_r1.entityId(source_r54))("selected", ctx_r1.entityId(source_r54) === (ctx_r1.relationFromValue(relation_r53) || ctx_r1.entityId(entity_r44)));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(source_r54.name || ctx_r1.entityId(source_r54));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_For_50_For_17_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const target_r55 = ctx.$implicit;
    const relation_r53 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("value", ctx_r1.entityId(target_r55))("selected", ctx_r1.entityId(target_r55) === ctx_r1.relationToValue(relation_r53));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(target_r55.name || ctx_r1.entityId(target_r55));
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template(rf, ctx) {
  if (rf & 1) {
    const _r52 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 126)(1, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_select_change_1_listener($event) {
      const relation_r53 = \u0275\u0275restoreView(_r52).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setRelation(relation_r53, "from", $event.target.value));
    });
    \u0275\u0275elementStart(2, "option", 153);
    \u0275\u0275text(3, "\u8BF7\u9009\u62E9\u5B9E\u4F53");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(4, EntityDesignWorkbenchComponent_Conditional_20_For_50_For_5_Template, 2, 3, "option", 114, _forTrack6, true);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "select", 154);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_select_change_6_listener($event) {
      const relation_r53 = \u0275\u0275restoreView(_r52).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setRelation(relation_r53, "type", $event.target.value));
    });
    \u0275\u0275elementStart(7, "option", 155);
    \u0275\u0275text(8, "1:1");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "option", 156);
    \u0275\u0275text(10, "1:N");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(11, "option", 157);
    \u0275\u0275text(12, "N:N");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(13, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_select_change_13_listener($event) {
      const relation_r53 = \u0275\u0275restoreView(_r52).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setRelation(relation_r53, "to", $event.target.value));
    });
    \u0275\u0275elementStart(14, "option", 153);
    \u0275\u0275text(15, "\u8BF7\u9009\u62E9\u5B9E\u4F53");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(16, EntityDesignWorkbenchComponent_Conditional_20_For_50_For_17_Template, 2, 3, "option", 114, _forTrack6, true);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "input", 158);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_input_input_18_listener($event) {
      const relation_r53 = \u0275\u0275restoreView(_r52).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setRelation(relation_r53, "label", $event.target.value));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(19, "div", 159)(20, "button", 160);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_button_click_20_listener() {
      \u0275\u0275restoreView(_r52);
      const entity_r44 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addRelation(entity_r44));
    });
    \u0275\u0275text(21, "+");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(22, "button", 161);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_button_click_22_listener() {
      const \u0275$index_543_r56 = \u0275\u0275restoreView(_r52).$index;
      const entity_r44 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveRelation(entity_r44, \u0275$index_543_r56, -1));
    });
    \u0275\u0275text(23, "\u2191");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(24, "button", 162);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_button_click_24_listener() {
      const \u0275$index_543_r56 = \u0275\u0275restoreView(_r52).$index;
      const entity_r44 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveRelation(entity_r44, \u0275$index_543_r56, 1));
    });
    \u0275\u0275text(25, "\u2193");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(26, "button", 150);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_For_50_Template_button_click_26_listener() {
      const \u0275$index_543_r56 = \u0275\u0275restoreView(_r52).$index;
      const entity_r44 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removeRelation(entity_r44, \u0275$index_543_r56));
    });
    \u0275\u0275text(27, "\u2715");
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const relation_r53 = ctx.$implicit;
    const \u0275$index_543_r56 = ctx.$index;
    const entity_r44 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("value", ctx_r1.relationFromValue(relation_r53) || ctx_r1.entityId(entity_r44));
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.entities());
    \u0275\u0275advance(2);
    \u0275\u0275property("value", ctx_r1.relationTypeValue(relation_r53));
    \u0275\u0275advance(7);
    \u0275\u0275property("value", ctx_r1.relationToValue(relation_r53));
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.entities());
    \u0275\u0275advance(2);
    \u0275\u0275property("value", relation_r53.label || "");
    \u0275\u0275advance(4);
    \u0275\u0275property("disabled", \u0275$index_543_r56 === 0);
    \u0275\u0275advance(2);
    \u0275\u0275property("disabled", \u0275$index_543_r56 === ctx_r1.relationsForEntity(entity_r44).length - 1);
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_ForEmpty_51_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "p", 121);
    \u0275\u0275text(1, "\u5F53\u524D\u5B9E\u4F53\u6682\u65E0\u5173\u7CFB");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_20_Template(rf, ctx) {
  if (rf & 1) {
    const _r43 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "aside", 100)(1, "div", 101);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_20_Template_div_mousedown_1_listener($event) {
      \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.startDrawerResize($event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "div", 102)(3, "div", 103)(4, "span");
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(6, "div", 104)(7, "button", 105);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_7_listener() {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.duplicateEntity(entity_r44));
    });
    \u0275\u0275text(8, "\u590D\u5236\u5B9E\u4F53");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "button", 106);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_9_listener() {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removeEntity(entity_r44));
    });
    \u0275\u0275text(10, "\u5220\u9664");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(11, "button", 107);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_11_listener() {
      \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.editorOpen.set(false));
    });
    \u0275\u0275text(12, "\u2715");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(13, "div", 108)(14, "div", 109)(15, "div", 110)(16, "label");
    \u0275\u0275text(17, "\u5B9E\u4F53\u540D\u79F0");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "input", 111);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_20_Template_input_input_18_listener($event) {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setEntityName(entity_r44, $event.target.value));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(19, "div", 110)(20, "label");
    \u0275\u0275text(21, "\u6240\u5C5E\u4E1A\u52A1\u6784\u4EF6");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(22, "select", 112);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_20_Template_select_change_22_listener($event) {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setEntityConstruct(entity_r44, $event.target.value));
    });
    \u0275\u0275elementStart(23, "option", 113);
    \u0275\u0275text(24, "\u672A\u5206\u7EC4\u6784\u4EF6");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(25, EntityDesignWorkbenchComponent_Conditional_20_For_26_Template, 2, 3, "option", 114, _forTrack10, true);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(27, "div", 115)(28, "label");
    \u0275\u0275text(29, "\u8BF4\u660E");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(30, "input", 116);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_20_Template_input_input_30_listener($event) {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setEntityNote(entity_r44, $event.target.value));
    });
    \u0275\u0275elementEnd()()();
    \u0275\u0275conditionalCreate(31, EntityDesignWorkbenchComponent_Conditional_20_Conditional_31_Template, 6, 0, "section", 117);
    \u0275\u0275elementStart(32, "section", 118)(33, "h4");
    \u0275\u0275text(34, "\u5B57\u6BB5 ");
    \u0275\u0275elementStart(35, "button", 119);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_35_listener() {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addField(entity_r44));
    });
    \u0275\u0275text(36, "\uFF0B");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(37, EntityDesignWorkbenchComponent_Conditional_20_Conditional_37_Template, 18, 1, "table", 120)(38, EntityDesignWorkbenchComponent_Conditional_20_Conditional_38_Template, 2, 0, "p", 121);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(39, "section", 118)(40, "h4");
    \u0275\u0275text(41, " \u5B9E\u4F53\u5173\u7CFB ");
    \u0275\u0275elementStart(42, "button", 122);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_42_listener() {
      const entity_r44 = \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addRelation(entity_r44));
    });
    \u0275\u0275text(43, "\uFF0B");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(44, "button", 123);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_20_Template_button_click_44_listener() {
      \u0275\u0275restoreView(_r43);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.toggleRelationDebug());
    });
    \u0275\u0275text(45);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(46, "p", 124);
    \u0275\u0275text(47, "\u4EC5\u663E\u793A\u4E0E\u5F53\u524D\u5B9E\u4F53\u76F4\u63A5\u76F8\u5173\u7684\u5173\u7CFB\uFF0C\u51CF\u5C11\u5168\u5C40\u566A\u97F3\u3002");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(48, EntityDesignWorkbenchComponent_Conditional_20_Conditional_48_Template, 27, 2, "div", 125);
    \u0275\u0275repeaterCreate(49, EntityDesignWorkbenchComponent_Conditional_20_For_50_Template, 28, 6, "div", 126, _forTrack11, false, EntityDesignWorkbenchComponent_Conditional_20_ForEmpty_51_Template, 2, 0, "p", 121);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const entity_r44 = ctx;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275styleProp("width", ctx_r1.drawerWidth(), "px");
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate(entity_r44.name || "\u672A\u547D\u540D");
    \u0275\u0275advance(13);
    \u0275\u0275property("value", entity_r44.name || "");
    \u0275\u0275advance(4);
    \u0275\u0275property("value", ctx_r1.entityConstructValue(entity_r44));
    \u0275\u0275attribute("data-selected-construct", ctx_r1.entityConstructValue(entity_r44));
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.constructs());
    \u0275\u0275advance(5);
    \u0275\u0275property("value", entity_r44.note || "");
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.entityReferences(entity_r44).length ? 31 : -1);
    \u0275\u0275advance(6);
    \u0275\u0275conditional((entity_r44.fields || \u0275\u0275pureFunction0(13, _c0)).length ? 37 : 38);
    \u0275\u0275advance(5);
    \u0275\u0275property("disabled", ctx_r1.entities().length < 2);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1(" ", ctx_r1.relationDebugEnabled() ? "\u5173\u95ED\u8BCA\u65AD" : "\u8BCA\u65AD", " ");
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.relationDebugEnabled() ? 48 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.relationsForEntity(entity_r44));
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_For_27_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 174);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const field_r58 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("value", field_r58.name || "");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.stateFieldOptionLabel(field_r58));
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_For_44_Template(rf, ctx) {
  if (rf & 1) {
    const _r59 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "label", 181)(1, "span", 186);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_21_For_44_Template_select_change_3_listener($event) {
      const node_r60 = \u0275\u0275restoreView(_r59).$implicit;
      const entity_r61 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setStateNodeKind(entity_r61, node_r60.name, $event.target.value));
    });
    \u0275\u0275elementStart(4, "option", 187);
    \u0275\u0275text(5, "\u521D\u59CB\u72B6\u6001");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "option", 188);
    \u0275\u0275text(7, "\u4E2D\u95F4\u72B6\u6001");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "option", 189);
    \u0275\u0275text(9, "\u7ED3\u675F\u72B6\u6001");
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const node_r60 = ctx.$implicit;
    const \u0275$index_670_r62 = ctx.$index;
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(node_r60.name);
    \u0275\u0275advance();
    \u0275\u0275property("value", node_r60.kind);
    \u0275\u0275attribute("data-testid", "entity-state-kind-" + \u0275$index_670_r62);
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_ForEmpty_45_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "p", 182);
    \u0275\u0275text(1, "\u6682\u65E0\u8282\u70B9\u7C7B\u578B\u3002");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_Conditional_46_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "p", 171);
    \u0275\u0275text(1, "\u540C\u4E00\u5B9E\u4F53\u7684\u591A\u4E2A\u72B6\u6001\u5B57\u6BB5\u5DF2\u540C\u6B65\u5C55\u793A\u5728\u5DE6\u4FA7\u603B\u89C8\u91CC\uFF1B\u5F53\u524D\u7F16\u8F91\u533A\u53EA\u843D\u5728\u53F3\u4FA7\u9009\u4E2D\u7684\u90A3\u4E00\u4E2A\u5B57\u6BB5\u4E0A\u3002");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_For_54_For_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const value_r65 = ctx.$implicit;
    const item_r64 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("value", value_r65)("selected", value_r65 === item_r64.transition.from);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(value_r65);
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_For_54_For_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 114);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const value_r66 = ctx.$implicit;
    const item_r64 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275property("value", value_r66)("selected", value_r66 === item_r64.transition.to);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(value_r66);
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template(rf, ctx) {
  if (rf & 1) {
    const _r63 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 185)(1, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_select_change_1_listener($event) {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setTransition(item_r64.transition, "from", $event.target.value));
    });
    \u0275\u0275repeaterCreate(2, EntityDesignWorkbenchComponent_Conditional_21_For_54_For_3_Template, 2, 3, "option", 114, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "span", 190);
    \u0275\u0275text(5, "\u2192");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "select", 135);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_select_change_6_listener($event) {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setTransition(item_r64.transition, "to", $event.target.value));
    });
    \u0275\u0275repeaterCreate(7, EntityDesignWorkbenchComponent_Conditional_21_For_54_For_8_Template, 2, 3, "option", 114, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "input", 191);
    \u0275\u0275listener("input", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_input_input_9_listener($event) {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setTransitionNote(item_r64.transition, $event.target.value));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(10, "div", 192)(11, "button", 193);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_button_click_11_listener() {
      \u0275\u0275restoreView(_r63);
      const entity_r61 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.addTransition(entity_r61));
    });
    \u0275\u0275text(12, "+");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(13, "button", 194);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_button_click_13_listener() {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const entity_r61 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveTransition(entity_r61, item_r64.index, -1));
    });
    \u0275\u0275text(14, "\u2191");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(15, "button", 195);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_button_click_15_listener() {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const entity_r61 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveTransition(entity_r61, item_r64.index, 1));
    });
    \u0275\u0275text(16, "\u2193");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(17, "button", 196);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_21_For_54_Template_button_click_17_listener() {
      const item_r64 = \u0275\u0275restoreView(_r63).$implicit;
      const entity_r61 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removeTransition(entity_r61, item_r64.index));
    });
    \u0275\u0275text(18, "\xD7");
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const item_r64 = ctx.$implicit;
    const \u0275$index_705_r67 = ctx.$index;
    const entity_r61 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance();
    \u0275\u0275property("value", item_r64.transition.from || "");
    \u0275\u0275attribute("data-testid", "entity-transition-from-" + \u0275$index_705_r67);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.transitionStateOptions(item_r64.transition));
    \u0275\u0275advance(4);
    \u0275\u0275property("value", item_r64.transition.to || "");
    \u0275\u0275attribute("data-testid", "entity-transition-to-" + \u0275$index_705_r67);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.transitionStateOptions(item_r64.transition));
    \u0275\u0275advance(2);
    \u0275\u0275property("value", item_r64.transition.note || item_r64.transition.action || item_r64.transition.label || "");
    \u0275\u0275attribute("data-testid", "entity-transition-note-" + item_r64.index);
    \u0275\u0275advance(4);
    \u0275\u0275property("disabled", \u0275$index_705_r67 === 0);
    \u0275\u0275advance(2);
    \u0275\u0275property("disabled", \u0275$index_705_r67 === ctx_r1.selectedStateTransitions(entity_r61).length - 1);
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_ForEmpty_55_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "p", 121);
    \u0275\u0275text(1, "\u6682\u65E0\u72B6\u6001\u6D41\u8F6C\uFF0C\u5148\u6DFB\u52A0\u4E00\u6761\u8FB9\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F \u2192 \u5F85\u5BA1\u6838\u3002");
    \u0275\u0275elementEnd();
  }
}
function EntityDesignWorkbenchComponent_Conditional_21_Template(rf, ctx) {
  if (rf & 1) {
    const _r57 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "aside", 163)(1, "div", 164);
    \u0275\u0275listener("mousedown", function EntityDesignWorkbenchComponent_Conditional_21_Template_div_mousedown_1_listener($event) {
      \u0275\u0275restoreView(_r57);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.startStateDrawerResize($event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "div", 165)(3, "div", 103)(4, "span");
    \u0275\u0275text(5, "\u72B6\u6001\u56FE\u7F16\u8F91");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "span", 166);
    \u0275\u0275text(7, "/");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "span");
    \u0275\u0275text(9);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(10, "div", 104)(11, "button", 167);
    \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Conditional_21_Template_button_click_11_listener() {
      \u0275\u0275restoreView(_r57);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.stateEditorOpen.set(false));
    });
    \u0275\u0275text(12, "\xD7");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(13, "div", 168)(14, "div", 169)(15, "div", 170)(16, "div")(17, "h4");
    \u0275\u0275text(18, "\u72B6\u6001\u56FE\u7F16\u8F91");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(19, "p", 171);
    \u0275\u0275text(20, "\u5DE6\u4FA7\u603B\u89C8\u4F1A\u540C\u65F6\u5C55\u793A\u4E3B\u72B6\u6001\u5B57\u6BB5\u548C\u6240\u6709\u5B50\u72B6\u6001\u5B57\u6BB5\uFF1B\u53F3\u4FA7\u53EA\u7F16\u8F91\u5F53\u524D\u9009\u4E2D\u7684\u72B6\u6001\u5B57\u6BB5\uFF0C\u70B9\u51FB\u603B\u89C8\u5757\u53EF\u5FEB\u901F\u5207\u6362\u3002");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(21, "div", 172)(22, "label", 110)(23, "span");
    \u0275\u0275text(24, "\u72B6\u6001\u5B57\u6BB5");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(25, "select", 173);
    \u0275\u0275listener("change", function EntityDesignWorkbenchComponent_Conditional_21_Template_select_change_25_listener($event) {
      \u0275\u0275restoreView(_r57);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setStateField($event.target.value));
    });
    \u0275\u0275repeaterCreate(26, EntityDesignWorkbenchComponent_Conditional_21_For_27_Template, 2, 2, "option", 174, _forTrack13, true);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(28, "label", 175)(29, "span");
    \u0275\u0275text(30, "\u72B6\u6001\u503C\u6765\u6E90 ");
    \u0275\u0275elementStart(31, "span", 176);
    \u0275\u0275text(32, "?");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(33, "div", 177);
    \u0275\u0275text(34);
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(35, "div", 178)(36, "div", 179)(37, "div")(38, "h5");
    \u0275\u0275text(39, "\u72B6\u6001\u8282\u70B9\u5C5E\u6027");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(40, "p", 171);
    \u0275\u0275text(41, "\u6BCF\u4E2A\u72B6\u6001\u90FD\u9700\u8981\u6807\u8BB0\u4E3A\u521D\u59CB\u72B6\u6001\u3001\u4E2D\u95F4\u72B6\u6001\u6216\u7ED3\u675F\u72B6\u6001\u3002\u56FE\u4E0A\u4F1A\u6309\u8FD9\u4E2A\u5C5E\u6027\u505A\u7EB5\u5411\u5C55\u5F00\u3002");
    \u0275\u0275elementEnd()()();
    \u0275\u0275elementStart(42, "div", 180);
    \u0275\u0275repeaterCreate(43, EntityDesignWorkbenchComponent_Conditional_21_For_44_Template, 10, 3, "label", 181, _forTrack4, false, EntityDesignWorkbenchComponent_Conditional_21_ForEmpty_45_Template, 2, 0, "p", 182);
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(46, EntityDesignWorkbenchComponent_Conditional_21_Conditional_46_Template, 2, 0, "p", 171);
    \u0275\u0275elementStart(47, "div", 183)(48, "h5");
    \u0275\u0275text(49, "\u72B6\u6001\u6D41\u8F6C");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(50, "p", 171);
    \u0275\u0275text(51, "\u7EF4\u62A4\u5F53\u524D\u72B6\u6001\u5B57\u6BB5\u7684\u6D41\u8F6C\u8D77\u70B9\u3001\u7EC8\u70B9\u548C\u8BF4\u660E\u3002");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(52, "div", 184);
    \u0275\u0275repeaterCreate(53, EntityDesignWorkbenchComponent_Conditional_21_For_54_Template, 19, 8, "div", 185, _forTrack16, false, EntityDesignWorkbenchComponent_Conditional_21_ForEmpty_55_Template, 2, 0, "p", 121);
    \u0275\u0275elementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275styleProp("width", ctx_r1.stateDrawerWidth(), "px");
    \u0275\u0275advance(9);
    \u0275\u0275textInterpolate(ctx_r1.selectedStateFieldOptionLabel());
    \u0275\u0275advance(16);
    \u0275\u0275property("value", ctx_r1.stateFieldName());
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.stateFields());
    \u0275\u0275advance(8);
    \u0275\u0275textInterpolate(ctx_r1.stateValues().join(" / ") || "\u8BF7\u5728\u4E0B\u65B9\u201C\u5B57\u6BB5\u89C4\u5219\u201D\u4E2D\u586B\u5199\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F/\u5F85\u5BA1\u6838/\u5BA1\u6838\u901A\u8FC7/\u5DF2\u4F5C\u5E9F");
    \u0275\u0275advance(9);
    \u0275\u0275repeater(ctx_r1.stateBoard().nodes);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.stateFields().length > 1 ? 46 : -1);
    \u0275\u0275advance(7);
    \u0275\u0275repeater(ctx_r1.selectedStateTransitions(ctx));
  }
}
var EntityDesignWorkbenchComponent = class _EntityDesignWorkbenchComponent {
  showEditorToggle = true;
  exportGraphId = "";
  focusConstructId = "";
  initialView = "relation";
  initialEntityId = "";
  editRequested = new EventEmitter();
  set editing(value) {
    const editable = Boolean(value);
    this.externalEditing.set(editable);
    if (!editable) {
      this.editorOpen.set(false);
      this.stateEditorOpen.set(false);
      return;
    }
    if (this.view() === "state") {
      this.ensureActiveStateField(this.selectedEntity());
      this.stateEditorOpen.set(true);
    }
  }
  // 远端同步后通过 blm-workbench-refresh 事件刷新视图
  onRefresh = () => {
    this.version.update((v) => v + 1);
  };
  ngOnInit() {
    this.view.set(this.initialView);
    const startupEntityId = String(this.initialEntityId || getAngularRuntimeState().ui["entityId"] || "").trim();
    if (startupEntityId) {
      this.selectedEntityId.set(startupEntityId);
      this.selectedEntityIds.set(/* @__PURE__ */ new Set([startupEntityId]));
      this.editorOpen.set(true);
    }
    if (this.view() === "state") {
      this.ensureActiveStateField(this.selectedEntity());
      this.stateEditorOpen.set(this.externalEditing());
    }
    window.addEventListener("blm-workbench-refresh", this.onRefresh);
  }
  ngOnChanges(changes) {
    if (!changes["initialEntityId"] || changes["initialEntityId"].firstChange)
      return;
    const nextId = String(this.initialEntityId || "").trim();
    if (!nextId || nextId === this.selectedEntityId())
      return;
    this.selectedEntityId.set(nextId);
    this.selectedEntityIds.set(/* @__PURE__ */ new Set([nextId]));
    this.syncRuntimeEntityId(nextId);
  }
  ngOnDestroy() {
    window.removeEventListener("blm-workbench-refresh", this.onRefresh);
  }
  // 模块意图：实体设计在构件工作台内独立运行，复刻旧实体关系图/状态图的核心体验，但不调用 entity-legacy 渲染函数。
  changeDetector = inject2(ChangeDetectorRef);
  nodeWidth = 120;
  nodeHeight = 38;
  entityGapX = 40;
  entityGapY = 70;
  entityPad = 20;
  groupHeaderHeight = 28;
  groupPadX = 22;
  groupPadY = 18;
  groupGapX = 56;
  groupGapY = 56;
  componentPadX = 18;
  componentPadY = 18;
  componentHeaderHeight = 28;
  view = signal("relation", ...ngDevMode ? [{ debugName: "view" }] : (
    /* istanbul ignore next */
    []
  ));
  version = signal(0, ...ngDevMode ? [{ debugName: "version" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedEntityId = signal("", ...ngDevMode ? [{ debugName: "selectedEntityId" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedEntityIds = signal(/* @__PURE__ */ new Set(), ...ngDevMode ? [{ debugName: "selectedEntityIds" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedTransitionIndex = signal(null, ...ngDevMode ? [{ debugName: "selectedTransitionIndex" }] : (
    /* istanbul ignore next */
    []
  ));
  editorOpen = signal(false, ...ngDevMode ? [{ debugName: "editorOpen" }] : (
    /* istanbul ignore next */
    []
  ));
  stateEditorOpen = signal(false, ...ngDevMode ? [{ debugName: "stateEditorOpen" }] : (
    /* istanbul ignore next */
    []
  ));
  relationDebugOpen = signal(false, ...ngDevMode ? [{ debugName: "relationDebugOpen" }] : (
    /* istanbul ignore next */
    []
  ));
  externalEditing = signal(false, ...ngDevMode ? [{ debugName: "externalEditing" }] : (
    /* istanbul ignore next */
    []
  ));
  stateFieldName = signal("", ...ngDevMode ? [{ debugName: "stateFieldName" }] : (
    /* istanbul ignore next */
    []
  ));
  drawerWidth = signal(620, ...ngDevMode ? [{ debugName: "drawerWidth" }] : (
    /* istanbul ignore next */
    []
  ));
  stateDrawerWidth = signal(620, ...ngDevMode ? [{ debugName: "stateDrawerWidth" }] : (
    /* istanbul ignore next */
    []
  ));
  relationZoom = signal(1, ...ngDevMode ? [{ debugName: "relationZoom" }] : (
    /* istanbul ignore next */
    []
  ));
  stateZoom = signal(1, ...ngDevMode ? [{ debugName: "stateZoom" }] : (
    /* istanbul ignore next */
    []
  ));
  selectionBox = signal(null, ...ngDevMode ? [{ debugName: "selectionBox" }] : (
    /* istanbul ignore next */
    []
  ));
  adapter = createEntityDesignLegacyAdapter();
  palette = [
    { color: "#1d4ed8", fill: "#dbeafe", stroke: "#3b82f6" },
    { color: "#047857", fill: "#dcfce7", stroke: "#22c55e" },
    { color: "#a16207", fill: "#fef9c3", stroke: "#eab308" },
    { color: "#be185d", fill: "#fce7f3", stroke: "#ec4899" },
    { color: "#6d28d9", fill: "#ede9fe", stroke: "#8b5cf6" },
    { color: "#0e7490", fill: "#cffafe", stroke: "#06b6d4" },
    { color: "#c2410c", fill: "#ffedd5", stroke: "#f97316" }
  ];
  relationStrokeColors = ["#3b82f6", "#22c55e", "#eab308", "#ec4899", "#8b5cf6", "#f97316"];
  fieldTypes = [
    { value: "string", label: "\u5B57\u7B26" },
    { value: "number", label: "\u6570\u503C" },
    { value: "decimal", label: "\u91D1\u989D" },
    { value: "date", label: "\u65E5\u671F" },
    { value: "datetime", label: "\u65E5\u671F\u65F6\u95F4" },
    { value: "boolean", label: "\u5E03\u5C14" },
    { value: "enum", label: "\u679A\u4E3E" },
    { value: "text", label: "\u957F\u6587\u672C" },
    { value: "id", label: "\u6807\u8BC6ID" },
    { value: "list", label: "\u5217\u8868" }
  ];
  dragState = null;
  stateNodeDragState = null;
  stateLabelDragState = null;
  stateRouteDragState = null;
  entities = computed(() => {
    this.version();
    const focusConstructId = String(this.focusConstructId || "").trim();
    const entities = this.adapter.entities();
    if (!focusConstructId || this.view() !== "relation")
      return entities;
    return entities.filter((entity) => this.entityConstructValue(entity) === focusConstructId);
  }, ...ngDevMode ? [{ debugName: "entities" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedEntity = computed(() => {
    const selectedId = this.selectedEntityId();
    return this.entities().find((entity) => this.entityId(entity) === selectedId) || this.entities()[0] || null;
  }, ...ngDevMode ? [{ debugName: "selectedEntity" }] : (
    /* istanbul ignore next */
    []
  ));
  nodes = computed(() => this.layoutEntities(), ...ngDevMode ? [{ debugName: "nodes" }] : (
    /* istanbul ignore next */
    []
  ));
  relationLines = computed(() => this.collectRelationLines(), ...ngDevMode ? [{ debugName: "relationLines" }] : (
    /* istanbul ignore next */
    []
  ));
  stateFields = computed(() => this.collectStateFields(this.selectedEntity()), ...ngDevMode ? [{ debugName: "stateFields" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedStateField = computed(() => {
    const fields = this.stateFields();
    const selected = this.stateFieldName();
    return fields.find((field) => String(field.name || "") === selected) || fields[0] || null;
  }, ...ngDevMode ? [{ debugName: "selectedStateField" }] : (
    /* istanbul ignore next */
    []
  ));
  stateValues = computed(() => this.collectStateValues(this.selectedEntity(), this.selectedStateField()?.name || ""), ...ngDevMode ? [{ debugName: "stateValues" }] : (
    /* istanbul ignore next */
    []
  ));
  statePanels = computed(() => {
    this.version();
    return this.buildStatePanels(this.selectedEntity());
  }, ...ngDevMode ? [{ debugName: "statePanels" }] : (
    /* istanbul ignore next */
    []
  ));
  stateEntityGroups = computed(() => this.groupStateEntities(), ...ngDevMode ? [{ debugName: "stateEntityGroups" }] : (
    /* istanbul ignore next */
    []
  ));
  groupFrames = computed(() => this.computeGroupFrames(this.nodes()), ...ngDevMode ? [{ debugName: "groupFrames" }] : (
    /* istanbul ignore next */
    []
  ));
  componentFrames = computed(() => this.computeComponentFrames(this.groupFrames()), ...ngDevMode ? [{ debugName: "componentFrames" }] : (
    /* istanbul ignore next */
    []
  ));
  stateBoard = computed(() => {
    this.version();
    return this.layoutStateBoard(this.selectedEntity());
  }, ...ngDevMode ? [{ debugName: "stateBoard" }] : (
    /* istanbul ignore next */
    []
  ));
  setView(view) {
    this.view.set(view);
    if (view === "state") {
      this.ensureActiveStateField(this.selectedEntity());
      this.stateEditorOpen.set(this.externalEditing() && !this.showEditorToggle ? true : false);
    }
  }
  drawerGridColumns() {
    if (this.view() === "state")
      return "minmax(0, 1fr)";
    return "minmax(0, 1fr)";
  }
  relationDrawerOffset() {
    return this.view() === "relation" && this.editorOpen() && this.selectedEntity() ? this.drawerWidth() : 0;
  }
  canEditRelation() {
    return this.externalEditing() || this.editorOpen();
  }
  canEditState() {
    return this.externalEditing() || this.editorOpen() || this.stateEditorOpen();
  }
  stateMainShellStyle() {
    return { marginRight: "0px" };
  }
  // 模块意图：实体抽屉复刻旧版的右侧面板体验，宽度由建模人员按字段和关系复杂度自由拉伸。
  // 关键流程：记录起点宽度后监听 document mousemove，按横向拖拽距离实时更新右侧抽屉宽度。
  // 边界细节：只保留最小可用宽度，不设置最大宽度；用户向左拉伸时不再被旧版迁移中的 860px 上限挡住。
  startDrawerResize(event) {
    event.preventDefault();
    event.stopPropagation();
    const startX = event.clientX;
    const startWidth = this.drawerWidth();
    const onMove = (moveEvent) => {
      const delta = startX - moveEvent.clientX;
      this.drawerWidth.set(Math.max(420, Math.round(startWidth + delta)));
    };
    const onUp = () => {
      document.removeEventListener("mousemove", onMove);
      document.removeEventListener("mouseup", onUp);
    };
    document.addEventListener("mousemove", onMove);
    document.addEventListener("mouseup", onUp);
  }
  startStateDrawerResize(event) {
    event.preventDefault();
    event.stopPropagation();
    const startX = event.clientX;
    const startWidth = this.stateDrawerWidth();
    const onMove = (moveEvent) => {
      const delta = startX - moveEvent.clientX;
      this.stateDrawerWidth.set(Math.max(520, startWidth + delta));
    };
    const onUp = () => {
      document.removeEventListener("mousemove", onMove);
      document.removeEventListener("mouseup", onUp);
    };
    document.addEventListener("mousemove", onMove);
    document.addEventListener("mouseup", onUp);
  }
  entityId(entity) {
    return String(entity?.uid || entity?.id || "").trim();
  }
  fieldId(field, index) {
    return String(field.uid || field.id || field.name || `field-${index}`);
  }
  selectEntity(entity, event) {
    const id = this.entityId(entity);
    if (event?.ctrlKey || event?.metaKey) {
      const next = new Set(this.selectedEntityIds());
      if (next.has(id))
        next.delete(id);
      else
        next.add(id);
      this.selectedEntityIds.set(next);
    } else if (event?.shiftKey) {
      return;
    } else {
      this.selectedEntityIds.set(/* @__PURE__ */ new Set([id]));
    }
    this.selectedEntityId.set(id);
    this.ensureActiveStateField(entity);
    this.syncRuntimeEntityId(id);
  }
  openEntityDrawer(entity, event) {
    event?.preventDefault();
    event?.stopPropagation();
    this.editRequested.emit();
    this.selectEntity(entity);
    this.editorOpen.set(true);
  }
  openSelectedEntityDrawer() {
    const entity = this.selectedEntity();
    if (!entity)
      return;
    this.openEntityDrawer(entity);
  }
  isSelected(entity) {
    return this.selectedEntityIds().has(this.entityId(entity));
  }
  startEntityDrag(entity, event) {
    if (event.button !== 0)
      return;
    if (event.ctrlKey || event.metaKey)
      return;
    if (event.shiftKey) {
      event.preventDefault();
      event.stopPropagation();
      this.startSelectionBox(event);
      return;
    }
    if (!this.canEditRelation())
      return;
    event.preventDefault();
    event.stopPropagation();
    const id = this.entityId(entity);
    if (!this.selectedEntityIds().has(id))
      this.selectEntity(entity, event);
    const draggedIds = this.selectedEntityIds().size ? this.selectedEntityIds() : /* @__PURE__ */ new Set([id]);
    const originalPositions = {};
    for (const item of this.entities()) {
      const itemId = this.entityId(item);
      if (!draggedIds.has(itemId))
        continue;
      const node = this.nodes().find((layout) => layout.id === itemId);
      originalPositions[itemId] = { x: item.pos?.x ?? node?.x ?? 0, y: item.pos?.y ?? node?.y ?? 0 };
    }
    this.dragState = { entityId: id, startClientX: event.clientX, startClientY: event.clientY, originalPositions, moved: false };
  }
  startFrameDrag(frame, event) {
    if (event.button !== 0)
      return;
    if (!this.canEditRelation())
      return;
    event.preventDefault();
    event.stopPropagation();
    this.selectFrameEntities(frame);
    const draggedIds = this.selectedEntityIds();
    if (!draggedIds.size)
      return;
    const originalPositions = {};
    for (const item of this.entities()) {
      const itemId = this.entityId(item);
      if (!draggedIds.has(itemId))
        continue;
      const node = this.nodes().find((layout) => layout.id === itemId);
      originalPositions[itemId] = { x: item.pos?.x ?? node?.x ?? 0, y: item.pos?.y ?? node?.y ?? 0 };
    }
    this.dragState = { entityId: [...draggedIds][0] || "", startClientX: event.clientX, startClientY: event.clientY, originalPositions, moved: false };
  }
  startSelectionBox(event) {
    if (!event.shiftKey || event.button !== 0)
      return;
    event.preventDefault();
    event.stopPropagation();
    const board = document.getElementById("ef-board-entity-diagram");
    const rect = (board || event.currentTarget).getBoundingClientRect();
    const startX = event.clientX - rect.left;
    const startY = event.clientY - rect.top;
    this.selectionBox.set({ startX, startY, currentX: startX, currentY: startY });
  }
  selectionBoxStyle() {
    const box = this.selectionBox();
    if (!box)
      return {};
    const left = Math.min(box.startX, box.currentX);
    const top = Math.min(box.startY, box.currentY);
    const width = Math.abs(box.currentX - box.startX);
    const height = Math.abs(box.currentY - box.startY);
    return { left: `${left}px`, top: `${top}px`, width: `${width}px`, height: `${height}px` };
  }
  _selectionBoxApplied = false;
  clearSelection() {
    if (this.selectionBox())
      return;
    if (this._selectionBoxApplied) {
      this._selectionBoxApplied = false;
      return;
    }
    this.selectedEntityIds.set(/* @__PURE__ */ new Set());
    this.selectedEntityId.set("");
    this.syncRuntimeEntityId("");
  }
  selectFrameEntities(frame, event) {
    event?.stopPropagation();
    const ids = frame.entityIds?.filter(Boolean) || [];
    if (ids.length === 0)
      return;
    this.selectedEntityIds.set(new Set(ids));
    this.selectedEntityId.set(ids[0]);
    this.syncRuntimeEntityId(ids[0]);
  }
  addEntity() {
    if (!this.canEditRelation())
      return;
    const entities = this.adapter.entities();
    const id = this.adapter.nextId("ENT", entities);
    const entity = {
      uid: id,
      name: "\u65B0\u5B9E\u4F53",
      note: "",
      fields: [],
      relations: [],
      state_transitions: []
    };
    entities.push(entity);
    this.selectedEntityId.set(id);
    this.stateFieldName.set("");
    this.syncRuntimeEntityId(id);
    this.editorOpen.set(true);
    this.changed();
  }
  resetLayout() {
    if (!this.canEditRelation() && !this.canEditState())
      return;
    const defaultPositions = this.computeDefaultEntityPositions();
    for (const entity of this.entities()) {
      const entityId2 = this.entityId(entity);
      entity.pos = defaultPositions.get(entityId2) || { x: this.entityPad + this.componentPadX + this.groupPadX, y: this.entityPad + this.componentHeaderHeight + this.componentPadY + this.groupHeaderHeight + this.groupPadY };
    }
    this.selectedEntityIds.set(/* @__PURE__ */ new Set());
    this.changed();
  }
  removeEntity(entity) {
    const entityId2 = this.entityId(entity);
    const entities = this.adapter.entities();
    const index = entities.findIndex((item) => this.entityId(item) === entityId2);
    if (index < 0)
      return;
    entities.splice(index, 1);
    const rootRelations = this.adapter.relations();
    for (let index2 = rootRelations.length - 1; index2 >= 0; index2 -= 1) {
      if (this.relationFrom(rootRelations[index2]) === entityId2 || this.relationTo(rootRelations[index2]) === entityId2)
        rootRelations.splice(index2, 1);
    }
    for (const item of entities) {
      item.relations = (item.relations || []).filter((relation) => this.relationTo(relation) !== entityId2 && this.relationFrom(relation) !== entityId2);
    }
    this.selectedEntityId.set(this.entityId(entities[0]) || "");
    this.syncRuntimeEntityId(this.selectedEntityId());
    this.changed();
  }
  duplicateEntity(entity) {
    const entities = this.adapter.entities();
    const id = this.adapter.nextId("ENT", entities);
    const copy = JSON.parse(JSON.stringify(entity));
    copy.uid = id;
    copy.id = id;
    copy.name = `${entity.name || "\u672A\u547D\u540D\u5B9E\u4F53"} \u526F\u672C`;
    copy.pos = entity.pos ? { x: Number(entity.pos.x || 0) + 40, y: Number(entity.pos.y || 0) + 40 } : void 0;
    copy.fields = Array.isArray(copy.fields) ? copy.fields.map((field, index) => __spreadProps(__spreadValues({}, field), { uid: this.adapter.nextId(`FLD${index}`, []) })) : [];
    copy.relations = [];
    entities.push(copy);
    this.selectedEntityId.set(id);
    this.selectedEntityIds.set(/* @__PURE__ */ new Set([id]));
    this.syncRuntimeEntityId(id);
    this.editorOpen.set(true);
    this.changed();
  }
  setEntityName(entity, value) {
    entity.name = value;
    this.changed();
  }
  setEntityNote(entity, value) {
    entity.note = value;
    this.changed();
  }
  setEntityConstruct(entity, constructId) {
    entity.businessConstructUid = constructId;
    entity.businessConstructId = constructId;
    entity.constructUid = constructId;
    entity.constructId = constructId;
    this.changed();
  }
  addField(entity) {
    entity.fields ||= [];
    entity.fields.push({ uid: this.adapter.nextId("FLD", entity.fields), name: "\u65B0\u5B57\u6BB5", type: "string", note: "" });
    this.changed();
  }
  setField(field, key, value) {
    const previousName = key === "name" ? String(field.name || "") : "";
    field[key] = value;
    if (key === "type") {
      field.dataType = value;
      field.fieldType = value;
    }
    if (key === "name" && previousName && this.stateFieldName() === previousName)
      this.stateFieldName.set(value);
    this.changed();
  }
  setFieldBoolean(field, key, value) {
    field[key] = value;
    if (key === "is_status" && value && !field.status_role)
      field.status_role = "primary";
    if (key === "is_status" && !value)
      field.status_role = "";
    this.changed();
  }
  setFieldStatusRole(field, value) {
    field.status_role = value === "primary" || value === "secondary" ? value : "";
    field.is_status = Boolean(field.status_role);
    this.changed();
  }
  removeField(entity, index) {
    entity.fields ||= [];
    const removedName = String(entity.fields[index]?.name || "");
    entity.fields.splice(index, 1);
    if (removedName && this.stateFieldName() === removedName)
      this.stateFieldName.set("");
    this.changed();
  }
  moveField(entity, index, direction) {
    entity.fields ||= [];
    const target = index + direction;
    if (target < 0 || target >= entity.fields.length)
      return;
    [entity.fields[index], entity.fields[target]] = [entity.fields[target], entity.fields[index]];
    this.changed();
  }
  addRelation(entity) {
    const candidates = this.entities().filter((item) => this.entityId(item) !== this.entityId(entity));
    const target = candidates[0];
    if (!target)
      return;
    const relations = this.adapter.relations();
    relations.push({
      uid: this.adapter.nextId("REL", relations),
      from: this.entityId(entity),
      to: this.entityId(target),
      type: "1:N",
      label: ""
    });
    this.changed();
  }
  setRelation(relation, key, value) {
    relation[key] = value;
    if (key === "from")
      relation.source = value;
    if (key === "to")
      relation.target = value;
    if (key === "from") {
      relation.sourceEntityUid = value;
      relation.fromEntityUid = value;
    }
    if (key === "to") {
      relation.targetEntityUid = value;
      relation.toEntityUid = value;
    }
    if (key === "type")
      relation.cardinality = value;
    this.changed();
  }
  relationFromValue(relation) {
    return this.relationFrom(relation);
  }
  relationToValue(relation) {
    return this.relationTo(relation);
  }
  relationTypeValue(relation) {
    const type = String(relation.type || relation.cardinality || "").trim();
    return type === "1:1" || type === "1:N" || type === "N:N" ? type : "1:1";
  }
  entityConstructValue(entity) {
    const raw = String(entity.businessConstructUid || entity.businessConstructId || entity.constructUid || entity.constructId || (entity.businessConstructUids || [])[0] || (entity.constructUids || [])[0] || "").trim();
    if (raw)
      return this.resolveConstructId(raw);
    const name = String(entity.businessConstructName || entity.constructName || "").trim();
    if (!name)
      return "";
    return this.constructId(this.constructs().find((construct) => construct.name === name) || {}) || "";
  }
  fieldTypeValue(field) {
    const raw = String(field.type || field.dataType || field.fieldType || field.valueType || "").trim();
    const lower = raw.toLowerCase();
    const aliases = {
      int: "number",
      integer: "number",
      long: "number",
      double: "decimal",
      float: "decimal",
      bool: "boolean",
      str: "string",
      varchar: "string",
      object: "text"
    };
    const normalized = aliases[lower] || lower;
    return this.fieldTypes.some((type) => type.value === normalized) ? normalized : "string";
  }
  fieldRuleHeight(field) {
    const text = String(field.note || field.state_values || "");
    const lineCount = Math.max(1, Math.ceil(text.length / 18), text.split(/\r?\n/).length);
    return Math.max(28, Math.min(120, 10 + lineCount * 20));
  }
  entityReferences(entity) {
    const entityId2 = this.entityId(entity);
    const refs = [];
    const doc = getAngularRuntimeState().doc;
    for (const process2 of doc.processes || []) {
      for (const task of this.processNodes(process2)) {
        const entityOps = Array.isArray(task?.entity_ops) ? task.entity_ops : [];
        if (entityOps.some((op) => String(op.entity_uid || op.entity_id || op.entityId || "") === entityId2)) {
          refs.push({ processName: process2.name || process2.title || process2.uid || process2.id || "\u672A\u547D\u540D\u6D41\u7A0B", taskName: task.name || task.title || task.uid || task.id || "\u672A\u547D\u540D\u8282\u70B9" });
        }
      }
    }
    return refs;
  }
  removeRelation(entity, index) {
    const relation = this.relationsForEntity(entity)[index];
    const rootIndex = this.adapter.relations().indexOf(relation);
    if (rootIndex >= 0)
      this.adapter.relations().splice(rootIndex, 1);
    else {
      entity.relations ||= [];
      const localIndex = entity.relations.indexOf(relation);
      if (localIndex >= 0)
        entity.relations.splice(localIndex, 1);
    }
    this.changed();
  }
  moveRelation(entity, index, direction) {
    const scoped = this.relationsForEntity(entity);
    const relation = scoped[index];
    const targetRelation = scoped[index + direction];
    if (!relation || !targetRelation)
      return;
    const root = this.adapter.relations();
    const currentIndex = root.indexOf(relation);
    const targetIndex = root.indexOf(targetRelation);
    if (currentIndex < 0 || targetIndex < 0)
      return;
    [root[currentIndex], root[targetIndex]] = [root[targetIndex], root[currentIndex]];
    this.changed();
  }
  addTransition(entity) {
    entity.state_transitions ||= [];
    const fieldName = this.selectedStateField()?.name || "";
    const values = this.collectStateValues(entity, fieldName);
    entity.state_transitions.push({
      uid: this.adapter.nextId("TRN", entity.state_transitions),
      from: values[0] || "\u5F00\u59CB",
      to: values[1] || values[0] || "\u7ED3\u675F",
      action: "\u6D41\u8F6C",
      field_name: fieldName
    });
    this.changed();
  }
  setTransition(transition, key, value) {
    transition[key] = value;
    this.changed();
  }
  setTransitionNote(transition, value) {
    transition.note = value;
    transition.action = value;
    this.changed();
  }
  resetAllStateTransitionRoutes(entity) {
    const fieldName = this.selectedStateField()?.name || "";
    let touched = false;
    for (const transition of this.transitionsForField(entity, fieldName)) {
      if (transition.labelPos || transition.route || transition.waypoints) {
        delete transition.labelPos;
        delete transition.route;
        delete transition.waypoints;
        touched = true;
      }
    }
    if (touched)
      this.changed();
  }
  removeTransition(entity, index) {
    entity.state_transitions ||= [];
    entity.state_transitions.splice(index, 1);
    this.changed();
  }
  moveTransition(entity, index, direction) {
    entity.state_transitions ||= [];
    const target = index + direction;
    if (target < 0 || target >= entity.state_transitions.length)
      return;
    [entity.state_transitions[index], entity.state_transitions[target]] = [entity.state_transitions[target], entity.state_transitions[index]];
    this.selectedTransitionIndex.set(target);
    this.changed();
  }
  setStateField(fieldName) {
    this.stateFieldName.set(fieldName);
    this.selectedTransitionIndex.set(null);
  }
  selectedStateTransitions(entity) {
    const fieldName = this.selectedStateField()?.name || "";
    return (entity.state_transitions || []).map((transition, index) => ({ transition, index })).filter(({ transition }) => !fieldName || !transition.field_name || transition.field_name === fieldName);
  }
  transitionStateOptions(transition) {
    const values = new Set(this.stateValues());
    if (transition.from)
      values.add(String(transition.from));
    if (transition.to)
      values.add(String(transition.to));
    return Array.from(values);
  }
  setStateNodeKind(entity, stateName, kind) {
    if (kind !== "initial" && kind !== "intermediate" && kind !== "terminal")
      return;
    const fieldName = this.selectedStateField()?.name || "";
    const nodes = this.syncEntityStateNodes(entity, this.collectStateValues(entity, fieldName), fieldName);
    const node = nodes.find((item) => String(item.name || "") === stateName);
    if (!node)
      return;
    node.kind = kind;
    this.changed();
  }
  nudgeStateZoom(delta) {
    this.stateZoom.set(Math.max(0.6, Math.min(1.8, Math.round((this.stateZoom() + delta) * 10) / 10)));
  }
  onStateDiagramWheel(event) {
    if (!event.ctrlKey)
      return;
    event.preventDefault();
    this.nudgeStateZoom(event.deltaY < 0 ? 0.1 : -0.1);
  }
  resetStateZoom() {
    this.stateZoom.set(1);
  }
  stateZoomLabel() {
    return `${Math.round(this.stateZoom() * 100)}%`;
  }
  constructLabel(entity) {
    const construct = this.constructForEntity(entity);
    return construct?.name || "\u672A\u5206\u7EC4\u6784\u4EF6";
  }
  constructOptionLabel(construct) {
    const legacyConstruct = construct;
    const componentKey = String(legacyConstruct.businessComponentUid || legacyConstruct.businessComponentId || legacyConstruct.businessComponent || "").trim();
    const component = this.adapter.components().find((item) => String(item.uid || item.id || item.name || "") === componentKey);
    const constructLabel = construct.name || this.constructId(construct);
    return component?.name ? `${component.name} / ${constructLabel}` : constructLabel;
  }
  stateRoleLabel(role) {
    if (role === "primary")
      return "\u4E3B\u72B6\u6001";
    if (role === "secondary")
      return "\u5B50\u72B6\u6001";
    return "\u72B6\u6001\u5B57\u6BB5";
  }
  stateNodeKindLabel(kind) {
    if (kind === "initial")
      return "\u521D\u59CB\u72B6\u6001";
    if (kind === "terminal")
      return "\u7ED3\u675F\u72B6\u6001";
    return "\u4E2D\u95F4\u72B6\u6001";
  }
  activeStateFieldLabel() {
    return this.selectedStateField()?.name || "\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5";
  }
  stateFieldOptionLabel(field) {
    const fieldName = String(field?.name || "").trim() || "\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5";
    const role = field?.status_role === "secondary" ? "secondary" : field?.status_role === "primary" || field?.is_status ? "primary" : "";
    if (role === "primary")
      return `\u4E3B\uFF1A${fieldName}`;
    if (role === "secondary")
      return `\u5B50\uFF1A${fieldName}`;
    return fieldName;
  }
  selectedStateFieldOptionLabel() {
    return this.stateFieldOptionLabel(this.selectedStateField());
  }
  constructId(construct) {
    return String(construct.uid || construct.id || "").trim();
  }
  constructs() {
    this.version();
    return this.adapter.constructs();
  }
  relationsForEntity(entity) {
    const eid = this.entityId(entity);
    return [
      ...this.adapter.relations().filter((relation) => this.relationFrom(relation) === eid || this.relationTo(relation) === eid),
      ...(entity.relations || []).map((relation) => __spreadProps(__spreadValues({}, relation), {
        from: this.relationFrom(relation) || eid,
        to: this.relationTo(relation) || eid
      }))
    ];
  }
  relationDebugEnabled() {
    try {
      const search = typeof window === "undefined" ? "" : window.location.search;
      return this.relationDebugOpen() || search.includes("entityRelationDebug=1") || window.localStorage?.getItem("blm.entityRelationDebug") === "1";
    } catch {
      return this.relationDebugOpen();
    }
  }
  toggleRelationDebug() {
    const next = !this.relationDebugEnabled();
    this.relationDebugOpen.set(next);
    try {
      if (next)
        window.localStorage?.setItem("blm.entityRelationDebug", "1");
      else
        window.localStorage?.removeItem("blm.entityRelationDebug");
    } catch {
    }
  }
  relationDebugRows(entity) {
    const currentEntityId = this.entityId(entity);
    const rows = [];
    const pushRow = (scope, relation, index, owner = "") => {
      const resolvedFrom = this.relationFrom(relation);
      const resolvedTo = this.relationTo(relation);
      rows.push({
        scope,
        index,
        owner,
        raw: JSON.stringify({
          uid: relation.uid,
          id: relation.id,
          from: relation.from,
          to: relation.to,
          source: relation.source,
          target: relation.target,
          sourceEntityUid: relation.sourceEntityUid,
          targetEntityUid: relation.targetEntityUid,
          fromEntityUid: relation.fromEntityUid,
          toEntityUid: relation.toEntityUid
        }),
        resolvedFrom,
        resolvedTo,
        fromSelectValue: resolvedFrom || currentEntityId,
        toSelectValue: resolvedTo,
        visible: resolvedFrom === currentEntityId || resolvedTo === currentEntityId
      });
    };
    this.adapter.relations().forEach((relation, index) => pushRow("doc.relations", relation, index));
    this.entities().forEach((item) => {
      const owner = `${item.name || "\u672A\u547D\u540D\u5B9E\u4F53"}(${this.entityId(item)})`;
      (item.relations || []).forEach((relation, index) => pushRow("entity.relations", relation, index, owner));
    });
    return rows;
  }
  nodeStyle(node) {
    return {
      left: `${node.x}px`,
      top: `${node.y}px`,
      width: `${node.width}px`,
      height: `${node.height}px`,
      "--entity-accent": node.color,
      "--entity-fill": node.fill,
      "--entity-stroke": node.stroke,
      "--entity-bg": node.background
    };
  }
  frameStyle(frame) {
    return {
      left: `${frame.left}px`,
      top: `${frame.top}px`,
      width: `${frame.width}px`,
      height: `${frame.height}px`,
      "--entity-frame-color": frame.color || "#94a3b8",
      "--entity-frame-fill": frame.fill || "rgba(148, 163, 184, 0.08)"
    };
  }
  relationLabelStyle(line) {
    return {
      left: `${line.labelX}px`,
      top: `${line.labelY}px`,
      color: line.color,
      opacity: String(line.opacity)
    };
  }
  stateNodeStyle(node) {
    return {
      left: `${node.x}px`,
      top: `${node.y}px`,
      width: `${node.width}px`,
      height: `${node.height}px`
    };
  }
  stateMarkerStyle(marker) {
    if (!marker)
      return {};
    return {
      left: `${Math.round(marker.x - marker.size / 2)}px`,
      top: `${Math.round(marker.y - marker.size / 2)}px`,
      width: `${marker.size}px`,
      height: `${marker.size}px`
    };
  }
  stateRouteHitboxStyle(line, segmentIndex) {
    const point = line.points[segmentIndex];
    const next = line.points[segmentIndex + 1];
    if (!point || !next)
      return {};
    const horizontal = Math.abs(point.y - next.y) <= Math.abs(point.x - next.x);
    const left = Math.min(point.x, next.x) - (horizontal ? 0 : 6);
    const top = Math.min(point.y, next.y) - (horizontal ? 6 : 0);
    const width = Math.max(horizontal ? Math.abs(next.x - point.x) : 12, 12);
    const height = Math.max(horizontal ? 12 : Math.abs(next.y - point.y), 12);
    return {
      left: `${Math.round(left)}px`,
      top: `${Math.round(top)}px`,
      width: `${Math.round(width)}px`,
      height: `${Math.round(height)}px`
    };
  }
  stateEndpointHandleStyle(line, endpoint) {
    const point = endpoint === "from" ? line.points[0] : line.points[line.points.length - 1];
    if (!point)
      return {};
    return {
      left: `${Math.round(point.x - 6)}px`,
      top: `${Math.round(point.y - 6)}px`
    };
  }
  stateBoardStyle(board = this.stateBoard()) {
    return { width: `${board.width}px`, height: `${board.height}px` };
  }
  stateZoomStyle(board = this.stateBoard()) {
    const zoom = this.stateZoom();
    return {
      width: `${Math.max(180, Math.round(board.width * zoom))}px`,
      height: `${Math.max(160, Math.round(board.height * zoom))}px`
    };
  }
  stateZoomTargetStyle(board = this.stateBoard()) {
    return {
      width: `${board.width}px`,
      height: `${board.height}px`,
      transform: `scale(${this.stateZoom()})`,
      transformOrigin: "0 0"
    };
  }
  selectTransition(index, event) {
    event.stopPropagation();
    this.selectedTransitionIndex.set(this.selectedTransitionIndex() === index ? null : index);
  }
  selectRelationLine(line, event) {
    event.stopPropagation();
    const selected = new Set([line.from.id, line.to.id].filter(Boolean));
    this.selectedEntityIds.set(selected);
    this.selectedEntityId.set(line.from.id || line.to.id || "");
    this.syncRuntimeEntityId(this.selectedEntityId());
    this.editorOpen.set(true);
  }
  startStateNodeDrag(node, event) {
    if (!this.canEditState())
      return;
    if (event.button !== 0)
      return;
    const entity = this.selectedEntity();
    if (!entity)
      return;
    event.preventDefault();
    event.stopPropagation();
    this.stateNodeDragState = {
      entityId: this.entityId(entity),
      stateName: node.name,
      fieldName: node.fieldName,
      dragKind: "node",
      startClientX: event.clientX,
      startClientY: event.clientY,
      startLeft: node.x,
      startTop: node.y,
      moved: false
    };
  }
  startStateMarkerDrag(node, event) {
    if (!this.canEditState())
      return;
    if (event.button !== 0 || !node.marker)
      return;
    const entity = this.selectedEntity();
    if (!entity)
      return;
    event.preventDefault();
    event.stopPropagation();
    this.stateNodeDragState = {
      entityId: this.entityId(entity),
      stateName: node.name,
      fieldName: node.fieldName,
      dragKind: "marker",
      startClientX: event.clientX,
      startClientY: event.clientY,
      startLeft: node.marker.x,
      startTop: node.marker.y,
      moved: false
    };
  }
  startStateLabelDrag(line, event) {
    if (!this.canEditState())
      return;
    if (event.button !== 0)
      return;
    const entity = this.selectedEntity();
    if (!entity)
      return;
    event.preventDefault();
    event.stopPropagation();
    this.stateLabelDragState = {
      entityId: this.entityId(entity),
      transitionIndex: line.index,
      startClientX: event.clientX,
      startClientY: event.clientY,
      startX: line.labelX,
      startY: line.labelY,
      moved: false
    };
  }
  startStateRouteDrag(line, segmentIndex, event) {
    if (!this.canEditState())
      return;
    if (event.button !== 0)
      return;
    const entity = this.selectedEntity();
    if (!entity)
      return;
    event.preventDefault();
    event.stopPropagation();
    this.selectTransition(line.index, event);
    this.stateRouteDragState = {
      entityId: this.entityId(entity),
      transitionIndex: line.index,
      segmentIndex,
      points: line.points.map((point) => ({ x: point.x, y: point.y })),
      startClientX: event.clientX,
      startClientY: event.clientY,
      moved: false
    };
  }
  boardSize() {
    const zoom = this.relationZoom();
    return {
      width: `${this.boardWidth()}px`,
      height: `${this.boardHeight()}px`,
      transform: `scale(${zoom})`,
      transformOrigin: "0 0"
    };
  }
  relationCanvasSize() {
    const zoom = this.relationZoom();
    return {
      width: `${Math.max(0, Math.round(this.boardWidth() * zoom))}px`,
      height: `${Math.max(0, Math.round(this.boardHeight() * zoom))}px`
    };
  }
  onRelationWheel(event) {
    if (!event.ctrlKey || this.view() !== "relation")
      return;
    event.preventDefault();
    const direction = event.deltaY < 0 ? 1 : -1;
    this.relationZoom.update((zoom) => Math.max(0.5, Math.min(2, Math.round((zoom + direction * 0.1) * 10) / 10)));
  }
  boardWidth() {
    const nodes = this.nodes();
    const groupFrames = this.groupFrames();
    const componentFrames = this.componentFrames();
    return Math.max(0, ...nodes.map((node) => node.x + node.width + 80), ...groupFrames.map((frame) => frame.left + frame.width + this.entityPad), ...componentFrames.map((frame) => frame.left + frame.width + this.entityPad * 2));
  }
  boardHeight() {
    const nodes = this.nodes();
    const groupFrames = this.groupFrames();
    const componentFrames = this.componentFrames();
    return Math.max(0, ...nodes.map((node) => node.y + node.height + 100), ...groupFrames.map((frame) => frame.top + frame.height + this.entityPad), ...componentFrames.map((frame) => frame.top + frame.height + this.componentHeaderHeight + this.componentPadY + this.componentPadX));
  }
  onDocumentMouseMove(event) {
    if (this.stateNodeDragState) {
      const drag = this.stateNodeDragState;
      const dx2 = event.clientX - drag.startClientX;
      const dy2 = event.clientY - drag.startClientY;
      if (Math.abs(dx2) > 2 || Math.abs(dy2) > 2)
        drag.moved = true;
      if (!drag.moved)
        return;
      const entity = this.entities().find((item) => this.entityId(item) === drag.entityId);
      if (!entity)
        return;
      const nextPoint = {
        x: Math.max(4, Math.round(drag.startLeft + dx2)),
        y: Math.max(4, Math.round(drag.startTop + dy2))
      };
      if (drag.dragKind === "marker")
        this.setStateMarkerPosition(entity, drag.stateName, nextPoint, false, drag.fieldName);
      else
        this.setStateNodePosition(entity, drag.stateName, nextPoint, false, drag.fieldName);
      this.refreshDuringStateDrag();
      return;
    }
    if (this.stateLabelDragState) {
      const drag = this.stateLabelDragState;
      const dx2 = event.clientX - drag.startClientX;
      const dy2 = event.clientY - drag.startClientY;
      if (Math.abs(dx2) > 2 || Math.abs(dy2) > 2)
        drag.moved = true;
      if (!drag.moved)
        return;
      const entity = this.entities().find((item) => this.entityId(item) === drag.entityId);
      if (!entity)
        return;
      this.setStateLabelPosition(entity, drag.transitionIndex, {
        x: Math.max(4, Math.round(drag.startX + dx2)),
        y: Math.max(4, Math.round(drag.startY + dy2))
      }, false);
      this.refreshDuringStateDrag();
      return;
    }
    if (this.stateRouteDragState) {
      const drag = this.stateRouteDragState;
      const dx2 = event.clientX - drag.startClientX;
      const dy2 = event.clientY - drag.startClientY;
      if (Math.abs(dx2) > 2 || Math.abs(dy2) > 2)
        drag.moved = true;
      if (!drag.moved)
        return;
      const entity = this.entities().find((item) => this.entityId(item) === drag.entityId);
      if (!entity)
        return;
      this.setStateTransitionRoute(entity, drag.transitionIndex, this.shiftStateRouteSegment(drag.points, drag.segmentIndex, dx2, dy2), false);
      this.refreshDuringStateDrag();
      return;
    }
    if (this.selectionBox()) {
      const board = document.querySelector(".entity-board");
      if (!board)
        return;
      const rect = board.getBoundingClientRect();
      const current = this.selectionBox();
      if (!current)
        return;
      this.selectionBox.set(__spreadProps(__spreadValues({}, current), { currentX: event.clientX - rect.left, currentY: event.clientY - rect.top }));
      return;
    }
    if (!this.dragState)
      return;
    const dx = event.clientX - this.dragState.startClientX;
    const dy = event.clientY - this.dragState.startClientY;
    if (Math.abs(dx) > 3 || Math.abs(dy) > 3)
      this.dragState.moved = true;
    if (!this.dragState.moved)
      return;
    const originals = Object.values(this.dragState.originalPositions);
    const minOrigX = Math.min(...originals.map((point) => point.x));
    const minOrigY = Math.min(...originals.map((point) => point.y));
    const safeDx = Math.max(dx, 24 - minOrigX);
    const safeDy = Math.max(dy, 24 - minOrigY);
    for (const [id, original] of Object.entries(this.dragState.originalPositions)) {
      const entity = this.entities().find((item) => this.entityId(item) === id);
      if (!entity)
        continue;
      entity.pos = {
        x: Math.max(24, Math.round(original.x + safeDx)),
        y: Math.max(24, Math.round(original.y + safeDy))
      };
    }
    this.version.update((value) => value + 1);
  }
  onDocumentMouseUp() {
    if (this.stateNodeDragState) {
      const drag2 = this.stateNodeDragState;
      this.stateNodeDragState = null;
      if (drag2.moved)
        this.adapter.markChanged();
      return;
    }
    if (this.stateLabelDragState) {
      const drag2 = this.stateLabelDragState;
      this.stateLabelDragState = null;
      if (drag2.moved)
        this.adapter.markChanged();
      return;
    }
    if (this.stateRouteDragState) {
      const drag2 = this.stateRouteDragState;
      this.stateRouteDragState = null;
      if (drag2.moved)
        this.adapter.markChanged();
      return;
    }
    if (this.selectionBox()) {
      this.applySelectionBox();
      this.selectionBox.set(null);
      return;
    }
    if (!this.dragState)
      return;
    const drag = this.dragState;
    this.dragState = null;
    if (drag.moved)
      this.adapter.markChanged();
  }
  onDocumentKeydown(event) {
    const target = event.target;
    if (!this.isRelationShortcutActive())
      return;
    if (target && (["INPUT", "TEXTAREA", "SELECT"].includes(target.tagName) || target.isContentEditable))
      return;
    if ((event.ctrlKey || event.metaKey) && !event.altKey && event.key.toLowerCase() === "a") {
      event.preventDefault();
      event.stopPropagation();
      this.selectedEntityIds.set(new Set(this.entities().map((entity) => this.entityId(entity))));
    }
  }
  isRelationShortcutActive() {
    return this.view() === "relation" && Boolean(document.getElementById("ef-board-entity-diagram"));
  }
  changed() {
    this.adapter.markChanged();
    this.version.update((value) => value + 1);
  }
  refreshDuringStateDrag() {
    this.version.update((value) => value + 1);
    this.changeDetector.detectChanges();
  }
  syncRuntimeEntityId(entityId2) {
    getAngularRuntimeState().ui["entityId"] = entityId2;
  }
  layoutEntities() {
    const nodes = [];
    const groups = this.sortEntityGroups().map((group) => __spreadProps(__spreadValues({}, group), { layout: this.measureGroup(group.entities) }));
    const gridCols = this.groupGridColumns(groups.length);
    const cellWidth = Math.max(...groups.map((group) => group.layout.width), this.nodeWidth + this.groupPadX * 2);
    const cellHeight = Math.max(...groups.map((group) => group.layout.height), this.nodeHeight + this.groupHeaderHeight + this.groupPadY * 2);
    groups.forEach((group, groupIndex) => {
      const palette = this.palette[groupIndex % this.palette.length];
      const rawRow = Math.floor(groupIndex / gridCols);
      const rawCol = groupIndex % gridCols;
      const itemsInRow = Math.min(gridCols, groups.length - rawRow * gridCols);
      const layoutCol = rawRow % 2 === 0 ? rawCol : itemsInRow - 1 - rawCol;
      const baseX = this.entityPad + this.componentPadX + layoutCol * (cellWidth + this.groupGapX);
      const baseY = this.entityPad + this.componentHeaderHeight + this.componentPadY + rawRow * (cellHeight + this.groupGapY);
      const colOffsets = group.layout.colWidths.reduce((acc, width, index) => {
        acc.push(index === 0 ? 0 : acc[index - 1] + group.layout.colWidths[index - 1] + this.entityGapX);
        return acc;
      }, []);
      group.entities.forEach((entity, entityIndex) => {
        const size = this.entityNodeSize(entity);
        const col = entityIndex % group.layout.colCount;
        const row = Math.floor(entityIndex / group.layout.colCount);
        nodes.push({
          entity,
          id: this.entityId(entity),
          x: entity.pos?.x ?? baseX + this.groupPadX + (colOffsets[col] || 0),
          y: entity.pos?.y ?? baseY + this.groupHeaderHeight + this.groupPadY + row * (this.nodeHeight + this.entityGapY),
          width: size.width,
          height: size.height,
          color: palette.color,
          fill: palette.fill,
          stroke: palette.stroke,
          background: palette.fill,
          componentKey: group.componentKey,
          componentName: group.componentName,
          constructKey: group.key,
          constructName: group.label
        });
      });
    });
    return nodes;
  }
  applySelectionBox() {
    const box = this.selectionBox();
    if (!box)
      return;
    this._selectionBoxApplied = true;
    const left = Math.min(box.startX, box.currentX);
    const right = Math.max(box.startX, box.currentX);
    const top = Math.min(box.startY, box.currentY);
    const bottom = Math.max(box.startY, box.currentY);
    const selected = this.nodes().filter((node) => node.x < right && node.x + node.width > left && node.y < bottom && node.y + node.height > top).map((node) => node.id);
    this.selectedEntityIds.set(new Set(selected));
    this.selectedEntityId.set(selected[0] || "");
  }
  entityNodeSize(entity) {
    const name = String(entity?.name || "\u672A\u547D\u540D\u5B9E\u4F53");
    const width = Array.from(name).reduce((sum, char) => {
      if (/[\u2e80-\u9fff\uff00-\uffef]/.test(char))
        return sum + 14;
      if (/[A-Z]/.test(char))
        return sum + 8;
      if (/\s/.test(char))
        return sum + 4;
      return sum + 7;
    }, 0);
    return { width: Math.max(this.nodeWidth, Math.ceil(width + 32)), height: this.nodeHeight };
  }
  entityMeta(entity) {
    const construct = this.constructForEntity(entity);
    const componentKey = String(construct?.businessComponentUid || construct?.businessComponentId || "").trim();
    const component = this.adapter.components().find((item) => String(item.uid || item.id || "") === componentKey);
    return {
      constructKey: this.constructId(construct || {}) || "__ungrouped_construct__",
      constructLabel: construct?.name || "\u672A\u5F52\u5C5E\u6784\u4EF6",
      componentKey: componentKey || "__ungrouped_component__",
      componentLabel: component?.name || "\u672A\u5F52\u5C5E\u7EC4\u4EF6"
    };
  }
  sortEntityGroups() {
    const entities = this.entities();
    const relations = this.allRelations();
    const degree = /* @__PURE__ */ new Map();
    const groupScore = /* @__PURE__ */ new Map();
    const groupLinks = /* @__PURE__ */ new Map();
    for (const entity of entities) {
      const id = this.entityId(entity);
      const meta = this.entityMeta(entity);
      degree.set(id, 0);
      groupScore.set(meta.constructKey, groupScore.get(meta.constructKey) || 0);
      if (!groupLinks.has(meta.constructKey))
        groupLinks.set(meta.constructKey, /* @__PURE__ */ new Map());
    }
    for (const relation of relations) {
      const from2 = this.relationFrom(relation);
      const to = this.relationTo(relation);
      degree.set(from2, (degree.get(from2) || 0) + 1);
      degree.set(to, (degree.get(to) || 0) + 1);
      const fromEntity = entities.find((entity) => this.entityId(entity) === from2);
      const toEntity = entities.find((entity) => this.entityId(entity) === to);
      const fromGroup = this.entityMeta(fromEntity || {}).constructKey;
      const toGroup = this.entityMeta(toEntity || {}).constructKey;
      groupScore.set(fromGroup, (groupScore.get(fromGroup) || 0) + 1);
      groupScore.set(toGroup, (groupScore.get(toGroup) || 0) + 1);
      if (fromGroup !== toGroup) {
        if (!groupLinks.has(fromGroup))
          groupLinks.set(fromGroup, /* @__PURE__ */ new Map());
        if (!groupLinks.has(toGroup))
          groupLinks.set(toGroup, /* @__PURE__ */ new Map());
        groupLinks.get(fromGroup)?.set(toGroup, (groupLinks.get(fromGroup)?.get(toGroup) || 0) + 1);
        groupLinks.get(toGroup)?.set(fromGroup, (groupLinks.get(toGroup)?.get(fromGroup) || 0) + 1);
      }
    }
    const keys = Array.from(new Set(entities.map((entity) => this.entityMeta(entity).constructKey)));
    const sortedKeys = [];
    const used = /* @__PURE__ */ new Set();
    let current = keys.sort((a, b) => (groupScore.get(b) || 0) - (groupScore.get(a) || 0))[0];
    while (current) {
      sortedKeys.push(current);
      used.add(current);
      let nextKey = "";
      let nextScore = -1;
      for (const key of keys) {
        if (used.has(key))
          continue;
        const score = (groupLinks.get(current)?.get(key) || 0) * 100 + (groupScore.get(key) || 0);
        if (score > nextScore) {
          nextScore = score;
          nextKey = key;
        }
      }
      current = nextKey;
    }
    return sortedKeys.map((key) => {
      const groupEntities = entities.filter((entity) => this.entityMeta(entity).constructKey === key).sort((a, b) => (degree.get(this.entityId(b)) || 0) - (degree.get(this.entityId(a)) || 0) || this.entityId(a).localeCompare(this.entityId(b)));
      const meta = this.entityMeta(groupEntities[0] || {});
      return { key, label: meta.constructLabel, componentKey: meta.componentKey, componentName: meta.componentLabel, entities: groupEntities };
    });
  }
  measureGroup(entities) {
    const colCount = entities.length >= 12 ? 3 : entities.length >= 7 ? 2 : 1;
    const rowCount = Math.max(1, Math.ceil(entities.length / colCount));
    const colWidths = Array.from({ length: colCount }, () => this.nodeWidth);
    entities.forEach((entity, index) => {
      const col = index % colCount;
      colWidths[col] = Math.max(colWidths[col], this.entityNodeSize(entity).width);
    });
    const contentWidth = colWidths.reduce((sum, width) => sum + width, 0) + Math.max(0, colCount - 1) * this.entityGapX;
    const contentHeight = rowCount * this.nodeHeight + Math.max(0, rowCount - 1) * this.entityGapY;
    return {
      colCount,
      colWidths,
      width: contentWidth + this.groupPadX * 2,
      height: contentHeight + this.groupHeaderHeight + this.groupPadY * 2
    };
  }
  groupGridColumns(groupCount) {
    if (groupCount <= 1)
      return 1;
    if (groupCount <= 4)
      return 2;
    if (groupCount <= 9)
      return 3;
    return 4;
  }
  computeDefaultEntityPositions() {
    const positions = /* @__PURE__ */ new Map();
    const groups = this.sortEntityGroups().map((group) => __spreadProps(__spreadValues({}, group), { layout: this.measureGroup(group.entities) }));
    if (!groups.length)
      return positions;
    const gridCols = this.groupGridColumns(groups.length);
    const cellWidth = Math.max(...groups.map((group) => group.layout.width), this.nodeWidth + this.groupPadX * 2);
    const cellHeight = Math.max(...groups.map((group) => group.layout.height), this.nodeHeight + this.groupHeaderHeight + this.groupPadY * 2);
    groups.forEach((group, groupIndex) => {
      const colOffsets = group.layout.colWidths.reduce((acc, width, index) => {
        acc.push(index === 0 ? 0 : acc[index - 1] + group.layout.colWidths[index - 1] + this.entityGapX);
        return acc;
      }, []);
      const rawRow = Math.floor(groupIndex / gridCols);
      const rawCol = groupIndex % gridCols;
      const itemsInRow = Math.min(gridCols, groups.length - rawRow * gridCols);
      const layoutCol = rawRow % 2 === 0 ? rawCol : itemsInRow - 1 - rawCol;
      const baseX = this.entityPad + this.componentPadX + layoutCol * (cellWidth + this.groupGapX);
      const baseY = this.entityPad + this.componentHeaderHeight + this.componentPadY + rawRow * (cellHeight + this.groupGapY);
      group.entities.forEach((entity, entityIndex) => {
        const col = entityIndex % group.layout.colCount;
        const row = Math.floor(entityIndex / group.layout.colCount);
        positions.set(this.entityId(entity), {
          x: baseX + this.groupPadX + (colOffsets[col] || 0),
          y: baseY + this.groupHeaderHeight + this.groupPadY + row * (this.nodeHeight + this.entityGapY)
        });
      });
    });
    return positions;
  }
  computeGroupFrames(nodes) {
    const frames = /* @__PURE__ */ new Map();
    for (const node of nodes) {
      const left = node.x - this.groupPadX;
      const top = node.y - this.groupHeaderHeight - this.groupPadY;
      const right = node.x + node.width + this.groupPadX;
      const bottom = node.y + node.height + this.groupPadY;
      let current = frames.get(node.constructKey);
      if (!current) {
        current = {
          frame: {
            key: node.constructKey,
            label: node.constructName,
            componentKey: node.componentKey,
            left,
            top,
            width: right - left,
            height: bottom - top,
            color: node.stroke,
            fill: `${node.fill}66`
          },
          entityIds: /* @__PURE__ */ new Set([node.id])
        };
        frames.set(node.constructKey, current);
      } else {
        current.entityIds.add(node.id);
        const cf = current.frame;
        const nextLeft = Math.min(cf.left, left);
        const nextTop = Math.min(cf.top, top);
        const nextRight = Math.max(cf.left + cf.width, right);
        const nextBottom = Math.max(cf.top + cf.height, bottom);
        cf.left = nextLeft;
        cf.top = nextTop;
        cf.width = nextRight - nextLeft;
        cf.height = nextBottom - nextTop;
      }
    }
    return Array.from(frames.values()).map((item) => __spreadProps(__spreadValues({}, item.frame), { entityIds: Array.from(item.entityIds) }));
  }
  computeComponentFrames(groupFrames) {
    const frames = /* @__PURE__ */ new Map();
    for (const frame of groupFrames) {
      const key = frame.componentKey || "__ungrouped_component__";
      const left = frame.left - this.componentPadX;
      const top = frame.top - this.componentHeaderHeight - this.componentPadY;
      const right = frame.left + frame.width + this.componentPadX;
      const bottom = frame.top + frame.height + this.componentPadY;
      const current = frames.get(key);
      if (!current) {
        frames.set(key, {
          key,
          label: this.componentLabel(key),
          left,
          top,
          width: right - left,
          height: bottom - top,
          entityIds: [...frame.entityIds || []]
        });
      } else {
        const nextLeft = Math.min(current.left, left);
        const nextTop = Math.min(current.top, top);
        const nextRight = Math.max(current.left + current.width, right);
        const nextBottom = Math.max(current.top + current.height, bottom);
        current.left = nextLeft;
        current.top = nextTop;
        current.width = nextRight - nextLeft;
        current.height = nextBottom - nextTop;
        if (frame.entityIds && current.entityIds) {
          current.entityIds = [.../* @__PURE__ */ new Set([...current.entityIds, ...frame.entityIds])];
        }
      }
    }
    return Array.from(frames.values());
  }
  collectRelationLines() {
    const nodeMap = new Map(this.nodes().map((node) => [node.id, node]));
    const lines = [];
    const selectedId = this.selectedEntityId();
    const channelCount = {};
    const channelIndex = (key) => {
      const index = channelCount[key] || 0;
      channelCount[key] = index + 1;
      return index;
    };
    this.allRelations().forEach((relation, index) => {
      const from2 = nodeMap.get(this.relationFrom(relation));
      const to = nodeMap.get(this.relationTo(relation));
      if (!from2 || !to)
        return;
      const color = this.relationStrokeColors[index % this.relationStrokeColors.length];
      const crossGroup = from2.constructKey !== to.constructKey;
      const focus = Boolean(selectedId && (from2.id === selectedId || to.id === selectedId));
      const route = this.routeRelation(from2, to, channelIndex);
      lines.push({
        from: from2,
        to,
        label: `${relation.type || ""}${relation.type && relation.label ? " " : ""}${relation.label || ""}` || "\u5173\u8054",
        path: route.path,
        labelX: route.labelX,
        labelY: route.labelY,
        color,
        opacity: selectedId ? focus ? 0.96 : 0.16 : crossGroup ? 0.3 : 0.82,
        width: selectedId ? focus ? 2.4 : 1.1 : crossGroup ? 1.1 : 1.7,
        dashed: crossGroup || relation.type === "N:N",
        focus,
        muted: Boolean(selectedId && !focus)
      });
    });
    return lines;
  }
  allRelations() {
    const relations = [...this.adapter.relations()];
    for (const entity of this.entities()) {
      for (const relation of entity.relations || []) {
        relations.push(__spreadProps(__spreadValues({}, relation), { from: this.relationFrom(relation) || this.entityId(entity) }));
      }
    }
    return relations;
  }
  routeRelation(from2, to, channelIndex) {
    const a = this.nodeRect(from2);
    const b = this.nodeRect(to);
    if (from2.id === to.id) {
      const index2 = channelIndex(`self-${from2.id}`);
      const loopW = 28 + index2 * 22;
      const loopH = Math.max(12, a.h / 3);
      const exitY = a.cy - loopH;
      const enterY = a.cy + loopH;
      return {
        path: `M ${a.r} ${exitY} L ${a.r + loopW} ${exitY} L ${a.r + loopW} ${enterY} L ${a.r} ${enterY}`,
        labelX: a.r + loopW + 4,
        labelY: a.cy + 4
      };
    }
    const dx = b.cx - a.cx;
    const dy = b.cy - a.cy;
    const absDx = Math.abs(dx);
    const absDy = Math.abs(dy);
    if (absDy < this.nodeHeight + 4) {
      const key2 = `h-${Math.round((a.cy + b.cy) / 2 / 8) * 8}`;
      const index2 = channelIndex(key2);
      const yOffset = (index2 % 2 === 0 ? 1 : -1) * Math.ceil(index2 / 2) * 8;
      const yCenter = (a.cy + b.cy) / 2 + yOffset;
      const path = dx >= 0 ? `M ${a.r} ${a.cy} L ${a.r + 4} ${a.cy} L ${a.r + 4} ${yCenter} L ${b.l - 4} ${yCenter} L ${b.l - 4} ${b.cy} L ${b.l} ${b.cy}` : `M ${a.l} ${a.cy} L ${a.l - 4} ${a.cy} L ${a.l - 4} ${yCenter} L ${b.r + 4} ${yCenter} L ${b.r + 4} ${b.cy} L ${b.r} ${b.cy}`;
      return { path, labelX: (a.cx + b.cx) / 2, labelY: yCenter - 8 };
    }
    if (absDy >= absDx) {
      const goDown = dy > 0;
      const sy = goDown ? a.b : a.t;
      const ey = goDown ? b.t : b.b;
      const key2 = `v-${Math.round(Math.min(sy, ey))}-${Math.round(Math.max(sy, ey))}`;
      const index2 = channelIndex(key2);
      const sign2 = index2 % 2 === 0 ? 1 : -1;
      const yMid = (sy + ey) / 2 + sign2 * Math.ceil(index2 / 2) * 10;
      return {
        path: `M ${a.cx} ${sy} L ${a.cx} ${yMid} L ${b.cx} ${yMid} L ${b.cx} ${ey}`,
        labelX: (a.cx + b.cx) / 2,
        labelY: yMid - 6
      };
    }
    const goRight = dx > 0;
    const sx = goRight ? a.r : a.l;
    const ex = goRight ? b.l : b.r;
    const key = `r-${Math.round(Math.min(sx, ex))}-${Math.round(Math.max(sx, ex))}`;
    const index = channelIndex(key);
    const sign = index % 2 === 0 ? 1 : -1;
    const xMid = (sx + ex) / 2 + sign * Math.ceil(index / 2) * 10;
    return {
      path: `M ${sx} ${a.cy} L ${xMid} ${a.cy} L ${xMid} ${b.cy} L ${ex} ${b.cy}`,
      labelX: xMid + 8,
      labelY: (a.cy + b.cy) / 2 - 4
    };
  }
  nodeRect(node) {
    return {
      l: node.x,
      t: node.y,
      r: node.x + node.width,
      b: node.y + node.height,
      cx: node.x + node.width / 2,
      cy: node.y + node.height / 2,
      w: node.width,
      h: node.height
    };
  }
  /** 从 relation 上提取来源实体 ID，并通过 entityId 归一化为当前 uid/id 体系 */
  relationFrom(relation) {
    return resolveRelationEndpoint(this.entities(), relation, "from");
  }
  /** 从 relation 上提取目标实体 ID，并通过 entityId 归一化为当前 uid/id 体系 */
  relationTo(relation) {
    return resolveRelationEndpoint(this.entities(), relation, "to");
  }
  constructForEntity(entity) {
    const constructId = this.entityConstructValue(entity);
    return this.constructs().find((construct) => this.constructId(construct) === constructId);
  }
  resolveConstructId(raw) {
    const value = String(raw || "").trim();
    if (!value)
      return "";
    const construct = this.constructs().find((item) => this.constructId(item) === value || item.name === value);
    return this.constructId(construct || {}) || value;
  }
  componentLabel(componentKey) {
    return this.adapter.components().find((component) => String(component.uid || component.id || "") === componentKey)?.name || "\u672A\u5F52\u5C5E\u7EC4\u4EF6";
  }
  layoutStateBoard(entity, fieldName = this.selectedStateField()?.name || "") {
    if (!entity)
      return { nodes: [], transitions: [], markerLinks: [], width: 720, height: 360 };
    const values = this.collectStateValues(entity, fieldName);
    const transitionValues = /* @__PURE__ */ new Set();
    for (const transition of this.transitionsForField(entity, fieldName)) {
      if (transition.from)
        transitionValues.add(transition.from);
      if (transition.to)
        transitionValues.add(transition.to);
    }
    const savedNodes = this.syncEntityStateNodes(entity, values, fieldName);
    const nodes = values.map((name, index) => {
      const scopedTransitions2 = this.transitionsForField(entity, fieldName);
      const incoming = scopedTransitions2.filter((transition) => transition.to === name).length;
      const outgoing = scopedTransitions2.filter((transition) => transition.from === name).length;
      const saved = savedNodes.find((item) => String(item.name || "") === name);
      const inferredKind = incoming === 0 && outgoing > 0 ? "initial" : outgoing === 0 && incoming > 0 ? "terminal" : "intermediate";
      const savedKind = saved?.kind === "initial" || saved?.kind === "terminal" || saved?.kind === "intermediate" ? saved.kind : inferredKind;
      return { name, kind: savedKind, originalIndex: index, savedPos: saved?.pos, savedMarkerPos: saved?.markerPos };
    });
    if (!nodes.length && transitionValues.size) {
      Array.from(transitionValues).forEach((name, index) => nodes.push({ name, kind: "intermediate", originalIndex: index, savedPos: void 0, savedMarkerPos: void 0 }));
    }
    const initial = nodes.filter((node) => node.kind === "initial");
    const terminal = nodes.filter((node) => node.kind === "terminal");
    const middle = nodes.filter((node) => node.kind === "intermediate");
    const orderedRows = [
      ...initial.length ? [initial] : [],
      ...middle.map((node) => [node]),
      ...terminal.length ? [terminal] : []
    ];
    const layouts = [];
    const scopedTransitions = this.transitionsForField(entity, fieldName);
    const rowByState = /* @__PURE__ */ new Map();
    orderedRows.forEach((row, rowIndex) => row.forEach((node) => rowByState.set(node.name, rowIndex)));
    const padX = 48;
    const padY = 28;
    const gapX = 68;
    const gapY = 68;
    const nodeH = 36;
    const markerGap = 28;
    const startDotR = 8;
    const endOuterR = 10;
    const topMarkerSpace = initial.length ? markerGap + startDotR * 2 : 0;
    const bottomMarkerSpace = terminal.length ? markerGap + endOuterR * 2 : 0;
    const rowWidths = orderedRows.map((row) => {
      const widths = row.map((node) => this.stateNodeDisplayWidth(node.name));
      return widths.reduce((sum, width2) => sum + width2, 0) + Math.max(0, row.length - 1) * gapX;
    });
    const specialCount = scopedTransitions.filter((transition) => {
      const fromRow = rowByState.get(String(transition.from || ""));
      const toRow = rowByState.get(String(transition.to || ""));
      if (!Number.isFinite(fromRow) || !Number.isFinite(toRow))
        return false;
      const isSelfLoop = transition.from === transition.to;
      const isBackward = !isSelfLoop && toRow < fromRow;
      const isForwardDetour = !isSelfLoop && !isBackward && toRow - fromRow > 1;
      return isSelfLoop || isBackward || isForwardDetour;
    }).length;
    const sideChannelEstimate = specialCount ? Math.max(1, Math.ceil(specialCount / 2)) : 0;
    const sideReserve = sideChannelEstimate ? 92 + Math.max(0, sideChannelEstimate - 1) * 18 : 0;
    const layoutBoardW = Math.max(360, padX * 2 + Math.max(...rowWidths, 0));
    orderedRows.forEach((row, rowIndex) => {
      const rowWidths2 = row.map((node) => this.stateNodeDisplayWidth(node.name));
      const rowWidth = rowWidths2.reduce((sum, width2) => sum + width2, 0) + Math.max(0, row.length - 1) * gapX;
      const startX = sideReserve + Math.max(padX, (layoutBoardW - rowWidth) / 2);
      row.forEach((node, colIndex) => {
        const autoX = Math.round(startX + rowWidths2.slice(0, colIndex).reduce((sum, width3) => sum + width3, 0) + colIndex * gapX);
        const autoY = padY + topMarkerSpace + rowIndex * (nodeH + gapY);
        const x = Number.isFinite(Number(node.savedPos?.x)) ? Math.max(4, Math.round(Number(node.savedPos?.x))) : autoX;
        const y = Number.isFinite(Number(node.savedPos?.y)) ? Math.max(4, Math.round(Number(node.savedPos?.y))) : autoY;
        const width2 = rowWidths2[colIndex];
        const savedMarker = this.normalizePoint(node.savedMarkerPos);
        const marker = node.kind === "initial" ? { kind: "initial", x: savedMarker?.x ?? x + width2 / 2, y: savedMarker?.y ?? y - markerGap, size: startDotR * 2 } : node.kind === "terminal" ? { kind: "terminal", x: savedMarker?.x ?? x + width2 / 2, y: savedMarker?.y ?? y + nodeH + markerGap, size: endOuterR * 2 } : void 0;
        layouts.push({ name: node.name, fieldName, kind: node.kind, row: rowIndex, x, y, width: width2, height: nodeH, marker });
      });
    });
    const nodeMap = new Map(layouts.map((node) => [node.name, node]));
    const routePlans = this.planLegacyStateTransitionRoutes(scopedTransitions, nodeMap, layouts);
    const transitions = scopedTransitions.map((transition) => {
      const index = (entity.state_transitions || []).indexOf(transition);
      const from2 = nodeMap.get(String(transition.from || ""));
      const to = nodeMap.get(String(transition.to || ""));
      if (!from2 || !to)
        return null;
      const route = this.routeStateTransition(from2, to, index, transition, routePlans.get(transition));
      const metrics = this.stateLinkLabelMetrics(transition.action || transition.label || "\u6D41\u8F6C");
      const placement = this.stateLinkLabelPlacement(route.points, (from2.x + from2.width / 2 + to.x + to.width / 2) / 2, (from2.y + from2.height / 2 + to.y + to.height / 2) / 2, metrics);
      const labelPos = this.normalizePoint(transition.labelPos);
      return {
        index,
        transition,
        from: from2,
        to,
        path: route.path,
        label: transition.action || transition.label || "\u6D41\u8F6C",
        labelX: labelPos?.x ?? placement.x,
        labelY: labelPos?.y ?? placement.y,
        labelWidth: metrics.width,
        labelHeight: metrics.height,
        labelAngle: labelPos ? 0 : placement.angle,
        points: route.points,
        selected: this.selectedTransitionIndex() === index
      };
    }).filter(Boolean);
    const markerLinks = layouts.filter((node) => node.marker).map((node) => {
      const marker = node.marker;
      const from2 = marker.kind === "initial" ? { x: marker.x, y: marker.y } : { x: node.x + node.width / 2, y: node.y + node.height };
      const to = marker.kind === "initial" ? { x: node.x + node.width / 2, y: node.y } : { x: marker.x, y: marker.y };
      return { path: this.statePathFromPoints([from2, to]), terminal: marker.kind === "terminal" };
    });
    const routeBounds = transitions.flatMap((line) => line.points);
    const labelBounds = transitions.map((line) => ({ x: line.labelX + line.labelWidth / 2, y: line.labelY + line.labelHeight / 2 }));
    const width = Math.max(sideReserve * 2 + layoutBoardW, 720, ...layouts.map((node) => node.x + node.width + 96), ...routeBounds.map((point) => point.x + 96), ...labelBounds.map((point) => point.x + 48));
    const height = Math.max(360, padY * 2 + topMarkerSpace + bottomMarkerSpace + orderedRows.length * nodeH + Math.max(0, orderedRows.length - 1) * gapY, ...layouts.map((node) => node.y + node.height + bottomMarkerSpace + 80), ...routeBounds.map((point) => point.y + 80), ...labelBounds.map((point) => point.y + 48));
    return { nodes: layouts, transitions, markerLinks, width, height };
  }
  planLegacyStateTransitionRoutes(transitions, nodeMap, nodes) {
    const plans = /* @__PURE__ */ new Map();
    const rows = Math.max(1, ...nodes.map((node) => node.row + 1));
    const minNodeLeft = Math.min(...nodes.map((node) => node.x), 48);
    const maxNodeRight = Math.max(...nodes.map((node) => node.x + node.width), 280);
    const sideUsage = {
      left: Array.from({ length: rows }, () => 0),
      right: Array.from({ length: rows }, () => 0)
    };
    const endpointUsage = {
      left: Array.from({ length: rows }, () => 0),
      right: Array.from({ length: rows }, () => 0)
    };
    const anchorUsage = { left: {}, right: {} };
    const metas = transitions.map((transition, index) => {
      const from2 = nodeMap.get(String(transition.from || ""));
      const to = nodeMap.get(String(transition.to || ""));
      const rowDelta = from2 && to ? to.row - from2.row : 0;
      const isSelfLoop = Boolean(from2 && to && from2.name === to.name);
      const isBackward = Boolean(from2 && to && !isSelfLoop && (to.row < from2.row || to.row === from2.row && nodes.indexOf(to) < nodes.indexOf(from2)));
      const isForwardDetour = Boolean(from2 && to && !isSelfLoop && !isBackward && rowDelta > 1);
      return { transition, index, from: from2, to, rowDelta, isSelfLoop, isBackward, isForwardDetour };
    }).filter((meta) => meta.from && meta.to && (meta.isSelfLoop || meta.isBackward || meta.isForwardDetour));
    const spanRows = (fromRow, toRow) => {
      const start = Math.max(0, Math.min(fromRow, toRow));
      const end = Math.max(start, Math.max(fromRow, toRow));
      return Array.from({ length: end - start + 1 }, (_, offset) => start + offset);
    };
    const sideRouteX = (side, channelIndex) => side === "right" ? maxNodeRight + 28 + channelIndex * 18 : minNodeLeft - 28 - channelIndex * 18;
    const priority = (meta) => {
      if (meta.isBackward)
        return 0;
      if (meta.isForwardDetour)
        return 1;
      return 2;
    };
    metas.slice().sort((left, right) => {
      const spanDiff = spanRows(right.from.row, right.to.row).length - spanRows(left.from.row, left.to.row).length;
      if (spanDiff)
        return spanDiff;
      const priorityDiff = priority(left) - priority(right);
      if (priorityDiff)
        return priorityDiff;
      return left.index - right.index;
    }).forEach((meta) => {
      const from2 = meta.from;
      const to = meta.to;
      const rowsCovered = spanRows(from2.row, to.row);
      const preferredSides = meta.isBackward ? ["left", "right"] : ["right", "left"];
      const [best] = ["left", "right"].map((side) => {
        const channelIndex = rowsCovered.reduce((max, row) => Math.max(max, sideUsage[side][row] || 0), 0);
        const routeX = sideRouteX(side, channelIndex);
        const sourceHookX = side === "right" ? from2.x + from2.width : from2.x;
        const targetHookX = side === "right" ? to.x + to.width : to.x;
        const distanceScore = Math.abs(sourceHookX - routeX) + Math.abs(targetHookX - routeX) + Math.abs(from2.y + from2.height / 2 - (to.y + to.height / 2));
        const crowdScore = rowsCovered.reduce((sum, row) => sum + (sideUsage[side][row] || 0), 0) * (meta.isBackward ? 104 : 64);
        const endpointRows = Array.from(/* @__PURE__ */ new Set([from2.row, to.row]));
        const endpointScore = endpointRows.reduce((sum, row) => sum + (endpointUsage[side][row] || 0), 0) * 108;
        const sourceAnchorKey = `${from2.name}:${from2.row}`;
        const targetAnchorKey = `${to.name}:${to.row}`;
        const anchorScore = ((anchorUsage[side][sourceAnchorKey] || 0) + (anchorUsage[side][targetAnchorKey] || 0)) * 88;
        const preferencePenalty = preferredSides.indexOf(side) * (meta.isBackward ? 36 : 14);
        return { side, channelIndex, routeX, score: distanceScore + crowdScore + endpointScore + anchorScore + preferencePenalty };
      }).sort((left, right) => left.score - right.score);
      rowsCovered.forEach((row) => {
        sideUsage[best.side][row] = Math.max(sideUsage[best.side][row] || 0, best.channelIndex + 1);
      });
      [from2.row, to.row].forEach((row) => {
        endpointUsage[best.side][row] = (endpointUsage[best.side][row] || 0) + 1;
      });
      anchorUsage[best.side][`${from2.name}:${from2.row}`] = (anchorUsage[best.side][`${from2.name}:${from2.row}`] || 0) + 1;
      anchorUsage[best.side][`${to.name}:${to.row}`] = (anchorUsage[best.side][`${to.name}:${to.row}`] || 0) + 1;
      plans.set(meta.transition, { side: best.side, channelIndex: best.channelIndex, routeX: best.routeX });
    });
    return plans;
  }
  stateNodeDisplayWidth(label) {
    const text = String(label || "").trim() || "\u72B6\u6001";
    let units = 0;
    for (const char of text) {
      if (/[\u3400-\u9fff\uf900-\ufaff]/u.test(char))
        units += 1.15;
      else if (/[A-Z0-9]/.test(char))
        units += 0.72;
      else if (/[a-z]/.test(char))
        units += 0.62;
      else
        units += 0.9;
    }
    return Math.max(72, Math.min(172, Math.round(22 + units * 15)));
  }
  syncEntityStateNodes(entity, values, fieldName = "") {
    const owner = fieldName && !(entity.state_nodes || []).length ? this.fieldByName(entity, fieldName) || entity : entity;
    owner.state_nodes ||= [];
    const names = new Set(values);
    owner.state_nodes = owner.state_nodes.filter((node) => names.has(String(node.name || "")));
    for (const [index, name] of values.entries()) {
      if (!owner.state_nodes.some((node) => String(node.name || "") === name)) {
        owner.state_nodes.push({ name, kind: this.defaultStateNodeKind(index, values.length) });
      }
    }
    return owner.state_nodes;
  }
  defaultStateNodeKind(index, total) {
    if (total <= 1)
      return "intermediate";
    if (index === 0)
      return "initial";
    if (index === total - 1)
      return "terminal";
    return "intermediate";
  }
  setStateNodePosition(entity, stateName, pos, markChanged = true, fieldName = this.selectedStateField()?.name || "") {
    const values = this.collectStateValues(entity, fieldName);
    const nodes = this.syncEntityStateNodes(entity, values, fieldName);
    const node = nodes.find((item) => String(item.name || "") === stateName);
    if (!node)
      return;
    node.pos = { x: pos.x, y: pos.y };
    if (markChanged)
      this.changed();
  }
  setStateMarkerPosition(entity, stateName, pos, markChanged = true, fieldName = this.selectedStateField()?.name || "") {
    const values = this.collectStateValues(entity, fieldName);
    const nodes = this.syncEntityStateNodes(entity, values, fieldName);
    const node = nodes.find((item) => String(item.name || "") === stateName);
    if (!node)
      return;
    node.markerPos = { x: pos.x, y: pos.y };
    if (markChanged)
      this.changed();
  }
  setStateLabelPosition(entity, transitionIndex, pos, markChanged = true) {
    const transition = entity.state_transitions?.[transitionIndex];
    if (!transition)
      return;
    transition.labelPos = { x: pos.x, y: pos.y };
    if (markChanged)
      this.changed();
  }
  setStateTransitionRoute(entity, transitionIndex, points, markChanged = true) {
    const transition = entity.state_transitions?.[transitionIndex];
    if (!transition || points.length < 2)
      return;
    const waypoints = points.slice(1, -1).map((point) => ({ x: Math.max(4, Math.round(point.x)), y: Math.max(4, Math.round(point.y)) }));
    transition.route = __spreadProps(__spreadValues({}, transition.route || {}), {
      mode: waypoints.length ? "manual" : "auto",
      waypoints
    });
    transition.waypoints = waypoints;
    if (markChanged)
      this.changed();
  }
  shiftStateRouteSegment(points, segmentIndex, dx, dy) {
    const next = points.map((point) => ({ x: point.x, y: point.y }));
    const current = next[segmentIndex];
    const target = next[segmentIndex + 1];
    if (!current || !target)
      return next;
    const horizontal = Math.abs(current.y - target.y) <= Math.abs(current.x - target.x);
    const delta = horizontal ? { x: 0, y: dy } : { x: dx, y: 0 };
    if (segmentIndex > 0) {
      next[segmentIndex] = { x: Math.max(4, Math.round(current.x + delta.x)), y: Math.max(4, Math.round(current.y + delta.y)) };
    }
    if (segmentIndex + 1 < next.length - 1) {
      next[segmentIndex + 1] = { x: Math.max(4, Math.round(target.x + delta.x)), y: Math.max(4, Math.round(target.y + delta.y)) };
    }
    if (segmentIndex === 0 && next.length === 2) {
      next.splice(1, 0, { x: Math.max(4, Math.round(current.x + delta.x)), y: Math.max(4, Math.round(current.y + delta.y)) });
    }
    return next;
  }
  normalizePoint(point) {
    const x = Number(point?.x);
    const y = Number(point?.y);
    if (!Number.isFinite(x) || !Number.isFinite(y))
      return null;
    return { x: Math.max(4, Math.round(x)), y: Math.max(4, Math.round(y)) };
  }
  routeStateTransition(from2, to, index, transition, plan) {
    const isSelfLoop = from2.name === to.name;
    const isBackward = !isSelfLoop && to.row < from2.row;
    const isForwardDetour = !isSelfLoop && to.row - from2.row > 1;
    if (isBackward || isForwardDetour) {
      const side = plan?.side || (isBackward ? "left" : "right");
      const channelX = plan?.routeX ?? (side === "left" ? Math.min(from2.x, to.x) - 28 : Math.max(from2.x + from2.width, to.x + to.width) + 28);
      const fromX2 = side === "left" ? from2.x : from2.x + from2.width;
      const toX2 = side === "left" ? to.x : to.x + to.width;
      const fromY2 = from2.y + from2.height / 2;
      const toY2 = to.y + to.height / 2;
      const points2 = [
        { x: fromX2, y: fromY2 },
        { x: channelX, y: fromY2 },
        { x: channelX, y: toY2 },
        { x: toX2, y: toY2 }
      ];
      const routedPoints2 = this.applyStateManualRoute(points2, transition, from2, to);
      return {
        path: this.statePathFromPoints(routedPoints2),
        points: routedPoints2,
        labelX: channelX + (side === "left" ? -36 : 10),
        labelY: (fromY2 + toY2) / 2 - 6
      };
    }
    const fromX = from2.x + from2.width / 2;
    const fromY = from2.y + from2.height;
    const toX = to.x + to.width / 2;
    const toY = to.y;
    if (isSelfLoop) {
      const side = plan?.side || "right";
      const routeX = plan?.routeX ?? from2.x + from2.width + 34;
      const loopW = Math.abs(routeX - (side === "right" ? from2.x + from2.width : from2.x));
      const midY2 = from2.y + from2.height / 2;
      const startX = side === "right" ? from2.x + from2.width : from2.x;
      const points2 = [
        { x: startX, y: midY2 - 7 - (plan?.channelIndex || 0) * 3 },
        { x: routeX, y: midY2 - 7 - (plan?.channelIndex || 0) * 3 },
        { x: routeX, y: midY2 + 7 + (plan?.channelIndex || 0) * 3 },
        { x: startX, y: midY2 + 7 + (plan?.channelIndex || 0) * 3 }
      ];
      const routedPoints2 = this.applyStateManualRoute(points2, transition, from2, to);
      return {
        path: this.statePathFromPoints(routedPoints2),
        points: routedPoints2,
        labelX: routeX + (side === "left" ? -36 : 10),
        labelY: midY2 + 14
      };
    }
    const midY = Math.round((fromY + toY) / 2) + (index % 2 ? 10 : 0);
    const points = [
      { x: fromX, y: fromY },
      { x: fromX, y: midY },
      { x: toX, y: midY },
      { x: toX, y: toY }
    ];
    const routedPoints = this.applyStateManualRoute(points, transition, from2, to);
    return {
      path: this.statePathFromPoints(routedPoints),
      points: routedPoints,
      labelX: Math.round((fromX + toX) / 2),
      labelY: midY - 8
    };
  }
  applyStateManualRoute(points, transition, from2, to) {
    const fromAnchor = this.normalizeStateTransitionAnchor(transition?.route?.fromAnchor);
    const toAnchor = this.normalizeStateTransitionAnchor(transition?.route?.toAnchor);
    const anchored = points.map((point) => __spreadValues({}, point));
    if (fromAnchor !== "auto") {
      anchored[0] = this.stateAnchorPoint(from2, fromAnchor, anchored[0]);
    }
    if (toAnchor !== "auto") {
      anchored[anchored.length - 1] = this.stateAnchorPoint(to, toAnchor, anchored[anchored.length - 1]);
    }
    const waypoints = (transition?.route?.waypoints?.length ? transition.route.waypoints : transition?.waypoints) || [];
    const normalized = waypoints.map((point) => this.normalizePoint(point)).filter((point) => Boolean(point));
    return normalized.length ? [anchored[0], ...normalized, anchored[anchored.length - 1]] : anchored;
  }
  normalizeStateTransitionAnchor(anchor) {
    return anchor === "top" || anchor === "right" || anchor === "bottom" || anchor === "left" ? anchor : "auto";
  }
  stateAnchorPoint(node, anchor, fallback) {
    if (!node)
      return fallback;
    if (anchor === "top")
      return { x: node.x + node.width / 2, y: node.y };
    if (anchor === "right")
      return { x: node.x + node.width, y: node.y + node.height / 2 };
    if (anchor === "bottom")
      return { x: node.x + node.width / 2, y: node.y + node.height };
    if (anchor === "left")
      return { x: node.x, y: node.y + node.height / 2 };
    return fallback;
  }
  stateLinkLabelMetrics(label) {
    const text = String(label || "").trim() || "\u6D41\u8F6C";
    let units = 0;
    for (const ch of text) {
      if (/[\u3400-\u9fff\uf900-\ufaff]/u.test(ch))
        units += 1.1;
      else if (/[A-Z0-9]/.test(ch))
        units += 0.72;
      else if (/[a-z]/.test(ch))
        units += 0.62;
      else
        units += 0.9;
    }
    return {
      text,
      width: Math.max(34, Math.min(160, Math.round(18 + units * 12))),
      height: 20
    };
  }
  stateLinkLabelPlacement(points, preferredX, preferredY, metrics) {
    const segments = [];
    for (let index = 0; index < points.length - 1; index += 1) {
      const start = points[index];
      const end = points[index + 1];
      const dx = end.x - start.x;
      const dy = end.y - start.y;
      const horizontal = Math.abs(dy) < 0.5;
      const vertical = Math.abs(dx) < 0.5;
      segments.push({
        axis: horizontal ? "horizontal" : vertical ? "vertical" : "diagonal",
        length: Math.hypot(dx, dy),
        midX: (start.x + end.x) / 2,
        midY: (start.y + end.y) / 2
      });
    }
    const xs = points.map((point) => point.x);
    const minX = Math.min(...xs);
    const maxX = Math.max(...xs);
    const ranked = segments.filter((segment) => segment.length > 0).sort((left, right) => {
      const score = (segment) => {
        const axisPenalty = segment.axis === "horizontal" ? 0 : segment.axis === "vertical" ? 6 : 999;
        const edgeGap = Math.min(Math.abs(segment.midX - minX), Math.abs(segment.midX - maxX));
        const edgePenalty = segment.axis === "diagonal" ? 999 : edgeGap < 16 ? 70 : 0;
        return Math.abs(segment.midX - preferredX) * 1.6 + Math.abs(segment.midY - preferredY) * 0.28 + axisPenalty + edgePenalty - Math.min(segment.length, 160) * 0.18;
      };
      return score(left) - score(right);
    });
    const target = ranked[0];
    if (!target)
      return { x: points[0]?.x || 0, y: points[0]?.y || 0, angle: 0 };
    return {
      x: target.axis === "vertical" ? target.midX + (target.midX <= preferredX ? 1 : -1) * Math.max(24, Math.min(34, Math.round(metrics.width / 2 - 2))) : target.midX,
      y: target.midY,
      angle: 0
    };
  }
  statePathFromPoints(points) {
    return points.map((point, index) => `${index === 0 ? "M" : "L"} ${Math.round(point.x)} ${Math.round(point.y)}`).join(" ");
  }
  collectStateValues(entity, fieldName = "") {
    if (!entity)
      return [];
    const values = /* @__PURE__ */ new Set();
    const fields = fieldName ? (entity.fields || []).filter((field) => String(field.name || "") === fieldName) : entity.fields || [];
    const explicitStatus = fields.some((field) => field.is_status || field.status_role);
    for (const field of fields) {
      const delimiter = field.is_status || field.status_role ? "/" : /[,\n，、/]/;
      const rawValues = [
        ...Array.isArray(field.states) ? field.states : [],
        ...String(field.state_values || "").split(delimiter),
        ...this.stateValuesFromNote(field.note || "")
      ];
      rawValues.map((value) => value.trim()).filter(Boolean).forEach((value) => values.add(value));
    }
    if (!explicitStatus || !values.size) {
      for (const transition of this.transitionsForField(entity, fieldName)) {
        if (transition.from)
          values.add(transition.from);
        if (transition.to)
          values.add(transition.to);
      }
    }
    return Array.from(values);
  }
  collectStateFields(entity) {
    if (!entity)
      return [];
    const explicit = (entity.fields || []).filter((field) => field.is_status || field.status_role).sort((left, right) => {
      const leftPriority = left.status_role === "primary" ? 0 : 1;
      const rightPriority = right.status_role === "primary" ? 0 : 1;
      return leftPriority - rightPriority;
    });
    return explicit.length ? explicit : (entity.fields || []).filter((field) => field.state_values || this.stateValuesFromNote(field.note || "").length);
  }
  buildStatePanels(entity) {
    if (!entity)
      return [];
    const activeName = this.selectedStateField()?.name || "";
    return this.stateFields().map((field) => {
      const name = String(field.name || "");
      const role = field.status_role === "secondary" ? "secondary" : field.status_role === "primary" || field.is_status ? "primary" : "none";
      return {
        field,
        name,
        label: `${name || "\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5"}${role === "primary" ? " \xB7 \u4E3B\u72B6\u6001" : role === "secondary" ? " \xB7 \u5B50\u72B6\u6001" : ""}`,
        values: this.collectStateValues(entity, name),
        board: this.layoutStateBoard(entity, name),
        active: name === activeName,
        role
      };
    });
  }
  groupStateEntities() {
    const groups = /* @__PURE__ */ new Map();
    for (const entity of this.entities()) {
      const name = this.constructLabel(entity);
      const group = groups.get(name) || { name, items: [] };
      group.items.push(entity);
      groups.set(name, group);
    }
    return Array.from(groups.values());
  }
  ensureActiveStateField(entity) {
    if (!entity)
      return;
    const fields = this.collectStateFields(entity);
    if (!fields.length)
      return;
    if (!fields.some((field) => String(field.name || "") === this.stateFieldName())) {
      this.stateFieldName.set(String(fields[0].name || ""));
    }
  }
  fieldByName(entity, fieldName) {
    return (entity.fields || []).find((field) => String(field.name || "") === fieldName) || null;
  }
  transitionsForField(entity, fieldName = "") {
    const transitions = entity.state_transitions || [];
    if (!fieldName)
      return transitions;
    return transitions.filter((transition) => !transition.field_name || transition.field_name === fieldName);
  }
  processNodes(process2) {
    const pools = [process2?.tasks, process2?.nodes, process2?.steps, process2?.flow?.nodes, process2?.taskDefinitions];
    return pools.flatMap((items) => Array.isArray(items) ? items : []);
  }
  stateValuesFromNote(note) {
    const text = String(note || "");
    if (!text)
      return [];
    const match = text.match(/(?:状态|取值|枚举)[：:]\s*([^。；;\n]+)/);
    if (match?.[1]) {
      return match[1].split(/[\/,，、\s]+/).filter(Boolean);
    }
    return text.split(/[\/,，、\s]+/).map((s) => s.trim()).filter(Boolean);
  }
  static \u0275fac = function EntityDesignWorkbenchComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _EntityDesignWorkbenchComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _EntityDesignWorkbenchComponent, selectors: [["app-entity-design-workbench"]], hostBindings: function EntityDesignWorkbenchComponent_HostBindings(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275listener("mousemove", function EntityDesignWorkbenchComponent_mousemove_HostBindingHandler($event) {
        return ctx.onDocumentMouseMove($event);
      }, \u0275\u0275resolveDocument)("mouseup", function EntityDesignWorkbenchComponent_mouseup_HostBindingHandler() {
        return ctx.onDocumentMouseUp();
      }, \u0275\u0275resolveDocument)("keydown", function EntityDesignWorkbenchComponent_keydown_HostBindingHandler($event) {
        return ctx.onDocumentKeydown($event);
      }, \u0275\u0275resolveDocument);
    }
  }, inputs: { showEditorToggle: "showEditorToggle", exportGraphId: "exportGraphId", focusConstructId: "focusConstructId", initialView: "initialView", initialEntityId: "initialEntityId", editing: "editing" }, outputs: { editRequested: "editRequested" }, features: [\u0275\u0275NgOnChangesFeature], decls: 22, vars: 17, consts: [["data-testid", "entity-design-angular", 1, "entity-design"], [1, "entity-design-toolbar"], ["role", "tablist", "aria-label", "\u5B9E\u4F53\u8BBE\u8BA1\u89C6\u56FE\u5207\u6362", 1, "data-view-switch"], ["type", "button", "data-testid", "entity-design-switch-relation", 1, "seg-btn", 3, "click"], ["type", "button", "data-testid", "entity-design-switch-state", 1, "seg-btn", 3, "click"], ["data-testid", "entity-state-shortcut-hint", 1, "live-diagram-hint", "entity-shortcut-hint"], ["data-testid", "entity-relation-shortcut-hint", 1, "live-diagram-hint", "entity-shortcut-hint"], ["data-testid", "entity-state-zoom-controls", 1, "entity-state-zoom-controls"], ["type", "button", "data-testid", "state-editor-hide", 1, "btn", "btn-outline", "btn-sm", "editor-toggle", "active"], ["type", "button", "data-testid", "entity-design-editor-hide", 1, "btn", "btn-outline", "btn-sm", "editor-toggle", "active"], ["type", "button", "data-testid", "entity-design-editor-open", 1, "btn", "btn-outline", "btn-sm"], [1, "entity-design-body"], ["data-testid", "entity-design-canvas-shell", 1, "entity-design-canvas-shell", 3, "wheel"], ["id", "ef-canvas-entity-diagram", "data-testid", "entity-design-relation-view", 1, "entity-relation-canvas", "ef-canvas", 3, "ngStyle"], ["data-testid", "entity-design-state-view", 1, "entity-state-workbench"], ["data-testid", "entity-design-drawer", 1, "entity-design-drawer", "entity-drawer", "open", 3, "width"], ["data-testid", "state-editor-drawer", 1, "state-editor-drawer", 3, "width"], ["type", "button", "data-testid", "entity-design-open-drawer", 1, "btn", "btn-outline", "btn-sm", 3, "click", "disabled"], ["type", "button", "data-testid", "entity-design-add-entity", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["type", "button", "data-testid", "entity-design-reset-layout", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["type", "button", "data-testid", "state-toolbar-add-transition", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["type", "button", "data-testid", "state-toolbar-reset-routes", 1, "btn", "btn-outline", "btn-sm", 3, "click", "disabled"], ["type", "button", "data-testid", "state-zoom-out", 1, "btn", "btn-ghost-sm", 3, "click"], ["type", "button", "data-testid", "state-zoom-reset", 1, "btn", "btn-ghost-sm", 3, "click"], ["type", "button", "data-testid", "state-zoom-in", 1, "btn", "btn-ghost-sm", 3, "click"], ["type", "button", "data-testid", "state-editor-hide", 1, "btn", "btn-outline", "btn-sm", "editor-toggle", "active", 3, "click"], ["type", "button", "data-testid", "state-editor-open", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["type", "button", "data-testid", "entity-design-editor-open", 1, "entity-visually-hidden", 3, "click"], ["type", "button", "data-testid", "entity-design-editor-hide", 1, "btn", "btn-outline", "btn-sm", "editor-toggle", "active", 3, "click"], ["type", "button", "data-testid", "entity-design-editor-open", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["id", "ef-board-entity-diagram", "data-export-graph-ready", "true", 1, "entity-board", "ef-board", 3, "mousedown", "click", "ngStyle"], [1, "entity-group-frame", "ef-group-frame", 3, "ngStyle", "title"], ["id", "ef-svg-entity-diagram", "aria-hidden", "true", 1, "entity-rel-svg", "ef-svg"], ["id", "entity-arrow", "viewBox", "0 0 10 10", "refX", "8", "refY", "5", "markerWidth", "6", "markerHeight", "6", "orient", "auto-start-reverse"], ["d", "M 0 0 L 10 5 L 0 10 z"], ["marker-end", "url(#entity-arrow)", 1, "entity-rel-line", "ef-rel", 3, "is-focus", "is-muted", "ef-muted", "ef-focus", "is-dashed"], [1, "entity-rel-label", 3, "ngStyle"], ["type", "button", "data-testid", "entity-design-node", 1, "entity-node", "ef-node", "ef-draggable", 3, "is-selected", "ef-selected", "is-focus", "ef-focus", "ef-muted", "ngStyle"], [1, "entity-selection-box", 3, "ngStyle"], [1, "entity-component-frame", "ef-component-frame", 3, "mousedown", "ngStyle", "title"], [1, "entity-component-title", "ef-component-title", 3, "mousedown", "title"], [1, "entity-group-frame", "ef-group-frame", 3, "mousedown", "ngStyle", "title"], [1, "entity-group-title", "ef-group-title", 3, "mousedown", "title"], ["marker-end", "url(#entity-arrow)", 1, "entity-rel-line", "ef-rel", 3, "click"], ["type", "button", "data-testid", "entity-design-node", 1, "entity-node", "ef-node", "ef-draggable", 3, "mousedown", "click", "dblclick", "ngStyle"], [1, "ef-nname"], [1, "entity-empty"], [1, "entity-state-browser"], [1, "entity-state-group"], [1, "entity-state-stage"], [1, "entity-state-main-shell", 3, "wheel", "ngStyle"], [1, "entity-state-main"], ["data-testid", "entity-state-diagram", 1, "entity-state-card"], [1, "entity-state-card-head"], [1, "entity-state-card-title"], [1, "entity-state-card-subtitle"], [1, "entity-state-card-summary"], ["type", "button", 1, "entity-state-field-chip", 3, "active"], [1, "entity-state-overview-grid"], ["data-testid", "entity-state-overview-field", 1, "entity-state-field-panel", 3, "active", "role-primary", "role-secondary"], ["data-testid", "entity-state-empty", 1, "diag-empty"], [1, "entity-state-group-title"], [1, "entity-state-group-list"], ["type", "button", 1, "entity-state-chip", 3, "active"], ["type", "button", 1, "entity-state-chip", 3, "click"], ["type", "button", 1, "entity-state-field-chip", 3, "click"], ["data-testid", "entity-state-overview-field", 1, "entity-state-field-panel", 3, "click"], [1, "entity-state-field-panel-head"], [1, "entity-state-field-panel-title"], [1, "entity-state-field-panel-meta"], [1, "entity-state-field-panel-badges"], [1, "entity-state-field-badge"], [1, "entity-state-field-badge", "active"], [1, "entity-state-card-values"], [1, "entity-state-value-chip", 3, "kind-initial", "kind-terminal"], ["data-testid", "entity-state-graph-canvas", "data-export-graph-ready", "true", 1, "entity-state-graph"], ["data-testid", "entity-state-zoom-shell", 1, "entity-state-zoom-shell", 3, "ngStyle"], ["data-testid", "entity-state-zoom-target", 1, "entity-state-zoom-target", 3, "ngStyle"], [1, "entity-state-canvas", 3, "ngStyle"], ["aria-hidden", "true", 1, "entity-state-svg"], ["id", "entity-state-arrow", "markerWidth", "10", "markerHeight", "10", "refX", "8", "refY", "4", "orient", "auto"], ["d", "M 0 0 L 8 4 L 0 8 z"], [1, "entity-state-link", "entity-state-link-anchor"], [1, "entity-state-board", 3, "ngStyle"], ["data-testid", "entity-state-empty", 1, "entity-empty"], [1, "entity-state-value-chip"], ["data-testid", "entity-state-graph-link", "marker-end", "url(#entity-state-arrow)", 1, "entity-state-link", 3, "click"], ["data-testid", "entity-state-link-label-group", 1, "entity-state-link-label-group", 3, "mousedown"], ["rx", "10", 1, "entity-state-link-label-box"], ["text-anchor", "middle", "dominant-baseline", "middle", 1, "entity-state-link-label"], ["data-testid", "entity-state-link-route-hitbox", 1, "entity-state-link-route-hitbox", 2, "position", "absolute", "z-index", "3", "cursor", "move", 3, "ngStyle"], ["type", "button", "data-testid", "entity-state-link-endpoint-handle", "title", "\u8C03\u6574\u8D77\u70B9\u951A\u70B9", 1, "entity-state-link-endpoint-handle", "entity-state-link-endpoint-from", 2, "position", "absolute", "z-index", "6", "width", "12px", "height", "12px", "border", "2px solid #2563eb", "border-radius", "50%", "background", "#fff", 3, "mousedown", "ngStyle"], ["type", "button", "data-testid", "entity-state-link-endpoint-handle", "title", "\u8C03\u6574\u7EC8\u70B9\u951A\u70B9", 1, "entity-state-link-endpoint-handle", "entity-state-link-endpoint-to", 2, "position", "absolute", "z-index", "6", "width", "12px", "height", "12px", "border", "2px solid #2563eb", "border-radius", "50%", "background", "#fff", 3, "mousedown", "ngStyle"], ["data-testid", "entity-state-link-route-hitbox", 1, "entity-state-link-route-hitbox", 2, "position", "absolute", "z-index", "3", "cursor", "move", 3, "mousedown", "ngStyle"], ["data-testid", "entity-state-start-dot", 1, "entity-state-marker", "entity-state-start-dot", 3, "ngStyle"], [1, "entity-state-node", 3, "mousedown", "ngStyle"], [1, "entity-state-node-label"], ["data-testid", "entity-state-end-dot", 1, "entity-state-marker", "entity-state-end-dot-outer", 3, "ngStyle"], ["data-testid", "entity-state-start-dot", 1, "entity-state-marker", "entity-state-start-dot", 3, "mousedown", "ngStyle"], ["data-testid", "entity-state-end-dot", 1, "entity-state-marker", "entity-state-end-dot-outer", 3, "mousedown", "ngStyle"], ["data-testid", "entity-design-drawer", 1, "entity-design-drawer", "entity-drawer", "open"], ["data-testid", "entity-drawer-resize-handle", 1, "drawer-resize-handle", "entity-design-drawer-resize", 3, "mousedown"], [1, "drawer-head", "entity-design-drawer-head"], [1, "drawer-crumb"], [1, "drawer-actions"], ["type", "button", "data-testid", "entity-duplicate-button", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["type", "button", "data-testid", "entity-design-delete", 1, "btn", "btn-danger", "btn-sm", 3, "click"], ["type", "button", "data-testid", "entity-design-drawer-close", "title", "\u5173\u95ED\u7F16\u8F91", 1, "drawer-close", 3, "click"], [1, "entity-design-drawer-body", "drawer-body"], [1, "form-grid", "entity-form-grid"], [1, "field-group"], ["type", "text", "data-testid", "entity-design-name", 3, "input", "value"], ["data-testid", "entity-design-construct", 3, "change", "value"], ["value", ""], [3, "value", "selected"], [1, "field-group", 2, "grid-column", "1/-1"], ["type", "text", "data-testid", "entity-design-note", "placeholder", "\u7B80\u8981\u8BF4\u660E", 3, "input", "value"], [1, "form-section", "entity-edit-section", "entity-reference-section"], [1, "form-section", "entity-edit-section"], ["type", "button", "data-testid", "entity-design-add-field", 1, "btn", "btn-outline", "btn-sm", 3, "click"], [1, "field-table"], [1, "no-refs"], ["type", "button", "data-testid", "entity-design-add-relation", 1, "btn", "btn-outline", "btn-sm", 3, "click", "disabled"], ["type", "button", "data-testid", "entity-relation-debug-toggle", 1, "btn", "btn-ghost-sm", "entity-relation-debug-toggle", 3, "click"], [1, "rel-scope-tip"], ["data-testid", "entity-relation-debug", 1, "entity-relation-debug"], ["data-testid", "entity-design-relation-row", 1, "rel-row", "entity-relation-row"], [1, "task-ref-list"], [1, "task-ref", 3, "title"], ["title", "\u4E3B\u952E"], ["title", "\u4E3B\u72B6\u6001\u5B57\u6BB5"], ["data-testid", "entity-design-field-row"], [1, "field-td-name"], ["type", "text", "placeholder", "\u5B57\u6BB5\u540D", 3, "input", "value"], [1, "field-td-type"], [3, "change", "value"], [2, "text-align", "center"], ["type", "checkbox", 3, "change", "checked"], [1, "field-status-cell"], [1, "field-status-role-wrap"], ["aria-label", "\u5B57\u6BB5\u72B6\u6001\u89D2\u8272", 1, "field-status-role-select", 3, "change", "value"], ["value", "primary"], ["value", "secondary"], [1, "field-td-note"], ["rows", "1", "placeholder", "\u5B57\u6BB5\u89C4\u5219", 1, "auto-resize", 3, "input", "value"], [1, "field-actions-cell"], [1, "field-actions"], ["type", "button", "title", "\u5728\u4E0B\u65B9\u63D2\u5165\u5B57\u6BB5", 1, "field-action", "field-add-after", 3, "click"], ["type", "button", "title", "\u4E0A\u79FB", 1, "field-action", "field-move-up", 3, "click", "disabled"], ["type", "button", "title", "\u4E0B\u79FB", 1, "field-action", "field-move-down", 3, "click", "disabled"], ["type", "button", 1, "field-del", 3, "click"], [1, "entity-relation-debug-title"], [3, "visible-row"], ["value", "", "disabled", ""], [1, "rel-type-select", 3, "change", "value"], ["value", "1:1"], ["value", "1:N"], ["value", "N:N"], ["type", "text", "data-testid", "entity-relation-label", "placeholder", "\u5173\u7CFB\u8BF4\u660E", 1, "rel-label-input", 3, "input", "value"], [1, "rel-actions"], ["type", "button", "title", "\u5728\u4E0B\u65B9\u63D2\u5165\u5173\u7CFB", 1, "rel-action", "rel-add-after", 3, "click"], ["type", "button", "title", "\u4E0A\u79FB", 1, "rel-action", "rel-move-up", 3, "click", "disabled"], ["type", "button", "title", "\u4E0B\u79FB", 1, "rel-action", "rel-move-down", 3, "click", "disabled"], ["data-testid", "state-editor-drawer", 1, "state-editor-drawer"], ["data-testid", "state-editor-drawer-resize-handle", 1, "state-editor-drawer-resize", 3, "mousedown"], [1, "drawer-head"], [1, "dc-sep"], ["type", "button", "data-testid", "state-editor-close", "title", "\u5173\u95ED\u7F16\u8F91", 1, "drawer-close", 3, "click"], [1, "drawer-body", "state-editor-body"], [1, "entity-state-editor"], [1, "entity-state-editor-head"], [1, "entity-state-editor-hint"], [1, "entity-state-config-row"], ["data-testid", "entity-state-field-select", 3, "change", "value"], [3, "value"], [1, "field-group", "entity-state-values-field"], ["tabindex", "0", "data-tip", "\u8BF7\u5728\u5B57\u6BB5\u8868\u7684\u5B57\u6BB5\u89C4\u5219\u4E2D\u586B\u5199\u72B6\u6001\u503C\uFF0C\u5E76\u7528 / \u5206\u9694\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F/\u5F85\u5BA1\u6838/\u5BA1\u6838\u901A\u8FC7/\u5DF2\u4F5C\u5E9F", 1, "inline-help"], ["data-testid", "entity-state-values-text", 1, "entity-state-values-readonly"], [1, "entity-state-kind-editor"], [1, "entity-state-kind-head"], ["data-testid", "entity-state-kind-list", 1, "entity-state-kind-list"], [1, "entity-state-kind-row"], [1, "entity-muted"], ["data-testid", "entity-transition-section-title", 1, "entity-state-transition-head"], [1, "entity-transition-list"], ["data-testid", "entity-design-transition-row", 1, "entity-transition-row"], [1, "entity-state-kind-name"], ["value", "initial"], ["value", "intermediate"], ["value", "terminal"], [1, "entity-transition-arrow"], ["type", "text", "placeholder", "\u5907\u6CE8\u8BF4\u660E", 3, "input", "value"], [1, "entity-transition-actions"], ["type", "button", "title", "\u5728\u4E0B\u65B9\u63D2\u5165\u6D41\u8F6C", 1, "transition-action", 3, "click"], ["type", "button", "title", "\u4E0A\u79FB", 1, "transition-action", 3, "click", "disabled"], ["type", "button", "title", "\u4E0B\u79FB", 1, "transition-action", 3, "click", "disabled"], ["type", "button", "title", "\u5220\u9664", 1, "transition-action", "transition-action-delete", 3, "click"]], template: function EntityDesignWorkbenchComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "section", 0)(1, "div", 1)(2, "div", 2)(3, "button", 3);
      \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Template_button_click_3_listener() {
        return ctx.setView("relation");
      });
      \u0275\u0275text(4, "\u5173\u7CFB\u56FE");
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(5, "button", 4);
      \u0275\u0275listener("click", function EntityDesignWorkbenchComponent_Template_button_click_5_listener() {
        return ctx.setView("state");
      });
      \u0275\u0275text(6, "\u72B6\u6001\u56FE");
      \u0275\u0275elementEnd()();
      \u0275\u0275conditionalCreate(7, EntityDesignWorkbenchComponent_Conditional_7_Template, 19, 0, "span", 5)(8, EntityDesignWorkbenchComponent_Conditional_8_Template, 19, 0, "span", 6);
      \u0275\u0275conditionalCreate(9, EntityDesignWorkbenchComponent_Conditional_9_Template, 6, 1)(10, EntityDesignWorkbenchComponent_Conditional_10_Template, 4, 1);
      \u0275\u0275conditionalCreate(11, EntityDesignWorkbenchComponent_Conditional_11_Template, 7, 1, "div", 7);
      \u0275\u0275conditionalCreate(12, EntityDesignWorkbenchComponent_Conditional_12_Template, 2, 0, "button", 8)(13, EntityDesignWorkbenchComponent_Conditional_13_Template, 4, 0)(14, EntityDesignWorkbenchComponent_Conditional_14_Template, 2, 0, "button", 9)(15, EntityDesignWorkbenchComponent_Conditional_15_Template, 2, 0, "button", 10);
      \u0275\u0275elementEnd();
      \u0275\u0275elementStart(16, "div", 11)(17, "div", 12);
      \u0275\u0275listener("wheel", function EntityDesignWorkbenchComponent_Template_div_wheel_17_listener($event) {
        return ctx.onRelationWheel($event);
      });
      \u0275\u0275conditionalCreate(18, EntityDesignWorkbenchComponent_Conditional_18_Template, 17, 8, "div", 13)(19, EntityDesignWorkbenchComponent_Conditional_19_Template, 3, 1, "div", 14);
      \u0275\u0275elementEnd();
      \u0275\u0275conditionalCreate(20, EntityDesignWorkbenchComponent_Conditional_20_Template, 52, 14, "aside", 15);
      \u0275\u0275conditionalCreate(21, EntityDesignWorkbenchComponent_Conditional_21_Template, 56, 8, "aside", 16);
      \u0275\u0275elementEnd()();
    }
    if (rf & 2) {
      let tmp_3_0;
      let tmp_10_0;
      let tmp_11_0;
      \u0275\u0275advance(3);
      \u0275\u0275classProp("active", ctx.view() === "relation");
      \u0275\u0275advance(2);
      \u0275\u0275classProp("active", ctx.view() === "state");
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.view() === "state" ? 7 : 8);
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.view() === "relation" && ctx.canEditRelation() ? 9 : (tmp_3_0 = ctx.view() === "state" && ctx.canEditState() && ctx.selectedEntity()) ? 10 : -1, tmp_3_0);
      \u0275\u0275advance(2);
      \u0275\u0275conditional(ctx.view() === "state" ? 11 : -1);
      \u0275\u0275advance();
      \u0275\u0275conditional(ctx.showEditorToggle && ctx.view() === "state" && ctx.stateEditorOpen() ? 12 : ctx.showEditorToggle && ctx.view() === "state" ? 13 : ctx.showEditorToggle && ctx.editorOpen() ? 14 : ctx.showEditorToggle ? 15 : -1);
      \u0275\u0275advance(4);
      \u0275\u0275styleProp("grid-template-columns", ctx.drawerGridColumns());
      \u0275\u0275classProp("has-drawer", ctx.editorOpen() && ctx.selectedEntity());
      \u0275\u0275advance();
      \u0275\u0275styleProp("margin-right", ctx.relationDrawerOffset(), "px");
      \u0275\u0275advance();
      \u0275\u0275conditional(ctx.view() === "relation" ? 18 : 19);
      \u0275\u0275advance(2);
      \u0275\u0275conditional((tmp_10_0 = ctx.view() === "relation" && ctx.editorOpen() && ctx.selectedEntity()) ? 20 : -1, tmp_10_0);
      \u0275\u0275advance();
      \u0275\u0275conditional((tmp_11_0 = ctx.view() === "state" && ctx.stateEditorOpen() && ctx.selectedEntity()) ? 21 : -1, tmp_11_0);
    }
  }, dependencies: [CommonModule, NgStyle], styles: ['\n[_nghost-%COMP%] {\n  display: block;\n  height: 100%;\n  min-height: 0;\n}\n.entity-design[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-rows: auto minmax(0, 1fr);\n  gap: 10px;\n  height: 100%;\n  min-height: 0;\n}\n.entity-design-toolbar[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  padding: 6px 10px;\n  border: 1px solid #d7e2ef;\n  border-radius: 8px;\n}\n.entity-visually-hidden[_ngcontent-%COMP%] {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n}\n.entity-design-body[_ngcontent-%COMP%] {\n  position: relative;\n  display: block;\n  min-height: 0;\n  overflow: hidden;\n}\n.entity-design-body.has-drawer[_ngcontent-%COMP%] {\n  display: block;\n}\n.entity-design-canvas-shell[_ngcontent-%COMP%] {\n  height: 100%;\n  min-height: 0;\n  min-width: 0;\n  overflow-x: auto;\n  overflow-y: auto;\n  border: 1px solid #d7e2ef;\n  border-radius: 10px;\n  background: #fff;\n  scrollbar-gutter: stable;\n  transition: margin-right 0.12s ease;\n}\n.entity-design-canvas-shell[_ngcontent-%COMP%]::-webkit-scrollbar, \n.entity-design-drawer-body[_ngcontent-%COMP%]::-webkit-scrollbar {\n  width: 6px;\n  height: 6px;\n}\n.entity-design-canvas-shell[_ngcontent-%COMP%]::-webkit-scrollbar-thumb, \n.entity-design-drawer-body[_ngcontent-%COMP%]::-webkit-scrollbar-thumb {\n  border-radius: 999px;\n  background: #c7d2df;\n}\n.entity-relation-canvas[_ngcontent-%COMP%], \n.entity-state-workbench[_ngcontent-%COMP%] {\n  min-width: 900px;\n  min-height: 520px;\n  padding: 8px 14px;\n}\n.entity-relation-canvas[_ngcontent-%COMP%] {\n  display: inline-block;\n}\n.entity-state-workbench[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: 240px minmax(0, 1fr);\n  gap: 14px;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  min-height: 0;\n  padding: 12px 16px 16px;\n  overflow: hidden;\n  background: #f8fafc;\n}\n.entity-state-browser[_ngcontent-%COMP%] {\n  min-height: 0;\n  overflow: hidden auto;\n  padding: 12px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 12px;\n  background: #fff;\n}\n.entity-state-group[_ngcontent-%COMP%]    + .entity-state-group[_ngcontent-%COMP%] {\n  margin-top: 12px;\n}\n.entity-state-group-title[_ngcontent-%COMP%] {\n  margin-bottom: 8px;\n  color: #475569;\n  font-size: 12px;\n  font-weight: 700;\n}\n.entity-state-group-list[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.entity-state-chip[_ngcontent-%COMP%] {\n  width: 100%;\n  padding: 7px 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 10px;\n  background: #fff;\n  color: #0f172a;\n  font: inherit;\n  text-align: left;\n  cursor: pointer;\n}\n.entity-state-chip.active[_ngcontent-%COMP%] {\n  border-color: #2563eb;\n  background: #eff6ff;\n  color: #2563eb;\n  font-weight: 600;\n}\n.entity-state-stage[_ngcontent-%COMP%] {\n  position: relative;\n  min-width: 0;\n  min-height: 0;\n  overflow: hidden;\n  display: flex;\n  flex-direction: column;\n}\n.entity-state-main-shell[_ngcontent-%COMP%] {\n  min-width: 0;\n  min-height: 0;\n  height: 100%;\n  overflow: auto;\n  transition: margin-right 0.12s ease;\n}\n.entity-state-main[_ngcontent-%COMP%] {\n  min-width: 0;\n  padding: 0 14px 14px 0;\n}\n.entity-state-card[_ngcontent-%COMP%] {\n  width: 100%;\n  padding: 14px 16px 16px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 12px;\n  background: #fff;\n}\n.entity-board[_ngcontent-%COMP%] {\n  position: relative;\n  border-radius: 0;\n  background: #fff;\n}\n.entity-board.is-editing[_ngcontent-%COMP%] {\n  background:\n    linear-gradient(#e6eef8 1px, transparent 1px),\n    linear-gradient(\n      90deg,\n      #e6eef8 1px,\n      transparent 1px),\n    #fff;\n  background-size: 180px 128px;\n}\n.entity-component-frame[_ngcontent-%COMP%], \n.entity-group-frame[_ngcontent-%COMP%] {\n  position: absolute;\n  box-sizing: border-box;\n  pointer-events: none;\n}\n.entity-component-frame[_ngcontent-%COMP%] {\n  z-index: 0;\n  border: 1px solid #cbd5e1;\n  border-radius: 8px;\n  background: rgba(248, 250, 252, 0.72);\n}\n.entity-component-title[_ngcontent-%COMP%], \n.entity-group-title[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 3;\n  min-width: 60px;\n  padding: 3px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.95);\n  color: #334155;\n  font-size: 12px;\n  font-weight: 800;\n  line-height: 1.4;\n  text-align: center;\n  white-space: nowrap;\n  cursor: pointer;\n  -webkit-user-select: none;\n  user-select: none;\n  transition:\n    background 0.15s,\n    border-color 0.15s,\n    box-shadow 0.15s;\n}\n.entity-component-title[_ngcontent-%COMP%]:hover, \n.entity-group-title[_ngcontent-%COMP%]:hover {\n  background: #dbeafe;\n  border-color: #60a5fa;\n  box-shadow: 0 0 0 2px rgba(96, 165, 250, 0.25);\n}\n.entity-group-frame[_ngcontent-%COMP%] {\n  z-index: 1;\n  border: 1px dashed var(--entity-frame-color);\n  border-radius: 8px;\n  background: var(--entity-frame-fill);\n}\n.entity-group-title[_ngcontent-%COMP%] {\n  top: 5px;\n  left: 10px;\n  max-width: calc(100% - 20px);\n  pointer-events: auto;\n}\n.entity-rel-svg[_ngcontent-%COMP%] {\n  z-index: 2;\n  position: absolute;\n  inset: 0;\n  overflow: visible;\n  pointer-events: none;\n}\n.entity-rel-svg[_ngcontent-%COMP%]   marker[_ngcontent-%COMP%]   path[_ngcontent-%COMP%] {\n  fill: #64748b;\n}\n.entity-rel-line[_ngcontent-%COMP%] {\n  fill: none;\n  stroke-linecap: round;\n  cursor: pointer;\n  pointer-events: stroke;\n}\n.entity-rel-line.is-dashed[_ngcontent-%COMP%] {\n  stroke-dasharray: 5 3;\n}\n.entity-rel-line.is-muted[_ngcontent-%COMP%] {\n  filter: grayscale(0.3);\n}\n.entity-rel-label[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 5;\n  max-width: 160px;\n  overflow: hidden;\n  padding: 1px 5px;\n  border: 1px solid rgba(203, 213, 225, 0.8);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.94);\n  color: #475569;\n  font-size: 10px;\n  line-height: 1.2;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  transform: translate(-50%, -50%);\n  pointer-events: none;\n}\n.entity-node[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 4;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  box-sizing: border-box;\n  padding: 5px 13px;\n  border: 2px solid var(--entity-stroke);\n  border-radius: 6px;\n  background: var(--entity-fill);\n  color: var(--entity-accent);\n  cursor: pointer;\n  font-family: inherit;\n  font-size: 14px;\n  text-align: center;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.entity-node[_ngcontent-%COMP%]:hover, \n.entity-node.is-focus[_ngcontent-%COMP%] {\n  border-color: #2563eb;\n  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.14);\n}\n.entity-node.is-selected[_ngcontent-%COMP%] {\n  outline: 2px solid #2563eb;\n  outline-offset: 2px;\n}\n.entity-node-component[_ngcontent-%COMP%] {\n  display: none;\n  color: var(--entity-accent);\n  font-size: 11px;\n  font-weight: 800;\n}\n.entity-node[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  overflow: hidden;\n  font-size: 14px;\n  font-weight: 800;\n  line-height: normal;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-node[_ngcontent-%COMP%]   em[_ngcontent-%COMP%] {\n  display: none;\n  color: #64748b;\n  font-size: 12px;\n  font-style: normal;\n}\n.entity-selection-box[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 4;\n  border: 1px dashed #60a5fa;\n  border-radius: 8px;\n  background: rgba(147, 197, 253, 0.14);\n  pointer-events: none;\n}\n.entity-state-card-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n  margin-bottom: 12px;\n}\n.entity-state-card-summary[_ngcontent-%COMP%] {\n  display: flex;\n  flex-wrap: wrap;\n  justify-content: flex-end;\n  gap: 8px;\n}\n.entity-state-field-chip[_ngcontent-%COMP%] {\n  padding: 5px 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 999px;\n  background: #fff;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.entity-state-field-chip.active[_ngcontent-%COMP%] {\n  border-color: #2563eb;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.entity-state-card-title[_ngcontent-%COMP%] {\n  color: #0f172a;\n  font-size: 16px;\n  font-weight: 700;\n}\n.entity-state-card-subtitle[_ngcontent-%COMP%] {\n  margin-top: 4px;\n  color: #64748b;\n  font-size: 12px;\n}\n.entity-state-card-values[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 6px;\n}\n.entity-state-value-chip[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  padding: 4px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 12px;\n  font-weight: 600;\n}\n.entity-state-value-chip.kind-initial[_ngcontent-%COMP%] {\n  border-color: #67e8f9;\n  background: #ecfeff;\n  color: #155e75;\n}\n.entity-state-value-chip.kind-terminal[_ngcontent-%COMP%] {\n  border-color: #fda4af;\n  background: #fff1f2;\n  color: #be123c;\n}\n.entity-state-overview-grid[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));\n  gap: 12px;\n}\n.entity-state-field-panel[_ngcontent-%COMP%] {\n  padding: 12px;\n  border: 1px solid #e2e8f0;\n  border-radius: 12px;\n  background:\n    linear-gradient(\n      180deg,\n      #fbfdff 0%,\n      #fff 100%);\n  cursor: pointer;\n  transition:\n    border-color 0.15s ease,\n    box-shadow 0.15s ease,\n    background 0.15s ease;\n}\n.entity-state-field-panel[_ngcontent-%COMP%]:hover {\n  border-color: #bfdbfe;\n  box-shadow: 0 8px 18px rgba(59, 130, 246, 0.08);\n}\n.entity-state-field-panel.active[_ngcontent-%COMP%] {\n  border-color: #3b82f6;\n  background: #fff;\n  box-shadow: 0 10px 22px rgba(59, 130, 246, 0.12);\n}\n.entity-state-field-panel-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 10px;\n  margin-bottom: 10px;\n}\n.entity-state-field-panel-title[_ngcontent-%COMP%] {\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 700;\n}\n.entity-state-field-panel-meta[_ngcontent-%COMP%] {\n  margin-top: 4px;\n  color: #64748b;\n  font-size: 11px;\n  line-height: 1.5;\n}\n.entity-state-field-panel-badges[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  flex-wrap: wrap;\n  gap: 6px;\n}\n.entity-state-field-badge[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  padding: 3px 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 999px;\n  background: #f8fafc;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n}\n.entity-state-field-badge.role-primary[_ngcontent-%COMP%] {\n  border-color: #93c5fd;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.entity-state-field-badge.role-secondary[_ngcontent-%COMP%] {\n  border-color: #fdba74;\n  background: #fff7ed;\n  color: #c2410c;\n}\n.entity-state-field-badge.active[_ngcontent-%COMP%] {\n  border-color: #bfdbfe;\n  background: #dbeafe;\n  color: #1d4ed8;\n}\n.entity-state-field-empty[_ngcontent-%COMP%] {\n  margin-top: 8px;\n}\n.entity-state-graph[_ngcontent-%COMP%] {\n  width: 100%;\n  overflow: auto;\n  padding-bottom: 4px;\n}\n.entity-state-zoom-shell[_ngcontent-%COMP%] {\n  position: relative;\n}\n.entity-state-zoom-target[_ngcontent-%COMP%] {\n  position: relative;\n}\n.entity-state-canvas[_ngcontent-%COMP%] {\n  position: relative;\n  min-width: 100%;\n}\n.entity-state-board[_ngcontent-%COMP%] {\n  position: relative;\n  pointer-events: auto;\n  overflow: visible;\n}\n.entity-state-svg[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 1;\n  left: 0;\n  top: 0;\n  overflow: visible;\n  pointer-events: none;\n}\n.entity-state-svg[_ngcontent-%COMP%]   marker[_ngcontent-%COMP%]   path[_ngcontent-%COMP%] {\n  fill: #64748b;\n}\n.entity-state-link[_ngcontent-%COMP%] {\n  fill: none;\n  stroke: #94a3b8;\n  stroke-linecap: round;\n  stroke-width: 1.8;\n  cursor: pointer;\n  pointer-events: stroke;\n}\n.entity-state-link.selected[_ngcontent-%COMP%] {\n  stroke: #2563eb;\n  stroke-width: 2.5;\n}\n.entity-state-link-label-group[_ngcontent-%COMP%] {\n  cursor: move;\n  pointer-events: auto;\n}\n.entity-state-link-label-group[_ngcontent-%COMP%]   rect[_ngcontent-%COMP%] {\n  fill: rgba(255, 255, 255, 0.01);\n  stroke: none;\n  pointer-events: all;\n}\n.entity-state-link-label[_ngcontent-%COMP%] {\n  fill: #475569;\n  paint-order: stroke;\n  stroke: rgba(255, 255, 255, 0.92);\n  stroke-linejoin: round;\n  stroke-width: 4px;\n  font-size: 12px;\n  font-weight: 600;\n}\n.entity-state-node[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 3;\n  display: grid;\n  place-items: center;\n  box-sizing: border-box;\n  padding: 0 12px;\n  border: 1px solid #cbd5e1;\n  border-radius: 12px;\n  background: #f8fafc;\n  color: #334155;\n  box-shadow: 0 4px 12px rgba(15, 23, 42, 0.05);\n  text-align: center;\n  cursor: move;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.entity-state-node.kind-initial[_ngcontent-%COMP%] {\n  border-color: #38bdf8;\n  background: #e0f2fe;\n  color: #0f4c81;\n}\n.entity-state-node.kind-terminal[_ngcontent-%COMP%] {\n  border-color: #fb7185;\n  background: #ffe4e6;\n  color: #9f1239;\n}\n.entity-state-node-label[_ngcontent-%COMP%] {\n  max-width: 100%;\n  overflow: hidden;\n  font-size: 11px;\n  font-weight: 700;\n  line-height: 1.25;\n  text-align: center;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-state-marker[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 3;\n  box-sizing: border-box;\n  border-radius: 50%;\n  cursor: move;\n  pointer-events: auto;\n}\n.entity-state-start-dot[_ngcontent-%COMP%] {\n  width: 12px;\n  height: 12px;\n  background: #0f172a;\n}\n.entity-state-end-dot-outer[_ngcontent-%COMP%] {\n  width: 20px;\n  height: 20px;\n  border: 2.4px solid #0f172a;\n  background: #fff;\n}\n.entity-state-end-dot-outer[_ngcontent-%COMP%]::after {\n  content: "";\n  position: absolute;\n  left: 50%;\n  top: 50%;\n  width: 10px;\n  height: 10px;\n  transform: translate(-50%, -50%);\n  border-radius: 50%;\n  background: #0f172a;\n}\n.entity-design-drawer[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 20;\n  top: 0;\n  right: 0;\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  min-width: 420px;\n  min-height: 0;\n  overflow: hidden;\n  border: none;\n  border-left: 1px solid var(--border, #d7e2ef);\n  border-radius: 0;\n  background: #fff;\n  box-shadow: -6px 0 24px rgba(0, 0, 0, 0.1);\n}\n.state-editor-drawer[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 20;\n  top: 0;\n  right: 0;\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  min-width: 520px;\n  overflow: hidden;\n  border: none;\n  border-left: 1px solid var(--border, #d7e2ef);\n  border-radius: 0;\n  background: #fff;\n  box-shadow: -6px 0 24px rgba(0, 0, 0, 0.08);\n}\n.state-editor-drawer-resize[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 5;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 6px;\n  cursor: ew-resize;\n}\n.state-editor-drawer-resize[_ngcontent-%COMP%]::after {\n  content: none;\n}\n.state-editor-body[_ngcontent-%COMP%] {\n  background: #fff;\n  padding: 12px 14px;\n  color: #1e293b;\n  font-size: 12px;\n}\n.state-editor-drawer[_ngcontent-%COMP%]   .entity-state-editor[_ngcontent-%COMP%] {\n  width: 100%;\n  padding: 0;\n  background: transparent;\n}\n.entity-state-editor-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n  margin-bottom: 10px;\n  padding-bottom: 10px;\n}\n.entity-state-editor-head[_ngcontent-%COMP%]   h4[_ngcontent-%COMP%] {\n  margin: 0 0 4px;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 800;\n}\n.entity-state-editor-hint[_ngcontent-%COMP%] {\n  margin: 0;\n  color: #64748b;\n  font-size: 11px;\n  line-height: 1.55;\n}\n.entity-state-editor-actions[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  flex-wrap: wrap;\n  gap: 8px;\n}\n.entity-state-config-row[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(180px, 240px) minmax(0, 1fr);\n  gap: 12px;\n  margin-bottom: 10px;\n}\n.entity-state-values-readonly[_ngcontent-%COMP%] {\n  width: 100%;\n  min-height: 32px;\n  padding: 7px 10px;\n  border: 1px dashed var(--border, #d7e2ef);\n  border-radius: 8px;\n  background: #f8fafc;\n  color: #64748b;\n  font-size: 12px;\n  line-height: 1.45;\n}\n.entity-state-kind-editor[_ngcontent-%COMP%] {\n  margin-bottom: 10px;\n  padding: 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 10px;\n  background: #f8fafc;\n}\n.entity-state-kind-head[_ngcontent-%COMP%]   h5[_ngcontent-%COMP%] {\n  margin: 0 0 4px;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n}\n.entity-transition-list[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.entity-transition-row[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 24px minmax(0, 1fr) minmax(160px, 1.1fr) 116px;\n  gap: 6px;\n  align-items: center;\n  padding: 8px 10px;\n  border: 1px solid #e2e8f0;\n  border-radius: 9px;\n  background: #fbfdff;\n}\n.entity-transition-row[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  min-width: 0;\n  min-height: 30px;\n  padding: 5px 8px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.entity-transition-arrow[_ngcontent-%COMP%] {\n  color: #64748b;\n  font-weight: 700;\n  text-align: center;\n}\n.entity-transition-actions[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 4px;\n}\n.transition-action[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 24px;\n  height: 24px;\n  border: 1px solid #dbe5f0;\n  background: #fff;\n  cursor: pointer;\n}\n.transition-action[_ngcontent-%COMP%]:hover {\n  border-color: #93c5fd;\n  color: #2563eb;\n}\n.transition-action[_ngcontent-%COMP%]:disabled {\n  opacity: 0.4;\n  cursor: not-allowed;\n}\n.entity-design-drawer-resize[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 5;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 5px;\n  cursor: ew-resize;\n  background: transparent;\n  transition: background 0.15s;\n}\n.entity-design-drawer-resize[_ngcontent-%COMP%]::after {\n  content: none;\n}\n.entity-design-drawer-resize[_ngcontent-%COMP%]:hover {\n  background: #3b82f6;\n  opacity: 0.5;\n}\n.entity-design-drawer-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  flex-shrink: 0;\n  gap: 8px;\n  padding: 10px 14px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n  background: #fff;\n}\n.drawer-crumb[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  flex: 1;\n  gap: 6px;\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 600;\n}\n.drawer-crumb[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.drawer-actions[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  flex-shrink: 0;\n  gap: 6px;\n}\n.drawer-close[_ngcontent-%COMP%] {\n  padding: 2px 7px;\n  border: none;\n  border-radius: 4px;\n  background: none;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 16px;\n  line-height: 1;\n}\n.drawer-close[_ngcontent-%COMP%]:hover {\n  background: #f1f5f9;\n  color: #0f172a;\n}\n.entity-design-drawer-body[_ngcontent-%COMP%] {\n  flex: 1 1 auto;\n  min-height: 0;\n  overflow: auto;\n  padding: 14px 16px;\n}\n.form-grid[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: repeat(2, minmax(0, 1fr));\n  gap: 12px;\n}\n.entity-form-grid[_ngcontent-%COMP%] {\n  margin-bottom: 16px;\n}\n.field-group[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.field-group[_ngcontent-%COMP%]   label[_ngcontent-%COMP%] {\n  color: #475569;\n  font-size: 12px;\n  font-weight: 600;\n}\n.field-group[_ngcontent-%COMP%]   input[_ngcontent-%COMP%], \n.field-group[_ngcontent-%COMP%]   select[_ngcontent-%COMP%] {\n  min-height: 32px;\n  padding: 6px 9px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 13px;\n}\n.entity-edit-section[_ngcontent-%COMP%] {\n  margin-top: 14px;\n}\n.entity-edit-section[_ngcontent-%COMP%]   h4[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  margin: 0 0 8px;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 700;\n}\n.entity-relation-debug-toggle[_ngcontent-%COMP%] {\n  margin-left: auto;\n}\n.entity-section-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n}\n.entity-shortcut-hint[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  display: inline-block;\n  padding: 0 3px;\n  border-radius: 4px;\n  background: #eff6ff;\n  color: var(--accent, #2563eb);\n  font-weight: 800;\n}\n.task-ref-list[_ngcontent-%COMP%] {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  padding-top: 4px;\n}\n.task-ref[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  min-height: 28px;\n  padding: 3px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 4px;\n  background: #eff6ff;\n  color: var(--accent, #2563eb);\n  font-size: 12px;\n  font-weight: 600;\n}\n.field-table[_ngcontent-%COMP%] {\n  width: 100%;\n  margin-bottom: 8px;\n  border-collapse: collapse;\n}\n.field-table[_ngcontent-%COMP%]   th[_ngcontent-%COMP%] {\n  padding: 6px 8px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n  background: #eef2f7;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 600;\n  text-align: left;\n}\n.field-table[_ngcontent-%COMP%]   td[_ngcontent-%COMP%] {\n  padding: 4px 6px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n}\n.field-table[_ngcontent-%COMP%]   tr[_ngcontent-%COMP%]:hover   td[_ngcontent-%COMP%] {\n  background: #f8fafc;\n}\n.field-table[_ngcontent-%COMP%]   input[type=text][_ngcontent-%COMP%], \n.field-table[_ngcontent-%COMP%]   select[_ngcontent-%COMP%] {\n  width: 100%;\n  box-sizing: border-box;\n  min-height: 28px;\n  padding: 4px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.field-table[_ngcontent-%COMP%]   input[type=checkbox][_ngcontent-%COMP%] {\n  width: 15px;\n  height: 15px;\n  cursor: pointer;\n}\n.field-td-name[_ngcontent-%COMP%] {\n  width: 120px;\n}\n.field-td-type[_ngcontent-%COMP%] {\n  width: 80px;\n}\n.field-td-note[_ngcontent-%COMP%] {\n  min-width: 160px;\n}\n.field-td-note[_ngcontent-%COMP%]   textarea[_ngcontent-%COMP%] {\n  width: 100%;\n  box-sizing: border-box;\n  min-height: 28px;\n  max-height: 28px;\n  resize: none;\n  overflow: hidden;\n  padding: 4px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fafbfc;\n  color: #0f172a;\n  font-size: 12px;\n  line-height: 1.5;\n}\n.field-td-note[_ngcontent-%COMP%]   textarea[style*=height][_ngcontent-%COMP%] {\n  max-height: none;\n  overflow: auto;\n}\n.field-status-cell[_ngcontent-%COMP%] {\n  width: 94px;\n  text-align: center;\n}\n.field-status-role-wrap[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  padding: 2px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 999px;\n  background: #f8fafc;\n}\n.field-status-role-select[_ngcontent-%COMP%] {\n  min-width: 62px;\n  height: 26px;\n  padding: 0 24px 0 10px;\n  border: none;\n  border-radius: 999px;\n  background: transparent;\n  color: #475569;\n  font-size: 12px;\n  font-weight: 700;\n  appearance: none;\n}\n.field-status-role-select[_ngcontent-%COMP%]:focus {\n  outline: none;\n}\n.field-status-role-wrap[_ngcontent-%COMP%]:focus-within {\n  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.14);\n}\n.field-status-role-wrap-primary[_ngcontent-%COMP%] {\n  border-color: #93c5fd;\n  background: #eff6ff;\n}\n.field-status-role-select-primary[_ngcontent-%COMP%] {\n  color: #1d4ed8;\n}\n.field-status-role-wrap-secondary[_ngcontent-%COMP%] {\n  border-color: #fdba74;\n  background: #fff7ed;\n}\n.field-status-role-select-secondary[_ngcontent-%COMP%] {\n  color: #c2410c;\n}\n.field-actions-cell[_ngcontent-%COMP%] {\n  width: 116px;\n  white-space: nowrap;\n  text-align: right;\n}\n.field-actions[_ngcontent-%COMP%], \n.rel-actions[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  flex-wrap: nowrap;\n  gap: 4px;\n}\n.field-action[_ngcontent-%COMP%], \n.rel-action[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  flex-shrink: 0;\n  width: 24px;\n  height: 24px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 12px;\n  line-height: 1;\n}\n.field-action[_ngcontent-%COMP%]:hover, \n.rel-action[_ngcontent-%COMP%]:hover {\n  border-color: #bfdbfe;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.field-action[_ngcontent-%COMP%]:disabled, \n.rel-action[_ngcontent-%COMP%]:disabled {\n  opacity: 0.45;\n  cursor: not-allowed;\n  background: #f8fafc;\n  color: #94a3b8;\n}\n.field-del[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  flex-shrink: 0;\n  width: 24px;\n  height: 24px;\n  padding: 0;\n  border: none;\n  border-radius: 4px;\n  background: none;\n  color: #64748b;\n  cursor: pointer;\n}\n.field-del[_ngcontent-%COMP%]:hover {\n  background: #fee2e2;\n  color: #dc2626;\n}\n.rel-scope-tip[_ngcontent-%COMP%], \n.no-refs[_ngcontent-%COMP%] {\n  margin: 0 0 8px;\n  color: #64748b;\n  font-size: 12px;\n}\n.no-refs[_ngcontent-%COMP%] {\n  font-style: italic;\n}\n.entity-relation-debug[_ngcontent-%COMP%] {\n  max-height: 220px;\n  margin: 8px 0 10px;\n  overflow: auto;\n  border: 1px solid #dbe7f3;\n  border-radius: 6px;\n  background: #f8fafc;\n  font-size: 11px;\n}\n.entity-relation-debug-title[_ngcontent-%COMP%] {\n  position: sticky;\n  top: 0;\n  z-index: 1;\n  padding: 6px 8px;\n  border-bottom: 1px solid #dbe7f3;\n  background: #eef5ff;\n  color: #17406d;\n  font-weight: 800;\n}\n.entity-relation-debug[_ngcontent-%COMP%]   table[_ngcontent-%COMP%] {\n  width: 100%;\n  min-width: 980px;\n  border-collapse: collapse;\n}\n.entity-relation-debug[_ngcontent-%COMP%]   th[_ngcontent-%COMP%], \n.entity-relation-debug[_ngcontent-%COMP%]   td[_ngcontent-%COMP%] {\n  padding: 5px 6px;\n  border-bottom: 1px solid #e5edf6;\n  color: #334155;\n  text-align: left;\n  vertical-align: top;\n}\n.entity-relation-debug[_ngcontent-%COMP%]   th[_ngcontent-%COMP%] {\n  background: #f1f5f9;\n  color: #0f172a;\n  font-weight: 800;\n  white-space: nowrap;\n}\n.entity-relation-debug[_ngcontent-%COMP%]   code[_ngcontent-%COMP%] {\n  color: #475569;\n  white-space: pre-wrap;\n  word-break: break-all;\n}\n.entity-relation-debug[_ngcontent-%COMP%]   .visible-row[_ngcontent-%COMP%]   td[_ngcontent-%COMP%] {\n  background: #ecfdf5;\n}\n.rel-row[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(110px, 1fr) 76px minmax(110px, 1fr) minmax(120px, 1fr) auto;\n  gap: 6px;\n  align-items: center;\n  margin-bottom: 6px;\n}\n.rel-row[_ngcontent-%COMP%]   input[_ngcontent-%COMP%], \n.rel-row[_ngcontent-%COMP%]   select[_ngcontent-%COMP%] {\n  min-width: 0;\n  min-height: 30px;\n  padding: 5px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.entity-state-kind-list[_ngcontent-%COMP%] {\n  display: grid;\n  gap: 6px;\n}\n.entity-state-kind-row[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 110px;\n  gap: 6px;\n  align-items: center;\n  padding: 6px 8px;\n  border: 1px solid #e5edf6;\n  border-radius: 8px;\n  background: #fff;\n}\n.entity-state-kind-row[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-state-kind-row[_ngcontent-%COMP%]   select[_ngcontent-%COMP%], \n.entity-transition-row[_ngcontent-%COMP%]   select[_ngcontent-%COMP%] {\n  min-width: 0;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  padding: 5px 8px;\n}\n.entity-field-note[_ngcontent-%COMP%] {\n  width: 100%;\n  box-sizing: border-box;\n}\n.entity-muted[_ngcontent-%COMP%], \n.entity-empty[_ngcontent-%COMP%] {\n  margin: 0;\n  color: #64748b;\n  font-size: 13px;\n}\n.entity-delete-btn[_ngcontent-%COMP%] {\n  justify-self: stretch;\n}\n/*# sourceMappingURL=entity-design-workbench.component.css.map */'] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(EntityDesignWorkbenchComponent, [{
    type: Component,
    args: [{ selector: "app-entity-design-workbench", standalone: true, imports: [CommonModule], template: `<section class="entity-design" data-testid="entity-design-angular">
  <div class="entity-design-toolbar">
    <div class="data-view-switch" role="tablist" aria-label="\u5B9E\u4F53\u8BBE\u8BA1\u89C6\u56FE\u5207\u6362">
      <button class="seg-btn" [class.active]="view() === 'relation'" type="button" data-testid="entity-design-switch-relation" (click)="setView('relation')">\u5173\u7CFB\u56FE</button>
      <button class="seg-btn" [class.active]="view() === 'state'" type="button" data-testid="entity-design-switch-state" (click)="setView('state')">\u72B6\u6001\u56FE</button>
    </div>
    @if (view() === 'state') {
      <span class="live-diagram-hint entity-shortcut-hint" data-testid="entity-state-shortcut-hint">
        <strong>\u62D6\u62FD\u72B6\u6001\u8282\u70B9</strong>\u8C03\u6574\u4F4D\u7F6E \xB7 <strong>\u62D6\u62FD\u8D77\u6B62\u5706\u70B9</strong>\u8C03\u6574\u5F00\u59CB/\u7ED3\u675F\u6807\u8BB0 \xB7 <strong>\u70B9\u51FB\u8FDE\u7EBF</strong>\u9009\u4E2D \xB7 <strong>\u62D6\u62FD\u8FDE\u7EBF\u6BB5</strong>\u8C03\u6574\u8DEF\u5F84 \xB7 <strong>\u62D6\u62FD\u6587\u5B57</strong>\u8C03\u6574\u8BF4\u660E \xB7 <strong>Ctrl+\u6EDA\u8F6E</strong>\u7F29\u653E
      </span>
    } @else {
      <span class="live-diagram-hint entity-shortcut-hint" data-testid="entity-relation-shortcut-hint">
        <strong>\u62D6\u62FD</strong>\u79FB\u52A8\u8282\u70B9 \xB7 \u70B9\u51FB\u5B9E\u4F53\u5207\u6362\u7F16\u8F91\u5BF9\u8C61 \xB7 <strong>\u53CC\u51FB\u5B9E\u4F53</strong>\u8FDB\u5165\u7F16\u8F91 \xB7 <strong>Ctrl+A</strong>\u5168\u9009\u5B9E\u4F53 \xB7 <strong>Ctrl+\u70B9\u51FB</strong>\u591A\u9009 \xB7 <strong>Shift+\u5DE6\u952E\u62D6\u62FD</strong>\u6846\u9009 \xB7 <strong>\u70B9\u51FB\u6784\u4EF6/\u7EC4\u4EF6</strong>\u9009\u62E9\u6240\u5C5E\u5168\u90E8\u5B9E\u4F53
      </span>
    }
    @if (view() === 'relation' && canEditRelation()) {
      <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-open-drawer" (click)="openSelectedEntityDrawer()" [disabled]="!selectedEntityId()">\u7F16\u8F91\u5C5E\u6027</button>
      <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-add-entity" (click)="addEntity()">\uFF0B \u65B0\u5EFA\u5B9E\u4F53</button>
      <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-reset-layout" (click)="resetLayout()">\u91CD\u7F6E\u5E03\u5C40</button>
    } @else if (view() === 'state' && canEditState() && selectedEntity(); as stateEntity) {
      <button class="btn btn-outline btn-sm" type="button" data-testid="state-toolbar-add-transition" (click)="addTransition(stateEntity)">\uFF0B \u6DFB\u52A0\u6D41\u8F6C</button>
      <button class="btn btn-outline btn-sm" type="button" data-testid="state-toolbar-reset-routes" (click)="resetAllStateTransitionRoutes(stateEntity)" [disabled]="!selectedStateField()">\u91CD\u7F6E\u8FDE\u7EBF</button>
    }
    @if (view() === 'state') {
      <div class="entity-state-zoom-controls" data-testid="entity-state-zoom-controls">
        <button class="btn btn-ghost-sm" type="button" data-testid="state-zoom-out" (click)="nudgeStateZoom(-0.1)">\uFF0D</button>
        <button class="btn btn-ghost-sm" type="button" data-testid="state-zoom-reset" (click)="resetStateZoom()">{{ stateZoomLabel() }}</button>
        <button class="btn btn-ghost-sm" type="button" data-testid="state-zoom-in" (click)="nudgeStateZoom(0.1)">\uFF0B</button>
      </div>
    }
    @if (showEditorToggle && view() === 'state' && stateEditorOpen()) {
      <button class="btn btn-outline btn-sm editor-toggle active" type="button" data-testid="state-editor-hide" (click)="stateEditorOpen.set(false)">\u5173\u95ED\u7F16\u8F91</button>
    } @else if (showEditorToggle && view() === 'state') {
      <button class="btn btn-outline btn-sm" type="button" data-testid="state-editor-open" (click)="stateEditorOpen.set(true); ensureActiveStateField(selectedEntity())">\u6253\u5F00\u7F16\u8F91</button>
      <button class="entity-visually-hidden" type="button" data-testid="entity-design-editor-open" (click)="stateEditorOpen.set(true); ensureActiveStateField(selectedEntity())">\u6253\u5F00\u7F16\u8F91</button>
    } @else if (showEditorToggle && editorOpen()) {
      <button class="btn btn-outline btn-sm editor-toggle active" type="button" data-testid="entity-design-editor-hide" (click)="editorOpen.set(false)">\u5173\u95ED\u7F16\u8F91</button>
    } @else if (showEditorToggle) {
      <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-editor-open" (click)="editorOpen.set(true)">\u6253\u5F00\u7F16\u8F91</button>
    }
  </div>

  <div class="entity-design-body" [class.has-drawer]="editorOpen() && selectedEntity()" [style.grid-template-columns]="drawerGridColumns()">
    <div class="entity-design-canvas-shell" data-testid="entity-design-canvas-shell" [style.margin-right.px]="relationDrawerOffset()" (wheel)="onRelationWheel($event)">
      @if (view() === 'relation') {
        <div class="entity-relation-canvas ef-canvas" id="ef-canvas-entity-diagram" data-testid="entity-design-relation-view" [ngStyle]="relationCanvasSize()">
          <div class="entity-board ef-board" id="ef-board-entity-diagram" [class.is-editing]="canEditRelation()" [attr.data-export-graph-id]="exportGraphId || null" data-export-graph-ready="true" [ngStyle]="boardSize()" (mousedown)="startSelectionBox($event)" (click)="clearSelection()">
            @for (frame of componentFrames(); track frame.key) {
              <div class="entity-component-frame ef-component-frame" [ngStyle]="frameStyle(frame)" (mousedown)="startFrameDrag(frame, $event)" [title]="'\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 ' + (frame.entityIds?.length || 0) + ' \u4E2A\u5B9E\u4F53'"></div>
              <div class="entity-component-title ef-component-title" [title]="'\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 ' + (frame.entityIds?.length || 0) + ' \u4E2A\u5B9E\u4F53'" [style.left.px]="frame.left + 18" [style.top.px]="frame.top + 10" (mousedown)="startFrameDrag(frame, $event)">{{ frame.label }}</div>
            }
            @for (frame of groupFrames(); track frame.key) {
              <div class="entity-group-frame ef-group-frame" [ngStyle]="frameStyle(frame)" (mousedown)="startFrameDrag(frame, $event)" [title]="'\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 ' + (frame.entityIds?.length || 0) + ' \u4E2A\u5B9E\u4F53'">
                <div class="entity-group-title ef-group-title" [title]="'\u62D6\u66F3\u79FB\u52A8\u5168\u90E8 ' + (frame.entityIds?.length || 0) + ' \u4E2A\u5B9E\u4F53'" (mousedown)="startFrameDrag(frame, $event)">{{ frame.label }}</div>
              </div>
            }
            <svg class="entity-rel-svg ef-svg" id="ef-svg-entity-diagram" [attr.width]="boardWidth()" [attr.height]="boardHeight()" aria-hidden="true">
              <defs>
                <marker id="entity-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
                  <path d="M 0 0 L 10 5 L 0 10 z"></path>
                </marker>
              </defs>
              @for (line of relationLines(); track line.from.id + '-' + line.to.id + '-' + $index) {
                <path class="entity-rel-line ef-rel" [class.is-focus]="line.focus" [class.is-muted]="line.muted" [class.ef-muted]="line.muted" [class.ef-focus]="line.focus" [class.is-dashed]="line.dashed" [attr.d]="line.path" [attr.stroke]="line.color" [attr.stroke-width]="line.width" [attr.stroke-opacity]="line.opacity" marker-end="url(#entity-arrow)" (click)="selectRelationLine(line, $event)"></path>
              }
            </svg>

            @for (line of relationLines(); track line.from.id + '-' + line.to.id + '-label-' + $index) {
              <span class="entity-rel-label" [ngStyle]="relationLabelStyle(line)">{{ line.label }}</span>
            }

            @for (node of nodes(); track node.id) {
              <button class="entity-node ef-node ef-draggable" [class.is-selected]="isSelected(node.entity)" [class.ef-selected]="isSelected(node.entity)" [class.is-focus]="selectedEntityId() === node.id" [class.ef-focus]="selectedEntityId() === node.id" [class.ef-muted]="selectedEntityId() && selectedEntityId() !== node.id && !isSelected(node.entity)" type="button" data-testid="entity-design-node" [attr.data-id]="node.id" [ngStyle]="nodeStyle(node)" (mousedown)="startEntityDrag(node.entity, $event)" (click)="$event.stopPropagation(); selectEntity(node.entity, $event)" (dblclick)="openEntityDrawer(node.entity, $event)">
                <strong class="ef-nname">{{ node.entity.name || '\u672A\u547D\u540D\u5B9E\u4F53' }}</strong>
              </button>
            }
            @if (selectionBox()) {
              <div class="entity-selection-box" [ngStyle]="selectionBoxStyle()"></div>
            }
          </div>
        </div>
      } @else {
        <div class="entity-state-workbench" data-testid="entity-design-state-view">
          @if (selectedEntity(); as entity) {
            <aside class="entity-state-browser">
              @for (group of stateEntityGroups(); track group.name) {
                <div class="entity-state-group">
                  <div class="entity-state-group-title">{{ group.name }}</div>
                  <div class="entity-state-group-list">
                    @for (item of group.items; track entityId(item)) {
                      <button class="entity-state-chip" [class.active]="entityId(item) === entityId(entity)" type="button" (click)="selectEntity(item)">
                        {{ item.name || '\u672A\u547D\u540D\u5B9E\u4F53' }}
                      </button>
                    }
                  </div>
                </div>
              }
            </aside>

            <div class="entity-state-stage">
              <div class="entity-state-main-shell" [ngStyle]="stateMainShellStyle()" (wheel)="onStateDiagramWheel($event)">
                <div class="entity-state-main">
                  <div class="entity-state-card" data-testid="entity-state-diagram">
                    <div class="entity-state-card-head">
                      <div>
                        <div class="entity-state-card-title">{{ entity.name || '\u672A\u547D\u540D\u5B9E\u4F53' }}</div>
                        <div class="entity-state-card-subtitle">\u72B6\u6001\u603B\u89C8 \xB7 \u5F53\u524D\u7F16\u8F91\u5B57\u6BB5\uFF1A{{ activeStateFieldLabel() }}</div>
                      </div>
                      <div class="entity-state-card-summary">
                        @for (panel of statePanels(); track panel.name || $index) {
                          <button class="entity-state-field-chip" [class.active]="panel.active" type="button" [attr.data-testid]="'entity-state-overview-field-' + $index" (click)="setStateField(panel.name)">
                            {{ panel.name || '\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5' }}
                          </button>
                        }
                      </div>
                    </div>

                    <div class="entity-state-overview-grid">
                      @for (panel of statePanels(); track panel.name || $index) {
                        <section class="entity-state-field-panel" data-testid="entity-state-overview-field" [class.active]="panel.active" [class.role-primary]="panel.role === 'primary'" [class.role-secondary]="panel.role === 'secondary'" (click)="setStateField(panel.name)">
                          <div class="entity-state-field-panel-head">
                            <div>
                              <div class="entity-state-field-panel-title">{{ panel.name || '\u672A\u547D\u540D\u72B6\u6001\u5B57\u6BB5' }}</div>
                              <div class="entity-state-field-panel-meta">{{ panel.values.length }} \u4E2A\u72B6\u6001\u503C</div>
                            </div>
                            <div class="entity-state-field-panel-badges">
                              <span class="entity-state-field-badge" [class.role-primary]="panel.role === 'primary'" [class.role-secondary]="panel.role === 'secondary'">{{ stateRoleLabel(panel.role) }}</span>
                              @if (panel.active) {
                                <span class="entity-state-field-badge active">\u7F16\u8F91\u4E2D</span>
                              }
                            </div>
                          </div>

                          <div class="entity-state-card-values">
                            @for (node of panel.board.nodes; track node.name) {
                              <span class="entity-state-value-chip" [class.kind-initial]="node.kind === 'initial'" [class.kind-terminal]="node.kind === 'terminal'">{{ node.name }} \xB7 {{ stateNodeKindLabel(node.kind) }}</span>
                            }
                          </div>

                          <div class="entity-state-graph" data-testid="entity-state-graph-canvas" [class.readonly]="!canEditState() || !panel.active" [attr.data-export-graph-id]="panel.active ? (exportGraphId || null) : null" data-export-graph-ready="true">
                            <div class="entity-state-zoom-shell" data-testid="entity-state-zoom-shell" [ngStyle]="stateZoomStyle(panel.board)">
                              <div class="entity-state-zoom-target" data-testid="entity-state-zoom-target" [ngStyle]="stateZoomTargetStyle(panel.board)">
                                <div class="entity-state-canvas" [ngStyle]="stateBoardStyle(panel.board)">
                                  <svg class="entity-state-svg" [attr.width]="panel.board.width" [attr.height]="panel.board.height" [attr.viewBox]="'0 0 ' + panel.board.width + ' ' + panel.board.height" aria-hidden="true">
                                    <defs>
                                      <marker id="entity-state-arrow" markerWidth="10" markerHeight="10" refX="8" refY="4" orient="auto">
                                        <path d="M 0 0 L 8 4 L 0 8 z"></path>
                                      </marker>
                                    </defs>
                                    @for (link of panel.board.markerLinks; track link.path) {
                                      <path class="entity-state-link entity-state-link-anchor" [attr.d]="link.path" [attr.marker-end]="link.terminal ? null : 'url(#entity-state-arrow)'"></path>
                                    }
                                    @for (transition of panel.board.transitions; track transition.label + '-' + $index) {
                                      <path class="entity-state-link" data-testid="entity-state-graph-link" [class.selected]="transition.selected" [attr.d]="transition.path" [attr.data-state-action]="transition.label" marker-end="url(#entity-state-arrow)" (click)="selectTransition(transition.index, $event)"></path>
                                      <g class="entity-state-link-label-group" data-testid="entity-state-link-label-group" [attr.transform]="'rotate(' + transition.labelAngle + ' ' + transition.labelX + ' ' + transition.labelY + ')'" (mousedown)="startStateLabelDrag(transition, $event)">
                                        <rect class="entity-state-link-label-box" [attr.x]="transition.labelX - transition.labelWidth / 2" [attr.y]="transition.labelY - transition.labelHeight / 2" [attr.width]="transition.labelWidth" [attr.height]="transition.labelHeight" rx="10"></rect>
                                        <text class="entity-state-link-label" [attr.x]="transition.labelX" [attr.y]="transition.labelY" text-anchor="middle" dominant-baseline="middle">{{ transition.label }}</text>
                                      </g>
                                    }
                                  </svg>
                                  <div class="entity-state-board" [ngStyle]="stateBoardStyle(panel.board)">
                                    @if (canEditState()) {
                                      @for (transition of panel.board.transitions; track transition.label + '-hitbox-' + $index) {
                                        @for (_point of transition.points.slice(0, -1); track $index) {
                                          <div class="entity-state-link-route-hitbox" data-testid="entity-state-link-route-hitbox" style="position:absolute;z-index:3;cursor:move" [ngStyle]="stateRouteHitboxStyle(transition, $index)" (mousedown)="startStateRouteDrag(transition, $index, $event)"></div>
                                        }
                                        <button class="entity-state-link-endpoint-handle entity-state-link-endpoint-from" type="button" data-testid="entity-state-link-endpoint-handle" style="position:absolute;z-index:6;width:12px;height:12px;border:2px solid #2563eb;border-radius:50%;background:#fff" [ngStyle]="stateEndpointHandleStyle(transition, 'from')" (mousedown)="selectTransition(transition.index, $event)" title="\u8C03\u6574\u8D77\u70B9\u951A\u70B9"></button>
                                        <button class="entity-state-link-endpoint-handle entity-state-link-endpoint-to" type="button" data-testid="entity-state-link-endpoint-handle" style="position:absolute;z-index:6;width:12px;height:12px;border:2px solid #2563eb;border-radius:50%;background:#fff" [ngStyle]="stateEndpointHandleStyle(transition, 'to')" (mousedown)="selectTransition(transition.index, $event)" title="\u8C03\u6574\u7EC8\u70B9\u951A\u70B9"></button>
                                      }
                                    }
                                    @for (node of panel.board.nodes; track node.name; let nodeIndex = $index) {
                                      @if (node.marker?.kind === 'initial') {
                                        <span class="entity-state-marker entity-state-start-dot" data-testid="entity-state-start-dot" [ngStyle]="stateMarkerStyle(node.marker)" (mousedown)="startStateMarkerDrag(node, $event)"></span>
                                      }
                                      <div class="entity-state-node" [class.kind-initial]="node.kind === 'initial'" [class.kind-terminal]="node.kind === 'terminal'" [attr.data-testid]="'entity-state-node-' + nodeIndex" [ngStyle]="stateNodeStyle(node)" (mousedown)="startStateNodeDrag(node, $event)">
                                        <span class="entity-state-node-label">{{ node.name }}</span>
                                      </div>
                                      @if (node.marker?.kind === 'terminal') {
                                        <span class="entity-state-marker entity-state-end-dot-outer" data-testid="entity-state-end-dot" [ngStyle]="stateMarkerStyle(node.marker)" (mousedown)="startStateMarkerDrag(node, $event)"></span>
                                      }
                                    }
                                    @if (!panel.board.nodes.length) {
                                      <div class="entity-empty" data-testid="entity-state-empty">\u6682\u65E0\u72B6\u6001\u503C\u3002</div>
                                    }
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        </section>
                      } @empty {
                        <div class="diag-empty" data-testid="entity-state-empty">
                          \u5F53\u524D\u5B9E\u4F53\u8FD8\u672A\u5B9A\u4E49\u72B6\u6001\u5B57\u6BB5\uFF0C\u8BF7\u5148\u5728\u5B57\u6BB5\u8868\u4E2D\u6807\u8BB0\u4E3B\u72B6\u6001\u6216\u5B50\u72B6\u6001\uFF0C\u518D\u5728\u5B57\u6BB5\u89C4\u5219\u4E2D\u586B\u5199\u72B6\u6001\u503C\u3002
                        </div>
                      }
                    </div>
                  </div>
                </div>
              </div>
            </div>
          } @else {
            <div class="entity-empty">\u6682\u65E0\u5B9E\u4F53\uFF0C\u70B9\u51FB\u201C\u65B0\u5EFA\u5B9E\u4F53\u201D\u5F00\u59CB\u8BBE\u8BA1\u3002</div>
          }
        </div>
      }
    </div>


    @if (view() === 'relation' && editorOpen() && selectedEntity(); as entity) {
      <aside class="entity-design-drawer entity-drawer open" data-testid="entity-design-drawer" [style.width.px]="drawerWidth()">
        <div class="drawer-resize-handle entity-design-drawer-resize" data-testid="entity-drawer-resize-handle" (mousedown)="startDrawerResize($event)"></div>
        <div class="drawer-head entity-design-drawer-head">
          <div class="drawer-crumb">
            <span>{{ entity.name || '\u672A\u547D\u540D' }}</span>
          </div>
          <div class="drawer-actions">
            <button class="btn btn-outline btn-sm" type="button" data-testid="entity-duplicate-button" (click)="duplicateEntity(entity)">\u590D\u5236\u5B9E\u4F53</button>
            <button class="btn btn-danger btn-sm" type="button" data-testid="entity-design-delete" (click)="removeEntity(entity)">\u5220\u9664</button>
            <button class="drawer-close" type="button" data-testid="entity-design-drawer-close" (click)="editorOpen.set(false)" title="\u5173\u95ED\u7F16\u8F91">\u2715</button>
          </div>
        </div>
        <div class="entity-design-drawer-body drawer-body">
          <div class="form-grid entity-form-grid">
            <div class="field-group">
              <label>\u5B9E\u4F53\u540D\u79F0</label>
              <input type="text" data-testid="entity-design-name" [value]="entity.name || ''" (input)="setEntityName(entity, $any($event.target).value)">
            </div>
            <div class="field-group">
              <label>\u6240\u5C5E\u4E1A\u52A1\u6784\u4EF6</label>
              <select data-testid="entity-design-construct" [attr.data-selected-construct]="entityConstructValue(entity)" [value]="entityConstructValue(entity)" (change)="setEntityConstruct(entity, $any($event.target).value)">
                <option value="">\u672A\u5206\u7EC4\u6784\u4EF6</option>
                @for (construct of constructs(); track constructId(construct)) {
                  <option [value]="constructId(construct)" [selected]="entityConstructValue(entity) === constructId(construct)">{{ constructOptionLabel(construct) }}</option>
                }
              </select>
            </div>
            <div class="field-group" style="grid-column:1/-1">
              <label>\u8BF4\u660E</label>
              <input type="text" data-testid="entity-design-note" [value]="entity.note || ''" placeholder="\u7B80\u8981\u8BF4\u660E" (input)="setEntityNote(entity, $any($event.target).value)">
            </div>
          </div>

          @if (entityReferences(entity).length) {
            <section class="form-section entity-edit-section entity-reference-section">
              <h4>\u88AB\u4EE5\u4E0B\u6D41\u7A0B\u8282\u70B9\u5F15\u7528</h4>
              <div class="task-ref-list">
                @for (ref of entityReferences(entity); track ref.processName + '-' + ref.taskName + '-' + $index) {
                  <span class="task-ref" [title]="ref.processName">{{ ref.taskName }}</span>
                }
              </div>
            </section>
          }

          <section class="form-section entity-edit-section">
            <h4>\u5B57\u6BB5 <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-add-field" (click)="addField(entity)">\uFF0B</button></h4>
            @if ((entity.fields || []).length) {
              <table class="field-table">
                <thead>
                  <tr><th>\u5B57\u6BB5\u540D</th><th>\u7C7B\u578B</th><th title="\u4E3B\u952E">\u4E3B\u952E</th><th title="\u4E3B\u72B6\u6001\u5B57\u6BB5">\u72B6\u6001</th><th>\u5B57\u6BB5\u89C4\u5219</th><th>\u64CD\u4F5C</th></tr>
                </thead>
                <tbody>
                  @for (field of entity.fields || []; track fieldId(field, $index); let index = $index) {
                    <tr data-testid="entity-design-field-row">
                      <td class="field-td-name"><input type="text" [attr.data-testid]="'entity-field-name-' + index" [value]="field.name || ''" placeholder="\u5B57\u6BB5\u540D" (input)="setField(field, 'name', $any($event.target).value)"></td>
                      <td class="field-td-type"><select [attr.data-testid]="'entity-field-type-' + index" [attr.data-selected-type]="fieldTypeValue(field)" [value]="fieldTypeValue(field)" (change)="setField(field, 'type', $any($event.target).value)">
                        @for (type of fieldTypes; track type.value) {
                          <option [value]="type.value" [selected]="fieldTypeValue(field) === type.value">{{ type.label }}</option>
                        }
                      </select></td>
                      <td style="text-align:center"><input type="checkbox" [checked]="field.is_key" (change)="setFieldBoolean(field, 'is_key', $any($event.target).checked)"></td>
                      <td class="field-status-cell">
                        <span class="field-status-role-wrap" [class.field-status-role-wrap-none]="!field.status_role" [class.field-status-role-wrap-primary]="field.status_role === 'primary'" [class.field-status-role-wrap-secondary]="field.status_role === 'secondary'">
                          <select class="field-status-role-select" [class.field-status-role-select-none]="!field.status_role" [class.field-status-role-select-primary]="field.status_role === 'primary'" [class.field-status-role-select-secondary]="field.status_role === 'secondary'" [attr.data-testid]="'entity-status-role-' + index" aria-label="\u5B57\u6BB5\u72B6\u6001\u89D2\u8272" [value]="field.status_role || ''" (change)="setFieldStatusRole(field, $any($event.target).value)">
                            <option value="">\u5426</option>
                            <option value="primary">\u4E3B</option>
                            <option value="secondary">\u5B50</option>
                          </select>
                        </span>
                      </td>
                      <td class="field-td-note"><textarea class="auto-resize" rows="1" placeholder="\u5B57\u6BB5\u89C4\u5219" [style.height.px]="fieldRuleHeight(field)" [value]="field.note || field.state_values || ''" (input)="setField(field, 'note', $any($event.target).value)"></textarea></td>
                      <td class="field-actions-cell"><div class="field-actions">
                        <button class="field-action field-add-after" type="button" [attr.data-testid]="'entity-field-add-after-' + index" title="\u5728\u4E0B\u65B9\u63D2\u5165\u5B57\u6BB5" (click)="addField(entity)">+</button>
                        <button class="field-action field-move-up" type="button" [attr.data-testid]="'entity-field-move-up-' + index" title="\u4E0A\u79FB" (click)="moveField(entity, index, -1)" [disabled]="index === 0">\u2191</button>
                        <button class="field-action field-move-down" type="button" [attr.data-testid]="'entity-field-move-down-' + index" title="\u4E0B\u79FB" (click)="moveField(entity, index, 1)" [disabled]="index === (entity.fields || []).length - 1">\u2193</button>
                        <button class="field-del" type="button" [attr.data-testid]="'entity-field-delete-' + index" (click)="removeField(entity, index)">\u2715</button>
                      </div></td>
                    </tr>
                  }
                </tbody>
              </table>
            } @else {
              <p class="no-refs">\u6682\u65E0\u5B57\u6BB5</p>
            }
          </section>

          <section class="form-section entity-edit-section">
            <h4>
              \u5B9E\u4F53\u5173\u7CFB
              <button class="btn btn-outline btn-sm" type="button" data-testid="entity-design-add-relation" (click)="addRelation(entity)" [disabled]="entities().length < 2">\uFF0B</button>
              <button class="btn btn-ghost-sm entity-relation-debug-toggle" type="button" data-testid="entity-relation-debug-toggle" (click)="toggleRelationDebug()">
                {{ relationDebugEnabled() ? '\u5173\u95ED\u8BCA\u65AD' : '\u8BCA\u65AD' }}
              </button>
            </h4>
            <p class="rel-scope-tip">\u4EC5\u663E\u793A\u4E0E\u5F53\u524D\u5B9E\u4F53\u76F4\u63A5\u76F8\u5173\u7684\u5173\u7CFB\uFF0C\u51CF\u5C11\u5168\u5C40\u566A\u97F3\u3002</p>
            @if (relationDebugEnabled()) {
              <div class="entity-relation-debug" data-testid="entity-relation-debug">
                <div class="entity-relation-debug-title">\u5173\u7CFB\u8C03\u8BD5\uFF1A{{ entity.name || entityId(entity) }} / {{ entityId(entity) }}</div>
                <table>
                  <thead>
                    <tr>
                      <th>\u6765\u6E90</th>
                      <th>\u5E8F\u53F7</th>
                      <th>\u5F52\u5C5E</th>
                      <th>\u539F\u59CB\u503C</th>
                      <th>\u89E3\u6790 from</th>
                      <th>\u89E3\u6790 to</th>
                      <th>select from</th>
                      <th>select to</th>
                      <th>\u547D\u4E2D\u5F53\u524D</th>
                    </tr>
                  </thead>
                  <tbody>
                    @for (row of relationDebugRows(entity); track row.scope + '-' + row.index + '-' + row.owner) {
                      <tr [class.visible-row]="row.visible">
                        <td>{{ row.scope }}</td>
                        <td>{{ row.index }}</td>
                        <td>{{ row.owner || '-' }}</td>
                        <td><code>{{ row.raw }}</code></td>
                        <td>{{ row.resolvedFrom || '-' }}</td>
                        <td>{{ row.resolvedTo || '-' }}</td>
                        <td>{{ row.fromSelectValue || '-' }}</td>
                        <td>{{ row.toSelectValue || '-' }}</td>
                        <td>{{ row.visible ? '\u662F' : '\u5426' }}</td>
                      </tr>
                    }
                  </tbody>
                </table>
              </div>
            }
            @for (relation of relationsForEntity(entity); track relation.uid || relation.id || $index; let index = $index) {
              <div class="rel-row entity-relation-row" data-testid="entity-design-relation-row">
                <select [value]="relationFromValue(relation) || entityId(entity)" (change)="setRelation(relation, 'from', $any($event.target).value)">
                  <option value="" disabled>\u8BF7\u9009\u62E9\u5B9E\u4F53</option>
                  @for (source of entities(); track entityId(source)) {
                    <option [value]="entityId(source)" [selected]="entityId(source) === (relationFromValue(relation) || entityId(entity))">{{ source.name || entityId(source) }}</option>
                  }
                </select>
                <select class="rel-type-select" (change)="setRelation(relation, 'type', $any($event.target).value)" [value]="relationTypeValue(relation)">
                  <option value="1:1">1:1</option>
                  <option value="1:N">1:N</option>
                  <option value="N:N">N:N</option>
                </select>
                <select [value]="relationToValue(relation)" (change)="setRelation(relation, 'to', $any($event.target).value)">
                  <option value="" disabled>\u8BF7\u9009\u62E9\u5B9E\u4F53</option>
                  @for (target of entities(); track entityId(target)) {
                    <option [value]="entityId(target)" [selected]="entityId(target) === relationToValue(relation)">{{ target.name || entityId(target) }}</option>
                  }
                </select>
                <input type="text" data-testid="entity-relation-label" class="rel-label-input" placeholder="\u5173\u7CFB\u8BF4\u660E" [value]="relation.label || ''" (input)="setRelation(relation, 'label', $any($event.target).value)">
                <div class="rel-actions">
                  <button class="rel-action rel-add-after" type="button" title="\u5728\u4E0B\u65B9\u63D2\u5165\u5173\u7CFB" (click)="addRelation(entity)">+</button>
                  <button class="rel-action rel-move-up" type="button" title="\u4E0A\u79FB" (click)="moveRelation(entity, index, -1)" [disabled]="index === 0">\u2191</button>
                  <button class="rel-action rel-move-down" type="button" title="\u4E0B\u79FB" (click)="moveRelation(entity, index, 1)" [disabled]="index === relationsForEntity(entity).length - 1">\u2193</button>
                  <button class="field-del" type="button" (click)="removeRelation(entity, index)">\u2715</button>
                </div>
              </div>
            } @empty {
              <p class="no-refs">\u5F53\u524D\u5B9E\u4F53\u6682\u65E0\u5173\u7CFB</p>
            }
          </section>
        </div>
      </aside>
    }
    @if (view() === 'state' && stateEditorOpen() && selectedEntity(); as entity) {
      <aside class="state-editor-drawer" data-testid="state-editor-drawer" [style.width.px]="stateDrawerWidth()">
        <div class="state-editor-drawer-resize" data-testid="state-editor-drawer-resize-handle" (mousedown)="startStateDrawerResize($event)"></div>
        <div class="drawer-head">
          <div class="drawer-crumb">
            <span>\u72B6\u6001\u56FE\u7F16\u8F91</span>
            <span class="dc-sep">/</span>
            <span>{{ selectedStateFieldOptionLabel() }}</span>
          </div>
          <div class="drawer-actions">
            <button class="drawer-close" type="button" data-testid="state-editor-close" (click)="stateEditorOpen.set(false)" title="\u5173\u95ED\u7F16\u8F91">\xD7</button>
          </div>
        </div>
        <div class="drawer-body state-editor-body">
          <div class="entity-state-editor">
            <div class="entity-state-editor-head">
              <div>
                <h4>\u72B6\u6001\u56FE\u7F16\u8F91</h4>
                <p class="entity-state-editor-hint">\u5DE6\u4FA7\u603B\u89C8\u4F1A\u540C\u65F6\u5C55\u793A\u4E3B\u72B6\u6001\u5B57\u6BB5\u548C\u6240\u6709\u5B50\u72B6\u6001\u5B57\u6BB5\uFF1B\u53F3\u4FA7\u53EA\u7F16\u8F91\u5F53\u524D\u9009\u4E2D\u7684\u72B6\u6001\u5B57\u6BB5\uFF0C\u70B9\u51FB\u603B\u89C8\u5757\u53EF\u5FEB\u901F\u5207\u6362\u3002</p>
              </div>
            </div>
            <div class="entity-state-config-row">
              <label class="field-group">
                <span>\u72B6\u6001\u5B57\u6BB5</span>
                <select data-testid="entity-state-field-select" [value]="stateFieldName()" (change)="setStateField($any($event.target).value)">
                  @for (field of stateFields(); track fieldId(field, $index)) {
                    <option [value]="field.name || ''">{{ stateFieldOptionLabel(field) }}</option>
                  }
                </select>
              </label>
              <label class="field-group entity-state-values-field">
                <span>\u72B6\u6001\u503C\u6765\u6E90 <span class="inline-help" tabindex="0" data-tip="\u8BF7\u5728\u5B57\u6BB5\u8868\u7684\u5B57\u6BB5\u89C4\u5219\u4E2D\u586B\u5199\u72B6\u6001\u503C\uFF0C\u5E76\u7528 / \u5206\u9694\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F/\u5F85\u5BA1\u6838/\u5BA1\u6838\u901A\u8FC7/\u5DF2\u4F5C\u5E9F">?</span></span>
                <div class="entity-state-values-readonly" data-testid="entity-state-values-text">{{ stateValues().join(' / ') || '\u8BF7\u5728\u4E0B\u65B9\u201C\u5B57\u6BB5\u89C4\u5219\u201D\u4E2D\u586B\u5199\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F/\u5F85\u5BA1\u6838/\u5BA1\u6838\u901A\u8FC7/\u5DF2\u4F5C\u5E9F' }}</div>
              </label>
            </div>
            <div class="entity-state-kind-editor">
              <div class="entity-state-kind-head">
                <div>
                  <h5>\u72B6\u6001\u8282\u70B9\u5C5E\u6027</h5>
                  <p class="entity-state-editor-hint">\u6BCF\u4E2A\u72B6\u6001\u90FD\u9700\u8981\u6807\u8BB0\u4E3A\u521D\u59CB\u72B6\u6001\u3001\u4E2D\u95F4\u72B6\u6001\u6216\u7ED3\u675F\u72B6\u6001\u3002\u56FE\u4E0A\u4F1A\u6309\u8FD9\u4E2A\u5C5E\u6027\u505A\u7EB5\u5411\u5C55\u5F00\u3002</p>
                </div>
              </div>
              <div class="entity-state-kind-list" data-testid="entity-state-kind-list">
                @for (node of stateBoard().nodes; track node.name; let index = $index) {
                  <label class="entity-state-kind-row">
                    <span class="entity-state-kind-name">{{ node.name }}</span>
                    <select [attr.data-testid]="'entity-state-kind-' + index" [value]="node.kind" (change)="setStateNodeKind(entity, node.name, $any($event.target).value)">
                      <option value="initial">\u521D\u59CB\u72B6\u6001</option>
                      <option value="intermediate">\u4E2D\u95F4\u72B6\u6001</option>
                      <option value="terminal">\u7ED3\u675F\u72B6\u6001</option>
                    </select>
                  </label>
                } @empty {
                  <p class="entity-muted">\u6682\u65E0\u8282\u70B9\u7C7B\u578B\u3002</p>
                }
              </div>
            </div>
            @if (stateFields().length > 1) {
              <p class="entity-state-editor-hint">\u540C\u4E00\u5B9E\u4F53\u7684\u591A\u4E2A\u72B6\u6001\u5B57\u6BB5\u5DF2\u540C\u6B65\u5C55\u793A\u5728\u5DE6\u4FA7\u603B\u89C8\u91CC\uFF1B\u5F53\u524D\u7F16\u8F91\u533A\u53EA\u843D\u5728\u53F3\u4FA7\u9009\u4E2D\u7684\u90A3\u4E00\u4E2A\u5B57\u6BB5\u4E0A\u3002</p>
            }
            <div class="entity-state-transition-head" data-testid="entity-transition-section-title">
              <h5>\u72B6\u6001\u6D41\u8F6C</h5>
              <p class="entity-state-editor-hint">\u7EF4\u62A4\u5F53\u524D\u72B6\u6001\u5B57\u6BB5\u7684\u6D41\u8F6C\u8D77\u70B9\u3001\u7EC8\u70B9\u548C\u8BF4\u660E\u3002</p>
            </div>
            <div class="entity-transition-list">
              @for (item of selectedStateTransitions(entity); track item.transition.uid || item.transition.id || item.index; let rowIndex = $index) {
                <div class="entity-transition-row" data-testid="entity-design-transition-row">
                  <select [attr.data-testid]="'entity-transition-from-' + rowIndex" [value]="item.transition.from || ''" (change)="setTransition(item.transition, 'from', $any($event.target).value)">
                    @for (value of transitionStateOptions(item.transition); track value) {
                      <option [value]="value" [selected]="value === item.transition.from">{{ value }}</option>
                    }
                  </select>
                  <span class="entity-transition-arrow">\u2192</span>
                  <select [attr.data-testid]="'entity-transition-to-' + rowIndex" [value]="item.transition.to || ''" (change)="setTransition(item.transition, 'to', $any($event.target).value)">
                    @for (value of transitionStateOptions(item.transition); track value) {
                      <option [value]="value" [selected]="value === item.transition.to">{{ value }}</option>
                    }
                  </select>
                  <input type="text" [attr.data-testid]="'entity-transition-note-' + item.index" placeholder="\u5907\u6CE8\u8BF4\u660E" [value]="item.transition.note || item.transition.action || item.transition.label || ''" (input)="setTransitionNote(item.transition, $any($event.target).value)">
                  <div class="entity-transition-actions">
                    <button class="transition-action" type="button" title="\u5728\u4E0B\u65B9\u63D2\u5165\u6D41\u8F6C" (click)="addTransition(entity)">+</button>
                    <button class="transition-action" type="button" title="\u4E0A\u79FB" (click)="moveTransition(entity, item.index, -1)" [disabled]="rowIndex === 0">\u2191</button>
                    <button class="transition-action" type="button" title="\u4E0B\u79FB" (click)="moveTransition(entity, item.index, 1)" [disabled]="rowIndex === selectedStateTransitions(entity).length - 1">\u2193</button>
                    <button class="transition-action transition-action-delete" type="button" title="\u5220\u9664" (click)="removeTransition(entity, item.index)">\xD7</button>
                  </div>
                </div>
              } @empty {
                <p class="no-refs">\u6682\u65E0\u72B6\u6001\u6D41\u8F6C\uFF0C\u5148\u6DFB\u52A0\u4E00\u6761\u8FB9\uFF0C\u4F8B\u5982\uFF1A\u8349\u7A3F \u2192 \u5F85\u5BA1\u6838\u3002</p>
              }
            </div>
          </div>
        </div>
      </aside>
    }
  </div>
</section>
`, styles: ['/* src/app/workbenches/component/entity-design/entity-design-workbench.component.scss */\n:host {\n  display: block;\n  height: 100%;\n  min-height: 0;\n}\n.entity-design {\n  display: grid;\n  grid-template-rows: auto minmax(0, 1fr);\n  gap: 10px;\n  height: 100%;\n  min-height: 0;\n}\n.entity-design-toolbar {\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  padding: 6px 10px;\n  border: 1px solid #d7e2ef;\n  border-radius: 8px;\n}\n.entity-visually-hidden {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n}\n.entity-design-body {\n  position: relative;\n  display: block;\n  min-height: 0;\n  overflow: hidden;\n}\n.entity-design-body.has-drawer {\n  display: block;\n}\n.entity-design-canvas-shell {\n  height: 100%;\n  min-height: 0;\n  min-width: 0;\n  overflow-x: auto;\n  overflow-y: auto;\n  border: 1px solid #d7e2ef;\n  border-radius: 10px;\n  background: #fff;\n  scrollbar-gutter: stable;\n  transition: margin-right 0.12s ease;\n}\n.entity-design-canvas-shell::-webkit-scrollbar,\n.entity-design-drawer-body::-webkit-scrollbar {\n  width: 6px;\n  height: 6px;\n}\n.entity-design-canvas-shell::-webkit-scrollbar-thumb,\n.entity-design-drawer-body::-webkit-scrollbar-thumb {\n  border-radius: 999px;\n  background: #c7d2df;\n}\n.entity-relation-canvas,\n.entity-state-workbench {\n  min-width: 900px;\n  min-height: 520px;\n  padding: 8px 14px;\n}\n.entity-relation-canvas {\n  display: inline-block;\n}\n.entity-state-workbench {\n  display: grid;\n  grid-template-columns: 240px minmax(0, 1fr);\n  gap: 14px;\n  flex: 1;\n  height: 100%;\n  min-width: 0;\n  min-height: 0;\n  padding: 12px 16px 16px;\n  overflow: hidden;\n  background: #f8fafc;\n}\n.entity-state-browser {\n  min-height: 0;\n  overflow: hidden auto;\n  padding: 12px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 12px;\n  background: #fff;\n}\n.entity-state-group + .entity-state-group {\n  margin-top: 12px;\n}\n.entity-state-group-title {\n  margin-bottom: 8px;\n  color: #475569;\n  font-size: 12px;\n  font-weight: 700;\n}\n.entity-state-group-list {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.entity-state-chip {\n  width: 100%;\n  padding: 7px 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 10px;\n  background: #fff;\n  color: #0f172a;\n  font: inherit;\n  text-align: left;\n  cursor: pointer;\n}\n.entity-state-chip.active {\n  border-color: #2563eb;\n  background: #eff6ff;\n  color: #2563eb;\n  font-weight: 600;\n}\n.entity-state-stage {\n  position: relative;\n  min-width: 0;\n  min-height: 0;\n  overflow: hidden;\n  display: flex;\n  flex-direction: column;\n}\n.entity-state-main-shell {\n  min-width: 0;\n  min-height: 0;\n  height: 100%;\n  overflow: auto;\n  transition: margin-right 0.12s ease;\n}\n.entity-state-main {\n  min-width: 0;\n  padding: 0 14px 14px 0;\n}\n.entity-state-card {\n  width: 100%;\n  padding: 14px 16px 16px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 12px;\n  background: #fff;\n}\n.entity-board {\n  position: relative;\n  border-radius: 0;\n  background: #fff;\n}\n.entity-board.is-editing {\n  background:\n    linear-gradient(#e6eef8 1px, transparent 1px),\n    linear-gradient(\n      90deg,\n      #e6eef8 1px,\n      transparent 1px),\n    #fff;\n  background-size: 180px 128px;\n}\n.entity-component-frame,\n.entity-group-frame {\n  position: absolute;\n  box-sizing: border-box;\n  pointer-events: none;\n}\n.entity-component-frame {\n  z-index: 0;\n  border: 1px solid #cbd5e1;\n  border-radius: 8px;\n  background: rgba(248, 250, 252, 0.72);\n}\n.entity-component-title,\n.entity-group-title {\n  position: absolute;\n  z-index: 3;\n  min-width: 60px;\n  padding: 3px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.95);\n  color: #334155;\n  font-size: 12px;\n  font-weight: 800;\n  line-height: 1.4;\n  text-align: center;\n  white-space: nowrap;\n  cursor: pointer;\n  -webkit-user-select: none;\n  user-select: none;\n  transition:\n    background 0.15s,\n    border-color 0.15s,\n    box-shadow 0.15s;\n}\n.entity-component-title:hover,\n.entity-group-title:hover {\n  background: #dbeafe;\n  border-color: #60a5fa;\n  box-shadow: 0 0 0 2px rgba(96, 165, 250, 0.25);\n}\n.entity-group-frame {\n  z-index: 1;\n  border: 1px dashed var(--entity-frame-color);\n  border-radius: 8px;\n  background: var(--entity-frame-fill);\n}\n.entity-group-title {\n  top: 5px;\n  left: 10px;\n  max-width: calc(100% - 20px);\n  pointer-events: auto;\n}\n.entity-rel-svg {\n  z-index: 2;\n  position: absolute;\n  inset: 0;\n  overflow: visible;\n  pointer-events: none;\n}\n.entity-rel-svg marker path {\n  fill: #64748b;\n}\n.entity-rel-line {\n  fill: none;\n  stroke-linecap: round;\n  cursor: pointer;\n  pointer-events: stroke;\n}\n.entity-rel-line.is-dashed {\n  stroke-dasharray: 5 3;\n}\n.entity-rel-line.is-muted {\n  filter: grayscale(0.3);\n}\n.entity-rel-label {\n  position: absolute;\n  z-index: 5;\n  max-width: 160px;\n  overflow: hidden;\n  padding: 1px 5px;\n  border: 1px solid rgba(203, 213, 225, 0.8);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.94);\n  color: #475569;\n  font-size: 10px;\n  line-height: 1.2;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n  transform: translate(-50%, -50%);\n  pointer-events: none;\n}\n.entity-node {\n  position: absolute;\n  z-index: 4;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  box-sizing: border-box;\n  padding: 5px 13px;\n  border: 2px solid var(--entity-stroke);\n  border-radius: 6px;\n  background: var(--entity-fill);\n  color: var(--entity-accent);\n  cursor: pointer;\n  font-family: inherit;\n  font-size: 14px;\n  text-align: center;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.entity-node:hover,\n.entity-node.is-focus {\n  border-color: #2563eb;\n  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.14);\n}\n.entity-node.is-selected {\n  outline: 2px solid #2563eb;\n  outline-offset: 2px;\n}\n.entity-node-component {\n  display: none;\n  color: var(--entity-accent);\n  font-size: 11px;\n  font-weight: 800;\n}\n.entity-node strong {\n  overflow: hidden;\n  font-size: 14px;\n  font-weight: 800;\n  line-height: normal;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-node em {\n  display: none;\n  color: #64748b;\n  font-size: 12px;\n  font-style: normal;\n}\n.entity-selection-box {\n  position: absolute;\n  z-index: 4;\n  border: 1px dashed #60a5fa;\n  border-radius: 8px;\n  background: rgba(147, 197, 253, 0.14);\n  pointer-events: none;\n}\n.entity-state-card-head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n  margin-bottom: 12px;\n}\n.entity-state-card-summary {\n  display: flex;\n  flex-wrap: wrap;\n  justify-content: flex-end;\n  gap: 8px;\n}\n.entity-state-field-chip {\n  padding: 5px 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 999px;\n  background: #fff;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.entity-state-field-chip.active {\n  border-color: #2563eb;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.entity-state-card-title {\n  color: #0f172a;\n  font-size: 16px;\n  font-weight: 700;\n}\n.entity-state-card-subtitle {\n  margin-top: 4px;\n  color: #64748b;\n  font-size: 12px;\n}\n.entity-state-card-values {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 6px;\n}\n.entity-state-value-chip {\n  display: inline-flex;\n  align-items: center;\n  padding: 4px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 12px;\n  font-weight: 600;\n}\n.entity-state-value-chip.kind-initial {\n  border-color: #67e8f9;\n  background: #ecfeff;\n  color: #155e75;\n}\n.entity-state-value-chip.kind-terminal {\n  border-color: #fda4af;\n  background: #fff1f2;\n  color: #be123c;\n}\n.entity-state-overview-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));\n  gap: 12px;\n}\n.entity-state-field-panel {\n  padding: 12px;\n  border: 1px solid #e2e8f0;\n  border-radius: 12px;\n  background:\n    linear-gradient(\n      180deg,\n      #fbfdff 0%,\n      #fff 100%);\n  cursor: pointer;\n  transition:\n    border-color 0.15s ease,\n    box-shadow 0.15s ease,\n    background 0.15s ease;\n}\n.entity-state-field-panel:hover {\n  border-color: #bfdbfe;\n  box-shadow: 0 8px 18px rgba(59, 130, 246, 0.08);\n}\n.entity-state-field-panel.active {\n  border-color: #3b82f6;\n  background: #fff;\n  box-shadow: 0 10px 22px rgba(59, 130, 246, 0.12);\n}\n.entity-state-field-panel-head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 10px;\n  margin-bottom: 10px;\n}\n.entity-state-field-panel-title {\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 700;\n}\n.entity-state-field-panel-meta {\n  margin-top: 4px;\n  color: #64748b;\n  font-size: 11px;\n  line-height: 1.5;\n}\n.entity-state-field-panel-badges {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  flex-wrap: wrap;\n  gap: 6px;\n}\n.entity-state-field-badge {\n  display: inline-flex;\n  align-items: center;\n  padding: 3px 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 999px;\n  background: #f8fafc;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n}\n.entity-state-field-badge.role-primary {\n  border-color: #93c5fd;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.entity-state-field-badge.role-secondary {\n  border-color: #fdba74;\n  background: #fff7ed;\n  color: #c2410c;\n}\n.entity-state-field-badge.active {\n  border-color: #bfdbfe;\n  background: #dbeafe;\n  color: #1d4ed8;\n}\n.entity-state-field-empty {\n  margin-top: 8px;\n}\n.entity-state-graph {\n  width: 100%;\n  overflow: auto;\n  padding-bottom: 4px;\n}\n.entity-state-zoom-shell {\n  position: relative;\n}\n.entity-state-zoom-target {\n  position: relative;\n}\n.entity-state-canvas {\n  position: relative;\n  min-width: 100%;\n}\n.entity-state-board {\n  position: relative;\n  pointer-events: auto;\n  overflow: visible;\n}\n.entity-state-svg {\n  position: absolute;\n  z-index: 1;\n  left: 0;\n  top: 0;\n  overflow: visible;\n  pointer-events: none;\n}\n.entity-state-svg marker path {\n  fill: #64748b;\n}\n.entity-state-link {\n  fill: none;\n  stroke: #94a3b8;\n  stroke-linecap: round;\n  stroke-width: 1.8;\n  cursor: pointer;\n  pointer-events: stroke;\n}\n.entity-state-link.selected {\n  stroke: #2563eb;\n  stroke-width: 2.5;\n}\n.entity-state-link-label-group {\n  cursor: move;\n  pointer-events: auto;\n}\n.entity-state-link-label-group rect {\n  fill: rgba(255, 255, 255, 0.01);\n  stroke: none;\n  pointer-events: all;\n}\n.entity-state-link-label {\n  fill: #475569;\n  paint-order: stroke;\n  stroke: rgba(255, 255, 255, 0.92);\n  stroke-linejoin: round;\n  stroke-width: 4px;\n  font-size: 12px;\n  font-weight: 600;\n}\n.entity-state-node {\n  position: absolute;\n  z-index: 3;\n  display: grid;\n  place-items: center;\n  box-sizing: border-box;\n  padding: 0 12px;\n  border: 1px solid #cbd5e1;\n  border-radius: 12px;\n  background: #f8fafc;\n  color: #334155;\n  box-shadow: 0 4px 12px rgba(15, 23, 42, 0.05);\n  text-align: center;\n  cursor: move;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.entity-state-node.kind-initial {\n  border-color: #38bdf8;\n  background: #e0f2fe;\n  color: #0f4c81;\n}\n.entity-state-node.kind-terminal {\n  border-color: #fb7185;\n  background: #ffe4e6;\n  color: #9f1239;\n}\n.entity-state-node-label {\n  max-width: 100%;\n  overflow: hidden;\n  font-size: 11px;\n  font-weight: 700;\n  line-height: 1.25;\n  text-align: center;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-state-marker {\n  position: absolute;\n  z-index: 3;\n  box-sizing: border-box;\n  border-radius: 50%;\n  cursor: move;\n  pointer-events: auto;\n}\n.entity-state-start-dot {\n  width: 12px;\n  height: 12px;\n  background: #0f172a;\n}\n.entity-state-end-dot-outer {\n  width: 20px;\n  height: 20px;\n  border: 2.4px solid #0f172a;\n  background: #fff;\n}\n.entity-state-end-dot-outer::after {\n  content: "";\n  position: absolute;\n  left: 50%;\n  top: 50%;\n  width: 10px;\n  height: 10px;\n  transform: translate(-50%, -50%);\n  border-radius: 50%;\n  background: #0f172a;\n}\n.entity-design-drawer {\n  position: absolute;\n  z-index: 20;\n  top: 0;\n  right: 0;\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  min-width: 420px;\n  min-height: 0;\n  overflow: hidden;\n  border: none;\n  border-left: 1px solid var(--border, #d7e2ef);\n  border-radius: 0;\n  background: #fff;\n  box-shadow: -6px 0 24px rgba(0, 0, 0, 0.1);\n}\n.state-editor-drawer {\n  position: absolute;\n  z-index: 20;\n  top: 0;\n  right: 0;\n  bottom: 0;\n  display: flex;\n  flex-direction: column;\n  min-width: 520px;\n  overflow: hidden;\n  border: none;\n  border-left: 1px solid var(--border, #d7e2ef);\n  border-radius: 0;\n  background: #fff;\n  box-shadow: -6px 0 24px rgba(0, 0, 0, 0.08);\n}\n.state-editor-drawer-resize {\n  position: absolute;\n  z-index: 5;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 6px;\n  cursor: ew-resize;\n}\n.state-editor-drawer-resize::after {\n  content: none;\n}\n.state-editor-body {\n  background: #fff;\n  padding: 12px 14px;\n  color: #1e293b;\n  font-size: 12px;\n}\n.state-editor-drawer .entity-state-editor {\n  width: 100%;\n  padding: 0;\n  background: transparent;\n}\n.entity-state-editor-head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: 12px;\n  margin-bottom: 10px;\n  padding-bottom: 10px;\n}\n.entity-state-editor-head h4 {\n  margin: 0 0 4px;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 800;\n}\n.entity-state-editor-hint {\n  margin: 0;\n  color: #64748b;\n  font-size: 11px;\n  line-height: 1.55;\n}\n.entity-state-editor-actions {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  flex-wrap: wrap;\n  gap: 8px;\n}\n.entity-state-config-row {\n  display: grid;\n  grid-template-columns: minmax(180px, 240px) minmax(0, 1fr);\n  gap: 12px;\n  margin-bottom: 10px;\n}\n.entity-state-values-readonly {\n  width: 100%;\n  min-height: 32px;\n  padding: 7px 10px;\n  border: 1px dashed var(--border, #d7e2ef);\n  border-radius: 8px;\n  background: #f8fafc;\n  color: #64748b;\n  font-size: 12px;\n  line-height: 1.45;\n}\n.entity-state-kind-editor {\n  margin-bottom: 10px;\n  padding: 10px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 10px;\n  background: #f8fafc;\n}\n.entity-state-kind-head h5 {\n  margin: 0 0 4px;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n}\n.entity-transition-list {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.entity-transition-row {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 24px minmax(0, 1fr) minmax(160px, 1.1fr) 116px;\n  gap: 6px;\n  align-items: center;\n  padding: 8px 10px;\n  border: 1px solid #e2e8f0;\n  border-radius: 9px;\n  background: #fbfdff;\n}\n.entity-transition-row input {\n  min-width: 0;\n  min-height: 30px;\n  padding: 5px 8px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.entity-transition-arrow {\n  color: #64748b;\n  font-weight: 700;\n  text-align: center;\n}\n.entity-transition-actions {\n  display: inline-flex;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 4px;\n}\n.transition-action {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 24px;\n  height: 24px;\n  border: 1px solid #dbe5f0;\n  background: #fff;\n  cursor: pointer;\n}\n.transition-action:hover {\n  border-color: #93c5fd;\n  color: #2563eb;\n}\n.transition-action:disabled {\n  opacity: 0.4;\n  cursor: not-allowed;\n}\n.entity-design-drawer-resize {\n  position: absolute;\n  z-index: 5;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  width: 5px;\n  cursor: ew-resize;\n  background: transparent;\n  transition: background 0.15s;\n}\n.entity-design-drawer-resize::after {\n  content: none;\n}\n.entity-design-drawer-resize:hover {\n  background: #3b82f6;\n  opacity: 0.5;\n}\n.entity-design-drawer-head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  flex-shrink: 0;\n  gap: 8px;\n  padding: 10px 14px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n  background: #fff;\n}\n.drawer-crumb {\n  display: flex;\n  align-items: center;\n  flex: 1;\n  gap: 6px;\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 600;\n}\n.drawer-crumb span {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.drawer-actions {\n  display: flex;\n  align-items: center;\n  flex-shrink: 0;\n  gap: 6px;\n}\n.drawer-close {\n  padding: 2px 7px;\n  border: none;\n  border-radius: 4px;\n  background: none;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 16px;\n  line-height: 1;\n}\n.drawer-close:hover {\n  background: #f1f5f9;\n  color: #0f172a;\n}\n.entity-design-drawer-body {\n  flex: 1 1 auto;\n  min-height: 0;\n  overflow: auto;\n  padding: 14px 16px;\n}\n.form-grid {\n  display: grid;\n  grid-template-columns: repeat(2, minmax(0, 1fr));\n  gap: 12px;\n}\n.entity-form-grid {\n  margin-bottom: 16px;\n}\n.field-group {\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n}\n.field-group label {\n  color: #475569;\n  font-size: 12px;\n  font-weight: 600;\n}\n.field-group input,\n.field-group select {\n  min-height: 32px;\n  padding: 6px 9px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 13px;\n}\n.entity-edit-section {\n  margin-top: 14px;\n}\n.entity-edit-section h4 {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  margin: 0 0 8px;\n  color: #0f172a;\n  font-size: 13px;\n  font-weight: 700;\n}\n.entity-relation-debug-toggle {\n  margin-left: auto;\n}\n.entity-section-head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 10px;\n}\n.entity-shortcut-hint strong {\n  display: inline-block;\n  padding: 0 3px;\n  border-radius: 4px;\n  background: #eff6ff;\n  color: var(--accent, #2563eb);\n  font-weight: 800;\n}\n.task-ref-list {\n  display: flex;\n  flex-wrap: wrap;\n  gap: 8px;\n  padding-top: 4px;\n}\n.task-ref {\n  display: inline-flex;\n  align-items: center;\n  min-height: 28px;\n  padding: 3px 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 4px;\n  background: #eff6ff;\n  color: var(--accent, #2563eb);\n  font-size: 12px;\n  font-weight: 600;\n}\n.field-table {\n  width: 100%;\n  margin-bottom: 8px;\n  border-collapse: collapse;\n}\n.field-table th {\n  padding: 6px 8px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n  background: #eef2f7;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 600;\n  text-align: left;\n}\n.field-table td {\n  padding: 4px 6px;\n  border-bottom: 1px solid var(--border, #d7e2ef);\n}\n.field-table tr:hover td {\n  background: #f8fafc;\n}\n.field-table input[type=text],\n.field-table select {\n  width: 100%;\n  box-sizing: border-box;\n  min-height: 28px;\n  padding: 4px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.field-table input[type=checkbox] {\n  width: 15px;\n  height: 15px;\n  cursor: pointer;\n}\n.field-td-name {\n  width: 120px;\n}\n.field-td-type {\n  width: 80px;\n}\n.field-td-note {\n  min-width: 160px;\n}\n.field-td-note textarea {\n  width: 100%;\n  box-sizing: border-box;\n  min-height: 28px;\n  max-height: 28px;\n  resize: none;\n  overflow: hidden;\n  padding: 4px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fafbfc;\n  color: #0f172a;\n  font-size: 12px;\n  line-height: 1.5;\n}\n.field-td-note textarea[style*=height] {\n  max-height: none;\n  overflow: auto;\n}\n.field-status-cell {\n  width: 94px;\n  text-align: center;\n}\n.field-status-role-wrap {\n  display: inline-flex;\n  align-items: center;\n  padding: 2px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 999px;\n  background: #f8fafc;\n}\n.field-status-role-select {\n  min-width: 62px;\n  height: 26px;\n  padding: 0 24px 0 10px;\n  border: none;\n  border-radius: 999px;\n  background: transparent;\n  color: #475569;\n  font-size: 12px;\n  font-weight: 700;\n  appearance: none;\n}\n.field-status-role-select:focus {\n  outline: none;\n}\n.field-status-role-wrap:focus-within {\n  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.14);\n}\n.field-status-role-wrap-primary {\n  border-color: #93c5fd;\n  background: #eff6ff;\n}\n.field-status-role-select-primary {\n  color: #1d4ed8;\n}\n.field-status-role-wrap-secondary {\n  border-color: #fdba74;\n  background: #fff7ed;\n}\n.field-status-role-select-secondary {\n  color: #c2410c;\n}\n.field-actions-cell {\n  width: 116px;\n  white-space: nowrap;\n  text-align: right;\n}\n.field-actions,\n.rel-actions {\n  display: inline-flex;\n  align-items: center;\n  flex-wrap: nowrap;\n  gap: 4px;\n}\n.field-action,\n.rel-action {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  flex-shrink: 0;\n  width: 24px;\n  height: 24px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #64748b;\n  cursor: pointer;\n  font-size: 12px;\n  line-height: 1;\n}\n.field-action:hover,\n.rel-action:hover {\n  border-color: #bfdbfe;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.field-action:disabled,\n.rel-action:disabled {\n  opacity: 0.45;\n  cursor: not-allowed;\n  background: #f8fafc;\n  color: #94a3b8;\n}\n.field-del {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  flex-shrink: 0;\n  width: 24px;\n  height: 24px;\n  padding: 0;\n  border: none;\n  border-radius: 4px;\n  background: none;\n  color: #64748b;\n  cursor: pointer;\n}\n.field-del:hover {\n  background: #fee2e2;\n  color: #dc2626;\n}\n.rel-scope-tip,\n.no-refs {\n  margin: 0 0 8px;\n  color: #64748b;\n  font-size: 12px;\n}\n.no-refs {\n  font-style: italic;\n}\n.entity-relation-debug {\n  max-height: 220px;\n  margin: 8px 0 10px;\n  overflow: auto;\n  border: 1px solid #dbe7f3;\n  border-radius: 6px;\n  background: #f8fafc;\n  font-size: 11px;\n}\n.entity-relation-debug-title {\n  position: sticky;\n  top: 0;\n  z-index: 1;\n  padding: 6px 8px;\n  border-bottom: 1px solid #dbe7f3;\n  background: #eef5ff;\n  color: #17406d;\n  font-weight: 800;\n}\n.entity-relation-debug table {\n  width: 100%;\n  min-width: 980px;\n  border-collapse: collapse;\n}\n.entity-relation-debug th,\n.entity-relation-debug td {\n  padding: 5px 6px;\n  border-bottom: 1px solid #e5edf6;\n  color: #334155;\n  text-align: left;\n  vertical-align: top;\n}\n.entity-relation-debug th {\n  background: #f1f5f9;\n  color: #0f172a;\n  font-weight: 800;\n  white-space: nowrap;\n}\n.entity-relation-debug code {\n  color: #475569;\n  white-space: pre-wrap;\n  word-break: break-all;\n}\n.entity-relation-debug .visible-row td {\n  background: #ecfdf5;\n}\n.rel-row {\n  display: grid;\n  grid-template-columns: minmax(110px, 1fr) 76px minmax(110px, 1fr) minmax(120px, 1fr) auto;\n  gap: 6px;\n  align-items: center;\n  margin-bottom: 6px;\n}\n.rel-row input,\n.rel-row select {\n  min-width: 0;\n  min-height: 30px;\n  padding: 5px 8px;\n  border: 1px solid var(--border, #d7e2ef);\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n}\n.entity-state-kind-list {\n  display: grid;\n  gap: 6px;\n}\n.entity-state-kind-row {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) 110px;\n  gap: 6px;\n  align-items: center;\n  padding: 6px 8px;\n  border: 1px solid #e5edf6;\n  border-radius: 8px;\n  background: #fff;\n}\n.entity-state-kind-row span {\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.entity-state-kind-row select,\n.entity-transition-row select {\n  min-width: 0;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  padding: 5px 8px;\n}\n.entity-field-note {\n  width: 100%;\n  box-sizing: border-box;\n}\n.entity-muted,\n.entity-empty {\n  margin: 0;\n  color: #64748b;\n  font-size: 13px;\n}\n.entity-delete-btn {\n  justify-self: stretch;\n}\n/*# sourceMappingURL=entity-design-workbench.component.css.map */\n'] }]
  }], null, { showEditorToggle: [{
    type: Input
  }], exportGraphId: [{
    type: Input
  }], focusConstructId: [{
    type: Input
  }], initialView: [{
    type: Input
  }], initialEntityId: [{
    type: Input
  }], editRequested: [{
    type: Output
  }], editing: [{
    type: Input
  }], onDocumentMouseMove: [{
    type: HostListener,
    args: ["document:mousemove", ["$event"]]
  }], onDocumentMouseUp: [{
    type: HostListener,
    args: ["document:mouseup"]
  }], onDocumentKeydown: [{
    type: HostListener,
    args: ["document:keydown", ["$event"]]
  }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(EntityDesignWorkbenchComponent, { className: "EntityDesignWorkbenchComponent", filePath: "src/app/workbenches/component/entity-design/entity-design-workbench.component.ts", lineNumber: 190 });
})();

// src/app/core/document/value-domain-model.ts
function ensureValueDomainModel(document2, nextId) {
  document2.panorama ??= {};
  document2.panorama.columns ??= [];
  document2.panorama.lanes ??= [];
  document2.panorama.cells ??= [];
  if (!document2.panorama.columns.length) {
    document2.panorama.columns.push({ uid: nextId("panorama-column"), name: "\u9ED8\u8BA4\u4EF7\u503C\u6D41", badge: "", scope: "" });
  }
  if (!document2.panorama.lanes.length) {
    document2.panorama.lanes.push({ uid: nextId("panorama-lane"), name: "\u9ED8\u8BA4\u4E1A\u52A1\u57DF", badge: "", note: "" });
  }
  return document2.panorama;
}
function ensureValueDomainStages(document2) {
  if (!Array.isArray(document2.stages))
    document2.stages = [];
  return document2.stages;
}
function getValueDomainStageId(stage) {
  return String(stage.id || stage.uid || "");
}
function getValueDomainColumnUid(column) {
  return String(column?.uid || "");
}
function getValueDomainLaneUid(lane) {
  return String(lane?.uid || "");
}
function findOrCreateValueDomainCell(model2, laneUid, columnUid) {
  let cell = model2.cells.find((item) => item.laneUid === laneUid && item.columnUid === columnUid);
  if (!cell) {
    cell = { laneUid, columnUid, status: "", text: "" };
    model2.cells.push(cell);
  }
  return cell;
}

// src/app/workbenches/process/editor/process-editor-legacy-adapter.ts
function createProcessEditorLegacyAdapter(legacyWindow = getAngularRuntimeState()) {
  const state = () => {
    const direct = legacyWindow;
    if (direct.doc || direct.ui)
      return { doc: direct.doc, ui: direct.ui };
    return legacyWindow.S || {};
  };
  const document2 = () => state().doc || {};
  const ui = () => {
    state().ui ||= {};
    return state().ui;
  };
  const expandedPrototypeKeys = /* @__PURE__ */ new Map();
  const prototypeExpandKey = (ownerId, prototypeUid) => `${String(ownerId || "").trim()}::${String(prototypeUid || "").trim()}`;
  const nextId = (prefix, items = []) => {
    const used = new Set(items.map((item) => String(item.id || item.uid || "")));
    for (let index = items.length + 1; index < items.length + 200; index += 1) {
      const id = `${prefix}${index}`;
      if (!used.has(id))
        return id;
    }
    return `${prefix}${Date.now()}`;
  };
  const dirty = () => {
    if (legacyWindow.markModified)
      legacyWindow.markModified();
    else
      markAngularRuntimeModified();
    if (legacyWindow.renderSidebar)
      legacyWindow.renderSidebar();
    else
      emitRuntimeRefresh();
  };
  const refreshUiOnly = () => {
    if (legacyWindow.renderSidebar)
      legacyWindow.renderSidebar();
    else
      emitRuntimeRefresh();
  };
  function processId(process2) {
    return String(process2?.uid || process2?.id || "").trim();
  }
  function taskId(task) {
    return String(task?.uid || task?.id || "").trim();
  }
  function processes() {
    return document2().processes || [];
  }
  function tasks(process2) {
    if (!process2)
      return [];
    process2.nodes ||= process2.tasks || [];
    return process2.nodes;
  }
  function normalizeFlow(process2) {
    if (!process2)
      return { version: 2, orientation: "horizontal", nodes: [], edges: [] };
    const rawFlow = process2.flow && typeof process2.flow === "object" ? process2.flow : {};
    rawFlow.version = Number(rawFlow.version || 2) || 2;
    rawFlow.orientation = rawFlow.orientation === "vertical" ? "vertical" : "horizontal";
    rawFlow.nodes = Array.isArray(rawFlow.nodes) ? rawFlow.nodes : [];
    rawFlow.edges = Array.isArray(rawFlow.edges) ? rawFlow.edges : [];
    rawFlow.nodes = rawFlow.nodes.filter((node) => node && typeof node === "object").map((node, index) => __spreadProps(__spreadValues({}, node), {
      id: String(node.id || node.uid || `B${index + 1}`),
      kind: "gateway",
      gatewayType: node.gatewayType || "exclusive",
      title: String(node.title || ""),
      role_id: String(node.role_id || "")
    }));
    rawFlow.edges = rawFlow.edges.filter((edge) => edge && typeof edge === "object").map((edge, index) => __spreadProps(__spreadValues({}, edge), {
      id: String(edge.id || edge.uid || `L${index + 1}`),
      from: String(edge.from || ""),
      to: String(edge.to || ""),
      label: String(edge.label || edge.condition || ""),
      condition: String(edge.condition || "")
    }));
    process2.flow = rawFlow;
    return rawFlow;
  }
  function gateways(process2) {
    return normalizeFlow(process2).nodes || [];
  }
  function edges(process2) {
    return normalizeFlow(process2).edges || [];
  }
  function currentProcess() {
    const currentId = String(ui().procId || "").trim();
    return processes().find((process2) => processId(process2) === currentId || process2.uid === currentId || process2.id === currentId) || processes()[0] || null;
  }
  function uploadProcessAttachment(file) {
    return new Promise((resolve, reject) => {
      const xhr = new XMLHttpRequest();
      xhr.open("POST", "/api/attachment-upload");
      xhr.setRequestHeader("Content-Type", file.type || "application/octet-stream");
      xhr.setRequestHeader("X-Attachment-Name", encodeURIComponent(file.name || "attachment"));
      xhr.responseType = "json";
      xhr.onload = () => {
        const result = xhr.response || JSON.parse(xhr.responseText || "{}");
        resolve(__spreadProps(__spreadValues({}, result && typeof result === "object" ? result : {}), { status: xhr.status, ok: xhr.status >= 200 && xhr.status < 300 && !result?.error }));
      };
      xhr.onerror = () => reject(new Error("attachment upload failed"));
      xhr.send(file);
    });
  }
  function currentTask() {
    const process2 = currentProcess();
    const currentTaskId = String(ui().taskId || "").trim();
    if (!process2 || !currentTaskId)
      return null;
    return tasks(process2).find((task) => taskId(task) === currentTaskId) || null;
  }
  function normalizeStageId(stageId) {
    const target = String(stageId || "").trim();
    if (!target)
      return "";
    const stage = (document2().stages || []).find((item) => String(item.uid || "").trim() === target || String(item.id || "").trim() === target);
    return stage ? String(stage.id || stage.uid || "").trim() : target;
  }
  function stageIdForProcess(targetProcessId) {
    const process2 = processes().find((item) => processId(item) === targetProcessId || item.id === targetProcessId || item.uid === targetProcessId);
    const processKeys = new Set([targetProcessId, process2?.id, process2?.uid].filter(Boolean).map(String));
    const currentStageId = normalizeStageId(String(ui().stageId || "").trim());
    const refs = document2().stageFlowRefs || [];
    const currentRef = refs.find((ref2) => normalizeStageId(String(ref2.stageUid || ref2.stageId || "").trim()) === currentStageId && (processKeys.has(String(ref2.processUid || "").trim()) || processKeys.has(String(ref2.processId || "").trim())));
    if (currentRef)
      return normalizeStageId(String(currentRef.stageUid || currentRef.stageId || currentStageId).trim());
    const ref = refs.filter((item) => processKeys.has(String(item.processUid || "").trim()) || processKeys.has(String(item.processId || "").trim())).sort((left, right) => Number(left.order || 0) - Number(right.order || 0))[0];
    return normalizeStageId(String(ref?.stageUid || ref?.stageId || process2?.stageUid || process2?.stageId || "").trim());
  }
  function findPrototypeOwner(ownerId) {
    const target = String(ownerId || "").trim();
    if (!target)
      return null;
    for (const process2 of processes()) {
      if (processId(process2) === target) {
        return { process: process2, task: null, files: Array.isArray(process2.prototypeFiles) ? process2.prototypeFiles : [] };
      }
      for (const task of tasks(process2)) {
        if (taskId(task) === target) {
          return { process: process2, task, files: Array.isArray(task.prototypeFiles) ? task.prototypeFiles : [] };
        }
      }
    }
    return null;
  }
  function findPrototypeFile(ownerId, prototypeUid) {
    const target = String(prototypeUid || "").trim();
    const owner = findPrototypeOwner(ownerId);
    if (!owner || !target)
      return null;
    return owner.files.find((file) => String(file.uid || file.id || file.name || "").trim() === target) || null;
  }
  function findPrototypeFileAnywhere(prototypeUid) {
    const target = String(prototypeUid || "").trim();
    if (!target)
      return null;
    for (const process2 of processes()) {
      const processFile = (Array.isArray(process2.prototypeFiles) ? process2.prototypeFiles : []).find((file) => String(file.uid || file.id || file.name || "").trim() === target);
      if (processFile)
        return processFile;
      for (const task of tasks(process2)) {
        const taskFile = (Array.isArray(task.prototypeFiles) ? task.prototypeFiles : []).find((file) => String(file.uid || file.id || file.name || "").trim() === target);
        if (taskFile)
          return taskFile;
      }
    }
    return null;
  }
  function getAttachmentExtension(name = "") {
    const match = String(name || "").trim().toLowerCase().match(/\.([a-z0-9]+)$/);
    return match ? match[1] : "";
  }
  function canPreviewAttachment(versionOrFile) {
    const ext = getAttachmentExtension(versionOrFile?.name || "");
    return ["html", "htm", "png", "jpg", "jpeg", "gif", "webp", "svg", "pdf", "md", "txt", "json", "csv"].includes(ext);
  }
  function decodeAttachmentContent(version) {
    if (String(version.contentEncoding || "").toLowerCase() !== "base64")
      return version.content || "";
    const binary = atob(String(version.content || ""));
    const bytes = new Uint8Array(binary.length);
    for (let index = 0; index < binary.length; index += 1)
      bytes[index] = binary.charCodeAt(index);
    return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
  }
  function attachmentObjectUrl(version) {
    if ("localUrl" in version && version.localUrl)
      return version.localUrl;
    const contentType = String(version.contentType || "text/html").trim() || "text/html";
    const BlobCtor = legacyWindow.Blob || Blob;
    const URLApi = legacyWindow.URL || URL;
    const blob = new BlobCtor([decodeAttachmentContent(version)], {
      type: /charset=/i.test(contentType) ? contentType : `${contentType};charset=utf-8`
    });
    return URLApi.createObjectURL(blob);
  }
  function hasInlineAttachmentContent(version) {
    return Boolean(version && "localUrl" in version && version.localUrl) || Object.prototype.hasOwnProperty.call(version || {}, "content") && String(version?.content || "") !== "";
  }
  function persistedAttachmentUrl(file, version, options = {}) {
    const documentMeta = document2().meta || {};
    const documentName = String(state().currentFile || documentMeta.domain || documentMeta.title || "").trim();
    const attachmentUid = String(file.uid || "").trim();
    const versionUid = String(version?.uid || file.versionUid || "").trim();
    if (documentName && attachmentUid && versionUid) {
      const base = `/api/attachment/${encodeURIComponent(documentName)}/${encodeURIComponent(attachmentUid)}/${encodeURIComponent(versionUid)}`;
      return options["download"] ? `${base}?download=1` : base;
    }
    const api = legacyWindow.api || globalThis.api;
    if (!documentName || !attachmentUid || !versionUid || typeof api?.attachmentUrl !== "function")
      return "";
    return api.attachmentUrl(documentName, attachmentUid, versionUid, options);
  }
  function openAttachmentInNewTab(file, version) {
    const target = version || file;
    if (!canPreviewAttachment(target))
      return;
    if (!hasInlineAttachmentContent(target)) {
      const persisted = persistedAttachmentUrl(file, version);
      if (persisted) {
        const popup2 = (legacyWindow.open || window.open).call(window, persisted, "_blank", "noopener");
        if (!popup2)
          window.alert?.("\u6D4F\u89C8\u5668\u62E6\u622A\u4E86\u9644\u4EF6\u9884\u89C8\u7A97\u53E3\uFF0C\u8BF7\u5141\u8BB8\u5F39\u7A97\u540E\u91CD\u8BD5\u3002");
        return;
      }
    }
    const URLApi = legacyWindow.URL || URL;
    const objectUrl = attachmentObjectUrl(target);
    const popup = (legacyWindow.open || window.open).call(window, objectUrl, "_blank", "noopener");
    if (!popup) {
      URLApi.revokeObjectURL(objectUrl);
      window.alert?.("\u6D4F\u89C8\u5668\u62E6\u622A\u4E86\u9644\u4EF6\u9884\u89C8\u7A97\u53E3\uFF0C\u8BF7\u5141\u8BB8\u5F39\u7A97\u540E\u91CD\u8BD5\u3002");
      return;
    }
    setTimeout(() => URLApi.revokeObjectURL(objectUrl), 6e4);
  }
  function downloadAttachment(file, version) {
    const target = version || file;
    if (!hasInlineAttachmentContent(target)) {
      const persisted = persistedAttachmentUrl(file, version, { download: true });
      if (persisted) {
        const link2 = globalThis.document.createElement("a");
        link2.href = persisted;
        link2.download = String(target.name || file.name || "attachment").trim();
        link2.rel = "noopener";
        link2.style.display = "none";
        globalThis.document.body.appendChild(link2);
        link2.click();
        globalThis.document.body.removeChild(link2);
        return;
      }
    }
    const objectUrl = attachmentObjectUrl(target);
    const link = globalThis.document.createElement("a");
    link.href = objectUrl;
    link.download = String(target.name || file.name || "attachment").trim();
    link.rel = "noopener";
    link.style.display = "none";
    globalThis.document.body.appendChild(link);
    link.click();
    globalThis.document.body.removeChild(link);
    setTimeout(() => URL.revokeObjectURL(objectUrl), 6e4);
  }
  return {
    currentProcess,
    currentTask,
    processes,
    processId,
    selectProcess(targetProcessId) {
      ui().procId = targetProcessId;
      ui().taskId = null;
      const nextStageId = stageIdForProcess(targetProcessId);
      if (nextStageId)
        ui().stageId = nextStageId;
      if (legacyWindow.renderSidebar)
        legacyWindow.renderSidebar();
      else
        emitRuntimeRefresh();
    },
    openProcessFlow(targetProcessId) {
      ui().tab = "process";
      ui().procId = targetProcessId;
      ui().taskId = null;
      ui().procView = "flow";
      const nextStageId = stageIdForProcess(targetProcessId);
      if (nextStageId)
        ui().stageId = nextStageId;
      if (legacyWindow.renderSidebar)
        legacyWindow.renderSidebar();
      else
        emitRuntimeRefresh();
    },
    taskId,
    tasks,
    entities: () => document2().entities || [],
    taskDefinitions: () => document2().taskDefinitions || [],
    stages: () => document2().stages || [],
    stageFlowRefs: () => document2().stageFlowRefs || [],
    gateways,
    edges,
    drawerWidth() {
      return legacyWindow.getDrawerWidth?.("process") || 480;
    },
    setDrawerWidth(width) {
      legacyWindow.setDrawerWidth?.("process", width);
    },
    roles() {
      const roles = typeof legacyWindow.getRoles === "function" ? legacyWindow.getRoles() : document2().roles || [];
      return roles.filter((role) => String(role.id || role.uid || "").trim());
    },
    taskRoleIds(task) {
      if (typeof legacyWindow.getTaskRoleIds === "function")
        return legacyWindow.getTaskRoleIds(task);
      const roleList = this.roles();
      const roleById = new Map(roleList.map((role) => [String(role.id || role.uid || "").trim(), role]));
      const roleByName = new Map(roleList.map((role) => [String(role.name || "").trim(), role]).filter(([name]) => Boolean(name)));
      const resolveRoleId = (value) => {
        const raw = String(value || "").trim();
        if (!raw)
          return "";
        const role = roleById.get(raw) || roleByName.get(raw);
        return role ? String(role.id || role.uid || "").trim() : "";
      };
      const rawIds = Array.isArray(task.roleIds) && task.roleIds.length ? task.roleIds : Array.isArray(task.role_uids) && task.role_uids.length ? task.role_uids : Array.isArray(task.role_ids) && task.role_ids.length ? task.role_ids : Array.isArray(task.roles) && task.roles.length ? task.roles : task.role_id ? [task.role_id] : task.role ? [task.role] : [];
      return rawIds.flatMap((item) => String(item || "").split(/[、,，/]/)).map((item) => resolveRoleId(item)).filter(Boolean);
    },
    setTaskRoleIds(task, roleIds) {
      const process2 = currentProcess();
      const ids = roleIds.map((item) => String(item || "").trim()).filter(Boolean);
      const roleList = this.roles();
      const roleById = new Map(roleList.map((role) => [String(role.id || role.uid || "").trim(), role]));
      const roleNames = ids.map((id) => String(roleById.get(id)?.name || "").trim()).filter(Boolean);
      if (process2 && typeof legacyWindow.setTaskRoles === "function") {
        legacyWindow.setTaskRoles(processId(process2), taskId(task), ids);
      } else {
        task.roleIds = ids;
        task.role_ids = ids;
        task.role_uids = ids;
        task.role_uid = ids[0] || "";
        task.role_id = ids[0] || "";
        task.roles = roleNames;
        task.role = roleNames.join("\u3001");
      }
      if (!ids.length) {
        task.roleIds = [];
        task.role_ids = [];
        task.role_uids = [];
        task.role_uid = "";
        task.roles = [];
        task.role_id = "";
        task.role = "";
      }
      dirty();
    },
    stageRefs(process2) {
      const id = processId(process2);
      if (!id)
        return [];
      const refs = typeof legacyWindow.getProcessStageRefs === "function" ? legacyWindow.getProcessStageRefs(id, document2()) : (document2().stageFlowRefs || []).filter((ref) => {
        const processKeys = new Set([id, process2?.id, process2?.uid].filter(Boolean).map(String));
        return processKeys.has(String(ref.processUid || "").trim()) || processKeys.has(String(ref.processId || "").trim());
      });
      return refs.map((ref) => normalizeStageId(String(ref.stageUid || ref.stageId || "").trim())).filter(Boolean).map((stageId) => ({
        id: stageId,
        name: legacyWindow.getStageDisplayName?.(stageId, document2()) || (document2().stages || []).find((stage) => String(stage.uid || stage.id || "").trim() === stageId || normalizeStageId(String(stage.uid || stage.id || "")) === stageId)?.name || stageId
      }));
    },
    openStage(stageId) {
      if (legacyWindow.openStageDetail)
        legacyWindow.openStageDetail(stageId);
      else
        navigateAngularWorkbench("process", { stageId });
    },
    prototypeFiles(process2) {
      return Array.isArray(process2?.prototypeFiles) ? process2.prototypeFiles : [];
    },
    currentPrototypeVersion(file) {
      if (typeof legacyWindow.findProcessPrototypeVersion === "function")
        return legacyWindow.findProcessPrototypeVersion(file);
      const versions = Array.isArray(file.versions) ? file.versions : [];
      const versionUid = String(file.versionUid || "").trim();
      return versions.find((version) => String(version.uid || "") === versionUid) || versions[versions.length - 1] || null;
    },
    prototypeKind(file) {
      const version = this.currentPrototypeVersion(file);
      return legacyWindow.getProcessAttachmentKind?.(version || file) || "\u9644\u4EF6";
    },
    canPreviewPrototype(file, version = null) {
      return Boolean(legacyWindow.canPreviewProcessAttachment?.(version || this.currentPrototypeVersion(file) || file)) || canPreviewAttachment(version || this.currentPrototypeVersion(file) || file);
    },
    isPrototypeExpanded(processId2, prototypeUid) {
      const key = prototypeExpandKey(processId2, prototypeUid);
      if (expandedPrototypeKeys.has(key))
        return Boolean(expandedPrototypeKeys.get(key));
      return Boolean(legacyWindow.isProcessPrototypeExpanded?.(processId2, prototypeUid));
    },
    togglePrototypeVersions(processId2, prototypeUid) {
      const key = prototypeExpandKey(processId2, prototypeUid);
      const expanded = this.isPrototypeExpanded(processId2, prototypeUid);
      expandedPrototypeKeys.set(key, !expanded);
      legacyWindow.toggleProcessPrototypeVersions?.(processId2, prototypeUid);
    },
    previewPrototype(ownerId, prototypeUid, versionUid = "") {
      const file = findPrototypeFile(ownerId, prototypeUid) || findPrototypeFileAnywhere(prototypeUid);
      if (file) {
        openAttachmentInNewTab(file, this.currentPrototypeVersion(__spreadProps(__spreadValues({}, file), { versionUid: versionUid || file.versionUid })));
        return;
      }
      legacyWindow.openProcessPrototypeFile?.(ownerId, prototypeUid, versionUid);
    },
    openPrototype(processId2, prototypeUid, versionUid = "") {
      const file = findPrototypeFile(processId2, prototypeUid) || findPrototypeFileAnywhere(prototypeUid);
      if (file) {
        openAttachmentInNewTab(file, this.currentPrototypeVersion(__spreadProps(__spreadValues({}, file), { versionUid: versionUid || file.versionUid })));
        return;
      }
      legacyWindow.openProcessPrototypeFile?.(processId2, prototypeUid, versionUid);
    },
    downloadPrototype(processId2, prototypeUid, versionUid = "") {
      const file = findPrototypeFile(processId2, prototypeUid) || findPrototypeFileAnywhere(prototypeUid);
      if (file) {
        downloadAttachment(file, this.currentPrototypeVersion(__spreadProps(__spreadValues({}, file), { versionUid: versionUid || file.versionUid })));
        return;
      }
      legacyWindow.downloadProcessPrototypeFile?.(processId2, prototypeUid, versionUid);
    },
    removePrototype(ownerProcessId, prototypeUid) {
      const targetProcess = processes().find((process2) => processId(process2) === ownerProcessId);
      if (targetProcess) {
        const targetUid = String(prototypeUid || "").trim();
        targetProcess.prototypeFiles = (Array.isArray(targetProcess.prototypeFiles) ? targetProcess.prototypeFiles : []).filter((file) => String(file.uid || file.id || "") !== targetUid);
        dirty();
        return;
      }
      legacyWindow.removeProcessPrototypeFile?.(ownerProcessId, prototypeUid);
    },
    uploadPrototypeFiles(ownerProcessId, inputId) {
      const targetProcess = processes().find((process2) => processId(process2) === ownerProcessId);
      const input2 = globalThis.document.getElementById(inputId);
      const files = Array.from(input2?.files || []);
      if (!targetProcess || !files.length)
        return;
      targetProcess.prototypeFiles ||= [];
      void (async () => {
        for (const file of files) {
          const staged = await uploadProcessAttachment(file);
          if (!staged.ok)
            throw new Error(staged.error || "\u9644\u4EF6\u4E0A\u4F20\u5931\u8D25");
          const versionUid = `protover-${Date.now()}-${Math.random().toString(16).slice(2)}`;
          const version = { uid: versionUid, number: 1, name: file.name, uploadToken: String(staged.token || ""), contentType: staged.contentType || file.type || "application/octet-stream", size: Number(staged.size || file.size || 0), uploadedAt: (/* @__PURE__ */ new Date()).toLocaleString() };
          const existing = targetProcess.prototypeFiles.find((item) => String(item.name || "").trim() === String(file.name || "").trim());
          if (existing) {
            existing.versions = [...existing.versions || [], __spreadProps(__spreadValues({}, version), { number: (existing.versions || []).length + 1 })];
            existing.versionUid = versionUid;
          } else {
            targetProcess.prototypeFiles.push({ uid: `proto-${Date.now()}-${Math.random().toString(16).slice(2)}`, id: `proto-${Date.now()}`, name: file.name, versionUid, versions: [version] });
          }
        }
        if (input2)
          input2.value = "";
        dirty();
      })().catch((error) => {
        if (input2)
          input2.value = "";
        console.error(error);
      });
    },
    nodePrototypeFiles(task) {
      return Array.isArray(task?.prototypeFiles) ? task.prototypeFiles : [];
    },
    removeNodePrototype(task, prototypeUid) {
      const targetUid = String(prototypeUid || "").trim();
      task.prototypeFiles = (Array.isArray(task.prototypeFiles) ? task.prototypeFiles : []).filter((file) => String(file.uid || file.id || "") !== targetUid);
      dirty();
    },
    uploadNodePrototypeFiles(task, inputId) {
      const input2 = globalThis.document.getElementById(inputId);
      const files = Array.from(input2?.files || []);
      if (!files.length)
        return;
      task.prototypeFiles ||= [];
      void (async () => {
        for (const file of files) {
          const staged = await uploadProcessAttachment(file);
          if (!staged.ok)
            throw new Error(staged.error || "\u9644\u4EF6\u4E0A\u4F20\u5931\u8D25");
          const versionUid = `node-proto-ver-${Date.now()}-${Math.random().toString(16).slice(2)}`;
          const version = {
            uid: versionUid,
            number: 1,
            name: file.name,
            uploadToken: String(staged.token || ""),
            contentType: staged.contentType || file.type || "application/octet-stream",
            size: Number(staged.size || file.size || 0),
            uploadedAt: (/* @__PURE__ */ new Date()).toLocaleString()
          };
          const existing = task.prototypeFiles.find((item) => String(item.name || "").trim() === String(file.name || "").trim());
          if (existing) {
            existing.versions = [...existing.versions || [], __spreadProps(__spreadValues({}, version), { number: (existing.versions || []).length + 1 })];
            existing.versionUid = versionUid;
          } else {
            task.prototypeFiles.push({ uid: `node-proto-${Date.now()}-${Math.random().toString(16).slice(2)}`, id: `node-proto-${Date.now()}`, name: file.name, versionUid, versions: [version] });
          }
        }
        if (input2)
          input2.value = "";
        dirty();
      })().catch((error) => {
        if (input2)
          input2.value = "";
        console.error(error);
      });
    },
    flowNodeOptions(process2, side) {
      const options = tasks(process2).map((task, index) => ({ id: taskId(task), label: task.name || `\u8282\u70B9${index + 1}` }));
      const gatewayOptions = gateways(process2).map((gateway, index) => ({ id: String(gateway.id || ""), label: gateway.title || `\u5206\u652F${index + 1}` }));
      return [
        ...side === "from" ? [{ id: "START", label: "\u5F00\u59CB" }] : [],
        ...options,
        ...gatewayOptions,
        ...side === "to" ? [{ id: "END", label: "\u7ED3\u675F" }] : []
      ];
    },
    renderDiagram(containerId, process2, onSelectTask) {
      if (!process2 || typeof legacyWindow.renderProcFlow !== "function")
        return false;
      const clickMap = {};
      for (const task of tasks(process2)) {
        const id = taskId(task);
        if (id)
          clickMap[id] = () => onSelectTask(id);
      }
      legacyWindow.renderProcFlow(containerId, process2, clickMap);
      return true;
    },
    selectTask(targetTaskId) {
      ui().taskId = targetTaskId;
      refreshUiOnly();
    },
    closeEditor() {
      ui().procView = "flow";
      ui().taskId = null;
      if (legacyWindow.renderProcessTab)
        legacyWindow.renderProcessTab();
      else
        emitRuntimeRefresh();
    },
    touch() {
      if (legacyWindow.markModified)
        legacyWindow.markModified();
      else
        markAngularRuntimeModified();
    },
    setProcessField(field, value) {
      const process2 = currentProcess();
      if (!process2)
        return;
      process2[field] = value;
      dirty();
    },
    setTaskField(task, field, value) {
      task[field] = value;
      dirty();
    },
    addTask(afterTaskId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = tasks(process2);
      const id = nextId("T", list);
      const node = { id, uid: id, name: `\u65B0\u8282\u70B9${list.length + 1}`, userSteps: [], forms: [], entity_ops: [], orchestrationTasks: [], businessRules: [] };
      const index = list.findIndex((item) => taskId(item) === afterTaskId);
      if (index >= 0)
        list.splice(index + 1, 0, node);
      else
        list.push(node);
      ui().taskId = id;
      dirty();
    },
    removeTask(targetTaskId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      process2.nodes = tasks(process2).filter((task) => taskId(task) !== targetTaskId);
      if (ui().taskId === targetTaskId)
        ui().taskId = null;
      dirty();
    },
    moveTask(targetTaskId, delta) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = tasks(process2);
      const index = list.findIndex((task) => taskId(task) === targetTaskId);
      const nextIndex = index + delta;
      if (index < 0 || nextIndex < 0 || nextIndex >= list.length)
        return;
      [list[index], list[nextIndex]] = [list[nextIndex], list[index]];
      dirty();
    },
    addGateway(afterGatewayId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = gateways(process2);
      const id = nextId("B", list);
      const gateway = { id, uid: id, kind: "gateway", gatewayType: "exclusive", title: "", role_id: "" };
      const index = list.findIndex((item) => String(item.id || item.uid) === afterGatewayId);
      if (index >= 0)
        list.splice(index + 1, 0, gateway);
      else
        list.push(gateway);
      dirty();
    },
    setGateway(gateway, field, value) {
      gateway[field] = value;
      dirty();
    },
    moveGateway(gatewayId, delta) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = gateways(process2);
      const index = list.findIndex((gateway) => String(gateway.id || gateway.uid) === gatewayId);
      const nextIndex = index + delta;
      if (index < 0 || nextIndex < 0 || nextIndex >= list.length)
        return;
      [list[index], list[nextIndex]] = [list[nextIndex], list[index]];
      dirty();
    },
    removeGateway(gatewayId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const flow = normalizeFlow(process2);
      flow.nodes = (flow.nodes || []).filter((gateway) => String(gateway.id || gateway.uid) !== gatewayId);
      flow.edges = (flow.edges || []).filter((edge) => edge.from !== gatewayId && edge.to !== gatewayId);
      dirty();
    },
    addEdge(afterEdgeId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = edges(process2);
      const id = nextId("L", list);
      const edge = { id, uid: id, from: "", to: "", label: "", condition: "" };
      const index = list.findIndex((item) => String(item.id || item.uid) === afterEdgeId);
      if (index >= 0)
        list.splice(index + 1, 0, edge);
      else
        list.push(edge);
      dirty();
    },
    setEdge(edge, field, value) {
      const nextValue = String(value || "").trim();
      if (field === "from" && nextValue === "END")
        return;
      if (field === "to" && nextValue === "START")
        return;
      edge[field] = nextValue;
      if (field === "label")
        edge.condition = nextValue;
      dirty();
    },
    moveEdge(edgeId, delta) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const list = edges(process2);
      const index = list.findIndex((edge) => String(edge.id || edge.uid) === edgeId);
      const nextIndex = index + delta;
      if (index < 0 || nextIndex < 0 || nextIndex >= list.length)
        return;
      [list[index], list[nextIndex]] = [list[nextIndex], list[index]];
      dirty();
    },
    removeEdge(edgeId) {
      const process2 = currentProcess();
      if (!process2)
        return;
      const flow = normalizeFlow(process2);
      flow.edges = (flow.edges || []).filter((edge) => String(edge.id || edge.uid) !== edgeId);
      dirty();
    },
    addUserStep(task) {
      task.userSteps ||= [];
      const id = nextId("US", task.userSteps);
      task.userSteps.push({ id, uid: id, name: "", note: "", type: "" });
      dirty();
    },
    setUserStep(task, index, value) {
      task.userSteps ||= [];
      task.userSteps[index] ||= {};
      task.userSteps[index].name = value;
      dirty();
    },
    moveUserStep(task, index, delta) {
      task.userSteps ||= [];
      const nextIndex = index + delta;
      if (nextIndex < 0 || nextIndex >= task.userSteps.length)
        return;
      [task.userSteps[index], task.userSteps[nextIndex]] = [task.userSteps[nextIndex], task.userSteps[index]];
      dirty();
    },
    removeUserStep(task, index) {
      task.userSteps ||= [];
      task.userSteps.splice(index, 1);
      dirty();
    },
    addForm(task) {
      task.forms ||= [];
      const id = nextId("F", task.forms);
      task.forms.push({ id, uid: id, name: `\u8868\u5355${task.forms.length + 1}`, fields: [] });
      dirty();
    },
    setFormName(_task, form, value) {
      form.name = value;
      dirty();
    },
    removeForm(task, form) {
      task.forms = (task.forms || []).filter((item) => item !== form);
      dirty();
    },
    addFormField(form) {
      form.fields ||= [];
      const id = nextId("FF", form.fields);
      form.fields.push({ id, uid: id, name: "", type: "text", required: false });
      dirty();
    },
    setFormField(field, key, value) {
      field[key] = value;
      dirty();
    },
    setFormFieldRequired(field, value) {
      field.required = value;
      dirty();
    },
    removeFormField(form, field) {
      form.fields = (form.fields || []).filter((item) => item !== field);
      dirty();
    },
    addEntityOp(task, entityId2) {
      if (!entityId2)
        return;
      task.entity_ops ||= [];
      if (!task.entity_ops.some((item) => String(item.entity_uid || item.entity_id || item.entityId) === entityId2)) {
        task.entity_ops.push({ entity_uid: entityId2, ops: ["R"] });
      }
      dirty();
    },
    toggleEntityOp(task, entityId2, op, checked) {
      task.entity_ops ||= [];
      const item = task.entity_ops.find((entry) => String(entry.entity_uid || entry.entity_id || entry.entityId) === entityId2);
      if (!item)
        return;
      item.ops ||= [];
      if (checked && !item.ops.includes(op))
        item.ops.push(op);
      if (!checked)
        item.ops = item.ops.filter((value) => value !== op);
      dirty();
    },
    removeEntityOp(task, entityId2) {
      task.entity_ops = (task.entity_ops || []).filter((item) => String(item.entity_uid || item.entity_id || item.entityId) !== entityId2);
      dirty();
    },
    addTaskDefinition(task) {
      task.orchestrationTasks ||= [];
      const id = nextId("TD", task.orchestrationTasks);
      task.orchestrationTasks.push({ id, uid: id, name: `\u4EFB\u52A1${task.orchestrationTasks.length + 1}`, target: "", address: "" });
      dirty();
    },
    setTaskDefinition(taskDefinition, key, value) {
      taskDefinition[key] = value;
      dirty();
    },
    removeTaskDefinition(task, taskDefinition) {
      task.orchestrationTasks = (task.orchestrationTasks || []).filter((item) => item !== taskDefinition);
      dirty();
    },
    createBusinessRule(task) {
      task.businessRules ||= [];
      const id = nextId("BR", task.businessRules.filter((item) => Boolean(item) && typeof item !== "string"));
      return { id, uid: id, name: `\u89C4\u5219${task.businessRules.length + 1}`, content: "" };
    },
    addBusinessRule(task) {
      task.businessRules ||= [];
      task.businessRules.push(this.createBusinessRule(task));
      dirty();
    },
    setBusinessRule(task, index, value) {
      task.businessRules ||= [];
      const current = task.businessRules[index];
      if (!current || typeof current === "string") {
        task.businessRules[index] = { id: `BR${index + 1}`, uid: `BR${index + 1}`, name: value, content: value };
      } else {
        current.content = value;
      }
      dirty();
    },
    removeBusinessRule(task, index) {
      task.businessRules ||= [];
      task.businessRules.splice(index, 1);
      dirty();
    }
  };
}

// src/app/workbenches/process/flow/process-flow-model.service.ts
var ProcessFlowModelService = class _ProcessFlowModelService {
  // 模块意图：流程视图的模型写操作只修改文档数据，不触发旧 DOM 渲染。
  tasks(process2) {
    if (!process2)
      return [];
    process2.nodes ||= process2.tasks || [];
    return process2.nodes;
  }
  gateways(process2) {
    return this.normalizeFlow(process2).nodes || [];
  }
  edges(process2) {
    return this.normalizeFlow(process2).edges || [];
  }
  addTask(process2) {
    const list = this.tasks(process2);
    const id = this.nextId("T", list);
    const task = {
      id,
      uid: id,
      name: `\u65B0\u8282\u70B9${list.length + 1}`,
      userSteps: [],
      forms: [],
      entity_ops: [],
      orchestrationTasks: [],
      businessRules: []
    };
    list.push(task);
    return task;
  }
  addGateway(process2) {
    const list = this.gateways(process2);
    const id = this.nextId("B", list);
    const gateway = { id, uid: id, kind: "gateway", gatewayType: "exclusive", title: "\u7F51\u5173", role_id: "" };
    list.push(gateway);
    return gateway;
  }
  addEdge(process2, from2 = "", to = "") {
    const list = this.edges(process2);
    const id = this.nextId("L", list);
    const edge = { id, uid: id, from: from2, to, label: "", condition: "" };
    list.push(edge);
    return edge;
  }
  setProcessField(process2, field, value) {
    process2[field] = value;
  }
  setTaskName(task, value) {
    task.name = value;
  }
  setTaskRoleIds(task, roleIds) {
    const ids = roleIds.map((item) => String(item || "").trim()).filter(Boolean);
    const roles = Array.isArray(getAngularRuntimeState().doc?.roles) ? getAngularRuntimeState().doc.roles : [];
    const roleById = new Map(roles.map((role) => [String(role.id || role.uid || "").trim(), role]));
    const roleNames = ids.map((id) => String(roleById.get(id)?.name || "").trim()).filter(Boolean);
    task.roleIds = ids;
    task.role_ids = ids;
    task.role_uids = ids;
    task.role_uid = ids[0] || "";
    task.role_id = ids[0] || "";
    task.roles = roleNames;
    task.role = roleNames.join("\u3001");
    if (!ids.length) {
      task.roles = [];
      task.role = "";
    }
  }
  setGateway(gateway, field, value) {
    gateway[field] = value;
  }
  setEdge(edge, field, value) {
    edge[field] = value;
    if (field === "label")
      edge.condition = value;
  }
  duplicateTask(process2, task) {
    const list = this.tasks(process2);
    const index = list.indexOf(task);
    if (index < 0)
      return null;
    const clone = structuredClone(task);
    const id = this.nextId("T", list);
    clone.id = id;
    clone.uid = id;
    const baseName = String(task.name || "").replace(/\s*副本(\d*)$/, "");
    const copies = list.filter((t) => t.name?.startsWith(baseName) && t.name !== task.name).length + 1;
    clone.name = `${baseName} \u526F\u672C${copies > 1 ? copies : ""}`;
    list.splice(index + 1, 0, clone);
    return clone;
  }
  duplicateGateway(process2, gateway) {
    const list = this.gateways(process2);
    const index = list.indexOf(gateway);
    if (index < 0)
      return null;
    const clone = structuredClone(gateway);
    const id = this.nextId("B", list);
    clone.id = id;
    clone.uid = id;
    const baseName = String(gateway.title || "").replace(/\s*副本(\d*)$/, "");
    const copies = list.filter((g) => g.title?.startsWith(baseName) && g.title !== gateway.title).length + 1;
    clone.title = `${baseName} \u526F\u672C${copies > 1 ? copies : ""}`;
    list.splice(index + 1, 0, clone);
    return clone;
  }
  removeElement(process2, id) {
    if (!id || id === "START" || id === "END")
      return;
    process2.nodes = this.tasks(process2).filter((task) => this.nodeId(task) !== id);
    const flow = this.normalizeFlow(process2);
    flow.nodes = (flow.nodes || []).filter((gateway) => this.nodeId(gateway) !== id);
    flow.edges = (flow.edges || []).filter((edge) => this.nodeId(edge) !== id && edge.from !== id && edge.to !== id);
  }
  swimlaneLayout(process2) {
    const flow = this.normalizeFlow(process2);
    const layout = flow.layout && typeof flow.layout === "object" ? flow.layout : {};
    layout.swimlane ||= { laneOrder: [], items: {}, labels: {} };
    layout.swimlane.laneOrder ||= [];
    layout.swimlane.items ||= {};
    layout.swimlane.labels ||= {};
    flow.layout = layout;
    return layout.swimlane;
  }
  setFlowOffset(process2, key, dx, dy) {
    const layout = this.swimlaneLayout(process2);
    layout.items ||= {};
    layout.items[key] = { dx: Math.round(dx), dy: Math.round(dy) };
  }
  setFlowLabelOffset(process2, key, dx, dy) {
    const layout = this.swimlaneLayout(process2);
    layout.labels ||= {};
    layout.labels[key] = { dx: Math.round(dx), dy: Math.round(dy) };
  }
  normalizeFlow(process2) {
    if (!process2)
      return { version: 2, orientation: "horizontal", nodes: [], edges: [] };
    const rawFlow = process2.flow && typeof process2.flow === "object" ? process2.flow : {};
    rawFlow.version = Number(rawFlow.version || 2) || 2;
    rawFlow.orientation = rawFlow.orientation === "vertical" ? "vertical" : "horizontal";
    rawFlow.nodes = Array.isArray(rawFlow.nodes) ? rawFlow.nodes : [];
    rawFlow.edges = Array.isArray(rawFlow.edges) ? rawFlow.edges : [];
    process2.flow = rawFlow;
    return rawFlow;
  }
  nextId(prefix, items) {
    const used = new Set(items.map((item) => this.nodeId(item)));
    for (let index = items.length + 1; index < items.length + 200; index += 1) {
      const id = `${prefix}${index}`;
      if (!used.has(id))
        return id;
    }
    return `${prefix}${Date.now()}`;
  }
  nodeId(item) {
    return String(item.id || item.uid || "").trim();
  }
  static \u0275fac = function ProcessFlowModelService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ProcessFlowModelService)();
  };
  static \u0275prov = /* @__PURE__ */ \u0275\u0275defineInjectable({ token: _ProcessFlowModelService, factory: _ProcessFlowModelService.\u0275fac, providedIn: "root" });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProcessFlowModelService, [{
    type: Injectable,
    args: [{ providedIn: "root" }]
  }], null, null);
})();

// src/app/workbenches/process/flow/process-flow-workbench.component.ts
function _forTrack02($index, $item) {
  return this.stageId($item);
}
function _forTrack17($index, $item) {
  return this.processId($item);
}
var _forTrack22 = ($index, $item) => $item.id;
function _forTrack32($index, $item) {
  return this.roleId($item);
}
var _forTrack42 = ($index, $item) => $item.uid || $item.id || $index;
var _forTrack52 = ($index, $item) => $item.uid || $index;
function ProcessFlowWorkbenchComponent_Conditional_1_For_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 6);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r3 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("value", ctx_r1.stageId(stage_r3));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.stageName(stage_r3));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "option", 6);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const item_r4 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("value", ctx_r1.processId(item_r4));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(item_r4.name || "\u672A\u547D\u540D\u6D41\u7A0B");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_26_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 13)(1, "span")(2, "strong");
    \u0275\u0275text(3, "\u53CC\u51FB");
    \u0275\u0275elementEnd();
    \u0275\u0275text(4, "\u53EF\u4EE5\u4FEE\u6539\u8282\u70B9\u3001\u7F51\u5173\u548C\u8FDE\u7EBF\u7684\u63CF\u8FF0");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "span", 26);
    \u0275\u0275text(6);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const process_r5 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(6);
    \u0275\u0275textInterpolate3("\u8282\u70B9 ", ctx_r1.taskCount(process_r5), " \xB7 \u7F51\u5173 ", ctx_r1.gatewayCount(process_r5), " \xB7 \u9644\u4EF6 ", ctx_r1.prototypeCount(process_r5));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 32);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_15_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r7);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.connectingFromId.set(""));
    });
    \u0275\u0275elementStart(1, "b");
    \u0275\u0275text(2, "!");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "span");
    \u0275\u0275text(4, "\u53D6\u6D88");
    \u0275\u0275elementEnd()();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 33);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_16_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r8);
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.duplicateSelected(process_r5));
    });
    \u0275\u0275elementStart(1, "b");
    \u0275\u0275text(2, "\u29C9");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "span");
    \u0275\u0275text(4, "\u590D\u5236");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(5, "button", 34);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_16_Template_button_click_5_listener() {
      \u0275\u0275restoreView(_r8);
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removeSelected(process_r5));
    });
    \u0275\u0275elementStart(6, "b");
    \u0275\u0275text(7, "x");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "span");
    \u0275\u0275text(9, "\u5220\u9664");
    \u0275\u0275elementEnd()();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 27);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.addTask());
    });
    \u0275\u0275elementStart(1, "b");
    \u0275\u0275text(2, "+");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "span");
    \u0275\u0275text(4, "\u8282\u70B9");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(5, "button", 28);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Template_button_click_5_listener() {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.addGateway());
    });
    \u0275\u0275elementStart(6, "b");
    \u0275\u0275text(7, "\u25C7");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(8, "span");
    \u0275\u0275text(9, "\u7F51\u5173");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(10, "button", 29);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Template_button_click_10_listener() {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.attachmentDrawerOpen.set(true));
    });
    \u0275\u0275elementStart(11, "b", 30);
    \u0275\u0275text(12, "\u25A1");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(13, "span");
    \u0275\u0275text(14, "\u9644\u4EF6");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(15, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_15_Template, 5, 0, "button", 31);
    \u0275\u0275conditionalCreate(16, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Conditional_16_Template, 10, 0);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(15);
    \u0275\u0275conditional(ctx_r1.connectingFromId() ? 15 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.selectedElementId() ? 16 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_30_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 35);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_30_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r9);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.attachmentDrawerOpen.set(true));
    });
    \u0275\u0275elementStart(1, "span");
    \u0275\u0275text(2, "\u9644\u4EF6");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "b");
    \u0275\u0275text(4);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const process_r5 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(4);
    \u0275\u0275textInterpolate(ctx_r1.prototypeCount(process_r5));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_34_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 36);
    \u0275\u0275listener("dragover", function ProcessFlowWorkbenchComponent_Conditional_1_For_34_Template_div_dragover_0_listener($event) {
      return $event.preventDefault();
    })("drop", function ProcessFlowWorkbenchComponent_Conditional_1_For_34_Template_div_drop_0_listener($event) {
      const lane_r11 = \u0275\u0275restoreView(_r10).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.onLaneDrop($event, lane_r11));
    });
    \u0275\u0275elementStart(1, "div", 37);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const lane_r11 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("--lane-color", ctx_r1.laneColor(lane_r11.name))("top", lane_r11.top, "px")("height", lane_r11.height, "px");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(lane_r11.name);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_36_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275element(0, "div", 38);
  }
  if (rf & 2) {
    const guide_r12 = ctx.$implicit;
    \u0275\u0275styleProp("left", guide_r12.axis === "x" ? guide_r12.position : guide_r12.from, "px")("top", guide_r12.axis === "y" ? guide_r12.position : guide_r12.from, "px")("width", guide_r12.axis === "y" ? guide_r12.to - guide_r12.from : null, "px")("height", guide_r12.axis === "x" ? guide_r12.to - guide_r12.from : null, "px");
    \u0275\u0275classProp("is-vertical", guide_r12.axis === "x")("is-horizontal", guide_r12.axis === "y");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = \u0275\u0275getCurrentView();
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(0, "text", 42);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Conditional_2_Template_text_click_0_listener($event) {
      \u0275\u0275restoreView(_r15);
      const edge_r14 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openEdgeLabelEditor(edge_r14.baseId, $event));
    })("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Conditional_2_Template_text_pointerdown_0_listener($event) {
      \u0275\u0275restoreView(_r15);
      const edge_r14 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startEdgeLabelDrag(edge_r14, $event));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const edge_r14 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275attribute("x", edge_r14.labelX)("y", edge_r14.labelY);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(edge_r14.label);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = \u0275\u0275getCurrentView();
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(0, "path", 39);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Template_path_click_0_listener($event) {
      const edge_r14 = \u0275\u0275restoreView(_r13).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openEdgeLabelEditor(edge_r14.baseId, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(1, "path", 40);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_42_Template_path_click_1_listener($event) {
      const edge_r14 = \u0275\u0275restoreView(_r13).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openEdgeLabelEditor(edge_r14.baseId, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(2, ProcessFlowWorkbenchComponent_Conditional_1_For_42_Conditional_2_Template, 2, 3, ":svg:text", 41);
  }
  if (rf & 2) {
    const edge_r14 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275attribute("d", edge_r14.d);
    \u0275\u0275advance();
    \u0275\u0275classProp("active", ctx_r1.selectedElementId() === edge_r14.baseId);
    \u0275\u0275attribute("d", edge_r14.d);
    \u0275\u0275advance();
    \u0275\u0275conditional(edge_r14.label ? 2 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_43_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275namespaceSVG();
    \u0275\u0275element(0, "path", 24);
  }
  if (rf & 2) {
    \u0275\u0275attribute("d", ctx.d);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 47);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_0_Template_button_pointerdown_0_listener($event) {
      \u0275\u0275restoreView(_r16);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeDrag($event, node_r17));
    })("pointerup", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_0_Template_button_pointerup_0_listener($event) {
      \u0275\u0275restoreView(_r16);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.endNodeDrag($event, node_r17));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_0_Template_button_click_0_listener($event) {
      \u0275\u0275restoreView(_r16);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      $event.stopPropagation();
      return \u0275\u0275resetView(ctx_r1.connectingFromId() ? ctx_r1.finishConnect(node_r17.id) : ctx_r1.selectElement(node_r17.id));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("left", node_r17.x, "px")("top", node_r17.y, "px");
    \u0275\u0275classProp("active", ctx_r1.selectedElementId() === node_r17.id)("connecting", ctx_r1.connectingFromId() === node_r17.id)("is-end", node_r17.kind === "end");
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(node_r17.name);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r19 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 52);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template_input_ngModelChange_0_listener($event) {
      \u0275\u0275restoreView(_r19);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setEditableNodeName(node_r17, $event));
    })("keydown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template_input_keydown_0_listener($event) {
      \u0275\u0275restoreView(_r19);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleNodeNameKeydown($event));
    })("blur", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template_input_blur_0_listener($event) {
      \u0275\u0275restoreView(_r19);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.finishNodeNameEdit($event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template_input_click_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("id", ctx_r1.nodeNameInputId(node_r17))("ngModel", ctx_r1.editableNodeName(node_r17));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r20 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 53);
    \u0275\u0275listener("dblclick", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_2_Template_span_dblclick_0_listener($event) {
      \u0275\u0275restoreView(_r20);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeNameEdit(node_r17, $event));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(node_r17.name);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r21 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 54);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_3_Template_span_pointerdown_0_listener($event) {
      return $event.stopPropagation();
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_3_Template_span_click_0_listener($event) {
      \u0275\u0275restoreView(_r21);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeNameEdit(node_r17, $event));
    });
    \u0275\u0275text(1, "\u270E");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 48);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template_div_pointerdown_0_listener($event) {
      \u0275\u0275restoreView(_r18);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeDrag($event, node_r17));
    })("pointerup", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template_div_pointerup_0_listener($event) {
      \u0275\u0275restoreView(_r18);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.endNodeDrag($event, node_r17));
    })("dblclick", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template_div_dblclick_0_listener($event) {
      \u0275\u0275restoreView(_r18);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeNameEdit(node_r17, $event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template_div_click_0_listener($event) {
      \u0275\u0275restoreView(_r18);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      $event.stopPropagation();
      return \u0275\u0275resetView(ctx_r1.connectingFromId() ? ctx_r1.finishConnect(node_r17.baseId) : ctx_r1.selectElement(node_r17.baseId));
    });
    \u0275\u0275conditionalCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_1_Template, 1, 2, "input", 49)(2, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_2_Template, 2, 1, "span", 50);
    \u0275\u0275conditionalCreate(3, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Conditional_3_Template, 2, 0, "span", 51);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("--lane-color", ctx_r1.laneColor(node_r17.role))("left", node_r17.x, "px")("top", node_r17.y, "px")("width", node_r17.width, "px")("height", node_r17.height, "px");
    \u0275\u0275classProp("active", ctx_r1.selectedElementId() === node_r17.baseId)("connecting", ctx_r1.connectingFromId() === node_r17.baseId);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.editing && ctx_r1.isEditingNodeName(node_r17) && node_r17.gateway ? 1 : 2);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.editing ? 3 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 56);
    \u0275\u0275text(1, "\u5171\u4EAB");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r23 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 61);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template_input_ngModelChange_0_listener($event) {
      \u0275\u0275restoreView(_r23);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setTaskName(node_r17.task, $event));
    })("keydown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template_input_keydown_0_listener($event) {
      \u0275\u0275restoreView(_r23);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleNodeNameKeydown($event));
    })("blur", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template_input_blur_0_listener($event) {
      \u0275\u0275restoreView(_r23);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.finishNodeNameEdit($event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template_input_click_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("id", ctx_r1.nodeNameInputId(node_r17))("ngModel", node_r17.task.name || "");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "strong");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(node_r17.name);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r24 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 62);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_5_Template_span_pointerdown_0_listener($event) {
      return $event.stopPropagation();
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_5_Template_span_click_0_listener($event) {
      \u0275\u0275restoreView(_r24);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.renameFlowNode(node_r17, $event));
    });
    \u0275\u0275text(1, "\u270E");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r26 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 66);
    \u0275\u0275text(1);
    \u0275\u0275elementStart(2, "button", 67);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_For_2_Template_button_click_2_listener($event) {
      const roleName_r27 = \u0275\u0275restoreView(_r26).$implicit;
      const node_r17 = \u0275\u0275nextContext(3).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.removeNodeRole(node_r17, roleName_r27, $event));
    });
    \u0275\u0275text(3, "\xD7");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const roleName_r27 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(5);
    \u0275\u0275styleProp("--lane-color", ctx_r1.laneColor(roleName_r27));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(roleName_r27);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_For_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r29 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "label", 69)(1, "input", 72);
    \u0275\u0275listener("change", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_For_4_Template_input_change_1_listener($event) {
      const role_r30 = \u0275\u0275restoreView(_r29).$implicit;
      const node_r17 = \u0275\u0275nextContext(4).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.toggleTaskRole(node_r17.task, ctx_r1.roleId(role_r30), $event.target.checked));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(2, "span");
    \u0275\u0275text(3);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const role_r30 = ctx.$implicit;
    const node_r17 = \u0275\u0275nextContext(4).$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275property("checked", ctx_r1.taskRoleIds(node_r17.task).includes(ctx_r1.roleId(role_r30)));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(role_r30.name || ctx_r1.roleId(role_r30));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r28 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 68);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_Template_div_click_0_listener($event) {
      return $event.stopPropagation();
    })("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_Template_div_pointerdown_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementStart(1, "strong");
    \u0275\u0275text(2, "\u6267\u884C\u89D2\u8272");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(3, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_For_4_Template, 4, 2, "label", 69, _forTrack32, true);
    \u0275\u0275elementStart(5, "div", 70)(6, "button", 71);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_Template_button_click_6_listener($event) {
      \u0275\u0275restoreView(_r28);
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.closeCardRolePicker($event));
    });
    \u0275\u0275text(7, "\u5B8C\u6210");
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(5);
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.roles());
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r25 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 60);
    \u0275\u0275repeaterCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_For_2_Template, 4, 3, "span", 63, \u0275\u0275repeaterTrackByIdentity);
    \u0275\u0275elementStart(3, "button", 64);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Template_button_click_3_listener($event) {
      \u0275\u0275restoreView(_r25);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openNodeRolePicker(node_r17, $event));
    });
    \u0275\u0275text(4, "+ \u89D2\u8272");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(5, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Conditional_5_Template, 8, 0, "div", 65);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.nodeRoleNames(node_r17));
    \u0275\u0275advance(4);
    \u0275\u0275conditional(ctx_r1.isCardRolePickerOpen(node_r17) && node_r17.task ? 5 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "small");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(node_r17.role || "\u672A\u5206\u914D\u89D2\u8272");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r22 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 55);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template_div_pointerdown_0_listener($event) {
      \u0275\u0275restoreView(_r22);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeDrag($event, node_r17));
    })("pointerup", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template_div_pointerup_0_listener($event) {
      \u0275\u0275restoreView(_r22);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.endNodeDrag($event, node_r17));
    })("dblclick", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template_div_dblclick_0_listener($event) {
      \u0275\u0275restoreView(_r22);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startNodeNameEdit(node_r17, $event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template_div_click_0_listener($event) {
      \u0275\u0275restoreView(_r22);
      const node_r17 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      $event.stopPropagation();
      return \u0275\u0275resetView(ctx_r1.editing ? ctx_r1.handleNodeClick(node_r17) : ctx_r1.openNodeEditor(node_r17));
    });
    \u0275\u0275conditionalCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_1_Template, 2, 0, "span", 56);
    \u0275\u0275elementStart(2, "div", 57);
    \u0275\u0275conditionalCreate(3, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_3_Template, 1, 2, "input", 58)(4, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_4_Template, 2, 1, "strong");
    \u0275\u0275conditionalCreate(5, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_5_Template, 2, 0, "span", 59);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(6, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_6_Template, 6, 1, "div", 60)(7, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Conditional_7_Template, 2, 1, "small");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("--lane-color", ctx_r1.laneColor(node_r17.role))("left", node_r17.x, "px")("top", node_r17.y, "px");
    \u0275\u0275classProp("active", ctx_r1.selectedElementId() === node_r17.baseId)("connecting", ctx_r1.connectingFromId() === node_r17.baseId)("is-editing", ctx_r1.editing);
    \u0275\u0275attribute("data-base-id", node_r17.baseId)("data-role", node_r17.role);
    \u0275\u0275advance();
    \u0275\u0275conditional(node_r17.shared ? 1 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.editing && ctx_r1.isEditingNodeName(node_r17) && node_r17.task ? 3 : 4);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.editing ? 5 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.editing ? 6 : 7);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r31 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 75);
    \u0275\u0275listener("pointerdown", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_For_2_Template_button_pointerdown_0_listener($event) {
      \u0275\u0275restoreView(_r31);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startConnectFromAnchor($event, node_r17.baseId));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_For_2_Template_button_click_0_listener($event) {
      \u0275\u0275restoreView(_r31);
      const node_r17 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startConnectFromAnchor($event, node_r17.baseId));
    });
    \u0275\u0275text(1, "+");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const anchor_r32 = ctx.$implicit;
    const node_r17 = \u0275\u0275nextContext(2).$implicit;
    \u0275\u0275classMap(anchor_r32.side);
    \u0275\u0275styleProp("left", anchor_r32.x - node_r17.x, "px")("top", anchor_r32.y - node_r17.y, "px");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 73);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_Template_div_click_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275repeaterCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_For_2_Template, 2, 6, "button", 74, _forTrack22);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r17 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("left", node_r17.x - 2, "px")("top", node_r17.y - 2, "px")("width", node_r17.width + 4, "px")("height", node_r17.height + 4, "px");
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.connectAnchors(node_r17));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_For_45_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_0_Template, 2, 11, "button", 43)(1, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_1_Template, 4, 16, "div", 44)(2, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_2_Template, 8, 18, "div", 45);
    \u0275\u0275conditionalCreate(3, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Conditional_3_Template, 3, 8, "div", 46);
  }
  if (rf & 2) {
    const node_r17 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional(node_r17.kind === "start" || node_r17.kind === "end" ? 0 : node_r17.kind === "gateway" ? 1 : 2);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.editing && (ctx_r1.selectedElementId() === node_r17.baseId || ctx_r1.selectedElementId() === node_r17.id) ? 3 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r33 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 77);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template_input_ngModelChange_0_listener($event) {
      \u0275\u0275restoreView(_r33);
      const edge_r34 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.setEdge(edge_r34, "label", $event));
    })("keydown", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template_input_keydown_0_listener($event) {
      \u0275\u0275restoreView(_r33);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.handleEdgeLabelKeydown($event));
    })("blur", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template_input_blur_0_listener($event) {
      \u0275\u0275restoreView(_r33);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.finishEdgeLabelEdit($event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template_input_click_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const canvasEdge_r35 = \u0275\u0275nextContext();
    const edge_r34 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("left", canvasEdge_r35.labelX, "px")("top", canvasEdge_r35.labelY - 18, "px");
    \u0275\u0275property("id", ctx_r1.edgeLabelInputId(ctx_r1.edgeId(edge_r34)))("ngModel", edge_r34.label || edge_r34.condition || "");
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Conditional_0_Template, 1, 6, "input", 76);
  }
  if (rf & 2) {
    const edge_r34 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275conditional(ctx_r1.editingEdgeLabelId() === ctx_r1.edgeId(edge_r34) ? 0 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Conditional_0_Template, 1, 1);
  }
  if (rf & 2) {
    let tmp_4_0;
    const process_r5 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275conditional((tmp_4_0 = ctx_r1.selectedFlowEdge(process_r5)) ? 0 : -1, tmp_4_0);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r37 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 83);
    \u0275\u0275listener("change", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Conditional_8_Template_input_change_0_listener() {
      \u0275\u0275restoreView(_r37);
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.uploadPrototypeFilesImmediately(process_r5));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(1, "label", 84);
    \u0275\u0275text(2, "+ \u6DFB\u52A0\u9644\u4EF6");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const process_r5 = \u0275\u0275nextContext(2);
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275property("id", ctx_r1.prototypeInputId(process_r5));
    \u0275\u0275advance();
    \u0275\u0275property("for", ctx_r1.prototypeInputId(process_r5));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r40 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 93);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_11_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r40);
      const file_r39 = \u0275\u0275nextContext().$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.previewPrototype(process_r5, file_r39));
    });
    \u0275\u0275text(1, "\u9884\u89C8");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r41 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 94);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_14_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r41);
      const file_r39 = \u0275\u0275nextContext().$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.removePrototype(process_r5, file_r39));
    });
    \u0275\u0275text(1, "\u5220\u9664");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r43 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 98);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Conditional_7_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r43);
      const version_r44 = \u0275\u0275nextContext().$implicit;
      const file_r39 = \u0275\u0275nextContext(2).$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.previewPrototype(process_r5, file_r39, version_r44));
    });
    \u0275\u0275text(1, "\u9884\u89C8");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r42 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 95)(1, "div")(2, "strong");
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "span");
    \u0275\u0275text(5);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(6, "div");
    \u0275\u0275conditionalCreate(7, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Conditional_7_Template, 2, 0, "button", 96);
    \u0275\u0275elementStart(8, "button", 97);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Template_button_click_8_listener() {
      const version_r44 = \u0275\u0275restoreView(_r42).$implicit;
      const file_r39 = \u0275\u0275nextContext(2).$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.downloadPrototype(process_r5, file_r39, version_r44));
    });
    \u0275\u0275text(9, "\u4E0B\u8F7D");
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const version_r44 = ctx.$implicit;
    const $index_r45 = ctx.$index;
    const file_r39 = \u0275\u0275nextContext(2).$implicit;
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate2("v", version_r44.number || $index_r45 + 1, "", version_r44.uid === file_r39.versionUid ? " \xB7 \u5F53\u524D\u5F15\u7528" : "");
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(version_r44.uploadedAt || "\u672A\u8BB0\u5F55\u4E0A\u4F20\u65F6\u95F4");
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.canPreviewPrototype(file_r39, version_r44) ? 7 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 92);
    \u0275\u0275repeaterCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_For_2_Template, 10, 4, "div", 95, _forTrack52);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const file_r39 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275repeater(file_r39.versions);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r38 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 81)(1, "div", 85)(2, "strong", 86);
    \u0275\u0275text(3);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "span");
    \u0275\u0275text(5);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "small");
    \u0275\u0275text(7);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(8, "div", 87)(9, "button", 88);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Template_button_click_9_listener() {
      const file_r39 = \u0275\u0275restoreView(_r38).$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.togglePrototypeVersions(process_r5, file_r39));
    });
    \u0275\u0275text(10);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(11, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_11_Template, 2, 0, "button", 89);
    \u0275\u0275elementStart(12, "button", 90);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Template_button_click_12_listener() {
      const file_r39 = \u0275\u0275restoreView(_r38).$implicit;
      const process_r5 = \u0275\u0275nextContext(2);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.downloadPrototype(process_r5, file_r39));
    });
    \u0275\u0275text(13, "\u4E0B\u8F7D");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(14, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_14_Template, 2, 0, "button", 91);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(15, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Conditional_15_Template, 3, 0, "div", 92);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const file_r39 = ctx.$implicit;
    const \u0275$index_269_r46 = ctx.$index;
    const process_r5 = \u0275\u0275nextContext(2);
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(2);
    \u0275\u0275property("title", ctx_r1.prototypeFileName(file_r39, \u0275$index_269_r46));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.prototypeFileName(file_r39, \u0275$index_269_r46));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(ctx_r1.prototypeMeta(file_r39));
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(ctx_r1.prototypeKind(file_r39));
    \u0275\u0275advance(3);
    \u0275\u0275textInterpolate1("", ctx_r1.isPrototypeExpanded(process_r5, file_r39) ? "\u6536\u8D77" : "\u5C55\u5F00", "\u7248\u672C");
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.canPreviewPrototype(file_r39) ? 11 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.editing ? 14 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.isPrototypeExpanded(process_r5, file_r39) && (file_r39.versions == null ? null : file_r39.versions.length) ? 15 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_ForEmpty_12_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 82);
    \u0275\u0275text(1, "\u6682\u65E0\u9644\u4EF6");
    \u0275\u0275elementEnd();
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Template(rf, ctx) {
  if (rf & 1) {
    const _r36 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "aside", 25)(1, "div", 78)(2, "strong");
    \u0275\u0275text(3, "\u6D41\u7A0B\u539F\u578B/\u9644\u4EF6");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(4, "span");
    \u0275\u0275text(5);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(6, "button", 79);
    \u0275\u0275listener("click", function ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Template_button_click_6_listener() {
      \u0275\u0275restoreView(_r36);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.attachmentDrawerOpen.set(false));
    });
    \u0275\u0275text(7, "x");
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(8, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Conditional_8_Template, 3, 2);
    \u0275\u0275elementStart(9, "div", 80);
    \u0275\u0275repeaterCreate(10, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_For_11_Template, 16, 8, "div", 81, _forTrack42, false, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_ForEmpty_12_Template, 2, 0, "div", 82);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const process_r5 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(5);
    \u0275\u0275textInterpolate1("", ctx_r1.prototypeCount(process_r5), "\u9879");
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.editing ? 8 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.prototypeFiles(process_r5));
  }
}
function ProcessFlowWorkbenchComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "section", 1)(1, "header", 3)(2, "label", 4)(3, "span");
    \u0275\u0275text(4, "\u9636\u6BB5");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "select", 5);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Template_select_ngModelChange_5_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.selectStage($event));
    });
    \u0275\u0275repeaterCreate(6, ProcessFlowWorkbenchComponent_Conditional_1_For_7_Template, 2, 2, "option", 6, _forTrack02, true);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(8, "label", 4)(9, "span");
    \u0275\u0275text(10, "\u6D41\u7A0B");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(11, "select", 7);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Template_select_ngModelChange_11_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.selectProcess($event));
    });
    \u0275\u0275repeaterCreate(12, ProcessFlowWorkbenchComponent_Conditional_1_For_13_Template, 2, 2, "option", 6, _forTrack17, true);
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(14, "label", 8)(15, "span");
    \u0275\u0275text(16, "\u540D\u79F0");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(17, "input", 9);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Template_input_ngModelChange_17_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setProcessField("name", $event));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(18, "label", 10)(19, "span");
    \u0275\u0275text(20, "\u89E6\u53D1");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(21, "input", 11);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Template_input_ngModelChange_21_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setProcessField("trigger", $event));
    });
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(22, "label", 10)(23, "span");
    \u0275\u0275text(24, "\u9884\u671F");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(25, "input", 12);
    \u0275\u0275listener("ngModelChange", function ProcessFlowWorkbenchComponent_Conditional_1_Template_input_ngModelChange_25_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.setProcessField("outcome", $event));
    });
    \u0275\u0275elementEnd()()();
    \u0275\u0275conditionalCreate(26, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_26_Template, 7, 3, "div", 13);
    \u0275\u0275elementStart(27, "section", 14)(28, "div", 15);
    \u0275\u0275conditionalCreate(29, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_29_Template, 17, 2);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(30, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_30_Template, 5, 1, "button", 16);
    \u0275\u0275elementStart(31, "section", 17);
    \u0275\u0275listener("wheel", function ProcessFlowWorkbenchComponent_Conditional_1_Template_section_wheel_31_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onCanvasWheel($event));
    });
    \u0275\u0275elementStart(32, "div", 18);
    \u0275\u0275listener("mousemove", function ProcessFlowWorkbenchComponent_Conditional_1_Template_div_mousemove_32_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.onCanvasPointer($event));
    })("pointermove", function ProcessFlowWorkbenchComponent_Conditional_1_Template_div_pointermove_32_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.moveNodeDrag($event));
    })("pointerup", function ProcessFlowWorkbenchComponent_Conditional_1_Template_div_pointerup_32_listener($event) {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.finishNodeDrag($event));
    })("click", function ProcessFlowWorkbenchComponent_Conditional_1_Template_div_click_32_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext();
      return \u0275\u0275resetView(ctx_r1.clearCanvasSelection());
    });
    \u0275\u0275repeaterCreate(33, ProcessFlowWorkbenchComponent_Conditional_1_For_34_Template, 3, 7, "div", 19, _forTrack22);
    \u0275\u0275repeaterCreate(35, ProcessFlowWorkbenchComponent_Conditional_1_For_36_Template, 1, 12, "div", 20, _forTrack22);
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(37, "svg", 21)(38, "defs")(39, "marker", 22);
    \u0275\u0275element(40, "path", 23);
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(41, ProcessFlowWorkbenchComponent_Conditional_1_For_42_Template, 3, 5, null, null, _forTrack22);
    \u0275\u0275conditionalCreate(43, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_43_Template, 1, 1, ":svg:path", 24);
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(44, ProcessFlowWorkbenchComponent_Conditional_1_For_45_Template, 4, 2, null, null, _forTrack22);
    \u0275\u0275conditionalCreate(46, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_46_Template, 1, 1);
    \u0275\u0275elementEnd()();
    \u0275\u0275conditionalCreate(47, ProcessFlowWorkbenchComponent_Conditional_1_Conditional_47_Template, 13, 3, "aside", 25);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    let tmp_25_0;
    let tmp_27_0;
    const process_r5 = ctx;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(5);
    \u0275\u0275property("ngModel", ctx_r1.currentStageId(process_r5));
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.stages());
    \u0275\u0275advance(5);
    \u0275\u0275property("ngModel", ctx_r1.processId(process_r5));
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.processesForStage(ctx_r1.currentStageId(process_r5)));
    \u0275\u0275advance(5);
    \u0275\u0275property("readonly", !ctx_r1.editing)("ngModel", process_r5.name || "");
    \u0275\u0275advance(4);
    \u0275\u0275property("readonly", !ctx_r1.editing)("ngModel", process_r5.trigger || "");
    \u0275\u0275advance(4);
    \u0275\u0275property("readonly", !ctx_r1.editing)("ngModel", process_r5.outcome || "");
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.editing ? 26 : -1);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.editing ? 29 : -1);
    \u0275\u0275advance();
    \u0275\u0275conditional(!ctx_r1.editing ? 30 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275styleProp("width", ctx_r1.canvasWidth(process_r5), "px")("height", ctx_r1.canvasHeight(process_r5), "px")("zoom", ctx_r1.zoomValue());
    \u0275\u0275attribute("data-export-graph-id", ctx_r1.exportGraphId || null)("data-export-process-id", ctx_r1.processId(process_r5));
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.lanes(process_r5));
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.alignmentGuides());
    \u0275\u0275advance(2);
    \u0275\u0275attribute("width", ctx_r1.canvasWidth(process_r5))("height", ctx_r1.canvasHeight(process_r5));
    \u0275\u0275advance(4);
    \u0275\u0275repeater(ctx_r1.flowEdges(process_r5));
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_25_0 = ctx_r1.previewEdge(process_r5)) ? 43 : -1, tmp_25_0);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.flowNodes(process_r5));
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_27_0 = ctx_r1.selectedEdge(process_r5)) ? 46 : -1, tmp_27_0);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.attachmentDrawerOpen() ? 47 : -1);
  }
}
function ProcessFlowWorkbenchComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 2);
    \u0275\u0275text(1, "\u6682\u65E0\u6D41\u7A0B\u3002\u8BF7\u5148\u5728\u9636\u6BB5\u89C6\u56FE\u65B0\u589E\u6D41\u7A0B\u3002");
    \u0275\u0275elementEnd();
  }
}
var ProcessFlowWorkbenchComponent = class _ProcessFlowWorkbenchComponent {
  // 远端同步后通过 blm-workbench-refresh 事件刷新视图
  onRefresh = () => {
    this.version.update((v) => v + 1);
    this.syncAttachmentDrawer(this.currentProcess());
  };
  attachmentStateProcessId = "";
  ngOnInit() {
    window.addEventListener("blm-workbench-refresh", this.onRefresh);
    this.syncAttachmentDrawer(this.currentProcess(), true);
  }
  ngOnDestroy() {
    window.removeEventListener("blm-workbench-refresh", this.onRefresh);
  }
  // Module intent: this view owns process structure; node details stay in the node view.
  version = signal(0, ...ngDevMode ? [{ debugName: "version" }] : (
    /* istanbul ignore next */
    []
  ));
  selectedElementId = signal("", ...ngDevMode ? [{ debugName: "selectedElementId" }] : (
    /* istanbul ignore next */
    []
  ));
  connectingFromId = signal("", ...ngDevMode ? [{ debugName: "connectingFromId" }] : (
    /* istanbul ignore next */
    []
  ));
  rolePickerOpen = signal(false, ...ngDevMode ? [{ debugName: "rolePickerOpen" }] : (
    /* istanbul ignore next */
    []
  ));
  editingNodeNameId = signal("", ...ngDevMode ? [{ debugName: "editingNodeNameId" }] : (
    /* istanbul ignore next */
    []
  ));
  editingEdgeLabelId = signal("", ...ngDevMode ? [{ debugName: "editingEdgeLabelId" }] : (
    /* istanbul ignore next */
    []
  ));
  cardRolePickerNodeId = signal("", ...ngDevMode ? [{ debugName: "cardRolePickerNodeId" }] : (
    /* istanbul ignore next */
    []
  ));
  // 模块意图：流程视图进入后直接展示流程附件，避免附件存在但被折叠造成不可见。
  // 关键流程：用户仍可通过关闭按钮收起，上传完成后保持打开状态。
  // 边界细节：这里只改变流程视图的初始展示状态，不改变附件数据或编辑权限。
  attachmentDrawerOpen = signal(false, ...ngDevMode ? [{ debugName: "attachmentDrawerOpen" }] : (
    /* istanbul ignore next */
    []
  ));
  zoomValue = signal(1, ...ngDevMode ? [{ debugName: "zoomValue" }] : (
    /* istanbul ignore next */
    []
  ));
  dragState = signal(null, ...ngDevMode ? [{ debugName: "dragState" }] : (
    /* istanbul ignore next */
    []
  ));
  labelDragState = signal(null, ...ngDevMode ? [{ debugName: "labelDragState" }] : (
    /* istanbul ignore next */
    []
  ));
  dragPreview = signal(null, ...ngDevMode ? [{ debugName: "dragPreview" }] : (
    /* istanbul ignore next */
    []
  ));
  previewPoint = signal(null, ...ngDevMode ? [{ debugName: "previewPoint" }] : (
    /* istanbul ignore next */
    []
  ));
  adapter = createProcessEditorLegacyAdapter();
  flowModel = inject2(ProcessFlowModelService);
  laneHeight = 96;
  laneTitleWidth = 86;
  nodeWidth = 132;
  nodeHeight = 54;
  gatewaySize = 22;
  terminalWidth = 50;
  terminalHeight = 18;
  // 对齐旧版 process.js 布局常量：firstNodeX=180, colW=180, startX=130
  graphStartX = 118;
  graphNodeStartX = 208;
  columnGap = 180;
  snapThreshold = 6;
  lanePalette = ["#2563eb", "#d97706", "#059669", "#7c3aed", "#dc2626", "#0891b2", "#4f46e5", "#65a30d", "#c026d3", "#ea580c"];
  editing = true;
  exportGraphId = "";
  previewProcessId = "";
  taskEditorRequested = new EventEmitter();
  processes() {
    this.version();
    return this.adapter.processes();
  }
  stages() {
    this.version();
    return this.adapter.stages();
  }
  currentProcess() {
    this.version();
    const previewId = String(this.previewProcessId || "").trim();
    if (previewId) {
      return this.adapter.processes().find((process2) => this.processId(process2) === previewId || process2.id === previewId || process2.uid === previewId) || null;
    }
    return this.adapter.currentProcess();
  }
  processId(process2) {
    return this.adapter.processId(process2);
  }
  taskId(task) {
    return this.adapter.taskId(task);
  }
  gatewayId(gateway) {
    return String(gateway.id || gateway.uid || "");
  }
  edgeId(edge) {
    return String(edge.id || edge.uid || "");
  }
  tasks(process2) {
    this.version();
    return this.flowModel.tasks(process2);
  }
  gateways(process2) {
    this.version();
    return this.flowModel.gateways(process2);
  }
  edges(process2) {
    this.version();
    return this.flowModel.edges(process2);
  }
  roles() {
    this.version();
    return this.adapter.roles();
  }
  stageId(stage) {
    return String(stage?.id || stage?.uid || "").trim();
  }
  stageName(stage) {
    return stage?.name || this.stageId(stage) || "\u672A\u547D\u540D\u9636\u6BB5";
  }
  roleId(role) {
    return String(role.id || role.uid || "");
  }
  roleName(roleId) {
    const raw = String(roleId || "").trim();
    if (!raw)
      return "";
    const role = this.roles().find((item) => this.roleId(item) === raw || item.name === raw);
    return role?.name || raw;
  }
  taskRoleId(task) {
    return this.adapter.taskRoleIds(task)[0] || task.role_id || task.role || "";
  }
  taskRoleIds(task) {
    const ids = this.adapter.taskRoleIds(task);
    return ids.length ? ids : [task.role_id || task.role || ""].filter(Boolean);
  }
  taskRoleSummary(task) {
    const names = this.taskRoleIds(task).map((roleId) => this.roleName(roleId)).filter(Boolean);
    return names.length ? names.join("\u3001") : "\u9009\u62E9\u6267\u884C\u89D2\u8272";
  }
  // 编辑态点击节点：选中并显示连线锚点
  handleNodeClick(node) {
    if (this.connectingFromId()) {
      this.finishConnect(node.baseId);
    } else {
      this.selectElement(node.baseId);
    }
  }
  // 非编辑态点击节点：跳转节点视图
  openNodeEditor(node) {
    if (node.task) {
      this.adapter.selectTask(this.taskId(node.task));
      this.taskEditorRequested.emit(this.taskId(node.task));
    }
  }
  // 节点角色名列表
  nodeRoleNames(node) {
    if (!node.task)
      return [];
    return this.taskRoleIds(node.task).map((roleId) => this.roleName(roleId)).filter(Boolean);
  }
  // 移除节点角色
  removeNodeRole(node, roleName, event) {
    event.stopPropagation();
    if (!node.task)
      return;
    const roleId = this.taskRoleIds(node.task).find((id) => this.roleName(id) === roleName);
    if (roleId) {
      this.flowModel.setTaskRoleIds(node.task, this.taskRoleIds(node.task).filter((id) => id !== roleId));
      this.adapter.touch();
      this.refresh();
    }
  }
  // 打开角色选择器（小内联下拉）
  openNodeRolePicker(node, event) {
    event.stopPropagation();
    this.selectedElementId.set(node.baseId || node.id);
    const nodeId = String(node.baseId || node.id || "");
    this.cardRolePickerNodeId.set(this.cardRolePickerNodeId() === nodeId ? "" : nodeId);
    this.rolePickerOpen.set(false);
  }
  // 重命名节点
  renameFlowNode(node, event) {
    event.stopPropagation();
    this.startNodeNameEdit(node, event);
  }
  // 模块意图：流程图卡片只承载轻量属性编辑，避免再弹出遮挡拖拽/连线的节点编辑窗。
  startNodeNameEdit(node, event) {
    event.stopPropagation();
    if (!this.editing)
      return;
    const nodeId = String(node.baseId || node.id || "");
    this.selectedElementId.set(nodeId);
    this.editingNodeNameId.set(nodeId);
    this.cardRolePickerNodeId.set("");
    window.setTimeout(() => {
      const input2 = document.getElementById(this.nodeNameInputId(node));
      input2?.focus();
      input2?.select();
    });
  }
  finishNodeNameEdit(event) {
    event?.stopPropagation();
    this.editingNodeNameId.set("");
  }
  editableNodeName(node) {
    return node.task?.name || node.gateway?.title || node.name || "";
  }
  setEditableNodeName(node, value) {
    if (node.task) {
      this.setTaskName(node.task, value);
      return;
    }
    if (node.gateway)
      this.setGateway(node.gateway, "title", value);
  }
  handleNodeNameKeydown(event) {
    event.stopPropagation();
    if (event.key === "Enter" || event.key === "Escape") {
      event.preventDefault();
      this.finishNodeNameEdit(event);
    }
  }
  nodeNameInputId(node) {
    return `process-flow-node-name-${String(node.baseId || node.id || "").replace(/[^a-zA-Z0-9_-]/g, "-")}`;
  }
  isEditingNodeName(node) {
    return this.editingNodeNameId() === String(node.baseId || node.id || "");
  }
  isCardRolePickerOpen(node) {
    return this.cardRolePickerNodeId() === String(node.baseId || node.id || "") && this.isPrimarySharedRoleNode(node);
  }
  isPrimarySharedRoleNode(node) {
    if (!node?.task || !node.shared)
      return true;
    const primaryRoleName = this.taskRoleIds(node.task).map((roleId) => this.roleName(roleId) || roleId).filter(Boolean)[0] || "\u672A\u5206\u914D\u89D2\u8272";
    return node.role === primaryRoleName;
  }
  closeCardRolePicker(event) {
    event?.stopPropagation();
    this.cardRolePickerNodeId.set("");
  }
  currentStageId(process2) {
    this.version();
    const runtimeStageId = this.normalizeStageId(getAngularRuntimeState().ui?.stageId || "");
    if (runtimeStageId && this.stageRefs(process2).some((stage) => this.normalizeStageId(stage.id) === runtimeStageId))
      return runtimeStageId;
    return this.stageRefs(process2)[0]?.id || "";
  }
  stageRefs(process2) {
    return this.adapter.stageRefs(process2);
  }
  processesForStage(stageId) {
    const normalizedStageId = this.normalizeStageId(stageId);
    if (!normalizedStageId)
      return this.processes();
    const processIds = new Set(this.adapter.stageFlowRefs().filter((item) => this.normalizeStageId(String(item.stageUid || item.stageId || "").trim()) === normalizedStageId).map((item) => String(item.processUid || item.processId || "").trim()).filter(Boolean));
    const scoped = this.processes().filter((process2) => processIds.has(this.processId(process2)));
    return scoped.length ? scoped : this.processes();
  }
  lanes(process2) {
    const names = /* @__PURE__ */ new Set();
    for (const task of this.tasks(process2)) {
      const roleIds = this.taskRoleIds(task);
      if (!roleIds.length)
        names.add("\u672A\u5206\u914D\u89D2\u8272");
      for (const roleId of roleIds)
        names.add(this.roleName(roleId) || "\u672A\u5206\u914D\u89D2\u8272");
    }
    if (!names.size)
      names.add("\u672A\u5206\u914D\u89D2\u8272");
    const laneOrder = this.flowModel.swimlaneLayout(process2).laneOrder || [];
    const orderedNames = [...names].sort((a, b) => {
      const ia = laneOrder.indexOf(a);
      const ib = laneOrder.indexOf(b);
      if (ia >= 0 && ib >= 0)
        return ia - ib;
      if (ia >= 0)
        return -1;
      if (ib >= 0)
        return 1;
      return 0;
    });
    return orderedNames.map((name, index) => ({
      id: `lane-${index}`,
      name,
      top: index * this.laneHeight,
      height: this.laneHeight
    }));
  }
  canvasWidth(process2) {
    const nodeCount = Math.max(this.flowOrder(process2).length, 1);
    return Math.max(1120, this.graphNodeStartX + Math.max(0, nodeCount - 2) * this.columnGap + 240);
  }
  canvasHeight(process2) {
    const laneBottom = this.lanes(process2).length * this.laneHeight;
    const nodeBottom = this.flowNodes(process2).reduce((bottom, node) => Math.max(bottom, node.y + node.height), 0);
    return Math.max(260, laneBottom + 120, nodeBottom + 180);
  }
  flowNodes(process2) {
    const tasks = new Map(this.tasks(process2).map((task) => [this.taskId(task), task]));
    const gateways = new Map(this.gateways(process2).map((gateway) => [this.gatewayId(gateway), gateway]));
    const laneMap = new Map(this.lanes(process2).map((lane, index) => [lane.name, index]));
    const primaryNodesById = /* @__PURE__ */ new Map();
    return this.flowOrder(process2).flatMap((id, index) => {
      const task = tasks.get(id);
      const gateway = gateways.get(id);
      const kind = id === "START" ? "start" : id === "END" ? "end" : gateway ? "gateway" : "task";
      const roleNames = task ? this.taskRoleIds(task).map((roleId) => this.roleName(roleId) || roleId).filter(Boolean) : gateway ? [this.roleName(gateway.role_id) || this.inferGatewayLane(process2, id) || "\u672A\u5206\u914D\u89D2\u8272"] : [""];
      const visibleRoleNames = roleNames.length ? roleNames : ["\u672A\u5206\u914D\u89D2\u8272"];
      const graphIndex = Math.max(0, index - 1);
      const x = kind === "start" ? this.graphStartX : this.graphNodeStartX + graphIndex * this.columnGap;
      if (kind === "start" || kind === "end") {
        const offset = this.flowOffset(process2, id);
        const connected = this.connectedNodesForBoundary(process2, id, primaryNodesById);
        const connectedCenterY = connected.length ? connected.reduce((sum, node) => sum + node.y + node.height / 2, 0) / connected.length : this.laneHeight / 2;
        const connectedRight = connected.length ? Math.max(...connected.map((node) => node.x + node.width)) : x;
        const baseX = kind === "end" ? Math.max(x, connectedRight + 72) : x;
        const clamped = this.clampNodePosition(process2, id, baseX + offset.dx, connectedCenterY - this.terminalHeight / 2 + offset.dy, this.terminalWidth, this.terminalHeight, "");
        const boundaryNode = {
          id,
          baseId: id,
          kind,
          name: id === "START" ? "\u5F00\u59CB" : "\u7ED3\u675F",
          role: "",
          shared: false,
          x: clamped.x,
          y: clamped.y,
          width: this.terminalWidth,
          height: this.terminalHeight
        };
        primaryNodesById.set(id, boundaryNode);
        return [boundaryNode];
      }
      return visibleRoleNames.map((roleName, roleIndex) => {
        const laneIndex = laneMap.get(roleName) ?? 0;
        const offset = this.flowOffset(process2, id);
        const width = kind === "gateway" ? this.gatewaySize : this.nodeWidth;
        const height = kind === "gateway" ? this.gatewaySize : this.nodeHeight;
        const baseY = laneIndex * this.laneHeight + Math.round((this.laneHeight - height) / 2);
        const clamped = this.clampNodePosition(process2, id, x + offset.dx, baseY + offset.dy, width, height, roleName);
        const canvasNode = {
          id: `${id}::${roleName || roleIndex}`,
          baseId: id,
          kind,
          name: task?.name || gateway?.title || (kind === "gateway" ? "\u7F51\u5173" : "\u672A\u547D\u540D\u8282\u70B9"),
          role: roleName,
          shared: Boolean(task && visibleRoleNames.length > 1),
          x: clamped.x,
          y: clamped.y,
          width,
          height,
          task,
          gateway
        };
        if (!primaryNodesById.has(id))
          primaryNodesById.set(id, canvasNode);
        return canvasNode;
      });
    });
  }
  laneColor(laneName) {
    const name = String(laneName || "").trim();
    const globalRoleNames = this.roles().map((role) => String(role.name || this.roleId(role) || "").trim()).filter(Boolean);
    let index = globalRoleNames.indexOf(name);
    if (index < 0) {
      const current = this.currentProcess();
      const localLaneNames = current ? this.lanes(current).map((lane) => lane.name) : [name];
      index = Math.max(0, localLaneNames.indexOf(name));
    }
    return this.lanePalette[index % this.lanePalette.length];
  }
  inlinePanelLeft(process2, node) {
    const panelWidth = 270;
    const right = node.x + node.width + 14;
    if (right + panelWidth < this.canvasWidth(process2) - 16)
      return right;
    return Math.max(this.laneTitleWidth + 12, node.x - panelWidth - 14);
  }
  inlinePanelTop(process2, node) {
    const panelHeight = node.task ? 210 : 150;
    const below = node.y + node.height + 12;
    if (below + panelHeight < this.canvasHeight(process2) - 12)
      return below;
    return Math.max(12, node.y - panelHeight - 12);
  }
  flowEdges(process2) {
    const nodes = this.flowNodes(process2);
    return this.edges(process2).flatMap((edge, index) => {
      const fromNodes = nodes.filter((node) => node.baseId === String(edge.from || ""));
      const toNodes = nodes.filter((node) => node.baseId === String(edge.to || ""));
      if (!fromNodes.length || !toNodes.length)
        return [];
      return fromNodes.flatMap((from2, fromIndex) => toNodes.map((to, toIndex) => {
        const startX = Math.round(from2.x + from2.width);
        const startY = Math.round(from2.y + from2.height / 2);
        const endX = Math.round(to.x);
        const endY = Math.round(to.y + to.height / 2);
        const sameRow = Math.abs(startY - endY) < 2;
        const midX = Math.max(startX + 24, Math.round((startX + endX) / 2));
        const d = sameRow ? `M ${startX} ${startY} L ${endX} ${endY}` : `M ${startX} ${startY} H ${midX} V ${endY} H ${endX}`;
        const edgeId = this.edgeId(edge) || `edge-${index}`;
        const labelOffset = this.flowLabelOffset(process2, edgeId);
        return {
          id: `${edgeId}::${from2.id}::${to.id}`,
          baseId: edgeId,
          label: fromIndex === 0 && toIndex === 0 ? String(edge.label || edge.condition || "") : "",
          d,
          labelX: midX + 6 + labelOffset.dx,
          labelY: (startY + endY) / 2 - 8 + labelOffset.dy
        };
      }));
    });
  }
  previewEdge(process2) {
    const source = this.connectingFromId();
    const point = this.previewPoint();
    if (!source || !point)
      return null;
    const from2 = this.flowNodes(process2).find((node) => node.baseId === source || node.id === source);
    if (!from2)
      return null;
    const startX = from2.x + from2.width;
    const startY = from2.y + from2.height / 2;
    const midX = Math.max(startX + 24, Math.round((startX + point.x) / 2));
    return { id: "preview", baseId: "preview", label: "", d: `M ${startX} ${startY} H ${midX} V ${point.y} H ${point.x}`, labelX: 0, labelY: 0 };
  }
  connectAnchors(node) {
    return [
      { id: "top", side: "top", x: node.x + node.width / 2, y: node.y },
      { id: "right", side: "right", x: node.x + node.width, y: node.y + node.height / 2 },
      { id: "bottom", side: "bottom", x: node.x + node.width / 2, y: node.y + node.height },
      { id: "left", side: "left", x: node.x, y: node.y + node.height / 2 }
    ];
  }
  selectedNode(process2) {
    return this.flowNodes(process2).find((node) => node.baseId === this.selectedElementId() && node.kind !== "start" && node.kind !== "end") || null;
  }
  selectedEdge(process2) {
    return this.edges(process2).find((edge) => this.edgeId(edge) === this.selectedElementId()) || null;
  }
  selectedFlowEdge(process2) {
    return this.flowEdges(process2).find((edge) => edge.baseId === this.selectedElementId()) || null;
  }
  flowNodeOptions(process2, side) {
    return this.adapter.flowNodeOptions(process2, side);
  }
  prototypeInputId(process2) {
    return `proc-prototype-input-${this.processId(process2).replace(/[^a-zA-Z0-9_-]/g, "-")}`;
  }
  prototypeCount(process2) {
    this.version();
    return this.adapter.prototypeFiles(process2).length;
  }
  prototypeFiles(process2) {
    return this.adapter.prototypeFiles(process2);
  }
  prototypeFileName(file, index) {
    return file.name || file.uid || file.id || `\u9644\u4EF6${index + 1}`;
  }
  prototypeUid(file) {
    return String(file.uid || file.id || "").trim();
  }
  prototypeCurrentVersion(file) {
    return this.adapter.currentPrototypeVersion(file);
  }
  prototypeVersionCount(file) {
    return Array.isArray(file.versions) && file.versions.length ? file.versions.length : 1;
  }
  prototypeMeta(file) {
    const current = this.prototypeCurrentVersion(file);
    const version = current?.number || 1;
    const uploadedAt = current?.uploadedAt ? ` ? ${current.uploadedAt}` : "";
    return `\u5F53\u524D v${version} \xB7 \u5171${this.prototypeVersionCount(file)}\u7248${uploadedAt}`;
  }
  prototypeKind(file) {
    return this.adapter.prototypeKind(file);
  }
  isPrototypeExpanded(process2, file) {
    const processId = this.processId(process2);
    const prototypeUid = this.prototypeUid(file);
    return Boolean(processId && prototypeUid && this.adapter.isPrototypeExpanded(processId, prototypeUid));
  }
  canPreviewPrototype(file, version = null) {
    return this.adapter.canPreviewPrototype(file, version);
  }
  togglePrototypeVersions(process2, file) {
    const prototypeUid = this.prototypeUid(file);
    if (!prototypeUid)
      return;
    this.adapter.togglePrototypeVersions(this.processId(process2), prototypeUid);
    this.refresh();
  }
  openPrototype(process2, file, version = null) {
    const prototypeUid = this.prototypeUid(file);
    if (!prototypeUid)
      return;
    this.adapter.openPrototype(this.processId(process2), prototypeUid, String(version?.uid || ""));
  }
  previewPrototype(process2, file, version = null) {
    const prototypeUid = this.prototypeUid(file);
    if (!prototypeUid)
      return;
    this.adapter.previewPrototype(this.processId(process2), prototypeUid, String(version?.uid || ""));
  }
  downloadPrototype(process2, file, version = null) {
    const prototypeUid = this.prototypeUid(file);
    if (!prototypeUid)
      return;
    this.adapter.downloadPrototype(this.processId(process2), prototypeUid, String(version?.uid || ""));
  }
  removePrototype(process2, file) {
    const prototypeUid = this.prototypeUid(file);
    if (!prototypeUid)
      return;
    if (!window.confirm(`\u786E\u8BA4\u5220\u9664\u9644\u4EF6\u201C${this.prototypeFileName(file, 0)}\u201D\u5417\uFF1F`))
      return;
    this.adapter.removePrototype(this.processId(process2), prototypeUid);
    this.refresh();
  }
  selectStage(stageId) {
    const normalizedStageId = this.normalizeStageId(stageId);
    getAngularRuntimeState().ui.stageId = normalizedStageId;
    const process2 = this.processesForStage(normalizedStageId)[0] || null;
    if (process2)
      this.selectProcess(this.processId(process2));
    else
      this.refresh();
  }
  taskCount(process2) {
    return this.tasks(process2).length;
  }
  gatewayCount(process2) {
    return this.gateways(process2).length;
  }
  selectProcess(processId) {
    const target = this.processes().find((process2) => this.processId(process2) === processId);
    if (!target)
      return;
    this.selectedElementId.set("");
    this.connectingFromId.set("");
    this.adapter.selectProcess(processId);
    this.syncAttachmentDrawer(target, true);
    this.refresh();
  }
  // 模块意图：附件面板的默认状态跟随当前流程，而不是跟随组件生命周期固定开启。
  // 关键流程：流程初次加载和下拉切换都经过这里，统一检查当前流程的附件数量。
  // 边界细节：用户手动关闭后不在刷新中强行打开，只有流程真正切换时才重新计算默认状态。
  syncAttachmentDrawer(process2, force = false) {
    const processId = process2 ? this.processId(process2) : "";
    if (!force && processId === this.attachmentStateProcessId)
      return;
    this.attachmentStateProcessId = processId;
    this.attachmentDrawerOpen.set(Boolean(process2 && this.prototypeFiles(process2).length));
  }
  selectElement(id) {
    this.selectedElementId.set(id);
    this.rolePickerOpen.set(false);
    this.cardRolePickerNodeId.set("");
  }
  alignmentGuides() {
    return this.dragPreview()?.guides || [];
  }
  setProcessField(field, value) {
    const process2 = this.currentProcess();
    if (!process2)
      return;
    this.flowModel.setProcessField(process2, field, value);
    this.adapter.touch();
    this.refresh();
  }
  setTaskName(task, value) {
    this.flowModel.setTaskName(task, value);
    this.adapter.touch();
    this.refresh();
  }
  setTaskRole(task, roleId) {
    this.flowModel.setTaskRoleIds(task, roleId ? [roleId] : []);
    this.adapter.touch();
    this.refresh();
  }
  setTaskRolesFromSelect(task, target) {
    const select = target;
    const roleIds = select ? Array.from(select.selectedOptions).map((option) => option.value).filter(Boolean) : [];
    this.flowModel.setTaskRoleIds(task, roleIds);
    this.adapter.touch();
    this.refresh();
  }
  toggleTaskRole(task, roleId, checked) {
    const current = new Set(this.taskRoleIds(task));
    if (checked)
      current.add(roleId);
    else
      current.delete(roleId);
    this.flowModel.setTaskRoleIds(task, [...current]);
    this.adapter.touch();
    this.refresh();
  }
  setGateway(gateway, field, value) {
    this.flowModel.setGateway(gateway, field, value);
    this.adapter.touch();
    this.refresh();
  }
  setEdge(edge, field, value) {
    this.flowModel.setEdge(edge, field, value);
    this.adapter.touch();
    this.refresh();
  }
  openEdgeLabelEditor(edgeId, event) {
    event?.stopPropagation();
    if (!this.editing)
      return;
    this.selectedElementId.set(edgeId);
    this.editingEdgeLabelId.set(edgeId);
    window.setTimeout(() => {
      const input2 = document.getElementById(this.edgeLabelInputId(edgeId));
      input2?.focus();
      input2?.select();
    });
  }
  edgeLabelInputId(edgeId) {
    return `process-flow-edge-label-${edgeId.replace(/[^a-zA-Z0-9_-]/g, "-")}`;
  }
  finishEdgeLabelEdit(event) {
    event?.stopPropagation();
    this.editingEdgeLabelId.set("");
  }
  handleEdgeLabelKeydown(event) {
    event.stopPropagation();
    if (event.key === "Enter" || event.key === "Escape") {
      event.preventDefault();
      this.finishEdgeLabelEdit(event);
    }
  }
  startEdgeLabelDrag(edge, event) {
    if (!this.editing)
      return;
    event.stopPropagation();
    const process2 = this.currentProcess();
    if (!process2)
      return;
    const startOffset = this.flowLabelOffset(process2, edge.baseId);
    const timer = window.setTimeout(() => {
      const current = this.labelDragState();
      if (current?.edgeId === edge.baseId)
        this.labelDragState.set(__spreadProps(__spreadValues({}, current), { dragging: true, timer: null }));
    }, 220);
    this.labelDragState.set({
      edgeId: edge.baseId,
      startX: event.clientX,
      startY: event.clientY,
      startOffset,
      timer,
      dragging: false
    });
  }
  moveEdgeLabelDrag(event) {
    const drag = this.labelDragState();
    const process2 = this.currentProcess();
    if (!drag || !process2)
      return;
    const dx = (event.clientX - drag.startX) / this.zoomValue();
    const dy = (event.clientY - drag.startY) / this.zoomValue();
    if (!drag.dragging) {
      if (Math.abs(dx) > 3 || Math.abs(dy) > 3)
        this.clearEdgeLabelDragTimer(drag);
      return;
    }
    this.flowModel.setFlowLabelOffset(process2, drag.edgeId, drag.startOffset.dx + dx, drag.startOffset.dy + dy);
    this.adapter.touch();
    this.refresh();
  }
  finishEdgeLabelDrag(event) {
    const drag = this.labelDragState();
    if (!drag)
      return;
    event?.stopPropagation();
    if (drag)
      this.clearEdgeLabelDragTimer(drag);
    this.labelDragState.set(null);
  }
  addTask() {
    const process2 = this.currentProcess();
    if (!process2)
      return;
    const task = this.flowModel.addTask(process2);
    const id = this.taskId(task);
    const rightEdge = this.flowNodes(process2).filter((n) => n.baseId !== id).reduce((max, n) => Math.max(max, n.x + n.width), this.graphNodeStartX);
    const orderIndex = Math.max(0, this.flowOrder(process2).indexOf(id) - 1);
    const baseX = this.graphNodeStartX + orderIndex * this.columnGap;
    const dx = Math.max(0, rightEdge + this.columnGap - baseX);
    if (dx > 0)
      this.flowModel.setFlowOffset(process2, id, dx, 0);
    this.adapter.touch();
    this.selectedElementId.set(id);
    this.refresh();
    this.scrollToNewElement();
  }
  addGateway() {
    const process2 = this.currentProcess();
    if (!process2)
      return;
    const gateway = this.flowModel.addGateway(process2);
    const id = this.gatewayId(gateway);
    const rightEdge = this.flowNodes(process2).filter((n) => n.baseId !== id).reduce((max, n) => Math.max(max, n.x + n.width), this.graphNodeStartX);
    const orderIndex = Math.max(0, this.flowOrder(process2).indexOf(id) - 1);
    const baseX = this.graphNodeStartX + orderIndex * this.columnGap;
    const dx = Math.max(0, rightEdge + this.columnGap - baseX);
    if (dx > 0)
      this.flowModel.setFlowOffset(process2, id, dx, 0);
    this.adapter.touch();
    this.selectedElementId.set(id);
    this.refresh();
    this.scrollToNewElement();
  }
  scrollToNewElement() {
    setTimeout(() => {
      const shell = document.querySelector('[data-testid="process-flow-canvas-shell"]');
      if (shell)
        shell.scrollLeft = shell.scrollWidth;
    }, 0);
  }
  startConnect(nodeId) {
    if (!this.editing)
      return;
    this.connectingFromId.set(nodeId);
  }
  finishConnect(targetNodeId) {
    if (!this.editing)
      return;
    const source = this.connectingFromId();
    if (!source || source === targetNodeId)
      return;
    const process2 = this.currentProcess();
    if (!process2)
      return;
    const edge = this.flowModel.addEdge(process2, source, targetNodeId);
    this.adapter.touch();
    this.selectedElementId.set(this.edgeId(edge));
    this.connectingFromId.set("");
    this.previewPoint.set(null);
    this.refresh();
  }
  startConnectFromAnchor(event, nodeId) {
    event.preventDefault();
    event.stopPropagation();
    if (!this.editing)
      return;
    this.connectingFromId.set(nodeId);
    this.selectedElementId.set(nodeId);
  }
  onCanvasPointer(event) {
    const canvas = event.currentTarget;
    const rect = canvas.getBoundingClientRect();
    this.previewPoint.set({
      x: (event.clientX - rect.left) / this.zoomValue(),
      y: (event.clientY - rect.top) / this.zoomValue()
    });
  }
  clearCanvasSelection() {
    this.selectedElementId.set("");
    this.connectingFromId.set("");
    this.rolePickerOpen.set(false);
    this.cardRolePickerNodeId.set("");
    this.editingNodeNameId.set("");
    this.editingEdgeLabelId.set("");
    this.previewPoint.set(null);
  }
  removeSelected(process2) {
    const id = this.selectedElementId();
    if (!id || id === "START" || id === "END")
      return;
    this.flowModel.removeElement(process2, id);
    this.adapter.touch();
    this.selectedElementId.set("");
    this.refresh();
  }
  duplicateSelected(process2) {
    const id = this.selectedElementId();
    if (!id || id === "START" || id === "END")
      return;
    const task = this.tasks(process2).find((t) => this.taskId(t) === id);
    const gateway = !task ? this.gateways(process2).find((g) => this.gatewayId(g) === id) : null;
    const clone = task ? this.flowModel.duplicateTask(process2, task) : gateway ? this.flowModel.duplicateGateway(process2, gateway) : null;
    if (!clone)
      return;
    const cloneId = String(clone.uid || clone.id || "").trim();
    cloneId ? this.flowModel.setFlowOffset(process2, cloneId, this.columnGap, 0) : null;
    this.adapter.touch();
    this.selectedElementId.set(cloneId);
    this.refresh();
  }
  openNodeView(node) {
    if (!node.task)
      return;
    this.adapter.selectTask(this.taskId(node.task));
    this.taskEditorRequested.emit(this.taskId(node.task));
  }
  uploadPrototypeFiles(process2) {
    this.adapter.uploadPrototypeFiles(this.processId(process2), this.prototypeInputId(process2));
    this.refresh();
  }
  uploadPrototypeFilesImmediately(process2) {
    this.attachmentDrawerOpen.set(true);
    this.uploadPrototypeFiles(process2);
    [120, 600, 1200].forEach((delay) => window.setTimeout(() => this.collapsePrototypeVersions(process2), delay));
  }
  zoom(delta) {
    const next = Math.max(0.5, Math.min(1.8, Math.round((this.zoomValue() + delta) * 10) / 10));
    this.zoomValue.set(next);
  }
  resetZoom() {
    this.zoomValue.set(1);
  }
  onCanvasWheel(event) {
    if (!event.ctrlKey)
      return;
    event.preventDefault();
    this.zoom(event.deltaY < 0 ? 0.1 : -0.1);
  }
  onDocumentPointerUp(event) {
    this.finishEdgeLabelDrag(event);
    this.finishNodeDrag(event);
  }
  onDocumentPointerMove(event) {
    this.moveEdgeLabelDrag(event);
    this.moveNodeDrag(event);
  }
  onWindowPointerUp(event) {
    this.finishEdgeLabelDrag(event);
    this.finishNodeDrag(event);
  }
  startNodeDrag(event, node) {
    if (!this.editing)
      return;
    if (typeof event.button === "number" && event.button !== 0)
      return;
    if (this.isNodeCardControl(event.target))
      return;
    event.preventDefault();
    const process2 = this.currentProcess();
    if (!process2)
      return;
    this.selectedElementId.set(node.baseId);
    this.dragState.set({
      nodeId: node.baseId,
      startX: event.clientX,
      startY: event.clientY,
      startNodeX: node.x,
      startNodeY: node.y,
      startOffset: this.flowOffset(process2, node.baseId),
      task: node.task
    });
    this.dragPreview.set({ nodeId: node.baseId, dx: this.flowOffset(process2, node.baseId).dx, dy: this.flowOffset(process2, node.baseId).dy, guides: [] });
    if ("pointerId" in event) {
      try {
        event.currentTarget?.setPointerCapture?.(event.pointerId);
      } catch {
      }
    }
  }
  moveNodeDrag(event) {
    const drag = this.dragState();
    const process2 = this.currentProcess();
    if (!drag || !process2)
      return;
    const dx = (event.clientX - drag.startX) / this.zoomValue();
    const dy = (event.clientY - drag.startY) / this.zoomValue();
    const current = this.flowNodes(process2).find((node) => node.baseId === drag.nodeId || node.id === drag.nodeId);
    if (!current)
      return;
    const free = this.clampNodePosition(process2, drag.nodeId, drag.startNodeX + dx, drag.startNodeY + dy, current.width, current.height, current.role);
    const alignment = this.alignmentForPosition(process2, drag.nodeId, free.x, free.y, current.width, current.height, current.role);
    this.dragPreview.set({
      nodeId: drag.nodeId,
      dx: drag.startOffset.dx + free.x - drag.startNodeX,
      dy: drag.startOffset.dy + free.y - drag.startNodeY,
      guides: alignment.guides
    });
  }
  endNodeDrag(event, node) {
    this.finishNodeDrag(event, node);
  }
  finishNodeDrag(event, node) {
    const drag = this.dragState();
    const process2 = this.currentProcess();
    if (!drag || !process2)
      return;
    const dx = (event.clientX - drag.startX) / this.zoomValue();
    const dy = (event.clientY - drag.startY) / this.zoomValue();
    const moved = Math.abs(dx) > 2 || Math.abs(dy) > 2;
    if (moved) {
      const snapped = this.snapPosition(process2, drag.nodeId, drag.startNodeX + dx, drag.startNodeY + dy);
      this.setFlowOffset(process2, drag.nodeId, drag.startOffset.dx + snapped.x - drag.startNodeX, drag.startOffset.dy + snapped.y - drag.startNodeY, true);
    }
    if ("pointerId" in event) {
      try {
        event.currentTarget?.releasePointerCapture?.(event.pointerId);
      } catch {
      }
    }
    this.dragState.set(null);
    this.dragPreview.set(null);
    this.selectedElementId.set(drag.nodeId);
    this.refresh();
  }
  isNodeCardControl(target) {
    const element = target;
    if (!element)
      return false;
    if (element.closest("input,select,textarea,label,.flow-node-role-picker,.flow-node-edit-icon,.flow-gateway-edit-icon,.flow-gateway-label"))
      return true;
    if (element.closest(".flow-terminal,.flow-gateway"))
      return false;
    return Boolean(element.closest("button"));
  }
  onLaneDrop(event, lane) {
    event.preventDefault();
    const taskId = event.dataTransfer?.getData("application/x-blm-task-id") || "";
    const process2 = this.currentProcess();
    const task = process2 ? this.tasks(process2).find((item) => this.taskId(item) === taskId) : null;
    if (!task)
      return;
    const role = this.roles().find((item) => item.name === lane.name || this.roleId(item) === lane.name);
    this.setTaskRole(task, role ? this.roleId(role) : "");
  }
  flowOrder(process2) {
    const taskIds = this.tasks(process2).map((task) => this.taskId(task)).filter(Boolean);
    const gatewayIds = this.gateways(process2).map((gateway) => this.gatewayId(gateway)).filter(Boolean);
    const allIds = ["START", ...taskIds, ...gatewayIds, "END"];
    const rawEdges = this.edges(process2).map((edge) => ({ from: String(edge.from || ""), to: String(edge.to || "") })).filter((edge) => allIds.includes(edge.from) && allIds.includes(edge.to));
    const connected = /* @__PURE__ */ new Set();
    for (const edge of rawEdges) {
      connected.add(edge.from);
      connected.add(edge.to);
    }
    const connectedIds = allIds.filter((id) => connected.has(id));
    const disconnectedIds = allIds.filter((id) => !connected.has(id));
    const rankProbeIds = this.orderFlowNodeIds(connectedIds, rawEdges);
    const rankProbe = new Map(rankProbeIds.map((id, index) => [id, index]));
    const mainEdges = rawEdges.filter((edge) => edge.to === "END" || (rankProbe.get(edge.to) || 0) > (rankProbe.get(edge.from) || 0));
    const ordered = this.orderFlowNodeIds(connectedIds, mainEdges);
    const result = [...ordered];
    for (const id of disconnectedIds) {
      if (!result.includes(id))
        result.push(id);
    }
    return [...result.filter((id) => id !== "END"), "END"];
  }
  orderFlowNodeIds(ids, edges) {
    const idSet = new Set(ids);
    const inDegree = new Map(ids.map((id) => [id, 0]));
    const outgoing = new Map(ids.map((id) => [id, []]));
    for (const edge of edges) {
      if (!idSet.has(edge.from) || !idSet.has(edge.to))
        continue;
      outgoing.get(edge.from)?.push(edge.to);
      inDegree.set(edge.to, (inDegree.get(edge.to) || 0) + 1);
    }
    const queue = ids.filter((id) => (inDegree.get(id) || 0) === 0);
    const ordered = [];
    const seen = /* @__PURE__ */ new Set();
    while (queue.length) {
      const id = queue.shift();
      if (seen.has(id))
        continue;
      seen.add(id);
      ordered.push(id);
      for (const to of outgoing.get(id) || []) {
        inDegree.set(to, Math.max(0, (inDegree.get(to) || 0) - 1));
        if ((inDegree.get(to) || 0) === 0)
          queue.push(to);
      }
    }
    for (const id of ids) {
      if (!seen.has(id))
        ordered.push(id);
    }
    return ordered;
  }
  flowOffset(process2, key) {
    const offset = this.flowModel.swimlaneLayout(process2).items?.[key] || {};
    const preview = this.dragPreview();
    if (preview?.nodeId === key)
      return { dx: preview.dx, dy: preview.dy };
    return { dx: Number(offset.dx || 0), dy: Number(offset.dy || 0) };
  }
  flowLabelOffset(process2, key) {
    const offset = this.flowModel.swimlaneLayout(process2).labels?.[key] || {};
    return { dx: Number(offset.dx || 0), dy: Number(offset.dy || 0) };
  }
  clearEdgeLabelDragTimer(drag) {
    if (drag.timer !== null)
      window.clearTimeout(drag.timer);
  }
  setFlowOffset(process2, key, dx, dy, markDirty) {
    this.flowModel.setFlowOffset(process2, key, dx, dy);
    if (markDirty)
      this.adapter.touch();
  }
  snapPosition(process2, nodeId, x, y) {
    const current = this.flowNodes(process2).find((node) => node.baseId === nodeId || node.id === nodeId);
    if (!current)
      return { x, y };
    const free = this.clampNodePosition(process2, current.baseId, x, y, current.width, current.height, current.role);
    const alignment = this.alignmentForPosition(process2, current.baseId, free.x, free.y, current.width, current.height, current.role);
    return this.clampNodePosition(process2, current.baseId, alignment.x, alignment.y, current.width, current.height, current.role);
  }
  alignmentForPosition(process2, nodeId, x, y, width, height, roleName) {
    const nodes = this.flowNodes(process2).filter((node) => node.baseId !== nodeId && node.id !== nodeId);
    const canvasW = this.canvasWidth(process2);
    const canvasH = this.canvasHeight(process2);
    const current = {
      left: x,
      centerX: x + width / 2,
      right: x + width,
      top: y,
      centerY: y + height / 2,
      bottom: y + height
    };
    let bestX = null;
    let bestY = null;
    const considerX = (target, source, otherTop, otherBottom) => {
      const distance = Math.abs(source - target);
      if (distance > this.snapThreshold || bestX && distance >= bestX.distance)
        return;
      bestX = { distance, delta: target - source, position: target, from: Math.min(y, otherTop) - 18, to: Math.max(y + height, otherBottom) + 18 };
    };
    const considerY = (target, source, otherLeft, otherRight) => {
      const distance = Math.abs(source - target);
      if (distance > this.snapThreshold || bestY && distance >= bestY.distance)
        return;
      bestY = { distance, delta: target - source, position: target, from: Math.min(x, otherLeft) - 18, to: Math.max(x + width, otherRight) + 18 };
    };
    for (const other of nodes) {
      const otherEdges = {
        left: other.x,
        centerX: other.x + other.width / 2,
        right: other.x + other.width,
        top: other.y,
        centerY: other.y + other.height / 2,
        bottom: other.y + other.height
      };
      considerX(otherEdges.left, current.left, other.y, other.y + other.height);
      considerX(otherEdges.centerX, current.centerX, other.y, other.y + other.height);
      considerX(otherEdges.right, current.right, other.y, other.y + other.height);
      considerY(otherEdges.top, current.top, other.x, other.x + other.width);
      considerY(otherEdges.centerY, current.centerY, other.x, other.x + other.width);
      considerY(otherEdges.bottom, current.bottom, other.x, other.x + other.width);
    }
    for (const lane of this.lanes(process2)) {
      const target = lane.top + lane.height / 2;
      considerY(target, current.centerY, this.laneTitleWidth, canvasW);
    }
    const guides = [];
    const xHit = bestX;
    const yHit = bestY;
    if (xHit) {
      guides.push({ id: `x-${Math.round(xHit.position)}`, axis: "x", position: Math.round(xHit.position), from: Math.max(0, Math.round(xHit.from)), to: Math.min(canvasH, Math.round(xHit.to)) });
    }
    if (yHit) {
      guides.push({ id: `y-${Math.round(yHit.position)}`, axis: "y", position: Math.round(yHit.position), from: Math.max(this.laneTitleWidth, Math.round(yHit.from)), to: Math.min(canvasW, Math.round(yHit.to)) });
    }
    return { x: x + (xHit?.delta || 0), y: y + (yHit?.delta || 0), guides };
  }
  clampNodePosition(process2, nodeId, x, y, width, height, roleName) {
    const minX = nodeId === "START" ? this.laneTitleWidth + 8 : this.laneTitleWidth + 24;
    const laneBottom = this.lanes(process2).length * this.laneHeight;
    let minY = 0;
    let maxY = Math.max(minY, laneBottom + 180 - height - 24);
    return {
      x: Math.max(minX, x),
      y: Math.min(maxY, Math.max(minY, y))
    };
  }
  collapsePrototypeVersions(process2) {
    const processId = this.processId(process2);
    for (const file of this.prototypeFiles(process2)) {
      const prototypeUid = this.prototypeUid(file);
      if (prototypeUid && this.adapter.isPrototypeExpanded(processId, prototypeUid)) {
        this.adapter.togglePrototypeVersions(processId, prototypeUid);
      }
    }
    this.attachmentDrawerOpen.set(true);
    this.refresh();
  }
  connectedNodesForBoundary(process2, boundaryId, nodesById) {
    return this.edges(process2).filter((edge) => boundaryId === "START" ? String(edge.from || "") === boundaryId : String(edge.to || "") === boundaryId).map((edge) => nodesById.get(boundaryId === "START" ? String(edge.to || "") : String(edge.from || ""))).filter((node) => Boolean(node));
  }
  inferGatewayLane(process2, gatewayId) {
    const edge = this.edges(process2).find((item) => String(item.to || "") === gatewayId) || this.edges(process2).find((item) => String(item.from || "") === gatewayId);
    const relatedId = String(edge?.from || edge?.to || "");
    const task = this.tasks(process2).find((item) => this.taskId(item) === relatedId);
    return task ? this.roleName(this.taskRoleId(task)) : "";
  }
  normalizeStageId(stageId) {
    const target = String(stageId || "").trim();
    if (!target)
      return "";
    const stage = this.stages().find((item) => String(item.uid || "").trim() === target || String(item.id || "").trim() === target);
    return stage ? this.stageId(stage) : target;
  }
  refresh() {
    this.version.update((value) => value + 1);
  }
  static \u0275fac = function ProcessFlowWorkbenchComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ProcessFlowWorkbenchComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _ProcessFlowWorkbenchComponent, selectors: [["app-process-flow-workbench"]], hostBindings: function ProcessFlowWorkbenchComponent_HostBindings(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275listener("pointerup", function ProcessFlowWorkbenchComponent_pointerup_HostBindingHandler($event) {
        return ctx.onDocumentPointerUp($event);
      }, \u0275\u0275resolveDocument)("pointermove", function ProcessFlowWorkbenchComponent_pointermove_HostBindingHandler($event) {
        return ctx.onDocumentPointerMove($event);
      }, \u0275\u0275resolveDocument)("pointerup", function ProcessFlowWorkbenchComponent_pointerup_HostBindingHandler($event) {
        return ctx.onWindowPointerUp($event);
      }, \u0275\u0275resolveWindow);
    }
  }, inputs: { editing: "editing", exportGraphId: "exportGraphId", previewProcessId: "previewProcessId" }, outputs: { taskEditorRequested: "taskEditorRequested" }, decls: 3, vars: 3, consts: [["data-testid", "process-flow-view", 1, "process-flow-view", "redesigned"], [1, "process-flow-frame"], ["data-testid", "process-flow-empty", 1, "diag-empty"], ["data-testid", "process-flow-basic-fields", 1, "process-flow-topbar"], [1, "compact-field", "select-field"], ["data-testid", "process-flow-stage-select", 1, "stage-select", 3, "ngModelChange", "ngModel"], [3, "value"], ["data-testid", "process-flow-select", 1, "process-select", 3, "ngModelChange", "ngModel"], [1, "compact-field", "name-field"], ["type", "text", "data-testid", "process-flow-name-input", 3, "ngModelChange", "readonly", "ngModel"], [1, "compact-field"], ["type", "text", "data-testid", "process-flow-trigger-input", 3, "ngModelChange", "readonly", "ngModel"], ["type", "text", "data-testid", "process-flow-outcome-input", 3, "ngModelChange", "readonly", "ngModel"], ["data-testid", "process-flow-edit-shortcut", 1, "process-flow-edit-shortcut"], [1, "process-flow-body"], [1, "process-flow-side-tools"], ["type", "button", "data-testid", "process-flow-open-attachments-readonly", "title", "\u6D41\u7A0B\u9644\u4EF6", 1, "process-flow-readonly-attachment"], ["data-testid", "process-flow-canvas-shell", 1, "process-flow-canvas-shell", 3, "wheel"], ["data-testid", "process-flow-canvas", "data-export-graph-ready", "true", 1, "process-flow-canvas", 3, "mousemove", "pointermove", "pointerup", "click"], [1, "flow-lane", 3, "--lane-color", "top", "height"], [1, "flow-alignment-guide", 3, "is-vertical", "is-horizontal", "left", "top", "width", "height"], [1, "flow-edges"], ["id", "process-flow-arrow", "viewBox", "0 0 10 10", "refX", "9", "refY", "5", "markerWidth", "6", "markerHeight", "6", "orient", "auto-start-reverse"], ["d", "M 0 0 L 10 5 L 0 10 z"], [1, "flow-edge", "preview"], ["data-testid", "process-flow-attachment-panel", 1, "process-attachment-drawer"], ["aria-label", "\u6D41\u7A0B\u6570\u91CF\u7EDF\u8BA1", 1, "process-flow-tool-counts"], ["type", "button", "data-testid", "process-flow-add-node", "title", "\u65B0\u589E\u8282\u70B9", 3, "click"], ["type", "button", "data-testid", "process-flow-add-gateway", "title", "\u65B0\u589E\u7F51\u5173", 3, "click"], ["type", "button", "data-testid", "process-flow-open-attachments", "title", "\u6D41\u7A0B\u9644\u4EF6", 3, "click"], [1, "tool-file-icon"], ["type", "button", "data-testid", "process-flow-cancel-connect", "title", "\u53D6\u6D88\u8FDE\u7EBF"], ["type", "button", "data-testid", "process-flow-cancel-connect", "title", "\u53D6\u6D88\u8FDE\u7EBF", 3, "click"], ["type", "button", "data-testid", "process-flow-copy-selected", "title", "\u590D\u5236\u9009\u4E2D", 3, "click"], ["type", "button", "data-testid", "process-flow-delete-selected", "title", "\u5220\u9664\u9009\u4E2D", 1, "danger", 3, "click"], ["type", "button", "data-testid", "process-flow-open-attachments-readonly", "title", "\u6D41\u7A0B\u9644\u4EF6", 1, "process-flow-readonly-attachment", 3, "click"], [1, "flow-lane", 3, "dragover", "drop"], [1, "flow-lane-title"], [1, "flow-alignment-guide"], [1, "flow-edge-hit", 3, "click"], [1, "flow-edge", 3, "click"], ["data-testid", "process-flow-edge-label", 1, "flow-edge-label"], ["data-testid", "process-flow-edge-label", 1, "flow-edge-label", 3, "click", "pointerdown"], ["data-testid", "process-flow-terminal", "type", "button", 1, "flow-terminal", 3, "active", "connecting", "is-end", "left", "top"], ["data-testid", "process-flow-gateway", 1, "flow-gateway", 3, "--lane-color", "active", "connecting", "left", "top", "width", "height"], ["data-testid", "process-flow-node", 1, "flow-node", 3, "--lane-color", "active", "connecting", "is-editing", "left", "top"], [1, "flow-connect-box", 3, "left", "top", "width", "height"], ["data-testid", "process-flow-terminal", "type", "button", 1, "flow-terminal", 3, "pointerdown", "pointerup", "click"], ["data-testid", "process-flow-gateway", 1, "flow-gateway", 3, "pointerdown", "pointerup", "dblclick", "click"], ["type", "text", 1, "flow-gateway-name-input", 3, "id", "ngModel"], ["data-testid", "process-flow-gateway-label", "title", "\u53CC\u51FB\u4FEE\u6539\u7F51\u5173\u540D\u79F0", 1, "flow-gateway-label"], ["title", "\\u4fee\\u6539\\u540d\\u79f0", 1, "flow-gateway-edit-icon"], ["type", "text", 1, "flow-gateway-name-input", 3, "ngModelChange", "keydown", "blur", "click", "id", "ngModel"], ["data-testid", "process-flow-gateway-label", "title", "\u53CC\u51FB\u4FEE\u6539\u7F51\u5173\u540D\u79F0", 1, "flow-gateway-label", 3, "dblclick"], ["title", "\\u4fee\\u6539\\u540d\\u79f0", 1, "flow-gateway-edit-icon", 3, "pointerdown", "click"], ["data-testid", "process-flow-node", 1, "flow-node", 3, "pointerdown", "pointerup", "dblclick", "click"], [1, "shared-badge"], [1, "flow-node-name-row"], ["type", "text", 1, "flow-node-name-input", 3, "id", "ngModel"], ["title", "\\u4fee\\u6539\\u540d\\u79f0", 1, "flow-node-edit-icon"], [1, "flow-node-role-tags"], ["type", "text", 1, "flow-node-name-input", 3, "ngModelChange", "keydown", "blur", "click", "id", "ngModel"], ["title", "\\u4fee\\u6539\\u540d\\u79f0", 1, "flow-node-edit-icon", 3, "pointerdown", "click"], [1, "flow-node-role-tag", 3, "--lane-color"], ["title", "\\u6dfb\\u52a0\\u89d2\\u8272", 1, "flow-node-role-add", 3, "click"], ["data-testid", "process-flow-node-role-picker", 1, "flow-node-role-picker"], [1, "flow-node-role-tag"], ["title", "\\u79fb\\u9664\\u89d2\\u8272", 1, "flow-node-role-remove", 3, "click"], ["data-testid", "process-flow-node-role-picker", 1, "flow-node-role-picker", 3, "click", "pointerdown"], [1, "flow-role-option"], [1, "flow-role-menu-footer"], ["type", "button", 3, "click"], ["type", "checkbox", 3, "change", "checked"], [1, "flow-connect-box", 3, "click"], ["type", "button", 1, "connect-anchor", 3, "class", "left", "top"], ["type", "button", 1, "connect-anchor", 3, "pointerdown", "click"], ["data-testid", "process-flow-edge-label-input", "type", "text", 1, "flow-edge-label-input", 3, "id", "left", "top", "ngModel"], ["data-testid", "process-flow-edge-label-input", "type", "text", 1, "flow-edge-label-input", 3, "ngModelChange", "keydown", "blur", "click", "id", "ngModel"], [1, "attachment-panel-head"], ["type", "button", "aria-label", "\u5173\u95ED\u9644\u4EF6", 3, "click"], [1, "attachment-list"], ["data-testid", "proc-prototype-item", 1, "attachment-card"], [1, "attachment-empty"], ["type", "file", "data-testid", "proc-prototype-input", "multiple", "", 3, "change", "id"], [1, "attachment-add-button", 3, "for"], [1, "attachment-card-meta"], [1, "attachment-file-name", 3, "title"], [1, "attachment-card-actions"], ["type", "button", "data-testid", "proc-prototype-toggle", 3, "click"], ["type", "button", "data-testid", "proc-prototype-preview"], ["type", "button", "data-testid", "proc-prototype-download", 3, "click"], ["type", "button", "data-testid", "proc-prototype-remove"], ["data-testid", "proc-prototype-version-list", 1, "attachment-version-list"], ["type", "button", "data-testid", "proc-prototype-preview", 3, "click"], ["type", "button", "data-testid", "proc-prototype-remove", 3, "click"], ["data-testid", "proc-prototype-version-item", 1, "attachment-version-item"], ["type", "button", "data-testid", "proc-prototype-version-preview"], ["type", "button", "data-testid", "proc-prototype-version-download", 3, "click"], ["type", "button", "data-testid", "proc-prototype-version-preview", 3, "click"]], template: function ProcessFlowWorkbenchComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 0);
      \u0275\u0275conditionalCreate(1, ProcessFlowWorkbenchComponent_Conditional_1_Template, 48, 24, "section", 1)(2, ProcessFlowWorkbenchComponent_Conditional_2_Template, 2, 0, "div", 2);
      \u0275\u0275elementEnd();
    }
    if (rf & 2) {
      let tmp_1_0;
      \u0275\u0275classProp("flow-readonly", !ctx.editing);
      \u0275\u0275advance();
      \u0275\u0275conditional((tmp_1_0 = ctx.currentProcess()) ? 1 : 2, tmp_1_0);
    }
  }, dependencies: [CommonModule, FormsModule, NgSelectOption, \u0275NgSelectMultipleOption, DefaultValueAccessor, SelectControlValueAccessor, NgControlStatus, NgModel], styles: ["\n[_nghost-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  min-width: 0;\n  min-height: 0;\n  flex: 1;\n}\n/*# sourceMappingURL=process-flow-workbench.component.css.map */", '@charset "UTF-8";\n\n\n.process-flow-view[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  width: 100%;\n  min-width: 0;\n  min-height: 0;\n  flex: 1;\n  padding: 10px 14px 16px;\n}\n.process-flow-frame[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-rows: auto auto 1fr;\n  gap: 8px;\n  min-width: 0;\n  min-height: 0;\n  height: 100%;\n}\n.process-flow-topbar[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(150px, 0.7fr) minmax(180px, 0.9fr) minmax(180px, 1fr) minmax(160px, 0.85fr) minmax(160px, 0.85fr);\n  align-items: center;\n  gap: 8px;\n  min-height: 48px;\n  padding: 6px 10px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: #fff;\n  box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);\n}\n.stage-select[_ngcontent-%COMP%], \n.process-select[_ngcontent-%COMP%] {\n  width: 100%;\n  height: 34px;\n  padding: 0 12px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 14px;\n  font-weight: 700;\n}\n.select-field[_ngcontent-%COMP%] {\n  grid-template-columns: auto minmax(0, 1fr);\n}\n.process-attachment-drawer[_ngcontent-%COMP%]   input[type=file][_ngcontent-%COMP%] {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  opacity: 0;\n  pointer-events: none;\n}\n.process-flow-body[_ngcontent-%COMP%] {\n  position: relative;\n  display: flex;\n  flex-direction: column;\n  min-width: 0;\n  min-height: 0;\n}\n.process-flow-edit-shortcut[_ngcontent-%COMP%] {\n  display: inline-flex;\n  width: calc(100% - 16px);\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  padding: 2px 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 999px;\n  background: #f8fbff;\n  color: #64748b;\n  font-size: 12px;\n  font-weight: 600;\n}\n.process-flow-edit-shortcut[_ngcontent-%COMP%]   .process-flow-tool-counts[_ngcontent-%COMP%] {\n  flex: 0 0 auto;\n  margin-left: auto;\n  margin-right: 0;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n  white-space: nowrap;\n}\n.process-flow-edit-shortcut[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  color: #1d4ed8;\n  font-weight: 900;\n}\n.attachment-add-button[_ngcontent-%COMP%] {\n  display: inline-flex;\n  height: 32px;\n  align-items: center;\n  justify-content: center;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 8px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 700;\n}\n.compact-field[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr);\n  align-items: center;\n  gap: 6px;\n}\n.compact-field[_ngcontent-%COMP%]   span[_ngcontent-%COMP%], \n.flow-inline-panel[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  color: #64748b;\n  font-size: 12px;\n  font-weight: 700;\n}\n.compact-field[_ngcontent-%COMP%]   input[_ngcontent-%COMP%], \n.flow-inline-panel[_ngcontent-%COMP%]   input[_ngcontent-%COMP%], \n.flow-inline-panel[_ngcontent-%COMP%]   select[_ngcontent-%COMP%], \n.flow-edge-panel[_ngcontent-%COMP%]   input[_ngcontent-%COMP%], \n.flow-edge-panel[_ngcontent-%COMP%]   select[_ngcontent-%COMP%] {\n  height: 32px;\n  min-width: 0;\n  padding: 0 10px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 14px;\n}\n.flow-inline-panel[_ngcontent-%COMP%]   select[multiple][_ngcontent-%COMP%] {\n  height: 72px;\n}\n.flow-readonly[_ngcontent-%COMP%]   .process-flow-side-tools[_ngcontent-%COMP%] {\n  display: grid;\n}\n.flow-readonly[_ngcontent-%COMP%]   .process-flow-topbar[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  pointer-events: none;\n  opacity: 0.6;\n  background: #f8fafc;\n}\n.flow-readonly[_ngcontent-%COMP%]   .flow-node[_ngcontent-%COMP%], \n.flow-readonly[_ngcontent-%COMP%]   .flow-terminal[_ngcontent-%COMP%], \n.flow-readonly[_ngcontent-%COMP%]   .flow-gateway[_ngcontent-%COMP%] {\n  pointer-events: auto;\n  cursor: pointer;\n}\n.flow-readonly[_ngcontent-%COMP%] {\n}\n/*# sourceMappingURL=process-flow-layout.css.map */', "\n.process-flow-canvas-shell[_ngcontent-%COMP%] {\n  position: relative;\n  flex: 1;\n  min-height: 240px;\n  padding: 10px;\n  overflow: auto;\n  border: 1px solid #cfe0f3;\n  border-radius: 12px;\n  background: #fff;\n  scrollbar-width: thin;\n}\n.process-flow-canvas-shell[_ngcontent-%COMP%]::-webkit-scrollbar {\n  width: 7px;\n  height: 7px;\n}\n.process-flow-canvas-shell[_ngcontent-%COMP%]::-webkit-scrollbar-thumb {\n  border-radius: 999px;\n  background: #c7d3df;\n}\n.process-flow-canvas[_ngcontent-%COMP%] {\n  position: relative;\n  min-width: 100%;\n  border: 1px solid #d8e4f2;\n  border-radius: 10px;\n  overflow: visible;\n  background:\n    linear-gradient(\n      90deg,\n      #f8fbff 0 116px,\n      transparent 116px),\n    #fff;\n  background-size: 100% 100%, 100% 100%;\n}\n/*# sourceMappingURL=process-flow-canvas.css.map */", '\n.process-flow-side-tools[_ngcontent-%COMP%] {\n  position: absolute;\n  top: 10px;\n  right: 10px;\n  z-index: 20;\n  display: grid;\n  gap: 6px;\n  width: 72px;\n  padding: 5px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: rgba(255, 255, 255, 0.94);\n  box-shadow: 0 10px 24px rgba(15, 23, 42, 0.12);\n}\n.process-flow-side-tools[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: 18px 1fr;\n  width: 62px;\n  height: 30px;\n  align-items: center;\n  justify-content: center;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #fff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 800;\n}\n.process-flow-tool-counts[_ngcontent-%COMP%] {\n  align-self: center;\n  margin-right: 2px;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n  white-space: nowrap;\n}\n.process-flow-readonly-attachment[_ngcontent-%COMP%] {\n  position: absolute;\n  top: 10px;\n  right: 10px;\n  z-index: 20;\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  height: 30px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #fff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.process-flow-readonly-attachment[_ngcontent-%COMP%]   b[_ngcontent-%COMP%] {\n  min-width: 18px;\n  padding: 1px 5px;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 11px;\n  text-align: center;\n}\n.process-flow-side-tools[_ngcontent-%COMP%]   button[_ngcontent-%COMP%]   b[_ngcontent-%COMP%] {\n  justify-self: center;\n}\n.process-flow-side-tools[_ngcontent-%COMP%]   button[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  height: auto;\n  background: transparent;\n  color: #1d4ed8;\n  font-size: 12px;\n  font-weight: 700;\n  line-height: normal;\n}\n.tool-file-icon[_ngcontent-%COMP%] {\n  position: relative;\n  width: 13px;\n  height: 10px;\n  border: 1.5px solid currentColor;\n  border-radius: 2px;\n  font-size: 0;\n}\n.tool-file-icon[_ngcontent-%COMP%]::before {\n  content: "";\n  position: absolute;\n  left: 1px;\n  top: -4px;\n  width: 7px;\n  height: 4px;\n  border: 1.5px solid currentColor;\n  border-bottom: 0;\n  border-radius: 2px 2px 0 0;\n  background: #fff;\n}\n.process-flow-side-tools[_ngcontent-%COMP%]   .danger[_ngcontent-%COMP%] {\n  color: #dc2626;\n}\n/*# sourceMappingURL=process-flow-tools.css.map */', '\n.flow-lane[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 0;\n  right: 0;\n  border-bottom: 1px solid #dbeafe;\n  background:\n    linear-gradient(\n      90deg,\n      color-mix(in srgb, var(--lane-color, #2563eb) 18%, #f8fafc) 0 116px,\n      color-mix(in srgb, var(--lane-color, #2563eb) 7%, #fff) 116px);\n}\n.flow-lane-title[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 0;\n  top: 0;\n  display: flex;\n  width: 116px;\n  height: 100%;\n  align-items: center;\n  justify-content: center;\n  border-right: 1px solid #94a3b8;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 14%, #fff);\n  color: var(--lane-color, #0f172a);\n  font-size: 14px;\n  font-weight: 800;\n  writing-mode: vertical-rl;\n  letter-spacing: 2px;\n}\n.flow-lane-title[_ngcontent-%COMP%]::before {\n  content: "";\n  position: absolute;\n  left: 10px;\n  top: 14px;\n  bottom: 14px;\n  width: 4px;\n  border-radius: 999px;\n  background: var(--lane-color, #2563eb);\n  opacity: 0.8;\n}\n.shared-badge[_ngcontent-%COMP%] {\n  position: absolute;\n  top: -12px;\n  right: -8px;\n  z-index: 4;\n  padding: 2px 7px;\n  border: 1px solid #93c5fd;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 11px;\n  font-weight: 800;\n  line-height: 1;\n  white-space: nowrap;\n  writing-mode: horizontal-tb;\n}\n.flow-edges[_ngcontent-%COMP%] {\n  position: absolute;\n  inset: 0;\n  overflow: visible;\n}\n.flow-alignment-guide[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 16;\n  pointer-events: none;\n  background: #2563eb;\n  box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.14);\n}\n.flow-alignment-guide.is-vertical[_ngcontent-%COMP%] {\n  width: 1px;\n}\n.flow-alignment-guide.is-horizontal[_ngcontent-%COMP%] {\n  height: 1px;\n}\n.flow-edge[_ngcontent-%COMP%] {\n  fill: none;\n  stroke: #64748b;\n  stroke-width: 2;\n  marker-end: url(#process-flow-arrow);\n  cursor: pointer;\n}\n.flow-edge-hit[_ngcontent-%COMP%] {\n  fill: none;\n  stroke: transparent;\n  stroke-width: 14;\n  cursor: pointer;\n}\n.flow-edge.preview[_ngcontent-%COMP%] {\n  stroke: #2563eb;\n  stroke-dasharray: 7 5;\n  marker-end: none;\n  pointer-events: none;\n}\n.flow-edge.active[_ngcontent-%COMP%] {\n  stroke: #2563eb;\n  stroke-width: 3;\n}\n.flow-edges[_ngcontent-%COMP%]   marker[_ngcontent-%COMP%]   path[_ngcontent-%COMP%] {\n  fill: #64748b;\n}\n.flow-edge-label[_ngcontent-%COMP%] {\n  fill: #0f172a;\n  font-size: 12px;\n  cursor: text;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.flow-edge-label-input[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 130;\n  width: 148px;\n  min-height: 26px;\n  padding: 2px 7px;\n  border: 1px solid #93c5fd;\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  outline: none;\n  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.12);\n}\n.flow-node[_ngcontent-%COMP%], \n.flow-gateway[_ngcontent-%COMP%], \n.flow-terminal[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 5;\n  color: #0f3b70;\n  cursor: pointer;\n  font-weight: 800;\n}\n.flow-connect-box[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 18;\n  border: 1px dashed #60a5fa;\n  border-radius: 6px;\n  pointer-events: none;\n}\n.connect-anchor[_ngcontent-%COMP%] {\n  position: absolute;\n  display: grid;\n  width: 11px;\n  height: 11px;\n  transform: translate(-50%, -50%);\n  place-items: center;\n  border: 1px solid #93c5fd;\n  border-radius: 999px;\n  background: #fff;\n  color: #2563eb;\n  cursor: crosshair;\n  font-size: 9px;\n  font-weight: 800;\n  line-height: 1;\n  pointer-events: auto;\n}\n.flow-node[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  min-width: 132px;\n  min-height: 54px;\n  padding: 7px 10px;\n  border: 1px solid color-mix(in srgb, var(--lane-color, #2563eb) 42%, #bfdbfe);\n  border-radius: 8px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 16%, #eff6ff);\n  box-shadow: inset 4px 0 0 color-mix(in srgb, var(--lane-color, #2563eb) 78%, #1e40af);\n  text-align: center;\n}\n.flow-node.is-editing[_ngcontent-%COMP%] {\n  padding-bottom: 8px;\n}\n.flow-node-name-row[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n.flow-node-name-input[_ngcontent-%COMP%] {\n  min-width: 0;\n  width: 92px;\n  height: 22px;\n  padding: 0 5px;\n  border: 1px solid #93c5fd;\n  border-radius: 5px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n  text-align: center;\n  outline: none;\n}\n.flow-node-name-input[_ngcontent-%COMP%]:focus {\n  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.16);\n}\n.flow-node-edit-icon[_ngcontent-%COMP%] {\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n  flex-shrink: 0;\n}\n.flow-node-edit-icon[_ngcontent-%COMP%]:hover {\n  color: #3b82f6;\n}\n.flow-node[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%], \n.flow-node[_ngcontent-%COMP%]   small[_ngcontent-%COMP%] {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.flow-node[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 82%, #0f172a);\n}\n.flow-node[_ngcontent-%COMP%]   small[_ngcontent-%COMP%] {\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 66%, #475569);\n  font-size: 12px;\n  font-weight: 600;\n}\n.flow-node-role-tags[_ngcontent-%COMP%] {\n  position: relative;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 3px;\n  justify-content: center;\n}\n.flow-node-role-tag[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  gap: 2px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 10%, #fff);\n  border: 1px solid color-mix(in srgb, var(--lane-color, #2563eb) 34%, #cbd5e1);\n  border-radius: 4px;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 72%, #334155);\n  font-size: 10px;\n  font-weight: 600;\n  padding: 1px 5px;\n}\n.flow-node-role-remove[_ngcontent-%COMP%] {\n  background: none;\n  border: 0;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n  padding: 0 1px;\n  line-height: 1;\n}\n.flow-node-role-remove[_ngcontent-%COMP%]:hover {\n  color: #ef4444;\n}\n.flow-node-role-add[_ngcontent-%COMP%] {\n  background: none;\n  border: 1px dashed #cbd5e1;\n  border-radius: 4px;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 10px;\n  padding: 1px 5px;\n}\n.flow-node-role-add[_ngcontent-%COMP%]:hover {\n  border-color: #3b82f6;\n  color: #3b82f6;\n}\n.flow-node-role-picker[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 50%;\n  top: calc(100% + 6px);\n  z-index: 160;\n  display: grid;\n  width: 184px;\n  max-height: 220px;\n  transform: translateX(-50%);\n  gap: 7px;\n  overflow-y: auto;\n  padding: 9px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  box-shadow: 0 14px 28px rgba(15, 23, 42, 0.16);\n  text-align: left;\n}\n.flow-node-role-picker[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  color: #475569;\n  font-size: 12px;\n}\n.flow-terminal[_ngcontent-%COMP%] {\n  display: inline-flex;\n  width: 50px;\n  height: auto;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 3px;\n  border: 0;\n  background: transparent;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n}\n.flow-terminal[_ngcontent-%COMP%]:not(.is-end) {\n  flex-direction: row-reverse;\n}\n.flow-terminal[_ngcontent-%COMP%]::before {\n  content: "";\n  width: 18px;\n  height: 18px;\n  border-radius: 999px;\n  background: #3f6fb5;\n}\n.flow-terminal.is-end[_ngcontent-%COMP%]::before {\n  background: #64748b;\n}\n.flow-gateway[_ngcontent-%COMP%] {\n  display: grid;\n  width: 22px;\n  height: 22px;\n  align-items: center;\n  justify-content: center;\n  border: 0;\n  background: transparent;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 78%, #0f172a);\n  font-size: 12px;\n  font-weight: 800;\n  text-align: center;\n}\n.flow-gateway[_ngcontent-%COMP%]::before {\n  content: "";\n  position: absolute;\n  left: 2px;\n  top: 2px;\n  width: 16px;\n  height: 16px;\n  z-index: -1;\n  transform: rotate(45deg);\n  border: 1.4px solid color-mix(in srgb, var(--lane-color, #2563eb) 48%, #93c5fd);\n  border-radius: 2px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 8%, #fff);\n}\n.flow-gateway[_ngcontent-%COMP%]::after {\n  content: "+";\n  position: absolute;\n  left: 2px;\n  top: 2px;\n  display: grid;\n  width: 16px;\n  height: 16px;\n  place-items: center;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 70%, #0f172a);\n  font-size: 16px;\n  font-weight: 900;\n  line-height: 1;\n  opacity: 0.25;\n}\n.flow-gateway-label[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 50%;\n  top: 22px;\n  display: block;\n  width: max-content;\n  max-width: 96px;\n  transform: translateX(-50%);\n  white-space: normal;\n  line-height: 1.25;\n  overflow-wrap: anywhere;\n  word-break: break-word;\n  font-size: 11px;\n}\n.flow-gateway-name-input[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 50%;\n  top: 20px;\n  z-index: 8;\n  width: 128px;\n  min-height: 28px;\n  transform: translateX(-50%);\n  padding: 3px 6px;\n  border: 1px solid #93c5fd;\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 11px;\n  font-weight: 800;\n  text-align: center;\n  outline: none;\n}\n.flow-gateway-edit-icon[_ngcontent-%COMP%] {\n  position: absolute;\n  right: 6px;\n  top: 2px;\n  z-index: 2;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n}\n.flow-gateway-edit-icon[_ngcontent-%COMP%]:hover {\n  color: #3b82f6;\n}\n.flow-node.active[_ngcontent-%COMP%], \n.flow-gateway.active[_ngcontent-%COMP%], \n.flow-terminal.active[_ngcontent-%COMP%], \n.flow-node.connecting[_ngcontent-%COMP%], \n.flow-gateway.connecting[_ngcontent-%COMP%], \n.flow-terminal.connecting[_ngcontent-%COMP%] {\n  border-color: #2563eb;\n  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.18);\n}\n/*# sourceMappingURL=process-flow-nodes.css.map */', "\n.flow-inline-panel[_ngcontent-%COMP%], \n.flow-edge-panel[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 120;\n  display: grid;\n  gap: 8px;\n  width: 270px;\n  padding: 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 10px;\n  background: rgba(255, 255, 255, 0.96);\n  box-shadow: 0 12px 24px rgba(15, 23, 42, 0.12);\n}\n.flow-inline-panel[_ngcontent-%COMP%]   label[_ngcontent-%COMP%] {\n  display: grid;\n  gap: 4px;\n}\n.flow-role-combo[_ngcontent-%COMP%] {\n  position: relative;\n}\n.flow-role-trigger[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  height: 32px;\n  align-items: center;\n  gap: 8px;\n  padding: 0 9px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  cursor: pointer;\n  font-size: 13px;\n  text-align: left;\n}\n.flow-role-trigger.has-value[_ngcontent-%COMP%] {\n  border-color: #60a5fa;\n  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.12);\n}\n.flow-role-trigger[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.flow-role-trigger[_ngcontent-%COMP%]   b[_ngcontent-%COMP%] {\n  color: #64748b;\n  font-weight: 700;\n}\n.flow-role-menu[_ngcontent-%COMP%] {\n  position: absolute;\n  left: 0;\n  right: 0;\n  top: calc(100% + 4px);\n  z-index: 140;\n  display: grid;\n  gap: 8px;\n  max-height: min(280px, 100vh - 180px);\n  overflow-y: auto;\n  overscroll-behavior: contain;\n  padding: 10px;\n  border: 1px solid #cbd8e6;\n  border-radius: 9px;\n  background: #fff;\n  box-shadow: 0 14px 28px rgba(15, 23, 42, 0.15);\n}\n.flow-role-menu[_ngcontent-%COMP%]::-webkit-scrollbar {\n  width: 8px;\n}\n.flow-role-menu[_ngcontent-%COMP%]::-webkit-scrollbar-thumb {\n  border: 2px solid #fff;\n  border-radius: 999px;\n  background: #c7d4e3;\n}\n.flow-role-menu[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  color: #475569;\n  font-size: 13px;\n}\n.flow-role-option[_ngcontent-%COMP%] {\n  display: grid !important;\n  grid-template-columns: auto minmax(0, 1fr);\n  align-items: center;\n  gap: 8px !important;\n  min-height: 26px;\n  color: #0f172a;\n  font-size: 13px;\n}\n.flow-role-option[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  width: 14px;\n  height: 14px;\n  accent-color: #2563eb;\n}\n.flow-role-menu-footer[_ngcontent-%COMP%] {\n  display: flex;\n  justify-content: flex-end;\n  padding-top: 8px;\n  border-top: 1px solid #e2e8f0;\n}\n.flow-role-menu-footer[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  height: 28px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #eff6ff;\n  color: #2563eb;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.inline-panel-actions[_ngcontent-%COMP%] {\n  display: flex;\n  justify-content: flex-end;\n  gap: 8px;\n}\n.inline-panel-actions[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  min-height: 30px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 8px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 700;\n}\n.flow-edge-panel[_ngcontent-%COMP%] {\n  right: 24px;\n  bottom: 24px;\n  grid-template-columns: 1fr auto 1fr;\n  width: 520px;\n  align-items: center;\n}\n.flow-edge-panel[_ngcontent-%COMP%]   input[_ngcontent-%COMP%] {\n  grid-column: 1/-1;\n}\n/*# sourceMappingURL=process-flow-inline.css.map */", '@charset "UTF-8";\n\n\n.process-attachment-drawer[_ngcontent-%COMP%] {\n  display: grid;\n  align-content: start;\n  gap: 10px;\n  height: auto;\n  max-height: min(520px, 100vh - 218px);\n  min-height: 0;\n  padding: 12px;\n  border: 1px solid #d7e4f3;\n  border-radius: 12px;\n  background: #fff;\n  box-shadow: 0 10px 24px rgba(15, 23, 42, 0.06);\n}\n.process-attachment-drawer[_ngcontent-%COMP%] {\n  position: absolute;\n  top: 0;\n  right: 12px;\n  z-index: 35;\n  width: min(360px, 100vw - 32px);\n  box-shadow: 0 18px 46px rgba(15, 23, 42, 0.18);\n}\n.attachment-panel-head[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  color: #0f172a;\n}\n.attachment-panel-head[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  flex: 1;\n}\n.attachment-panel-head[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  padding: 2px 8px;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  font-size: 12px;\n  font-weight: 700;\n}\n.attachment-panel-head[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  width: 28px;\n  height: 28px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.attachment-list[_ngcontent-%COMP%] {\n  display: grid;\n  gap: 8px;\n  max-height: min(420px, 100vh - 310px);\n  overflow: auto;\n}\n.attachment-card[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr);\n  gap: 10px;\n  align-items: start;\n  padding: 12px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: #f8fbff;\n}\n.attachment-card-meta[_ngcontent-%COMP%] {\n  display: grid;\n  gap: 4px;\n  min-width: 0;\n}\n.attachment-card-meta[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  display: block;\n  max-width: 100%;\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 14px;\n  font-weight: 800;\n  line-height: 1.35;\n  overflow-wrap: anywhere;\n}\n.attachment-card-meta[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  overflow: hidden;\n  color: #475569;\n  font-size: 12px;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.attachment-card-meta[_ngcontent-%COMP%]   small[_ngcontent-%COMP%], \n.attachment-empty[_ngcontent-%COMP%] {\n  color: #64748b;\n  font-size: 12px;\n}\n.attachment-card-actions[_ngcontent-%COMP%] {\n  display: flex;\n  flex-wrap: wrap;\n  justify-content: flex-start;\n  gap: 5px;\n}\n.attachment-card-actions[_ngcontent-%COMP%]   button[_ngcontent-%COMP%], \n.attachment-version-item[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  height: 28px;\n  padding: 0 10px;\n  border: 1px solid #d7e4f3;\n  border-radius: 8px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n}\n.attachment-version-list[_ngcontent-%COMP%] {\n  grid-column: 1/-1;\n  display: grid;\n  gap: 6px;\n  padding-top: 10px;\n  border-top: 1px dashed #d7e4f3;\n}\n.attachment-version-item[_ngcontent-%COMP%] {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 8px;\n  padding: 7px 0;\n}\n.attachment-version-meta[_ngcontent-%COMP%] {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  min-width: 0;\n  flex-wrap: wrap;\n}\n.attachment-version-meta[_ngcontent-%COMP%]   strong[_ngcontent-%COMP%] {\n  min-width: 42px;\n  color: #1d4ed8;\n  font-size: 13px;\n}\n.attachment-version-meta[_ngcontent-%COMP%]   span[_ngcontent-%COMP%] {\n  color: #64748b;\n  font-size: 12px;\n  white-space: nowrap;\n}\n.attachment-version-actions[_ngcontent-%COMP%] {\n  display: flex;\n  gap: 5px;\n  flex-wrap: wrap;\n  justify-content: flex-end;\n}\n.attachment-version-item[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%]:first-child {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 6px;\n  min-width: 0;\n}\n.attachment-version-item[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%]:first-child   strong[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  min-width: 34px;\n  height: 24px;\n  padding: 0 8px;\n  border-radius: 7px;\n  background: #e0edff;\n  color: #1557b0;\n  font-size: 13px;\n  font-weight: 800;\n}\n.attachment-version-item[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%]:first-child   span[_ngcontent-%COMP%] {\n  display: inline-flex;\n  align-items: center;\n  min-height: 24px;\n  color: #64748b;\n  font-size: 12px;\n  white-space: nowrap;\n}\n.attachment-version-item[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%]:first-child   strong[_ngcontent-%COMP%]    + span[_ngcontent-%COMP%] {\n  padding: 0 6px;\n  border-radius: 999px;\n  background: #dcfce7;\n  color: #15803d;\n  font-weight: 700;\n}\n.attachment-version-item[_ngcontent-%COMP%]    > div[_ngcontent-%COMP%]:first-child   strong[_ngcontent-%COMP%]    + span[_ngcontent-%COMP%]    + span[_ngcontent-%COMP%] {\n  padding-left: 2px;\n  color: #64748b;\n}\n/*# sourceMappingURL=process-flow-attachments.css.map */'] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProcessFlowWorkbenchComponent, [{
    type: Component,
    args: [{ selector: "app-process-flow-workbench", standalone: true, imports: [CommonModule, FormsModule], template: `<div class="process-flow-view redesigned" data-testid="process-flow-view" [class.flow-readonly]="!editing">
  @if (currentProcess(); as process) {
    <section class="process-flow-frame">
      <header class="process-flow-topbar" data-testid="process-flow-basic-fields">
        <label class="compact-field select-field">
          <span>\u9636\u6BB5</span>
          <select class="stage-select" data-testid="process-flow-stage-select" [ngModel]="currentStageId(process)" (ngModelChange)="selectStage($event)">
            @for (stage of stages(); track stageId(stage)) {
              <option [value]="stageId(stage)">{{ stageName(stage) }}</option>
            }
          </select>
        </label>
        <label class="compact-field select-field">
          <span>\u6D41\u7A0B</span>
          <select class="process-select" data-testid="process-flow-select" [ngModel]="processId(process)" (ngModelChange)="selectProcess($event)">
            @for (item of processesForStage(currentStageId(process)); track processId(item)) {
              <option [value]="processId(item)">{{ item.name || '\\u672a\\u547d\\u540d\\u6d41\\u7a0b' }}</option>
            }
          </select>
        </label>
        <label class="compact-field name-field">
          <span>&#x540d;&#x79f0;</span>
          <input type="text" data-testid="process-flow-name-input" [readonly]="!editing" [ngModel]="process.name || ''" (ngModelChange)="setProcessField('name', $event)" />
        </label>
        <label class="compact-field">
          <span>&#x89e6;&#x53d1;</span>
          <input type="text" data-testid="process-flow-trigger-input" [readonly]="!editing" [ngModel]="process.trigger || ''" (ngModelChange)="setProcessField('trigger', $event)" />
        </label>
        <label class="compact-field">
          <span>&#x9884;&#x671f;</span>
          <input type="text" data-testid="process-flow-outcome-input" [readonly]="!editing" [ngModel]="process.outcome || ''" (ngModelChange)="setProcessField('outcome', $event)" />
        </label>
      </header>
      @if (editing) {
        <div class="process-flow-edit-shortcut" data-testid="process-flow-edit-shortcut">
          <span> <strong>\u53CC\u51FB</strong>\u53EF\u4EE5\u4FEE\u6539\u8282\u70B9\u3001\u7F51\u5173\u548C\u8FDE\u7EBF\u7684\u63CF\u8FF0</span>
          <span class="process-flow-tool-counts" aria-label="\u6D41\u7A0B\u6570\u91CF\u7EDF\u8BA1">\u8282\u70B9 {{ taskCount(process) }} \xB7 \u7F51\u5173 {{ gatewayCount(process) }} \xB7 \u9644\u4EF6 {{ prototypeCount(process) }}</span>
        </div>
      }

      <section class="process-flow-body">
        <div class="process-flow-side-tools">
          @if (editing) {
            <button type="button" data-testid="process-flow-add-node" title="&#x65b0;&#x589e;&#x8282;&#x70b9;" (click)="addTask()"><b>+</b><span>&#x8282;&#x70b9;</span></button>
            <button type="button" data-testid="process-flow-add-gateway" title="&#x65b0;&#x589e;&#x7f51;&#x5173;" (click)="addGateway()"><b>&#x25c7;</b><span>&#x7f51;&#x5173;</span></button>
            <button type="button" data-testid="process-flow-open-attachments" title="&#x6d41;&#x7a0b;&#x9644;&#x4ef6;" (click)="attachmentDrawerOpen.set(true)"><b class="tool-file-icon">\u25A1</b><span>&#x9644;&#x4ef6;</span></button>
            @if (connectingFromId()) {
              <button type="button" data-testid="process-flow-cancel-connect" title="&#x53d6;&#x6d88;&#x8fde;&#x7ebf;" (click)="connectingFromId.set('')"><b>!</b><span>&#x53d6;&#x6d88;</span></button>
            }
            @if (selectedElementId()) {
              <button type="button" data-testid="process-flow-copy-selected" title="&#x590d;&#x5236;&#x9009;&#x4e2d;" (click)="duplicateSelected(process)"><b>\u29C9</b><span>&#x590d;&#x5236;</span></button>
              <button class="danger" type="button" data-testid="process-flow-delete-selected" title="&#x5220;&#x9664;&#x9009;&#x4e2d;" (click)="removeSelected(process)"><b>x</b><span>&#x5220;&#x9664;</span></button>
            }
          }
        </div>

        @if (!editing) {
          <button class="process-flow-readonly-attachment" type="button" data-testid="process-flow-open-attachments-readonly" title="\u6D41\u7A0B\u9644\u4EF6" (click)="attachmentDrawerOpen.set(true)">
            <span>\u9644\u4EF6</span><b>{{ prototypeCount(process) }}</b>
          </button>
        }
        <section class="process-flow-canvas-shell" data-testid="process-flow-canvas-shell" (wheel)="onCanvasWheel($event)">
          <div class="process-flow-canvas" data-testid="process-flow-canvas" [attr.data-export-graph-id]="exportGraphId || null" [attr.data-export-process-id]="processId(process)" data-export-graph-ready="true" [style.width.px]="canvasWidth(process)" [style.height.px]="canvasHeight(process)" [style.zoom]="zoomValue()" (mousemove)="onCanvasPointer($event)" (pointermove)="moveNodeDrag($event)" (pointerup)="finishNodeDrag($event)" (click)="clearCanvasSelection()">
            @for (lane of lanes(process); track lane.id) {
              <div class="flow-lane" [style.--lane-color]="laneColor(lane.name)" [style.top.px]="lane.top" [style.height.px]="lane.height" (dragover)="$event.preventDefault()" (drop)="onLaneDrop($event, lane)">
                <div class="flow-lane-title">{{ lane.name }}</div>
              </div>
            }
            @for (guide of alignmentGuides(); track guide.id) {
              <div class="flow-alignment-guide" [class.is-vertical]="guide.axis === 'x'" [class.is-horizontal]="guide.axis === 'y'" [style.left.px]="guide.axis === 'x' ? guide.position : guide.from" [style.top.px]="guide.axis === 'y' ? guide.position : guide.from" [style.width.px]="guide.axis === 'y' ? guide.to - guide.from : null" [style.height.px]="guide.axis === 'x' ? guide.to - guide.from : null"></div>
            }

            <svg class="flow-edges" [attr.width]="canvasWidth(process)" [attr.height]="canvasHeight(process)">
              <defs>
                <marker id="process-flow-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
                  <path d="M 0 0 L 10 5 L 0 10 z"></path>
                </marker>
              </defs>
              @for (edge of flowEdges(process); track edge.id) {
                <path class="flow-edge-hit" [attr.d]="edge.d" (click)="openEdgeLabelEditor(edge.baseId, $event)"></path>
                <path class="flow-edge" [class.active]="selectedElementId() === edge.baseId" [attr.d]="edge.d" (click)="openEdgeLabelEditor(edge.baseId, $event)"></path>
                @if (edge.label) {
                  <text class="flow-edge-label" data-testid="process-flow-edge-label" [attr.x]="edge.labelX" [attr.y]="edge.labelY" (click)="openEdgeLabelEditor(edge.baseId, $event)" (pointerdown)="startEdgeLabelDrag(edge, $event)">{{ edge.label }}</text>
                }
              }
              @if (previewEdge(process); as edge) {
                <path class="flow-edge preview" [attr.d]="edge.d"></path>
              }
            </svg>

            @for (node of flowNodes(process); track node.id) {
              @if (node.kind === 'start' || node.kind === 'end') {
                <button class="flow-terminal" data-testid="process-flow-terminal" [class.active]="selectedElementId() === node.id" [class.connecting]="connectingFromId() === node.id" [class.is-end]="node.kind === 'end'" type="button" [style.left.px]="node.x" [style.top.px]="node.y" (pointerdown)="startNodeDrag($event, node)" (pointerup)="endNodeDrag($event, node)" (click)="$event.stopPropagation(); connectingFromId() ? finishConnect(node.id) : selectElement(node.id)">{{ node.name }}</button>
              } @else if (node.kind === 'gateway') {
                <div class="flow-gateway" data-testid="process-flow-gateway" [style.--lane-color]="laneColor(node.role)" [class.active]="selectedElementId() === node.baseId" [class.connecting]="connectingFromId() === node.baseId" [style.left.px]="node.x" [style.top.px]="node.y" [style.width.px]="node.width" [style.height.px]="node.height" (pointerdown)="startNodeDrag($event, node)" (pointerup)="endNodeDrag($event, node)" (dblclick)="startNodeNameEdit(node, $event)" (click)="$event.stopPropagation(); connectingFromId() ? finishConnect(node.baseId) : selectElement(node.baseId)">
                  @if (editing && isEditingNodeName(node) && node.gateway) {
                    <input class="flow-gateway-name-input" [id]="nodeNameInputId(node)" type="text" [ngModel]="editableNodeName(node)" (ngModelChange)="setEditableNodeName(node, $event)" (keydown)="handleNodeNameKeydown($event)" (blur)="finishNodeNameEdit($event)" (click)="$event.stopPropagation()" />
                  } @else {
                    <span class="flow-gateway-label" data-testid="process-flow-gateway-label" title="\u53CC\u51FB\u4FEE\u6539\u7F51\u5173\u540D\u79F0" (dblclick)="startNodeNameEdit(node, $event)">{{ node.name }}</span>
                  }
                  @if (editing) { <span class="flow-gateway-edit-icon" (pointerdown)="$event.stopPropagation()" (click)="startNodeNameEdit(node, $event)" title="\\u4fee\\u6539\\u540d\\u79f0">&#x270e;</span> }
                </div>
              } @else {
                <div class="flow-node" [style.--lane-color]="laneColor(node.role)" [class.active]="selectedElementId() === node.baseId" [class.connecting]="connectingFromId() === node.baseId" [class.is-editing]="editing" data-testid="process-flow-node" [attr.data-base-id]="node.baseId" [attr.data-role]="node.role" [style.left.px]="node.x" [style.top.px]="node.y"
                  (pointerdown)="startNodeDrag($event, node)"
                  (pointerup)="endNodeDrag($event, node)"
                  (dblclick)="startNodeNameEdit(node, $event)"
                  (click)="$event.stopPropagation(); editing ? handleNodeClick(node) : openNodeEditor(node)">
                  @if (node.shared) { <span class="shared-badge">&#x5171;&#x4eab;</span> }
                  <div class="flow-node-name-row">
                    @if (editing && isEditingNodeName(node) && node.task) {
                      <input class="flow-node-name-input" [id]="nodeNameInputId(node)" type="text" [ngModel]="node.task.name || ''" (ngModelChange)="setTaskName(node.task, $event)" (keydown)="handleNodeNameKeydown($event)" (blur)="finishNodeNameEdit($event)" (click)="$event.stopPropagation()" />
                    } @else {
                      <strong>{{ node.name }}</strong>
                    }
                    @if (editing) { <span class="flow-node-edit-icon" (pointerdown)="$event.stopPropagation()" (click)="renameFlowNode(node, $event)" title="\\u4fee\\u6539\\u540d\\u79f0">&#x270e;</span> }
                  </div>
                  @if (editing) {
                    <div class="flow-node-role-tags">
                      @for (roleName of nodeRoleNames(node); track roleName) {
                        <span class="flow-node-role-tag" [style.--lane-color]="laneColor(roleName)">{{ roleName }}<button class="flow-node-role-remove" (click)="removeNodeRole(node, roleName, $event)" title="\\u79fb\\u9664\\u89d2\\u8272">&#x00d7;</button></span>
                      }
                      <button class="flow-node-role-add" (click)="openNodeRolePicker(node, $event)" title="\\u6dfb\\u52a0\\u89d2\\u8272">+ &#x89d2;&#x8272;</button>
                      @if (isCardRolePickerOpen(node) && node.task) {
                        <div class="flow-node-role-picker" data-testid="process-flow-node-role-picker" (click)="$event.stopPropagation()" (pointerdown)="$event.stopPropagation()">
                          <strong>&#x6267;&#x884c;&#x89d2;&#x8272;</strong>
                          @for (role of roles(); track roleId(role)) {
                            <label class="flow-role-option">
                              <input type="checkbox" [checked]="taskRoleIds(node.task).includes(roleId(role))" (change)="toggleTaskRole(node.task, roleId(role), $any($event.target).checked)" />
                              <span>{{ role.name || roleId(role) }}</span>
                            </label>
                          }
                          <div class="flow-role-menu-footer">
                            <button type="button" (click)="closeCardRolePicker($event)">&#x5b8c;&#x6210;</button>
                          </div>
                        </div>
                      }
                    </div>
                  } @else {
                    <small>{{ node.role || '\\u672a\\u5206\\u914d\\u89d2\\u8272' }}</small>
                  }
                </div>
              }
              @if (editing && (selectedElementId() === node.baseId || selectedElementId() === node.id)) {
                <div class="flow-connect-box" [style.left.px]="node.x - 2" [style.top.px]="node.y - 2" [style.width.px]="node.width + 4" [style.height.px]="node.height + 4" (click)="$event.stopPropagation()">
                  @for (anchor of connectAnchors(node); track anchor.id) {
                    <button type="button" class="connect-anchor" [class]="anchor.side" [style.left.px]="anchor.x - node.x" [style.top.px]="anchor.y - node.y" (pointerdown)="startConnectFromAnchor($event, node.baseId)" (click)="startConnectFromAnchor($event, node.baseId)">+</button>
                  }
                </div>
              }
            }

            @if (selectedEdge(process); as edge) {
              @if (selectedFlowEdge(process); as canvasEdge) {
                @if (editingEdgeLabelId() === edgeId(edge)) {
                  <input class="flow-edge-label-input" data-testid="process-flow-edge-label-input" [id]="edgeLabelInputId(edgeId(edge))" type="text" [style.left.px]="canvasEdge.labelX" [style.top.px]="canvasEdge.labelY - 18" [ngModel]="edge.label || edge.condition || ''" (ngModelChange)="setEdge(edge, 'label', $event)" (keydown)="handleEdgeLabelKeydown($event)" (blur)="finishEdgeLabelEdit($event)" (click)="$event.stopPropagation()" />
                }
              }
            }
          </div>
        </section>

        @if (attachmentDrawerOpen()) {
          <aside class="process-attachment-drawer" data-testid="process-flow-attachment-panel">
            <div class="attachment-panel-head"><strong>&#x6d41;&#x7a0b;&#x539f;&#x578b;/&#x9644;&#x4ef6;</strong><span>{{ prototypeCount(process) }}&#x9879;</span><button type="button" aria-label="&#x5173;&#x95ed;&#x9644;&#x4ef6;" (click)="attachmentDrawerOpen.set(false)">x</button></div>
            @if (editing) {
              <input type="file" [id]="prototypeInputId(process)" data-testid="proc-prototype-input" multiple (change)="uploadPrototypeFilesImmediately(process)" />
              <label class="attachment-add-button" [for]="prototypeInputId(process)">+ &#x6dfb;&#x52a0;&#x9644;&#x4ef6;</label>
            }
            <div class="attachment-list">
              @for (file of prototypeFiles(process); track file.uid || file.id || $index; let index = $index) {
                <div class="attachment-card" data-testid="proc-prototype-item">
                  <div class="attachment-card-meta"><strong class="attachment-file-name" [title]="prototypeFileName(file, index)">{{ prototypeFileName(file, index) }}</strong><span>{{ prototypeMeta(file) }}</span><small>{{ prototypeKind(file) }}</small></div>
                  <div class="attachment-card-actions">
                    <button type="button" data-testid="proc-prototype-toggle" (click)="togglePrototypeVersions(process, file)">{{ isPrototypeExpanded(process, file) ? '\\u6536\\u8d77' : '\\u5c55\\u5f00' }}&#x7248;&#x672c;</button>
                    @if (canPreviewPrototype(file)) { <button type="button" data-testid="proc-prototype-preview" (click)="previewPrototype(process, file)">\u9884\u89C8</button> }
                    <button type="button" data-testid="proc-prototype-download" (click)="downloadPrototype(process, file)">&#x4e0b;&#x8f7d;</button>
                    @if (editing) { <button type="button" data-testid="proc-prototype-remove" (click)="removePrototype(process, file)">&#x5220;&#x9664;</button> }
                  </div>
                  @if (isPrototypeExpanded(process, file) && file.versions?.length) {
                    <div class="attachment-version-list" data-testid="proc-prototype-version-list">
                      @for (version of file.versions; track version.uid || $index) {
                        <div class="attachment-version-item" data-testid="proc-prototype-version-item">
                          <div><strong>v{{ version.number || $index + 1 }}{{ version.uid === file.versionUid ? ' \\u00b7 \\u5f53\\u524d\\u5f15\\u7528' : '' }}</strong><span>{{ version.uploadedAt || '\\u672a\\u8bb0\\u5f55\\u4e0a\\u4f20\\u65f6\\u95f4' }}</span></div>
                          <div>@if (canPreviewPrototype(file, version)) { <button type="button" data-testid="proc-prototype-version-preview" (click)="previewPrototype(process, file, version)">\u9884\u89C8</button> }<button type="button" data-testid="proc-prototype-version-download" (click)="downloadPrototype(process, file, version)">&#x4e0b;&#x8f7d;</button></div>
                        </div>
                      }
                    </div>
                  }
                </div>
              } @empty {
                <div class="attachment-empty">&#x6682;&#x65e0;&#x9644;&#x4ef6;</div>
              }
            </div>
          </aside>
        }
      </section>
    </section>
  } @else {
    <div class="diag-empty" data-testid="process-flow-empty">&#x6682;&#x65e0;&#x6d41;&#x7a0b;&#x3002;&#x8bf7;&#x5148;&#x5728;&#x9636;&#x6bb5;&#x89c6;&#x56fe;&#x65b0;&#x589e;&#x6d41;&#x7a0b;&#x3002;</div>
  }
</div>
`, styles: ["/* src/app/workbenches/process/flow/process-flow-workbench.component.scss */\n:host {\n  display: flex;\n  flex-direction: column;\n  min-width: 0;\n  min-height: 0;\n  flex: 1;\n}\n/*# sourceMappingURL=process-flow-workbench.component.css.map */\n", '@charset "UTF-8";\n\n/* src/app/workbenches/process/flow/process-flow-layout.scss */\n.process-flow-view {\n  display: flex;\n  flex-direction: column;\n  width: 100%;\n  min-width: 0;\n  min-height: 0;\n  flex: 1;\n  padding: 10px 14px 16px;\n}\n.process-flow-frame {\n  display: grid;\n  grid-template-rows: auto auto 1fr;\n  gap: 8px;\n  min-width: 0;\n  min-height: 0;\n  height: 100%;\n}\n.process-flow-topbar {\n  display: grid;\n  grid-template-columns: minmax(150px, 0.7fr) minmax(180px, 0.9fr) minmax(180px, 1fr) minmax(160px, 0.85fr) minmax(160px, 0.85fr);\n  align-items: center;\n  gap: 8px;\n  min-height: 48px;\n  padding: 6px 10px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: #fff;\n  box-shadow: 0 1px 0 rgba(15, 23, 42, 0.03);\n}\n.stage-select,\n.process-select {\n  width: 100%;\n  height: 34px;\n  padding: 0 12px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 14px;\n  font-weight: 700;\n}\n.select-field {\n  grid-template-columns: auto minmax(0, 1fr);\n}\n.process-attachment-drawer input[type=file] {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  opacity: 0;\n  pointer-events: none;\n}\n.process-flow-body {\n  position: relative;\n  display: flex;\n  flex-direction: column;\n  min-width: 0;\n  min-height: 0;\n}\n.process-flow-edit-shortcut {\n  display: inline-flex;\n  width: calc(100% - 16px);\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n  padding: 2px 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 999px;\n  background: #f8fbff;\n  color: #64748b;\n  font-size: 12px;\n  font-weight: 600;\n}\n.process-flow-edit-shortcut .process-flow-tool-counts {\n  flex: 0 0 auto;\n  margin-left: auto;\n  margin-right: 0;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n  white-space: nowrap;\n}\n.process-flow-edit-shortcut strong {\n  color: #1d4ed8;\n  font-weight: 900;\n}\n.attachment-add-button {\n  display: inline-flex;\n  height: 32px;\n  align-items: center;\n  justify-content: center;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 8px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 700;\n}\n.compact-field {\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr);\n  align-items: center;\n  gap: 6px;\n}\n.compact-field span,\n.flow-inline-panel span {\n  color: #64748b;\n  font-size: 12px;\n  font-weight: 700;\n}\n.compact-field input,\n.flow-inline-panel input,\n.flow-inline-panel select,\n.flow-edge-panel input,\n.flow-edge-panel select {\n  height: 32px;\n  min-width: 0;\n  padding: 0 10px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 14px;\n}\n.flow-inline-panel select[multiple] {\n  height: 72px;\n}\n.flow-readonly .process-flow-side-tools {\n  display: grid;\n}\n.flow-readonly .process-flow-topbar input {\n  pointer-events: none;\n  opacity: 0.6;\n  background: #f8fafc;\n}\n.flow-readonly .flow-node,\n.flow-readonly .flow-terminal,\n.flow-readonly .flow-gateway {\n  pointer-events: auto;\n  cursor: pointer;\n}\n.flow-readonly {\n}\n/*# sourceMappingURL=process-flow-layout.css.map */\n', "/* src/app/workbenches/process/flow/process-flow-canvas.scss */\n.process-flow-canvas-shell {\n  position: relative;\n  flex: 1;\n  min-height: 240px;\n  padding: 10px;\n  overflow: auto;\n  border: 1px solid #cfe0f3;\n  border-radius: 12px;\n  background: #fff;\n  scrollbar-width: thin;\n}\n.process-flow-canvas-shell::-webkit-scrollbar {\n  width: 7px;\n  height: 7px;\n}\n.process-flow-canvas-shell::-webkit-scrollbar-thumb {\n  border-radius: 999px;\n  background: #c7d3df;\n}\n.process-flow-canvas {\n  position: relative;\n  min-width: 100%;\n  border: 1px solid #d8e4f2;\n  border-radius: 10px;\n  overflow: visible;\n  background:\n    linear-gradient(\n      90deg,\n      #f8fbff 0 116px,\n      transparent 116px),\n    #fff;\n  background-size: 100% 100%, 100% 100%;\n}\n/*# sourceMappingURL=process-flow-canvas.css.map */\n", '/* src/app/workbenches/process/flow/process-flow-tools.scss */\n.process-flow-side-tools {\n  position: absolute;\n  top: 10px;\n  right: 10px;\n  z-index: 20;\n  display: grid;\n  gap: 6px;\n  width: 72px;\n  padding: 5px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: rgba(255, 255, 255, 0.94);\n  box-shadow: 0 10px 24px rgba(15, 23, 42, 0.12);\n}\n.process-flow-side-tools button {\n  display: grid;\n  grid-template-columns: 18px 1fr;\n  width: 62px;\n  height: 30px;\n  align-items: center;\n  justify-content: center;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #fff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 800;\n}\n.process-flow-tool-counts {\n  align-self: center;\n  margin-right: 2px;\n  color: #64748b;\n  font-size: 11px;\n  font-weight: 700;\n  white-space: nowrap;\n}\n.process-flow-readonly-attachment {\n  position: absolute;\n  top: 10px;\n  right: 10px;\n  z-index: 20;\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  height: 30px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #fff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.process-flow-readonly-attachment b {\n  min-width: 18px;\n  padding: 1px 5px;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 11px;\n  text-align: center;\n}\n.process-flow-side-tools button b {\n  justify-self: center;\n}\n.process-flow-side-tools button span {\n  height: auto;\n  background: transparent;\n  color: #1d4ed8;\n  font-size: 12px;\n  font-weight: 700;\n  line-height: normal;\n}\n.tool-file-icon {\n  position: relative;\n  width: 13px;\n  height: 10px;\n  border: 1.5px solid currentColor;\n  border-radius: 2px;\n  font-size: 0;\n}\n.tool-file-icon::before {\n  content: "";\n  position: absolute;\n  left: 1px;\n  top: -4px;\n  width: 7px;\n  height: 4px;\n  border: 1.5px solid currentColor;\n  border-bottom: 0;\n  border-radius: 2px 2px 0 0;\n  background: #fff;\n}\n.process-flow-side-tools .danger {\n  color: #dc2626;\n}\n/*# sourceMappingURL=process-flow-tools.css.map */\n', '/* src/app/workbenches/process/flow/process-flow-nodes.scss */\n.flow-lane {\n  position: absolute;\n  left: 0;\n  right: 0;\n  border-bottom: 1px solid #dbeafe;\n  background:\n    linear-gradient(\n      90deg,\n      color-mix(in srgb, var(--lane-color, #2563eb) 18%, #f8fafc) 0 116px,\n      color-mix(in srgb, var(--lane-color, #2563eb) 7%, #fff) 116px);\n}\n.flow-lane-title {\n  position: absolute;\n  left: 0;\n  top: 0;\n  display: flex;\n  width: 116px;\n  height: 100%;\n  align-items: center;\n  justify-content: center;\n  border-right: 1px solid #94a3b8;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 14%, #fff);\n  color: var(--lane-color, #0f172a);\n  font-size: 14px;\n  font-weight: 800;\n  writing-mode: vertical-rl;\n  letter-spacing: 2px;\n}\n.flow-lane-title::before {\n  content: "";\n  position: absolute;\n  left: 10px;\n  top: 14px;\n  bottom: 14px;\n  width: 4px;\n  border-radius: 999px;\n  background: var(--lane-color, #2563eb);\n  opacity: 0.8;\n}\n.shared-badge {\n  position: absolute;\n  top: -12px;\n  right: -8px;\n  z-index: 4;\n  padding: 2px 7px;\n  border: 1px solid #93c5fd;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n  font-size: 11px;\n  font-weight: 800;\n  line-height: 1;\n  white-space: nowrap;\n  writing-mode: horizontal-tb;\n}\n.flow-edges {\n  position: absolute;\n  inset: 0;\n  overflow: visible;\n}\n.flow-alignment-guide {\n  position: absolute;\n  z-index: 16;\n  pointer-events: none;\n  background: #2563eb;\n  box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.14);\n}\n.flow-alignment-guide.is-vertical {\n  width: 1px;\n}\n.flow-alignment-guide.is-horizontal {\n  height: 1px;\n}\n.flow-edge {\n  fill: none;\n  stroke: #64748b;\n  stroke-width: 2;\n  marker-end: url(#process-flow-arrow);\n  cursor: pointer;\n}\n.flow-edge-hit {\n  fill: none;\n  stroke: transparent;\n  stroke-width: 14;\n  cursor: pointer;\n}\n.flow-edge.preview {\n  stroke: #2563eb;\n  stroke-dasharray: 7 5;\n  marker-end: none;\n  pointer-events: none;\n}\n.flow-edge.active {\n  stroke: #2563eb;\n  stroke-width: 3;\n}\n.flow-edges marker path {\n  fill: #64748b;\n}\n.flow-edge-label {\n  fill: #0f172a;\n  font-size: 12px;\n  cursor: text;\n  -webkit-user-select: none;\n  user-select: none;\n}\n.flow-edge-label-input {\n  position: absolute;\n  z-index: 130;\n  width: 148px;\n  min-height: 26px;\n  padding: 2px 7px;\n  border: 1px solid #93c5fd;\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  outline: none;\n  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.12);\n}\n.flow-node,\n.flow-gateway,\n.flow-terminal {\n  position: absolute;\n  z-index: 5;\n  color: #0f3b70;\n  cursor: pointer;\n  font-weight: 800;\n}\n.flow-connect-box {\n  position: absolute;\n  z-index: 18;\n  border: 1px dashed #60a5fa;\n  border-radius: 6px;\n  pointer-events: none;\n}\n.connect-anchor {\n  position: absolute;\n  display: grid;\n  width: 11px;\n  height: 11px;\n  transform: translate(-50%, -50%);\n  place-items: center;\n  border: 1px solid #93c5fd;\n  border-radius: 999px;\n  background: #fff;\n  color: #2563eb;\n  cursor: crosshair;\n  font-size: 9px;\n  font-weight: 800;\n  line-height: 1;\n  pointer-events: auto;\n}\n.flow-node {\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n  min-width: 132px;\n  min-height: 54px;\n  padding: 7px 10px;\n  border: 1px solid color-mix(in srgb, var(--lane-color, #2563eb) 42%, #bfdbfe);\n  border-radius: 8px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 16%, #eff6ff);\n  box-shadow: inset 4px 0 0 color-mix(in srgb, var(--lane-color, #2563eb) 78%, #1e40af);\n  text-align: center;\n}\n.flow-node.is-editing {\n  padding-bottom: 8px;\n}\n.flow-node-name-row {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: 4px;\n}\n.flow-node-name-input {\n  min-width: 0;\n  width: 92px;\n  height: 22px;\n  padding: 0 5px;\n  border: 1px solid #93c5fd;\n  border-radius: 5px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n  text-align: center;\n  outline: none;\n}\n.flow-node-name-input:focus {\n  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.16);\n}\n.flow-node-edit-icon {\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n  flex-shrink: 0;\n}\n.flow-node-edit-icon:hover {\n  color: #3b82f6;\n}\n.flow-node strong,\n.flow-node small {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.flow-node strong {\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 82%, #0f172a);\n}\n.flow-node small {\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 66%, #475569);\n  font-size: 12px;\n  font-weight: 600;\n}\n.flow-node-role-tags {\n  position: relative;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 3px;\n  justify-content: center;\n}\n.flow-node-role-tag {\n  display: inline-flex;\n  align-items: center;\n  gap: 2px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 10%, #fff);\n  border: 1px solid color-mix(in srgb, var(--lane-color, #2563eb) 34%, #cbd5e1);\n  border-radius: 4px;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 72%, #334155);\n  font-size: 10px;\n  font-weight: 600;\n  padding: 1px 5px;\n}\n.flow-node-role-remove {\n  background: none;\n  border: 0;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n  padding: 0 1px;\n  line-height: 1;\n}\n.flow-node-role-remove:hover {\n  color: #ef4444;\n}\n.flow-node-role-add {\n  background: none;\n  border: 1px dashed #cbd5e1;\n  border-radius: 4px;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 10px;\n  padding: 1px 5px;\n}\n.flow-node-role-add:hover {\n  border-color: #3b82f6;\n  color: #3b82f6;\n}\n.flow-node-role-picker {\n  position: absolute;\n  left: 50%;\n  top: calc(100% + 6px);\n  z-index: 160;\n  display: grid;\n  width: 184px;\n  max-height: 220px;\n  transform: translateX(-50%);\n  gap: 7px;\n  overflow-y: auto;\n  padding: 9px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  box-shadow: 0 14px 28px rgba(15, 23, 42, 0.16);\n  text-align: left;\n}\n.flow-node-role-picker strong {\n  color: #475569;\n  font-size: 12px;\n}\n.flow-terminal {\n  display: inline-flex;\n  width: 50px;\n  height: auto;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 3px;\n  border: 0;\n  background: transparent;\n  color: #0f172a;\n  font-size: 12px;\n  font-weight: 800;\n}\n.flow-terminal:not(.is-end) {\n  flex-direction: row-reverse;\n}\n.flow-terminal::before {\n  content: "";\n  width: 18px;\n  height: 18px;\n  border-radius: 999px;\n  background: #3f6fb5;\n}\n.flow-terminal.is-end::before {\n  background: #64748b;\n}\n.flow-gateway {\n  display: grid;\n  width: 22px;\n  height: 22px;\n  align-items: center;\n  justify-content: center;\n  border: 0;\n  background: transparent;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 78%, #0f172a);\n  font-size: 12px;\n  font-weight: 800;\n  text-align: center;\n}\n.flow-gateway::before {\n  content: "";\n  position: absolute;\n  left: 2px;\n  top: 2px;\n  width: 16px;\n  height: 16px;\n  z-index: -1;\n  transform: rotate(45deg);\n  border: 1.4px solid color-mix(in srgb, var(--lane-color, #2563eb) 48%, #93c5fd);\n  border-radius: 2px;\n  background: color-mix(in srgb, var(--lane-color, #2563eb) 8%, #fff);\n}\n.flow-gateway::after {\n  content: "+";\n  position: absolute;\n  left: 2px;\n  top: 2px;\n  display: grid;\n  width: 16px;\n  height: 16px;\n  place-items: center;\n  color: color-mix(in srgb, var(--lane-color, #2563eb) 70%, #0f172a);\n  font-size: 16px;\n  font-weight: 900;\n  line-height: 1;\n  opacity: 0.25;\n}\n.flow-gateway-label {\n  position: absolute;\n  left: 50%;\n  top: 22px;\n  display: block;\n  width: max-content;\n  max-width: 96px;\n  transform: translateX(-50%);\n  white-space: normal;\n  line-height: 1.25;\n  overflow-wrap: anywhere;\n  word-break: break-word;\n  font-size: 11px;\n}\n.flow-gateway-name-input {\n  position: absolute;\n  left: 50%;\n  top: 20px;\n  z-index: 8;\n  width: 128px;\n  min-height: 28px;\n  transform: translateX(-50%);\n  padding: 3px 6px;\n  border: 1px solid #93c5fd;\n  border-radius: 6px;\n  background: #fff;\n  color: #0f172a;\n  font-size: 11px;\n  font-weight: 800;\n  text-align: center;\n  outline: none;\n}\n.flow-gateway-edit-icon {\n  position: absolute;\n  right: 6px;\n  top: 2px;\n  z-index: 2;\n  color: #94a3b8;\n  cursor: pointer;\n  font-size: 11px;\n}\n.flow-gateway-edit-icon:hover {\n  color: #3b82f6;\n}\n.flow-node.active,\n.flow-gateway.active,\n.flow-terminal.active,\n.flow-node.connecting,\n.flow-gateway.connecting,\n.flow-terminal.connecting {\n  border-color: #2563eb;\n  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.18);\n}\n/*# sourceMappingURL=process-flow-nodes.css.map */\n', "/* src/app/workbenches/process/flow/process-flow-inline.scss */\n.flow-inline-panel,\n.flow-edge-panel {\n  position: absolute;\n  z-index: 120;\n  display: grid;\n  gap: 8px;\n  width: 270px;\n  padding: 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 10px;\n  background: rgba(255, 255, 255, 0.96);\n  box-shadow: 0 12px 24px rgba(15, 23, 42, 0.12);\n}\n.flow-inline-panel label {\n  display: grid;\n  gap: 4px;\n}\n.flow-role-combo {\n  position: relative;\n}\n.flow-role-trigger {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  height: 32px;\n  align-items: center;\n  gap: 8px;\n  padding: 0 9px;\n  border: 1px solid #cbd8e6;\n  border-radius: 8px;\n  background: #fff;\n  color: #0f172a;\n  cursor: pointer;\n  font-size: 13px;\n  text-align: left;\n}\n.flow-role-trigger.has-value {\n  border-color: #60a5fa;\n  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.12);\n}\n.flow-role-trigger span {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.flow-role-trigger b {\n  color: #64748b;\n  font-weight: 700;\n}\n.flow-role-menu {\n  position: absolute;\n  left: 0;\n  right: 0;\n  top: calc(100% + 4px);\n  z-index: 140;\n  display: grid;\n  gap: 8px;\n  max-height: min(280px, 100vh - 180px);\n  overflow-y: auto;\n  overscroll-behavior: contain;\n  padding: 10px;\n  border: 1px solid #cbd8e6;\n  border-radius: 9px;\n  background: #fff;\n  box-shadow: 0 14px 28px rgba(15, 23, 42, 0.15);\n}\n.flow-role-menu::-webkit-scrollbar {\n  width: 8px;\n}\n.flow-role-menu::-webkit-scrollbar-thumb {\n  border: 2px solid #fff;\n  border-radius: 999px;\n  background: #c7d4e3;\n}\n.flow-role-menu strong {\n  color: #475569;\n  font-size: 13px;\n}\n.flow-role-option {\n  display: grid !important;\n  grid-template-columns: auto minmax(0, 1fr);\n  align-items: center;\n  gap: 8px !important;\n  min-height: 26px;\n  color: #0f172a;\n  font-size: 13px;\n}\n.flow-role-option input {\n  width: 14px;\n  height: 14px;\n  accent-color: #2563eb;\n}\n.flow-role-menu-footer {\n  display: flex;\n  justify-content: flex-end;\n  padding-top: 8px;\n  border-top: 1px solid #e2e8f0;\n}\n.flow-role-menu-footer button {\n  height: 28px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 7px;\n  background: #eff6ff;\n  color: #2563eb;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 700;\n}\n.inline-panel-actions {\n  display: flex;\n  justify-content: flex-end;\n  gap: 8px;\n}\n.inline-panel-actions button {\n  min-height: 30px;\n  padding: 0 10px;\n  border: 1px solid #bfdbfe;\n  border-radius: 8px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  cursor: pointer;\n  font-size: 13px;\n  font-weight: 700;\n}\n.flow-edge-panel {\n  right: 24px;\n  bottom: 24px;\n  grid-template-columns: 1fr auto 1fr;\n  width: 520px;\n  align-items: center;\n}\n.flow-edge-panel input {\n  grid-column: 1/-1;\n}\n/*# sourceMappingURL=process-flow-inline.css.map */\n", '@charset "UTF-8";\n\n/* src/app/workbenches/process/flow/process-flow-attachments.scss */\n.process-attachment-drawer {\n  display: grid;\n  align-content: start;\n  gap: 10px;\n  height: auto;\n  max-height: min(520px, 100vh - 218px);\n  min-height: 0;\n  padding: 12px;\n  border: 1px solid #d7e4f3;\n  border-radius: 12px;\n  background: #fff;\n  box-shadow: 0 10px 24px rgba(15, 23, 42, 0.06);\n}\n.process-attachment-drawer {\n  position: absolute;\n  top: 0;\n  right: 12px;\n  z-index: 35;\n  width: min(360px, 100vw - 32px);\n  box-shadow: 0 18px 46px rgba(15, 23, 42, 0.18);\n}\n.attachment-panel-head {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  color: #0f172a;\n}\n.attachment-panel-head strong {\n  flex: 1;\n}\n.attachment-panel-head span {\n  padding: 2px 8px;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #1d4ed8;\n  font-size: 12px;\n  font-weight: 700;\n}\n.attachment-panel-head button {\n  width: 28px;\n  height: 28px;\n  border: 1px solid #bfdbfe;\n  border-radius: 999px;\n  background: #eff6ff;\n  color: #2563eb;\n}\n.attachment-list {\n  display: grid;\n  gap: 8px;\n  max-height: min(420px, 100vh - 310px);\n  overflow: auto;\n}\n.attachment-card {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr);\n  gap: 10px;\n  align-items: start;\n  padding: 12px;\n  border: 1px solid #d7e4f3;\n  border-radius: 10px;\n  background: #f8fbff;\n}\n.attachment-card-meta {\n  display: grid;\n  gap: 4px;\n  min-width: 0;\n}\n.attachment-card-meta strong {\n  display: block;\n  max-width: 100%;\n  overflow: hidden;\n  color: #0f172a;\n  font-size: 14px;\n  font-weight: 800;\n  line-height: 1.35;\n  overflow-wrap: anywhere;\n}\n.attachment-card-meta span {\n  overflow: hidden;\n  color: #475569;\n  font-size: 12px;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.attachment-card-meta small,\n.attachment-empty {\n  color: #64748b;\n  font-size: 12px;\n}\n.attachment-card-actions {\n  display: flex;\n  flex-wrap: wrap;\n  justify-content: flex-start;\n  gap: 5px;\n}\n.attachment-card-actions button,\n.attachment-version-item button {\n  height: 28px;\n  padding: 0 10px;\n  border: 1px solid #d7e4f3;\n  border-radius: 8px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n}\n.attachment-version-list {\n  grid-column: 1/-1;\n  display: grid;\n  gap: 6px;\n  padding-top: 10px;\n  border-top: 1px dashed #d7e4f3;\n}\n.attachment-version-item {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto;\n  align-items: center;\n  gap: 8px;\n  padding: 7px 0;\n}\n.attachment-version-meta {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  min-width: 0;\n  flex-wrap: wrap;\n}\n.attachment-version-meta strong {\n  min-width: 42px;\n  color: #1d4ed8;\n  font-size: 13px;\n}\n.attachment-version-meta span {\n  color: #64748b;\n  font-size: 12px;\n  white-space: nowrap;\n}\n.attachment-version-actions {\n  display: flex;\n  gap: 5px;\n  flex-wrap: wrap;\n  justify-content: flex-end;\n}\n.attachment-version-item > div:first-child {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 6px;\n  min-width: 0;\n}\n.attachment-version-item > div:first-child strong {\n  display: inline-flex;\n  align-items: center;\n  min-width: 34px;\n  height: 24px;\n  padding: 0 8px;\n  border-radius: 7px;\n  background: #e0edff;\n  color: #1557b0;\n  font-size: 13px;\n  font-weight: 800;\n}\n.attachment-version-item > div:first-child span {\n  display: inline-flex;\n  align-items: center;\n  min-height: 24px;\n  color: #64748b;\n  font-size: 12px;\n  white-space: nowrap;\n}\n.attachment-version-item > div:first-child strong + span {\n  padding: 0 6px;\n  border-radius: 999px;\n  background: #dcfce7;\n  color: #15803d;\n  font-weight: 700;\n}\n.attachment-version-item > div:first-child strong + span + span {\n  padding-left: 2px;\n  color: #64748b;\n}\n/*# sourceMappingURL=process-flow-attachments.css.map */\n'] }]
  }], null, { editing: [{
    type: Input
  }], exportGraphId: [{
    type: Input
  }], previewProcessId: [{
    type: Input
  }], taskEditorRequested: [{
    type: Output
  }], onDocumentPointerUp: [{
    type: HostListener,
    args: ["document:pointerup", ["$event"]]
  }], onDocumentPointerMove: [{
    type: HostListener,
    args: ["document:pointermove", ["$event"]]
  }], onWindowPointerUp: [{
    type: HostListener,
    args: ["window:pointerup", ["$event"]]
  }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(ProcessFlowWorkbenchComponent, { className: "ProcessFlowWorkbenchComponent", filePath: "src/app/workbenches/process/flow/process-flow-workbench.component.ts", lineNumber: 114 });
})();

// src/app/workbenches/process/stage/process-stage-legacy-adapter.ts
function createProcessStageLegacyAdapter(legacyWindow = getAngularRuntimeState()) {
  const state = () => {
    const direct = legacyWindow;
    if (direct.doc || direct.ui)
      return { doc: direct.doc, ui: direct.ui };
    return legacyWindow.S || {};
  };
  const document2 = () => state().doc || {};
  const ui = () => {
    state().ui ||= {};
    return state().ui;
  };
  function markModified(sidebar = false) {
    if (legacyWindow.markModified)
      legacyWindow.markModified();
    else
      markAngularRuntimeModified();
    if (sidebar) {
      if (legacyWindow.renderSidebar)
        legacyWindow.renderSidebar();
      else
        emitRuntimeRefresh();
    }
  }
  function stageId(stage) {
    return String(stage?.id || stage?.uid || "").trim();
  }
  function processId(process2) {
    return String(process2?.uid || process2?.id || "").trim();
  }
  function refId(ref) {
    return String(ref?.id || ref?.uid || "").trim();
  }
  function findStage(targetId) {
    return (document2().stages || []).find((stage) => stageId(stage) === targetId || stage.id === targetId || stage.uid === targetId);
  }
  function findProcess(targetId) {
    return (document2().processes || []).find((process2) => processId(process2) === targetId || process2.id === targetId || process2.uid === targetId);
  }
  function normalizeStageId(targetStageId) {
    const target = String(targetStageId || "").trim();
    if (!target)
      return "";
    const stage = findStage(target);
    return stage ? stageId(stage) : target;
  }
  function refsForStage(targetStageId) {
    const stage = findStage(targetStageId);
    const keys = new Set([targetStageId, stage?.uid, stage?.id].filter(Boolean));
    return (document2().stageFlowRefs || []).filter((ref) => keys.has(ref.stageUid || "") || keys.has(ref.stageId || "")).sort((left, right) => Number(left.order || 0) - Number(right.order || 0));
  }
  function processForRef(ref) {
    return findProcess(ref.processUid || ref.processId || "") || null;
  }
  function stageIdForProcess(targetProcessId) {
    const process2 = findProcess(targetProcessId);
    const keys = new Set([targetProcessId, process2?.uid, process2?.id].filter(Boolean).map(String));
    const currentStageId = normalizeStageId(String(ui().stageId || "").trim());
    const currentRef = (document2().stageFlowRefs || []).find((ref2) => normalizeStageId(String(ref2.stageUid || ref2.stageId || "").trim()) === currentStageId && (keys.has(String(ref2.processUid || "")) || keys.has(String(ref2.processId || ""))));
    if (currentRef)
      return normalizeStageId(String(currentRef.stageUid || currentRef.stageId || currentStageId).trim());
    const ref = (document2().stageFlowRefs || []).filter((item) => keys.has(String(item.processUid || "")) || keys.has(String(item.processId || ""))).sort((left, right) => Number(left.order || 0) - Number(right.order || 0))[0];
    return normalizeStageId(String(ref?.stageUid || ref?.stageId || process2?.stageUid || process2?.stageId || "").trim());
  }
  function nextProcessId() {
    const used = new Set((document2().processes || []).map((process2) => process2.id || process2.uid).filter(Boolean));
    for (let index = 1; index < 1e4; index += 1) {
      const id = `P${index}`;
      if (!used.has(id))
        return id;
    }
    return `P${Date.now()}`;
  }
  function nextRefId() {
    const used = new Set((document2().stageFlowRefs || []).map((ref) => ref.id || ref.uid).filter(Boolean));
    for (let index = 1; index < 1e4; index += 1) {
      const id = `SFR${index}`;
      if (!used.has(id))
        return id;
    }
    return `SFR${Date.now()}`;
  }
  function nextLinkId() {
    const used = new Set((document2().stageFlowLinks || []).map((link) => link.id || link.uid).filter(Boolean));
    for (let index = 1; index < 1e4; index += 1) {
      const id = `SFL${index}`;
      if (!used.has(id))
        return id;
    }
    return `SFL${Date.now()}`;
  }
  function nextId(prefix) {
    return `${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
  }
  return {
    document: document2,
    ui,
    columns: () => ensureValueDomainModel(document2(), nextId).columns,
    lanes: () => ensureValueDomainModel(document2(), nextId).lanes,
    stages: () => document2().stages || [],
    processes: () => document2().processes || [],
    stageId,
    processId,
    refId,
    stageRefs: refsForStage,
    stageProcesses: (targetStageId) => refsForStage(targetStageId).map((ref) => ({ ref, process: processForRef(ref) })),
    stageLinks: (targetStageId) => {
      const refIds = new Set(refsForStage(targetStageId).map(refId));
      return (document2().stageFlowLinks || []).filter((link) => link.stageUid === targetStageId || link.stageId === targetStageId || refIds.has(link.fromRefUid || link.fromRefId || "") && refIds.has(link.toRefUid || link.toRefId || ""));
    },
    processCount: (targetStageId) => refsForStage(targetStageId).length,
    openPanorama() {
      ui().procView = "stage";
      ui().stageViewMode = "panorama";
      ui().stageEditorCollapsed = true;
      if (legacyWindow.renderProcessTab)
        legacyWindow.renderProcessTab();
      else
        emitRuntimeRefresh();
    },
    openDetail(targetStageId) {
      ui().procView = "stage";
      ui().stageViewMode = "detail";
      ui().stageId = targetStageId;
      if (legacyWindow.renderProcessTab)
        legacyWindow.renderProcessTab();
      else
        emitRuntimeRefresh();
    },
    openProcess(targetProcessId) {
      const nextStageId = stageIdForProcess(targetProcessId);
      if (nextStageId)
        ui().stageId = nextStageId;
      const options = { procView: "flow", procId: targetProcessId, stageId: nextStageId || void 0, taskId: null };
      if (legacyWindow.navigate)
        legacyWindow.navigate("process", options);
      else
        navigateAngularWorkbench("process", options);
    },
    setStageName(targetStageId, value) {
      const stage = findStage(targetStageId);
      if (!stage)
        return;
      stage.name = value;
      markModified(true);
    },
    setProcessName(targetProcessId, value) {
      const process2 = findProcess(targetProcessId);
      if (!process2)
        return;
      process2.name = value;
      markModified(true);
    },
    setProcessGroup(targetProcessId, value) {
      const process2 = findProcess(targetProcessId);
      if (!process2)
        return;
      process2.flowGroup = value;
      markModified();
    },
    setRefOffset(targetRefId, offset) {
      const ref = (document2().stageFlowRefs || []).find((item) => refId(item) === targetRefId);
      if (!ref)
        return;
      ref.pos = {
        x: Math.round(Number(offset.x) || 0),
        y: Math.round(Number(offset.y) || 0)
      };
      console.log("[stage-adapter] setRefOffset", targetRefId, ref.pos);
      markModified();
    },
    addProcess(targetStageId) {
      const id = nextProcessId();
      const refId2 = nextRefId();
      const stage = findStage(targetStageId);
      const doc = document2();
      doc.processes ||= [];
      doc.stageFlowRefs ||= [];
      doc.processes.push({ uid: id, id, name: `\u65B0\u6D41\u7A0B${doc.processes.length + 1}`, stageId: stage?.id || targetStageId, flowGroup: "", nodes: [] });
      doc.stageFlowRefs.push({ uid: refId2, id: refId2, stageId: stage?.id || targetStageId, processUid: id, processId: id, order: refsForStage(targetStageId).length + 1 });
      markModified(true);
    },
    addExistingProcess(targetStageId, targetProcessId) {
      const process2 = findProcess(targetProcessId);
      const stage = findStage(targetStageId);
      if (!process2 || !stage)
        return;
      const alreadyInStage = refsForStage(targetStageId).some((ref) => {
        const refProcess = processForRef(ref);
        return processId(refProcess) === processId(process2);
      });
      if (alreadyInStage)
        return;
      const doc = document2();
      doc.stageFlowRefs ||= [];
      doc.stageFlowRefs.push({
        id: nextRefId(),
        stageId: stage.id || targetStageId,
        stageUid: stage.uid || stage.id || targetStageId,
        processId: process2.id || process2.uid,
        processUid: process2.uid || process2.id,
        order: refsForStage(targetStageId).length + 1
      });
      markModified(true);
    },
    duplicateProcess(targetProcessId) {
      const source = findProcess(targetProcessId);
      if (!source)
        return;
      const doc = document2();
      doc.processes ||= [];
      doc.stageFlowRefs ||= [];
      const baseName = String(source.name || "\u672A\u547D\u540D\u6D41\u7A0B").replace(/\s*[-–—]?\s*副本(\d*)$/, "");
      const existingNames = new Set(doc.processes.map((p) => String(p.name || "").trim()));
      let copies = doc.processes.filter((p) => String(p.name || "").startsWith(baseName)).length;
      let cloneName = `${baseName}${copies > 0 ? ` - \u526F\u672C${copies}` : " - \u526F\u672C"}`;
      while (existingNames.has(cloneName)) {
        copies++;
        cloneName = `${baseName} - \u526F\u672C${copies}`;
      }
      const procId = `P${Date.now()}-${Math.random().toString(36).slice(2, 6)}`;
      const clone = JSON.parse(JSON.stringify(source));
      clone.id = procId;
      clone.uid = procId;
      clone.name = cloneName;
      const raw = clone;
      raw.stageUid = "";
      raw.stageId = "";
      raw.prototypeFiles = [];
      const nodes = Array.isArray(raw.nodes) ? raw.nodes : [];
      const taskIdMap = /* @__PURE__ */ new Map();
      for (const node of nodes) {
        const oldId = String(node.id || node.uid || "");
        const newId = nextProcessId();
        node.id = newId;
        node.uid = newId;
        node.name = String(node.name || "") + " \u526F\u672C";
        taskIdMap.set(oldId, newId);
      }
      const flow = raw.flow || {};
      const gateways = Array.isArray(flow.nodes) ? flow.nodes : [];
      const gatewayIdMap = /* @__PURE__ */ new Map();
      for (const gw of gateways) {
        const oldId = String(gw.id || gw.uid || "");
        const newId = `B${Date.now()}` + (gatewayIdMap.size + 1);
        gw.id = newId;
        gw.uid = newId;
        gatewayIdMap.set(oldId, newId);
      }
      flow.edges = (Array.isArray(flow.edges) ? flow.edges : []).map((edge) => {
        const eid = `L${Date.now()}` + Math.random().toString(16).slice(2, 6);
        const from2 = edge.from && edge.from !== "START" && edge.from !== "END" ? taskIdMap.get(edge.from) || gatewayIdMap.get(edge.from) || "" : edge.from || "";
        const to = edge.to && edge.to !== "START" && edge.to !== "END" ? taskIdMap.get(edge.to) || gatewayIdMap.get(edge.to) || "" : edge.to || "";
        return from2 && to ? __spreadProps(__spreadValues({}, edge), { id: eid, uid: eid, from: from2, to }) : null;
      }).filter(Boolean);
      const sourceIds = new Set([targetProcessId, source.uid, source.id, source.uid || source.id].filter(Boolean));
      const existingRefs = (doc.stageFlowRefs || []).filter((ref) => {
        const puid = ref.processUid || ref.processId || "";
        return puid && sourceIds.has(puid);
      });
      const seenStages = /* @__PURE__ */ new Set();
      for (const ref of existingRefs) {
        const stageKey = ref.stageUid || ref.stageId || "";
        if (!stageKey || seenStages.has(stageKey))
          continue;
        seenStages.add(stageKey);
        const alreadyLinked = (doc.stageFlowRefs || []).some((r) => {
          const rpuid = r.processUid || r.processId || "";
          const rsid = r.stageUid || r.stageId || "";
          return rpuid === procId && rsid === stageKey;
        });
        if (alreadyLinked)
          continue;
        doc.stageFlowRefs.push({
          id: nextRefId(),
          stageId: ref.stageId || "",
          stageUid: ref.stageUid || "",
          processId: procId,
          processUid: procId,
          order: doc.stageFlowRefs.length + 1
        });
      }
      doc.processes.push(raw);
      markModified(true);
    },
    removeProcessFromStage(targetStageId, targetProcessId) {
      const refs = refsForStage(targetStageId);
      const removedRefIds = new Set(refs.filter((ref) => {
        const process2 = processForRef(ref);
        return processId(process2) === targetProcessId || process2?.id === targetProcessId || process2?.uid === targetProcessId;
      }).map(refId));
      document2().stageFlowRefs = (document2().stageFlowRefs || []).filter((ref) => !removedRefIds.has(refId(ref)));
      document2().stageFlowLinks = (document2().stageFlowLinks || []).filter((link) => !removedRefIds.has(link.fromRefUid || link.fromRefId || "") && !removedRefIds.has(link.toRefUid || link.toRefId || ""));
      markModified(true);
    },
    deleteProcess(targetProcessId) {
      const process2 = findProcess(targetProcessId);
      if (!process2)
        return;
      const keys = new Set([process2.id, process2.uid, targetProcessId].filter(Boolean));
      const removedRefIds = new Set((document2().stageFlowRefs || []).filter((ref) => keys.has(ref.processId || "") || keys.has(ref.processUid || "")).map(refId));
      document2().processes = (document2().processes || []).filter((item) => !keys.has(item.id || "") && !keys.has(item.uid || ""));
      document2().stageFlowRefs = (document2().stageFlowRefs || []).filter((ref) => !removedRefIds.has(refId(ref)));
      document2().stageFlowLinks = (document2().stageFlowLinks || []).filter((link) => !removedRefIds.has(link.fromRefUid || link.fromRefId || "") && !removedRefIds.has(link.toRefUid || link.toRefId || ""));
      markModified(true);
    },
    addLink(targetStageId, fromRefId, toRefId) {
      if (!fromRefId || !toRefId || fromRefId === toRefId)
        return;
      document2().stageFlowLinks ||= [];
      const duplicate = document2().stageFlowLinks?.some((link) => (link.stageUid === targetStageId || link.stageId === targetStageId) && (link.fromRefUid || link.fromRefId) === fromRefId && (link.toRefUid || link.toRefId) === toRefId);
      if (duplicate)
        return;
      document2().stageFlowLinks?.push({ id: nextLinkId(), stageId: targetStageId, fromRefId, toRefId });
      markModified();
    },
    removeLink(_targetStageId, linkId) {
      document2().stageFlowLinks = (document2().stageFlowLinks || []).filter((link) => (link.id || link.uid) !== linkId);
      markModified();
    }
  };
}

// src/app/workbenches/process/stage/process-stage-workbench.component.ts
var _forTrack03 = ($index, $item) => $item.id;
var _forTrack18 = ($index, $item) => $item.link.id || $item.link.uid;
function _forTrack23($index, $item) {
  return this.columnUid($item);
}
function _forTrack33($index, $item) {
  return this.stageId($item);
}
function _forTrack43($index, $item) {
  return this.laneUid($item);
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 16);
    \u0275\u0275listener("blur", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_6_Template_input_blur_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const stage_r4 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.finishStageRename(stage_r4, $event.target.value));
    })("keydown.enter", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_6_Template_input_keydown_enter_0_listener($event) {
      \u0275\u0275restoreView(_r3);
      const stage_r4 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.finishStageRename(stage_r4, $event.target.value));
    })("keydown.escape", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_6_Template_input_keydown_escape_0_listener() {
      \u0275\u0275restoreView(_r3);
      const ctx_r1 = \u0275\u0275nextContext(3);
      return \u0275\u0275resetView(ctx_r1.renamingStageId.set(""));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r4 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275property("ngModel", stage_r4.name || ctx_r1.stageId(stage_r4));
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "span", 17);
    \u0275\u0275listener("dblclick", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_7_Template_span_dblclick_0_listener($event) {
      \u0275\u0275restoreView(_r5);
      const stage_r4 = \u0275\u0275nextContext();
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.startStageRename(stage_r4, $event));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r4 = \u0275\u0275nextContext();
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate1(" ", stage_r4.name || ctx_r1.stageId(stage_r4), " ");
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 27);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_3_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r6);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.addProcess());
    });
    \u0275\u0275text(1, "+ \u6D41\u7A0B");
    \u0275\u0275elementEnd();
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 28)(1, "div", 29);
    \u0275\u0275text(2);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const group_r7 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275styleProp("left", group_r7.x, "px")("top", group_r7.y, "px")("width", group_r7.w, "px")("height", group_r7.h, "px");
    \u0275\u0275classProp("is-drag-target", ctx_r1.isGroupDragTarget(group_r7));
    \u0275\u0275attribute("data-flow-group", group_r7.label);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(group_r7.label);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275namespaceSVG();
    \u0275\u0275element(0, "path", 25);
  }
  if (rf & 2) {
    const link_r8 = ctx.$implicit;
    \u0275\u0275attribute("d", link_r8.path);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_12_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 31);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_12_For_1_Template_button_click_0_listener() {
      const link_r10 = \u0275\u0275restoreView(_r9).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(5);
      return \u0275\u0275resetView(ctx_r1.removeLink(link_r10));
    });
    \u0275\u0275text(1, "\xD7");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const link_r10 = ctx.$implicit;
    \u0275\u0275styleProp("left", (link_r10.from.x + link_r10.to.x) / 2, "px")("top", link_r10.from.y + 52, "px");
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275repeaterCreate(0, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_12_For_1_Template, 2, 4, "button", 30, _forTrack18);
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275repeater(ctx_r1.flowLinks());
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 47);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_4_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r14);
      const ctx_r1 = \u0275\u0275nextContext(6);
      return \u0275\u0275resetView(ctx_r1.cancelLink());
    });
    \u0275\u0275text(1, "\u21BA");
    \u0275\u0275elementEnd();
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 48);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_5_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r15);
      const node_r12 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.completeLink(node_r12));
    });
    \u0275\u0275text(1, "\u21A6");
    \u0275\u0275elementEnd();
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 49);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_6_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r16);
      const node_r12 = \u0275\u0275nextContext(2).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.beginLink(node_r12));
    });
    \u0275\u0275text(1, "\u2192");
    \u0275\u0275elementEnd();
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 54);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_For_3_Template_button_click_0_listener() {
      const col_r18 = \u0275\u0275restoreView(_r17).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(7);
      return \u0275\u0275resetView(ctx_r1.selectMigrateColumn(ctx_r1.columnUid(col_r18)));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const col_r18 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(7);
    \u0275\u0275classProp("active", ctx_r1.migratePickerColumnUid() === ctx_r1.columnUid(col_r18));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(col_r18.name || ctx_r1.columnUid(col_r18));
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Conditional_4_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r19 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 54);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Conditional_4_For_2_Template_button_click_0_listener() {
      const st_r20 = \u0275\u0275restoreView(_r19).$implicit;
      const node_r12 = \u0275\u0275nextContext(4).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.migrateProcess(node_r12, ctx_r1.stageId(st_r20)));
    });
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const st_r20 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(8);
    \u0275\u0275classProp("current", (ctx_r1.currentStage() ? ctx_r1.stageId(ctx_r1.currentStage()) : "") === ctx_r1.stageId(st_r20));
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(st_r20.name || ctx_r1.stageId(st_r20));
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 53);
    \u0275\u0275repeaterCreate(1, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Conditional_4_For_2_Template, 2, 3, "button", 55, _forTrack33, true);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(7);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.migratePickerStages());
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 50);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Template_div_click_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementStart(1, "div", 51);
    \u0275\u0275repeaterCreate(2, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_For_3_Template, 2, 3, "button", 52, _forTrack23, true);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(4, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Conditional_4_Template, 3, 0, "div", 53);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    let tmp_17_0;
    const ctx_r1 = \u0275\u0275nextContext(6);
    \u0275\u0275advance(2);
    \u0275\u0275repeater(ctx_r1.columns());
    \u0275\u0275advance(2);
    \u0275\u0275conditional((tmp_17_0 = ctx_r1.migratePickerColumnUid()) ? 4 : -1, tmp_17_0);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "textarea", 34);
    \u0275\u0275listener("ngModelChange", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_textarea_ngModelChange_0_listener($event) {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.setProcessName(node_r12, $event));
    })("mousedown", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_textarea_mousedown_0_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(1, "div", 35);
    \u0275\u0275listener("mousedown", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_div_mousedown_1_listener($event) {
      return $event.stopPropagation();
    })("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_div_click_1_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementStart(2, "button", 36);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_button_click_2_listener() {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.openProcess(node_r12));
    });
    \u0275\u0275text(3, "\u2197");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(4, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_4_Template, 2, 0, "button", 37)(5, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_5_Template, 2, 0, "button", 38)(6, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_6_Template, 2, 0, "button", 39);
    \u0275\u0275elementStart(7, "button", 40);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_button_click_7_listener() {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.duplicateProcess(node_r12));
    });
    \u0275\u0275text(8, "\u29C9");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "button", 41);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_button_click_9_listener($event) {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.toggleMigratePicker(node_r12, $event));
    });
    \u0275\u0275text(10, "\u25A4");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(11, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Conditional_11_Template, 5, 1, "div", 42);
    \u0275\u0275elementStart(12, "button", 43);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_button_click_12_listener() {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.deleteProcess(node_r12));
    });
    \u0275\u0275text(13, "\xD7");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(14, "div", 44);
    \u0275\u0275listener("mousedown", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_div_mousedown_14_listener($event) {
      return $event.stopPropagation();
    })("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_div_click_14_listener($event) {
      return $event.stopPropagation();
    });
    \u0275\u0275elementStart(15, "span");
    \u0275\u0275text(16, "\u5206\u7EC4");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(17, "input", 45);
    \u0275\u0275listener("ngModelChange", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_input_ngModelChange_17_listener($event) {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.setProcessGroup(node_r12, $event));
    });
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(18, "button", 46);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template_button_click_18_listener() {
      \u0275\u0275restoreView(_r13);
      const node_r12 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.clearProcessGroup(node_r12));
    });
    \u0275\u0275text(19, "\xD7");
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const node_r12 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("ngModel", node_r12.label);
    \u0275\u0275attribute("data-process-id", node_r12.processId);
    \u0275\u0275advance(4);
    \u0275\u0275conditional(ctx_r1.draftLinkFromRefId() === node_r12.id ? 4 : ctx_r1.draftLinkFromRefId() ? 5 : 6);
    \u0275\u0275advance(7);
    \u0275\u0275conditional(ctx_r1.migratePickerNodeId() === node_r12.id ? 11 : -1);
    \u0275\u0275advance(6);
    \u0275\u0275property("ngModel", node_r12.group);
    \u0275\u0275attribute("data-process-id", node_r12.processId);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 33);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r12 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(node_r12.label);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 32);
    \u0275\u0275listener("mousedown", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Template_div_mousedown_0_listener($event) {
      const node_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startFlowNodeDrag(node_r12, $event));
    })("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Template_div_click_0_listener() {
      const node_r12 = \u0275\u0275restoreView(_r11).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(!ctx_r1.editing() && ctx_r1.openProcess(node_r12));
    });
    \u0275\u0275conditionalCreate(1, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_1_Template, 20, 6)(2, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Conditional_2_Template, 2, 1, "span", 33);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const node_r12 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275classProp("is-editable", ctx_r1.editing())("is-link-source", ctx_r1.draftLinkFromRefId() === node_r12.id)("is-link-target", ctx_r1.draftLinkFromRefId() && ctx_r1.draftLinkFromRefId() !== node_r12.id);
    \u0275\u0275property("ngStyle", ctx_r1.flowNodeStyle(node_r12));
    \u0275\u0275attribute("data-node-id", node_r12.id)("data-process-id", node_r12.processId);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.editing() ? 1 : 2);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 14)(1, "div", 18)(2, "div", 19);
    \u0275\u0275conditionalCreate(3, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_3_Template, 2, 0, "button", 20);
    \u0275\u0275repeaterCreate(4, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_5_Template, 3, 12, "div", 21, _forTrack03);
    \u0275\u0275namespaceSVG();
    \u0275\u0275elementStart(6, "svg", 22)(7, "defs")(8, "marker", 23);
    \u0275\u0275element(9, "path", 24);
    \u0275\u0275elementEnd()();
    \u0275\u0275repeaterCreate(10, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_11_Template, 1, 1, ":svg:path", 25, _forTrack18);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(12, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Conditional_12_Template, 2, 0);
    \u0275\u0275repeaterCreate(13, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_For_14_Template, 3, 10, "div", 26, _forTrack03);
    \u0275\u0275elementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275property("ngStyle", ctx_r1.flowBoardStyle());
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.flowBoardStyle());
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.flowBoardStyle());
    \u0275\u0275attribute("data-export-graph-id", ctx_r1.exportGraphId || null);
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.editing() ? 3 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.flowGroups());
    \u0275\u0275advance(2);
    \u0275\u0275attribute("width", ctx_r1.flowBoardStyle()["width"])("height", ctx_r1.flowBoardStyle()["height"]);
    \u0275\u0275advance(4);
    \u0275\u0275repeater(ctx_r1.flowLinks());
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.editing() ? 12 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.flowNodes());
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_14_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r21 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "button", 57);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_14_Conditional_3_Template_button_click_0_listener() {
      \u0275\u0275restoreView(_r21);
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.addProcess());
    });
    \u0275\u0275text(1, "+ \u65B0\u6D41\u7A0B");
    \u0275\u0275elementEnd();
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 15)(1, "span");
    \u0275\u0275text(2, "\u5F53\u524D\u9636\u6BB5\u8FD8\u6CA1\u6709\u6D41\u7A0B\u3002\u6253\u5F00\u7F16\u8F91\u540E\uFF0C\u53EF\u76F4\u63A5\u5728\u56FE\u4E0A\u65B0\u589E\u6D41\u7A0B\u3002");
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(3, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_14_Conditional_3_Template, 2, 0, "button", 56);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(3);
    \u0275\u0275advance(3);
    \u0275\u0275conditional(ctx_r1.editing() ? 3 : -1);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 5)(1, "button", 6);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Template_button_click_1_listener() {
      \u0275\u0275restoreView(_r1);
      const ctx_r1 = \u0275\u0275nextContext(2);
      return \u0275\u0275resetView(ctx_r1.openPanorama());
    });
    \u0275\u0275text(2, "\u4E1A\u52A1\u5168\u666F");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(3, "span", 7);
    \u0275\u0275text(4, "/");
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(5, "div", 8);
    \u0275\u0275conditionalCreate(6, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_6_Template, 1, 1, "input", 9)(7, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_7_Template, 2, 1, "span", 10);
    \u0275\u0275elementStart(8, "span", 11);
    \u0275\u0275text(9, " \xB7 \u9636\u6BB5\u8BE6\u60C5");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(10, "span", 12);
    \u0275\u0275text(11, "\u63D0\u793A\uFF1A\u6D41\u7A0B\u5361\u7247\u5E95\u90E8\u5177\u5907\u590D\u5236\u6D41\u7A0B\u3001\u8FC1\u79FB\u6D41\u7A0B\u81F3\u5176\u4ED6\u9636\u6BB5\u7B49\u529F\u80FD");
    \u0275\u0275elementEnd()();
    \u0275\u0275elementStart(12, "div", 13);
    \u0275\u0275conditionalCreate(13, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_13_Template, 15, 8, "div", 14)(14, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Conditional_14_Template, 4, 1, "div", 15);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(6);
    \u0275\u0275conditional(ctx_r1.renamingStageId() === ctx_r1.stageId(ctx) ? 6 : 7);
    \u0275\u0275advance(6);
    \u0275\u0275classProp("is-editing", ctx_r1.editing());
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.flowNodes().length ? 13 : 14);
  }
}
function ProcessStageWorkbenchComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275conditionalCreate(0, ProcessStageWorkbenchComponent_Conditional_4_Conditional_0_Template, 15, 4);
  }
  if (rf & 2) {
    let tmp_1_0;
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275conditional((tmp_1_0 = ctx_r1.currentStage()) ? 0 : -1, tmp_1_0);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_8_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const column_r22 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(column_r22.scope);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_8_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 63)(1, "strong");
    \u0275\u0275text(2);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(3, ProcessStageWorkbenchComponent_Conditional_5_For_8_Conditional_3_Template, 2, 1, "span");
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const column_r22 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(column_r22.name || ctx_r1.columnUid(column_r22));
    \u0275\u0275advance();
    \u0275\u0275conditional(column_r22.scope ? 3 : -1);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 68);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const lane_r23 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(lane_r23.badge);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span");
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const lane_r23 = \u0275\u0275nextContext().$implicit;
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(lane_r23.note);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r26 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "input", 77);
    \u0275\u0275listener("blur", function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_1_Template_input_blur_0_listener($event) {
      \u0275\u0275restoreView(_r26);
      const stage_r25 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.finishStageRename(stage_r25, $event.target.value));
    })("keydown.enter", function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_1_Template_input_keydown_enter_0_listener($event) {
      \u0275\u0275restoreView(_r26);
      const stage_r25 = \u0275\u0275nextContext().$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.finishStageRename(stage_r25, $event.target.value));
    });
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r25 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("ngModel", stage_r25.name || ctx_r1.stageId(stage_r25));
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 75);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r25 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(stage_r25.name || ctx_r1.stageId(stage_r25));
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "span", 76);
    \u0275\u0275text(1);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r25 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275advance();
    \u0275\u0275textInterpolate(ctx_r1.adapter.processCount(ctx_r1.stageId(stage_r25)));
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r24 = \u0275\u0275getCurrentView();
    \u0275\u0275elementStart(0, "div", 73);
    \u0275\u0275listener("click", function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Template_div_click_0_listener() {
      const stage_r25 = \u0275\u0275restoreView(_r24).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.openStage(stage_r25));
    })("dblclick", function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Template_div_dblclick_0_listener($event) {
      const stage_r25 = \u0275\u0275restoreView(_r24).$implicit;
      const ctx_r1 = \u0275\u0275nextContext(4);
      return \u0275\u0275resetView(ctx_r1.startStageRename(stage_r25, $event));
    });
    \u0275\u0275conditionalCreate(1, ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_1_Template, 1, 1, "input", 74)(2, ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_2_Template, 2, 1, "span", 75);
    \u0275\u0275conditionalCreate(3, ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Conditional_3_Template, 2, 1, "span", 76);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const stage_r25 = ctx.$implicit;
    const \u0275$index_188_r27 = ctx.$index;
    const ctx_r1 = \u0275\u0275nextContext(4);
    \u0275\u0275property("ngStyle", ctx_r1.stageStyle(stage_r25, \u0275$index_188_r27));
    \u0275\u0275attribute("data-node-id", ctx_r1.stageId(stage_r25))("data-flow-count", ctx_r1.adapter.processCount(ctx_r1.stageId(stage_r25)));
    \u0275\u0275advance();
    \u0275\u0275conditional(ctx_r1.renamingStageId() === ctx_r1.stageId(stage_r25) ? 1 : 2);
    \u0275\u0275advance(2);
    \u0275\u0275conditional(ctx_r1.adapter.processCount(ctx_r1.stageId(stage_r25)) > 0 ? 3 : -1);
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 70)(1, "div", 71);
    \u0275\u0275repeaterCreate(2, ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_For_3_Template, 4, 5, "div", 72, _forTrack33, true);
    \u0275\u0275elementEnd()();
  }
  if (rf & 2) {
    const column_r28 = ctx.$implicit;
    const lane_r23 = \u0275\u0275nextContext().$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275classProp("has-stages", ctx_r1.cellStages(ctx_r1.laneUid(lane_r23), ctx_r1.columnUid(column_r28)).length)("is-editing", ctx_r1.editing());
    \u0275\u0275attribute("data-cell-id", ctx_r1.laneUid(lane_r23) + "::" + ctx_r1.columnUid(column_r28))("data-lane-id", ctx_r1.laneUid(lane_r23))("data-column-id", ctx_r1.columnUid(column_r28));
    \u0275\u0275advance();
    \u0275\u0275property("ngStyle", ctx_r1.stageBoardStyle(ctx_r1.cellStages(ctx_r1.laneUid(lane_r23), ctx_r1.columnUid(column_r28))));
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.cellStages(ctx_r1.laneUid(lane_r23), ctx_r1.columnUid(column_r28)));
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_For_11_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 66)(1, "div", 67);
    \u0275\u0275conditionalCreate(2, ProcessStageWorkbenchComponent_Conditional_5_For_11_Conditional_2_Template, 2, 1, "span", 68);
    \u0275\u0275elementStart(3, "strong");
    \u0275\u0275text(4);
    \u0275\u0275elementEnd();
    \u0275\u0275conditionalCreate(5, ProcessStageWorkbenchComponent_Conditional_5_For_11_Conditional_5_Template, 2, 1, "span");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(6, ProcessStageWorkbenchComponent_Conditional_5_For_11_For_7_Template, 4, 8, "div", 69, _forTrack23, true);
    \u0275\u0275elementEnd();
  }
  if (rf & 2) {
    const lane_r23 = ctx.$implicit;
    const ctx_r1 = \u0275\u0275nextContext(2);
    \u0275\u0275styleProp("grid-template-columns", ctx_r1.gridTemplateColumns());
    \u0275\u0275advance(2);
    \u0275\u0275conditional(lane_r23.badge ? 2 : -1);
    \u0275\u0275advance(2);
    \u0275\u0275textInterpolate(lane_r23.name || ctx_r1.laneUid(lane_r23));
    \u0275\u0275advance();
    \u0275\u0275conditional(lane_r23.note ? 5 : -1);
    \u0275\u0275advance();
    \u0275\u0275repeater(ctx_r1.columns());
  }
}
function ProcessStageWorkbenchComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    \u0275\u0275elementStart(0, "div", 4)(1, "div", 58)(2, "div", 59)(3, "div", 60)(4, "div", 61)(5, "div", 62);
    \u0275\u0275text(6, "\u4E1A\u52A1\u57DF / \u4EF7\u503C\u6D41");
    \u0275\u0275elementEnd();
    \u0275\u0275repeaterCreate(7, ProcessStageWorkbenchComponent_Conditional_5_For_8_Template, 4, 2, "div", 63, _forTrack23, true);
    \u0275\u0275elementEnd();
    \u0275\u0275elementStart(9, "div", 64);
    \u0275\u0275repeaterCreate(10, ProcessStageWorkbenchComponent_Conditional_5_For_11_Template, 8, 5, "div", 65, _forTrack43, true);
    \u0275\u0275elementEnd()()()()();
  }
  if (rf & 2) {
    const ctx_r1 = \u0275\u0275nextContext();
    \u0275\u0275advance(3);
    \u0275\u0275classProp("is-editing", ctx_r1.editing());
    \u0275\u0275attribute("data-export-graph-id", ctx_r1.exportGraphId || null)("data-editing", ctx_r1.editing() ? "true" : "false");
    \u0275\u0275advance();
    \u0275\u0275styleProp("grid-template-columns", ctx_r1.gridTemplateColumns());
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.columns());
    \u0275\u0275advance(3);
    \u0275\u0275repeater(ctx_r1.lanes());
  }
}
var ProcessStageWorkbenchComponent = class _ProcessStageWorkbenchComponent {
  // 远端同步后通过 blm-workbench-refresh 事件刷新视图
  onRefresh = () => {
    this.version.update((v) => v + 1);
  };
  ngOnInit() {
    window.addEventListener("blm-workbench-refresh", this.onRefresh);
    document.addEventListener("click", this.onDocumentClick);
  }
  ngOnDestroy() {
    window.removeEventListener("blm-workbench-refresh", this.onRefresh);
    document.removeEventListener("click", this.onDocumentClick);
  }
  ngAfterViewChecked() {
    this.consumeFlowGroupFocus();
  }
  onDocumentClick = () => {
    this.closeMigratePicker();
  };
  // 模块意图：阶段视图的 Angular 渲染层。流程工作台壳仍由 legacy 控制，本组件负责阶段全景和阶段详情画布。
  version = signal(0, ...ngDevMode ? [{ debugName: "version" }] : (
    /* istanbul ignore next */
    []
  ));
  renamingStageId = signal("", ...ngDevMode ? [{ debugName: "renamingStageId" }] : (
    /* istanbul ignore next */
    []
  ));
  draftLinkFromRefId = signal("", ...ngDevMode ? [{ debugName: "draftLinkFromRefId" }] : (
    /* istanbul ignore next */
    []
  ));
  migratePickerNodeId = signal("", ...ngDevMode ? [{ debugName: "migratePickerNodeId" }] : (
    /* istanbul ignore next */
    []
  ));
  migratePickerColumnUid = signal("", ...ngDevMode ? [{ debugName: "migratePickerColumnUid" }] : (
    /* istanbul ignore next */
    []
  ));
  dragState = signal(null, ...ngDevMode ? [{ debugName: "dragState" }] : (
    /* istanbul ignore next */
    []
  ));
  adapter = createProcessStageLegacyAdapter();
  exportGraphId = "";
  previewMode = "auto";
  previewStageId = "";
  processEditorRequested = new EventEmitter();
  stageSlotWidth = 184;
  stageSlotHeight = 38;
  stageBoardPad = 8;
  flowNodeWidth = 62;
  flowNodeHeight = 128;
  flowGapX = 46;
  flowRowGap = 34;
  flowPadX = 24;
  flowPadY = 38;
  mode() {
    this.version();
    if (this.previewMode !== "auto")
      return this.previewMode;
    return this.adapter.ui().stageViewMode === "detail" ? "detail" : "panorama";
  }
  editing() {
    this.version();
    if (this.previewMode !== "auto")
      return false;
    return this.adapter.ui().stageEditorCollapsed === false;
  }
  stages() {
    this.version();
    return this.adapter.stages().filter((stage) => !stage.virtual);
  }
  currentStage() {
    const currentId = String(this.previewStageId || this.adapter.ui().stageId || "");
    return this.stages().find((stage) => this.stageId(stage) === currentId || stage.id === currentId || stage.uid === currentId) || this.stages()[0] || null;
  }
  businessDomainLabel(stage) {
    const stageLaneId = String(stage.panoramaLaneUid || stage.panoramaLaneId || "").trim();
    const lane = this.lanes().find((item) => this.laneUid(item) === stageLaneId) || this.lanes().find((_, index) => index === 0);
    return lane?.name || "\u672A\u5F52\u5C5E\u4E1A\u52A1\u57DF";
  }
  stageId(stage) {
    return this.adapter.stageId(stage);
  }
  columns() {
    this.version();
    return this.adapter.columns();
  }
  lanes() {
    this.version();
    return this.adapter.lanes();
  }
  columnUid(column) {
    return getValueDomainColumnUid(column);
  }
  laneUid(lane) {
    return getValueDomainLaneUid(lane);
  }
  gridTemplateColumns() {
    const axisMin = this.editing() ? 220 : 154;
    const tracks = this.columns().map((column) => `${this.columnWidth(this.columnUid(column))}px`).join(" ");
    return `${axisMin}px ${tracks}`;
  }
  cellStages(laneId, columnId) {
    return this.stages().filter((stage, index) => (stage.panoramaLaneUid || stage.panoramaLaneId || getValueDomainLaneUid(this.lanes()[0]) || "") === laneId && (stage.panoramaColumnUid || stage.panoramaColumnId || getValueDomainColumnUid(this.columns()[index % Math.max(1, this.columns().length)]) || "") === columnId);
  }
  stageStyle(stage, index) {
    const slot = this.stageSlot(stage, index);
    return {
      left: `${this.stageBoardPad + slot.col * this.stageSlotWidth}px`,
      top: `${this.stageBoardPad + slot.row * this.stageSlotHeight}px`,
      width: `${this.stageNodeWidth(stage)}px`
    };
  }
  stageBoardStyle(stages) {
    return {
      minWidth: `${this.stageBoardWidth(stages)}px`,
      height: `${this.stageBoardHeight(stages)}px`
    };
  }
  openStage(stage) {
    if (this.editing())
      return;
    this.adapter.openDetail(this.stageId(stage));
  }
  openPanorama() {
    this.adapter.openPanorama();
  }
  startStageRename(stage, event) {
    if (!this.editing())
      return;
    event.preventDefault();
    event.stopPropagation();
    this.renamingStageId.set(this.stageId(stage));
  }
  finishStageRename(stage, value) {
    const text = value.trim();
    if (text)
      this.adapter.setStageName(this.stageId(stage), text);
    this.renamingStageId.set("");
    this.refresh();
  }
  flowNodes() {
    const stage = this.currentStage();
    if (!stage)
      return [];
    const drag = this.dragState();
    const refs = this.adapter.stageProcesses(this.stageId(stage));
    const rowKeys = /* @__PURE__ */ new Map();
    const rowCols = /* @__PURE__ */ new Map();
    const groupCounts = /* @__PURE__ */ new Map();
    refs.forEach(({ ref, process: process2 }) => {
      const id = this.adapter.refId(ref);
      const key = String(process2?.flowGroup || "").trim() || id;
      rowKeys.set(key, rowKeys.get(key) ?? rowKeys.size);
      rowCols.set(id, groupCounts.get(key) || 0);
      groupCounts.set(key, (groupCounts.get(key) || 0) + 1);
    });
    return refs.map(({ ref, process: process2 }) => {
      const id = this.adapter.refId(ref);
      const key = String(process2?.flowGroup || "").trim() || id;
      const row = rowKeys.get(key) || 0;
      const col = rowCols.get(id) || 0;
      return {
        ref,
        process: process2,
        id,
        processId: this.adapter.processId(process2),
        label: process2?.name || "Invalid process ref",
        group: process2?.flowGroup || "",
        x: this.flowPadX + col * (this.flowNodeWidth + this.flowGapX) + Math.round(Number(ref.pos?.x || 0)) + (drag?.nodeId === id ? drag.dx : 0),
        y: this.flowPadY + row * (this.flowNodeHeight + this.flowRowGap) + Math.round(Number(ref.pos?.y || 0)) + (drag?.nodeId === id ? drag.dy : 0)
      };
    });
  }
  availableProcesses() {
    const stage = this.currentStage();
    if (!stage)
      return [];
    const usedIds = new Set((this.adapter.document().stageFlowRefs || []).map((ref) => String(ref.processUid || ref.processId || "").trim()).filter(Boolean));
    return this.adapter.processes().filter((process2) => !usedIds.has(this.adapter.processId(process2)));
  }
  flowLinks() {
    const stage = this.currentStage();
    if (!stage)
      return [];
    const nodeById = new Map(this.flowNodes().map((node) => [node.id, node]));
    return this.adapter.stageLinks(this.stageId(stage)).map((link, index) => {
      const from2 = nodeById.get(link.fromRefUid || link.fromRefId || "");
      const to = nodeById.get(link.toRefUid || link.toRefId || "");
      if (!from2 || !to)
        return null;
      return { link, from: from2, to, path: this.routeFlowLink(from2, to, index) };
    }).filter((link) => Boolean(link));
  }
  flowGroups() {
    const groups = /* @__PURE__ */ new Map();
    for (const node of this.flowNodes()) {
      const label = node.group.trim();
      if (!label)
        continue;
      if (!groups.has(label))
        groups.set(label, []);
      groups.get(label)?.push(node);
    }
    return [...groups.entries()].map(([label, nodes], index) => {
      const minX = Math.min(...nodes.map((node) => node.x));
      const maxX = Math.max(...nodes.map((node) => node.x + this.flowNodeWidth));
      const minY = Math.min(...nodes.map((node) => node.y));
      const maxY = Math.max(...nodes.map((node) => node.y + this.flowNodeHeight));
      return {
        id: `group-${index}-${label}`,
        label,
        x: Math.max(6, minX - 14),
        y: Math.max(6, minY - 28),
        w: Math.max(92, maxX - minX + 28),
        h: Math.max(this.flowNodeHeight + 58, maxY - minY + 58)
      };
    });
  }
  flowBoardStyle() {
    const nodes = this.flowNodes();
    const width = Math.max(720, ...nodes.map((node) => node.x + this.flowNodeWidth + this.flowPadX));
    const height = Math.max(260, ...nodes.map((node) => node.y + this.flowNodeHeight + this.flowPadY));
    return {
      width: `${width}px`,
      height: `${height}px`
    };
  }
  flowNodeStyle(node) {
    const isDragging = this.dragState()?.nodeId === node.id;
    return {
      left: `${node.x}px`,
      top: `${node.y}px`,
      width: `${this.flowNodeWidth}px`,
      height: `${this.flowNodeHeight}px`,
      zIndex: isDragging ? "5" : ""
    };
  }
  isGroupDragTarget(group) {
    return this.dragState()?.targetGroup === group.label;
  }
  setProcessName(node, value) {
    this.adapter.setProcessName(node.processId, value);
    this.refresh();
  }
  setProcessGroup(node, value) {
    this.adapter.setProcessGroup(node.processId, value);
    this.refresh();
  }
  clearProcessGroup(node) {
    this.adapter.setProcessGroup(node.processId, "");
    this.refresh();
  }
  startFlowNodeDrag(node, event) {
    if (!this.editing() || event.button !== 0 || this.isInteractiveDragChild(event.target))
      return;
    event.preventDefault();
    event.stopPropagation();
    const startOffset = node.ref.pos || {};
    const nextState = {
      nodeId: node.id,
      startX: event.clientX,
      startY: event.clientY,
      startOffsetX: Math.round(Number(startOffset.x || 0)),
      startOffsetY: Math.round(Number(startOffset.y || 0)),
      dx: 0,
      dy: 0,
      targetGroup: ""
    };
    this.dragState.set(nextState);
    document.addEventListener("mousemove", this.onDocumentFlowDrag);
    document.addEventListener("mouseup", this.onDocumentFlowDragEnd);
  }
  addProcess() {
    const stage = this.currentStage();
    if (!stage)
      return;
    this.adapter.addProcess(this.stageId(stage));
    this.refresh();
  }
  addExistingProcess(processId) {
    const stage = this.currentStage();
    if (!stage || !processId)
      return;
    this.adapter.addExistingProcess(this.stageId(stage), processId);
    this.refresh();
  }
  toggleMigratePicker(node, event) {
    event.stopPropagation();
    if (this.migratePickerNodeId() === node.id) {
      this.closeMigratePicker();
      return;
    }
    this.migratePickerNodeId.set(node.id);
    this.migratePickerColumnUid.set("");
  }
  closeMigratePicker() {
    this.migratePickerNodeId.set("");
    this.migratePickerColumnUid.set("");
  }
  selectMigrateColumn(columnUid) {
    this.migratePickerColumnUid.set(this.migratePickerColumnUid() === columnUid ? "" : columnUid);
  }
  migratePickerStages() {
    const columnUid = this.migratePickerColumnUid();
    if (!columnUid)
      return [];
    return this.stages().filter((stage) => (stage.panoramaColumnUid || stage.panoramaColumnId || "") === columnUid);
  }
  migrateProcess(node, targetStageId) {
    const stage = this.currentStage();
    if (!stage || !node.processId)
      return;
    const sourceStageId = this.stageId(stage);
    if (sourceStageId === targetStageId)
      return;
    this.adapter.removeProcessFromStage(sourceStageId, node.processId);
    this.adapter.addExistingProcess(targetStageId, node.processId);
    this.closeMigratePicker();
    this.refresh();
  }
  async deleteProcess(node) {
    if (!node.processId)
      return;
    const doc = this.adapter.document();
    const pid = node.processId;
    const refs = (doc.stageFlowRefs || []).filter((ref) => {
      const puid = ref.processUid || ref.processId || "";
      return puid && puid === pid;
    });
    const stage = this.currentStage();
    const currentStageId = stage ? this.stageId(stage) : "";
    if (refs.length > 1) {
      const confirmed = await confirmRuntimeAction(`\u786E\u8BA4\u4ECE\u672C\u9636\u6BB5\u79FB\u9664\u6D41\u7A0B\u201D${node.label || pid}\u201D\u5417\uFF1F\uFF08\u8BE5\u6D41\u7A0B\u8FD8\u5B58\u5728\u4E8E\u5176\u4ED6 ${refs.length - 1} \u4E2A\u9636\u6BB5\uFF09`, {
        title: "\u4ECE\u672C\u9636\u6BB5\u79FB\u9664",
        confirmLabel: "\u79FB\u9664"
      });
      if (!confirmed)
        return;
      this.adapter.removeProcessFromStage(currentStageId, pid);
    } else {
      const confirmed = await confirmRuntimeAction(`\u786E\u8BA4\u5220\u9664\u6D41\u7A0B\u201D${node.label || pid}\u201D\u5417\uFF1F\u76F8\u5173\u9636\u6BB5\u5F15\u7528\u548C\u9636\u6BB5\u8FDE\u7EBF\u4E5F\u4F1A\u4E00\u5E76\u79FB\u9664\u3002`, {
        title: "\u5220\u9664\u6D41\u7A0B",
        confirmLabel: "\u5220\u9664"
      });
      if (!confirmed)
        return;
      this.adapter.deleteProcess(pid);
    }
    this.refresh();
  }
  duplicateProcess(node) {
    if (!node.processId)
      return;
    this.adapter.duplicateProcess(node.processId);
    this.refresh();
  }
  openProcess(node) {
    if (node.processId)
      this.processEditorRequested.emit(node.processId);
  }
  beginLink(node) {
    this.draftLinkFromRefId.set(node.id);
  }
  cancelLink() {
    this.draftLinkFromRefId.set("");
  }
  completeLink(node) {
    const stage = this.currentStage();
    const from2 = this.draftLinkFromRefId();
    if (!stage || !from2)
      return;
    this.adapter.addLink(this.stageId(stage), from2, node.id);
    this.draftLinkFromRefId.set("");
    this.refresh();
  }
  removeLink(link) {
    const stage = this.currentStage();
    if (!stage)
      return;
    this.adapter.removeLink(this.stageId(stage), link.link.id || link.link.uid || "");
    this.refresh();
  }
  onDocumentFlowDrag = (event) => {
    const drag = this.dragState();
    if (!drag)
      return;
    const dx = Math.round(event.clientX - drag.startX);
    const dy = Math.round(event.clientY - drag.startY);
    this.dragState.set(__spreadProps(__spreadValues({}, drag), {
      dx,
      dy,
      targetGroup: this.findDragTargetGroup(drag.nodeId, dx, dy)
    }));
  };
  onDocumentFlowDragEnd = (event) => {
    const drag = this.dragState();
    document.removeEventListener("mousemove", this.onDocumentFlowDrag);
    document.removeEventListener("mouseup", this.onDocumentFlowDragEnd);
    if (!drag)
      return;
    const dx = Math.round(event.clientX - drag.startX);
    const dy = Math.round(event.clientY - drag.startY);
    this.dragState.set(__spreadProps(__spreadValues({}, drag), { dx, dy }));
    const targetGroup = this.findDragTargetGroup(drag.nodeId, dx, dy);
    const node = this.flowNodes().find((item) => item.id === drag.nodeId);
    this.dragState.set(null);
    if (!node || Math.abs(dx) < 5 && Math.abs(dy) < 5) {
      this.refresh();
      return;
    }
    if (targetGroup) {
      this.adapter.setProcessGroup(node.processId, targetGroup);
      this.refresh();
      return;
    }
    this.adapter.setRefOffset(node.id, {
      x: drag.startOffsetX + dx,
      y: drag.startOffsetY + dy
    });
    this.refresh();
  };
  refresh() {
    this.version.update((value) => value + 1);
  }
  consumeFlowGroupFocus() {
    const ui = this.adapter.ui();
    const focus = ui["stageFlowGroupFocus"];
    if (!focus?.groupName || this.mode() !== "detail")
      return;
    const stage = this.currentStage();
    if (!stage || String(focus.stageId || "").trim() !== this.stageId(stage))
      return;
    const target = Array.from(document.querySelectorAll(".stage-flow-group-box")).find((item) => String(item.dataset["flowGroup"] || "").trim() === String(focus.groupName || "").trim());
    if (!target)
      return;
    const scroller = target.closest(".stage-flow-zoom-shell");
    if (scroller) {
      scroller.scrollTo({
        left: Math.max(0, target.offsetLeft - 24),
        top: Math.max(0, target.offsetTop - 24),
        behavior: "smooth"
      });
    } else {
      target.scrollIntoView({ behavior: "smooth", block: "center", inline: "center" });
    }
    ui["stageFlowGroupFocus"] = null;
  }
  isInteractiveDragChild(target) {
    return Boolean(target?.closest?.("textarea,input,select,button,.stage-flow-node-group-editor,.stage-flow-node-actions"));
  }
  findDragTargetGroup(nodeId, dx, dy) {
    const dragNode = this.flowNodes().find((node) => node.id === nodeId);
    if (!dragNode)
      return "";
    const centerX = dragNode.x + dx + this.flowNodeWidth / 2;
    const centerY = dragNode.y + dy + this.flowNodeHeight / 2;
    return this.flowGroups().find((group) => {
      if (!group.label || group.label === dragNode.group)
        return false;
      return centerX >= group.x && centerX <= group.x + group.w && centerY >= group.y && centerY <= group.y + group.h;
    })?.label || "";
  }
  stageSlot(stage, index) {
    if (stage.panoramaSlot) {
      return { row: Math.max(0, Number(stage.panoramaSlot.row || 0)), col: Math.max(0, Number(stage.panoramaSlot.col || 0)) };
    }
    return { row: Math.floor(index / 2), col: index % 2 };
  }
  stageNodeWidth(stage) {
    const countWidth = this.adapter.processCount(this.stageId(stage)) > 0 ? 24 : 6;
    const textWidth = Array.from(String(stage.name || this.stageId(stage) || "")).reduce((width, char) => width + (/[\u0000-\u00ff]/.test(char) ? 6 : 12), 0);
    return Math.max(108, textWidth + countWidth + 28);
  }
  stageBoardWidth(stages) {
    if (!stages.length)
      return this.editing() ? 132 : 120;
    return Math.max(this.editing() ? 250 : 132, ...stages.map((stage, index) => {
      const slot = this.stageSlot(stage, index);
      return this.stageBoardPad + slot.col * this.stageSlotWidth + this.stageNodeWidth(stage) + this.stageBoardPad;
    }));
  }
  stageBoardHeight(stages) {
    if (!stages.length)
      return this.editing() ? 48 : 42;
    return Math.max(this.editing() ? 82 : 54, ...stages.map((stage, index) => {
      const slot = this.stageSlot(stage, index);
      return this.stageBoardPad + slot.row * this.stageSlotHeight + 28 + (this.editing() ? 30 : 8);
    }));
  }
  columnWidth(columnId) {
    const min = this.editing() ? 220 : 210;
    return Math.max(min, ...this.lanes().map((lane) => this.stageBoardWidth(this.cellStages(this.laneUid(lane), columnId)) + 16));
  }
  routeFlowLink(from2, to, index) {
    const sx = from2.x + this.flowNodeWidth;
    const sy = from2.y + this.flowNodeHeight / 2;
    const tx = to.x;
    const ty = to.y + this.flowNodeHeight / 2;
    const offset = index * 4;
    const midX = sx + Math.max(24, (tx - sx) / 2) + offset;
    return `M ${sx} ${sy} H ${midX} V ${ty} H ${tx}`;
  }
  static \u0275fac = function ProcessStageWorkbenchComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ProcessStageWorkbenchComponent)();
  };
  static \u0275cmp = /* @__PURE__ */ \u0275\u0275defineComponent({ type: _ProcessStageWorkbenchComponent, selectors: [["app-process-stage-workbench"]], inputs: { exportGraphId: "exportGraphId", previewMode: "previewMode", previewStageId: "previewStageId" }, outputs: { processEditorRequested: "processEditorRequested" }, decls: 6, vars: 1, consts: [["data-testid", "process-stage-view", 1, "stage-workbench"], [1, "stage-main-shell", "workbench-scroll-body"], [1, "stage-main"], [1, "stage-card"], ["data-testid", "stage-panorama-graph", 1, "stage-graph", "value-stream-graph"], ["data-testid", "stage-compact-head", 1, "stage-compact-head"], ["type", "button", 1, "btn", "btn-ghost-sm", 3, "click"], [1, "stage-breadcrumb-sep"], ["data-testid", "stage-detail-title", 1, "stage-card-title"], ["data-testid", "stage-name-inline-input", "autofocus", "", 1, "stage-name-inline-input", "stage-detail-name-text", 3, "ngModel"], ["title", "\u53CC\u51FB\u4FEE\u6539\u9636\u6BB5\u540D\u79F0", 1, "stage-detail-name-text"], [1, "stage-detail-title-suffix"], [1, "stage-detail-hint"], ["data-testid", "stage-detail-graph", 1, "stage-graph", "stage-flow-guide"], [1, "stage-graph-zoom-shell", "stage-flow-zoom-shell", 3, "ngStyle"], ["data-testid", "stage-detail-graph-empty", 1, "diag-empty", "stage-flow-empty"], ["data-testid", "stage-name-inline-input", "autofocus", "", 1, "stage-name-inline-input", "stage-detail-name-text", 3, "blur", "keydown.enter", "keydown.escape", "ngModel"], ["title", "\u53CC\u51FB\u4FEE\u6539\u9636\u6BB5\u540D\u79F0", 1, "stage-detail-name-text", 3, "dblclick"], [1, "stage-graph-zoom-target", 3, "ngStyle"], ["data-export-graph-ready", "true", 1, "stage-graph-board", "stage-flow-board", 3, "ngStyle"], ["type", "button", "data-testid", "stage-flow-node-add-button", 1, "stage-flow-board-add"], ["data-testid", "stage-flow-group", 1, "stage-flow-group-box", 3, "is-drag-target", "left", "top", "width", "height"], ["aria-hidden", "true", 1, "stage-graph-svg"], ["id", "stage-flow-arrow-angular", "markerWidth", "10", "markerHeight", "10", "refX", "8", "refY", "4", "orient", "auto"], ["d", "M0,0 L0,8 L8,4 z", "fill", "#52677f"], ["fill", "none", "stroke", "#52677f", "stroke-width", "2", "stroke-linecap", "round", "stroke-linejoin", "round", "marker-end", "url(#stage-flow-arrow-angular)", 1, "stage-graph-link", "stage-flow-link"], ["data-testid", "stage-graph-node", 1, "stage-graph-node", "process-kind", "stage-flow-node", 3, "is-editable", "is-link-source", "is-link-target", "ngStyle"], ["type", "button", "data-testid", "stage-flow-node-add-button", 1, "stage-flow-board-add", 3, "click"], ["data-testid", "stage-flow-group", 1, "stage-flow-group-box"], [1, "stage-flow-group-title"], ["type", "button", "data-testid", "stage-process-link-remove-button", 1, "stage-flow-link-remove", 3, "left", "top"], ["type", "button", "data-testid", "stage-process-link-remove-button", 1, "stage-flow-link-remove", 3, "click"], ["data-testid", "stage-graph-node", 1, "stage-graph-node", "process-kind", "stage-flow-node", 3, "mousedown", "click", "ngStyle"], [1, "stage-flow-node-title"], ["data-testid", "stage-flow-name-input", 1, "stage-flow-name-input", 3, "ngModelChange", "mousedown", "ngModel"], [1, "stage-flow-node-actions", 3, "mousedown", "click"], ["type", "button", "data-testid", "stage-member-view-button", 1, "stage-quick-btn", 3, "click"], ["type", "button", "data-testid", "stage-flow-link-cancel-button", 1, "stage-quick-btn", "warning"], ["type", "button", "data-testid", "stage-flow-link-target-button", 1, "stage-quick-btn", "success"], ["type", "button", "data-testid", "stage-flow-link-source-button", 1, "stage-quick-btn"], ["type", "button", "data-testid", "stage-member-copy-button", "title", "\u590D\u5236\u6D41\u7A0B\uFF08\u5305\u542B\u6240\u6709\u8282\u70B9\u3001\u8FB9\u7EBF\u3001\u8868\u5355\uFF09", 1, "stage-quick-btn", 3, "click"], ["type", "button", "data-testid", "stage-member-migrate-button", "title", "\u8FC1\u79FB\u6D41\u7A0B\u81F3\u5176\u4ED6\u9636\u6BB5", 1, "stage-quick-btn", 3, "click"], ["data-testid", "stage-migrate-picker", 1, "stage-migrate-picker"], ["type", "button", "data-testid", "stage-member-delete-button", 1, "stage-quick-btn", "danger", 3, "click"], ["data-testid", "stage-flow-node-group-editor", 1, "stage-flow-node-group-editor", 3, "mousedown", "click"], ["data-testid", "stage-flow-node-group-input", 3, "ngModelChange", "ngModel"], ["type", "button", "title", "\u6E05\u7A7A\u5206\u7EC4", "aria-label", "\u6E05\u7A7A\u5206\u7EC4", 3, "click"], ["type", "button", "data-testid", "stage-flow-link-cancel-button", 1, "stage-quick-btn", "warning", 3, "click"], ["type", "button", "data-testid", "stage-flow-link-target-button", 1, "stage-quick-btn", "success", 3, "click"], ["type", "button", "data-testid", "stage-flow-link-source-button", 1, "stage-quick-btn", 3, "click"], ["data-testid", "stage-migrate-picker", 1, "stage-migrate-picker", 3, "click"], [1, "stage-migrate-columns"], ["type", "button", 3, "active"], [1, "stage-migrate-stages"], ["type", "button", 3, "click"], ["type", "button", 3, "current"], ["type", "button", "data-testid", "stage-flow-node-add-button", 1, "btn", "btn-outline", "btn-sm"], ["type", "button", "data-testid", "stage-flow-node-add-button", 1, "btn", "btn-outline", "btn-sm", 3, "click"], ["data-testid", "value-stream-scroll-wrap", 1, "value-stream-scroll-wrap"], ["data-testid", "value-stream-scroll", 1, "value-stream-scroll", "workbench-scroll-body"], ["data-testid", "value-stream-matrix", "data-export-graph-ready", "true", 1, "value-stream-matrix"], [1, "value-stream-header-row"], [1, "value-stream-axis"], ["data-testid", "value-stream-header", 1, "value-stream-header"], [1, "value-stream-body"], ["data-testid", "value-stream-row", 1, "value-stream-row", 3, "grid-template-columns"], ["data-testid", "value-stream-row", 1, "value-stream-row"], [1, "value-stream-lane"], [1, "value-stream-lane-badge"], [1, "value-stream-cell", 3, "has-stages", "is-editing"], [1, "value-stream-cell"], ["data-testid", "stage-workbench-stage-board", 1, "value-stream-stage-board", 3, "ngStyle"], ["role", "button", "tabindex", "0", "data-testid", "stage-graph-node", 1, "stage-graph-node", "stage-kind", "stage-matrix-stage", 3, "ngStyle"], ["role", "button", "tabindex", "0", "data-testid", "stage-graph-node", 1, "stage-graph-node", "stage-kind", "stage-matrix-stage", 3, "click", "dblclick", "ngStyle"], ["data-testid", "stage-name-inline-input", 1, "stage-name-inline-input", "stage-graph-node-title", 3, "ngModel"], [1, "stage-graph-node-title"], [1, "stage-node-count"], ["data-testid", "stage-name-inline-input", 1, "stage-name-inline-input", "stage-graph-node-title", 3, "blur", "keydown.enter", "ngModel"]], template: function ProcessStageWorkbenchComponent_Template(rf, ctx) {
    if (rf & 1) {
      \u0275\u0275elementStart(0, "div", 0)(1, "div", 1)(2, "div", 2)(3, "div", 3);
      \u0275\u0275conditionalCreate(4, ProcessStageWorkbenchComponent_Conditional_4_Template, 1, 1)(5, ProcessStageWorkbenchComponent_Conditional_5_Template, 12, 6, "div", 4);
      \u0275\u0275elementEnd()()()();
    }
    if (rf & 2) {
      \u0275\u0275advance(4);
      \u0275\u0275conditional(ctx.mode() === "detail" ? 4 : 5);
    }
  }, dependencies: [CommonModule, NgStyle, FormsModule, DefaultValueAccessor, NgControlStatus, NgModel], styles: ['@charset "UTF-8";\n\n\n[_nghost-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-width: 0;\n  min-height: 0;\n}\n.stage-workbench[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-main[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-card[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-graph[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.value-stream-scroll-wrap[_ngcontent-%COMP%] {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.value-stream-scroll[_ngcontent-%COMP%] {\n  flex: 1;\n  overflow: auto;\n  max-height: none;\n  min-height: 0;\n}\n.value-stream-matrix[_ngcontent-%COMP%] {\n  min-width: max-content;\n}\n.stage-flow-zoom-shell[_ngcontent-%COMP%] {\n  overflow: auto;\n  flex: 1;\n  min-height: 0;\n}\n.stage-workbench[_ngcontent-%COMP%]   .stage-main-shell.workbench-scroll-body[_ngcontent-%COMP%] {\n  overflow: hidden;\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-detail-hint[_ngcontent-%COMP%] {\n  margin-left: auto;\n  color: #94a3b8;\n  font-size: 11px;\n  white-space: nowrap;\n}\n.stage-migrate-picker[_ngcontent-%COMP%] {\n  position: absolute;\n  z-index: 1260;\n  left: 50%;\n  bottom: calc(100% + 4px);\n  transform: translateX(-50%);\n  display: grid;\n  grid-template-columns: 140px minmax(180px, 1fr);\n  min-width: 320px;\n  border: 1px solid #bfdbfe;\n  border-radius: 10px;\n  background: #fff;\n  box-shadow: 0 18px 44px rgba(15, 23, 42, 0.18);\n}\n.stage-migrate-columns[_ngcontent-%COMP%] {\n  display: grid;\n  align-content: start;\n  gap: 4px;\n  max-height: 260px;\n  padding: 8px;\n  overflow-y: auto;\n  border-right: 1px solid #e2e8f0;\n  background: #f8fafc;\n}\n.stage-migrate-columns[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  min-height: 30px;\n  padding: 0 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 7px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 800;\n  text-align: left;\n}\n.stage-migrate-columns[_ngcontent-%COMP%]   button.active[_ngcontent-%COMP%], \n.stage-migrate-columns[_ngcontent-%COMP%]   button[_ngcontent-%COMP%]:hover {\n  border-color: #60a5fa;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.stage-migrate-stages[_ngcontent-%COMP%] {\n  display: grid;\n  align-content: start;\n  gap: 4px;\n  max-height: 260px;\n  padding: 8px;\n  overflow-y: auto;\n}\n.stage-migrate-stages[_ngcontent-%COMP%]   button[_ngcontent-%COMP%] {\n  min-height: 30px;\n  padding: 0 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 7px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n  text-align: left;\n}\n.stage-migrate-stages[_ngcontent-%COMP%]   button[_ngcontent-%COMP%]:hover {\n  border-color: #60a5fa;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.stage-migrate-stages[_ngcontent-%COMP%]   button.current[_ngcontent-%COMP%] {\n  color: #94a3b8;\n  font-style: italic;\n  pointer-events: none;\n}\n.stage-graph-node[_ngcontent-%COMP%]:has(.stage-migrate-picker), \n.stage-flow-node[_ngcontent-%COMP%]:has(.stage-migrate-picker) {\n  overflow: visible;\n  z-index: 1260;\n}\n.stage-flow-node-group-editor[_ngcontent-%COMP%] {\n  left: -20px;\n  top: 132px;\n  width: 104px;\n}\n.stage-flow-node-actions[_ngcontent-%COMP%] {\n  z-index: 2;\n}\n.stage-flow-link-remove[_ngcontent-%COMP%] {\n  z-index: 4;\n}\n/*# sourceMappingURL=process-stage-workbench.component.css.map */'] });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ProcessStageWorkbenchComponent, [{
    type: Component,
    args: [{ selector: "app-process-stage-workbench", standalone: true, imports: [CommonModule, FormsModule], template: `<div class="stage-workbench" data-testid="process-stage-view">
  <div class="stage-main-shell workbench-scroll-body">
    <div class="stage-main">
      <div class="stage-card">
        @if (mode() === 'detail') {
          @if (currentStage(); as stage) {
            <div class="stage-compact-head" data-testid="stage-compact-head">
              <button class="btn btn-ghost-sm" type="button" (click)="openPanorama()">\u4E1A\u52A1\u5168\u666F</button>
              <span class="stage-breadcrumb-sep">/</span>
              <div class="stage-card-title" data-testid="stage-detail-title">
                @if (renamingStageId() === stageId(stage)) {
                  <input
                    class="stage-name-inline-input stage-detail-name-text"
                    data-testid="stage-name-inline-input"
                    [ngModel]="stage.name || stageId(stage)"
                    (blur)="finishStageRename(stage, $any($event.target).value)"
                    (keydown.enter)="finishStageRename(stage, $any($event.target).value)"
                    (keydown.escape)="renamingStageId.set('')"
                    autofocus
                  />
                } @else {
                  <span class="stage-detail-name-text" title="\u53CC\u51FB\u4FEE\u6539\u9636\u6BB5\u540D\u79F0" (dblclick)="startStageRename(stage, $event)">
                    {{ stage.name || stageId(stage) }}
                  </span>
                }
                <span class="stage-detail-title-suffix"> \xB7 \u9636\u6BB5\u8BE6\u60C5</span>
              </div>
              <span class="stage-detail-hint">\u63D0\u793A\uFF1A\u6D41\u7A0B\u5361\u7247\u5E95\u90E8\u5177\u5907\u590D\u5236\u6D41\u7A0B\u3001\u8FC1\u79FB\u6D41\u7A0B\u81F3\u5176\u4ED6\u9636\u6BB5\u7B49\u529F\u80FD</span>
            </div>

            <div class="stage-graph stage-flow-guide" [class.is-editing]="editing()" data-testid="stage-detail-graph">
              @if (flowNodes().length) {
                <div class="stage-graph-zoom-shell stage-flow-zoom-shell" [ngStyle]="flowBoardStyle()">
                  <div class="stage-graph-zoom-target" [ngStyle]="flowBoardStyle()">
                    <div class="stage-graph-board stage-flow-board" [attr.data-export-graph-id]="exportGraphId || null" data-export-graph-ready="true" [ngStyle]="flowBoardStyle()">
                      @if (editing()) {
                        <button class="stage-flow-board-add" type="button" data-testid="stage-flow-node-add-button" (click)="addProcess()">+ \u6D41\u7A0B</button>
                      }
                      @for (group of flowGroups(); track group.id) {
                        <div
                          class="stage-flow-group-box"
                          [class.is-drag-target]="isGroupDragTarget(group)"
                          data-testid="stage-flow-group"
                          [attr.data-flow-group]="group.label"
                          [style.left.px]="group.x"
                          [style.top.px]="group.y"
                          [style.width.px]="group.w"
                          [style.height.px]="group.h"
                        >
                          <div class="stage-flow-group-title">{{ group.label }}</div>
                        </div>
                      }
                      <svg class="stage-graph-svg" [attr.width]="flowBoardStyle()['width']" [attr.height]="flowBoardStyle()['height']" aria-hidden="true">
                        <defs>
                          <marker id="stage-flow-arrow-angular" markerWidth="10" markerHeight="10" refX="8" refY="4" orient="auto">
                            <path d="M0,0 L0,8 L8,4 z" fill="#52677f"></path>
                          </marker>
                        </defs>
                        @for (link of flowLinks(); track link.link.id || link.link.uid) {
                          <path class="stage-graph-link stage-flow-link" [attr.d]="link.path" fill="none" stroke="#52677f" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" marker-end="url(#stage-flow-arrow-angular)"></path>
                        }
                      </svg>
                      @if (editing()) {
                        @for (link of flowLinks(); track link.link.id || link.link.uid) {
                          <button class="stage-flow-link-remove" type="button" data-testid="stage-process-link-remove-button" [style.left.px]="(link.from.x + link.to.x) / 2" [style.top.px]="link.from.y + 52" (click)="removeLink(link)">\xD7</button>
                        }
                      }
                      @for (node of flowNodes(); track node.id) {
                        <div
                          class="stage-graph-node process-kind stage-flow-node"
                          [class.is-editable]="editing()"
                          [class.is-link-source]="draftLinkFromRefId() === node.id"
                          [class.is-link-target]="draftLinkFromRefId() && draftLinkFromRefId() !== node.id"
                          data-testid="stage-graph-node"
                          [attr.data-node-id]="node.id"
                          [attr.data-process-id]="node.processId"
                          [ngStyle]="flowNodeStyle(node)"
                          (mousedown)="startFlowNodeDrag(node, $event)"
                          (click)="!editing() && openProcess(node)"
                        >
                          @if (editing()) {
                            <textarea class="stage-flow-name-input" data-testid="stage-flow-name-input" [attr.data-process-id]="node.processId" [ngModel]="node.label" (ngModelChange)="setProcessName(node, $event)" (mousedown)="$event.stopPropagation()"></textarea>
                            <div class="stage-flow-node-actions" (mousedown)="$event.stopPropagation()" (click)="$event.stopPropagation()">
                              <button class="stage-quick-btn" type="button" data-testid="stage-member-view-button" (click)="openProcess(node)">\u2197</button>
                              @if (draftLinkFromRefId() === node.id) {
                                <button class="stage-quick-btn warning" type="button" data-testid="stage-flow-link-cancel-button" (click)="cancelLink()">\u21BA</button>
                              } @else if (draftLinkFromRefId()) {
                                <button class="stage-quick-btn success" type="button" data-testid="stage-flow-link-target-button" (click)="completeLink(node)">\u21A6</button>
                              } @else {
                                <button class="stage-quick-btn" type="button" data-testid="stage-flow-link-source-button" (click)="beginLink(node)">\u2192</button>
                              }
                              <button class="stage-quick-btn" type="button" data-testid="stage-member-copy-button" title="\u590D\u5236\u6D41\u7A0B\uFF08\u5305\u542B\u6240\u6709\u8282\u70B9\u3001\u8FB9\u7EBF\u3001\u8868\u5355\uFF09" (click)="duplicateProcess(node)">\u29C9</button>
                              <button class="stage-quick-btn" type="button" data-testid="stage-member-migrate-button" title="\u8FC1\u79FB\u6D41\u7A0B\u81F3\u5176\u4ED6\u9636\u6BB5" (click)="toggleMigratePicker(node, $event)">\u25A4</button>
                              @if (migratePickerNodeId() === node.id) {
                                <div class="stage-migrate-picker" data-testid="stage-migrate-picker" (click)="$event.stopPropagation()">
                                  <div class="stage-migrate-columns">
                                    @for (col of columns(); track columnUid(col)) {
                                      <button type="button" [class.active]="migratePickerColumnUid() === columnUid(col)" (click)="selectMigrateColumn(columnUid(col))">{{ col.name || columnUid(col) }}</button>
                                    }
                                  </div>
                                  @if (migratePickerColumnUid(); as colUid) {
                                    <div class="stage-migrate-stages">
                                      @for (st of migratePickerStages(); track stageId(st)) {
                                        <button type="button" (click)="migrateProcess(node, stageId(st))" [class.current]="(currentStage() ? stageId(currentStage()!) : '') === stageId(st)">{{ st.name || stageId(st) }}</button>
                                      }
                                    </div>
                                  }
                                </div>
                              }
                              <button class="stage-quick-btn danger" type="button" data-testid="stage-member-delete-button" (click)="deleteProcess(node)">\xD7</button>
                            </div>
                            <div class="stage-flow-node-group-editor" data-testid="stage-flow-node-group-editor" (mousedown)="$event.stopPropagation()" (click)="$event.stopPropagation()">
                              <span>\u5206\u7EC4</span>
                              <input data-testid="stage-flow-node-group-input" [attr.data-process-id]="node.processId" [ngModel]="node.group" (ngModelChange)="setProcessGroup(node, $event)" />
                              <button type="button" title="\u6E05\u7A7A\u5206\u7EC4" aria-label="\u6E05\u7A7A\u5206\u7EC4" (click)="clearProcessGroup(node)">\xD7</button>
                            </div>
                          } @else {
                            <span class="stage-flow-node-title">{{ node.label }}</span>
                          }
                        </div>
                      }
                    </div>
                  </div>
                </div>
              } @else {
                <div class="diag-empty stage-flow-empty" data-testid="stage-detail-graph-empty">
                  <span>\u5F53\u524D\u9636\u6BB5\u8FD8\u6CA1\u6709\u6D41\u7A0B\u3002\u6253\u5F00\u7F16\u8F91\u540E\uFF0C\u53EF\u76F4\u63A5\u5728\u56FE\u4E0A\u65B0\u589E\u6D41\u7A0B\u3002</span>
                  @if (editing()) {
                    <button class="btn btn-outline btn-sm" type="button" data-testid="stage-flow-node-add-button" (click)="addProcess()">+ \u65B0\u6D41\u7A0B</button>
                  }
                </div>
              }
            </div>
          }
        } @else {
          <div class="stage-graph value-stream-graph" data-testid="stage-panorama-graph">
            <div class="value-stream-scroll-wrap" data-testid="value-stream-scroll-wrap">
              <div class="value-stream-scroll workbench-scroll-body" data-testid="value-stream-scroll">
                <div class="value-stream-matrix" [class.is-editing]="editing()" data-testid="value-stream-matrix" [attr.data-export-graph-id]="exportGraphId || null" data-export-graph-ready="true" [attr.data-editing]="editing() ? 'true' : 'false'">
                  <div class="value-stream-header-row" [style.grid-template-columns]="gridTemplateColumns()">
                    <div class="value-stream-axis">\u4E1A\u52A1\u57DF / \u4EF7\u503C\u6D41</div>
                    @for (column of columns(); track columnUid(column)) {
                      <div class="value-stream-header" data-testid="value-stream-header">
                        <strong>{{ column.name || columnUid(column) }}</strong>
                        @if (column.scope) { <span>{{ column.scope }}</span> }
                      </div>
                    }
                  </div>
                  <div class="value-stream-body">
                    @for (lane of lanes(); track laneUid(lane)) {
                      <div class="value-stream-row" data-testid="value-stream-row" [style.grid-template-columns]="gridTemplateColumns()">
                        <div class="value-stream-lane">
                          @if (lane.badge) { <span class="value-stream-lane-badge">{{ lane.badge }}</span> }
                          <strong>{{ lane.name || laneUid(lane) }}</strong>
                          @if (lane.note) { <span>{{ lane.note }}</span> }
                        </div>
                        @for (column of columns(); track columnUid(column)) {
                          <div class="value-stream-cell" [class.has-stages]="cellStages(laneUid(lane), columnUid(column)).length" [class.is-editing]="editing()" [attr.data-cell-id]="laneUid(lane) + '::' + columnUid(column)" [attr.data-lane-id]="laneUid(lane)" [attr.data-column-id]="columnUid(column)">
                            <div class="value-stream-stage-board" data-testid="stage-workbench-stage-board" [ngStyle]="stageBoardStyle(cellStages(laneUid(lane), columnUid(column)))">
                              @for (stage of cellStages(laneUid(lane), columnUid(column)); track stageId(stage); let stageIndex = $index) {
                                <div class="stage-graph-node stage-kind stage-matrix-stage" role="button" tabindex="0" data-testid="stage-graph-node" [attr.data-node-id]="stageId(stage)" [attr.data-flow-count]="adapter.processCount(stageId(stage))" [ngStyle]="stageStyle(stage, stageIndex)" (click)="openStage(stage)" (dblclick)="startStageRename(stage, $event)">
                                  @if (renamingStageId() === stageId(stage)) {
                                    <input class="stage-name-inline-input stage-graph-node-title" data-testid="stage-name-inline-input" [ngModel]="stage.name || stageId(stage)" (blur)="finishStageRename(stage, $any($event.target).value)" (keydown.enter)="finishStageRename(stage, $any($event.target).value)" />
                                  } @else {
                                    <span class="stage-graph-node-title">{{ stage.name || stageId(stage) }}</span>
                                  }
                                  @if (adapter.processCount(stageId(stage)) > 0) {
                                    <span class="stage-node-count">{{ adapter.processCount(stageId(stage)) }}</span>
                                  }
                                </div>
                              }
                            </div>
                          </div>
                        }
                      </div>
                    }
                  </div>
                </div>
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</div>
`, styles: ['@charset "UTF-8";\n\n/* src/app/workbenches/process/stage/process-stage-workbench.component.scss */\n:host {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-width: 0;\n  min-height: 0;\n}\n.stage-workbench {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-main {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-card {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-graph {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.value-stream-scroll-wrap {\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.value-stream-scroll {\n  flex: 1;\n  overflow: auto;\n  max-height: none;\n  min-height: 0;\n}\n.value-stream-matrix {\n  min-width: max-content;\n}\n.stage-flow-zoom-shell {\n  overflow: auto;\n  flex: 1;\n  min-height: 0;\n}\n.stage-workbench .stage-main-shell.workbench-scroll-body {\n  overflow: hidden;\n  display: flex;\n  flex-direction: column;\n  flex: 1;\n  min-height: 0;\n}\n.stage-detail-hint {\n  margin-left: auto;\n  color: #94a3b8;\n  font-size: 11px;\n  white-space: nowrap;\n}\n.stage-migrate-picker {\n  position: absolute;\n  z-index: 1260;\n  left: 50%;\n  bottom: calc(100% + 4px);\n  transform: translateX(-50%);\n  display: grid;\n  grid-template-columns: 140px minmax(180px, 1fr);\n  min-width: 320px;\n  border: 1px solid #bfdbfe;\n  border-radius: 10px;\n  background: #fff;\n  box-shadow: 0 18px 44px rgba(15, 23, 42, 0.18);\n}\n.stage-migrate-columns {\n  display: grid;\n  align-content: start;\n  gap: 4px;\n  max-height: 260px;\n  padding: 8px;\n  overflow-y: auto;\n  border-right: 1px solid #e2e8f0;\n  background: #f8fafc;\n}\n.stage-migrate-columns button {\n  min-height: 30px;\n  padding: 0 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 7px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n  font-weight: 800;\n  text-align: left;\n}\n.stage-migrate-columns button.active,\n.stage-migrate-columns button:hover {\n  border-color: #60a5fa;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.stage-migrate-stages {\n  display: grid;\n  align-content: start;\n  gap: 4px;\n  max-height: 260px;\n  padding: 8px;\n  overflow-y: auto;\n}\n.stage-migrate-stages button {\n  min-height: 30px;\n  padding: 0 8px;\n  border: 1px solid #dbeafe;\n  border-radius: 7px;\n  background: #fff;\n  color: #475569;\n  cursor: pointer;\n  font-size: 12px;\n  text-align: left;\n}\n.stage-migrate-stages button:hover {\n  border-color: #60a5fa;\n  background: #eff6ff;\n  color: #1d4ed8;\n}\n.stage-migrate-stages button.current {\n  color: #94a3b8;\n  font-style: italic;\n  pointer-events: none;\n}\n.stage-graph-node:has(.stage-migrate-picker),\n.stage-flow-node:has(.stage-migrate-picker) {\n  overflow: visible;\n  z-index: 1260;\n}\n.stage-flow-node-group-editor {\n  left: -20px;\n  top: 132px;\n  width: 104px;\n}\n.stage-flow-node-actions {\n  z-index: 2;\n}\n.stage-flow-link-remove {\n  z-index: 4;\n}\n/*# sourceMappingURL=process-stage-workbench.component.css.map */\n'] }]
  }], null, { exportGraphId: [{
    type: Input
  }], previewMode: [{
    type: Input
  }], previewStageId: [{
    type: Input
  }], processEditorRequested: [{
    type: Output
  }] });
})();
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && \u0275setClassDebugInfo(ProcessStageWorkbenchComponent, { className: "ProcessStageWorkbenchComponent", filePath: "src/app/workbenches/process/stage/process-stage-workbench.component.ts", lineNumber: 66 });
})();

export {
  Subscription,
  pipe,
  Observable,
  Subject,
  BehaviorSubject,
  EMPTY,
  from,
  of,
  throwError,
  isObservable,
  EmptyError,
  map,
  combineLatest,
  mergeMap,
  mergeAll,
  concat,
  defer,
  filter,
  catchError,
  concatMap,
  take,
  finalize,
  first,
  takeLast,
  startWith,
  switchMap,
  takeUntil,
  tap,
  RuntimeError,
  formatRuntimeError,
  ɵɵdefineInjectable,
  ɵɵdefineInjector,
  isInjectable,
  InjectionToken,
  isStandalone,
  ɵɵinject,
  inject2 as inject,
  ENVIRONMENT_INITIALIZER,
  makeEnvironmentProviders,
  EnvironmentInjector,
  runInInjectionContext,
  ɵɵrestoreView,
  ɵɵresetView,
  ɵɵnamespaceSVG,
  Injector,
  DOCUMENT,
  DestroyRef,
  PendingTasksInternal,
  EventEmitter,
  NgZone,
  INTERNAL_APPLICATION_ERROR_HANDLER,
  provideBrowserGlobalErrorListeners,
  signal,
  effect,
  Inject,
  ɵɵNgOnChangesFeature,
  ɵɵgetInheritedFactory,
  ɵɵinjectAttribute,
  Attribute,
  Injectable,
  ElementRef,
  ChangeDetectionStrategy,
  IS_HYDRATION_DOM_REUSE_ENABLED,
  IS_ENABLED_BLOCKING_INITIAL_NAVIGATION,
  ɵɵsanitizeHtml,
  ɵɵsanitizeUrl,
  ɵɵsanitizeUrlOrResourceUrl,
  ɵɵresolveWindow,
  ɵɵresolveDocument,
  ɵɵadvance,
  performanceMarkFeature,
  afterNextRender,
  Renderer2,
  isNgModule,
  ɵɵdirectiveInject,
  ɵɵinvalidFactory,
  ViewContainerRef,
  NgModuleFactory$1,
  createEnvironmentInjector,
  ɵɵdefineComponent,
  ɵɵdefineNgModule,
  ɵɵdefineDirective,
  ɵɵtemplate,
  ɵɵdomTemplate,
  setClassMetadataAsync,
  setClassMetadata,
  Console,
  publishExternalGlobalUtil,
  isPromise2 as isPromise,
  provideAppInitializer,
  APP_BOOTSTRAP_LISTENER,
  ApplicationRef,
  promiseWithResolvers,
  ɵɵdefer,
  ɵɵdeferWhen,
  ɵɵattribute,
  ɵɵconditionalCreate,
  ɵɵconditional,
  ɵɵrepeaterTrackByIndex,
  ɵɵrepeaterTrackByIdentity,
  ɵɵrepeaterCreate,
  ɵɵrepeater,
  ɵɵproperty,
  ɵɵelementStart,
  ɵɵelementEnd,
  ɵɵelement,
  ɵɵdomElementStart,
  ɵɵdomElementEnd,
  ɵɵdomElement,
  ɵɵelementContainerStart,
  ɵɵelementContainerEnd,
  ɵɵelementContainer,
  ɵɵgetCurrentView,
  ɵɵlistener,
  ɵɵdomListener,
  ɵɵnextContext,
  ɵɵcontentQuery,
  ɵɵviewQuery,
  ɵɵqueryRefresh,
  ɵɵloadQuery,
  ɵɵreference,
  ɵɵstyleProp,
  ɵɵclassProp,
  ɵɵclassMap,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵtextInterpolate2,
  ɵɵtextInterpolate3,
  ɵɵtextInterpolate4,
  ɵɵtwoWayProperty,
  ɵɵtwoWayBindingSet,
  ɵɵtwoWayListener,
  ɵɵdeclareLet,
  ɵɵstoreLet,
  ɵɵreadContextLet,
  ɵɵinterpolate1,
  ɵɵarrowFunction,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction2,
  ɵɵpureFunction3,
  ɵɵtemplateRefExtractor,
  ɵsetClassDebugInfo,
  Directive,
  Component,
  Input,
  Output,
  HostListener,
  NgModule,
  Compiler,
  untracked2 as untracked,
  computed,
  linkedSignal,
  HostAttributeToken,
  input,
  ContentChildren,
  ViewChildren,
  ViewChild,
  ChangeDetectorRef,
  booleanAttribute,
  reflectComponentType,
  PlatformLocation,
  LOCATION_INITIALIZED,
  LocationStrategy,
  PathLocationStrategy,
  Location,
  HashLocationStrategy,
  NgClass,
  NgForOf,
  NgIf,
  NgSwitch,
  NgSwitchCase,
  NgSwitchDefault,
  NgStyle,
  NgTemplateOutlet,
  CommonModule,
  PRECOMMIT_HANDLER_SUPPORTED,
  PlatformNavigation,
  ViewportScroller,
  bootstrapApplication,
  Title,
  DomSanitizer,
  CheckboxControlValueAccessor,
  DefaultValueAccessor,
  NgControlStatus,
  NgModel,
  SelectControlValueAccessor,
  NgSelectOption,
  ɵNgSelectMultipleOption,
  FormsModule,
  identityOf,
  normalizeDocument,
  getStageProcesses,
  getComponentSupportedStages,
  SAMPLE_DOCUMENT,
  getAngularRuntimeState,
  replaceRuntimeDocument,
  markAngularRuntimeModified,
  canUndoAngularRuntimeDocument,
  canRedoAngularRuntimeDocument,
  undoAngularRuntimeDocument,
  redoAngularRuntimeDocument,
  switchAngularMainTab,
  normalizeMainWorkbenchId,
  isAngularUtilityWorkbench,
  navigateAngularWorkbench,
  recordAngularNavigationBoundary,
  emitRuntimeRefresh,
  confirmRuntimeAction,
  canGoBackAngularNavigation,
  getAngularBackNavigationTitle,
  goBackAngularNavigation,
  goBackAngularUtilityWorkbench,
  ApiService,
  EntityDesignWorkbenchComponent,
  ensureValueDomainModel,
  ensureValueDomainStages,
  getValueDomainStageId,
  getValueDomainColumnUid,
  getValueDomainLaneUid,
  findOrCreateValueDomainCell,
  createProcessEditorLegacyAdapter,
  ProcessFlowWorkbenchComponent,
  ProcessStageWorkbenchComponent,
  exportGraphId,
  listExportGraphs
};
//# sourceMappingURL=chunk-NXZKD25Q.js.map
