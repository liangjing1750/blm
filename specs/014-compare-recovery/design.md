# 设计

## 落点

比对入口仍在 Shell 工具栏，结果弹窗属于 Shell 公共工具能力。差异计算先以小型纯函数落在 `ShellComponent` 内，后续恢复完整报告时再抽到 `core/document`。

## 数据流

1. 用户点击工具栏“比对”。
2. Shell 刷新工作区文档列表。
3. 用户选择右侧文档。
4. Shell 通过 `ApiService.load` 读取右侧文档。
5. 本地比较当前文档与右侧文档，渲染摘要。

## 验收

当前文档比旧文档多一个流程，并修改一个流程名称时，比对结果显示新增 1、修改 1，并展示对应流程名。
