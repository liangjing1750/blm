# 012 旧版返回能力恢复

## 目标

恢复旧版“返回”能力，让用户在工作台之间切换后，可以通过顶部返回按钮回到上一个位置。

## 旧版证据

旧版在 `app/app.js` 中维护 `S.ui.navHistory`，切换 UI 前记录快照，点击返回时弹出上一个不同快照并恢复。

## 本切片范围

- 记录 Angular 主工作台切换前的位置。
- 顶部返回按钮可用时显示上一位置标题。
- 点击返回恢复上一主工作台，并同步浏览器地址。

## 暂不处理

- 旧版完整 viewport 滚动恢复。
- 节点内锚点、抽屉折叠、图表缩放等细粒度 UI 快照。
