# Status

## 2026-06-29

### 本轮完成

- 按 BLM 五步法建立 `specs/009-v3-application-orchestration`。
- 固化三个已确认设计前提：
  - 构件任务首先是 BLM 抽象任务，用于表达可生成、可承接、可复用的业务能力。
  - 前端接口需求和应用服务暂不拆概念，统一叫“应用服务”。
  - 变量只存在于一次应用服务编排上下文中，不做全局变量。
- 明确流程节点、应用服务、应用编排、构件任务、技术承接和编排变量的边界。
- 用“提交入库预约”推演应用服务、构件任务和变量传递。
- 将后续开发拆为 T1 到 T7，先从模型兼容和构件任务技术承接开始，再进入应用服务、编排步骤、变量映射和流程节点引用。
- 完成 T1 文档模型兼容地基：
  - `BlmDocument` 增加 `businessConstructs` 和 `services`。
  - `TaskDefinition` 支持结构化输入输出参数和 `technicalHandover`。
  - `services[]` 支持应用服务基础字段、请求/响应参数和 `orchestration`。
  - 旧 `services[].taskDefinitionUids` 可规范化为兼容编排步骤。
  - 保留旧服务 `inputs/outputs/steps` 的兼容读取路径，供后续 UI 迁移承接。
- 完成 T2 构件任务技术承接编辑：
  - 构件工作台“任务定义”tab 的任务类型增加 V3 构件任务意图选项。
  - 任务编辑态增加“技术承接”区，支持运行承接类型、目标标识和备注。
  - 任务查看态展开后展示技术承接摘要。
  - 新增组件级集成测试覆盖查看、编辑、保存和 runtime 文档写回。
- 完成 T3 应用服务基础定义：
  - 应用服务工作台读取和编辑统一 `requestParams`、`responseParams` 字段。
  - 旧 `inputs`、`outputs` 仅作为兼容读取来源，保存时清理为统一字段。
  - 新建应用服务默认使用统一字段。
  - 新增集成测试覆盖已有服务请求/响应参数编辑和 runtime 文档写回。
- 完成 T4 应用服务编排步骤：
  - 应用编排 tab 读取和维护统一 `orchestration.steps`。
  - 旧 `steps` 仅作为兼容迁移来源，编辑后不再回写旧字段。
  - 新增步骤时生成稳定可读的 `uid` 和 `stepAlias`。
  - 编排步骤列表展示步骤别名，为后续 `step.<stepAlias>.*` 参数变量映射做准备。
- 完成 T5 参数变量映射：
  - 每个编排步骤支持维护 `inputMapping`，用于把 `request.*`、`context.*`、`step.<alias>.*`、`const.*` 等变量映射到构件任务输入。
  - 每个编排步骤支持维护 `outputMapping`，用于把任务输出绑定到 `step.<alias>.*` 变量。
  - 应用服务支持维护 `returnMapping`，用于把步骤变量或常量组装成返回字段。
  - 旧 `parameterMappings` 不再作为主路径显示，后续可按需做只读兼容提示。
- 完成 T6 流程节点引用应用服务：
  - 流程节点详情新增“应用服务”只读区域，可按 `services[].nodeRefs` 或节点侧 `serviceUids/serviceIds` 找到关联服务。
  - 节点侧只展示应用服务名称、HTTP 方法、路径和说明，方便产品经理回看节点与接入服务关系。
  - 参数映射、编排步骤和技术承接不在流程节点侧展开，继续留在应用编排工作台和构件工作台。
  - 新增集成测试覆盖有关联服务的流程节点展示，并确认不会暴露 `request.*` 映射和构件技术承接目标。
- 完成 T7 设计文档与用户手册同步：
  - `docs/BLM设计文档.md` 新增 V3 应用编排模型，说明流程节点、应用服务、应用编排、构件任务和技术承接边界。
  - `docs/BLM用户手册.md` 新增 V3 构件任务与应用编排使用路径，说明构件工作台、应用编排工作台和流程工作台各自职责。
  - `docs/BLM测试用例.md` 新增 2026-06 V3 应用编排回归点，覆盖模型兼容、技术承接、应用服务、编排步骤、参数映射和流程节点回看。

### 当前状态

- 当前已完成设计文档、T1 Angular 文档模型兼容实现、T2 构件任务技术承接编辑、T3 应用服务基础定义、T4 应用服务编排步骤、T5 参数变量映射、T6 流程节点引用应用服务和 T7 文档同步。
- 已修改 Angular 文档模型、规范化逻辑、模型测试、示例文档、构件工作台任务定义 UI、应用服务 UI、应用编排步骤/映射 UI、流程节点应用服务回看 UI 和组件集成测试。
- 已运行 Angular 构建，`app/` 构建产物已随构建更新。
- `specs/007-blm-v3-workbench-planning` 仍作为 V3 工作台边界规格，本规格作为其下游的应用编排专项规格。

### 下一步

- 等用户验收 T1/T2/T3/T4/T5/T6/T7。
- 下一轮如果继续深化，建议单独开规格讨论两个方向：一是应用服务与流程节点的双向选择/跳转体验，二是变量路径校验和可生成 JSON 的执行语义。

### 风险与备注

- 现有 `services[]` 已能表达应用服务基础信息；T1 已补 `orchestration` 空态和旧链路兼容，但 UI 尚未切到新结构。
- `taskDefinitions[]` 已扩展为构件任务承接结构，构件工作台已提供 `technicalHandover` 编辑入口；`services[]` 基础参数、编排步骤和参数映射已切到统一字段。当前变量路径只做文本录入和保存，尚未做根路径校验、表达式语言或执行语义。
- 第一版不支持复杂条件、循环、并行和补偿，后续如果流程应用平台确实需要，应在变量映射稳定后单独开规格。
- 技术承接当前是说明性结构，不是可执行运行时配置。
