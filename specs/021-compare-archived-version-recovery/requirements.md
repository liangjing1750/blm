# 需求梳理

## 背景

旧版比对支持选择工作区文档的当前版本、归档版本、历史/提交记录作为比对来源。Angular 当前只能将当前打开文档与另一个工作区文档的当前版本比对。

## 本轮范围

- 比对弹窗右侧支持选择“当前版本 / 归档版本”。
- 选择归档版本后，加载 `/api/versions/<name>` 的版本列表。
- 点击开始比对时，调用 `/api/version/load` 读取所选归档版本，再生成差异摘要。

## 暂不负责

- 远端历史快照和本地提交记录来源。
- 旧版完整分层比对报告视图。
- 左侧来源切换。
