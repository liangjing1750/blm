# 需求

- 比对报告业务差异表格恢复旧版每组最多展示 40 条的截断行为。
- 当某个业务分组超过 40 条时，表格只展示前 40 条。
- 超出时显示提示：本小节仅显示前 40 条，请优先确认高重要级别项目。
- 本切片不实现旧版排序权重，只按当前比对行顺序截断。

# 用户旅程

```gherkin
Feature: 比对业务表格截断

Scenario: 用户查看大量流程差异
  Given 比对结果中流程分组有 45 条差异
  When 用户执行版本比对
  Then 流程差异表格只显示 40 条
  And 页面提示本小节仅显示前 40 条
```
