# 013 预览导出恢复

## 目标

把新版 `预览/导出` 从占位状态恢复为可用入口，让用户在发版前至少可以预览当前文档结构，并导出可审计的 JSON/Markdown。

## 旧版证据

旧版 `app/app.js` 中存在预览路由和导出入口，导出支持后端 DOCX/ZIP 作业以及本地下载。

## 本切片范围

- 渲染当前文档标题和模型概要。
- 展示流程和实体清单。
- 支持导出当前 JSON。
- 支持导出 Markdown 摘要。

## 暂不处理

- DOCX 导出。
- ZIP 文档包导出。
- 附件打包。
- 后端导出任务轮询。
