# 需求梳理

## 背景

旧版预览/导出支持 ZIP 文档包导出，包含 JSON、Markdown 和附件目录。Angular 目前只提供浏览器本地 JSON/Markdown 导出，还没有接回后端文档包导出。

## 本轮范围

- 在预览工作台增加“导出 ZIP 文档包”按钮。
- 当前文档已保存时，请求 `/api/export-bundle/<name>`。
- 下载后端返回的 zip blob。

## 暂不负责

- DOCX 异步导出任务与进度轮询。
- 导出格式确认弹窗。
- 未保存文档的后端导出。
