# 需求梳理

## 背景

旧版比对支持将远端历史快照作为比对来源。Angular 当前已恢复右侧当前版本和归档版本比对，但历史快照来源仍缺失。

## 本轮范围

- 比对弹窗右侧来源增加“历史快照”。
- 选择历史快照时，加载 `/api/history/<name>` 的快照列表。
- 点击开始比对时，调用 `/api/history/load` 读取所选快照并生成差异摘要。

## 暂不负责

- 左侧来源切换。
- 本地提交记录来源。
- 旧版完整分层报告视图。
