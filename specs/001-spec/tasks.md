# Tasks

- [x] 建立实时协作 spec 上下文
- [x] 完成 requirements.md
- [x] 完成 design.md
- [x] S1 后端：实现最小 WebSocket 握手、join、presence、ping
- [x] S1 后端：实现 change ack、广播、JSONL 追加日志
- [x] S1 前端：连接协作会话并展示在线用户
- [x] S1 前端：实现低风险字段变更发送和远程应用
- [x] S1 前端：通过 `markModified()` 接入 3 秒准实时整文档快照同步，覆盖所有既有编辑操作
- [x] S1 前端：本地待同步时收到远程快照，阻止静默覆盖并提示用户处理
- [x] S1 测试：增加协议级单元/集成验证
- [x] S1 验收：两个浏览器打开同一文档，在线状态和变更广播可见
- [x] 更新 status.md

## 与最终目标的差距清单

- [x] 冲突提示：本地有待同步修改时收到远程快照，不能静默覆盖，需要提示“保留本地/应用远程/稍后处理”。
- [x] 自动落盘：协作会话中的最新工作稿会自动写回 `manifest.json`，且不生成普通历史快照。
- [x] 命名版本：已提供“归档版本”和历史记录入口，可给评审通过稿生成稳定只读快照。
- [x] 定位链接：已提供右键定位菜单，可复制当前视图、流程、节点、实体和版本链接。
- [x] 在线用户体验：已显示协作状态和在线人数，并提供“立即同步”入口。
- [ ] 断线重连：WebSocket 断开后尚未自动重连并按 `seq` 补齐远程变更。
- [ ] 权限与只读：还没有区分工作稿编辑、快照只读、版本只读。
- [x] 日志体积治理：协作日志按条数或体积自动压缩为最新 snapshot，避免保存点无限膨胀。

## 产品打磨任务

- [x] 删除自动保存时代的黄色未保存横幅，只保留轻量顶部状态。
- [x] 顶部增加“历史记录”入口，合并展示归档版本和近期历史。
- [x] 增加文档团队空间和标签元数据，并在打开弹窗按空间分组。
- [x] 增加右键定位点链接菜单，支持当前视图/流程/节点/实体/版本。
- [x] 协作日志按条数或体积压缩，减少自动保存保存点膨胀。
- [x] 用浏览器验证代表性流程六级模型元素协作同步。
