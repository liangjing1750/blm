# 状态

## 2026-06-30

已确认旧版 locator 机制使用查询参数传递文档、视图、流程、节点、实体和版本信息；新版 Angular 当前只恢复了历史版本链接复制，尚未实现启动时自动加载文档和流程/节点/实体级复制入口。

已补充 BDD/TDD 场景，覆盖当前节点复制链接和通过定位链接启动后恢复流程节点状态。实现上采用 Shell 级“复制链接”入口生成当前视图、流程、节点、实体链接，并在启动时解析 `doc`、`tab`、`proc`、`task`、`entity` 参数恢复运行时状态。实体设计工作台同步选中实体到 `runtime.ui.entityId`，保证实体链接来源稳定。
