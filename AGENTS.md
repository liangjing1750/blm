# 全局 Codex 偏好

- 默认用中文回复；命令、错误、API 名称和文件路径可以保留英文原文。
- 先读取相关文件和项目约定，再做修改或判断。
- 改动保持小而集中，不主动重构无关内容。
- 能验证就运行测试、构建或最小检查；不能验证时说明原因。
- 最终回复优先说明改了什么、验证结果、剩余风险。
- 如果被 Claude Code 或 `codex-plugin-cc` 调用，同一任务的后续指令尽量延续当前上下文，避免重复创建无关工作流。

# BLM 项目补充约定

- 中文优先：沟通、文档、提交信息默认使用中文；代码标识符、命令、API、错误信息保持英文更清晰时可用英文。
- 三级注释：新增代码按“模块意图、关键流程、边界细节”三层补充维护性注释，重点解释为什么这样组织、关键数据如何流动、哪些边界不能误改。
- 新代码面向人工维护：新增业务逻辑、适配器、复杂 UI 交互必须有必要注释；注释要解释约束和意图，避免只复述语句本身。
